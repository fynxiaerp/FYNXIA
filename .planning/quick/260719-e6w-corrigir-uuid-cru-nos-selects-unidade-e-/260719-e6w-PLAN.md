---
phase: quick-260719-e6w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/professionals/ProfessionalForm.tsx
autonomous: true
requirements: [QUICK-260719-e6w]

must_haves:
  truths:
    - "Campo 'Unidade' (aba Ficha) mostra o nome da unidade selecionada, não o UUID cru"
    - "Campo 'Login vinculado' (aba Ficha) mostra o full_name do usuário selecionado, não o UUID cru"
    - "Campo 'Login vinculado' mostra 'Sem login' quando nenhum usuário está vinculado (field.value null ou '__none__')"
    - "Ambos os Selects exibem o placeholder correto quando nada está selecionado"
  artifacts:
    - path: "src/components/professionals/ProfessionalForm.tsx"
      provides: "Dois SelectValue com children resolvendo id→nome via .find()"
      contains: "units.find"
  key_links:
    - from: "SelectValue (Unidade)"
      to: "units array"
      via: "units.find(u => u.id === field.value)?.name"
      pattern: "units\\.find"
    - from: "SelectValue (Login vinculado)"
      to: "dentistUsers array"
      via: "dentistUsers.find(u => u.id === field.value)?.full_name"
      pattern: "dentistUsers\\.find"
---

<objective>
Corrigir dois `<Select>` da aba "Ficha" do ProfessionalForm que exibem o UUID cru
selecionado em vez do nome legível.

Purpose: Confirmado ao vivo em produção (Playwright) — os campos "Unidade" e
"Login vinculado" mostram literalmente UUIDs (ex.: "94f67748-9797-4d4f-bf02-645a96bc98ee")
em vez dos nomes, prejudicando usabilidade do cadastro de profissionais (PRO-01/PRO-03).

Output: ProfessionalForm.tsx com ambos os `<SelectValue>` resolvendo id→nome,
mesmo padrão já aplicado hoje em AgendaCalendar.tsx (commit 4c7f2b5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/professionals/ProfessionalForm.tsx

<interfaces>
<!-- Contratos já presentes no arquivo — usar diretamente, sem exploração. -->

// Props do form (linhas 56-74)
export interface Unit { id: string; name: string }
export interface DentistUser { id: string; full_name: string }
// units: Unit[] e dentistUsers: DentistUser[] estão em escopo dentro de ProfessionalForm.

// Padrão de referência já corrigido em src/components/agenda/AgendaCalendar.tsx (commit 4c7f2b5):
//   <SelectValue placeholder="Selecionar dentista...">
//     {selectedDentistId
//       ? (dentists.find((d) => d.id === selectedDentistId)?.full_name ?? 'Selecionar dentista...')
//       : null}
//   </SelectValue>
// Quando o filho é null, o base-ui/shadcn Select cai para o placeholder.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolver id→nome nos SelectValue de "Unidade" e "Login vinculado"</name>
  <files>src/components/professionals/ProfessionalForm.tsx</files>
  <action>
Aplicar o mesmo padrão do commit 4c7f2b5 (AgendaCalendar.tsx): passar children ao
`<SelectValue>` que resolvem o id selecionado para o nome via `.find()`, com fallback
a `null` quando nada está selecionado (o que faz o Select exibir o placeholder).

1. Campo "Unidade" (~linha 450-452). Substituir:
   `<SelectValue placeholder="Selecione a unidade" />`
   por:
   ```tsx
   <SelectValue placeholder="Selecione a unidade">
     {field.value
       ? (units.find((u) => u.id === field.value)?.name ?? 'Selecione a unidade')
       : null}
   </SelectValue>
   ```

2. Campo "Login vinculado" (~linha 480-482). Preservar o caso especial `__none__`.
   Substituir:
   `<SelectValue placeholder="Sem login" />`
   por:
   ```tsx
   <SelectValue placeholder="Sem login">
     {field.value && field.value !== '__none__'
       ? (dentistUsers.find((u) => u.id === field.value)?.full_name ?? 'Sem login')
       : 'Sem login'}
   </SelectValue>
   ```
   Nota: o `value` do Select já é `field.value ?? '__none__'` (linha 477), portanto
   quando não há login vinculado deve mostrar "Sem login".

Não alterar nenhuma outra lógica: `onValueChange`, `value`, schema, defaultValues
permanecem intactos. Este é um fix puramente de apresentação do valor selecionado.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- `<SelectValue>` de "Unidade" tem children resolvendo `units.find(...)?.name`
- `<SelectValue>` de "Login vinculado" tem children resolvendo `dentistUsers.find(...)?.full_name` com "Sem login" no caso `__none__`/null
- `npx tsc --noEmit` passa sem novos erros
- Nenhuma outra lógica do form foi alterada
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpo (sem novos erros)
- Verificação visual (checkpoint pós-deploy, fora deste plano): em /clinica/profissionais/[id]
  aba Ficha, os campos "Unidade" e "Login vinculado" exibem nomes, não UUIDs;
  "Login vinculado" mostra "Sem login" quando não há vínculo
</verification>

<success_criteria>
- Ambos os campos exibem nomes legíveis em vez de UUIDs crus
- Placeholder / "Sem login" corretos quando nada selecionado
- TypeScript compila sem novos erros
</success_criteria>

<output>
After completion, create `.planning/quick/260719-e6w-corrigir-uuid-cru-nos-selects-unidade-e-/260719-e6w-SUMMARY.md`
</output>
