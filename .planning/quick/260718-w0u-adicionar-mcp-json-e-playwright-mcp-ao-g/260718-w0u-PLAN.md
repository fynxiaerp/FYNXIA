---
phase: quick
plan: 260718-w0u
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements: [git-hygiene, secret-protection]
must_haves:
  truths:
    - "git status no longer lists .mcp.json or .playwright-mcp/ as untracked (both ignored)"
    - ".vercelignore is tracked/committed"
    - "Phase 16/17 planning docs and the selectvalue-fix SUMMARY are committed"
    - "No VERCEL_TOKEN or .mcp.json content enters git history"
  artifacts:
    - path: .gitignore
      provides: "Ignore rules for .mcp.json and .playwright-mcp/"
      contains: ".mcp.json"
  key_links:
    - from: .gitignore
      to: .mcp.json
      via: "ignore rule matches untracked file"
      pattern: "\\.mcp\\.json"
---

<objective>
Local git hygiene: prevent the real VERCEL_TOKEN in `.mcp.json` and the local Playwright cache/logs in `.playwright-mcp/` from ever being committed, then commit already-produced documentation that reflects completed work.

Purpose: `.mcp.json` contains a real `VERCEL_TOKEN` in plaintext (confirmed untracked — the secret has NOT leaked into history, so a simple ignore rule fully protects it). `.playwright-mcp/` is local browser-test cache/log noise. Both must be ignored. Separately, several planning docs (pre-check #1 of DEPLOY-HANDOFF plus Phase 16/17 artifacts) are untracked/modified and should be committed so the repo reflects completed work.

Output: Updated `.gitignore`; one local commit containing `.gitignore`, `.vercelignore`, and the pending Phase 16/17 planning docs. NO git push. NO Vercel deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Confirmed git state (verified during planning):
- `.mcp.json` — untracked (`?? .mcp.json`); NOT in history. Contains real VERCEL_TOKEN.
- `.playwright-mcp/` — untracked directory of `console-*.log` / `page-*.yml` / `*.png` artifacts.
- `.vercelignore` — untracked (`?? .vercelignore`); DEPLOY-HANDOFF pre-check #1.
- `.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-UAT.md` — modified.
- `.planning/phases/17-estoque-materiais/17-02-PLAN.md` — modified.
- `.planning/phases/17-estoque-materiais/17-01-PLAN.md` — untracked.
- `.planning/phases/17-estoque-materiais/17-VALIDATION.md` — untracked.
- `.planning/quick/260629-ivj-criar-pagina-de-gerenciamento-de-fornece/selectvalue-fix-SUMMARY.md` — untracked.

Current `.gitignore` already handles `.env*`, `.vercel`, and `.claude/settings.local.json`. It does NOT yet mention `.mcp.json` or `.playwright-mcp/`.

OUT OF SCOPE (do not stage): `ARQUITETURA.docx`, `FYNXIA-ERP.md`, `.claude/`, `.mcp.json`, `.playwright-mcp/`, any code files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .mcp.json and .playwright-mcp/ ignore rules to .gitignore</name>
  <files>.gitignore</files>
  <action>
Append a new section at the end of `.gitignore` (after the existing "local scratch / temp" block) containing:

```
# MCP local config (contains real tokens/secrets — never commit)
.mcp.json

# Playwright MCP local cache/logs (browser-test artifacts)
.playwright-mcp/
```

Use the Edit tool to add these lines — do not rewrite unrelated parts of the file. The `.mcp.json` rule protects the plaintext VERCEL_TOKEN; the `.playwright-mcp/` rule silences local browser-test noise.
  </action>
  <verify>
    <automated>git check-ignore .mcp.json .playwright-mcp/ && git status --porcelain | grep -E '(\.mcp\.json|\.playwright-mcp)' && echo "FAIL: still listed" || echo "OK: ignored"</automated>
  </verify>
  <done>`git check-ignore .mcp.json .playwright-mcp/` prints both paths; `git status` no longer lists either as untracked.</done>
</task>

<task type="auto">
  <name>Task 2: Stage and commit .gitignore, .vercelignore, and pending Phase 16/17 planning docs (local commit only)</name>
  <files>.gitignore, .vercelignore, .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-UAT.md, .planning/phases/17-estoque-materiais/17-01-PLAN.md, .planning/phases/17-estoque-materiais/17-02-PLAN.md, .planning/phases/17-estoque-materiais/17-VALIDATION.md, .planning/quick/260629-ivj-criar-pagina-de-gerenciamento-de-fornece/selectvalue-fix-SUMMARY.md</files>
  <action>
Stage ONLY the explicit paths below — never use `git add -A` or `git add .` (that would sweep in ARQUITETURA.docx, FYNXIA-ERP.md, .claude/, etc.):

```
git add .gitignore \
        .vercelignore \
        .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-UAT.md \
        .planning/phases/17-estoque-materiais/17-01-PLAN.md \
        .planning/phases/17-estoque-materiais/17-02-PLAN.md \
        .planning/phases/17-estoque-materiais/17-VALIDATION.md \
        .planning/quick/260629-ivj-criar-pagina-de-gerenciamento-de-fornece/selectvalue-fix-SUMMARY.md
```

Before committing, run `git diff --cached --name-only` and confirm the staged set is EXACTLY those 7 paths — no `.mcp.json`, no `.playwright-mcp/`, no `.docx`, no `.claude/`. If anything extra appears, `git restore --staged <path>` it.

Then commit (local only — do NOT push):

```
git commit -m "chore(repo): ignore .mcp.json + .playwright-mcp, commit .vercelignore and pending phase 16/17 docs

Protect plaintext VERCEL_TOKEN in .mcp.json from ever being committed and
silence local Playwright MCP cache/logs. Commit .vercelignore (DEPLOY-HANDOFF
pre-check #1) and Phase 16/17 planning artifacts reflecting completed work.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

Do NOT run `git push` and do NOT run any `vercel` command.
  </action>
  <verify>
    <automated>git diff --cached --name-only | wc -l | grep -q '^0$' && echo "committed" && git show --stat --oneline HEAD | grep -qv '\.mcp\.json' && echo "OK: no secret committed"</automated>
  </verify>
  <done>A new local commit exists containing exactly the 7 intended paths; `git log --stat -1` shows no `.mcp.json` and no `.playwright-mcp/`; working tree no longer lists the committed docs; nothing was pushed.</done>
</task>

</tasks>

<verification>
- `git check-ignore .mcp.json .playwright-mcp/` returns both paths.
- `git log --stat -1` shows `.gitignore`, `.vercelignore`, and the 5 planning docs — and nothing else.
- `git status` is clean of the targeted files; `.mcp.json` / `.playwright-mcp/` no longer appear (ignored); out-of-scope files (ARQUITETURA.docx, FYNXIA-ERP.md, .claude/) remain untracked and unstaged.
- No remote was contacted (no push), no Vercel action ran.
</verification>

<success_criteria>
- `.gitignore` ignores `.mcp.json` and `.playwright-mcp/`; the plaintext VERCEL_TOKEN can never be committed.
- One local commit lands `.vercelignore` plus the Phase 16 UAT update, Phase 17 plans (17-01, 17-02), 17-VALIDATION, and the selectvalue-fix SUMMARY.
- No secret, no deploy, no push.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-w0u-adicionar-mcp-json-e-playwright-mcp-ao-g/260718-w0u-SUMMARY.md`
</output>
