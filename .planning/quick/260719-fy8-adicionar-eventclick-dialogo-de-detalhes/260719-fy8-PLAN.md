---
phase: quick-260719-fy8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/agenda/AgendaCalendar.tsx
autonomous: false
requirements: [QUICK-260719-fy8]
must_haves:
  truths:
    - "Clicar numa consulta existente no calendário abre um diálogo de detalhes"
    - "O diálogo mostra paciente, dentista (nome, não UUID), data/hora e o status atual"
    - "O usuário pode trocar o status para qualquer uma das 5 opções do enum e salvar"
    - "Salvar como 'concluido' dispara a auto-criação de OS (OS-01) via updateAppointment sem código novo"
    - "Após salvar, a cor do evento no calendário reflete o novo status sem recarregar a página"
    - "Erro do server action (ex.: conflito) aparece num Alert dentro do diálogo"
    - "O diálogo fecha após salvar com sucesso"
  artifacts:
    - path: "src/components/agenda/AgendaCalendar.tsx"
      provides: "handleEventClick + eventClick prop + componente AppointmentDetailDialog"
      contains: "eventClick"
  key_links:
    - from: "src/components/agenda/AgendaCalendar.tsx (AppointmentDetailDialog)"
      to: "updateAppointment (src/actions/appointments.ts)"
      via: "chamada com { status } no botão Salvar"
      pattern: "updateAppointment\\("
    - from: "FullCalendar eventClick"
      to: "handleEventClick → setDetailDialog"
      via: "abre o diálogo com dados do event clicado"
      pattern: "eventClick=\\{handleEventClick\\}"
---

<objective>
Adicionar um caminho de UI para ver e editar o status de uma consulta JÁ EXISTENTE na Agenda.

Hoje `/clinica/agenda` (AgendaCalendar.tsx) tem `select` (slot vazio → nova consulta) e `eventDrop` (arrastar → reagenda), mas NÃO tem `eventClick` — clicar num evento existente não faz nada. Não há outro caminho no produto para marcar uma consulta como `concluido` ou `cancelado`, o que trava o NPS pós-consulta (nps-scan) e o faturamento automático (OS-01) — ambos disparam só quando `appointments.status = 'concluido'`.

Purpose: destravar a transição de status clínico (`appointments.status`) via UI, reusando as server actions já prontas.
Output: `eventClick` + novo diálogo `AppointmentDetailDialog` (status-only) dentro de AgendaCalendar.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/agenda/AgendaCalendar.tsx
@src/actions/appointments.ts
@src/lib/validators/appointment.ts

<interfaces>
<!-- Contratos que o executor deve usar diretamente — sem explorar o codebase. -->

Server action já pronta (NÃO criar nova) — src/actions/appointments.ts (~linha 182):
```typescript
export async function updateAppointment(
  id: string,
  input: Partial<AppointmentInput>
): Promise<{ success: boolean; error?: string }>
```
- Já aceita `{ status }`, valida roles (admin/dentist/receptionist/superadmin),
  re-valida disponibilidade só quando horário/dentista mudam (aqui NÃO mudam),
  dispara auto-criação de OS rascunho quando `status === 'concluido'` (OS-01),
  e trata conflito 23P01. Este diálogo só envia `{ status }`.

Enum de status (src/lib/validators/appointment.ts) — 5 valores fixos:
```typescript
'agendado' | 'confirmado' | 'em_atendimento' | 'concluido' | 'cancelado'
```
Rótulos pt-BR: Agendado, Confirmado, Em Atendimento, Concluído, Cancelado.

CalendarEvent (src/lib/validators/appointment.ts):
```typescript
interface CalendarEvent {
  id: string; start: string; end: string;
  dentistId: string; status: string; title: string; patientName?: string;
}
```

Dados disponíveis no `eventClick(info)`:
- `info.event.id` → appointment id
- `info.event.title` → nome do paciente
- `info.event.start` / `info.event.end` → Date | null
- `info.event.extendedProps.status` → status atual (string)
- `info.event.extendedProps.dentistId` → dentist id (resolver nome via `dentists.find`)

Padrões JÁ existentes no mesmo arquivo a espelhar:
- `NewAppointmentDialog` (Dialog/DialogContent/DialogHeader/Alert/Select/Button já importados)
- Resolução id→nome do dentista: `dentists.find((d) => d.id === X)?.full_name` (usado no filtro de topo e no Select do NewAppointmentDialog)
- `handleEventCreated` / `handleEventDrop`: atualizam `calendarEvents` local (setCalendarEvents map/push) para refletir mudança sem reload
- `STATUS_CLASS_MAP` (linha ~50): mapeia status → classes de cor; `eventClassNames` já lê `extendedProps.status`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar eventClick + AppointmentDetailDialog (status-only) em AgendaCalendar.tsx</name>
  <files>src/components/agenda/AgendaCalendar.tsx</files>
  <action>
Editar SOMENTE src/components/agenda/AgendaCalendar.tsx. Não criar novo arquivo, não criar server action.

1. Importar o tipo `EventClickArg` de `@fullcalendar/core` (juntar ao import já existente: `import type { EventDropArg, DateSelectArg, EventClickArg } from '@fullcalendar/core'`).

2. Criar o componente `AppointmentDetailDialog` (mesmo arquivo, mesmo padrão visual de `NewAppointmentDialog`). Props:
   ```typescript
   interface AppointmentDetailDialogProps {
     open: boolean
     onClose: () => void
     appointmentId: string
     patientTitle: string
     dentistId: string
     startTime: string   // ISO
     endTime: string     // ISO
     currentStatus: string
     dentists: Dentist[]
     onStatusUpdated: (id: string, newStatus: string) => void
   }
   ```
   Corpo do diálogo:
   - `<DialogTitle>Detalhes da Consulta</DialogTitle>`.
   - Alert destrutivo no topo quando houver `error` (mesmo padrão do NewAppointmentDialog).
   - Campos read-only (texto simples, com `<Label className="font-semibold">`): Paciente (`patientTitle`), Dentista (resolver via `dentists.find((d) => d.id === dentistId)?.full_name ?? 'Não informado'` — NUNCA exibir o UUID cru), Data/Hora (formatar `startTime`/`endTime` com `new Date(...).toLocaleString('pt-BR', ...)` mostrando data + `HH:mm` início-fim).
   - Um `<Select>` de status inicializado com `useState(currentStatus)` e 5 `<SelectItem>` com os values do enum e rótulos pt-BR (Agendado, Confirmado, Em Atendimento, Concluído, Cancelado). Espelhar o padrão do Select de dentista: `<SelectValue>` com children resolvendo value→rótulo pt-BR via um mapa `STATUS_LABELS` (definir `const STATUS_LABELS: Record<string,string>` no módulo).
   - Botões: "Cancelar" (variant outline, chama onClose) e "Salvar" (chama handleSave, `disabled={isSubmitting}`, texto "Salvando…" enquanto submete).
   - `handleSave`: `setError(null)`; `setIsSubmitting(true)`; chamar `await updateAppointment(appointmentId, { status: selectedStatus })`; se `result.success` → `onStatusUpdated(appointmentId, selectedStatus)` e `onClose()`; senão → `setError(result.error ?? 'Erro ao atualizar status')`; `finally setIsSubmitting(false)`.
   - NÃO adicionar edição de data/hora/dentista aqui (esse fluxo já existe via drag-and-drop e NewAppointmentDialog). Só troca de status. NÃO duplicar nenhuma lógica de disponibilidade/conflito — tudo já está dentro de updateAppointment.

3. No componente `AgendaCalendar`, adicionar estado do diálogo de detalhes:
   ```typescript
   const [detailDialog, setDetailDialog] = useState<{
     open: boolean; appointmentId: string; patientTitle: string;
     dentistId: string; startTime: string; endTime: string; currentStatus: string
   } | null>(null)
   ```

4. Adicionar `handleEventClick` (useCallback) que lê `info.event` e faz `setDetailDialog({ open: true, appointmentId: info.event.id, patientTitle: info.event.title, dentistId: String(info.event.extendedProps.dentistId ?? ''), startTime: info.event.start?.toISOString() ?? '', endTime: info.event.end?.toISOString() ?? '', currentStatus: String(info.event.extendedProps.status ?? 'agendado') })`.

5. Adicionar `eventClick={handleEventClick}` ao `<FullCalendar>` (junto de `eventDrop`/`select`).

6. Adicionar `handleStatusUpdated(id, newStatus)`: `setCalendarEvents((prev) => prev.map((e) => e.id === id ? { ...e, status: newStatus } : e))` — igual ao padrão de handleEventDrop, para que `eventClassNames`/`STATUS_CLASS_MAP` recolore o evento sem reload.

7. Renderizar `<AppointmentDetailDialog>` condicionalmente (`detailDialog?.open && ...`) passando as props e `onClose={() => setDetailDialog(null)}`, `onStatusUpdated={handleStatusUpdated}`.

Restrições: reusar `updateAppointment` exatamente (NÃO usar cancelAppointment — o Select já cobre 'cancelado' via updateAppointment). NÃO criar rota/página nova. Não tocar em nenhum outro arquivo.
  </action>
  <verify>
    <automated>cd "c:/Users/ReinaldoLima/Desktop/Cowork/FYNXIA" && npx tsc --noEmit</automated>
  </verify>
  <done>
- `npx tsc --noEmit` passa sem erros.
- AgendaCalendar.tsx contém `eventClick={handleEventClick}`, o componente `AppointmentDetailDialog`, e `handleStatusUpdated` atualizando `calendarEvents`.
- O diálogo chama `updateAppointment(id, { status })` e não referencia lógica de conflito/disponibilidade.
- Nenhuma nova server action ou arquivo criado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>eventClick na Agenda abrindo um diálogo de detalhes (status-only) que grava via updateAppointment e recolore o evento sem reload.</what-built>
  <how-to-verify>
Em `/clinica/agenda` (dev local ou produção após deploy):
1. Clicar numa consulta existente no calendário → o diálogo "Detalhes da Consulta" abre mostrando paciente, dentista (NOME, não UUID), data/hora e o status atual selecionado.
2. Trocar o status para "Concluído" e clicar Salvar → diálogo fecha; o evento no calendário muda de cor (cinza/muted de concluído) sem recarregar a página.
3. (Opcional) Verificar que uma OS rascunho foi criada em /clinica/financeiro/faturamento/os para essa consulta (OS-01 disparado por updateAppointment).
4. Reabrir a mesma consulta → o status agora mostra "Concluído".
5. Trocar para "Cancelado" e salvar → evento fica vermelho/riscado (STATUS_CLASS_MAP cancelado).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpo.
- eventClick abre o diálogo; Salvar grava status via updateAppointment; cor do evento atualiza sem reload; erros aparecem em Alert; diálogo fecha após sucesso.
</verification>

<success_criteria>
- É possível, pela UI da Agenda, marcar uma consulta existente como Concluída ou Cancelada (destravando NPS pós-consulta e OS-01).
- Nenhuma server action nova criada; updateAppointment reusado com `{ status }`.
- Apenas src/components/agenda/AgendaCalendar.tsx modificado.
</success_criteria>

<output>
After completion, create `.planning/quick/260719-fy8-adicionar-eventclick-dialogo-de-detalhes/260719-fy8-SUMMARY.md`
</output>
