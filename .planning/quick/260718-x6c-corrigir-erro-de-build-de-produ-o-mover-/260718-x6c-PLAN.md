---
task: 260718-x6c
type: quick
wave: 1
depends_on: []
files_modified:
  - src/lib/validators/crc.ts
  - src/actions/referrals.ts
autonomous: true
must_haves:
  truths:
    - "Production build no longer fails with 'Only async functions are allowed to be exported in a use server file'"
    - "src/actions/referrals.ts exports only async functions (Server Action file constraint)"
    - "creditReferralReward still credits the same fixed reward amount (50.0) as before"
    - "listReferrals and listRewardsBalance import successfully in crc/indicacoes/page.tsx (no cascading module-compile failure)"
  artifacts:
    - path: "src/lib/validators/crc.ts"
      provides: "REFERRAL_REWARD_DEFAULT constant (moved from referrals.ts)"
      contains: "export const REFERRAL_REWARD_DEFAULT"
    - path: "src/actions/referrals.ts"
      provides: "Server Action file with only async exports; imports REFERRAL_REWARD_DEFAULT from validators/crc"
      contains: "import { REFERRAL_REWARD_DEFAULT"
  key_links:
    - from: "src/actions/referrals.ts"
      to: "src/lib/validators/crc.ts"
      via: "import { referralSchema, REFERRAL_REWARD_DEFAULT } from '@/lib/validators/crc'"
      pattern: "import \\{[^}]*REFERRAL_REWARD_DEFAULT[^}]*\\} from '@/lib/validators/crc'"
---

<objective>
Fix a production build break: `src/actions/referrals.ts` is a `'use server'` file that exports a non-async constant (`REFERRAL_REWARD_DEFAULT`), which violates Next.js's rule that only async functions may be exported from a `'use server'` module. This broke the Vercel/Turbopack build with a cascading failure ("Export listReferrals doesn't exist" / "Export listRewardsBalance doesn't exist" in `src/app/(dashboard)/clinica/crc/indicacoes/page.tsx`) because the whole module fails to compile.

Purpose: Restore a working production build by moving the constant to a non-`'use server'` module, following the exact precedent set by decision D-197 (AUTONOMY_LEVELS/AGENT_KEYS extracted from `ai-agent-config.ts` to `ai-agent-config-types.ts` for the identical reason).

Output: `REFERRAL_REWARD_DEFAULT` relocated to `src/lib/validators/crc.ts` (already imported by `referrals.ts` for `referralSchema` — zero new import statements needed, just one added named import), `referrals.ts` updated to consume it from there, and `npx tsc --noEmit` confirmed clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/actions/referrals.ts
@src/lib/validators/crc.ts
</context>

<interfaces>
Current `src/actions/referrals.ts` (line 13):
```typescript
export const REFERRAL_REWARD_DEFAULT = 50.0
```
Used at lines ~348, ~366, ~367, ~381 inside `creditReferralReward`.

Current `src/actions/referrals.ts` import (line 7):
```typescript
import { referralSchema } from '@/lib/validators/crc'
```

Current `src/lib/validators/crc.ts` — plain module, no `'use server'` directive, already exports `referralSchema` (line 91) alongside other CRC Zod schemas/constants (e.g. `LEAD_STAGES`, `isValidStageTransition`). This is the correct destination — same pattern as D-197's `ai-agent-config-types.ts`.

Confirmed via grep: no other file in the project imports `REFERRAL_REWARD_DEFAULT` from `referrals.ts` — safe to remove the local export entirely.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move REFERRAL_REWARD_DEFAULT to crc.ts and update referrals.ts</name>
  <files>src/lib/validators/crc.ts, src/actions/referrals.ts</files>
  <action>
  In `src/lib/validators/crc.ts`: add, near the "Referral program (CRC-05, D-16)" section (around line 89-95, next to `referralSchema`), the constant:
  ```typescript
  /**
   * D-17: recompensa por indicação — crédito em serviços, valor por indicação
   * configurável. v1 mantém um único valor documentado (constante), sem tabela
   * de configuração nova (18-04-PLAN interfaces) — configurabilidade real fica
   * para uma fase futura (ex.: cadastro por clínica).
   * Lives here (not in referrals.ts) because that file is a 'use server' module
   * and Next.js only allows async function exports from 'use server' files —
   * mirrors D-197 (ai-agent-config-types.ts extraction).
   */
  export const REFERRAL_REWARD_DEFAULT = 50.0
  ```
  Place it directly above or below `referralSchema` so the two related exports stay co-located.

  In `src/actions/referrals.ts`:
  1. Remove the local `export const REFERRAL_REWARD_DEFAULT = 50.0` declaration and its comment block (lines 9-13).
  2. Update the import on line 7 from `import { referralSchema } from '@/lib/validators/crc'` to `import { referralSchema, REFERRAL_REWARD_DEFAULT } from '@/lib/validators/crc'`.
  3. Leave every usage site of `REFERRAL_REWARD_DEFAULT` inside `creditReferralReward` (the CAS update, the ledger insert, the audit log details) completely untouched — only the declaration/import changes, not the call sites.
  4. Do not touch any other exports in the file (`linkReferral`, `listReferrals`, `listRewardsBalance`, `creditReferralReward` all remain async and exported exactly as-is).
  </action>
  <verify>
    <automated>cd "c:\Users\ReinaldoLima\Desktop\Cowork\FYNXIA" && npx tsc --noEmit</automated>
  </verify>
  <done>`src/actions/referrals.ts` no longer declares `REFERRAL_REWARD_DEFAULT` locally; it imports it from `@/lib/validators/crc`. `src/lib/validators/crc.ts` exports the constant. `npx tsc --noEmit` exits 0 with no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no non-async exports remain in referrals.ts and confirm build-relevant imports resolve</name>
  <files>src/actions/referrals.ts, src/app/(dashboard)/clinica/crc/indicacoes/page.tsx</files>
  <action>
  Re-read `src/actions/referrals.ts` top to bottom and confirm every top-level `export` is an `async function` (linkReferral, listReferrals, listRewardsBalance, creditReferralReward) — no `export const`, `export type` used to leak a non-function value is acceptable for `type`/`interface` exports since Next.js only restricts *value* exports to async functions, but as an extra safety check, verify `ReferralRow` and `RewardsBalanceRow` type exports do not trigger the same lint (type-only exports are erased at compile time and are exempt from the 'use server' restriction, unlike the `const` that caused this bug).

  Confirm `src/app/(dashboard)/clinica/crc/indicacoes/page.tsx` imports `listReferrals` and `listRewardsBalance` from `@/actions/referrals` (no changes needed there — this file was only a symptom of the cascading compile failure, not the cause; do not modify it unless `tsc` surfaces a real error in it).
  </action>
  <verify>
    <automated>cd "c:\Users\ReinaldoLima\Desktop\Cowork\FYNXIA" && npx tsc --noEmit</automated>
  </verify>
  <done>`npx tsc --noEmit` completes with exit code 0 and no errors referencing `src/actions/referrals.ts` or `src/app/(dashboard)/clinica/crc/indicacoes/page.tsx`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This is a pure refactor (constant relocation) with no new trust boundary, no new input parsing, and no behavior change to `creditReferralReward`'s security-sensitive logic (CAS guard, service-role client, server-side-only reward amount). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260718-x6c-01 | Tampering | REFERRAL_REWARD_DEFAULT relocation | accept | Constant remains server-only (crc.ts has no 'use client' or client-importing consumers in this change); value is still never accepted as a Server Action argument (T-18-13 preserved) — no new attack surface introduced by the move. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0 with no errors in `src/actions/referrals.ts`, `src/lib/validators/crc.ts`, or `src/app/(dashboard)/clinica/crc/indicacoes/page.tsx`.
2. `grep -n "export const REFERRAL_REWARD_DEFAULT" src/actions/referrals.ts` returns no match (constant removed from the 'use server' file).
3. `grep -n "REFERRAL_REWARD_DEFAULT" src/lib/validators/crc.ts` returns the new export.
4. `grep -n "REFERRAL_REWARD_DEFAULT" src/actions/referrals.ts` shows only the import line and the 4 usage sites inside `creditReferralReward` (unchanged call sites).
</verification>

<success_criteria>
- `npx tsc --noEmit` passes cleanly (no new errors vs. pre-fix baseline).
- `src/actions/referrals.ts` contains zero non-async top-level value exports.
- `REFERRAL_REWARD_DEFAULT` is defined exactly once, in `src/lib/validators/crc.ts`, with value `50.0` preserved.
- No other file's imports were touched (confirmed via grep — no other production file imported the constant from `referrals.ts`).
</success_criteria>

<output>
After completion, create `.planning/quick/260718-x6c-corrigir-erro-de-build-de-produ-o-mover-/260718-x6c-SUMMARY.md`
</output>
