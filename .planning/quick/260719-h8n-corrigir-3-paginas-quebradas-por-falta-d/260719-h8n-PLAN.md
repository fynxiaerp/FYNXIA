---
phase: quick-260719-h8n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/clinica/financeiro/faturamento/os/page.tsx
  - src/app/(dashboard)/clinica/financeiro/faturamento/glosas/page.tsx
  - src/app/(dashboard)/conformidade/auditoria/page.tsx
autonomous: false
requirements: [QUICK-260719-h8n]

must_haves:
  truths:
    - "Opening /clinica/financeiro/faturamento/os in production renders the OS list instead of the 'Algo deu errado' error screen"
    - "Opening /clinica/financeiro/faturamento/glosas in production renders the glosas list instead of the error screen"
    - "Opening /conformidade/auditoria in production renders the audit trail instead of the error screen"
    - "No '[nuqs] nuqs requires an adapter' console error appears on any of the 3 routes"
  artifacts:
    - path: "src/app/(dashboard)/clinica/financeiro/faturamento/os/page.tsx"
      provides: "OsPage returned JSX wrapped in NuqsAdapter"
      contains: "NuqsAdapter"
    - path: "src/app/(dashboard)/clinica/financeiro/faturamento/glosas/page.tsx"
      provides: "GlosasPage returned JSX wrapped in NuqsAdapter"
      contains: "NuqsAdapter"
    - path: "src/app/(dashboard)/conformidade/auditoria/page.tsx"
      provides: "AuditoriaPage main return wrapped in NuqsAdapter"
      contains: "NuqsAdapter"
  key_links:
    - from: "page.tsx (all 3)"
      to: "client component using useQueryState (OsListClient / GlosaListClient / AuditTrail)"
      via: "NuqsAdapter wrapping the JSX tree that contains the client component"
      pattern: "nuqs/adapters/next/app"
---

<objective>
Fix 3 production-breaking pages that render the "Algo deu errado" error screen because their client components call `useQueryState` from `nuqs` without a `<NuqsAdapter>` ancestor.

Purpose: Restore access to Ordens de Serviço, Tratamento de Glosas, and the Auditoria trail — all currently 100% broken in production (confirmed live via Playwright: console error `[nuqs] nuqs requires an adapter to work with your framework`).

Output: Each of the 3 `page.tsx` files imports `NuqsAdapter` from `'nuqs/adapters/next/app'` and wraps its returned JSX, exactly matching the pattern already used correctly by the other 11 nuqs pages (reference: `src/app/(dashboard)/clinica/agenda/page.tsx`). No client component or business logic is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Reference pattern — the ONLY correct way this project supplies the adapter (per-page, no global layout provides it):
@src/app/(dashboard)/clinica/agenda/page.tsx

# The 3 files to fix:
@src/app/(dashboard)/clinica/financeiro/faturamento/os/page.tsx
@src/app/(dashboard)/clinica/financeiro/faturamento/glosas/page.tsx
@src/app/(dashboard)/conformidade/auditoria/page.tsx

<interfaces>
<!-- The exact adapter import + wrapping pattern, copied from agenda/page.tsx (the confirmed-correct reference). -->

Import (top of file, alongside the other imports):
```typescript
import { NuqsAdapter } from 'nuqs/adapters/next/app'
```

Wrapping (agenda/page.tsx wraps its entire returned JSX):
```tsx
return (
  <NuqsAdapter>
    {/* the component's full existing return JSX, unchanged */}
  </NuqsAdapter>
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap all 3 pages' returned JSX in NuqsAdapter</name>
  <files>src/app/(dashboard)/clinica/financeiro/faturamento/os/page.tsx, src/app/(dashboard)/clinica/financeiro/faturamento/glosas/page.tsx, src/app/(dashboard)/conformidade/auditoria/page.tsx</files>
  <action>
Apply the identical mechanical fix to each of the 3 files. Do NOT edit any client component (`OsListClient.tsx`, `GlosaListClient.tsx`, `AuditTrail.tsx`) and do NOT change any business logic, data fetching, KPI math, or role gating.

For EACH file:

1. Add `import { NuqsAdapter } from 'nuqs/adapters/next/app'` at the top with the other imports.

2. Wrap the component's returned JSX in `<NuqsAdapter>...</NuqsAdapter>`. Currently each page returns a `<>...</>` fragment. Replace the outer fragment (or wrap the outer element) so the ENTIRE returned tree — including `<PageHeader>` and `<main>` — is inside `<NuqsAdapter>`. Match the abrangência used in `agenda/page.tsx`, which wraps its whole returned tree.

   Concretely, transform:
   ```tsx
   return (
     <>
       <PageHeader ... />
       <main ...>...</main>
     </>
   )
   ```
   into:
   ```tsx
   return (
     <NuqsAdapter>
       <PageHeader ... />
       <main ...>...</main>
     </NuqsAdapter>
   )
   ```
   (`NuqsAdapter` provides a single wrapping element, so the inner `<>...</>` fragment can be dropped, OR kept inside NuqsAdapter — either compiles; prefer dropping the now-redundant fragment for cleanliness.)

File-specific notes:

- **os/page.tsx**: single `return (<>...)`. Wrap the whole thing. `OsListClient` (uses `useQueryState`) is inside `<main>` — it will be covered.

- **glosas/page.tsx**: single `return (<>...)`. Wrap the whole thing. `GlosaListClient` is inside `<main>` — covered.

- **conformidade/auditoria/page.tsx**: this file has THREE `return` statements — two early-exit fragments (unauthenticated at ~line 45, and role-denied at ~line 68) and the MAIN return (~line 104) that renders `<AuditTrail>` (the component using `useQueryState`). Only the MAIN return needs `NuqsAdapter` — the two early-exit returns render no nuqs client component and MUST be left unchanged. Wrap only the final `return (<>...)` that contains `<AuditTrail ... />`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All 3 files import NuqsAdapter and their returned JSX (the AuditTrail-containing return for auditoria) is wrapped in `<NuqsAdapter>`. `npx tsc --noEmit` exits 0. No client component or business logic changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Added `<NuqsAdapter>` (from `nuqs/adapters/next/app`) around the returned JSX of the 3 previously-broken pages, matching the pattern already used by the other 11 nuqs pages. This is the only change — no client components or business logic were touched.

Before this checkpoint the executor must ensure the fix is live in production (build clean, push to origin, Vercel deploy `Ready`), since the nuqs adapter error only manifests at runtime in the browser and must be confirmed against production.
  </what-built>
  <how-to-verify>
For EACH of the 3 URLs below, open it in the browser (logged in), confirm the page content renders (not the "Algo deu errado" error screen), and open DevTools Console to confirm there is NO `[nuqs] nuqs requires an adapter to work with your framework` error:

1. https://fynxia.vercel.app/clinica/financeiro/faturamento/os
   — expect the OS list page (KPI cards + filter bar + table), no error.
2. https://fynxia.vercel.app/clinica/financeiro/faturamento/glosas
   — expect the Glosas page (3 KPI cards + filter bar + table), no error.
3. https://fynxia.vercel.app/conformidade/auditoria
   — expect the Audit trail (filters + rows), no error. (Log in as a permitted role: admin/superadmin/auditor/dpo.)

All 3 must render without the error screen AND without the nuqs console error.
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 render without the nuqs error, or describe which URL still fails and its console output.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (no type errors introduced).
- Production build succeeds and Vercel deploy is `Ready`.
- All 3 URLs render their real content with no `[nuqs] requires an adapter` console error.
- `git diff` shows changes ONLY in the 3 `page.tsx` files: one added import line + JSX wrapping per file. No client component files (`OsListClient.tsx`, `GlosaListClient.tsx`, `AuditTrail.tsx`) modified.
</verification>

<success_criteria>
- `/clinica/financeiro/faturamento/os`, `/clinica/financeiro/faturamento/glosas`, and `/conformidade/auditoria` all load without the "Algo deu errado" error screen in production.
- Zero nuqs-adapter console errors on those routes.
- No business logic, data fetching, or client component altered.
</success_criteria>

<output>
After completion, create `.planning/quick/260719-h8n-corrigir-3-paginas-quebradas-por-falta-d/260719-h8n-SUMMARY.md`
</output>
