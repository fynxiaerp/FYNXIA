---
phase: quick-260719-j2j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/clinica/agenda/page.tsx
  - src/components/agenda/AgendaCalendar.tsx
autonomous: true
requirements: [QUICK-260719-j2j]
must_haves:
  truths:
    - "Ao abrir 'Nova Consulta', o campo de paciente é um Select listando pacientes reais do tenant (não mais texto livre)"
    - "Selecionar um paciente e salvar grava appointments.patient_id com o UUID real desse paciente"
    - "Escolher 'Sem paciente vinculado' cria a consulta com patient_id nulo (comportamento opcional preservado)"
    - "O título do evento no calendário mostra o nome do paciente selecionado (fallback 'Novo Agendamento' quando nenhum)"
  artifacts:
    - path: "src/app/(dashboard)/clinica/agenda/page.tsx"
      provides: "Query de patients (id, full_name) do tenant, passada como prop para AgendaCalendar"
      contains: "from('patients')"
    - path: "src/components/agenda/AgendaCalendar.tsx"
      provides: "Prop patients propagada a NewAppointmentDialog; Select de paciente substituindo o Input de texto livre; patient_id enviado a createAppointment"
      contains: "patient_id"
  key_links:
    - from: "src/app/(dashboard)/clinica/agenda/page.tsx"
      to: "AgendaCalendar patients prop"
      via: "supabase.from('patients').select('id, full_name')"
      pattern: "from\\('patients'\\)"
    - from: "src/components/agenda/AgendaCalendar.tsx handleCreate"
      to: "createAppointment"
      via: "patient_id: selectedPatientId ?? undefined"
      pattern: "patient_id"
---

<objective>
Vincular um paciente real (`patient_id`) ao criar uma consulta na Agenda. Hoje o campo "Nome do Paciente (opcional)" em `NewAppointmentDialog` é texto livre usado só para o título local do evento — nunca chega a `createAppointment`, então `appointments.patient_id` fica sempre nulo. Isso bloqueia todo fluxo dependente de `patient_id` (ex.: cron de NPS em `src/lib/crc/nps-scan.ts` usa `patients!inner(...)`).

Purpose: Fechar o gap de produto confirmado ao vivo — a UI precisa coletar e enviar um `patient_id` real. Backend já suporta totalmente (Zod schema opcional + insert `patient_id: patient_id ?? null`).

Output: Select de paciente na criação de consulta, reusando EXATAMENTE o padrão "fetch all + client Select" já usado para dentistas nesta mesma tela.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Contratos existentes — o executor usa estes diretamente, sem explorar o codebase. -->

createAppointment JÁ ACEITA patient_id (src/actions/appointments.ts):
```typescript
// AppointmentInput (src/lib/validators/appointment.ts) — patient_id opcional/uuid:
export const appointmentSchema = z.object({
  dentist_id: z.string().uuid(...),
  patient_id: z.string().uuid('ID do paciente inválido').optional(),
  start_time: z.string().datetime(...),
  end_time: z.string().datetime(...),
  status: z.enum([...]).default('agendado'),
  notes: z.string().optional(),
  resource_id: z.string().uuid().optional(),
})
// createAppointment(input: AppointmentInput) — insere patient_id: patient_id ?? null. NÃO ALTERAR.
```

Padrão de fetch já usado para dentists em agenda/page.tsx (espelhar para patients):
```typescript
const { data: dentistRows } = tenantId
  ? await supabase.from('users').select('id, full_name')
      .eq('tenant_id', tenantId).eq('role', 'dentist').order('full_name', { ascending: true })
  : { data: [] }
const dentists = (dentistRows ?? []).map((d) => ({ id: d.id, full_name: d.full_name ?? 'Dentista' }))
```

Padrão de Select com resolução id→nome via .find() (já em AgendaCalendar.tsx, Select de dentista do dialog):
```tsx
<Select value={selectedDentistId ?? undefined} onValueChange={(v) => setSelectedDentistId(v)}>
  <SelectTrigger className="w-full">
    <SelectValue placeholder="Selecionar dentista...">
      {selectedDentistId ? (dentists.find((d) => d.id === selectedDentistId)?.full_name ?? 'Selecionar dentista...') : null}
    </SelectValue>
  </SelectTrigger>
  <SelectContent>
    {dentists.map((d) => (<SelectItem key={d.id} value={d.id}>{d.full_name}</SelectItem>))}
  </SelectContent>
</Select>
```

Padrão "__none__" para opção opcional (já em ProfessionalForm.tsx):
`onValueChange={(v) => setSelectedPatientId(v === '__none__' ? null : v)}` com value inicial `'__none__'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch de patients no Server Component e propagação da prop</name>
  <files>src/app/(dashboard)/clinica/agenda/page.tsx, src/components/agenda/AgendaCalendar.tsx</files>
  <action>
Em `src/app/(dashboard)/clinica/agenda/page.tsx`:
- Logo após o bloco de fetch de `dentists` (linha ~37), adicionar uma query IGUAL, mas na tabela `patients`, selecionando `id, full_name`, filtrando `.eq('tenant_id', tenantId)` e `.is('deleted_at', null)`, ordenado por `.order('full_name', { ascending: true })`. Mesmo guard ternário `tenantId ? ... : { data: [] }`.
- Mapear para `const patients = (patientRows ?? []).map((p) => ({ id: p.id, full_name: p.full_name ?? 'Paciente' }))`.
- Passar `patients={patients}` como nova prop no `<AgendaCalendar ... />`.

Em `src/components/agenda/AgendaCalendar.tsx`:
- Adicionar `interface Patient { id: string; full_name: string }` (mesmo shape de `Dentist`).
- Adicionar `patients: Patient[]` a `AgendaCalendarProps` e desestruturar `patients` em `export function AgendaCalendar({ dentists, patients, events: initialEvents, tenantId })`.
- Repassar `patients={patients}` ao `<NewAppointmentDialog ... />` (bloco ~611).

NÃO tocar em createAppointment/updateAppointment nem no Zod schema — patient_id já é suportado server-side (D-256 / task 260719-goi confirmam o insert). NÃO criar nova server action nem componente de autocomplete/busca assíncrona.
  </action>
  <verify>
    <automated>cd "c:/Users/ReinaldoLima/Desktop/Cowork/FYNXIA" && npx tsc --noEmit</automated>
  </verify>
  <done>page.tsx faz `from('patients').select('id, full_name')` filtrando tenant + deleted_at IS NULL e passa `patients` prop; AgendaCalendar aceita `patients: Patient[]` e repassa a NewAppointmentDialog; `tsc --noEmit` limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Substituir Input de texto livre por Select de paciente no NewAppointmentDialog</name>
  <files>src/components/agenda/AgendaCalendar.tsx</files>
  <action>
Em `NewAppointmentDialogProps` (interface ~123): adicionar `patients: Patient[]`. Desestruturar `patients` na assinatura de `NewAppointmentDialog` (~133).

Estado: substituir `const [patientName, setPatientName] = useState('')` por `const [selectedPatientId, setSelectedPatientId] = useState<string | null>(null)` (padrão inicial = sem paciente vinculado).

UI (bloco ~260-270): remover o `<Label htmlFor="patient-name">Nome do Paciente (opcional)</Label>` + `<Input id="patient-name" ...>` e colocar no lugar um `<Select>` seguindo EXATAMENTE o padrão do Select de dentista já presente neste mesmo componente:
- `value={selectedPatientId ?? '__none__'}`
- `onValueChange={(v) => setSelectedPatientId(v === '__none__' ? null : v)}`
- `<SelectTrigger className="w-full">` com `<SelectValue placeholder="Selecionar paciente...">` cujos children resolvem: `selectedPatientId ? (patients.find((p) => p.id === selectedPatientId)?.full_name ?? 'Selecionar paciente...') : 'Sem paciente vinculado'`
- `<SelectContent>`: primeiro `<SelectItem value="__none__">Sem paciente vinculado</SelectItem>`, depois `{patients.map((p) => (<SelectItem key={p.id} value={p.id}>{p.full_name}</SelectItem>))}`
- Manter o `<Label className="font-semibold">Paciente</Label>` acima.

Em `handleCreate` (~151): na chamada a `createAppointment({...})`, adicionar `patient_id: selectedPatientId ?? undefined` (omitido/undefined quando nulo — respeita o schema opcional). No `onCreated({...})`, trocar `title: patientName || 'Novo Agendamento'` por `title: (selectedPatientId ? patients.find((p) => p.id === selectedPatientId)?.full_name : null) || 'Novo Agendamento'`.

NÃO alterar a assinatura de createAppointment nem o Zod schema.
  </action>
  <verify>
    <automated>cd "c:/Users/ReinaldoLima/Desktop/Cowork/FYNXIA" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>O campo de texto livre foi removido; existe um Select de paciente com opção "Sem paciente vinculado" (value `__none__`) como padrão; `handleCreate` envia `patient_id: selectedPatientId ?? undefined` a createAppointment e usa o nome do paciente (via .find) como título com fallback 'Novo Agendamento'; `tsc --noEmit` e `npm run build` limpos.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpo (strict mode).
- `npm run build` conclui sem erros.
- Inspeção: nenhum `patientName`/`<Input id="patient-name">` remanescente em AgendaCalendar.tsx; `patient_id` presente na chamada a createAppointment.
- Manual (produção pós-deploy, fora do escopo de código): abrir "Nova Consulta" → Select lista pacientes reais + "Sem paciente vinculado"; selecionar paciente + salvar → evento com nome do paciente; verificar via trilha de auditoria/DB que `appointments.patient_id` foi gravado.
</verification>

<success_criteria>
- NewAppointmentDialog coleta um `patient_id` real via Select (padrão espelhado do Select de dentista).
- Consulta criada com paciente selecionado grava `appointments.patient_id`; opção "Sem paciente vinculado" mantém patient_id nulo.
- Zero alterações em createAppointment/updateAppointment/Zod schema; nenhuma nova server action ou componente de busca assíncrona.
- Build e typecheck limpos.
</success_criteria>

<output>
After completion, create `.planning/quick/260719-j2j-vincular-paciente-real-patient-id-ao-cri/260719-j2j-SUMMARY.md`
</output>
