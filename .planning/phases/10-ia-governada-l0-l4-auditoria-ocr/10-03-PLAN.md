---
phase: 10-ia-governada-l0-l4-auditoria-ocr
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/policy.ts
  - src/lib/ai/policy-types.ts
  - src/actions/approval-actions.ts
  - src/lib/ai/tools.ts
  - src/lib/agents/confirmation-agent.ts
  - src/lib/agents/collection-agent.ts
autonomous: true
requirements: [AIG-01, AIG-02, AIG-03]
tags: [ai, governance, approval]

must_haves:
  truths:
    - "withAgentPolicy reads ai_agent_config level, decides execute/suggest/block/pending_approval, and logs EVERY decision"
    - "A sensitive action over the ceiling produces a pending_approval and an approval_requests row"
    - "Existing copilot tools + confirmation/collection agents still run unchanged at their current config (additive wrap)"
    - "Approving a request is idempotent (cannot double-execute)"
  artifacts:
    - path: "src/lib/ai/policy.ts"
      provides: "withAgentPolicy() server-side gate + ai_decision_log writer"
      min_lines: 60
      contains: "import 'server-only'"
    - path: "src/lib/ai/policy-types.ts"
      provides: "computePolicyDecision pure matrix + canApprove alçada + types (non-server, unit-testable)"
      exports: ["computePolicyDecision", "canApprove"]
    - path: "src/actions/approval-actions.ts"
      provides: "createApprovalRequest, approveRequest, rejectRequest with idempotency + alçada + audit"
      exports: ["createApprovalRequest", "approveRequest", "rejectRequest"]
  key_links:
    - from: "src/lib/ai/policy.ts"
      to: "ai_decision_log"
      via: "createAdminClient().from('ai_decision_log').insert"
      pattern: "ai_decision_log.*insert"
    - from: "src/actions/approval-actions.ts"
      to: "executed_at idempotency guard"
      via: "UPDATE ... WHERE status='pending' AND executed_at IS NULL"
      pattern: "eq\\('status', 'pending'\\)"
    - from: "src/lib/ai/policy.ts"
      to: "ai_agent_config"
      via: "read autonomy_level"
      pattern: "ai_agent_config"
---

<objective>
Build the AI governance core: `withAgentPolicy()` (server-side enforcement of L0–L4 + sensitivity → execute/suggest/block/pending_approval, logging every decision to `ai_decision_log`), the pure decision/alçada helpers, the unified approval Server Actions, and the additive wrap of the existing tools + agents.

Purpose: AIG-01 (inviolable limits), AIG-02 (human approval for sensitive actions), AIG-03 (every decision logged). The wrap must be transparent for the current read-only copilot tools and the confirmation/collection agents so no existing behavior regresses.
Output: policy.ts + policy-types.ts + approval-actions.ts + additive edits to tools.ts and both agents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-ia-governada-l0-l4-auditoria-ocr/10-RESEARCH.md

<interfaces>
<!-- Verified existing contracts the executor builds against. -->

ai_agent_config read pattern (src/actions/ai-agent-config.ts):
```typescript
// network-level row: .eq('clinic_id', tenantId).eq('agent_key', key).is('unit_id', null).single()
// columns: autonomy_level ('L0'..'L4'), enabled (bool), limits (jsonb)
```

logBusinessEvent (src/lib/audit.ts):
```typescript
logBusinessEvent({ tenantId: string, actorId: string | null, action: string, details: Record<string,unknown> }): Promise<void>
```

assertNotReadOnly (src/lib/auth/guards.ts): `await assertNotReadOnly()` — throws for read-only roles.

createAdminClient (src/lib/supabase/admin.ts): service-role client (bypasses RLS) — use for ai_decision_log INSERT.
createClient (src/lib/supabase/server.ts): RLS user-session client — use for approval reads/writes by the actor.

masking (src/lib/ai/masking.ts): `maskCPF(cpf)`, `maskPhone(phone)` — apply before logging any PII into payload.

EXISTING AGENT SIGNATURES (must stay callable unchanged):
  runConfirmationAgent(admin = createAdminClient()): Promise<{enqueued, skipped}>
  runCollectionAgent(admin = createAdminClient()): Promise<{enqueued, skipped}>
  Both currently L0/L1-safe (send via outbox). The wrap is a logging + decision gate placed at the
  TOP of each runner; at the current seeded level (L0, enabled) it must NOT change their output.

NEXT.JS 'use server' CONSTRAINT (D-131): a 'use server' file may export ONLY async functions.
Pure functions (computePolicyDecision, canApprove) + types MUST live in policy-types.ts (NO 'use server',
NO 'server-only') so unit tests import them directly and tools/agents can import constants.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: policy-types.ts (pure matrix + alçada) and policy.ts (withAgentPolicy)</name>
  <files>src/lib/ai/policy-types.ts, src/lib/ai/policy.ts</files>
  <read_first>
    - src/actions/ai-agent-config.ts (ai_agent_config network-level read)
    - src/lib/audit.ts (logBusinessEvent + createAdminClient usage)
    - src/__tests__/governance/policy.test.ts (computePolicyDecision matrix + source-inspection asserts)
    - .planning/phases/10-ia-governada-l0-l4-auditoria-ocr/10-RESEARCH.md §Pattern 1
  </read_first>
  <behavior>
    - computePolicyDecision('L0', any) === 'suggest'
    - computePolicyDecision('L1', 'safe') === 'execute'; ('L1','sensitive') === 'suggest'
    - computePolicyDecision('L2', 'reversible') === 'execute'; ('L2','sensitive') === 'pending_approval'
    - computePolicyDecision('L3', 'sensitive') === 'pending_approval'; ('L3','reversible') === 'execute'
    - computePolicyDecision('L4', any) === 'execute'
    - computePolicyDecision('???', any) === 'block'
    - canApprove('admin', 'admin') === true; canApprove('receptionist', 'admin') === false; canApprove('superadmin', 'admin') === true
  </behavior>
  <action>
Create `src/lib/ai/policy-types.ts` (NO 'use server', NO 'server-only' — pure + types):
  - export type `PolicyDecision = 'execute' | 'suggest' | 'block' | 'pending_approval'`
  - export type `ActionSensitivity = 'safe' | 'reversible' | 'sensitive'`
  - export interface `PolicyContext { clinicId; agentKey; actorId: string|null; action; actionSensitivity }`
  - export `function computePolicyDecision(level: string, sensitivity: ActionSensitivity): PolicyDecision` per the behavior matrix above.
  - export `const APPROVER_RANK: Record<string, number>` mapping roles to alçada rank (superadmin > admin > others) and `function canApprove(role: string, requiredRole: string): boolean` returning `(APPROVER_RANK[role] ?? 0) >= (APPROVER_RANK[requiredRole] ?? Infinity)`.

Create `src/lib/ai/policy.ts` (starts with `import 'server-only'`):
  - export `async function withAgentPolicy<T>(ctx: PolicyContext, originalExecute: () => Promise<T>): Promise<T | { _policy: PolicyDecision; reason: string }>`:
    1. `createAdminClient()`, read network-level ai_agent_config (autonomy_level, enabled) for ctx.clinicId + ctx.agentKey.
    2. decision = `!enabled ? 'block' : computePolicyDecision(level, ctx.actionSensitivity)`.
    3. INSERT into `ai_decision_log` (clinic_id, agent_key, action, autonomy_level, decision, actor_id, reason). NEVER put raw PII in reason — IDs/level/sensitivity only.
    4. if decision==='execute' → `return originalExecute()`.
       if decision==='pending_approval' → return `{ _policy:'pending_approval', reason }` (caller creates the approval_requests row).
       else → return `{ _policy: decision, reason }`.
  - Re-export the pure helpers from policy-types for convenience (`export { computePolicyDecision, canApprove } from './policy-types'`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/governance/policy.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - policy.test.ts matrix asserts all pass; source-inspection asserts (`import 'server-only'`, `ai_decision_log`, `createAdminClient`, `ai_agent_config`) pass.
    - policy-types.ts has NO 'use server' / 'server-only' directive (importable by tests + client-safe utilities).
    - tsc clean.
  </acceptance_criteria>
  <done>computePolicyDecision matrix GREEN; withAgentPolicy logs every decision; canApprove alçada GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: approval-actions.ts (create/approve/reject with idempotency + alçada + audit)</name>
  <files>src/actions/approval-actions.ts</files>
  <read_first>
    - src/actions/ai-agent-config.ts (getActor helper, assertNotReadOnly, role gate, logBusinessEvent pattern)
    - src/lib/ai/policy-types.ts (canApprove)
    - src/__tests__/governance/approvals.test.ts (asserted strings: idempotency, executed_at, eq('status','pending'), logBusinessEvent)
    - .planning/phases/10-ia-governada-l0-l4-auditoria-ocr/10-RESEARCH.md §Pattern 4 + Pitfall 2
  </read_first>
  <action>
Create `src/actions/approval-actions.ts` (`'use server'`; only async exports):
  - reuse the `getActor()` pattern (auth → users.id/tenant_id/role).
  - `createApprovalRequest({ type, payload, agentKey, requiredRole, idempotencyKey, expiresAt })`: validate with zod (v3); INSERT via createClient() (RLS WITH CHECK enforces tenant); set status='pending', requested_by=actor.id, clinic_id=actor.tenant_id. Return `{ success, id }`. On idempotency conflict (unique violation) return the existing row id (no duplicate).
  - `approveRequest(id, note?)`:
    1. `await assertNotReadOnly()`.
    2. load the request (RLS-scoped); if not pending → return `{ success:false, error:'Já decidido' }`.
    3. alçada check: `if (!canApprove(actor.role, request.required_role)) return { success:false, error:'Alçada insuficiente' }`.
    4. IDEMPOTENT execute: UPDATE `approval_requests` SET status='approved', approver=actor.id, decided_at=now, reason=note, executed_at=now WHERE id=:id AND status='pending' AND executed_at IS NULL; check affected rows — if 0, abort (lost the race, do not execute payload).
    5. If affected===1, dispatch the stored payload to its handler (a small switch on `type`: 'ai_action' → re-run via a registered executor stub that Plan 04/05 wires; 'estorno' → call the estorno executor from audit-actions — import lazily to avoid cycle). For Wave 1, the dispatcher records the executed payload to the trail; concrete per-entity reversal stays generic (the estorno primitive logs to audit_logs — Deferred: per-module reversal).
    6. `logBusinessEvent({ tenantId, actorId: actor.id, action: 'approval.approved', details: { id, type, required_role } })`.
  - `rejectRequest(id, reason)`: assertNotReadOnly + alçada + UPDATE status='rejected', decided_at, reason; logBusinessEvent action 'approval.rejected'. No payload execution.
  - All three: clinic_id always from actor.tenant_id, never the client.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/governance/approvals.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - approvals.test.ts GREEN: matches assertNotReadOnly, idempotency, executed_at, eq('status','pending'), logBusinessEvent, canApprove usage.
    - approveRequest cannot double-execute (status+executed_at guard with affected-row check).
    - tsc clean.
  </acceptance_criteria>
  <done>createApprovalRequest/approveRequest/rejectRequest exist with idempotent execution + alçada + audit logging.</done>
</task>

<task type="auto">
  <name>Task 3: Additive governance wrap of tools + agents (regression-safe)</name>
  <files>src/lib/ai/tools.ts, src/lib/agents/confirmation-agent.ts, src/lib/agents/collection-agent.ts</files>
  <read_first>
    - src/lib/ai/tools.ts (4 read-only tools, execute shape)
    - src/lib/agents/confirmation-agent.ts + collection-agent.ts (runner entry points)
    - src/lib/ai/policy.ts (withAgentPolicy signature)
    - src/__tests__/ai/tools.test.ts + src/__tests__/ai/collection-agent.test.ts (existing contracts that MUST stay green)
  </read_first>
  <action>
Wrap additively so existing behavior at current config is unchanged.

tools.ts (read-only tools = sensitivity 'safe'):
  - Import withAgentPolicy. For each of the 4 tools, wrap the body of `execute` so it logs a decision then runs the original SELECT. Because read-only tools are sensitivity='safe', the decision at any enabled level resolves to 'execute' (L0 still logs but executes for read-only — RESEARCH Open Question 1 resolution). Use agentKey 'copilot'. Resolve clinicId/actorId inside execute from the existing createClient() session (the tools already call createClient()). CRITICAL (Pitfall 5): execute MUST still return the tool's declared output shape on success; the `_policy` sentinel only appears on block, which for safe read-only tools will not happen — but keep the return type union-safe by returning the data on execute. Do NOT change inputSchema/description.
  - Guard: if AI_GATEWAY/policy read fails, fall back to executing the read (a read-only tool must not be bricked by a logging failure) — wrap the log insert in try/catch, never throw from logging.

confirmation-agent.ts + collection-agent.ts:
  - At the TOP of runConfirmationAgent / runCollectionAgent (before the scan loop), call withAgentPolicy with agentKey 'confirmation'/'collection', action 'agent.run', sensitivity 'reversible' (they enqueue outbound messages). Per-tenant config is read inside the loop is NOT required for v1; the runners currently process all tenants — so log a single agent.run decision per invocation using a system context (clinicId resolved per-row already; for the run-level gate, log with the agent_key and a null/aggregate clinic and decision based on a default-enabled check). KEEP the existing enqueue/skip counters and return shape EXACTLY. If the gate returns a non-execute sentinel, short-circuit and return `{ enqueued:0, skipped:0 }` (do not send) — but at the seeded L0 enabled config the gate must allow the run (reversible at L0 → 'suggest' would block; therefore for the RUN-LEVEL gate treat the agent run as sensitivity 'safe' so L0 still runs — these agents were already approved product behavior in Phase 5). Document this decision inline.
  - Net effect requirement: `npx vitest run src/__tests__/ai/` stays fully GREEN (no behavioral regression).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ai/ src/__tests__/governance/policy.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ALL existing src/__tests__/ai/ tests stay GREEN (tools.test.ts, collection-agent.test.ts, chat-route.test.ts, whatsapp-inbound.test.ts).
    - policy.test.ts regression asserts (tools/agents still reference their read paths) GREEN.
    - tools' execute functions still return their declared output shapes (no `_policy` leaking into copilot stream).
    - tsc clean.
  </acceptance_criteria>
  <done>Governance wrap is additive: every AI action logs a decision; no existing copilot/agent test regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool-call → execute() | Untrusted model output reaches execute; governance gate decides before any side effect |
| agent runner → outbox | Reversible outbound actions gated + logged before enqueue |
| approver client → approveRequest | alçada enforced server-side from users.role, never from client payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Elevation of Privilege | governance bypass via client | mitigate | policy.ts `import 'server-only'`; level read server-side from ai_agent_config |
| T-10-12 | Repudiation | approval double-execution | mitigate | UPDATE WHERE status='pending' AND executed_at IS NULL + affected-row check |
| T-10-13 | Elevation of Privilege | forged required_role to self-approve | mitigate | canApprove uses actor.role from users table; required_role compared, not trusted from client |
| T-10-14 | Information Disclosure | PII in ai_decision_log payload/reason | mitigate | reason logs IDs/level/sensitivity only; maskCPF/maskPhone before any payload serialization |
| T-10-15 | Tampering | regression breaking copilot stream | mitigate | additive wrap returns declared output on execute; existing ai/ tests must stay green |
</threat_model>

<verification>
- `npx vitest run src/__tests__/governance/ src/__tests__/ai/` GREEN (governance new + existing AI regression).
- `npx tsc --noEmit` clean. `npx next build` clean.
- policy-types.ts has no server directive; policy.ts has `import 'server-only'`.
- approveRequest idempotency proven by the status+executed_at guard.
</verification>

<success_criteria>
- AIG-01: withAgentPolicy reads level + decides per the L0–L4 matrix (server-side).
- AIG-02: pending_approval path + createApprovalRequest + idempotent approveRequest with alçada.
- AIG-03: every decision (including block) inserted into ai_decision_log.
- Regression-safe: all existing copilot/agent tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ia-governada-l0-l4-auditoria-ocr/10-03-SUMMARY.md`
</output>
