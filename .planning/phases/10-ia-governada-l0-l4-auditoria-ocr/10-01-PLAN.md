---
phase: 10-ia-governada-l0-l4-auditoria-ocr
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/migrations/phase10.test.ts
  - src/__tests__/governance/policy.test.ts
  - src/__tests__/governance/approvals.test.ts
  - src/__tests__/audit/audit-ui.test.ts
  - src/__tests__/audit/estorno.test.ts
  - src/__tests__/ocr/extract.test.ts
autonomous: true
requirements: [AIG-01, AIG-02, AIG-03, AUD-01, AUD-02, AUD-03, OCR-01, OCR-02]
tags: [ai, governance, audit, ocr, testing]

must_haves:
  truths:
    - "Running the phase 10 test files fails with clear RED errors (missing migrations/modules), not import crashes"
    - "Every Phase 10 requirement (AIG/AUD/OCR) has at least one assertion in a scaffold"
  artifacts:
    - path: "src/__tests__/migrations/phase10.test.ts"
      provides: "Source-inspection assertions for the 3 new tables + audit_logs indexes"
    - path: "src/__tests__/governance/policy.test.ts"
      provides: "computePolicyDecision matrix unit tests + withAgentPolicy source-inspection"
    - path: "src/__tests__/governance/approvals.test.ts"
      provides: "approval state machine + idempotency + conformidade proxy module assertions"
    - path: "src/__tests__/audit/audit-ui.test.ts"
      provides: "audit query lib + audit page source-inspection"
    - path: "src/__tests__/audit/estorno.test.ts"
      provides: "estorno action alçada + approval routing assertions"
    - path: "src/__tests__/ocr/extract.test.ts"
      provides: "OCR route ZDR + FilePart + confidence threshold gating (mocked model)"
  key_links:
    - from: "test files"
      to: "supabase/migrations + src/lib/ai/policy.ts + src/actions/* + src/app/conformidade/*"
      via: "readFileSync source-inspection + dynamic import with existsSync guard"
      pattern: "readFileSync|existsSync|toMatch"
---

<objective>
Create the Wave 0 RED test scaffolds for all of Phase 10 — three governance subsystems (IA governada, auditoria/estorno, OCR). These tests assert the artifacts that Waves 1–4 will produce; they are RED now (target files do not exist) and turn GREEN incrementally as each plan lands.

Purpose: Establish the verification contract before implementation (Nyquist). Every AIG/AUD/OCR requirement gets at least one automated assertion, and the test style is locked to the source-inspection + pure-unit + mocked-model convention used in Phases 5–9.
Output: 6 test files under `src/__tests__/{migrations,governance,audit,ocr}/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ia-governada-l0-l4-auditoria-ocr/10-RESEARCH.md
@.planning/phases/10-ia-governada-l0-l4-auditoria-ocr/10-VALIDATION.md

<interfaces>
<!-- Established test conventions the executor MUST mirror. No exploration needed. -->

Source-inspection helper (from src/__tests__/migrations/phase8.test.ts):
```typescript
import { readFileSync, readdirSync, existsSync } from 'node:fs'
import { join, resolve } from 'node:path'
const MIGRATIONS_DIR = join(process.cwd(), 'supabase/migrations')
function M(suffix: string): string {
  const files = readdirSync(MIGRATIONS_DIR)
  const match = files.find(f => f.endsWith(suffix))
  if (!match) throw new Error(`Migration ending '${suffix}' not found`)
  return readFileSync(join(MIGRATIONS_DIR, match), 'utf8')
}
// SRC(relPath): read a source file, return '' if missing (assert on content, not ENOENT)
function SRC(rel: string): string {
  const p = resolve(process.cwd(), rel)
  return existsSync(p) ? readFileSync(p, 'utf8') : ''
}
```

server-only mock pattern (from connectors.test.ts):
```typescript
import { vi } from 'vitest'
vi.mock('server-only', () => ({}))
```

Phase 10 target migration suffixes (Plan 02 will create these):
- `_ai_decision_log.sql`, `_approval_requests.sql`, `_ocr_extractions.sql`, `_audit_logs_indexes.sql`

Phase 10 target source files:
- `src/lib/ai/policy.ts` (withAgentPolicy, computePolicyDecision)
- `src/actions/approval-actions.ts`, `src/actions/audit-actions.ts`, `src/actions/ocr-actions.ts`
- `src/app/api/ocr/route.ts`
- `src/app/(dashboard)/conformidade/{aprovacoes,auditoria,ocr}/page.tsx`
- proxy.ts gains a `conformidade` ModuleKey

Mocked-model pattern for OCR unit tests — extract the PURE threshold function so it
can be unit-tested WITHOUT importing the route (route imports `ai`/`server-only`):
the route MUST delegate to an exported pure helper `needsReview(fields, threshold)` in
`src/lib/ai/ocr-confidence.ts` so extract.test.ts can import it directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + governance scaffolds (phase10.test.ts, policy.test.ts, approvals.test.ts)</name>
  <files>src/__tests__/migrations/phase10.test.ts, src/__tests__/governance/policy.test.ts, src/__tests__/governance/approvals.test.ts</files>
  <read_first>
    - src/__tests__/migrations/phase8.test.ts (M() helper + describe/it structure)
    - src/__tests__/integrations/connectors.test.ts (server-only mock + SRC pattern)
    - .planning/phases/10-ia-governada-l0-l4-auditoria-ocr/10-RESEARCH.md §Database Schema Design (exact columns/indexes to assert)
    - src/proxy.ts (MODULE_PERMISSIONS shape for conformidade assertion)
  </read_first>
  <action>
Create `src/__tests__/migrations/phase10.test.ts` (source-inspection on supabase/migrations via M(suffix)):
  - `_ai_decision_log.sql`: asserts `CREATE TABLE public.ai_decision_log`, `clinic_id`, `agent_key`, `autonomy_level`, `decision` with CHECK `IN ('execute','suggest','block','pending_approval')`, `idx_ai_decision_log_clinic`, `ENABLE ROW LEVEL SECURITY`, a SELECT policy `clinic_id = get_my_tenant_id()`, and NO `FOR INSERT`/`FOR UPDATE`/`FOR DELETE` policy (immutable — assert the SQL does NOT match `/FOR (INSERT|UPDATE|DELETE)/i`). (AIG-03)
  - `_approval_requests.sql`: asserts `CREATE TABLE public.approval_requests`, `type` CHECK `IN ('ai_action', 'estorno')`, `payload JSONB`, `required_role`, `status` CHECK `IN ('pending', 'approved', 'rejected', 'expired')`, `idempotency_key`, `executed_at`, a UNIQUE index on `(clinic_id, idempotency_key)` with `WHERE idempotency_key IS NOT NULL`, `idx_approval_requests_status`, USING + WITH CHECK on the write policy. (AIG-02, AUD-02)
  - `_ocr_extractions.sql`: asserts `CREATE TABLE public.ocr_extractions`, `extracted_fields JSONB`, `min_confidence`, `status` CHECK including `'pending_review'`, `reviewed_by`, `target_table`, `target_id`, `deleted_at` (soft delete — LGPD), `idx_ocr_extractions_status`, USING + WITH CHECK. (OCR-02)
  - `_audit_logs_indexes.sql`: asserts `idx_audit_logs_table_name` on `(tenant_id, table_name)` AND `idx_audit_logs_record_id` on `(tenant_id, table_name, record_id)`, both `IF NOT EXISTS`. (AUD-01/03) Also assert forward partitions are guarded with `IF NOT EXISTS` (note in a comment: 2026-07/08/09 already exist from earlier migrations).

Create `src/__tests__/governance/policy.test.ts`:
  - `vi.mock('server-only', () => ({}))` then dynamic-import `computePolicyDecision` from `@/lib/ai/policy` via existsSync guard (skip the unit asserts cleanly if the module is absent — RED until Plan 03). Use the absolute-path dynamic import gotcha (D-144): `const p = resolve(process.cwd(),'src/lib/ai/policy.ts')` guard.
  - PURE unit asserts on `computePolicyDecision(level, sensitivity)`: L0→'suggest'; L1+'safe'→'execute'; L1+'sensitive'→'suggest'; L2+'reversible'→'execute'; L2+'sensitive'→'pending_approval'; L3+'sensitive'→'pending_approval'; L4+anything→'execute'; unknown level→'block'.
  - SRC source-inspection on `src/lib/ai/policy.ts`: matches `import 'server-only'`, `ai_decision_log`, `createAdminClient`, and reads `ai_agent_config`.
  - SRC on `src/lib/ai/tools.ts` + `src/lib/agents/confirmation-agent.ts` + `src/lib/agents/collection-agent.ts`: assert they STILL reference their existing read paths (regression guard — files exist now, will assert the wrap is additive in Plan 03).

Create `src/__tests__/governance/approvals.test.ts`:
  - SRC on `src/actions/approval-actions.ts`: matches `assertNotReadOnly`, `idempotency`, `executed_at`, a status guard `eq('status', 'pending')`, and `logBusinessEvent` (approve/reject logged to trail — AUD-02).
  - PURE unit on an exported `canApprove(role, requiredRole)` helper (alçada check) from approval-actions or a types file: admin can approve required_role='admin'; receptionist cannot approve required_role='admin'.
  - SRC on `src/proxy.ts`: assert a `conformidade` ModuleKey exists, that `auditor` and `dpo` have `conformidade: { allowed: true, readOnly: true }`, that `admin` has `conformidade: { allowed: true }`, and that `ROUTE_MODULE_MAP` includes `{ prefix: '/conformidade', module: 'conformidade' }`. (AUD-03)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/migrations/phase10.test.ts src/__tests__/governance/policy.test.ts src/__tests__/governance/approvals.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and the suite RUNS (no import/parse crash) — assertions fail RED because target artifacts don't exist yet, which is the intended Wave 0 state.
    - `npx tsc --noEmit` stays clean (existsSync/SRC guards prevent TS2307 on absent modules).
  </acceptance_criteria>
  <done>phase10.test.ts, policy.test.ts, approvals.test.ts exist; RED for missing artifacts; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Audit + OCR scaffolds (audit-ui.test.ts, estorno.test.ts, extract.test.ts)</name>
  <files>src/__tests__/audit/audit-ui.test.ts, src/__tests__/audit/estorno.test.ts, src/__tests__/ocr/extract.test.ts</files>
  <read_first>
    - src/lib/audit.ts (logBusinessEvent shape)
    - src/app/api/copilot/route.ts (ZDR + generateObject/streamText + FilePart reference pattern)
    - .planning/phases/10-ia-governata-l0-l4-auditoria-ocr/10-RESEARCH.md §Pattern 2 (OCR) + §Pattern 5 (audit RSC)
  </read_first>
  <action>
Create `src/__tests__/audit/audit-ui.test.ts`:
  - SRC on `src/actions/audit-actions.ts`: matches `audit_logs`, a `table_name` filter, `actor_id` filter, `created_at` range (`gte`/`lte`), `order('created_at'`, and pagination (`range(`). (AUD-01/03)
  - SRC on `src/app/(dashboard)/conformidade/auditoria/page.tsx`: matches it is a Server Component (no `'use client'` at top), references `queryAuditLogs`/`audit-actions`, and renders a before/after diff (matches `old_values` AND `new_values`). (AUD-03)

Create `src/__tests__/audit/estorno.test.ts`:
  - SRC on `src/actions/audit-actions.ts` (or a dedicated estorno export): matches `createEstorno`, requires a `motivo`/`reason` (assert it references `reason`), creates an `approval_requests` row with `type: 'estorno'`, sets `required_role` (alçada), and `assertNotReadOnly`. (AUD-02)
  - PURE unit on the alçada helper reused from approvals (import `canApprove`): estorno with required_role='admin' rejects a 'receptionist' approver.

Create `src/__tests__/ocr/extract.test.ts`:
  - `vi.mock('server-only', () => ({}))`. Dynamic-import the PURE helper `needsReview` from `@/lib/ai/ocr-confidence` (existsSync guard). PURE unit: a fields object with all confidences ≥ 0.80 → `needsReview === false`; any field < 0.80 → `true`; default threshold is 0.80.
  - SRC on `src/app/api/ocr/route.ts`: matches `export const runtime = 'nodejs'`, `generateObject`, `type: 'file'` (FilePart), `zeroDataRetention: true` (ZDR — LGPD on RG/comprovante), `ocr_extractions` insert, and `status` set to `'pending_review'` when below threshold. (OCR-01/02)
  - SRC on `src/app/api/ocr/route.ts`: assert it references masking before logging any extracted CPF (matches `maskCPF` or a comment asserting no raw CPF in logs) — Pitfall 3 guard.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/audit/audit-ui.test.ts src/__tests__/audit/estorno.test.ts src/__tests__/ocr/extract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and the suite RUNS; assertions are RED for the not-yet-created targets.
    - `needsReview` pure-unit asserts are written so they will pass the moment Plan 05 creates `src/lib/ai/ocr-confidence.ts`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>audit-ui.test.ts, estorno.test.ts, extract.test.ts exist; RED for missing artifacts; tsc green; full suite still runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → source files | Tests only READ source; no privilege boundary crossed. Scaffolds encode the security contract that later waves must satisfy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | ai_decision_log immutability | mitigate | phase10.test.ts asserts NO FOR INSERT/UPDATE/DELETE client policy (INSERT-only via admin client) |
| T-10-02 | Information Disclosure | OCR PII to vision model | mitigate | extract.test.ts asserts `zeroDataRetention: true` present in OCR route |
| T-10-03 | Elevation of Privilege | governance bypass | mitigate | policy.test.ts asserts `import 'server-only'` in policy.ts |
| T-10-04 | Repudiation | approval double-execution | mitigate | approvals.test.ts asserts idempotency_key + executed_at + status guard |
</threat_model>

<verification>
- `npx vitest run src/__tests__/migrations/phase10.test.ts src/__tests__/governance/ src/__tests__/audit/ src/__tests__/ocr/` runs without import crash (RED on assertions is expected).
- `npx tsc --noEmit` clean.
- Existing suites unaffected: `npx vitest run src/__tests__/ai/ src/__tests__/proxy/` still green.
</verification>

<success_criteria>
- 6 scaffold files created covering all 8 requirements with grep-verifiable assertions.
- Test style matches the Phase 5–9 source-inspection + pure-unit convention.
- tsc green; existing tests untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ia-governada-l0-l4-auditoria-ocr/10-01-SUMMARY.md`
</output>
