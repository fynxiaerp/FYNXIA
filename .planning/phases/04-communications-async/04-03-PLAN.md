---
phase: 04-communications-async
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/emails/AppointmentReminderEmail.tsx
  - src/lib/messaging/reminder-scan.ts
autonomous: true
requirements: [COMMS-01, COMMS-02]
must_haves:
  truths:
    - "An AppointmentReminderEmail React Email template renders patientName, clinicName, appointmentDate, appointmentTime, dentistName in Brazilian Portuguese"
    - "A pure reminder-scan function selects tomorrow's non-cancelled appointments and produces enqueue targets for BOTH channels (whatsapp + email)"
    - "Each target carries the dedup key components (appointment_id, channel, type='24h') and the idempotency_key 'reminder:{appointmentId}:{channel}:24h'"
    - "A cancelled appointment ('cancelado') produces zero targets"
    - "A patient missing phone produces no whatsapp target; missing email produces no email target (channels are independent)"
  artifacts:
    - path: "src/emails/AppointmentReminderEmail.tsx"
      provides: "React Email appointment reminder template (COMMS-02)"
      contains: "appointmentDate"
    - path: "src/lib/messaging/reminder-scan.ts"
      provides: "Pure selectReminderTargets() scan logic"
      contains: "cancelado"
  key_links:
    - from: "src/lib/messaging/reminder-scan.ts"
      to: "message_log dedup key"
      via: "type='24h' + idempotency_key format"
      pattern: "reminder:"
---

<objective>
Build the two pure/presentational pieces the reminder cron needs: the email template (COMMS-02) and the appointment-scan logic (COMMS-01/02 selection + dedup keys). Both are unit-testable without DB or network. Plan 04 wires them into the cron.

The scan is a PURE function (takes appointments + today, returns targets) following the proven `selectReminders` pattern from the collection ruler — no server-only imports so it is fully node-testable.

Purpose: Isolate the testable reminder selection logic and the email presentation from the I/O cron, mirroring Phase 3's pure `ruler.ts` + `CollectionReminderEmail.tsx` split.
Output: AppointmentReminderEmail.tsx, reminder-scan.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-communications-async/04-RESEARCH.md
@.planning/phases/04-communications-async/04-CONTEXT.md

# Reference patterns (mirror these exactly):
@src/emails/CollectionReminderEmail.tsx
@src/lib/collection/ruler.ts
@src/__tests__/comms/email.test.ts
@src/__tests__/comms/reminders.test.ts

<interfaces>
<!-- Mirror the existing collection-ruler split exactly. -->

CollectionReminderEmail (src/emails/CollectionReminderEmail.tsx): React Email component using @react-email/components (Html/Head/Body/Container/Heading/Text/Button/Section/Hr), pt-BR, FYNXIA-branded header. Copy this structure for the appointment reminder.

selectReminders (src/lib/collection/ruler.ts): the pure-function pattern — `(rule, receivables, today: Date) => ReminderTarget[]`, no server-only import, date normalized to UTC midnight. Mirror this purity for the appointment scan.

appointments status values (Phase 2): 'agendado','confirmado','em_atendimento','concluido','cancelado'. Cancelled = 'cancelado'.
patients fields: full_name, phone (nullable, user-entered), email (nullable).

idempotency_key format (RESEARCH §Pattern 5): `reminder:${appointmentId}:${channel}:24h`.
message_log dedup key (Plan 01 migration): UNIQUE (appointment_id, channel, type) — type is '24h'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppointmentReminderEmail React Email template</name>
  <files>src/emails/AppointmentReminderEmail.tsx</files>
  <read_first>
    - src/emails/CollectionReminderEmail.tsx (exact structure, branding, styling to mirror)
    - .planning/phases/04-communications-async/04-RESEARCH.md (§React Email — Appointment Reminder Template)
    - src/__tests__/comms/email.test.ts (the render assertions this must satisfy)
  </read_first>
  <behavior>
    - renders with props { patientName, clinicName, appointmentDate, appointmentTime, dentistName }
    - rendered HTML contains all 5 prop values
    - pt-BR copy; FYNXIA-branded header (mirror CollectionReminderEmail)
    - informational CTA "Ver minha agenda" (no action wiring needed)
  </behavior>
  <action>
Create `src/emails/AppointmentReminderEmail.tsx` mirroring `CollectionReminderEmail.tsx`:
- Import from `@react-email/components`: Html, Head, Body, Container, Heading, Text, Button, Section, Hr.
- `export interface AppointmentReminderEmailProps { patientName: string; clinicName: string; appointmentDate: string; appointmentTime: string; dentistName: string }`.
- `export function AppointmentReminderEmail(props)` — `<Html lang="pt-BR">`, FYNXIA dark header (`#0f172a`), heading "Lembrete: sua consulta é amanhã", body greeting "Olá, {patientName}!" plus an appointment-details card (Section) showing Data: {appointmentDate}, Horário: {appointmentTime}, Dentista: {dentistName}, Clínica: {clinicName}. CTA Button "Ver minha agenda" linking to `https://app.fynxia.com/clinica/agenda` (informational). Footer matching CollectionReminderEmail.
- Keep all 5 prop values in the visible body so the render test finds them.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/comms/email.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>email.test.ts GREEN: rendered HTML contains patientName, clinicName, appointmentDate, appointmentTime, dentistName.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure reminder-scan selection logic</name>
  <files>src/lib/messaging/reminder-scan.ts</files>
  <read_first>
    - src/lib/collection/ruler.ts (pure-function purity + date-handling pattern to mirror)
    - .planning/phases/04-communications-async/04-RESEARCH.md (§Architecture Patterns 5 & 6 — dedup key, scan window)
    - src/__tests__/comms/reminders.test.ts (the assertions this must satisfy)
  </read_first>
  <behavior>
    - input: a list of appointment records (id, start_time, status, patient { phone, email }) already scoped to the scan window, plus nothing else (window filtering is the cron's job, but the function re-checks status)
    - excludes status === 'cancelado'
    - for each remaining appointment, emits a whatsapp target IF patient.phone present, and an email target IF patient.email present
    - each target: { appointmentId, channel, type: '24h', idempotencyKey: `reminder:${appointmentId}:${channel}:24h` }
    - returns [] for empty input or all-cancelled input
  </behavior>
  <action>
Create `src/lib/messaging/reminder-scan.ts` (PURE — no server-only import, importable in node/vitest):
- `export interface ScanAppointment { id: string; start_time: string; status: string; patient: { phone: string | null; email: string | null } }`.
- `export interface ReminderTarget { appointmentId: string; channel: 'whatsapp' | 'email'; type: '24h'; idempotencyKey: string }`.
- `export function selectReminderTargets(appointments: ScanAppointment[]): ReminderTarget[]` — loop; skip `status === 'cancelado'`; if `patient.phone` push a whatsapp target; if `patient.email` push an email target; build `idempotencyKey = \`reminder:${appt.id}:${channel}:24h\``. Return the flat array.
- Add a tiny comment noting the cron supplies the date window (tomorrow 00:00–next-day 00:00) and applies E.164 normalization (toE164) before enqueue — this function is window-agnostic and channel-independent so each channel fails/succeeds on its own (Pitfall 8).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/comms/reminders.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>The reminder-scan portion of reminders.test.ts GREEN: non-cancelled tomorrow appt → 2 targets (whatsapp+email) with correctly-formatted idempotency keys; cancelled → 0; missing phone/email drops that channel only. (The cron source-inspection portion may still fail until Plan 04.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reminder-scan (pure) → no external boundary | In-memory transformation; no I/O, no secrets |
| email template → recipient inbox | Patient-facing content; must contain no other patient's data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-scan-i | Information Disclosure | reminder-scan output | mitigate | Function operates only on records the caller already tenant-scoped; per-appointment targets carry only that appointment's id |
| T-4-email-i | Information Disclosure | AppointmentReminderEmail | mitigate | Template renders only the single recipient's props; no list/loop over other patients |
| T-4-scan-channel | (reliability) | one channel failing blocks the other | mitigate | whatsapp and email targets are independent rows; email always fires even if phone missing/unreachable (Pitfall 8) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/comms/email.test.ts src/__tests__/comms/reminders.test.ts` GREEN (reminders cron-source portion may await Plan 04)
- `npx tsc --noEmit` exit 0
</verification>

<success_criteria>
- AppointmentReminderEmail renders all appointment details in pt-BR
- selectReminderTargets emits independent whatsapp+email targets, skips cancelled, drops missing-contact channels, with correct dedup/idempotency keys
</success_criteria>

<output>
After completion, create `.planning/phases/04-communications-async/04-03-SUMMARY.md`
</output>
