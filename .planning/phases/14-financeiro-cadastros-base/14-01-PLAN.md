---
phase: 14-financeiro-cadastros-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/financeiro/migrations-phase14.test.ts
  - src/__tests__/financeiro/chart-of-accounts.test.ts
  - src/__tests__/financeiro/transaction-classification.test.ts
  - src/__tests__/financeiro/regression-guard-phase14.test.ts
autonomous: true
requirements: [FCAD-01, FCAD-02]

must_haves:
  truths:
    - "Test suite has source-inspection assertions for the 3 new tables + ALTERs that FAIL until migrations exist"
    - "Test suite has unit assertions for buildTree() and the expanded Zod schema that FAIL until those modules exist"
    - "Regression guard imports the existing Phase 3 financial tests so Phase 14 cannot silently break them"
  artifacts:
    - path: "src/__tests__/financeiro/migrations-phase14.test.ts"
      provides: "Source-inspection of Phase 14 SQL (tables, columns, RLS, seed, backfill)"
      min_lines: 60
    - path: "src/__tests__/financeiro/chart-of-accounts.test.ts"
      provides: "buildTree() + account-code unit tests"
      min_lines: 30
    - path: "src/__tests__/financeiro/transaction-classification.test.ts"
      provides: "Zod schema unit tests for required accountId + costCenterId"
      min_lines: 30
    - path: "src/__tests__/financeiro/regression-guard-phase14.test.ts"
      provides: "Re-runs Phase 3 financial guards"
      min_lines: 15
  key_links:
    - from: "migrations-phase14.test.ts"
      to: "supabase/migrations/20260619001*.sql"
      via: "readFileSync source inspection"
      pattern: "readFileSync.*20260619001"
---

<objective>
Wave 0 (RED): Create the failing test scaffolds that encode every Phase 14 requirement BEFORE any implementation exists. Per `nyquist_validation: true`, no schema/Server-Action task may ship without an automated check — this plan creates those checks first, all RED.

Purpose: Lock the acceptance contract (table shapes, RLS, backfill, required classification, buildTree) into executable tests so later plans turn them GREEN. Prevents shallow implementation and silent Phase 3 regressions.
Output: 4 new test files under `src/__tests__/financeiro/`, all failing (modules/migrations not yet created), plus the regression guard re-running existing Phase 3 financial suites GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-financeiro-cadastros-base/14-RESEARCH.md
@.planning/phases/14-financeiro-cadastros-base/14-VALIDATION.md

# Existing test patterns to mirror (source-inspection + dynamic-import-guard)
@src/__tests__/financeiro/receivables.test.ts

<interfaces>
<!-- Contracts the later plans will satisfy. Executor: assert against these exact strings. -->

buildTree() — to be created in src/lib/financeiro/chart-tree.ts (Plan 04 source, but pure fn tested here):
```typescript
type AccountNodeFlat = {
  id: string; parent_id: string | null; code: string; name: string;
  type: 'grupo' | 'receita' | 'despesa'; ativo: boolean; depth: number
}
type AccountNode = AccountNodeFlat & { children: AccountNode[] }
export function buildTree(rows: AccountNodeFlat[]): AccountNode[]
```

transactionSchema (expanded) — src/actions/transactions.ts (Plan 03):
```typescript
// accountId + costCenterId become REQUIRED uuid; bankAccountId optional nullable
const transactionSchema = z.object({
  type: z.enum(['receita','despesa']),
  categoryId: z.string().uuid().optional().nullable(),
  accountId: z.string().uuid({ message: 'Conta contábil obrigatória' }),
  costCenterId: z.string().uuid({ message: 'Centro de custo obrigatório' }),
  bankAccountId: z.string().uuid().optional().nullable(),
  amount: z.number().positive().refine(isMoney2dp),
  transactionDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  description: z.string().max(500).optional().nullable(),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration source-inspection scaffold (RED)</name>
  <read_first>
    - src/__tests__/financeiro/receivables.test.ts (mirror its readFileSync + existsSync guard pattern)
    - .planning/phases/14-financeiro-cadastros-base/14-RESEARCH.md §"Validation Architecture" + §"Architecture Patterns" (exact DDL/RLS strings to assert)
    - supabase/migrations/20260606000200_financial_rls.sql (RLS policy-name pattern to mirror)
    - supabase/migrations/20260614000100_units_table.sql (partial-unique-index pattern)
  </read_first>
  <action>
Create `src/__tests__/financeiro/migrations-phase14.test.ts`. Use `import { readFileSync, existsSync } from 'fs'` and `import { join } from 'path'`. Resolve the three migration paths via `join(process.cwd(), 'supabase/migrations', '<file>')`:
- `20260619001100_financial_cadastros_tables.sql`
- `20260619001200_financial_cadastros_rls.sql`
- `20260619001300_financial_cadastros_seed.sql`

For each path, read content as `existsSync(p) ? readFileSync(p, 'utf-8') : ''` (empty string when missing → assertions FAIL, which is the desired RED state — do NOT skip).

Write these `describe`/`it` assertions (use `expect(sql).toMatch(/.../)`):

TABLES file (`...001100...`):
- `chart_of_accounts` table: `/CREATE TABLE public\.chart_of_accounts/`, `/parent_id\s+UUID\s+REFERENCES public\.chart_of_accounts\(id\) ON DELETE RESTRICT/`, `/code\s+TEXT\s+NOT NULL/`, `/type\s+TEXT\s+NOT NULL CHECK \(type IN \('grupo', 'receita', 'despesa'\)\)/`, `/clinic_id\s+UUID\s+NOT NULL REFERENCES public\.clinics\(id\) ON DELETE CASCADE/`, `/ativo\s+BOOLEAN\s+NOT NULL DEFAULT true/`
- chart indexes: `/idx_chart_of_accounts_clinic/`, `/CREATE UNIQUE INDEX idx_chart_of_accounts_code ON public\.chart_of_accounts\(clinic_id, code\)/`
- `cost_centers` table: `/CREATE TABLE public\.cost_centers/`, `/unit_id\s+UUID\s+NOT NULL REFERENCES public\.units\(id\)/`, `/is_default\s+BOOLEAN\s+NOT NULL DEFAULT false/`, partial unique `/CREATE UNIQUE INDEX idx_cost_centers_one_default ON public\.cost_centers\(unit_id\) WHERE is_default = true/`, `/idx_cost_centers_clinic/`
- `bank_accounts` table: `/CREATE TABLE public\.bank_accounts/`, `/saldo_inicial\s+NUMERIC\(12,2\)/`, `/idx_bank_accounts_clinic/`
- ALTERs: `/ALTER TABLE public\.financial_transactions/` with `/ADD COLUMN IF NOT EXISTS account_id\s+UUID REFERENCES public\.chart_of_accounts\(id\)/`, `/ADD COLUMN IF NOT EXISTS cost_center_id\s+UUID REFERENCES public\.cost_centers\(id\)/`, `/ADD COLUMN IF NOT EXISTS bank_account_id\s+UUID REFERENCES public\.bank_accounts\(id\)/`; partial indexes `/idx_financial_transactions_account.*WHERE account_id IS NOT NULL/s`, `/idx_financial_transactions_cost_center.*WHERE cost_center_id IS NOT NULL/s`
- `financial_categories` ALTER: `/ALTER TABLE public\.financial_categories[\s\S]*ADD COLUMN IF NOT EXISTS account_id\s+UUID REFERENCES public\.chart_of_accounts\(id\)/`
- Assert NO bare `NOT NULL` on the new financial_transactions columns: `expect(sql).not.toMatch(/account_id\s+UUID[^,]*NOT NULL/)` and same for cost_center_id (D-03b: columns stay NULLABLE).

RLS file (`...001200...`): for each of `chart_of_accounts`, `cost_centers`, `bank_accounts`:
- `/ALTER TABLE public\.<table> ENABLE ROW LEVEL SECURITY/`
- read policy: `/"<table>_tenant_read"[\s\S]*FOR SELECT USING \(clinic_id = get_my_tenant_id\(\)\)/`
- admin write policy with BOTH USING and WITH CHECK: `/"<table>_admin_write"[\s\S]*FOR ALL[\s\S]*USING \([\s\S]*clinic_id = get_my_tenant_id\(\)[\s\S]*get_my_role\(\) IN \('admin', 'superadmin'\)[\s\S]*WITH CHECK \([\s\S]*clinic_id = get_my_tenant_id\(\)[\s\S]*get_my_role\(\) IN \('admin', 'superadmin'\)/`

SEED file (`...001300...`):
- chart seed for existing clinics: `/INSERT INTO public\.chart_of_accounts/`, presence of codes `/'1'.*'Receitas'/`, `/'2'.*'Despesas'/`, leaf `/'1\.1\.1'.*'Consultas'/`, `/'2\.1\.4'.*'Laboratório'/`
- trigger ordering (Pitfall 5): `/CREATE TRIGGER seed_accounts_on_clinic\s+AFTER INSERT ON public\.clinics/` AND assert the function name `seed_chart_of_accounts` exists `/FUNCTION public\.seed_chart_of_accounts\(\)/`
- updated category seed: `/CREATE OR REPLACE FUNCTION public\.seed_financial_categories\(\)/` (it must be re-created to map account_id)
- CC default seed per unit: `/INSERT INTO public\.cost_centers[\s\S]*FROM public\.units/`
- category→account mapping backfill: `/UPDATE public\.financial_categories[\s\S]*SET account_id/`
- transaction backfill account: `/UPDATE public\.financial_transactions[\s\S]*SET account_id = fc\.account_id/`
- transaction backfill cost_center: `/UPDATE public\.financial_transactions[\s\S]*SET cost_center_id/` and `/is_default = true/`
- backfill assertion presence (0 unresolved): `/cost_center_id IS NULL/` appears in a DO/RAISE or comment block — assert `/ASSERT|RAISE EXCEPTION|0 rows/` near a cost_center backfill check OR a final `DO $$ ... ASSERT ... $$` block.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro/migrations-phase14.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/financeiro/migrations-phase14.test.ts` exists with ≥ 60 lines (`grep -c "" file`).
    - `grep -q "20260619001100_financial_cadastros_tables.sql" file` exits 0.
    - `grep -q "chart_of_accounts_admin_write" file` exits 0.
    - `grep -q "seed_accounts_on_clinic" file` exits 0.
    - `npx vitest run src/__tests__/financeiro/migrations-phase14.test.ts` exits NON-zero (RED — migrations not yet created). This is the intended state for Wave 0.
  </acceptance_criteria>
  <done>Migration source-inspection test exists and fails RED because the 3 migration files do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildTree + Zod classification unit scaffolds (RED) + regression guard (GREEN)</name>
  <read_first>
    - src/actions/transactions.ts (current schema — to be expanded in Plan 03; assert the FUTURE shape here)
    - .planning/phases/14-financeiro-cadastros-base/14-RESEARCH.md §"Code Examples" (buildTree reference impl)
    - src/__tests__/actions/transactions.test.ts (existing Phase 3 transaction tests to re-run in regression guard)
    - src/__tests__/migrations/financial.test.ts (existing Phase 3 financial migration guard)
    - src/__tests__/webhooks/asaas.test.ts (existing webhook guard)
  </read_first>
  <behavior>
    - buildTree(): given flat rows [{id:'a',parent_id:null,...},{id:'b',parent_id:'a',...}] returns 1 root with 1 child; empty input → []; two roots → length 2.
    - transactionSchema: input without accountId → safeParse.success === false with message 'Conta contábil obrigatória'; without costCenterId → false with 'Centro de custo obrigatório'; valid full input → success true; bankAccountId omitted → success true (optional).
    - regression guard: existing Phase 3 financial + webhook suites still pass.
  </behavior>
  <action>
Create THREE files.

(A) `src/__tests__/financeiro/chart-of-accounts.test.ts`:
Dynamic-import guard pattern (mirror receivables.test.ts): resolve the module via absolute path `join(process.cwd(),'src/lib/financeiro/chart-tree.ts')`; `if (!existsSync(modPath)) { it.skip(...) }` is NOT allowed — instead `it('buildTree builds nested tree', async () => { const mod = await import('@/lib/financeiro/chart-tree'); ... })`. Because the module does not exist yet, the dynamic import throws → test FAILS RED. Write 3 `it` cases for buildTree per the <behavior> block (root+child nesting, empty → [], two roots → length 2). Also test an account-code helper `nextChildCode(parentCode, siblingCount)` from the same module: `nextChildCode('1.1', 2)` → `'1.1.3'`, `nextChildCode(null, 1)` → `'2'` (next root). Assert these exact outputs.

(B) `src/__tests__/financeiro/transaction-classification.test.ts`:
`import { describe, it, expect } from 'vitest'`. Since `src/actions/transactions.ts` is `'use server'` (cannot import the internal const schema directly), test the RULE by re-declaring a mirror Zod schema inline AND asserting via dynamic import of a NEW pure module `@/lib/financeiro/transaction-schema` (to be created in Plan 03) that exports `transactionClassificationSchema`. Write `it` cases: missing accountId → `safeParse(...).success === false` and `.error.issues[0].message === 'Conta contábil obrigatória'`; missing costCenterId → false + 'Centro de custo obrigatório'; full valid → true; bankAccountId omitted → true. Module not existing yet → RED.

(C) `src/__tests__/financeiro/regression-guard-phase14.test.ts`:
A thin meta-test that asserts the existing Phase 3 guard files still exist and are importable: `existsSync(join(process.cwd(),'src/__tests__/migrations/financial.test.ts'))` toBe true, same for `src/__tests__/webhooks/asaas.test.ts` and `src/__tests__/actions/transactions.test.ts`. This file passes GREEN immediately (the guarded files exist) and documents the regression contract; the actual re-run happens via `npx vitest run` at wave-merge.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro/chart-of-accounts.test.ts src/__tests__/financeiro/transaction-classification.test.ts src/__tests__/financeiro/regression-guard-phase14.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist; chart-of-accounts.test.ts ≥ 30 lines, transaction-classification.test.ts ≥ 30 lines.
    - `grep -q "buildTree" src/__tests__/financeiro/chart-of-accounts.test.ts` exits 0.
    - `grep -q "Conta contábil obrigatória" src/__tests__/financeiro/transaction-classification.test.ts` exits 0.
    - chart-of-accounts + transaction-classification tests exit NON-zero (RED — target modules absent).
    - regression-guard-phase14.test.ts passes GREEN (`existsSync` of the 3 Phase 3 files is true).
  </acceptance_criteria>
  <done>buildTree + Zod classification tests fail RED (modules absent); regression guard passes GREEN confirming Phase 3 guards are present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — test-only plan) | This plan creates test files only; no runtime trust boundary is crossed. The threats it ENCODES (cross-tenant RLS, required classification) are mitigated in Plans 02/03 and asserted here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I (Info Disclosure) | new cadastro tables (encoded test) | mitigate | `migrations-phase14.test.ts` asserts `clinic_id = get_my_tenant_id()` on all read/write policies — RLS implemented in Plan 02 |
| T-14-02 | E (Elevation) | admin-write policies (encoded test) | mitigate | test asserts `get_my_role() IN ('admin','superadmin')` in BOTH USING and WITH CHECK — implemented in Plan 02 |
| T-14-03 | T (Tampering) | required classification (encoded test) | mitigate | `transaction-classification.test.ts` asserts accountId+costCenterId required — implemented in Plan 03 |
</threat_model>

<verification>
- `npx vitest run src/__tests__/financeiro/` runs the new suite: migration + buildTree + classification tests RED, regression guard GREEN.
- `npm run build` exits 0 (test files are typecheck-clean despite RED assertions).
- No `vitest --watch` flags anywhere.
</verification>

<success_criteria>
- 4 new test files created under `src/__tests__/financeiro/`.
- Migration + buildTree + Zod tests fail RED (no implementation yet) — proving they encode real contracts.
- Regression guard passes, confirming Phase 3 financial guards are present and will be re-run at wave merge.
- `npm run build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/14-financeiro-cadastros-base/14-01-SUMMARY.md`
</output>
