---
phase: 14-financeiro-cadastros-base
plan: 05
type: execute
wave: 5
depends_on: [14-04-PLAN.md]
files_modified:
  - src/components/financeiro/ChartOfAccountsTree.tsx
  - src/components/financeiro/AccountFormDialog.tsx
  - src/app/(dashboard)/clinica/financeiro/plano-de-contas/page.tsx
  - src/app/(dashboard)/clinica/financeiro/plano-de-contas/loading.tsx
  - src/app/(dashboard)/clinica/financeiro/plano-de-contas/error.tsx
  - src/app/(dashboard)/clinica/financeiro/page.tsx
autonomous: false
requirements: [FCAD-01]

must_haves:
  truths:
    - "Plano de contas renders as a hierarchical tree (grupos → subgrupos → contas folha) at /clinica/financeiro/plano-de-contas"
    - "Admin can create a root/child account and edit an account via dialog; non-admin sees read-only tree"
    - "Financeiro hub shows Plano de Contas / Centros de Custo / Contas Correntes cards"
  artifacts:
    - path: "src/components/financeiro/ChartOfAccountsTree.tsx"
      provides: "Accordion-based recursive tree with row actions"
      min_lines: 60
    - path: "src/app/(dashboard)/clinica/financeiro/plano-de-contas/page.tsx"
      provides: "RSC fetching listAccountsTree"
      contains: "listAccountsTree"
  key_links:
    - from: "plano-de-contas/page.tsx"
      to: "src/actions/chart-of-accounts.ts"
      via: "listAccountsTree()"
      pattern: "listAccountsTree"
    - from: "ChartOfAccountsTree.tsx"
      to: "AccountFormDialog.tsx"
      via: "edit/add-child opens dialog → createAccount/updateAccount"
      pattern: "AccountFormDialog"
---

<objective>
Build the Plano de Contas screen: a hierarchical Accordion tree (FCAD-01 SC1 — "a árvore é visualizada na tela de cadastro") with admin create/edit via dialog, and add the 3 new cadastro cards to the financeiro hub.

Purpose: FCAD-01 user-visible tree + entry points.
Output: tree component, account form dialog, route segment (page+loading+error), hub cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-financeiro-cadastros-base/14-UI-SPEC.md

# Patterns to replicate
@src/app/(dashboard)/clinica/financeiro/page.tsx
@src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx
@src/components/financeiro/TransactionModal.tsx
@src/components/ui/accordion.tsx

<interfaces>
From src/actions/chart-of-accounts.ts (Plan 04):
```typescript
listAccountsTree(): Promise<{ success: boolean; tree?: AccountNode[]; error?: string }>
createAccount(input: { name; type: 'grupo'|'receita'|'despesa'; parentId?: string|null; code?: string; ativo?: boolean }): Promise<{ success; id?; error? }>
updateAccount(input: { id; name?; ativo?; code? }): Promise<{ success; error? }>
deleteAccount(id: string): Promise<{ success; error? }>
```
From src/lib/financeiro/chart-tree.ts: AccountNode = { id; parent_id; code; name; type; ativo; depth; children: AccountNode[] }
@base-ui Dialog/Select use render-prop (NOT asChild) — see TransactionModal.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AccountFormDialog + ChartOfAccountsTree components</name>
  <read_first>
    - src/components/financeiro/TransactionModal.tsx (Dialog + RHF + zodResolver + @base-ui render-prop pattern; router.refresh on success)
    - src/components/ui/accordion.tsx (Accordion/AccordionItem/AccordionTrigger/AccordionContent API)
    - .planning/phases/14-financeiro-cadastros-base/14-UI-SPEC.md §"Tree Component", §"Account Form Dialog", §"Interaction Contracts", §"Copywriting Contract"
  </read_first>
  <action>
`src/components/financeiro/AccountFormDialog.tsx` ('use client'): Dialog (`sm:max-w-md`) with RHF + zodResolver. Fields per UI-SPEC: código (text, font-mono — pre-filled/auto-computed, editable), nome (required), tipo (Select: Grupo/Receita/Despesa), conta pai (Select of flattened existing accounts, optional — accept a `parents` prop), ativo (Switch, default true). Props: `{ mode: 'create'|'edit'; account?; parentId?; parents; trigger: ReactNode }`. On submit → createAccount or updateAccount; on success setOpen(false) + router.refresh(). Surface serverError in `Alert variant="destructive"`. Titles/buttons exactly per Copywriting Contract ("Nova Conta Contábil"/"Editar Conta Contábil"; "Salvar Conta"; "Cancelar"). Zod schema inline, NO `.default()` (defaultValues supply values).

`src/components/financeiro/ChartOfAccountsTree.tsx` ('use client'): props `{ accounts: AccountNode[]; canEdit: boolean }`. Render recursively:
- Group/parent nodes (children.length > 0 OR type==='grupo') → AccordionItem; default all open (`defaultValue` = all group ids; Accordion `multiple`).
- Leaf nodes → plain `div h-10 flex items-center` (no expand affordance), `role="listitem"`.
- Row anatomy per UI-SPEC: indent `pl-{depth*6}` (use a clamp; depth 0 pl-0, 1 pl-6, 2 pl-12), code span `font-mono tabular-nums text-sm font-semibold text-muted-foreground w-20`, name (`font-semibold` for group, `font-normal` for leaf; inactive → `text-muted-foreground line-through`), type Badge outline (receita green / despesa red / grupo muted — classes from UI-SPEC Color section), hover `hover:bg-muted/50`.
- When `canEdit`: show on hover an Edit icon button (opens AccountFormDialog mode=edit) and, on group rows, a "+ Conta Filha" icon button (opens AccountFormDialog mode=create with parentId set). aria-labels per UI-SPEC Accessibility.
- When `!canEdit`: render no action buttons (read-only tree for socio/auditor/dpo).
Flatten the tree once for the dialog's `parents` Select.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Both component files exist and start with `'use client'`.
    - `grep -q "Accordion" src/components/financeiro/ChartOfAccountsTree.tsx` exits 0.
    - `grep -q "Salvar Conta" src/components/financeiro/AccountFormDialog.tsx` and `Nova Conta Contábil` exit 0.
    - `grep -q "line-through" ChartOfAccountsTree.tsx` exits 0 (inactive styling).
    - `grep -q "canEdit" ChartOfAccountsTree.tsx` exits 0 (read-only gating).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Tree renders nested accounts with admin row actions and read-only mode; dialog creates/edits accounts.</done>
</task>

<task type="auto">
  <name>Task 2: plano-de-contas route segment + financeiro hub cards</name>
  <read_first>
    - src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx (RSC + PageHeader + role fetch pattern)
    - src/app/(dashboard)/clinica/financeiro/page.tsx (hub card grid + navItems shape — add 3 cards)
    - .planning/phases/14-financeiro-cadastros-base/14-UI-SPEC.md §"Page Structure" (plano-de-contas layout, empty state copy, loading skeleton, error.tsx pattern), §"Financeiro Hub — Extended"
  </read_first>
  <action>
`src/app/(dashboard)/clinica/financeiro/plano-de-contas/page.tsx` (RSC): fetch user role (mirror financeiro/page.tsx role fetch), `const isAdmin = role === 'admin' || role === 'superadmin'`; call `listAccountsTree()`. Render PageHeader (title "Plano de Contas", breadcrumbs [{Financeiro, /clinica/financeiro},{Plano de Contas}], actions: admin-only `AccountFormDialog mode=create` trigger Button "Nova Conta" size sm). Body per UI-SPEC: `<main className="p-6 max-w-5xl mx-auto w-full space-y-6">` → Card with CardHeader ("Estrutura Hierárquica" + CardDescription) → CardContent → `<ChartOfAccountsTree accounts={tree} canEdit={isAdmin} />`. Empty state (no accounts) → use EmptyState with UI-SPEC copy ("Plano de contas não configurado" / body). Error path → Alert if `!result.success`.

`src/app/(dashboard)/clinica/financeiro/plano-de-contas/loading.tsx`: skeleton per UI-SPEC (PageHeader skeleton + 3 group skeletons h-10 each with 2 indented child rows). Use `animate-pulse bg-muted rounded-md`.

`src/app/(dashboard)/clinica/financeiro/plano-de-contas/error.tsx`: exact UI-SPEC error.tsx pattern ('use client', AlertTriangle, "Algo deu errado", "Tentar novamente" reset button).

`src/app/(dashboard)/clinica/financeiro/page.tsx`: add 3 entries to `navItems` (after existing, before the regua filter) per UI-SPEC: `{ href:'/clinica/financeiro/plano-de-contas', title:'Plano de Contas', description:'...', icon: GitBranch, show:true }`, Centros de Custo (icon Building2, href /clinica/financeiro/centros-de-custo), Contas Correntes (icon Landmark, href /clinica/financeiro/contas-correntes). Import GitBranch, Building2, Landmark from lucide-react. (The centros-de-custo and contas-correntes routes are created in Plan 06 — links will 404 only until Plan 06 ships in the same wave; both are wave 5.)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/clinica/financeiro/plano-de-contas/page.tsx`, `loading.tsx`, `error.tsx` all exist.
    - `grep -q "listAccountsTree" plano-de-contas/page.tsx` exits 0.
    - `grep -q "ChartOfAccountsTree" plano-de-contas/page.tsx` exits 0.
    - `grep -q "canEdit={isAdmin}" plano-de-contas/page.tsx` exits 0.
    - `grep -q "Plano de Contas" src/app/(dashboard)/clinica/financeiro/page.tsx` and `Centros de Custo`, `Contas Correntes` exit 0.
    - `grep -q "GitBranch" src/app/(dashboard)/clinica/financeiro/page.tsx` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Plano de contas route renders the tree with admin actions; hub exposes 3 new cards.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — plano de contas tree (FCAD-01 SC1)</name>
  <action>Pause for human visual verification of the plano-de-contas screen built in Tasks 1-2. No code changes — the human follows the steps below and approves or reports issues.</action>
  <what-built>The Plano de Contas screen renders the seeded odontological chart as a nested tree, with admin create/edit dialogs and read-only mode for non-admins.</what-built>
  <how-to-verify>
1. Log in as an admin; visit `/clinica/financeiro` — confirm the 3 new cards (Plano de Contas, Centros de Custo, Contas Correntes) appear.
2. Click "Plano de Contas". Confirm the tree shows: 1 Receitas → 1.1 Receitas Operacionais → 1.1.1 Consultas ... and 2 Despesas → 2.1 → 2.1.4 Laboratório, all expanded, codes monospaced and aligned.
3. Click "Nova Conta", create a leaf under 2.1; confirm it appears with auto-computed code.
4. Edit an account; toggle ativo off; confirm it renders strikethrough.
5. Log in as a `socio` (read-only); confirm NO Edit/Add buttons appear.
  </how-to-verify>
  <resume-signal>Type "approved" or describe rendering/interaction issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC/Server Action | Tree edit actions cross into admin-gated Server Actions (Plan 04); UI only reveals controls to admins as defense-in-depth, not as the security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14 | E (Elevation) | edit controls shown to non-admin | mitigate | `canEdit={isAdmin}` hides controls; the real gate is the admin role check + RLS in Plan 04 Server Actions (UI hiding is cosmetic) |
| T-14-15 | I (Info Disclosure) | tree shows another tenant's accounts | mitigate | listAccountsTree runs under RLS (clinic_id = get_my_tenant_id()) — only own-tenant accounts returned |
</threat_model>

<verification>
- `npm run build` exits 0.
- Human visual checkpoint approves the tree render + admin/read-only modes.
</verification>

<success_criteria>
- FCAD-01 SC1 met: hierarchical chart visible on the cadastro screen.
- Admin create/edit works; non-admin read-only.
- Hub exposes the 3 new cadastros.
</success_criteria>

<output>
After completion, create `.planning/phases/14-financeiro-cadastros-base/14-05-SUMMARY.md`
</output>
