---
phase: 14-financeiro-cadastros-base
plan: 03
type: execute
wave: 3
depends_on: [14-02-PLAN.md]
files_modified:
  - src/lib/supabase/database.types.ts
autonomous: false
requirements: [FCAD-01, FCAD-02]

must_haves:
  truths:
    - "The 3 Phase 14 migrations are applied to the live Supabase project jqjwyqlbbuqnrffdnlpp"
    - "Generated DB types include chart_of_accounts, cost_centers, bank_accounts and the new financial_transactions/financial_categories columns"
    - "Backfill RAISE EXCEPTION assertion did not fire (push succeeded)"
  artifacts:
    - path: "src/lib/supabase/database.types.ts"
      provides: "Regenerated typed schema reflecting Phase 14 tables/columns"
      contains: "chart_of_accounts"
  key_links:
    - from: "supabase/migrations/20260619001*.sql"
      to: "live Supabase project"
      via: "supabase db push"
      pattern: "db push"
---

<objective>
[BLOCKING] Apply the three Phase 14 migrations to the live Supabase dev project and regenerate TypeScript DB types. This MUST run after Plan 02 (migrations written) and BEFORE any Server Action / UI plan, because build + tsc pass WITHOUT a push (types come from the committed types file, not the live DB) — creating a false-positive verification state. The phase cannot pass verification without this.

Purpose: Make the new schema real and the new tables/columns type-visible to Server Actions (Plan 04+).
Output: applied migrations + regenerated `database.types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-financeiro-cadastros-base/14-RESEARCH.md
@supabase/migrations/20260619001100_financial_cadastros_tables.sql
@supabase/migrations/20260619001200_financial_cadastros_rls.sql
@supabase/migrations/20260619001300_financial_cadastros_seed.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Verify correct project + push migrations + gen types</name>
  <read_first>
    - .planning/phases/14-financeiro-cadastros-base/14-RESEARCH.md §"Environment Availability"
    - the 3 migration files in supabase/migrations/20260619001*.sql
  </read_first>
  <what-built>
Plan 02 wrote 3 migrations (tables, RLS, seed+backfill). They are committed but NOT applied to the live database. This task applies them and regenerates types.

**CRITICAL account gotcha (MEMORY.md):** the Supabase CLI/MCP is frequently logged into the WRONG account (nexus-*). The active project MUST be `jqjwyqlbbuqnrffdnlpp` (org kczvihafddupruvsrrsc, sa-east-1) before pushing. Pushing to the wrong project corrupts another tenant's schema.
  </what-built>
  <action>
Claude attempts the automated path first; only if a non-suppressible interactive prompt or auth error appears does it pause for the human.

1. Confirm the active project:
   `supabase projects list` → verify `jqjwyqlbbuqnrffdnlpp` is present and is the linked project. If `supabase status`/link shows a different ref or a `nexus-*` account, STOP and re-login: `supabase login` (human provides token) then `supabase link --project-ref jqjwyqlbbuqnrffdnlpp`.
2. Ensure `SUPABASE_ACCESS_TOKEN` is set (non-TTY workaround). If not set, pause for human to export it.
3. Dry inspect pending migrations: `supabase migration list` — confirm the 3 `20260619001*` migrations are listed as not-yet-applied.
4. Push: `supabase db push`. Watch output for the STEP 10 backfill assertion `Phase14 backfill: N financial_transactions rows left without cost_center_id` — if it fires, the migration aborts atomically; report it and STOP (Plan 02 backfill must be fixed, not this plan).
5. Regenerate types into the project's existing types path (confirm the real path; CLAUDE.md references `supabase gen types typescript`):
   `supabase gen types typescript --project-id jqjwyqlbbuqnrffdnlpp --schema public > src/lib/supabase/database.types.ts`
   (If the project's canonical types file lives elsewhere, write to that path — grep for the existing `database.types.ts` import path first.)
6. Run `npm run build` to confirm regenerated types compile.
  </action>
  <how-to-verify>
1. `supabase migration list` shows the 3 `20260619001*` migrations as applied (remote column populated).
2. Open Supabase dashboard (or `supabase db diff` shows no drift) — tables `chart_of_accounts`, `cost_centers`, `bank_accounts` exist.
3. `grep -q "chart_of_accounts" src/lib/supabase/database.types.ts` exits 0.
4. RLS smoke (optional, via SQL editor as a non-admin tenant member): `SELECT * FROM cost_centers` for tenant A returns 0 rows of tenant B; a non-admin INSERT is rejected.
5. `npm run build` exits 0.
  </how-to-verify>
  <acceptance_criteria>
    - 3 migrations show as applied in `supabase migration list`.
    - `database.types.ts` contains `chart_of_accounts`, `cost_centers`, `bank_accounts`, and `account_id`/`cost_center_id`/`bank_account_id` under financial_transactions.
    - The backfill RAISE EXCEPTION did NOT fire (push completed without that error).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <resume-signal>Type "applied" once the 3 migrations are pushed to jqjwyqlbbuqnrffdnlpp and types regenerated, or paste the error if the push failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → live Supabase project | A push targets a remote production-adjacent DB; targeting the wrong project (nexus-*) would corrupt another tenant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | T (Tampering) | wrong-project push | mitigate | Task verifies active project ref = `jqjwyqlbbuqnrffdnlpp` before `db push`; re-login step if nexus-* detected |
| T-14-08 | I (Info Disclosure) | SUPABASE_ACCESS_TOKEN handling | accept | token provided via env var only, never committed; standard project practice |
</threat_model>

<verification>
- `supabase migration list` confirms applied migrations.
- `grep -q chart_of_accounts src/lib/supabase/database.types.ts`.
- `npm run build` exits 0.
</verification>

<success_criteria>
- Phase 14 schema live on the correct project.
- DB types regenerated and compiling.
- No backfill assertion failure.
</success_criteria>

<output>
After completion, create `.planning/phases/14-financeiro-cadastros-base/14-03-SUMMARY.md`
</output>
