---
phase: 14-financeiro-cadastros-base
plan: 06
type: execute
wave: 5
depends_on: [14-04-PLAN.md]
files_modified:
  - src/components/financeiro/CostCentersTable.tsx
  - src/components/financeiro/CostCenterFormDialog.tsx
  - src/components/financeiro/BankAccountsTable.tsx
  - src/components/financeiro/BankAccountFormDialog.tsx
  - src/app/(dashboard)/clinica/financeiro/centros-de-custo/page.tsx
  - src/app/(dashboard)/clinica/financeiro/centros-de-custo/loading.tsx
  - src/app/(dashboard)/clinica/financeiro/centros-de-custo/error.tsx
  - src/app/(dashboard)/clinica/financeiro/contas-correntes/page.tsx
  - src/app/(dashboard)/clinica/financeiro/contas-correntes/loading.tsx
  - src/app/(dashboard)/clinica/financeiro/contas-correntes/error.tsx
autonomous: false
requirements: [FCAD-01]

must_haves:
  truths:
    - "Admin cadastra/edita centros de custo (vinculados a unidade) em /clinica/financeiro/centros-de-custo"
    - "Admin cadastra/edita contas correntes (com saldo inicial NUMERIC) em /clinica/financeiro/contas-correntes"
    - "Each unit shows its seeded default cost center"
  artifacts:
    - path: "src/components/financeiro/CostCentersTable.tsx"
      provides: "Tabular CC list with edit + ativo toggle"
      min_lines: 40
    - path: "src/components/financeiro/BankAccountsTable.tsx"
      provides: "Tabular bank account list with edit"
      min_lines: 40
  key_links:
    - from: "centros-de-custo/page.tsx"
      to: "src/actions/cost-centers.ts"
      via: "listCostCenters()"
      pattern: "listCostCenters"
    - from: "contas-correntes/page.tsx"
      to: "src/actions/bank-accounts.ts"
      via: "listBankAccounts()"
      pattern: "listBankAccounts"
---

<objective>
Build the two tabular cadastros — Centros de Custo and Contas Correntes — using the established UnitsManager Table + Dialog pattern.

Purpose: FCAD-01 — cost centers (rateio por unidade/área) and bank accounts cadastro screens.
Output: 2 tables + 2 form dialogs + 2 route segments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-financeiro-cadastros-base/14-UI-SPEC.md

# Patterns to replicate
@src/components/config/UnitsManager.tsx
@src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx

<interfaces>
From src/actions/cost-centers.ts (Plan 04):
```typescript
listCostCenters(): Promise<{ success; centers?: { id; name; unit_id; unit_name; is_default; ativo }[]; error? }>
createCostCenter(input: { name; unitId; ativo? }): Promise<{ success; id?; error? }>
updateCostCenter(input: { id; name?; ativo? }): Promise<{ success; error? }>
```
From src/actions/bank-accounts.ts (Plan 04):
```typescript
listBankAccounts(): Promise<{ success; accounts?: { id; name; banco; agencia; conta; saldo_inicial; ativo }[]; error? }>
createBankAccount(input: { name; banco?; agencia?; conta?; saldoInicial: number }): Promise<{ success; id?; error? }>
updateBankAccount(input: { id; name?; banco?; agencia?; conta?; saldoInicial? }): Promise<{ success; error? }>
```
Units list for the CC unit Select: read via existing units action or pass from RSC. Grep src/actions for an existing listUnits before adding a query.
@base-ui Dialog/Select use render-prop (NOT asChild).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CostCenter table + dialog + route segment</name>
  <read_first>
    - src/components/config/UnitsManager.tsx (Table + inline Dialog + useTransition toggle pattern — replicate exactly)
    - src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx (RSC role fetch + PageHeader)
    - .planning/phases/14-financeiro-cadastros-base/14-UI-SPEC.md §"CostCentersTable", §"Cost Center Form Dialog", §"Page Structure /centros-de-custo", §"Copywriting Contract", §"Loading States", §"Error States"
  </read_first>
  <action>
`src/components/financeiro/CostCenterFormDialog.tsx` ('use client'): Dialog + RHF + zodResolver. Fields per UI-SPEC: nome (required), unidade (Select of units — prop `units: {id;name}[]`, required), ativo (Switch default true). Props `{ mode; center?; units; trigger }`. Submit → createCostCenter/updateCostCenter; success → setOpen(false)+router.refresh(). Titles/buttons per Copywriting ("Novo Centro de Custo"/"Editar Centro de Custo"; "Salvar Centro de Custo"; "Cancelar"). No `.default()` in Zod.

`src/components/financeiro/CostCentersTable.tsx` ('use client'): props `{ centers; units; canEdit }`. shadcn Table per UI-SPEC columns: Nome (two-line cell: name `text-sm font-normal` + unit affiliation subtitle `text-xs text-muted-foreground` — the SOLE permitted text-xs use), Unidade, Ativo (Badge outline "Ativo"/"Inativo"), Ações (Edit button → CostCenterFormDialog + ativo toggle Switch via useTransition calling updateCostCenter). When `!canEdit`, hide actions. Empty state per UI-SPEC copy.

Route segment `centros-de-custo/{page,loading,error}.tsx`:
- page.tsx (RSC): fetch role → isAdmin; `Promise.all([listCostCenters(), <units list>])`. PageHeader (title "Centros de Custo", breadcrumbs, action "Novo Centro de Custo" admin-only). Body: `<main className="p-6 max-w-5xl mx-auto w-full"><div className="rounded-md border"><CostCentersTable .../></div></main>`.
- loading.tsx + error.tsx per UI-SPEC (4 row skeletons; standard error.tsx).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `CostCentersTable.tsx`, `CostCenterFormDialog.tsx` exist, start with `'use client'`.
    - `centros-de-custo/page.tsx`, `loading.tsx`, `error.tsx` exist.
    - `grep -q "listCostCenters" centros-de-custo/page.tsx` exits 0.
    - `grep -q "Salvar Centro de Custo" CostCenterFormDialog.tsx` exits 0.
    - `grep -q "text-xs text-muted-foreground" CostCentersTable.tsx` exits 0 (unit subtitle).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Centros de custo cadastro screen lists, creates, edits, and toggles cost centers per unit.</done>
</task>

<task type="auto">
  <name>Task 2: BankAccount table + dialog + route segment</name>
  <read_first>
    - src/components/config/UnitsManager.tsx (Table + Dialog pattern)
    - src/components/financeiro/TransactionModal.tsx (BRL money mask handleAmountBlur for saldo inicial)
    - .planning/phases/14-financeiro-cadastros-base/14-UI-SPEC.md §"BankAccountsTable", §"Bank Account Form Dialog", §"Page Structure /contas-correntes", §"Copywriting Contract"
  </read_first>
  <action>
`src/components/financeiro/BankAccountFormDialog.tsx` ('use client'): Dialog + RHF. Fields per UI-SPEC: nome/apelido (required), banco, agência (font-mono), conta (font-mono), saldo inicial (numeric, BRL mask — reuse the handleAmountBlur pattern from TransactionModal; parse to number for createBankAccount.saldoInicial). Submit → create/updateBankAccount; titles/buttons per Copywriting ("Nova Conta Corrente"/"Editar Conta Corrente"; "Salvar Conta Corrente"; "Cancelar"). No `.default()`.

`src/components/financeiro/BankAccountsTable.tsx` ('use client'): props `{ accounts; canEdit }`. Table columns per UI-SPEC: Nome/Apelido (`text-sm font-semibold`), Banco (`text-muted-foreground`), Agência/Conta (`font-mono tabular-nums text-sm`), Saldo Inicial (`text-sm font-semibold tabular-nums text-right` — formatted BRL), Ações (Edit). `!canEdit` hides actions. Empty state per UI-SPEC copy.

Route segment `contas-correntes/{page,loading,error}.tsx`:
- page.tsx (RSC): role→isAdmin; `listBankAccounts()`. PageHeader (title "Contas Correntes", action "Nova Conta Corrente" admin-only). Body: rounded-md border wrapper around BankAccountsTable.
- loading.tsx (3 row skeletons) + error.tsx per UI-SPEC.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `BankAccountsTable.tsx`, `BankAccountFormDialog.tsx` exist, start with `'use client'`.
    - `contas-correntes/page.tsx`, `loading.tsx`, `error.tsx` exist.
    - `grep -q "listBankAccounts" contas-correntes/page.tsx` exits 0.
    - `grep -q "Salvar Conta Corrente" BankAccountFormDialog.tsx` exits 0.
    - `grep -q "tabular-nums" BankAccountsTable.tsx` exits 0 (saldo column).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Contas correntes cadastro screen lists, creates, and edits bank accounts with NUMERIC saldo inicial.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — centros de custo + contas correntes (FCAD-01)</name>
  <action>Pause for human visual verification of the centros-de-custo and contas-correntes screens built in Tasks 1-2. No code changes — the human follows the steps below and approves or reports issues.</action>
  <what-built>Two tabular cadastros: Centros de Custo (per-unit, with seeded defaults) and Contas Correntes (with BRL saldo inicial).</what-built>
  <how-to-verify>
1. As admin, visit `/clinica/financeiro/centros-de-custo` — confirm each existing unit shows its seeded default CC (name = unit name, "Ativo").
2. Create a new CC ("Marketing") tied to a unit; confirm it appears with the unit subtitle.
3. Toggle a CC inactive; confirm the badge updates.
4. Visit `/clinica/financeiro/contas-correntes`; create "Conta Itaú" with saldo R$ 1.000,00; confirm it lists right-aligned as R$ 1.000,00.
5. As a read-only role (socio), confirm no create/edit controls.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → cost-center/bank Server Actions | Cadastro writes cross into admin-gated actions (Plan 04); a CC could be tied to another tenant's unit if not validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-16 | T (Tampering) | createCostCenter unitId from another tenant | mitigate | units Select is populated only with the tenant's own units (RLS-filtered listing); Plan 04 createCostCenter sets clinic_id from actor and the unit FK + RLS reject cross-tenant unit_id |
| T-14-17 | E (Elevation) | non-admin cadastro writes | mitigate | UI hides controls when !canEdit; Plan 04 admin role gate + RLS admin_write is the real boundary |
| T-14-18 | I (Info Disclosure) | bank account data of another tenant | mitigate | listBankAccounts runs under RLS clinic_id isolation |
</threat_model>

<verification>
- `npm run build` exits 0.
- Human visual checkpoint approves both cadastros.
</verification>

<success_criteria>
- FCAD-01: cost centers (per unit) and bank accounts cadastros operational.
- Seeded default CC per unit visible.
- Read-only roles cannot mutate.
</success_criteria>

<output>
After completion, create `.planning/phases/14-financeiro-cadastros-base/14-06-SUMMARY.md`
</output>
