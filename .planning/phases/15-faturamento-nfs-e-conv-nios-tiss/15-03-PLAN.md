---
phase: 15-faturamento-nfs-e-conv-nios-tiss
plan: 03
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - supabase/migrations/20260620000200_faturamento_os_tables.sql
  - supabase/migrations/20260620000300_faturamento_tiss_tables.sql
  - supabase/migrations/20260620000400_faturamento_rls.sql
  - src/lib/validators/service-order.ts
autonomous: true
requirements: [OS-01, OS-02, OS-03, CONV-01, CONV-02, CONV-03]
user_setup: []

must_haves:
  truths:
    - "service_orders has the D-27 OS enum, sequential numero per unit, partial unique on appointment_id (1 OS per appointment), and unique idempotency_key (D-30)"
    - "appointment_procedures links appointment×service×professional (OS-01 source, base for Phase 17)"
    - "nfse_records carries D-27 NFS-e enum + ISS fields + storage paths; charges gains service_order_id FK (regime caixa)"
    - "tiss_lotes/tiss_guides/tiss_guide_items model glosa-by-item (D-28) with D-27 TISS enum"
    - "next_os_number() SECURITY DEFINER returns atomic sequential OS number per unit (D-25)"
    - "RLS: SELECT clinic-scoped; write by role per D-18 with WITH CHECK on every policy"
  artifacts:
    - path: "supabase/migrations/20260620000200_faturamento_os_tables.sql"
      provides: "insurers, appointment_procedures, service_orders, service_order_items, nfse_records, next_os_number(), charges ALTER, deferred insurer_prices FK"
      contains: "CREATE TABLE public.service_orders"
    - path: "supabase/migrations/20260620000300_faturamento_tiss_tables.sql"
      provides: "tiss_lotes, tiss_guides, tiss_guide_items"
      contains: "CREATE TABLE public.tiss_guide_items"
    - path: "supabase/migrations/20260620000400_faturamento_rls.sql"
      provides: "RLS for all 10 new tables (D-18 role write)"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "src/lib/validators/service-order.ts"
      provides: "serviceOrderSchema, serviceOrderItemSchema, faturarOsSchema"
      exports: ["serviceOrderSchema", "serviceOrderItemSchema"]
  key_links:
    - from: "service_orders.appointment_id"
      to: "appointments.id"
      via: "partial UNIQUE index"
      pattern: "UNIQUE INDEX[\\s\\S]*appointment_id[\\s\\S]*WHERE appointment_id IS NOT NULL"
    - from: "tiss_guide_items.motivo_glosa_id"
      to: "glosa_motivos.id"
      via: "FK ON DELETE SET NULL"
      pattern: "REFERENCES public\\.glosa_motivos"
    - from: "charges.service_order_id"
      to: "service_orders.id"
      via: "ALTER TABLE ADD COLUMN FK"
      pattern: "ALTER TABLE public\\.charges"
---

<objective>
Wave 1 — create the OS domain and TISS domain tables plus RLS for ALL 10 new faturamento tables, and the service-order Zod validators. This is the core schema: OS state machine columns (D-10/D-27), sequential numbering (D-25), idempotency (D-30), procedure lines (OS-01), NFS-e records (OS-02), TISS guides/lotes/items with glosa-by-item (CONV-02/03/D-28), and charges→OS link (D-20 caixa path). Runs parallel with Plan 02 (no file overlap; both Wave 1).

Purpose: All OS/NFS-e/TISS side-effects in later waves write to these tables under role-scoped RLS.
Output: 3 migration files (OS tables, TISS tables, unified RLS) + service-order validator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md

<interfaces>
RLS helpers (project-wide): `get_my_tenant_id()`, `get_my_role()` SECURITY DEFINER.
D-18 write matrix:
  - service_orders / service_order_items / appointment_procedures: SELECT clinic-scoped; INSERT/UPDATE write by role IN ('dentist','receptionist','admin','superadmin','financeiro') — but to stay aligned with existing financial RLS strictness, gate write to IN ('receptionist','admin','superadmin','financeiro') and allow dentist INSERT of OS rascunho via the Server Action using admin client OR add 'dentist' to write role list. RESOLUTION: write role list = ('dentist','receptionist','admin','superadmin','financeiro') for service_orders (dentist creates rascunho on conclude); ('receptionist','admin','superadmin','financeiro') for faturar-affecting tables is enforced in the Server Action layer, not RLS, since RLS cannot express "only rascunho→faturada".
  - insurers / insurer_prices / unit_fiscal_config / glosa_motivos / nfse_records / tiss_*: write by role IN ('admin','superadmin','financeiro').
  - glosa_motivos SELECT: `clinic_id IS NULL OR clinic_id = get_my_tenant_id()` (shared seed + per-clinic).
Existing charges table: 20260606000100_financial_tables.sql — has patient_id; add service_order_id.
D-133: no Zod `.default()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OS tables migration (insurers, appointment_procedures, service_orders, items, nfse_records, next_os_number, charges ALTER)</name>
  <files>supabase/migrations/20260620000200_faturamento_os_tables.sql</files>
  <read_first>
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md §"Schema Design" lines 400-559 (insurers, appointment_procedures, service_orders, service_order_items, nfse_records DDL) + lines 312-333 (next_os_number) + Pitfall 6 (partial unique appointment_id)
    - supabase/migrations/20260619001100_financial_cadastros_tables.sql (column conventions)
    - supabase/migrations/20260606000100_financial_tables.sql (charges table — confirm column set before ALTER)
    - src/actions/charges.ts (confirm charges has no service_order_id yet)
  </read_first>
  <action>
Create migration with header comment. Tables EXACTLY per RESEARCH §Schema Design (use NUMERIC(12,2) money, NUMERIC(5,4) aliquota, TIMESTAMPTZ, clinic_id index on each):

1. `public.insurers` (D-26/CONV-01) — full DDL from RESEARCH lines 404-421 including connector_id UUID REFERENCES public.integration_connectors(id) ON DELETE SET NULL; status CHECK ('ativo','em_negociacao','inativo'); tiss_version DEFAULT '3.05.00'; prazo_pagamento_dias INT DEFAULT 30. Index idx_insurers_clinic.

2. `public.appointment_procedures` (D-09/OS-01) — RESEARCH lines 446-463: appointment_id REFERENCES appointments ON DELETE CASCADE; service_id REFERENCES services ON DELETE RESTRICT; professional_id REFERENCES professionals ON DELETE SET NULL; quantity INT DEFAULT 1; valor_unitario NUMERIC(12,2); desconto NUMERIC(12,2) DEFAULT 0; nota/dente/face TEXT. Indexes on clinic_id and appointment_id.

3. `public.service_orders` (D-10/11/12/25/27/30/OS-01) — RESEARCH lines 469-499 VERBATIM including: numero TEXT NOT NULL; pagador TEXT NOT NULL DEFAULT 'particular' CHECK (pagador IN ('particular','convenio')); insurer_id REFERENCES insurers ON DELETE RESTRICT; status TEXT NOT NULL DEFAULT 'rascunho' CHECK (status IN ('rascunho','faturada','cancelada')); desconto_total/acrescimo_total/total NUMERIC(12,2) DEFAULT 0; idempotency_key TEXT; faturada_at/cancelada_at TIMESTAMPTZ; cancel_reason TEXT; created_by REFERENCES users(id); UNIQUE (clinic_id, numero). Indexes: idx_service_orders_clinic, _patient, _status (clinic_id,status). Plus the TWO unique indexes:
   `CREATE UNIQUE INDEX idx_service_orders_appointment ON public.service_orders(appointment_id) WHERE appointment_id IS NOT NULL;` (Pitfall 6 — 1 OS per appointment)
   `CREATE UNIQUE INDEX idx_service_orders_idem ON public.service_orders(idempotency_key) WHERE idempotency_key IS NOT NULL;` (D-30)

4. `public.service_order_items` (D-25/D-29) — RESEARCH lines 506-525: service_order_id REFERENCES service_orders ON DELETE CASCADE; service_id ON DELETE RESTRICT; professional_id ON DELETE SET NULL (D-29 repasse base); description TEXT NOT NULL; tuss_code TEXT; quantity INT DEFAULT 1; valor_unitario/desconto/valor_total NUMERIC(12,2); dente/face TEXT; account_id REFERENCES chart_of_accounts ON DELETE SET NULL; cost_center_id REFERENCES cost_centers ON DELETE SET NULL (FCAD-02 classification). Indexes on clinic_id and service_order_id.

5. `public.nfse_records` (D-17/OS-02) — RESEARCH lines 532-558: service_order_id REFERENCES service_orders ON DELETE RESTRICT; provider_ref TEXT; numero/serie TEXT; valor_servicos/aliquota_iss(NUMERIC(5,4))/valor_iss/valor_liquido NUMERIC; iss_retido BOOLEAN DEFAULT false; tomador_nome TEXT; status TEXT NOT NULL DEFAULT 'processando' CHECK (status IN ('processando','emitida','cancelada','erro')); error_message TEXT; xml_storage_path/pdf_storage_path TEXT; emitida_at/cancelada_at TIMESTAMPTZ. Indexes idx_nfse_records_clinic, _os, _status.

6. `next_os_number(p_unit_id UUID)` function — per D-25/A1. Use a per-unit counter on `unit_fiscal_config.proximo_numero_rps` is the RPS number; the OS number uses a SEPARATE counter. Create a small helper table OR reuse unit_fiscal_config. RESOLUTION (Claude's Discretion): add a dedicated counter column is overkill — implement next_os_number as: `UPDATE public.unit_fiscal_config SET proximo_numero_rps = proximo_numero_rps WHERE ...` is wrong (RPS ≠ OS). Instead create the function to atomically allocate from a new `public.unit_os_counters (unit_id UUID PRIMARY KEY REFERENCES units(id) ON DELETE CASCADE, clinic_id UUID NOT NULL, last_os_number INT NOT NULL DEFAULT 0)` table (create it in THIS file). Function body:
```sql
CREATE OR REPLACE FUNCTION public.next_os_number(p_unit_id UUID)
RETURNS TEXT LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
DECLARE next_num INT;
BEGIN
  INSERT INTO public.unit_os_counters (unit_id, clinic_id, last_os_number)
  SELECT u.id, u.clinic_id, 0 FROM public.units u WHERE u.id = p_unit_id
  ON CONFLICT (unit_id) DO NOTHING;
  UPDATE public.unit_os_counters SET last_os_number = last_os_number + 1
  WHERE unit_id = p_unit_id RETURNING last_os_number INTO next_num;
  RETURN 'OS-' || LPAD(next_num::TEXT, 6, '0');
END; $$;
```

7. Charges link (RESEARCH Open Question 2 / D-20 caixa): `ALTER TABLE public.charges ADD COLUMN IF NOT EXISTS service_order_id UUID REFERENCES public.service_orders(id) ON DELETE SET NULL;` + `CREATE INDEX IF NOT EXISTS idx_charges_service_order ON public.charges(service_order_id);`

8. Deferred FK from Plan 02 (insurer_prices.insurer_id → insurers): at end of file add `ALTER TABLE public.insurer_prices ADD CONSTRAINT fk_insurer_prices_insurer FOREIGN KEY (insurer_id) REFERENCES public.insurers(id) ON DELETE CASCADE;` with a comment referencing Plan 02.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/faturamento/migrations-phase15.test.ts -t "service_orders|nfse|appointment_procedures|insurers" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE public\." supabase/migrations/20260620000200_faturamento_os_tables.sql` returns 6 (insurers, appointment_procedures, service_orders, service_order_items, nfse_records, unit_os_counters)
    - Contains `CHECK (status IN ('rascunho', 'faturada', 'cancelada'))` and `CHECK (pagador IN ('particular', 'convenio'))` and `CHECK (status IN ('processando', 'emitida', 'cancelada', 'erro'))`
    - Contains both partial unique indexes (appointment_id and idempotency_key) with `WHERE ... IS NOT NULL`
    - Contains `CREATE OR REPLACE FUNCTION public.next_os_number` with `SECURITY DEFINER` and `LPAD`
    - Contains `ALTER TABLE public.charges ADD COLUMN IF NOT EXISTS service_order_id`
    - Contains `ADD CONSTRAINT fk_insurer_prices_insurer`
  </acceptance_criteria>
  <done>6 OS-domain tables + atomic numbering + charges link + deferred FK created; OS migration test rows GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: TISS tables migration + service-order validators</name>
  <files>supabase/migrations/20260620000300_faturamento_tiss_tables.sql, src/lib/validators/service-order.ts</files>
  <read_first>
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md §"Schema Design" lines 561-643 (tiss_lotes, tiss_guides, tiss_guide_items DDL) + Pitfall 5 (item-level glosa)
    - src/lib/validators/charge.ts (isMoney2dp)
    - src/lib/validators/service.ts (created in Plan 02 — for isMoney2dp reuse, may not exist yet at read time if parallel; re-declare locally)
  </read_first>
  <action>
**TISS migration** (20260620000300) — 3 tables VERBATIM per RESEARCH §Schema Design:

1. `public.tiss_lotes` (D-22) — lines 565-582: insurer_id REFERENCES insurers ON DELETE RESTRICT; numero TEXT NOT NULL; competencia TEXT NOT NULL; status TEXT NOT NULL DEFAULT 'em_analise' CHECK (status IN ('em_analise','autorizada','glosada','paga','recurso')); protocolo TEXT; valor_total NUMERIC(12,2) DEFAULT 0; data_envio TIMESTAMPTZ; provider_ref TEXT; xml_storage_path TEXT. Indexes idx_tiss_lotes_clinic, _insurer.

2. `public.tiss_guides` (D-13/22/CONV-02) — lines 589-615: lote_id REFERENCES tiss_lotes ON DELETE SET NULL; service_order_id REFERENCES service_orders ON DELETE RESTRICT; insurer_id REFERENCES insurers ON DELETE RESTRICT; patient_id REFERENCES patients ON DELETE RESTRICT; numero_guia TEXT NOT NULL; numero_carteira TEXT; registro_ans TEXT; status CHECK (same 5-value TISS enum); valor_total/valor_autorizado/valor_glosado/valor_pago NUMERIC(12,2); protocolo/provider_ref/xml_storage_path TEXT. Indexes idx_tiss_guides_clinic, _lote, _os, _status.

3. `public.tiss_guide_items` (D-28/CONV-03) — lines 621-642: guide_id REFERENCES tiss_guides ON DELETE CASCADE; service_order_item_id REFERENCES service_order_items ON DELETE SET NULL; tuss_code TEXT; description TEXT NOT NULL; quantity INT DEFAULT 1; dente/face TEXT; valor_unitario/valor_total/valor_glosado NUMERIC(12,2); motivo_glosa_id UUID REFERENCES glosa_motivos ON DELETE SET NULL; glosa_status TEXT CHECK (glosa_status IN ('pendente','glosada','em_recurso','paga')); recurso_texto TEXT; recurso_at TIMESTAMPTZ. Indexes idx_tiss_guide_items_clinic, _guide.

**src/lib/validators/service-order.ts** — Zod v3, no `.default()`:
  - `serviceOrderItemSchema`: serviceId z.string().uuid().optional().nullable(); professionalId z.string().uuid().optional().nullable(); description z.string().min(1,'Descrição obrigatória').max(300); tussCode z.string().max(20).optional().nullable(); quantity z.number().int().min(1); valorUnitario z.number().refine(isMoney2dp,'Valor inválido'); desconto z.number().refine(isMoney2dp); dente/face z.string().max(10).optional().nullable(); accountId/costCenterId z.string().uuid().optional().nullable().
  - `serviceOrderSchema`: patientId z.string().uuid().optional().nullable(); unitId z.string().uuid().optional().nullable(); pagador z.enum(['particular','convenio']); insurerId z.string().uuid().optional().nullable(); descontoTotal z.number().refine(isMoney2dp); acrescimoTotal z.number().refine(isMoney2dp); notes z.string().max(1000).optional().nullable(); items z.array(serviceOrderItemSchema). Add a `.refine` enforcing: when pagador==='convenio' then insurerId must be present (CONV path needs operadora) — message 'Operadora obrigatória para convênio'.
  - `faturarOsSchema`: osId z.string().uuid(); billingType z.enum(['PIX','BOLETO','CREDIT_CARD']).optional().nullable(); installmentCount z.number().int().min(1).max(21).optional().nullable() (D-21 up to 21x).
  - Export inferred types. Re-declare `isMoney2dp` locally (same impl as charge.ts) to avoid cross-plan import ordering issues.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/faturamento/migrations-phase15.test.ts -t "tiss" 2>&1 | tail -8 && npx tsc --noEmit 2>&1 | grep "service-order.ts" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE public\." supabase/migrations/20260620000300_faturamento_tiss_tables.sql` returns 3
    - Contains `CHECK (status IN ('em_analise', 'autorizada', 'glosada', 'paga', 'recurso'))` (both tiss_lotes and tiss_guides)
    - tiss_guide_items contains `motivo_glosa_id UUID REFERENCES public.glosa_motivos` and `glosa_status TEXT CHECK (glosa_status IN ('pendente', 'glosada', 'em_recurso', 'paga'))`
    - serviceOrderSchema enforces insurerId-required-when-convenio via `.refine`
    - faturarOsSchema caps installmentCount at 21
    - `grep -c "\.default(" src/lib/validators/service-order.ts` returns 0
  </acceptance_criteria>
  <done>3 TISS tables with glosa-by-item created; service-order validators compile with pagador/convênio refine and 21x cap.</done>
</task>

<task type="auto">
  <name>Task 3: Unified RLS for all 10 faturamento tables (D-18)</name>
  <files>supabase/migrations/20260620000400_faturamento_rls.sql</files>
  <read_first>
    - supabase/migrations/20260619001200_financial_cadastros_rls.sql (exact RLS policy structure: ENABLE RLS, tenant_read SELECT, role write with USING + WITH CHECK)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md §"Security Domain" (D-18 role matrix, WITH CHECK requirement)
  </read_first>
  <action>
Create one RLS migration enabling RLS + policies for all 10 new tables (services, insurer_prices, unit_fiscal_config, glosa_motivos, insurers, appointment_procedures, service_orders, service_order_items, nfse_records, tiss_lotes, tiss_guides, tiss_guide_items, unit_os_counters). Mirror the phase-14 RLS file exactly (header comment, one ENABLE + 2 policies per table). Every write policy MUST have BOTH USING and WITH CHECK (CLAUDE.md).

Per-table policy rules:
  - SELECT (all tables): `clinic_id = get_my_tenant_id()`.
  - EXCEPTION glosa_motivos SELECT: `clinic_id IS NULL OR clinic_id = get_my_tenant_id()` (shared seed).
  - Write (FOR ALL, USING + WITH CHECK = clinic_id match AND role):
    - service_orders, service_order_items, appointment_procedures: role IN ('dentist','receptionist','admin','superadmin','financeiro') (D-18: dentist creates rascunho; recepção/admin faturam — finer faturar gate is in Server Action).
    - insurers, insurer_prices, unit_fiscal_config, glosa_motivos, nfse_records, tiss_lotes, tiss_guides, tiss_guide_items, unit_os_counters: role IN ('admin','superadmin','financeiro').
  - glosa_motivos write WITH CHECK must also block writing system rows: require `clinic_id = get_my_tenant_id()` (so a clinic can only add its own rows, never edit NULL-clinic seed).
Use the role-name strings that exist in this project's RBAC (confirm against proxy.ts role set: includes 'dentist','receptionist','admin','superadmin','financeiro'). If 'financeiro' is not a literal role string in the project, substitute the actual financial role used in 20260619001200 (it used only 'admin','superadmin') — RESOLUTION: match phase-14 (admin/superadmin) for the cadastro tables and add the dentist/receptionist write only on the three OS tables. Read 20260619001200 to confirm exact role strings before writing.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/faturamento/migrations-phase15.test.ts -t "RLS|rls|policy" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260620000400_faturamento_rls.sql` returns ≥ 12
    - `grep -c "WITH CHECK" supabase/migrations/20260620000400_faturamento_rls.sql` ≥ 12 (every write policy)
    - Contains `clinic_id IS NULL OR clinic_id = get_my_tenant_id()` for glosa_motivos read
    - service_orders write policy references `get_my_role() IN` with at least 'admin' and 'superadmin'
    - No write policy lacks WITH CHECK (manual scan: each FOR ALL has both USING and WITH CHECK)
  </acceptance_criteria>
  <done>RLS enabled on all 10+ tables with clinic isolation + role-gated writes + WITH CHECK everywhere; migration RLS test rows GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| authenticated client → service_orders/tiss/nfse rows | RLS enforces clinic_id isolation + role write |
| cross-tenant read attempt | get_my_tenant_id() blocks |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Elevation of Privilege | RLS cross-tenant OS read/write | mitigate | clinic_id = get_my_tenant_id() on SELECT; USING+WITH CHECK on every write policy |
| T-15-08 | Tampering | glosa_motivos shared seed edit | mitigate | write WITH CHECK requires clinic_id = get_my_tenant_id() — NULL-clinic system rows immutable |
| T-15-09 | Tampering | duplicate OS per appointment (race) | mitigate | partial UNIQUE index on appointment_id WHERE NOT NULL (DB backstop, Pitfall 6) |
| T-15-10 | Tampering | duplicate faturar (double-click) | mitigate | UNIQUE index on idempotency_key (D-30); CAS in Server Action (Plan 04) |
| T-15-11 | Spoofing | next_os_number cross-unit | mitigate | SECURITY DEFINER fixed search_path; allocates only for passed unit_id under tenant RLS context |
</threat_model>

<verification>
- All migration-phase15.test.ts table/enum/RLS rows GREEN after Plans 02+03.
- `npx vitest run src/__tests__/faturamento/migrations-phase15.test.ts` exits 0.
</verification>

<success_criteria>
10 tables defined (4 in Plan 02, 6+ here), unified RLS with WITH CHECK, atomic OS numbering, charges link, glosa-by-item, service-order validators compile.
</success_criteria>

<output>
After completion, create `.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-03-SUMMARY.md`
</output>
