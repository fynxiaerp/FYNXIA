---
phase: 15-faturamento-nfs-e-conv-nios-tiss
plan: 09
type: execute
wave: 4
depends_on: [15-05, 15-07]
files_modified:
  - src/app/(dashboard)/clinica/financeiro/faturamento/convenios/page.tsx
  - src/app/(dashboard)/clinica/financeiro/faturamento/operadoras/page.tsx
  - src/app/(dashboard)/clinica/financeiro/faturamento/operadoras/[id]/precos/page.tsx
  - src/app/(dashboard)/clinica/financeiro/faturamento/glosas/page.tsx
  - src/components/financeiro/ConveniosKpiRow.tsx
  - src/components/financeiro/TissGuidesTable.tsx
  - src/components/financeiro/InsurerTable.tsx
  - src/components/financeiro/InsurerFormDialog.tsx
  - src/components/financeiro/GlosaTable.tsx
  - src/components/financeiro/GlosaRecursoSheet.tsx
  - src/components/financeiro/InsurerPricesTable.tsx
autonomous: true
requirements: [CONV-01, CONV-02, CONV-03]
user_setup: []

must_haves:
  truths:
    - "Convênios screen mirrors prototype (5 KPIs, donut + operadoras table, guias TISS table) with glosa rate threshold coloring at 6%"
    - "Operadoras screen lists insurers + Dialog CRUD (admin/financeiro gated); price table sub-page edits insurer_prices per cell (CONV-01)"
    - "Glosas screen lists glosa items with motivo ANS badge; Registrar Recurso Sheet submits per-item recurso (CONV-03)"
    - "Fechar lote groups guias and stores protocolo (CONV-02)"
    - "Patient names first_name+last_initial; no raw storage_path client-side (LGPD)"
  artifacts:
    - path: "src/app/(dashboard)/clinica/financeiro/faturamento/convenios/page.tsx"
      provides: "Convênios RSC (Screen 3)"
      min_lines: 30
    - path: "src/app/(dashboard)/clinica/financeiro/faturamento/operadoras/page.tsx"
      provides: "Operadoras RSC (Screen 4)"
      min_lines: 25
    - path: "src/app/(dashboard)/clinica/financeiro/faturamento/glosas/page.tsx"
      provides: "Glosas RSC (Screen 5)"
      min_lines: 25
    - path: "src/components/financeiro/GlosaRecursoSheet.tsx"
      provides: "Per-item recurso slide-over"
      min_lines: 30
  key_links:
    - from: "InsurerFormDialog"
      to: "createInsurer/updateInsurer"
      via: "server action"
      pattern: "createInsurer"
    - from: "GlosaRecursoSheet submit"
      to: "registrarRecurso"
      via: "server action"
      pattern: "registrarRecurso"
    - from: "convenios page Fechar lote"
      to: "fecharLote"
      via: "server action"
      pattern: "fecharLote"
---

<objective>
Wave 4 — build the Convênios (Screen 3), Operadoras + price table (Screen 4), and Glosas (Screen 5) UI under `/clinica/financeiro/faturamento/`. Follows 15-UI-SPEC.md exactly (5-card KPI row, donut, operadoras table, TISS badge contract, glosa 6% threshold coloring, recurso Sheet). Wires to Plan 07 (insurers/tiss actions) and Plan 05 (listOs for guia source).

Purpose: CONV-01 (operadora cadastro + preços), CONV-02 (guias + lote), CONV-03 (glosa + recurso UI).
Output: 4 pages + 7 components. Runs parallel with Plan 08 (distinct files; does NOT touch financeiro/page.tsx).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-UI-SPEC.md

<interfaces>
Actions (Plan 07): listInsurers(), createInsurer(input), updateInsurer(id,input), deactivateInsurer(id), getGuias(filters), getGlosas(filters), fecharLote(insurerId, competencia), registrarGlosa(itemId, motivoId, valorGlosado), registrarRecurso(itemId, texto, anexoPath?).
Actions (Plan 05): listInsurerPrices(insurerId), upsertInsurerPrice(input), listServices() (for price table rows).
Validators: insurerSchema (Plan 02) for the Dialog form.
Reuse: PageHeader, EmptyState, ErrorState, formatBRL, DonutChart (src/components/prototipos/charts.tsx), KpiCard, CashFlowFilters nuqs pattern.
Prototype reference (visual parity): src/app/(dashboard)/clinica/prototipos/convenios/page.tsx (5 KPIs, donut, operadoras table, guias table, glosa threshold line 108).
Badge contract: TISS paga=default, autorizada=secondary, em_analise=outline, glosada=destructive, recurso=outline. Operadora ativo=secondary, em_negociacao=outline.
Glosa rate coloring: `glosaRate >= 6 ? 'text-destructive' : 'text-muted-foreground'`.
Operadoras CRUD = Dialog (form-heavy), NOT Sheet. Glosa recurso = Sheet. Desativar = AlertDialog.
Access gate Operadoras: admin/financeiro only (D-18) — server-side role check filters/blocks.
nuqs keys: ?month, ?status, ?operadora=<id>.
LGPD: paciente first_name+last_initial; CNPJ masked input.
D-133: insurer form uses RHF defaultValues (tissVersion '3.05.00', status 'ativo', prazo 30); no Zod .default().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convênios screen + Operadoras cadastro + price table</name>
  <files>src/app/(dashboard)/clinica/financeiro/faturamento/convenios/page.tsx, src/app/(dashboard)/clinica/financeiro/faturamento/operadoras/page.tsx, src/app/(dashboard)/clinica/financeiro/faturamento/operadoras/[id]/precos/page.tsx, src/components/financeiro/ConveniosKpiRow.tsx, src/components/financeiro/TissGuidesTable.tsx, src/components/financeiro/InsurerTable.tsx, src/components/financeiro/InsurerFormDialog.tsx, src/components/financeiro/InsurerPricesTable.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/prototipos/convenios/page.tsx (mirror EXACTLY: 5 KPIs, DonutChart, operadoras table, guias table, glosa threshold line 108)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-UI-SPEC.md §"Screen 3", §"Screen 4" + §"Copywriting Contract" Convênios/Operadoras sections
    - src/actions/insurers.ts + src/actions/tiss.ts (Plan 07) + src/actions/services.ts (Plan 05 listServices/listInsurerPrices/upsertInsurerPrice)
    - src/lib/validators/insurer.ts (insurerSchema)
    - src/components/prototipos/charts.tsx (DonutChart, KpiCard)
  </read_first>
  <action>
**convenios/page.tsx** (Screen 3): RSC. PageHeader title "Convênios / Planos", breadcrumb Financeiro > Faturamento > Convênios, action Button "Nova guia TISS" (+ a "Fechar lote" action per copy). Read filters (month/operadora/status). Layout mirrors prototype: `<ConveniosKpiRow />` (5 cards: "Convênios ativos", "Guias no mês", "Faturado (convênios)", "Glosa" with "{%} média" sub, "Guias em análise"), 2-col grid [DonutChart "Faturamento por convênio" + Operadoras summary table (Convênio+status inline, Guias right tabular-nums, Faturado right, Glosa% colored per 6% threshold)], full-width `<TissGuidesTable />` "Guias TISS recentes". "Fechar lote" → AlertDialog confirm per copy → fecharLote(insurerId, competencia). EmptyState heading "Nenhuma guia TISS emitida".

**ConveniosKpiRow.tsx**: 5 KPI cards (2 cols mobile, 5 lg). Glosa card sub-label colored per `glosaRate >= 6` threshold.

**TissGuidesTable.tsx** ('use client'): columns Guia (numero tabular-nums), Paciente (first_name+last_initial), Convênio, Procedimento, Valor (formatBRL right), Status Badge (TISS contract). Ações: "Ver guia", "Registrar recurso" (if glosada → links to glosas screen / opens recurso).

**operadoras/page.tsx** (Screen 4): RSC. ACCESS GATE admin/financeiro only — server-side role check; if unauthorized render an access-denied notice (do not 500). PageHeader title "Operadoras de Convênio", action Button "Cadastrar Operadora". listInsurers() → `<InsurerTable />`: columns Nome, CNPJ, Registro ANS, Versão TISS, Prazo pagamento, Status Badge (operadora contract), Ações ("Editar", "Ver tabela de preços" → /operadoras/[id]/precos, "Desativar" → AlertDialog per copy → deactivateInsurer). EmptyState heading "Nenhuma operadora cadastrada" + CTA.

**InsurerFormDialog.tsx** ('use client'): shadcn Dialog (NOT Sheet). RHF + zodResolver(insurerSchema). Fields: nome, CNPJ (masked), Registro ANS, Versão TISS, prazo em dias (number), contato email/phone, status Select. defaultValues supply tissVersion '3.05.00'/status 'ativo'/prazo 30 (NO Zod .default()). Submit → createInsurer or updateInsurer. Titles "Nova Operadora"/"Editar Operadora", save CTA "Salvar".

**operadoras/[id]/precos/page.tsx**: RSC reading insurerId from params. listServices() + listInsurerPrices(insurerId) → `<InsurerPricesTable />` showing service × valor convênio with inline per-cell edit (optimistic) → upsertInsurerPrice({insurerId, serviceId, valor}). Use Accordion or table per UI-SPEC.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "convenios|operadoras|Insurer|TissGuides|ConveniosKpi" | head && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - convenios/page.tsx renders ConveniosKpiRow (5 cards) + DonutChart + operadoras table + TissGuidesTable per prototype
    - Glosa% uses `glosaRate >= 6 ? 'text-destructive' : 'text-muted-foreground'`
    - operadoras/page.tsx gated to admin/financeiro (grep role check); InsurerFormDialog uses Dialog + zodResolver(insurerSchema)
    - InsurerFormDialog has NO Zod .default() (grep returns 0 in component + validator); defaults via RHF defaultValues
    - precos page calls listInsurerPrices + upsertInsurerPrice
    - Fechar lote calls fecharLote inside an AlertDialog
    - TISS/operadora badges use the UI-SPEC variants; Paciente masked
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Convênios + Operadoras + price table screens wired; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Glosas screen + Recurso Sheet</name>
  <files>src/app/(dashboard)/clinica/financeiro/faturamento/glosas/page.tsx, src/components/financeiro/GlosaTable.tsx, src/components/financeiro/GlosaRecursoSheet.tsx</files>
  <read_first>
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-UI-SPEC.md §"Screen 5" + §"Copywriting Contract" Glosas section + §"Interaction Patterns" (Sheet recurso)
    - src/app/(dashboard)/clinica/prototipos/convenios/page.tsx (glosa visual reference)
    - src/actions/tiss.ts (getGlosas/registrarGlosa/registrarRecurso from Plan 07)
    - src/components/prototipos/charts.tsx (KpiCard)
  </read_first>
  <action>
**glosas/page.tsx** (Screen 5): RSC. PageHeader title "Tratamento de Glosas", breadcrumb Financeiro > Faturamento > Glosas (no primary CTA — read/action only). Read filters (operadora/status/month). getGlosas(filters). Layout: KPI row (3 cards: "Glosas pendentes", "Valor glosado" formatBRL, "Em recurso"), filter bar (Operadora Select + Status Select + month nuqs), `<GlosaTable />`. EmptyState heading "Nenhuma glosa registrada" + body per copy.

**GlosaTable.tsx** ('use client'): columns Guia (numero tabular-nums), Paciente (first_name+last_initial), Operadora, Procedimento, Valor glosado (formatBRL right, text-destructive), Motivo ANS (Badge with codigo+descricao), Status, Ações ("Registrar Recurso" per glosada row → opens GlosaRecursoSheet). Status subset em_analise/glosada/recurso/paga with TISS badge variants.

**GlosaRecursoSheet.tsx** ('use client'): shadcn Sheet. Title "Recurso — Guia #[numero]". Body: preview do item glosado (description, valor glosado, motivo ANS), Textarea "Motivo do recurso" (label + placeholder per copy), optional file input (anexo). Submit "Enviar recurso" → registrarRecurso(itemId, texto, anexoPath?). On success revalidate('/clinica/financeiro/faturamento/glosas') + close. The recurso updates the item glosa_status to em_recurso and the guide status derives to recurso (Plan 07) — surface the new status badge after refresh.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "glosas|Glosa" | head && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - glosas/page.tsx renders 3 KPI cards + filter bar + GlosaTable; calls getGlosas
    - GlosaTable shows Motivo ANS badge (codigo+descricao) and Valor glosado in text-destructive; Paciente masked
    - "Registrar Recurso" opens GlosaRecursoSheet (Sheet, not Dialog)
    - GlosaRecursoSheet submit calls registrarRecurso with the item id and motivo text
    - Recurso field uses copy "Motivo do recurso" label + the specified placeholder
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Glosas screen + per-item recurso Sheet wired to registrarRecurso; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → registrarRecurso/registrarGlosa | User edits glosa/recurso affecting expected receivable |
| operadora screen → insurer CRUD | Role-gated cadastro |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-35 | Elevation of Privilege | non-admin accessing operadoras cadastro | mitigate | operadoras page server-side role gate (admin/financeiro per D-18); RLS WITH CHECK backstop |
| T-15-36 | Information Disclosure | patient identity in guias/glosas | mitigate | paciente first_name+last_initial only (LGPD) |
| T-15-37 | Tampering | client sets glosa/recurso on wrong item | mitigate | actions validate item belongs to clinic; valor re-derived server-side (Plan 07) |
| T-15-38 | Tampering | recurso flips whole guide | mitigate | per-item glosa_status; guide status derived server-side (Plan 07 deriveGuideStatus) — UI reflects only |
</threat_model>

<verification>
- `npm run build` green.
- Manual: convênio OS faturada → guia appears → fechar lote → protocolo; glosa registered → recurso Sheet → status updates.
</verification>

<success_criteria>
CONV-01/02/03 screens functional, prototype-parity, role-gated operadoras, per-item glosa/recurso UI; build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-09-SUMMARY.md`
</output>
