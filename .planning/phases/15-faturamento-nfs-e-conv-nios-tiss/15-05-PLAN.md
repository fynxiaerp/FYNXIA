---
phase: 15-faturamento-nfs-e-conv-nios-tiss
plan: 05
type: execute
wave: 3
depends_on: [15-04]
files_modified:
  - src/lib/faturamento/os-math.ts
  - src/actions/service-orders.ts
  - src/actions/services.ts
  - src/actions/appointments.ts
autonomous: true
requirements: [OS-01, OS-03]
user_setup: []

must_haves:
  truths:
    - "Marking an appointment 'concluido' auto-creates exactly one OS rascunho with the patient/dentist/unit + procedure lines (OS-01)"
    - "faturarOs enforces rascunho→faturada via CAS (one winner on double-click), is idempotent, and is a no-op on re-call (D-30)"
    - "faturarOs particular path calls createCharge (Asaas, up to 21x); convênio path creates an insurer receivable + a TISS guide, never an Asaas charge (OS-03)"
    - "OS total = sum(item.valor_total) − desconto_total + acrescimo_total via integer-cent math (D-25)"
    - "OS line items snapshot service_order_items carry professional_id (D-29 repasse base) + account_id/cost_center_id classification (FCAD-02)"
  artifacts:
    - path: "src/lib/faturamento/os-math.ts"
      provides: "computeOsTotal, isValidOsTransition pure helpers"
      exports: ["computeOsTotal", "isValidOsTransition"]
    - path: "src/actions/service-orders.ts"
      provides: "createOs, faturarOs, cancelarOs, getOs, listOs"
      exports: ["faturarOs", "createOs"]
    - path: "src/actions/services.ts"
      provides: "listServices, createService, updateService, listInsurerPrices"
      exports: ["listServices", "createService"]
  key_links:
    - from: "src/actions/appointments.ts (status→concluido)"
      to: "service_orders insert (rascunho)"
      via: "createOsDraftFromAppointment"
      pattern: "createOsDraftFromAppointment"
    - from: "faturarOs particular"
      to: "createCharge"
      via: "import from src/actions/charges"
      pattern: "createCharge"
    - from: "faturarOs"
      to: "service_orders CAS update"
      via: ".eq('status','rascunho')"
      pattern: "\\.eq\\('status', 'rascunho'\\)"
---

<objective>
Wave 3 — build the OS domain: pure math/transition helpers, the `service-orders.ts` Server Actions (createOs, faturarOs with CAS+idempotency, cancelarOs via Phase 10 alçada, getOs/listOs), the `services.ts` catalog actions, and the auto-OS-creation hook on `appointment.status='concluido'`. faturarOs orchestrates the branch: particular → createCharge + (delegate NFS-e to Plan 06's `emitirNfseForOs`); convênio → insurer receivable + (delegate guide to Plan 07's `criarGuiaForOs`). The NFS-e/TISS orchestrators are defined as contracts here and implemented in Plans 06/07.

Purpose: OS-01 (auto OS + procedures) and OS-03 (forma de pagamento gera parcelas) — the spine of the phase.
Output: os-math lib + 2 action files + appointments.ts extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md
@.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-CONTEXT.md

<interfaces>
From src/actions/charges.ts:
```typescript
export async function createCharge(input: ChargeInput): Promise<{ success: boolean; chargeId?: string; error?: string }>
// ChargeInput (charge.ts): { patientId, value, description, billingType:'PIX'|'BOLETO'|'CREDIT_CARD', installmentCount, dueDate, ... }
```
From src/lib/integrations/worker.ts — CAS claim idiom: `.update({...}).eq('id', id).eq('status','pending').select('id')` → 0 rows = lost race.
From src/lib/validators/service-order.ts (Plan 03): serviceOrderSchema, serviceOrderItemSchema, faturarOsSchema.
DB function: `next_os_number(p_unit_id UUID)` → 'OS-000001' (Plan 03).
Phase 10 alçada (reuse for cancelarOs faturada — D-19): src/actions/approval-actions.ts (createApprovalRequest by alçada) + audit trail.

CONTRACTS this plan DEFINES for Plans 06/07 (they implement; faturarOs calls):
```typescript
// Plan 06 implements in src/actions/nfse.ts:
export async function emitirNfseForOs(osId: string): Promise<{ success: boolean; nfseId?: string; error?: string }>
// Plan 07 implements in src/actions/tiss.ts:
export async function criarGuiaForOs(osId: string): Promise<{ success: boolean; guideId?: string; error?: string }>
```
faturarOs imports these. Until Plans 06/07 land, import will resolve at runtime only when those files exist; faturarOs guards with a dynamic import + try/catch so the particular createCharge path is testable independently.
D-133: no Zod .default(). isMoney2dp from charge.ts.
LGPD: patient names rendered first_name + last_initial in any returned list shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OS math + state machine helpers + services actions</name>
  <files>src/lib/faturamento/os-math.ts, src/actions/services.ts</files>
  <behavior>
    - computeOsTotal([{valorTotal:100},{valorTotal:50}], 20, 0) === 130
    - computeOsTotal uses integer-cent math (no float drift): computeOsTotal([{valorTotal:0.1},{valorTotal:0.2}],0,0) === 0.3
    - isValidOsTransition('rascunho','faturada') === true; ('faturada','rascunho') === false; ('cancelada','faturada') === false
    - listServices returns clinic services; createService inserts a service row scoped to tenant
  </behavior>
  <read_first>
    - src/__tests__/faturamento/service-orders.test.ts (the RED assertions — implement to GREEN)
    - src/actions/chart-of-accounts.ts (Server Action structure: getUser, tenant resolution, zod parse, supabase insert, revalidatePath)
    - src/lib/validators/service.ts (serviceSchema from Plan 02)
    - src/lib/validators/charge.ts (isMoney2dp)
  </read_first>
  <action>
**src/lib/faturamento/os-math.ts** (pure, no 'use server', importable by tests + actions):
  - `export function computeOsTotal(items: { valorTotal: number }[], descontoTotal: number, acrescimoTotal: number): number` — integer-cent: `const cents = items.reduce((s,i)=>s+Math.round(i.valorTotal*100),0) - Math.round(descontoTotal*100) + Math.round(acrescimoTotal*100); return cents/100;` (D-25).
  - `export function computeItemTotal(valorUnitario: number, quantity: number, desconto: number): number` — `(Math.round(valorUnitario*100)*quantity - Math.round(desconto*100))/100`.
  - `const VALID_OS_TRANSITIONS: Record<string,string[]> = { rascunho:['faturada','cancelada'], faturada:['cancelada'], cancelada:[] }` and `export function isValidOsTransition(from: string, to: string): boolean` returning membership.

**src/actions/services.ts** ('use server' + import 'server-only'): mirror chart-of-accounts.ts structure (getUser, resolve tenant via users.role/clinic, zod parse with serviceSchema/insurerPriceSchema, insert/update, revalidatePath('/clinica/financeiro/faturamento')):
  - `listServices()` → { success, services } (clinic-scoped, ativo + inactive both returned for cadastro).
  - `createService(input)` → validates serviceSchema, inserts into public.services with clinic_id.
  - `updateService(id, input)`, `deactivateService(id)`.
  - `listInsurerPrices(insurerId)` and `upsertInsurerPrice(input)` (insurerPriceSchema) — for operadora price table (CONV-01 / D-06).
Return shapes use plain serializable objects.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/faturamento/service-orders.test.ts -t "computeOsTotal|isValidOsTransition" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - computeOsTotal and isValidOsTransition exported from src/lib/faturamento/os-math.ts
    - service-orders.test.ts math+transition assertions GREEN
    - src/actions/services.ts exports listServices, createService, updateService, listInsurerPrices, upsertInsurerPrice and contains `'use server'`
    - `grep -c "import 'server-only'" src/actions/services.ts` returns 1
    - tsc clean for both files
  </acceptance_criteria>
  <done>OS math + transitions GREEN; services catalog actions compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: service-orders.ts — createOs, faturarOs (CAS+idempotency), cancelarOs, getOs/listOs</name>
  <files>src/actions/service-orders.ts</files>
  <behavior>
    - faturarOs on an OS already 'faturada' returns { success:true } and does NOT call createCharge again (idempotent, D-30)
    - faturarOs where the CAS update returns 0 rows returns an error mentioning race/corrida
    - faturarOs pagador='particular' calls createCharge exactly once
    - faturarOs pagador='convenio' does NOT call createCharge; inserts an insurer receivable + calls criarGuiaForOs
  </behavior>
  <read_first>
    - src/__tests__/faturamento/service-orders.test.ts (RED idempotency/CAS/branch assertions)
    - src/actions/charges.ts (createCharge signature + idempotency style)
    - src/actions/lab-orders.ts (setLabOrderCost double-post guard — TOCTOU re-fetch pattern, STATE.md D-13 decisions)
    - src/actions/approval-actions.ts (alçada/estorno reuse for cancelarOs faturada — D-19)
    - src/lib/faturamento/os-math.ts (Task 1 helpers)
    - src/lib/validators/service-order.ts (serviceOrderSchema, faturarOsSchema)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md §"Pattern 3/4" + Pitfall 1
  </read_first>
  <action>
'use server' + import 'server-only'. Implement:

  - `createOs(input)` (manual OS avulsa, D-11): validate serviceOrderSchema; resolve tenant + unit; `numero = rpc('next_os_number',{p_unit_id})`; insert service_orders (status 'rascunho', pagador, insurer_id when convenio); insert service_order_items computing valor_total via computeItemTotal and carrying professional_id (D-29) + account_id/cost_center_id (FCAD-02); recompute and update service_orders.total via computeOsTotal. revalidatePath.

  - `faturarOs(rawInput)` — the orchestration (Pattern 3, Pitfall 1):
    1. Parse faturarOsSchema. Re-fetch OS by id (TOCTOU).
    2. Idempotency (D-30): if os.status !== 'rascunho' → return `{ success: true }` if already 'faturada' (idempotent no-op), else error if 'cancelada'.
    3. CAS claim: `update({ status:'faturada', idempotency_key:`os:${osId}:faturar`, faturada_at: now }).eq('id', osId).eq('status','rascunho').select('id')`. If 0 rows → `{ success:false, error:'Corrida detectada — tente novamente' }`. THIS MUST HAPPEN BEFORE ANY EXTERNAL CALL.
    4. Branch on os.pagador:
       - 'particular': call `createCharge({ patientId: os.patient_id, value: os.total, description:`OS ${os.numero}`, billingType: input.billingType ?? 'PIX', installmentCount: input.installmentCount ?? 1, dueDate ... })` (OS-03, D-21). Store returned chargeId link (update service_orders or charges.service_order_id). Then if unit regime='competencia' delegate NFS-e: dynamic `import('./nfse')` → `emitirNfseForOs(osId)` inside try/catch (regime=caixa defers to webhook — Plan 06 owns the regime check, but faturarOs only calls emitirNfseForOs which itself no-ops for caixa).
       - 'convenio': do NOT call createCharge. Insert an insurer receivable row (reuse receivables table with a flag/insurer linkage — create a receivable with patient_id null/insurer reference and expected value = os.total; D-15 baixa deferred to Phase 16). Then dynamic `import('./tiss')` → `criarGuiaForOs(osId)` in try/catch.
    5. Return `{ success:true, osId }`.
    Wrap external calls so a failure records but does not silently leave status inconsistent (log + return partial error; OS stays 'faturada' but flags emission error — emission retried via worker/manual).

  - `cancelarOs(osId, motivo)` (D-19): if OS 'faturada' → route through Phase 10 alçada (`createApprovalRequest` style) + audit; on approval set status 'cancelada', cancelada_at, cancel_reason. If 'rascunho' → direct cancel allowed (no receivable yet). Use isValidOsTransition guard.

  - `getOs(id)` → OS + items + (LGPD) patient first_name+last_initial, CPF masked `***.xxx.xxx-**`. `listOs(filters)` → clinic-scoped list with status/pagador/month filters (nuqs-driven).

  - Define and export a typed stub `emitirNfseForOs`/`criarGuiaForOs` import contract is satisfied by dynamic import; guard with `existsSync`/try-catch so unit tests can mock. In tests these are mocked.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/faturamento/service-orders.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - service-orders.test.ts fully GREEN (idempotency, CAS race, particular→createCharge once, convenio→no charge + insurer receivable, total math)
    - `grep "\.eq('status', 'rascunho')" src/actions/service-orders.ts` present (CAS guard)
    - `grep "idempotency_key" src/actions/service-orders.ts` present (D-30)
    - faturarOs calls createCharge ONLY in particular branch (grep shows createCharge inside the particular conditional)
    - cancelarOs references approval/alçada for faturada path (grep for createApprovalRequest or approval action import)
    - getOs masks CPF as `***.` and uses first-name + last-initial
    - `grep -c "import 'server-only'" src/actions/service-orders.ts` returns 1
  </acceptance_criteria>
  <done>Full OS lifecycle action with CAS, idempotency, dual-path billing, alçada cancel; service-orders.test.ts GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Auto-create OS rascunho on appointment 'concluido'</name>
  <files>src/actions/appointments.ts</files>
  <read_first>
    - src/actions/appointments.ts (updateAppointment — where status transitions; lines ~181-300)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md §"Code Examples" lines 956-995 (createOsDraftFromAppointment)
    - src/actions/professionals.ts (professional resolution via user_id join — Phase 11 decision, STATE.md)
  </read_first>
  <action>
Extend `updateAppointment` (and any other path that sets status): when the new status is 'concluido' AND it was not 'concluido' before, call a new internal helper `createOsDraftFromAppointment(appointmentId, tenantId)`:
  1. Fetch appointment (patient_id, dentist_id, unit_id).
  2. Resolve professional_id via `professionals WHERE user_id = appt.dentist_id` (query-time link, Phase 11 decision — no FK).
  3. `numero = rpc('next_os_number', { p_unit_id: appt.unit_id })`.
  4. Insert service_orders rascunho (clinic_id=tenantId, unit_id, numero, patient_id, appointment_id, professional_id, status 'rascunho', pagador 'particular' default, created_by user). The partial UNIQUE index on appointment_id (Plan 03) makes a duplicate insert fail cleanly → swallow the unique-violation error (OS already exists) and continue (OS-01 idempotent at DB level, Pitfall 6).
  5. Optionally seed appointment_procedures→service_order_items if appointment_procedures exist for this appointment; otherwise leave items empty for the user to add in the OS Sheet.
Do NOT block the appointment update if OS creation fails — log and continue (OS can be created manually). Wrap in try/catch.
Add a `revalidatePath('/clinica/financeiro/faturamento/os')` after successful OS creation.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/faturamento/ src/__tests__/agenda/ 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - appointments.ts contains `createOsDraftFromAppointment`
    - The helper is invoked only on transition INTO 'concluido' (grep shows guard `=== 'concluido'`)
    - Unique-violation on duplicate OS is caught (grep for error-code guard like '23505' or .catch swallowing)
    - Existing agenda tests still GREEN (regression — no double-booking/GIST behavior changed)
    - service-orders.test.ts OS-01 auto-create assertion GREEN
  </acceptance_criteria>
  <done>Concluding an appointment auto-creates exactly one OS rascunho; agenda regression suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client faturar button → faturarOs | Untrusted double-click / retry crosses into billing side-effects |
| dentist conclude → auto OS | Status change triggers financial object creation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Tampering | double-click faturar (dup charge/NFS-e) | mitigate | CAS UPDATE `.eq('status','rascunho')` before any external call (Pitfall 1, D-30); idempotency_key unique index |
| T-15-15 | Tampering | OS total manipulation client-side | mitigate | total recomputed server-side via computeOsTotal integer-cent; never trust client total |
| T-15-16 | Elevation of Privilege | faturar by unauthorized role | mitigate | getUser + role check in action (D-18: faturar = receptionist/admin/financeiro); RLS WITH CHECK backstop |
| T-15-17 | Repudiation | cancel faturada OS without trail | mitigate | cancelarOs routes faturada through Phase 10 alçada + audit (D-19) — motivo required |
| T-15-18 | Tampering | duplicate OS per appointment | mitigate | partial UNIQUE index + swallow 23505 in createOsDraftFromAppointment |
</threat_model>

<verification>
- `npx vitest run src/__tests__/faturamento/service-orders.test.ts` fully GREEN.
- Agenda regression suite GREEN.
</verification>

<success_criteria>
OS-01 (auto OS + procedures) and OS-03 (forma de pagamento → parcelas) functional; CAS+idempotency proven by tests; cancel faturada gated by alçada.
</success_criteria>

<output>
After completion, create `.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-05-SUMMARY.md`
</output>
