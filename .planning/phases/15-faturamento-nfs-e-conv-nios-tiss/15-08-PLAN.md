---
phase: 15-faturamento-nfs-e-conv-nios-tiss
plan: 08
type: execute
wave: 4
depends_on: [15-05, 15-06]
files_modified:
  - src/app/(dashboard)/clinica/financeiro/page.tsx
  - src/app/(dashboard)/clinica/financeiro/faturamento/page.tsx
  - src/app/(dashboard)/clinica/financeiro/faturamento/os/page.tsx
  - src/app/(dashboard)/clinica/financeiro/faturamento/nfse/page.tsx
  - src/components/financeiro/OsTable.tsx
  - src/components/financeiro/OsSheet.tsx
  - src/components/financeiro/NfseTable.tsx
  - src/components/financeiro/NfseKpiRow.tsx
  - src/components/financeiro/NfseEmitForm.tsx
autonomous: true
requirements: [OS-01, OS-02, OS-03]
user_setup: []

must_haves:
  truths:
    - "Faturamento hub at /clinica/financeiro/faturamento shows 5 nav cards; financeiro hub gains OS/NFS-e/Convênios cards"
    - "OS screen lists OS with KPI row + status/pagador/month filters; OS Sheet reviews items and triggers Faturar/Cancelar per OS lifecycle (D-10/D-19)"
    - "NFS-e screen mirrors prototype (4 KPIs, chart + emit form with ISS panel, histórico table); Emitir NFS-e works against a faturada OS"
    - "Patient names display first_name+last_initial; CPF masked; no raw storage_path client-side (LGPD)"
  artifacts:
    - path: "src/app/(dashboard)/clinica/financeiro/faturamento/os/page.tsx"
      provides: "OS list RSC (Screen 1)"
      min_lines: 30
    - path: "src/app/(dashboard)/clinica/financeiro/faturamento/nfse/page.tsx"
      provides: "NFS-e RSC (Screen 2)"
      min_lines: 30
    - path: "src/components/financeiro/OsSheet.tsx"
      provides: "OS review/faturar/cancel slide-over"
      min_lines: 40
  key_links:
    - from: "OsSheet Faturar button"
      to: "faturarOs action"
      via: "server action call"
      pattern: "faturarOs"
    - from: "NfseEmitForm"
      to: "emitirNfse action"
      via: "server action call"
      pattern: "emitirNfse"
    - from: "financeiro/page.tsx nav grid"
      to: "/clinica/financeiro/faturamento/*"
      via: "nav cards"
      pattern: "faturamento"
---

<objective>
Wave 4 — build the OS (Screen 1), NFS-e (Screen 2), and Faturamento Hub (Screen 6) UI under `/clinica/financeiro/faturamento/`, plus add the 3 faturamento nav cards to the financeiro hub. All visuals follow 15-UI-SPEC.md exactly (badge contracts, copy, KPI layout, masking). Wires to Plan 05 (faturarOs/cancelarOs/listOs/getOs) and Plan 06 (emitirNfse/getNfses).

Purpose: Make OS-01/OS-02/OS-03 usable — list, review, faturar, emit NFS-e.
Output: 4 pages/hub + 5 components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-UI-SPEC.md

<interfaces>
Actions (Plan 05): listOs(filters), getOs(id), createOs(input), faturarOs({osId, billingType?, installmentCount?}), cancelarOs(osId, motivo).
Actions (Plan 06): getNfses(filters), emitirNfse(osId), cancelarNfse(nfseId, motivo), getNfseDocumentUrl(nfseId).
Reuse: PageHeader (src/components/shell/PageHeader.tsx), EmptyState, ErrorState, formatBRL (src/lib/format/money.ts), CashFlowFilters nuqs pattern (src/components/.../fluxo-de-caixa), KpiCard pattern (src/components/prototipos/charts.tsx → CashFlowTotals).
Prototype reference (visual parity): src/app/(dashboard)/clinica/prototipos/nfse/page.tsx (KPIs, ISS panel lines 104-113, BarChart).
Badge contract + copy: 15-UI-SPEC.md §"Badge Color Contract" + §"Copywriting Contract". OS: rascunho=outline, faturada=default, cancelada=destructive. NFS-e: emitida=default, processando=secondary, cancelada=outline, erro=destructive.
Container: `PageHeader` + `main p-6 max-w-6xl mx-auto w-full space-y-6`.
Destructive actions use shadcn AlertDialog (NOT window.confirm). Faturar + Cancelar = AlertDialog; Cancelar requires Textarea motivo.
nuqs keys: ?month=YYYY-MM, ?status=, ?pagador=particular|convenio.
LGPD: first_name + last_initial; CPF `***.xxx.xxx-**`; never render storage_path.
Dual-library rule: prefer shadcn; Button/Badge use @base-ui per CLAUDE.md table.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Faturamento hub + financeiro nav cards + OS screen</name>
  <files>src/app/(dashboard)/clinica/financeiro/page.tsx, src/app/(dashboard)/clinica/financeiro/faturamento/page.tsx, src/app/(dashboard)/clinica/financeiro/faturamento/os/page.tsx, src/components/financeiro/OsTable.tsx, src/components/financeiro/OsSheet.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/financeiro/page.tsx (existing hub nav-card grid to extend)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-UI-SPEC.md §"Screen 1", §"Screen 6", §"Faturamento Hub — Navigation Addition" (exact card objects + icons), §"Copywriting Contract" OS section
    - src/components/financeiro/CashFlowFilters.tsx or the fluxo-de-caixa page (nuqs filter pattern + month selector)
    - src/components/prototipos/charts.tsx (KpiCard / CashFlowTotals pattern)
    - src/actions/service-orders.ts (listOs/getOs/faturarOs/cancelarOs shapes from Plan 05)
  </read_first>
  <action>
**financeiro/page.tsx**: add the 3 nav cards from UI-SPEC §"Faturamento Hub — Navigation Addition" VERBATIM (Ordens de Serviço → /faturamento/os icon ClipboardList; NFS-e Fiscal → /faturamento/nfse icon FileText; Convênios / TISS → /faturamento/convenios icon ShieldPlus; all show:true). Import the icons from lucide-react.

**faturamento/page.tsx** (Screen 6 hub): RSC. PageHeader title "Faturamento", breadcrumb Financeiro > Faturamento. Icon-card grid (mirror financeiro/page.tsx pattern) with 5 cards: Ordens de Serviço, NFS-e Fiscal, Convênios / Planos, Operadoras (admin/financeiro only — role check filters card), Glosas. Card hover `hover:border-primary hover:bg-accent/40`.

**faturamento/os/page.tsx** (Screen 1): RSC. PageHeader title "Ordens de Serviço", breadcrumb Financeiro > Faturamento > OS, action Button "Nova OS". Read filters from searchParams (month/status/pagador). Call listOs(filters). Render: KPI row (3 cards: "OS abertas (rascunho)", "Faturadas (mês)", "Canceladas (mês)"), filter bar (status Select + pagador Select + month selector via nuqs), then `<OsTable rows={...} />`. EmptyState copy: heading "Nenhuma ordem de serviço", body per UI-SPEC, CTA "Nova OS". Add loading.tsx-style Suspense skeletons OR inline Skeleton (h-[72px] KPIs, h-64 table).

**OsTable.tsx** ('use client'): @tanstack/react-table columns Número (tabular-nums), Paciente (first_name+last_initial), Dentista, Procedimentos (count Badge), Pagador Badge (Particular/Convênio), Total (formatBRL tabular-nums right), Status Badge (OS contract), Ações DropdownMenu ("Ver detalhes", "Faturar" if rascunho, "Cancelar" if rascunho/faturada). Clicking a row or "Ver detalhes" opens OsSheet. nuqs URL state for sort/filter.

**OsSheet.tsx** ('use client'): shadcn Sheet sliding from right. Tabs: "Itens" / "Pagamento" / "Histórico". Footer buttons by status (D-10):
  - rascunho → Button "Faturar OS" (primary, full-width) [AlertDialog confirm per copy "Confirmar faturamento…"; on confirm call faturarOs({osId, billingType, installmentCount}) — particular shows PIX/Boleto/Cartão + parcelas selector] + Button outline "Cancelar OS".
  - faturada → Button outline "Cancelar OS" only (AlertDialog + Textarea motivo obrigatório → cancelarOs → triggers alçada per copy).
  - cancelada → read-only notice.
Show CPF masked `***.xxx.xxx-**` in detail. Title "Revisar OS #[número]". Use router.refresh()/revalidate after actions.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "faturamento|OsTable|OsSheet|financeiro/page" | head && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - financeiro/page.tsx contains the 3 new nav card hrefs `/clinica/financeiro/faturamento/os`, `/nfse`, `/convenios` and icons ClipboardList/FileText/ShieldPlus
    - faturamento/page.tsx renders 5 nav cards; Operadoras card gated by role
    - os/page.tsx calls listOs and renders OsTable + 3 KPI cards + filter bar
    - OsSheet footer renders Faturar (rascunho) / Cancelar (faturada) per status and calls faturarOs/cancelarOs
    - Faturar and Cancelar use shadcn AlertDialog (grep AlertDialog), never window.confirm (grep returns 0)
    - Patient cells use first-name+last-initial; CPF shown only masked `***.`
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Hub + financeiro nav + OS list/sheet wired to faturarOs/cancelarOs; build green.</done>
</task>

<task type="auto">
  <name>Task 2: NFS-e screen (KPIs + chart + emit form with ISS panel + histórico)</name>
  <files>src/app/(dashboard)/clinica/financeiro/faturamento/nfse/page.tsx, src/components/financeiro/NfseTable.tsx, src/components/financeiro/NfseKpiRow.tsx, src/components/financeiro/NfseEmitForm.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/prototipos/nfse/page.tsx (mirror EXACTLY: 4 KPIs, BarChart tone chart-2, ISS panel lines 104-113, histórico columns)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-UI-SPEC.md §"Screen 2" + §"Copywriting Contract" NFS-e section + ISS panel spec
    - src/actions/nfse.ts (getNfses/emitirNfse/cancelarNfse from Plan 06)
    - src/lib/fiscal/iss.ts (computeIss/computeValorLiquido for the preview panel)
    - src/components/prototipos/charts.tsx (BarChart + KpiCard)
  </read_first>
  <action>
**nfse/page.tsx** (Screen 2): RSC. PageHeader title "NFS-e Fiscal", breadcrumb Financeiro > Faturamento > NFS-e, action Button "Emitir NFS-e" (opens emit flow against a faturada OS; disabled if none). Call getNfses(filters). Layout mirrors prototype: `<NfseKpiRow />` (4 cards), 2-col grid [BarChart "Notas emitidas — Últimos 6 meses" + `<NfseEmitForm />`], full-width `<NfseTable />` "Histórico de notas". EmptyState heading "Nenhuma nota emitida" + body per copy.

**NfseKpiRow.tsx** ('use client' or server — prefer server): 4 KPI cards (2 cols mobile, 4 lg): "Notas emitidas (mês)", "Valor emitido" (formatBRL), "ISS ({alíquota}%)", "Pendentes / erro". Use KpiCard/CashFlowTotals pattern, `text-2xl font-semibold tabular-nums`, min-h-[72px].

**NfseEmitForm.tsx** ('use client'): RHF form to emit NFS-e for a selected faturada OS. Shows the ISS calculation panel EXACTLY per UI-SPEC (rounded-lg border bg-muted/40 p-4, "ISS retido R$ X,XX" text-muted-foreground tabular-nums, "Valor líquido R$ X,XX" font-semibold tabular-nums) computed client-side via computeIss/computeValorLiquido for preview. Submit → emitirNfse(osId). On success revalidate.

**NfseTable.tsx** ('use client'): columns Número (tabular-nums), Tomador (first_name+last_initial), Serviço, Data (tabular-nums), Valor (formatBRL right), Status Badge (NFS-e contract: emitida=default, processando=secondary, cancelada=outline, erro=destructive). Ações DropdownMenu: "Ver nota" (getNfseDocumentUrl → opens 60s signed URL — never raw path), "Cancelar NFS-e" (if emitida + window) → AlertDialog + Textarea motivo → cancelarNfse.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "nfse|Nfse" | head && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - nfse/page.tsx renders NfseKpiRow (4 cards) + BarChart + NfseEmitForm + NfseTable per prototype layout
    - NfseEmitForm shows ISS panel with "ISS retido" + "Valor líquido" labels and tabular-nums
    - NfseEmitForm submit calls emitirNfse
    - NfseTable status badges use the NFS-e badge contract variants; Tomador masked
    - "Ver nota" uses getNfseDocumentUrl (signed URL), never renders storage_path (grep returns 0 for storage_path in components)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>NFS-e screen mirrors prototype with working emit + ISS preview + masked histórico; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client UI → faturarOs/emitirNfse | User-triggered billing/fiscal side-effects |
| nfse document link → bucket | Signed URL only, never raw path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-31 | Information Disclosure | patient name/CPF in OS/NFS-e tables | mitigate | first_name+last_initial in cells; CPF masked `***.` in Sheet only (LGPD/UI-SPEC) |
| T-15-32 | Information Disclosure | raw storage_path in NFS-e link | mitigate | "Ver nota" uses getNfseDocumentUrl signed URL TTL=60s; components never reference storage_path |
| T-15-33 | Tampering | client-side faturar bypass of lifecycle | accept | Button rendering is client guard only; state machine + role enforced server-side (D-19) — UI cannot bypass |
| T-15-34 | Repudiation | cancel without motivo | mitigate | Cancelar AlertDialog requires Textarea motivo before submit |
</threat_model>

<verification>
- `npm run build` green (Vitest/tsc do not catch 'use server' errors — build does, per project memory).
- Manual: OS list → Sheet → Faturar (stub) → NFS-e appears in histórico.
</verification>

<success_criteria>
OS + NFS-e + hub screens functional, visually matching prototypes, LGPD-masked, wired to Wave 3 actions; build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-08-SUMMARY.md`
</output>
