---
phase: 15-faturamento-nfs-e-conv-nios-tiss
plan: 04
type: execute
wave: 2
depends_on: [15-02, 15-03]
files_modified:
  - src/lib/database.types.ts
autonomous: false
requirements: [OS-01, OS-02, OS-03, CONV-01, CONV-02, CONV-03]
user_setup:
  - service: supabase
    why: "Push 5 phase-15 migrations to project jqjwyqlbbuqnrffdnlpp; CLI may be logged into wrong account (nexus-*) per project memory"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (for non-TTY push)"
    dashboard_config:
      - task: "Verify CLI is logged into the correct account before push (re-login if nexus-*)"
        location: "supabase login / supabase projects list"

must_haves:
  truths:
    - "All 5 phase-15 migrations are applied to the live Supabase project jqjwyqlbbuqnrffdnlpp"
    - "Regenerated database.types.ts includes service_orders, nfse_records, tiss_guides, insurers, services, glosa_motivos, appointment_procedures rows"
    - "charges row type now includes service_order_id"
  artifacts:
    - path: "src/lib/database.types.ts"
      provides: "Regenerated Supabase types including all 10 new tables"
      contains: "service_orders"
  key_links:
    - from: "supabase db push"
      to: "live DB jqjwyqlbbuqnrffdnlpp"
      via: "migration apply in timestamp order 000100→000500"
      pattern: "service_orders"
---

<objective>
Wave 2 — [BLOCKING] apply the 5 phase-15 migrations to the live Supabase database and regenerate TypeScript types. Without this, build/tsc would pass on stale types and verification would be a false positive (per schema_push_requirement). All Wave 3 Server Actions depend on the live tables and regenerated types existing.

Purpose: Make the new schema real and typed before any action/UI code runs.
Output: Applied migrations + regenerated src/lib/database.types.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Verify Supabase login is the correct account</name>
  <files>(none — login verification, no file changes)</files>
  <read_first>
    - Project memory: project_fynxia_supabase_account.md (project jqjwyqlbbuqnrffdnlpp, org kczvihafddupruvsrrsc, sa-east-1; gotcha: CLI often logged into nexus-* account → re-login before db push)
  </read_first>
  <what-built>Five migrations are staged: 20260620000100..000500. Before push, the CLI login must point at the org that owns project jqjwyqlbbuqnrffdnlpp (NOT a nexus-* account).</what-built>
  <action>
HUMAN CHECKPOINT — do not push until confirmed. The executor must pause and have the user verify the Supabase CLI is authenticated to the FYNXIA account before any migration push:
  1. Run `supabase projects list` and confirm `jqjwyqlbbuqnrffdnlpp` appears in the output.
  2. If it does NOT appear (CLI logged into a nexus-* account), run `supabase login` and authenticate with the FYNXIA account, OR export `SUPABASE_ACCESS_TOKEN` for a non-TTY environment.
  3. Confirm the project ref is linked: run `supabase link --project-ref jqjwyqlbbuqnrffdnlpp` if not already linked.
This guards against pushing schema to the wrong project (project memory gotcha).
  </action>
  <how-to-verify>
    1. Run `supabase projects list` and confirm `jqjwyqlbbuqnrffdnlpp` appears.
    2. If it does not appear (wrong account), run `supabase login` / export `SUPABASE_ACCESS_TOKEN`.
    3. Confirm `supabase link --project-ref jqjwyqlbbuqnrffdnlpp` if not already linked.
  </how-to-verify>
  <resume-signal>Type "logged in" once `supabase projects list` shows jqjwyqlbbuqnrffdnlpp.</resume-signal>
  <verify>Human confirms `supabase projects list` output includes jqjwyqlbbuqnrffdnlpp.</verify>
  <acceptance_criteria>
    - `supabase projects list` output includes jqjwyqlbbuqnrffdnlpp
  </acceptance_criteria>
  <done>CLI authenticated to the FYNXIA account; jqjwyqlbbuqnrffdnlpp linked and visible.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push migrations + regenerate types</name>
  <files>src/lib/database.types.ts</files>
  <read_first>
    - supabase/migrations/20260620000100_faturamento_catalog_tables.sql
    - supabase/migrations/20260620000200_faturamento_os_tables.sql
    - supabase/migrations/20260620000300_faturamento_tiss_tables.sql
    - supabase/migrations/20260620000400_faturamento_rls.sql
    - supabase/migrations/20260620000500_faturamento_seed.sql
  </read_first>
  <action>
Run, from project root:
```
supabase db push
```
This applies the 5 staged migrations in timestamp order (000100 catalog → 000200 os → 000300 tiss → 000400 rls → 000500 seed). If push reports the migrations already partially applied, do NOT re-edit migration files — resolve with `supabase migration repair` only if the CLI explicitly instructs, then re-run push.

After a successful push, regenerate types:
```
supabase gen types typescript --project-id jqjwyqlbbuqnrffdnlpp --schema public > src/lib/database.types.ts
```
Guard against truncation (established phase-9/10/11 pattern): if the generated file is smaller than the prior version or does not contain `service_orders`, the gen output was truncated — re-run the gen command. Confirm the file ends with a complete closing brace.

If `supabase` CLI is unavailable in the shell, fall back to the Supabase MCP `apply_migration` per file (catalog, os, tiss, rls, seed in order) then MCP type generation; but PREFER the CLI.
  </action>
  <verify>
    <automated>grep -c "service_orders\|nfse_records\|tiss_guides\|insurers\|appointment_procedures\|glosa_motivos" src/lib/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "service_orders" src/lib/database.types.ts` ≥ 1
    - `grep -c "nfse_records" src/lib/database.types.ts` ≥ 1
    - `grep -c "tiss_guides" src/lib/database.types.ts` ≥ 1
    - `grep "service_order_id" src/lib/database.types.ts` shows charges gained the column
    - database.types.ts is NOT truncated (ends with a closing `}` and is ≥ prior file size)
    - `npx vitest run src/__tests__/faturamento/migrations-phase15.test.ts` still GREEN (source-inspection unaffected, sanity)
  </acceptance_criteria>
  <done>5 migrations applied to live DB; database.types.ts regenerated with all 10 new tables + charges.service_order_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local CLI → production DB | Schema migration crosses into live tenant data store |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Tampering | wrong-account push | mitigate | Human checkpoint verifies `supabase projects list` shows jqjwyqlbbuqnrffdnlpp before push (project memory gotcha) |
| T-15-13 | Denial of Service | truncated gen types breaks build | mitigate | Truncation guard re-runs gen; verify file completeness + service_orders presence |
</threat_model>

<verification>
- database.types.ts contains all 10 new tables + charges.service_order_id.
- migrations-phase15 source-inspection still GREEN.
</verification>

<success_criteria>
Live schema matches migrations; types regenerated and complete; Wave 3 unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-04-SUMMARY.md`
</output>
