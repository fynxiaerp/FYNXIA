---
phase: 15-faturamento-nfs-e-conv-nios-tiss
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/faturamento/migrations-phase15.test.ts
  - src/__tests__/faturamento/service-orders.test.ts
  - src/__tests__/faturamento/nfse.test.ts
  - src/__tests__/faturamento/tiss.test.ts
  - src/__tests__/faturamento/regression-guard-phase15.test.ts
autonomous: true
requirements: [OS-01, OS-02, OS-03, CONV-01, CONV-02, CONV-03]
user_setup: []

must_haves:
  truths:
    - "Five faturamento test files exist and run RED (fail) before implementation"
    - "Tests encode every D-27 enum, the 10 new tables, idempotency CAS, OS total math, regime caixa/competência, and glosa-by-item math"
    - "Regression guard asserts appointment 'concluido' enum and integration_connectors 'nfse'/'tiss' types still present"
  artifacts:
    - path: "src/__tests__/faturamento/migrations-phase15.test.ts"
      provides: "Source-inspection of all 10 migrations + RLS + D-27 enums"
      min_lines: 80
    - path: "src/__tests__/faturamento/service-orders.test.ts"
      provides: "OS state machine, createOs, faturarOs, idempotency CAS, total math"
      min_lines: 60
    - path: "src/__tests__/faturamento/nfse.test.ts"
      provides: "FiscalProvider/Stub, emitirNfse, regime split, ISS calc"
      min_lines: 50
    - path: "src/__tests__/faturamento/tiss.test.ts"
      provides: "TissProvider/Stub, criarGuia, fecharLote, glosa, recurso"
      min_lines: 50
    - path: "src/__tests__/faturamento/regression-guard-phase15.test.ts"
      provides: "Phase 2/3/9 invariants unchanged"
      min_lines: 20
  key_links:
    - from: "test files"
      to: "src/lib/fiscal, src/lib/tiss, src/actions/service-orders.ts"
      via: "absolute-path dynamic import with existsSync guard"
      pattern: "existsSync"
---

<objective>
Wave 0 — create the 5 RED test scaffolds from 15-VALIDATION.md before any implementation. These tests encode the full phase contract (schema, enums, state machine, idempotency, ISS/glosa math, provider stubs) and MUST fail until later waves build the code. They are the executable specification every downstream plan implements against.

Purpose: Tests-first (Nyquist) — no implementation wave may begin without a RED test asserting its behavior.
Output: 5 Vitest files under src/__tests__/faturamento/ that run RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-VALIDATION.md
@.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md

<interfaces>
<!-- Established Wave 0 conventions in this project (extracted from codebase) -->

Source-inspection pattern (from src/__tests__/financeiro/migrations-phase14.test.ts):
```typescript
import { describe, it, expect } from 'vitest'
import { existsSync, readFileSync } from 'node:fs'
import { join } from 'node:path'
const MIGRATIONS_DIR = join(process.cwd(), 'supabase/migrations')
const F = join(MIGRATIONS_DIR, '<timestamp>_<name>.sql')
const sql = existsSync(F) ? readFileSync(F, 'utf-8') : ''   // empty → assertions FAIL RED
expect(sql).toMatch(/CREATE TABLE public\.service_orders/)
```

Dynamic-import-with-guard pattern for not-yet-existing modules (D-144, STATE.md):
```typescript
// Use ABSOLUTE path (not @-alias — @-alias causes TS2307 when target missing).
import { existsSync } from 'node:fs'
import { join } from 'node:path'
const MOD = join(process.cwd(), 'src/lib/fiscal/stub.ts')
it('StubFiscalProvider.emit returns emitida', async () => {
  expect(existsSync(MOD)).toBe(true)               // RED until file exists
  const { StubFiscalProvider } = await import(MOD)
  const r = await new StubFiscalProvider().emit({ idempotency_key: 'k' } as never)
  expect(r.status).toBe('emitida')
})
```

D-27 enum lock (assert verbatim in migration test):
- OS status: 'rascunho' | 'faturada' | 'cancelada'
- NFS-e status: 'processando' | 'emitida' | 'cancelada' | 'erro'
- TISS guia/lote status: 'em_analise' | 'autorizada' | 'glosada' | 'paga' | 'recurso'
- pagador: 'particular' | 'convenio'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + regression source-inspection tests</name>
  <files>src/__tests__/faturamento/migrations-phase15.test.ts, src/__tests__/faturamento/regression-guard-phase15.test.ts</files>
  <read_first>
    - src/__tests__/financeiro/migrations-phase14.test.ts (exact source-inspection pattern to replicate)
    - src/__tests__/financeiro/regression-guard-phase14.test.ts (regression-guard pattern)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md §"Schema Design" (table/column names + D-27 enums)
    - src/lib/integrations/types.ts (ConnectorType must keep 'nfse'/'tiss')
    - src/lib/validators/appointment.ts (status enum must keep 'concluido')
  </read_first>
  <action>
Create `migrations-phase15.test.ts` using the readFileSync source-inspection pattern from migrations-phase14.test.ts. Reference migration files by these EXACT planned paths (do NOT create the migrations — assert against absent files so tests run RED):
  - `supabase/migrations/20260620000100_faturamento_catalog_tables.sql` (services, insurer_prices, unit_fiscal_config, glosa_motivos)
  - `supabase/migrations/20260620000200_faturamento_os_tables.sql` (insurers, appointment_procedures, service_orders, service_order_items, nfse_records, charges.service_order_id ALTER)
  - `supabase/migrations/20260620000300_faturamento_tiss_tables.sql` (tiss_lotes, tiss_guides, tiss_guide_items)
  - `supabase/migrations/20260620000400_faturamento_rls.sql`
  - `supabase/migrations/20260620000500_faturamento_seed.sql`

Assert (one `it` each):
  - Each of these 10 tables created: `expect(catalogSQL).toMatch(/CREATE TABLE public\.services/)` etc. for services, insurer_prices, unit_fiscal_config, glosa_motivos (catalog file); insurers, appointment_procedures, service_orders, service_order_items, nfse_records (os file); tiss_lotes, tiss_guides, tiss_guide_items (tiss file).
  - D-27 enums verbatim CHECK constraints:
    - `/CHECK \(status IN \('rascunho', 'faturada', 'cancelada'\)\)/` in os file
    - `/CHECK \(status IN \('processando', 'emitida', 'cancelada', 'erro'\)\)/` in os file (nfse_records)
    - `/CHECK \(status IN \('em_analise', 'autorizada', 'glosada', 'paga', 'recurso'\)\)/` in tiss file (both tiss_lotes and tiss_guides)
    - `/CHECK \(pagador IN \('particular', 'convenio'\)\)/` in os file
  - OS-01 uniqueness: `/CREATE UNIQUE INDEX[\s\S]*service_orders\(appointment_id\)[\s\S]*WHERE appointment_id IS NOT NULL/` in os file.
  - D-30 idempotency: `/CREATE UNIQUE INDEX[\s\S]*service_orders\(idempotency_key\)/` in os file.
  - charges link (RESEARCH Open Question 2): `/ALTER TABLE public\.charges[\s\S]*service_order_id/` in os file.
  - clinic_id index on every table: `/CREATE INDEX[\s\S]*services\(clinic_id\)/` (sample 3-4 tables).
  - RLS (rls file): `/ALTER TABLE public\.service_orders ENABLE ROW LEVEL SECURITY/`, SELECT `/clinic_id = get_my_tenant_id\(\)/`, and `WITH CHECK` present: `/WITH CHECK/`. Assert write-by-role per D-18: `/get_my_role\(\) IN \([^)]*'admin'/`.
  - next_os_number function (D-25/A1): `/CREATE OR REPLACE FUNCTION[\s\S]*next_os_number/` and `/SECURITY DEFINER/` in os file.
  - Seed (seed file): glosa_motivos seed with ANS codes `/1001/` and `/9901/`; services seed function/trigger `/seed_services|seed_faturamento/`.

Create `regression-guard-phase15.test.ts`:
  - readFileSync `src/lib/validators/appointment.ts`; assert `/'concluido'/`.
  - readFileSync `src/lib/integrations/types.ts`; assert `/'nfse'/` and `/'tiss'/` present in ConnectorType union.
  - readFileSync `supabase/migrations/20260606000100_financial_tables.sql`; assert `/CREATE TABLE public\.financial_transactions/` and `/NUMERIC\(12, ?2\)/` still present (financial schema untouched).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/faturamento/migrations-phase15.test.ts src/__tests__/faturamento/regression-guard-phase15.test.ts 2>&1 | grep -E "fail|FAIL|✗|×" | head</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist under src/__tests__/faturamento/
    - `npx vitest run src/__tests__/faturamento/migrations-phase15.test.ts` exits non-zero (RED) — migrations not yet created
    - `grep -c "toMatch" src/__tests__/faturamento/migrations-phase15.test.ts` returns ≥ 18
    - regression-guard test runs GREEN immediately (appointment 'concluido' and connector 'nfse'/'tiss' already exist) — this file is the only GREEN one in Wave 0
  </acceptance_criteria>
  <done>migrations-phase15.test.ts RED (asserts absent migrations); regression-guard GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Behavior tests — service-orders, nfse, tiss</name>
  <files>src/__tests__/faturamento/service-orders.test.ts, src/__tests__/faturamento/nfse.test.ts, src/__tests__/faturamento/tiss.test.ts</files>
  <read_first>
    - src/__tests__/financeiro/transaction-classification.test.ts (Zod + action unit-test pattern)
    - src/lib/integrations/worker.ts (CAS claim pattern these tests assert against)
    - src/actions/charges.ts (createCharge signature the OS faturar path calls)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-VALIDATION.md (per-req test map — every row below is a test)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md §"Architecture Patterns" + §"Code Examples" (provider interfaces, ISS/glosa math)
  </read_first>
  <action>
All three files use the absolute-path dynamic-import-with-existsSync-guard pattern (RED until modules exist). Use pure-function imports where possible (math helpers) and mock the supabase admin client for action tests.

`service-orders.test.ts` — assert (cover VALIDATION rows OS-01, OS-03, D-30, D-25):
  - Module `src/lib/faturamento/os-math.ts` exists and exports `computeOsTotal(items, descontoTotal, acrescimoTotal)`:
    - `computeOsTotal([{valorTotal:100},{valorTotal:50}], 20, 0)` === 130 (D-25: sum items − descontoTotal + acrescimoTotal).
    - integer-cent: `computeOsTotal([{valorTotal:1200}], 0, 0)` and ISS base equals total after discounts.
  - VALID_TRANSITIONS from `src/actions/service-orders.ts`: rascunho→faturada allowed; faturada→rascunho rejected; cancelada→anything rejected (assert via exported `isValidOsTransition(from,to)` pure helper).
  - D-30 idempotency: simulate second `faturarOs` on an already-`faturada` OS returns `{ success: true }` without re-calling createCharge (assert mock createCharge call count stays 1).
  - D-30 CAS: when the CAS UPDATE returns 0 rows (mock), faturarOs returns an error mentioning corrida/race.
  - OS-03 particular: faturarOs with pagador='particular' invokes createCharge mock once.
  - OS-03 convenio: faturarOs with pagador='convenio' does NOT call createCharge but inserts an insurer receivable row (assert mock insert into receivables/insurer receivable, NOT a gateway charge).

`nfse.test.ts` — assert (OS-02, D-20, D-25 ISS):
  - `src/lib/fiscal/types.ts` exports `FiscalProvider` interface (assert via source-inspection: `/emit\(/`, `/query\(/`, `/cancel\(/`).
  - `src/lib/fiscal/stub.ts` `StubFiscalProvider.emit(input)` returns `status:'emitida'` synchronously with a numero.
  - ISS calc helper `src/lib/fiscal/iss.ts` `computeIss(valorServicos, aliquota)` uses integer-cent math: `computeIss(1200, 0.05)` === 60 (no float drift like 59.999).
  - service override: aliquota selection = `service.aliquota_iss_override ?? unitConfig.aliquota_iss_padrao` (Pitfall 7) via `resolveAliquota(service, unitConfig)`.
  - OS-02/D-20 competência: `emitirNfse` mock — when unit regime='competencia', emit() is called on faturar.
  - D-20 caixa: when regime='caixa', emitirNfse is NOT called on faturar (deferred to webhook).
  - OS-02 convenio guard: NFS-e NOT emitted when pagador='convenio'.
  - nfse_records inserted with status='processando' BEFORE provider.emit() (Pitfall 2) — assert insert ordering via mock call order.

`tiss.test.ts` — assert (CONV-02, CONV-03, D-28):
  - `src/lib/tiss/types.ts` exports `TissProvider` (source-inspection: `/sendLote\(/` or `/createGuia\(/`).
  - `src/lib/tiss/stub.ts` `StubTissProvider.sendLote(...)` returns a `protocolo`.
  - CONV-02: `criarGuia` inserts tiss_guides row with status='em_analise'.
  - CONV-02: `fecharLote` groups guides by insurer, calls provider.sendLote, stores returned protocolo on tiss_lotes.
  - CONV-03: `registrarGlosa(itemId, motivoId, valorGlosado)` sets tiss_guide_items.motivo_glosa_id and valor_glosado and glosa_status='glosada'.
  - CONV-03: `registrarRecurso(itemId, texto)` sets glosa_status='em_recurso'.
  - D-28 glosa math helper `src/lib/tiss/glosa-math.ts` `computeGuiaGlosaTotals(items)`: sum(valor_glosado) integer-cent equals guide valor_glosado; valorAutorizado = valorTotal − valorGlosado.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/faturamento/service-orders.test.ts src/__tests__/faturamento/nfse.test.ts src/__tests__/faturamento/tiss.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist; each runs RED (target modules absent)
    - service-orders.test.ts contains assertions for: computeOsTotal, isValidOsTransition, idempotent faturarOs (call count), CAS race, createCharge invoked (particular), insurer receivable (convenio)
    - nfse.test.ts asserts computeIss(1200,0.05)===60 and the competência/caixa regime branch and convenio guard
    - tiss.test.ts asserts criarGuia em_analise, fecharLote protocolo, registrarGlosa/registrarRecurso status transitions, glosa-math sum
    - No `vitest` watch-mode flags used (only `vitest run`)
  </acceptance_criteria>
  <done>service-orders/nfse/tiss test files RED, asserting every VALIDATION.md per-req row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → filesystem | Tests read source/migrations only; no secrets, no network |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Wave 0 tests | accept | Tests are read-only source inspection + pure-function unit tests; no side effects, no DB writes |
| T-15-02 | Information Disclosure | test fixtures | mitigate | No real CPF/credentials in fixtures; use synthetic 'k'/uuid placeholders only |
</threat_model>

<verification>
- `npx vitest run src/__tests__/faturamento/` → migrations + service-orders + nfse + tiss RED; regression-guard GREEN.
- All 20 VALIDATION.md per-req rows have a matching assertion in one of the 5 files.
</verification>

<success_criteria>
5 test files exist; 4 RED, 1 (regression-guard) GREEN; every requirement OS-01..03 / CONV-01..03 and decisions D-20/25/27/28/30 encoded as assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-01-SUMMARY.md`
</output>
