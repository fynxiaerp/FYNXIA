---
phase: 15-faturamento-nfs-e-conv-nios-tiss
plan: 07
type: execute
wave: 3
depends_on: [15-04]
files_modified:
  - src/lib/tiss/types.ts
  - src/lib/tiss/glosa-math.ts
  - src/lib/tiss/stub.ts
  - src/lib/tiss/index.ts
  - src/actions/tiss.ts
  - src/actions/insurers.ts
autonomous: true
requirements: [CONV-01, CONV-02, CONV-03]
user_setup: []

must_haves:
  truths:
    - "TissProvider abstraction exists with a Stub returning protocolo/status, gated on Hub connector credentials (D-01/D-03/D-13)"
    - "criarGuiaForOs creates a tiss_guides row em_analise with items mirrored from service_order_items (CONV-02)"
    - "fecharLote groups guides by operadora, calls provider.sendLote, stores returned protocolo (D-22, CONV-02)"
    - "registrarGlosa is per ITEM (motivo ANS + valor glosado); registrarRecurso sets item glosa_status em_recurso; guide status derived from items (D-28, CONV-03)"
    - "insurers + insurer_prices CRUD exists (CONV-01, D-26/D-06)"
  artifacts:
    - path: "src/lib/tiss/types.ts"
      provides: "TissProvider, GuiaInput, LoteResult"
      exports: ["TissProvider"]
    - path: "src/lib/tiss/glosa-math.ts"
      provides: "computeGuiaGlosaTotals, deriveGuideStatus"
      exports: ["computeGuiaGlosaTotals", "deriveGuideStatus"]
    - path: "src/actions/tiss.ts"
      provides: "criarGuiaForOs, fecharLote, registrarGlosa, registrarRecurso, getGuias, getGlosas"
      exports: ["criarGuiaForOs", "fecharLote", "registrarGlosa", "registrarRecurso"]
    - path: "src/actions/insurers.ts"
      provides: "listInsurers, createInsurer, updateInsurer, deactivateInsurer"
      exports: ["listInsurers", "createInsurer"]
  key_links:
    - from: "fecharLote"
      to: "TissProvider.sendLote"
      via: "gated provider + protocolo store"
      pattern: "sendLote"
    - from: "registrarGlosa"
      to: "tiss_guide_items.motivo_glosa_id + valor_glosado"
      via: "per-item update"
      pattern: "motivo_glosa_id"
    - from: "criarGuiaForOs"
      to: "tiss_guides insert em_analise"
      via: "guide from OS items"
      pattern: "em_analise"
---

<objective>
Wave 3 — build the convênio/TISS layer: the `TissProvider` abstraction (types + Stub + gated factory), glosa math, the `tiss.ts` Server Actions (criarGuiaForOs called by faturarOs, fecharLote, registrarGlosa, registrarRecurso, getGuias/getGlosas), and `insurers.ts` operadora CRUD. Glosa is modeled per item (D-28); guide status is derived. Runs parallel with Plans 05 and 06 (distinct files).

Purpose: CONV-01 (operadora + tabela de preços), CONV-02 (guia + lote + protocolo), CONV-03 (glosa por motivo + recurso).
Output: 4 lib/tiss files + tiss.ts action + insurers.ts action.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md

<interfaces>
From src/lib/supabase/admin.ts: createAdminClient(); integration_connectors type='tiss' for gating.
From src/lib/integrations/hub-log.ts: logToHub.
tiss_lotes (Plan 03): insurer_id, numero, competencia ('YYYY-MM'), status (5-value TISS enum), protocolo, valor_total, data_envio, provider_ref, xml_storage_path.
tiss_guides (Plan 03): lote_id, service_order_id, insurer_id, patient_id, numero_guia, numero_carteira, registro_ans, status (5-value), valor_total/valor_autorizado/valor_glosado/valor_pago, protocolo, provider_ref.
tiss_guide_items (Plan 03): guide_id, service_order_item_id, tuss_code, description, quantity, dente, face, valor_unitario, valor_total, valor_glosado, motivo_glosa_id, glosa_status ('pendente'|'glosada'|'em_recurso'|'paga'), recurso_texto, recurso_at.
glosa_motivos (Plan 02): codigo_ans, descricao; SELECT `clinic_id IS NULL OR clinic_id = get_my_tenant_id()`.
insurers (Plan 03) + insurer_prices (Plan 02). insurerSchema (Plan 02 validators).
Contract called by faturarOs (Plan 05): `export async function criarGuiaForOs(osId: string): Promise<{success:boolean; guideId?:string; error?:string}>`.
TISS GTO field model + glosa math: RESEARCH §"TISS Standard" lines 773-808 + §"Code Examples" lines 999-1019.
Pitfall 5: glosa is item-level; guide status DERIVED (any item glosada→guide glosada; any em_recurso→guide recurso).
LGPD: paciente display first_name + last_initial.
D-133: no Zod .default(). isMoney2dp.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TissProvider abstraction + glosa math + insurers CRUD</name>
  <files>src/lib/tiss/types.ts, src/lib/tiss/glosa-math.ts, src/lib/tiss/stub.ts, src/lib/tiss/index.ts, src/actions/insurers.ts</files>
  <behavior>
    - new StubTissProvider().sendLote(input) resolves with a defined protocolo string
    - computeGuiaGlosaTotals([{valor_total:100,valor_glosado:30},{valor_total:50,valor_glosado:0}]) === { valorTotal:150, valorGlosado:30, valorAutorizado:120 } (integer-cent)
    - deriveGuideStatus(items): any item glosa_status='em_recurso' → 'recurso'; else any 'glosada' → 'glosada'; all 'paga' → 'paga'; default 'em_analise'
    - listInsurers returns clinic insurers; createInsurer inserts under tenant
  </behavior>
  <read_first>
    - src/__tests__/faturamento/tiss.test.ts (RED assertions for TissProvider/Stub/glosa-math)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md §"TISS Standard" (773-808) + §"Code Examples" glosa math (999-1019) + Pitfall 4/5
    - src/lib/fiscal/index.ts (mirror the gated-factory pattern from Plan 06 — if not present yet, mirror src/lib/asaas/gateway.ts gating)
    - src/actions/chart-of-accounts.ts (action structure)
    - src/lib/validators/insurer.ts (insurerSchema from Plan 02)
  </read_first>
  <action>
**types.ts** — export `GuiaInput`, `LoteInput`, `LoteResult` (numeroProtocolo/protocolo, status, hash?), and `TissProvider` interface with `createGuia(input): Promise<{ provider_ref:string; numero_guia:string }>` and `sendLote(input): Promise<LoteResult>` and optionally `queryLote(ref)`. Drive XML version from `insurers.tiss_version` (Pitfall 4). Pure types.

**glosa-math.ts** (pure):
  - `export function computeGuiaGlosaTotals(items: {valor_total:number; valor_glosado:number}[]): { valorTotal:number; valorGlosado:number; valorAutorizado:number; glosaRate:number }` — integer-cent sums per RESEARCH lines 1003-1018.
  - `export function deriveGuideStatus(items: { glosa_status: string|null }[]): 'em_analise'|'glosada'|'recurso'|'paga'|'autorizada'` — Pitfall 5 derivation order: if any 'em_recurso'→'recurso'; else if any 'glosada'→'glosada'; else if items.length>0 && all 'paga'→'paga'; else 'em_analise'.

**stub.ts** — `export class StubTissProvider implements TissProvider`: createGuia returns `{ provider_ref:`stub:guia:${Date.now()}`, numero_guia:`G${Date.now()}` }`; sendLote returns `{ protocolo:`PROTO-${Date.now()}`, status:'em_analise' }`.

**index.ts** — `import 'server-only'`. `export async function getTissProvider(clinicId: string, insurerId?: string): Promise<TissProvider>`: admin client reads integration_connectors type='tiss' status='enabled' deleted_at null; no credential_enc → `new StubTissProvider()`; else real adapter (deferred — return Stub for now with a TODO, since real XML is gated per D-13; keep the factory shape so a real adapter slots in).

**src/actions/insurers.ts** ('use server' + 'server-only', CONV-01/D-26): listInsurers(), createInsurer(insurerSchema), updateInsurer(id), deactivateInsurer(id). Tenant-scoped; write gated by role (admin/financeiro per D-18 — RLS backstops). revalidatePath('/clinica/financeiro/faturamento/operadoras').
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/faturamento/tiss.test.ts -t "Stub|glosa|TissProvider|computeGuiaGlosaTotals|deriveGuideStatus" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tiss.test.ts provider/glosa-math assertions GREEN
    - types.ts exports TissProvider; stub.ts StubTissProvider.sendLote returns a protocolo
    - glosa-math computeGuiaGlosaTotals integer-cent sum + deriveGuideStatus priority (recurso > glosada > paga)
    - index.ts contains `type` filter `'tiss'` + `import 'server-only'`
    - insurers.ts exports listInsurers/createInsurer/updateInsurer/deactivateInsurer with `'use server'`
    - `grep -c "NEXT_PUBLIC" src/lib/tiss/*.ts` returns 0
  </acceptance_criteria>
  <done>TissProvider abstraction + glosa math + operadora CRUD; tiss.test.ts provider/math rows GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tiss.ts actions — criarGuiaForOs, fecharLote, registrarGlosa, registrarRecurso, getGuias/getGlosas</name>
  <files>src/actions/tiss.ts</files>
  <behavior>
    - criarGuiaForOs inserts a tiss_guides row with status='em_analise' and items mirrored from service_order_items
    - fecharLote groups em_analise guides for an insurer+competencia, calls provider.sendLote, stores returned protocolo on tiss_lotes and links guides
    - registrarGlosa(itemId, motivoId, valorGlosado) sets tiss_guide_items.motivo_glosa_id, valor_glosado, glosa_status='glosada' and re-derives guide status/totals
    - registrarRecurso(itemId, texto) sets glosa_status='em_recurso', recurso_texto, recurso_at and re-derives guide status to 'recurso'
    - sum(tiss_guide_items.valor_glosado) === tiss_guides.valor_glosado after registrarGlosa (D-28)
  </behavior>
  <read_first>
    - src/__tests__/faturamento/tiss.test.ts (RED criarGuia/fecharLote/registrarGlosa/registrarRecurso/D-28 assertions)
    - src/lib/tiss/index.ts + glosa-math.ts (Task 1)
    - src/lib/integrations/hub-log.ts (logToHub on lote send)
    - .planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-RESEARCH.md §"TISS Standard" + §"Code Examples" + Pitfall 4/5
    - src/actions/service-orders.ts (the OS shape criarGuiaForOs reads — created in Plan 05; if absent at read time, read RESEARCH schema for service_orders/items columns)
  </read_first>
  <action>
'use server' + import 'server-only'. Implement (CONV-02/CONV-03/D-28):

  - `criarGuiaForOs(osId)` (called by faturarOs convênio branch):
    1. Fetch OS (must be pagador='convenio', has insurer_id) + service_order_items + patient.
    2. Idempotency (D-30): if a tiss_guides row already exists for this service_order_id → return { success:true, guideId }.
    3. numero_guia sequential per clinic (Claude's Discretion: derive from a counter or `G-` + timestamp; keep deterministic enough for tests).
    4. Insert tiss_guides (status 'em_analise', insurer_id, patient_id, registro_ans from insurer, valor_total = OS total, numero_carteira if available).
    5. Insert tiss_guide_items mirroring each service_order_item (tuss_code, description, quantity, valor_unitario, valor_total, dente, face, glosa_status='pendente', service_order_item_id link).
    6. getTissProvider(clinicId, insurerId).createGuia(...) → store provider_ref/numero_guia.
    7. revalidatePath('/clinica/financeiro/faturamento/convenios').

  - `fecharLote(insurerId, competencia)` (D-22): gather all em_analise guides for insurer+competencia (period) without a lote_id; create a tiss_lotes row (numero, competencia, valor_total=sum); call getTissProvider().sendLote({ insurer, guides, tiss_version }) → store returned protocolo + provider_ref + data_envio on tiss_lotes; link guides.lote_id. logToHub fire-and-forget (`.catch`). revalidate.

  - `registrarGlosa(itemId, motivoGlosaId, valorGlosado)` (CONV-03/D-28): validate valorGlosado <= item.valor_total; update tiss_guide_items set motivo_glosa_id, valor_glosado, glosa_status='glosada'. Re-fetch the guide's items; recompute tiss_guides.valor_glosado/valor_autorizado via computeGuiaGlosaTotals; set guide.status via deriveGuideStatus. revalidate('/clinica/financeiro/faturamento/glosas').

  - `registrarRecurso(itemId, texto, anexoPath?)` (CONV-03): update item glosa_status='em_recurso', recurso_texto, recurso_at=now. Re-derive guide status → 'recurso'. revalidate.

  - `getGuias(filters)` and `getGlosas(filters)` — clinic-scoped lists (operadora/status/month nuqs filters). Paciente displayed first_name + last_initial (LGPD). getGlosas returns glosada/em_recurso items joined with motivo descricao for the Glosas screen.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/faturamento/tiss.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - tiss.test.ts fully GREEN (criarGuia em_analise, fecharLote protocolo, registrarGlosa per item, registrarRecurso em_recurso, D-28 sum)
    - criarGuiaForOs short-circuits when a guide already exists for the OS (idempotency grep)
    - registrarGlosa updates `motivo_glosa_id` and `valor_glosado` and sets `glosa_status` to 'glosada' (grep)
    - guide status recomputed via deriveGuideStatus after glosa/recurso (grep import of deriveGuideStatus)
    - fecharLote calls sendLote and stores `protocolo`; logToHub used with `.catch`
    - `grep -c "import 'server-only'" src/actions/tiss.ts` returns 1
  </acceptance_criteria>
  <done>Full TISS guide/lote/glosa/recurso lifecycle with item-level glosa + derived guide status; tiss.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client glosa/recurso form → tiss actions | Untrusted valor_glosado / motivo crosses into receivable-affecting state |
| FYNXIA server → TISS provider | Lote XML/credentials cross to operadora endpoint |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-25 | Tampering | guide valor / insurer_id manipulation | mitigate | valor re-computed server-side from service_order_items; insurer_id must belong to OS's clinic (RLS + server check) |
| T-15-26 | Tampering | glosa amount exceeds item value | mitigate | registrarGlosa validates valorGlosado ≤ item.valor_total; integer-cent totals |
| T-15-27 | Tampering | recurso flips whole guide instead of one item | mitigate | glosa_status is per-item; guide status DERIVED via deriveGuideStatus (Pitfall 5) |
| T-15-28 | Information Disclosure | patient identity in guides/glosas UI | mitigate | paciente displayed first_name + last_initial only (LGPD) |
| T-15-29 | Tampering | duplicate guide on retry | mitigate | criarGuiaForOs idempotent on service_order_id (D-30) |
| T-15-30 | Tampering | edit shared ANS glosa seed | mitigate | glosa_motivos write WITH CHECK requires clinic_id=get_my_tenant_id() (Plan 03 RLS); system rows immutable |
</threat_model>

<verification>
- `npx vitest run src/__tests__/faturamento/tiss.test.ts` fully GREEN.
- tsc clean.
</verification>

<success_criteria>
CONV-01 (operadora + preços), CONV-02 (guia + lote + protocolo via stub), CONV-03 (glosa por item + recurso, guide status derived) functional.
</success_criteria>

<output>
After completion, create `.planning/phases/15-faturamento-nfs-e-conv-nios-tiss/15-07-SUMMARY.md`
</output>
