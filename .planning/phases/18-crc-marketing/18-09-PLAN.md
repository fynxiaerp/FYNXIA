---
phase: 18-crc-marketing
plan: 09
type: execute
wave: 3
depends_on: [18-05]
files_modified:
  - src/app/(dashboard)/clinica/crc/campanhas/page.tsx
  - src/components/crc/CampaignsTable.tsx
  - src/components/crc/CampaignFormDialog.tsx
  - src/components/crc/SegmentPreview.tsx
  - src/components/conformidade/ApprovalInbox.tsx
autonomous: false
requirements: [CRC-03]
gap_closure: false

must_haves:
  truths:
    - "Marketing creates a reactivation campaign via a 3-step dialog (Segmento / Canal e Mensagem / Revisão)"
    - "Segment preview shows N eligible patients before sending (D-07)"
    - "AI personalization sample is shown before submit (D-09)"
    - "Submit sends the campaign to approval — never a direct blast (D-09)"
    - "The existing approval inbox shows a campaign card with message preview and Aprovar Disparo → approveCampaignAndDispatch"
    - "Rejecting a campaign card (Rejeitar → rejectCampaign) sets status 'rejeitada' and sends nothing"
    - "Rascunho campaigns can be edited (updateCampaign) and cancelled (cancelCampaign → status 'cancelada'); Enviada/Aprovada rows cannot"
  artifacts:
    - path: "src/components/crc/CampaignFormDialog.tsx"
      provides: "3-step campaign creation"
      contains: "Tabs"
    - path: "src/components/conformidade/ApprovalInbox.tsx"
      provides: "campaign approval card branch"
      contains: "crc-campaign"
  key_links:
    - from: "src/components/crc/CampaignFormDialog.tsx"
      to: "src/actions/campaigns.ts"
      via: "previewCampaignSegment + requestCampaignPersonalization + submitCampaignForApproval"
      pattern: "submitCampaignForApproval"
    - from: "src/components/conformidade/ApprovalInbox.tsx"
      to: "src/actions/campaigns.ts"
      via: "approveCampaignAndDispatch on approve"
      pattern: "approveCampaignAndDispatch"
    - from: "src/components/conformidade/ApprovalInbox.tsx"
      to: "src/actions/campaigns.ts"
      via: "rejectCampaign on reject (sets status rejeitada)"
      pattern: "rejectCampaign"
    - from: "src/components/crc/CampaignsTable.tsx"
      to: "src/actions/campaigns.ts"
      via: "updateCampaign (Editar) + cancelCampaign (Cancelar)"
      pattern: "cancelCampaign"
---

<objective>
Build the reactivation campaign UI (CRC-03): the campaigns list with status badges, the 3-step CampaignFormDialog (segment → channel+AI message → review → submit-for-approval), the segment preview, and the campaign-specific approval card in the EXISTING conformidade ApprovalInbox (extend requestSummary + add an Aprovar Disparo path that calls approveCampaignAndDispatch — do NOT build a new approval component, per UI-SPEC).

Purpose: Human-facing surface for D-07/D-08/D-09/D-10/D-11. The approval card is where the human gate is exercised; dispatch only fires from approveCampaignAndDispatch (Plan 05).
Output: campanhas page + 3 CRC components + ApprovalInbox extension. Contains a human-verify checkpoint (live send depends on Meta/Resend — VALIDATION Manual-Only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-crc-marketing/18-UI-SPEC.md
@.planning/phases/18-crc-marketing/18-RESEARCH.md

<interfaces>
From src/actions/campaigns.ts (Plan 05): createCampaign, updateCampaign (rascunho-only), cancelCampaign, previewCampaignSegment, requestCampaignPersonalization, submitCampaignForApproval, approveCampaignAndDispatch, rejectCampaign, listCampaigns.
ApprovalInbox.tsx (existing): `requestSummary(row)` special-cases `row.type === 'ai_action'` (row.agent_key, payload.action). Existing approve path calls approveRequest; existing reject path calls rejectRequest. ApprovalRequestRow has { id, type, agent_key, payload, required_role, ... }.
Status badge mapping (UI-SPEC §4): Rascunho=secondary, Aguardando Aprovação=amber, Aprovada=blue, Enviada=default(accent), Rejeitada=destructive.
Dialog uses shadcn Tabs (internal 3 steps, NOT route wizard). RHF v7 + zodResolver(campaignSegmentSchema/campaignChannelSchema), no .default().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campaigns page + CampaignsTable</name>
  <files>src/app/(dashboard)/clinica/crc/campanhas/page.tsx, src/components/crc/CampaignsTable.tsx</files>
  <read_first>
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"4. Campanhas de Reativação" (table columns + status colors)
    - src/components/financeiro/PayablesTable.tsx (DropdownMenu row-action + pointer-events-none icon convention, STATE quick task 260629-uaz)
  </read_first>
  <action>
    /clinica/crc/campanhas/page.tsx (RSC): PageHeader + `<main className="p-6 max-w-5xl mx-auto w-full space-y-6">`; fetch listCampaigns(); header action "Nova Campanha" (opens CampaignFormDialog). Empty state "Nenhuma campanha de reativação" per Copywriting.
    CampaignsTable.tsx: columns Nome, Segmento ("Inativos há {X}+ dias" + filter badges), Canal (WhatsApp / E-mail / WhatsApp + E-mail badges), Status (badge per mapping), Destinatários (recipient_count), Criada em (dd/MM/yyyy), Ações (DropdownMenu). Row actions wired to Plan 05 actions:
    - "Ver Detalhes" → opens read-only detail (reuses CampaignFormDialog in view mode or a sheet).
    - "Editar" → SHOWN ONLY when status === 'rascunho'; opens CampaignFormDialog in edit mode which calls `updateCampaign(campaignId, patch)` (rascunho-only guard also enforced server-side in Plan 05).
    - "Enviar para Aprovação" → SHOWN ONLY when status === 'rascunho'; calls `submitCampaignForApproval`.
    - "Cancelar" → SHOWN when status ∈ {'rascunho','aguardando_aprovacao'}; AlertDialog confirm then `cancelCampaign(campaignId)` → status 'cancelada'. Enviada/Aprovada/Rejeitada rows have no Cancelar.
    DropdownMenuTrigger uses render-prop + MoreHorizontal with `pointer-events-none`.
  </action>
  <acceptance_criteria>
    - File src/app/(dashboard)/clinica/crc/campanhas/page.tsx exists; `grep -q "listCampaigns" ...`
    - `grep -q "pointer-events-none" src/components/crc/CampaignsTable.tsx`
    - `grep -Eq "Rascunho|Enviada|Aguardando" src/components/crc/CampaignsTable.tsx`
    - `grep -q "updateCampaign" src/components/crc/CampaignsTable.tsx` (Editar wired)
    - `grep -q "cancelCampaign" src/components/crc/CampaignsTable.tsx` (Cancelar wired)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>Campaigns list with status badges + row actions.</done>
</task>

<task type="auto">
  <name>Task 2: CampaignFormDialog (3 steps) + SegmentPreview</name>
  <files>src/components/crc/CampaignFormDialog.tsx, src/components/crc/SegmentPreview.tsx</files>
  <read_first>
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"4 Fluxo CampaignFormDialog" (3 steps) + Copywriting (confirmations)
    - src/lib/validators/crc.ts (campaignSegmentSchema, campaignChannelSchema)
  </read_first>
  <action>
    CampaignFormDialog.tsx ('use client'): shadcn Tabs with 3 internal steps.
    - Passo 1 "Segmento": Nome (Input) + Inativo há (dias) numeric (required, D-07) + optional filters in an Accordion (Último procedimento Select, Faixa etária min/max, Unidade Select). Button "Pré-visualizar Segmento" → createCampaign (if new) then previewCampaignSegment → render SegmentPreview count. If N=0 → inline empty state per Copywriting.
    - Passo 2 "Canal e Mensagem": Checkbox WhatsApp / Checkbox E-mail (≥1 required, campaignChannelSchema). Meta note (D-11 template text). Button "Gerar Personalização com IA" → requestCampaignPersonalization (spinner "Personalizando…"); show 2-3 sample personalized message cards (first name only, chat-bubble style, read-only).
    - Passo 3 "Revisão e Envio": summary (N pacientes, canais, preview). Final button "Enviar para Aprovação" (NOT "Enviar") → submitCampaignForApproval → status 'Aguardando Aprovação'. Alert (ShieldCheck): "O disparo em massa só ocorre após aprovação…".
    SegmentPreview.tsx: renders "{N} pacientes elegíveis" or the empty-segment state.
    Confirmation copy per UI-SPEC "Enviar campanha para aprovação".
  </action>
  <acceptance_criteria>
    - `grep -q "Tabs" src/components/crc/CampaignFormDialog.tsx`
    - `grep -q "submitCampaignForApproval" src/components/crc/CampaignFormDialog.tsx`
    - `grep -q "requestCampaignPersonalization" src/components/crc/CampaignFormDialog.tsx`
    - `grep -q "Enviar para Aprovação" src/components/crc/CampaignFormDialog.tsx`
    - `! grep -q "approveCampaignAndDispatch" src/components/crc/CampaignFormDialog.tsx` (dispatch is NOT in the create dialog — approval inbox only)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5; grep -q "Enviar para Aprovação" src/components/crc/CampaignFormDialog.tsx && echo SUBMIT_OK</automated>
  </verify>
  <done>3-step campaign builder; submit routes to approval, never direct dispatch.</done>
</task>

<task type="auto">
  <name>Task 3: ApprovalInbox — campaign card branch + Aprovar Disparo</name>
  <files>src/components/conformidade/ApprovalInbox.tsx</files>
  <read_first>
    - src/components/conformidade/ApprovalInbox.tsx (full — requestSummary + approve path)
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"4 Tela de aprovação" + §"Padrões → Aprovação humana"
  </read_first>
  <action>
    Extend the existing component (do NOT create a new approval component — UI-SPEC explicit):
    1. In requestSummary(), add a branch: when row.type === 'ai_action' AND row.agent_key === 'crc-campaign', render a richer summary using payload fields: "Campanha: {N} destinatários via {canal} — preview: {previewMessage truncated}". Keep the PII guard (only counts + the AI-generated preview text, no patient PII).
    2. For crc-campaign rows, the "Aprovar Disparo" action must call `approveCampaignAndDispatch(row.id, row.payload.campaignId)` instead of the generic approveRequest (because approveRequest alone does NOT dispatch — Pitfall 2). The "Rejeitar" action must call `rejectCampaign(row.id, row.payload.campaignId, reason)` instead of the generic rejectRequest — because rejectRequest alone does NOT update campaigns.status, leaving the UI-SPEC 'Rejeitada' badge unreachable (same execution-gap as approve). Mandatory reason Textarea. Approve button label "Aprovar Disparo" (accent), reject "Rejeitar" (outline). Confirmation copy per UI-SPEC "Confirmar disparo em massa" ({N} + {canal}).
    Non-campaign ai_action/estorno rows keep their existing behavior untouched (approveRequest/rejectRequest).
  </action>
  <acceptance_criteria>
    - `grep -q "crc-campaign" src/components/conformidade/ApprovalInbox.tsx`
    - `grep -q "approveCampaignAndDispatch" src/components/conformidade/ApprovalInbox.tsx`
    - `grep -q "rejectCampaign" src/components/conformidade/ApprovalInbox.tsx` (reject wired to campaign wrapper)
    - `grep -q "Aprovar Disparo" src/components/conformidade/ApprovalInbox.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5; grep -q "approveCampaignAndDispatch" src/components/conformidade/ApprovalInbox.tsx && echo DISPATCH_OK</automated>
  </verify>
  <done>Campaign approval card shows preview; approving dispatches via approveCampaignAndDispatch; existing rows unaffected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify campaign flow end-to-end (create → approve → send)</name>
  <action>Manual verification only — no code changes. Exercise the create→approve→send flow described below.</action>
  <what-built>Campaign creation (3-step), segment preview, AI personalization sample, submit-to-approval, and the campaign approval card that dispatches via the outbox only after approval.</what-built>
  <read_first>.planning/phases/18-crc-marketing/18-VALIDATION.md §"Manual-Only" (Envio real de WhatsApp/e-mail)</read_first>
  <how-to-verify>
    1. `npm run dev`; /clinica/crc/campanhas → "Nova Campanha".
    2. Step 1: set "inativo há 90 dias", preview → a patient count appears.
    3. Step 2: check WhatsApp+E-mail, "Gerar Personalização com IA" → a sample message with only the first name appears (or the static fallback if no AI key).
    4. Step 3: "Enviar para Aprovação" → campaign status becomes "Aguardando Aprovação". Confirm NO message was sent yet.
    5. In the conformidade approval inbox, the campaign card shows N destinatários + canal + preview. Approve → status becomes "Enviada"; with live Meta/Resend creds, send a test to your own number/email and confirm receipt (fail-soft if creds absent — outbox row simply marked failed).
    6. Confirm rejecting a campaign leaves it "Rejeitada" and sends nothing.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| approval inbox click → mass send | Human gate; dispatch only via approveCampaignAndDispatch |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-28 | Elevation of Privilege | send bypassing approval from the create dialog | mitigate | CampaignFormDialog only calls submitCampaignForApproval (never enqueue/dispatch); dispatch lives only in the approval card. |
| T-18-29 | Information Disclosure | patient PII in approval preview | mitigate | requestSummary renders counts + AI preview text only (first-name personalization), no patient identifiers. |
| T-18-30 | Repudiation | who approved the blast | mitigate | approveRequest (inside approveCampaignAndDispatch) records approver + audit event. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Human checkpoint: create→approve→send flow approved
</verification>

<success_criteria>
Campaign builder + approval-gated dispatch fully wired through the existing inbox; no send before approval.
</success_criteria>

<output>
After completion, create `.planning/phases/18-crc-marketing/18-09-SUMMARY.md`
</output>
