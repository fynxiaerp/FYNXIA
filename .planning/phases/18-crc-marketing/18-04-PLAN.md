---
phase: 18-crc-marketing
plan: 04
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - src/actions/referrals.ts
autonomous: true
requirements: [CRC-05]
gap_closure: false

must_haves:
  truths:
    - "linkReferral records who referred whom, tied 1:1 to a lead (D-16)"
    - "creditReferralReward credits exactly once, only when the referred lead is 'convertido' (D-18)"
    - "Concurrent conversions cannot double-credit (CAS on credited_at IS NULL)"
    - "listReferrals + listRewardsBalance expose the internal rewards view (D-19)"
  artifacts:
    - path: "src/actions/referrals.ts"
      provides: "Referral program Server Actions"
      exports: ["linkReferral", "creditReferralReward", "listReferrals", "listRewardsBalance"]
  key_links:
    - from: "src/actions/referrals.ts"
      to: "public.referral_rewards"
      via: "service-role ledger insert guarded by credited_at CAS"
      pattern: "credited_at"
---

<objective>
Implement the referral program Server Actions (CRC-05): link a referrer patient to a new lead at cadastro time (D-16), credit a configurable reward exactly once when the referred lead converts (D-17/D-18), and expose the internal referrals list + per-patient rewards balance (D-19, modeled for future Portal exposure in Phase 20).

Purpose: convertLead (Plan 03) calls creditReferralReward; the LeadFormDialog (Plan 07) calls linkReferral; the indicações page (Plan 11) reads listReferrals/listRewardsBalance.
Output: src/actions/referrals.ts (turns Plan 01's referrals.test.ts RED scaffold GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-crc-marketing/18-CONTEXT.md
@.planning/phases/18-crc-marketing/18-RESEARCH.md

<interfaces>
From src/lib/validators/crc.ts (Plan 01):
```typescript
export const referralSchema: z.ZodType // { referrer_patient_id: uuid, lead_id: uuid }
```

Schema (Plan 02):
- referrals(id, clinic_id, referrer_patient_id, lead_id UNIQUE, reward_amount, credited_at, created_at)
- referral_rewards(id, clinic_id, patient_id, referral_id, amount, type='credito', created_at)
- leads(id, clinic_id, stage, referred_by_patient_id, patient_id)

CAS precedent (STATE D-244 selectFifoBatch / baixarPayable):
```typescript
// UPDATE ... SET credited_at=now() WHERE id=$ AND credited_at IS NULL → check affected rows; 0 = lost race
```

Reward amount source: read the clinic-level configurable value. If no config table exists, use a constant default (e.g. REFERRAL_REWARD_DEFAULT = 50.00) exported from this file with a comment that it is configurable in a later phase — do NOT invent a new config table (D-17 says "valor configurável"; keep it a single documented constant/env for v1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: referrals.ts — linkReferral + list/balance reads</name>
  <files>src/actions/referrals.ts</files>
  <read_first>
    - src/lib/validators/crc.ts (referralSchema)
    - src/actions/payables.ts (getActor + WRITER_ROLES + createClient conventions)
    - .planning/phases/18-crc-marketing/18-CONTEXT.md (D-16, D-19)
  </read_first>
  <action>
    Create a 'use server' file. `const WRITER_ROLES = ['receptionist','admin','superadmin'] as const`. Export:
    - `linkReferral(input: { referrer_patient_id: string; lead_id: string })`: validate referralSchema; assertNotReadOnly + WRITER_ROLES; INSERT referrals { clinic_id: actor.tenant_id, referrer_patient_id, lead_id } (UNIQUE(lead_id) makes this idempotent — treat 23505 as success). Also set leads.referred_by_patient_id if not already set. logBusinessEvent 'crc.referral.linked'. (Called from createLead in Plan 03 and from LeadFormDialog in Plan 07.)
    - `listReferrals()`: SELECT referrals joined to referrer patient name, lead (full_name, stage), reward_amount, credited_at, created_at, ORDER BY created_at DESC. RLS-scoped. Returns rows for ReferralsTable (Plan 11).
    - `listRewardsBalance()`: aggregate per referrer patient — SUM(referral_rewards.amount) WHERE type='credito' as saldoTotal, COUNT converted referrals, and (for forward-compat) SUM WHERE type='uso' as saldoUtilizado (0 in v1), saldoDisponivel = total − utilizado. Return [{ patientId, patientName, indicacoesConvertidas, saldoTotal, saldoUtilizado, saldoDisponivel }]. Used by PatientRewardsBalanceTable (Plan 11).
  </action>
  <acceptance_criteria>
    - `grep -q "'use server'" src/actions/referrals.ts`
    - `grep -q "export async function linkReferral" src/actions/referrals.ts`
    - `grep -q "export async function listRewardsBalance" src/actions/referrals.ts`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>linkReferral (idempotent) + read/balance actions implemented and tenant-scoped.</done>
</task>

<task type="auto">
  <name>Task 2: referrals.ts — creditReferralReward (idempotent, on conversion)</name>
  <files>src/actions/referrals.ts</files>
  <read_first>
    - src/actions/referrals.ts (Task 1 file — append)
    - src/actions/lab-orders.ts (postLabExpense) or src/actions/stock-draws.ts (CAS affected-row-count discipline)
    - .planning/phases/18-crc-marketing/18-RESEARCH.md §"Security Domain" (referral double-credit → Tampering) + Open Question 2 (v1 = 'credito' only, no 'uso')
  </read_first>
  <action>
    Append `export async function creditReferralReward(leadId: string)`. This runs inside convertLead (Plan 03) after the lead reaches 'convertido'. Use `createAdminClient()` (service role — the ledger has no authenticated write policy) BUT resolve and pass clinic_id explicitly from the referral row (never trust ambient).
    Logic:
    1. SELECT referral WHERE lead_id = leadId (may be none → return { success:true, credited:false }).
    2. Verify the lead is actually 'convertido' (re-read leads.stage — do not credit on any other stage; D-18).
    3. CAS: `UPDATE referrals SET credited_at=now(), reward_amount=$amount WHERE id=$refId AND credited_at IS NULL RETURNING id`. If 0 rows → already credited (lost race) → return { success:true, credited:false }.
    4. Only if the CAS won: INSERT referral_rewards { clinic_id: referral.clinic_id, patient_id: referral.referrer_patient_id, referral_id, amount: REFERRAL_REWARD_DEFAULT, type:'credito' }.
    5. logBusinessEvent 'crc.referral.credited' { referralId, patientId: referrer, amount }.
    Export `const REFERRAL_REWARD_DEFAULT = 50.00` with a comment (D-17 configurable — single documented constant for v1). Never create 'uso' rows in this phase (Open Question 2).
  </action>
  <acceptance_criteria>
    - `grep -q "export async function creditReferralReward" src/actions/referrals.ts`
    - `grep -q "credited_at" src/actions/referrals.ts` (CAS guard present)
    - `grep -q "REFERRAL_REWARD_DEFAULT" src/actions/referrals.ts`
    - `! grep -q "'uso'" src/actions/referrals.ts` (no redemption rows in v1)
    - `npx vitest run src/__tests__/crc/referrals.test.ts` passes (RED scaffold now GREEN)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/crc/referrals.test.ts; npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>creditReferralReward credits once via CAS on credited_at, only for converted leads; referrals.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| convertLead (session) → creditReferralReward (service role) | Financial credit crosses into service-role write |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Tampering (financial) | double referral credit | mitigate | CAS `UPDATE ... WHERE credited_at IS NULL` + affected-row check before ledger insert; ledger has no authenticated write policy. |
| T-18-12 | Elevation of Privilege | crediting a non-converted lead | mitigate | Re-read leads.stage === 'convertido' before crediting; caller (convertLead) already CAS-guards the transition. |
| T-18-13 | Spoofing | client-forged reward amount | mitigate | amount is server-side constant (REFERRAL_REWARD_DEFAULT), never from client input. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/crc/referrals.test.ts` GREEN
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
Referral linking + once-only conversion crediting + internal balance reads implemented safely.
</success_criteria>

<output>
After completion, create `.planning/phases/18-crc-marketing/18-04-SUMMARY.md`
</output>
