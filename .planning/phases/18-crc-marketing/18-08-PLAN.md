---
phase: 18-crc-marketing
plan: 08
type: execute
wave: 3
depends_on: [18-02, 18-03]
files_modified:
  - src/actions/roi.ts
  - src/app/(dashboard)/clinica/crc/roi/page.tsx
  - src/components/crc/RoiKpiRow.tsx
  - src/components/crc/RoiByOriginTable.tsx
  - src/components/financeiro/PayableFormDialog.tsx
autonomous: true
requirements: [CRC-02]
gap_closure: false

must_haves:
  truths:
    - "ROI panel shows Custo Total, CPL, CAC, Taxa de Conversão Geral from financial-linked cost (D-05/D-06)"
    - "Cost comes from payables tagged with campaign_id — never manual entry on the CRC side"
    - "CPL/CAC/Custo render '—' gracefully when no cost/converted attributed"
    - "A marketing despesa in Contas a Pagar can be tagged to a campaign (payables.campaign_id UI field)"
  artifacts:
    - path: "src/actions/roi.ts"
      provides: "CPL/CAC aggregates from payables + leads"
      exports: ["getRoiByCampaign", "getRoiByOrigin"]
    - path: "src/app/(dashboard)/clinica/crc/roi/page.tsx"
      provides: "ROI panel route"
      min_lines: 20
  key_links:
    - from: "src/actions/roi.ts"
      to: "public.payables"
      via: "SUM(valor_total) WHERE campaign_id"
      pattern: "campaign_id"
    - from: "src/actions/roi.ts"
      to: "src/lib/crc/roi-math.ts"
      via: "computeCpl/computeCac"
      pattern: "computeCpl"
---

<objective>
Build the campaign ROI panel (CRC-02): server aggregates for Custo Total / CPL / CAC / conversion-by-origin sourced from `payables.campaign_id` (D-05) and lead counts, the read-only ROI page, and the one new optional "Vincular a campanha" field on the existing Contas a Pagar PayableFormDialog so a marketing expense can be attributed to a campaign.

Purpose: Closes CRC-02 with cost as a single source of truth from the financial module (no manual cost entry on the CRC side, per D-05). Uses Plan 01's pure CPL/CAC math.
Output: src/actions/roi.ts + ROI page + 2 components + payables dialog field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-crc-marketing/18-UI-SPEC.md
@.planning/phases/18-crc-marketing/18-RESEARCH.md

<interfaces>
From src/lib/crc/roi-math.ts (Plan 01): computeCpl(cost, leads) | computeCac(cost, converted) → number | null.
From src/actions/leads.ts (Plan 03): listConversionByOrigin.
Schema (Plan 02): payables.campaign_id nullable FK; campaigns table.
CPL/CAC query (RESEARCH §Pattern 2):
```sql
SELECT COALESCE(SUM(p.valor_total),0) FROM payables p
WHERE p.clinic_id=$1 AND p.campaign_id=$2 AND p.deleted_at IS NULL AND p.status<>'cancelado';
-- CPL = custo / COUNT(leads WHERE campaign_id=$2)
-- CAC = custo / COUNT(leads WHERE campaign_id=$2 AND stage='convertido')
```
KPI row pattern: mirror src/components/financeiro/NfseKpiRow.tsx (min-h-[72px] cards, tabular-nums). formatBRL from '@/lib/format/money'.
PayableFormDialog.tsx (existing): add ONE optional Select "Vincular a campanha" bound to a new optional campaign_id field; list campaigns via a lightweight action (reuse listCampaigns from Plan 05 OR a minimal inline query). Keep all existing fields unchanged.
nuqs filters: campanha, from, to.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: roi.ts — CPL/CAC + conversion-by-origin aggregates</name>
  <files>src/actions/roi.ts</files>
  <read_first>
    - src/lib/crc/roi-math.ts (computeCpl/computeCac)
    - src/actions/leads.ts (listConversionByOrigin — reuse or mirror)
    - .planning/phases/18-crc-marketing/18-RESEARCH.md §"Pattern 2" (queries + '—' convention) + §"Assumptions A3" (lançado vs pago — use committed spend: status<>'cancelado')
  </read_first>
  <action>
    Create src/actions/roi.ts ('use server', read-only — no writes, no assertNotReadOnly needed but still session-scoped via createClient/RLS). Export:
    - `getRoiByCampaign(opts?: { campaignId?: string; from?: string; to?: string })`: for each campaign (or the filtered one), compute custoTotal = SUM(payables.valor_total) WHERE campaign_id AND deleted_at IS NULL AND status<>'cancelado'; leads = COUNT(leads WHERE campaign_id); convertidos = COUNT(leads WHERE campaign_id AND stage='convertido'); cpl = computeCpl(custoTotal, leads); cac = computeCac(custoTotal, convertidos); taxaConversao = leads>0 ? convertidos/leads : 0. Return per-campaign rows + an aggregate summary { custoTotal, cpl, cac, taxaConversaoGeral } for the KPI cards. Return null-typed cpl/cac untouched (UI renders '—').
    - `getRoiByOrigin(opts?)`: mirror listConversionByOrigin but also attribute cost per source (leads with a campaign_id contribute their campaign's cost proportionally is out of scope — keep source cost = SUM over campaigns whose leads share that source, or simply '—' when a source has no campaign-linked cost, per UI-SPEC RoiByOriginTable "—" convention). Return [{ sourceName, leads, convertidos, taxaConversao, custoAtribuido|null, cpl|null, cac|null }].
    Both are RLS-scoped via createClient(). Never divide by zero (delegate to computeCpl/computeCac → null).
  </action>
  <acceptance_criteria>
    - `grep -q "export async function getRoiByCampaign" src/actions/roi.ts`
    - `grep -q "getRoiByOrigin" src/actions/roi.ts`
    - `grep -q "campaign_id" src/actions/roi.ts`
    - `grep -Eq "computeCpl|computeCac" src/actions/roi.ts`
    - `grep -q "status" src/actions/roi.ts` (cancelado exclusion)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5; grep -q "getRoiByCampaign" src/actions/roi.ts && echo ROI_OK</automated>
  </verify>
  <done>CPL/CAC + origin aggregates sourced from payables.campaign_id; zero-safe.</done>
</task>

<task type="auto">
  <name>Task 2: ROI panel page + KPI row + RoiByOriginTable</name>
  <files>src/app/(dashboard)/clinica/crc/roi/page.tsx, src/components/crc/RoiKpiRow.tsx, src/components/crc/RoiByOriginTable.tsx</files>
  <read_first>
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"3. Painel de ROI de Campanha"
    - src/components/financeiro/NfseKpiRow.tsx (KPI card structure to mirror)
  </read_first>
  <action>
    /clinica/crc/roi/page.tsx (RSC): PageHeader + `<main className="p-6 max-w-6xl mx-auto w-full space-y-6">`; nuqs filters (campanha Select "Todas", date range from/to); fetch getRoiByCampaign + getRoiByOrigin. Header actions: none (read-only, D-05). Empty state per Copywriting "Nenhuma campanha registrada" with link to /clinica/financeiro/contas-a-pagar. Include the informative banner text (custo vem do financeiro + "Lançar despesa" link).
    RoiKpiRow.tsx: 4 KPI cards (grid sm:grid-cols-2 lg:grid-cols-4, min-h-[72px]) — Custo Total (formatBRL), CPL (formatBRL or "—"), CAC (formatBRL or "—"), Taxa de Conversão Geral ({pct}%). tabular-nums, 24px numbers.
    RoiByOriginTable.tsx: columns Origem, Leads, Convertidos, Taxa de Conversão, Custo Atribuído (formatBRL or "—"), CPL ("—" if no cost), CAC ("—" if no convertidos). No chart.
  </action>
  <acceptance_criteria>
    - File src/app/(dashboard)/clinica/crc/roi/page.tsx exists
    - `grep -q "getRoiByCampaign" "src/app/(dashboard)/clinica/crc/roi/page.tsx"`
    - `grep -q "min-h-\[72px\]" src/components/crc/RoiKpiRow.tsx`
    - `grep -q "—" src/components/crc/RoiByOriginTable.tsx` (graceful dash)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>ROI panel renders KPIs + origin table with '—' fallbacks; read-only.</done>
</task>

<task type="auto">
  <name>Task 3: PayableFormDialog — optional "Vincular a campanha" field (D-05)</name>
  <files>src/components/financeiro/PayableFormDialog.tsx</files>
  <read_first>
    - src/components/financeiro/PayableFormDialog.tsx (full — add one field, keep everything else)
    - src/actions/payables.ts (the create/update payable action — add optional campaign_id passthrough)
  </read_first>
  <action>
    Add one optional Select "Vincular a campanha" to PayableFormDialog bound to a new optional `campaign_id` form field. Populate options from campaigns (reuse listCampaigns from Plan 05, or a minimal read). On submit, pass campaign_id through to the existing payable create/update action (the payables action must accept an optional campaign_id and write it — extend its input type/insert to include campaign_id if not already; do NOT change any existing required field or behavior). This is the only touchpoint on the financeiro side (RESEARCH §Pattern 2 — zero new UI otherwise).
  </action>
  <acceptance_criteria>
    - `grep -q "campaign" src/components/financeiro/PayableFormDialog.tsx`
    - `grep -q "Vincular a campanha\|campanha" src/components/financeiro/PayableFormDialog.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>Marketing despesa can be tagged to a campaign; existing payable flow unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| authenticated client → ROI reads | RLS-scoped aggregates only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-26 | Information Disclosure | cross-tenant cost aggregation | mitigate | roi.ts uses createClient() RLS; payables/leads scoped to get_my_tenant_id(). |
| T-18-27 | Tampering | negative/absurd cost via forged campaign_id | mitigate | campaign_id is a FK (ON DELETE SET NULL); payable amounts validated by existing payables action; ROI read-only. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- ROI page renders KPI + origin table
</verification>

<success_criteria>
ROI panel computes CPL/CAC from financial-linked cost with graceful '—'; campaign tagging available in Contas a Pagar.
</success_criteria>

<output>
After completion, create `.planning/phases/18-crc-marketing/18-08-SUMMARY.md`
</output>
