---
phase: 18-crc-marketing
plan: 07
type: execute
wave: 3
depends_on: [18-03, 18-04]
files_modified:
  - src/components/shell/nav-config.ts
  - src/components/shell/nav-icons.ts
  - src/app/(dashboard)/clinica/crc/page.tsx
  - src/app/(dashboard)/clinica/crc/funil/page.tsx
  - src/components/crc/LeadKanbanBoard.tsx
  - src/components/crc/KanbanColumn.tsx
  - src/components/crc/LeadCard.tsx
  - src/components/crc/LeadFormDialog.tsx
  - src/components/crc/LeadDetailSheet.tsx
  - src/components/crc/LeadStageChangeDialog.tsx
  - src/components/crc/ConversionByOriginTable.tsx
  - src/components/crc/LeadSourceManager.tsx
autonomous: false
requirements: [CRC-01]
gap_closure: false

must_haves:
  truths:
    - "CRC hub at /clinica/crc links to the 5 sub-screens; a 'CRC' item appears in the sidebar"
    - "Funil kanban shows 5 columns and leads can be dragged between stages (mouse AND keyboard)"
    - "Dropping on Convertido opens the create/link-patient dialog; dropping on Perdido asks for reason"
    - "Admin can manage lead sources (add / deactivate) — D-03"
    - "Toggle shows Conversão por Origem table"
  artifacts:
    - path: "src/components/crc/LeadKanbanBoard.tsx"
      provides: "dnd-kit board with keyboard accessibility"
      contains: "DragDropProvider"
    - path: "src/app/(dashboard)/clinica/crc/funil/page.tsx"
      provides: "Funil route"
      min_lines: 20
  key_links:
    - from: "src/components/crc/LeadKanbanBoard.tsx"
      to: "src/actions/leads.ts"
      via: "moveLeadStage on drag end (optimistic)"
      pattern: "moveLeadStage"
    - from: "src/components/crc/LeadFormDialog.tsx"
      to: "src/actions/referrals.ts"
      via: "linkReferral when origem=Indicação"
      pattern: "linkReferral|createLead"
---

<objective>
Build the lead-funnel UI (CRC-01): the CRC module hub + sidebar nav entry, the drag-and-drop kanban funil (dnd-kit, keyboard-accessible), the lead create/detail/stage-change dialogs, the conversion-by-origin toggle table, and the admin lead-source manager (D-03).

Purpose: This is the primary CRC-01 user surface. Consumes Plan 03/04 actions. Contains a human-verify checkpoint (drag-and-drop + keyboard accessibility cannot be unit-tested — VALIDATION Manual-Only).
Output: CRC hub + funil route + 8 components under src/components/crc/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-crc-marketing/18-UI-SPEC.md
@.planning/phases/18-crc-marketing/18-CONTEXT.md

<interfaces>
From src/actions/leads.ts (Plan 03): createLead, listLeadsByStage, moveLeadStage, convertLead, listConversionByOrigin.
From src/actions/lead-sources.ts (Plan 03): listLeadSources, createLeadSource, toggleLeadSourceActive.
From src/actions/referrals.ts (Plan 04): linkReferral.

Nav (src/components/shell/nav-config.ts): add `| 'crc'` to NavIconKey union, add `{ href:'/clinica/crc', label:'CRC & Marketing', icon:'crc' }` to NAV_ITEMS. nav-icons.ts: add `crc: <LucideIcon>` (use `Megaphone` or `Users` from lucide-react) to NAV_ICONS.
Hub pattern: mirror /clinica/estoque + /clinica/financeiro hub — PageHeader + icon Link cards `group flex flex-col gap-3 rounded-lg border border-border bg-card p-6 ... hover:border-primary hover:bg-accent/40`.
Existing shadcn: PageHeader (@/components/shell/PageHeader), Card, Button, Badge, Dialog, Sheet, Select, Table, Textarea, Input, DropdownMenu, Accordion. Install Sheet via `npx shadcn add sheet` if missing.
dnd-kit v0.5 API: `import { DragDropProvider } from '@dnd-kit/react'`; `useSortable`/`useDroppable` from '@dnd-kit/react'; KeyboardSensor auto-registered. onDragEnd → moveLeadStage.
Base-ui Button trigger convention (STATE D-212): use render-prop, NOT asChild (`<DropdownMenuTrigger render={<Button .../>}>`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CRC hub page + sidebar nav + LeadSourceManager (D-03)</name>
  <files>src/components/shell/nav-config.ts, src/components/shell/nav-icons.ts, src/app/(dashboard)/clinica/crc/page.tsx, src/components/crc/LeadSourceManager.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/estoque/page.tsx (hub/RSC + PageHeader pattern)
    - src/components/shell/nav-config.ts + nav-icons.ts (add crc entry)
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"1. /clinica/crc — Hub" + §"7 header note" + Copywriting (Nova Origem)
  </read_first>
  <action>
    1. nav-config.ts: add `'crc'` to the NavIconKey union and a NAV_ITEMS entry `{ href:'/clinica/crc', label:'CRC & Marketing', icon:'crc' }` (place after estoque). nav-icons.ts: import `Megaphone` and add `crc: Megaphone` to NAV_ICONS.
    2. /clinica/crc/page.tsx (RSC): PageHeader + `<main className="p-6 max-w-5xl mx-auto w-full">` with a grid `sm:grid-cols-2 lg:grid-cols-3` of 5 Link cards per UI-SPEC §1 (Funil de Leads→/clinica/crc/funil, ROI de Campanhas→/clinica/crc/roi, Campanhas de Reativação→/clinica/crc/campanhas, NPS→/clinica/crc/nps, Programa de Indicação→/clinica/crc/indicacoes) with the specified lucide icons + one-line descriptions.
    3. LeadSourceManager.tsx (client): a Dialog listing lead_sources (listLeadSources) with an "ativo" toggle (toggleLeadSourceActive) and a "Nova Origem" input+button (createLeadSource, admin-gated). Uses the destructive-confirm copy "Não é possível excluir" only conceptually — v1 is deactivate-only (D-03). Surfaced from the funil header (Task 2).
  </action>
  <acceptance_criteria>
    - `grep -q "'crc'" src/components/shell/nav-config.ts`
    - `grep -q "crc:" src/components/shell/nav-icons.ts`
    - File src/app/(dashboard)/clinica/crc/page.tsx exists and references all 5 sub-routes (`grep -c "/clinica/crc/" ...` >= 5)
    - `grep -q "LeadSourceManager" src/components/crc/LeadSourceManager.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5; grep -c "/clinica/crc/" "src/app/(dashboard)/clinica/crc/page.tsx"</automated>
  </verify>
  <done>Hub + nav entry + source manager present; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Funil route + Lead Kanban board (dnd-kit, keyboard-accessible)</name>
  <files>src/app/(dashboard)/clinica/crc/funil/page.tsx, src/components/crc/LeadKanbanBoard.tsx, src/components/crc/KanbanColumn.tsx, src/components/crc/LeadCard.tsx</files>
  <read_first>
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"2. Funil de Leads (Kanban)" + §"Padrões de Interação → Drag-and-drop" + §"Acessibilidade"
    - @dnd-kit/react quickstart (DragDropProvider/useSortable/useDroppable; KeyboardSensor default)
  </read_first>
  <action>
    /clinica/crc/funil/page.tsx (RSC): PageHeader (no max-w) + `<main className="p-6 w-full space-y-4">`; fetch listLeadsByStage() + listLeadSources() + listConversionByOrigin(); header actions "Novo Lead" (opens LeadFormDialog) + "Ver Conversão por Origem" toggle (nuqs `view`) + "Gerenciar Origens" (opens LeadSourceManager). Render LeadKanbanBoard OR ConversionByOriginTable based on `view`.
    LeadKanbanBoard.tsx ('use client'): `<DragDropProvider onDragEnd={...}>` wrapping 5 KanbanColumn (Novo/Contatado/Agendado/Convertido/Perdido) per UI-SPEC structure (`flex gap-4 overflow-x-auto`, column min-w-[260px] max-w-[320px]). onDragEnd: optimistic local move + call moveLeadStage; on Convertido/Perdido drop → intercept with LeadStageChangeDialog (Task 3) BEFORE persisting (drop visual happens, persistence awaits confirm; cancel → card returns). Rollback local state on action error. KeyboardSensor is auto-registered — do NOT disable it.
    KanbanColumn.tsx: header `bg-secondary/50 rounded-t-lg px-4 py-3` (font-display text-sm font-semibold) + count Badge; Convertido header `border-t-2 border-primary`, Perdido `border-t-2 border-destructive`; body is a useDroppable zone `bg-muted/30 rounded-b-lg p-3 min-h-[400px]`; empty-column inline text.
    LeadCard.tsx: useSortable draggable Card `min-h-[88px] p-3 cursor-grab` showing name (truncate), origem Badge (outline), phone, "{diasNoEstagio} dia(s) no estágio"; click (non-drag) opens LeadDetailSheet (Task 3).
  </action>
  <acceptance_criteria>
    - `grep -q "DragDropProvider" src/components/crc/LeadKanbanBoard.tsx`
    - `grep -q "moveLeadStage" src/components/crc/LeadKanbanBoard.tsx`
    - `grep -q "useDroppable" src/components/crc/KanbanColumn.tsx`
    - `grep -q "min-h-\[88px\]" src/components/crc/LeadCard.tsx`
    - File src/app/(dashboard)/clinica/crc/funil/page.tsx exists
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -8; grep -q "DragDropProvider" src/components/crc/LeadKanbanBoard.tsx && echo DND_OK</automated>
  </verify>
  <done>5-column kanban with dnd-kit + keyboard sensor + optimistic move + intercept dialogs.</done>
</task>

<task type="auto">
  <name>Task 3: Lead dialogs + detail sheet + conversion-by-origin table</name>
  <files>src/components/crc/LeadFormDialog.tsx, src/components/crc/LeadDetailSheet.tsx, src/components/crc/LeadStageChangeDialog.tsx, src/components/crc/ConversionByOriginTable.tsx</files>
  <read_first>
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"2 LeadFormDialog" + §"2b Conversão por Origem" + §"Acessibilidade" (stage Select fallback)
    - src/components/estoque/ProductFormDialog.tsx (RHF v7 + Zod + trigger-wrapper convention)
  </read_first>
  <action>
    LeadFormDialog.tsx: RHF v7 + zodResolver(leadSchema), defaultValues (no Zod .default() — D-133). Fields: Nome (Input, required), Telefone (Input BR mask), E-mail (Input optional), Origem (Select from active listLeadSources), Indicado por (patient-search Select — visible only when origem name === 'Indicação', watch()), Observação (Textarea). Submit → createLead; if referred_by set, createLead already links referral (Plan 03) — do not double-call. Initial stage 'Novo' (not editable). Submit copy "Cadastrar Lead".
    LeadStageChangeDialog.tsx: two modes — Convertido: "Vincular a paciente existente ou criar novo?" (patient search Select OR create-new) → convertLead(leadId, { patientId? }); Perdido: optional reason Select (sem resposta / não tem interesse / preço / outro) + optional Textarea → moveLeadStage(leadId,'perdido', reason). Confirm persists; Cancel reverts the kanban optimistic move.
    LeadDetailSheet.tsx: shadcn Sheet (right) showing lead detail + a Stage `Select` (100% accessible fallback to DnD — UI-SPEC Acessibilidade) that calls moveLeadStage/convertLead. This is the mandatory non-mouse path.
    ConversionByOriginTable.tsx: table from listConversionByOrigin() — columns Origem (name + outline Badge), Total de Leads, Convertidos, Taxa de Conversão ({pct}%, tabular-nums, green if ≥ overall avg), Perdidos; sortable by Taxa desc default. No chart.
  </action>
  <acceptance_criteria>
    - `grep -q "leadSchema" src/components/crc/LeadFormDialog.tsx`
    - `grep -Eq "Indicação|indicacao" src/components/crc/LeadFormDialog.tsx` (conditional referral field)
    - `grep -q "convertLead" src/components/crc/LeadStageChangeDialog.tsx`
    - `grep -q "Select" src/components/crc/LeadDetailSheet.tsx` (accessible stage fallback)
    - `grep -q "listConversionByOrigin\|Taxa de Conversão" src/components/crc/ConversionByOriginTable.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -8</automated>
  </verify>
  <done>All lead dialogs + accessible stage fallback + conversion table implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify kanban drag-and-drop + keyboard accessibility</name>
  <action>Manual verification only — no code changes. Confirm the built kanban DnD + keyboard accessibility work as described below.</action>
  <what-built>Lead funil kanban with mouse drag-and-drop and keyboard-accessible stage changes (dnd-kit KeyboardSensor + LeadDetailSheet Select fallback), plus convert/lost intercept dialogs.</what-built>
  <read_first>.planning/phases/18-crc-marketing/18-VALIDATION.md §"Manual-Only Verifications" (Kanban DnD row)</read_first>
  <how-to-verify>
    1. `npm run dev`; visit /clinica/crc/funil.
    2. Mouse: drag a lead from "Novo" to "Contatado"; reload — stage persisted.
    3. Keyboard: Tab to a card, Space to pick up, Arrow to move column, Space to drop — confirm it moves (or use the LeadDetailSheet stage Select). Confirm the funil is fully operable WITHOUT a mouse.
    4. Drag a lead onto "Convertido" — the create/link-patient dialog must appear before persisting; cancel returns the card.
    5. Drag a lead onto "Perdido" — the reason dialog appears.
    6. Confirm the "CRC & Marketing" item shows in the sidebar and the hub links work.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client kanban → moveLeadStage/convertLead | All persistence is server-validated |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-24 | Tampering | client forces illegal stage via drag | mitigate | moveLeadStage/convertLead re-validate isValidStageTransition server-side (Plan 03); optimistic UI rolls back on rejection. |
| T-18-25 | Elevation of Privilege | read-only role edits leads via UI | accept/mitigate | Server Actions enforce assertNotReadOnly + WRITER_ROLES; UI is not the boundary. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Human checkpoint: DnD + keyboard accessibility approved
</verification>

<success_criteria>
Kanban funil operable by mouse and keyboard; convert/lost intercepts; hub + nav + source manager live.
</success_criteria>

<output>
After completion, create `.planning/phases/18-crc-marketing/18-07-SUMMARY.md`
</output>
