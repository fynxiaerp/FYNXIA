---
phase: 18-crc-marketing
plan: 11
type: execute
wave: 3
depends_on: [18-04]
files_modified:
  - src/app/(dashboard)/clinica/crc/indicacoes/page.tsx
  - src/components/crc/ReferralsTable.tsx
  - src/components/crc/PatientRewardsBalanceTable.tsx
autonomous: true
requirements: [CRC-05]
gap_closure: false

must_haves:
  truths:
    - "Indicações page shows total/converted referrals and total credited rewards (KPIs)"
    - "ReferralsTable lists indicador → indicado, the referred lead's stage, and reward (Pendente/credited)"
    - "PatientRewardsBalanceTable shows per-patient converted count + credit balance (D-19)"
    - "A per-patient statement (Sheet) shows credit history — read-only, no manual balance edits"
  artifacts:
    - path: "src/app/(dashboard)/clinica/crc/indicacoes/page.tsx"
      provides: "Referral program route"
      min_lines: 20
    - path: "src/components/crc/PatientRewardsBalanceTable.tsx"
      provides: "Per-patient rewards balance (portal-ready model)"
  key_links:
    - from: "src/app/(dashboard)/clinica/crc/indicacoes/page.tsx"
      to: "src/actions/referrals.ts"
      via: "listReferrals + listRewardsBalance"
      pattern: "listRewardsBalance"
---

<objective>
Build the referral program UI (CRC-05, D-19): the indicações page with referral KPIs, the referrals list (who referred whom + reward status), and the per-patient rewards balance table with a read-only credit statement — modeled now for the internal team, structured for future Portal exposure in Phase 20.

Purpose: Completes CRC-05's internal surface. Referral linking itself happens in the LeadFormDialog (Plan 07); this page is consultation/management of rewards. Consumes Plan 04 actions.
Output: indicações page + 2 CRC components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-crc-marketing/18-UI-SPEC.md

<interfaces>
From src/actions/referrals.ts (Plan 04): listReferrals (rows: indicador name, indicado lead name+stage, reward_amount, credited_at, created_at), listRewardsBalance (rows: patientId, patientName, indicacoesConvertidas, saldoTotal, saldoUtilizado, saldoDisponivel).
UI-SPEC §7: KPI cards (Indicações Registradas / Indicações Convertidas / Total em Recompensas Creditadas — formatBRL), ReferralsTable, PatientRewardsBalanceTable + "Ver Extrato" Sheet. formatBRL from '@/lib/format/money'. Stage badge colors reuse the funil mapping.
Header actions: none (linking happens in LeadFormDialog — D-16).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Indicações page + referral KPIs + ReferralsTable</name>
  <files>src/app/(dashboard)/clinica/crc/indicacoes/page.tsx, src/components/crc/ReferralsTable.tsx</files>
  <read_first>
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"7. Programa de Indicação" (KPIs + ReferralsTable)
    - src/components/crc/ConversionByOriginTable.tsx (Plan 07 — table + stage badge reuse)
  </read_first>
  <action>
    /clinica/crc/indicacoes/page.tsx (RSC): PageHeader + `<main className="p-6 max-w-5xl mx-auto w-full space-y-6">`; fetch listReferrals + listRewardsBalance; header actions none. KPI cards (grid sm:grid-cols-3, min-h-[72px]): "Indicações Registradas" (count), "Indicações Convertidas" (count credited_at not null / stage convertido), "Total em Recompensas Creditadas" (formatBRL sum). Empty state "Nenhuma indicação registrada" per Copywriting.
    ReferralsTable.tsx: columns Indicador (referrer name), Indicado (lead name), Status da Indicação (Badge = referred lead's funil stage, reuse funil color mapping), Recompensa (formatBRL green+semibold if credited, else "Pendente" muted), Data da Indicação (dd/MM/yyyy).
  </action>
  <acceptance_criteria>
    - File src/app/(dashboard)/clinica/crc/indicacoes/page.tsx exists; `grep -q "listReferrals" ...`
    - `grep -Eq "Pendente|formatBRL" src/components/crc/ReferralsTable.tsx`
    - `grep -q "min-h-\[72px\]" "src/app/(dashboard)/clinica/crc/indicacoes/page.tsx"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>Referral KPIs + referrals list with stage + reward status.</done>
</task>

<task type="auto">
  <name>Task 2: PatientRewardsBalanceTable + credit statement Sheet (D-19)</name>
  <files>src/components/crc/PatientRewardsBalanceTable.tsx</files>
  <read_first>
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"7 Seção: Saldo por Paciente Indicador"
    - src/actions/referrals.ts (listRewardsBalance shape)
  </read_first>
  <action>
    PatientRewardsBalanceTable.tsx ('use client'): table from listRewardsBalance — columns Paciente, Indicações Convertidas, Saldo Total de Crédito (formatBRL), Saldo Utilizado (formatBRL — 0 in v1), Saldo Disponível (formatBRL, green semibold). Per-row DropdownMenu → "Ver Extrato" opens a shadcn Sheet showing that patient's credit history (list of referral_rewards credits — read-only, NO manual balance edit in this phase; Open Question 2: no 'uso' rows). Label copy "Crédito de indicação: {valor} — disponível para uso em serviços" per Copywriting. Rendered as a toggle/second section on the indicações page.
  </action>
  <acceptance_criteria>
    - `grep -q "listRewardsBalance\|Saldo Disponível" src/components/crc/PatientRewardsBalanceTable.tsx`
    - `grep -q "Sheet" src/components/crc/PatientRewardsBalanceTable.tsx`
    - `! grep -q "editar saldo\|input.*saldo" src/components/crc/PatientRewardsBalanceTable.tsx` (read-only)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>Per-patient balance + read-only credit statement; portal-ready model, no manual edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| authenticated client → referral reads | RLS-scoped read-only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-34 | Tampering (financial) | manual balance edit from UI | mitigate | No write action exposed — balance is derived server-side (Plan 04); UI is read-only, credits only via creditReferralReward on conversion. |
| T-18-35 | Information Disclosure | cross-tenant reward exposure | mitigate | listReferrals/listRewardsBalance use createClient() RLS scoping. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Indicações page renders KPIs + referrals + balance
</verification>

<success_criteria>
Internal referral rewards visibility complete; read-only; modeled for Phase 20 Portal exposure.
</success_criteria>

<output>
After completion, create `.planning/phases/18-crc-marketing/18-11-SUMMARY.md`
</output>
