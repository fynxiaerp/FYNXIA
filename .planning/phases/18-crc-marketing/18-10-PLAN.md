---
phase: 18-crc-marketing
plan: 10
type: execute
wave: 3
depends_on: [18-06]
files_modified:
  - src/app/(dashboard)/clinica/crc/nps/page.tsx
  - src/components/crc/NpsScoreCard.tsx
  - src/components/crc/NpsResponsesTable.tsx
  - src/components/crc/DetractorAlertBanner.tsx
  - src/app/nps/[patient-id]/[token]/page.tsx
  - src/components/crc/NpsPublicForm.tsx
autonomous: false
requirements: [CRC-04]
gap_closure: false

must_haves:
  truths:
    - "NPS panel shows the score (+N), promotor/neutro/detrator breakdown, and recent responses (D-14)"
    - "A destructive banner surfaces untreated detractors; staff can mark them treated (D-15)"
    - "Public /nps/[patient-id]/[token] renders a 0-10 form, submits once, and shows a thank-you (D-13)"
    - "Invalid/expired/used token shows a full-page error; detractor status is never revealed to the patient"
  artifacts:
    - path: "src/app/nps/[patient-id]/[token]/page.tsx"
      provides: "Public NPS form route"
      contains: "isTokenValid"
    - path: "src/components/crc/NpsPublicForm.tsx"
      provides: "0-10 useState form"
      contains: "aria-pressed"
  key_links:
    - from: "src/components/crc/NpsPublicForm.tsx"
      to: "src/actions/nps.ts"
      via: "submitNpsPublic"
      pattern: "submitNpsPublic"
    - from: "src/app/(dashboard)/clinica/crc/nps/page.tsx"
      to: "src/actions/nps.ts"
      via: "getNpsSummary + listNpsResponses"
      pattern: "getNpsSummary"
---

<objective>
Build the NPS surfaces (CRC-04): the internal NPS panel (score card, promotor/neutro/detrator KPIs, detractor alert + treatment, recent responses table) and the public mobile-first single-field NPS form at /nps/[patient-id]/[token] (mirrors the anamnese public-token page, forced light theme).

Purpose: Completes CRC-04's user-facing loop. Consumes Plan 06 actions. Contains a human-verify checkpoint (cross-device link→form→submit — VALIDATION Manual-Only).
Output: NPS panel + 3 CRC components + public route + NpsPublicForm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-crc-marketing/18-UI-SPEC.md

<interfaces>
From src/actions/nps.ts (Plan 06): submitNpsPublic({ patientId, token, score, comment? }), markDetractorTreated, listNpsResponses, getNpsSummary.
From src/lib/crc/roi-math.ts (Plan 01): classifyNps (for badge), computeNpsScore (already used in getNpsSummary).
Public token page precedent: src/app/anamnese/[patient-id]/[token]/page.tsx (Server Component; createAdminClient read; full-page error on invalid). isTokenValid from src/lib/validators/anamnesis.ts (reuse or add isNpsTokenValid with the same shape — token_used_at + token_expires_at + score IS NULL).
UI-SPEC §6: force light theme (`<div className="light">` wrapper, NOT next-themes), mobile-first px-4, 0-10 buttons size-10 flex-wrap, buttons are real `<button type="button">` with aria-pressed. Post-submit swaps to thank-you (CheckCircle2 green). NO progress indicator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NPS panel page + ScoreCard + KPIs + DetractorAlertBanner + ResponsesTable</name>
  <files>src/app/(dashboard)/clinica/crc/nps/page.tsx, src/components/crc/NpsScoreCard.tsx, src/components/crc/DetractorAlertBanner.tsx, src/components/crc/NpsResponsesTable.tsx</files>
  <read_first>
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"5. Painel de NPS" (score card, KPIs, alert, table)
    - src/components/financeiro/NfseKpiRow.tsx (KPI card pattern)
  </read_first>
  <action>
    /clinica/crc/nps/page.tsx (RSC): PageHeader + `<main className="p-6 max-w-5xl mx-auto w-full space-y-6">`; nuqs date range + unit filter; fetch getNpsSummary + listNpsResponses. Layout order: NpsScoreCard, then DetractorAlertBanner (if detractorsPending>0), then secondary KPI cards (Promotores green / Neutros amber / Detratores red, grid sm:grid-cols-3), then NpsResponsesTable. Empty state "Nenhuma resposta de NPS ainda" (no CTA — collection is automatic).
    NpsScoreCard.tsx: centered Card, "NPS Score" label, big `text-2xl font-semibold font-display tabular-nums` number with sign ("+42"), color green if score>=0 else red-600, plus "{p}% promotores · {n}% neutros · {d}% detratores".
    DetractorAlertBanner.tsx: `Alert variant="destructive"` with AlertTriangle: "{N} avaliação(ões) detratora(s) aguardando retorno da equipe." (D-15).
    NpsResponsesTable.tsx: columns Paciente, Nota (big number + classification Badge Promotor/Neutro/Detrator via classifyNps), Comentário (truncate 2 lines, Popover to expand), Data (dd/MM/yyyy), Status (detractors only: "Pendente"/"Tratado" + DropdownMenu "Marcar como Tratado" → markDetractorTreated). Badge colors per UI-SPEC §Color.
  </action>
  <acceptance_criteria>
    - File src/app/(dashboard)/clinica/crc/nps/page.tsx exists; `grep -q "getNpsSummary" ...`
    - `grep -q "tabular-nums" src/components/crc/NpsScoreCard.tsx`
    - `grep -q "markDetractorTreated" src/components/crc/NpsResponsesTable.tsx`
    - `grep -q "aguardando retorno" src/components/crc/DetractorAlertBanner.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>NPS panel with score card, detractor alert + treatment, responses table.</done>
</task>

<task type="auto">
  <name>Task 2: Public NPS route + NpsPublicForm (single-use, light theme)</name>
  <files>src/app/nps/[patient-id]/[token]/page.tsx, src/components/crc/NpsPublicForm.tsx</files>
  <read_first>
    - src/app/anamnese/[patient-id]/[token]/page.tsx (exact structure to mirror — read fully)
    - src/lib/validators/anamnesis.ts (isTokenValid)
    - .planning/phases/18-crc-marketing/18-UI-SPEC.md §"6. Formulário Público de NPS" + §"Acessibilidade" (public form rules)
  </read_first>
  <action>
    /nps/[patient-id]/[token]/page.tsx (Server Component, no auth): createAdminClient() read of nps_responses by token + patient_id (select id, token_used_at, token_expires_at, score, clinic name via join for the question). isValid = row exists AND isTokenValid(...) AND score IS NULL. If invalid → full-page error per Copywriting "Link de Avaliação Inválido" (mirror anamnese error block, AlertCircle). If valid → render NpsPublicForm wrapped in a forced-light container (`<div className="light">` — NOT next-themes) per UI-SPEC §6 theme rule. Header FYNXIA + "Sua opinião importa" + the NPS question with clinic name.
    NpsPublicForm.tsx ('use client', useState only — NO react-hook-form): 0-10 scale as 11 real `<button type="button">` (size-10, flex-wrap, aria-pressed on selected, selected 7-10 uses primary bg per UI-SPEC — but do NOT color-code detractor range differently; never reveal classification). Optional comment Textarea (maxLength 500, placeholder per Copywriting). "Enviar Avaliação" disabled until a score is selected → submitNpsPublic({ patientId, token, score, comment }). On success, swap the container to the thank-you state ("Obrigado pela sua avaliação!" + CheckCircle2 green size-14) — same container, no route change. On failure show the generic error.
  </action>
  <acceptance_criteria>
    - File src/app/nps/[patient-id]/[token]/page.tsx exists
    - `grep -Eq "isTokenValid|isNpsTokenValid" "src/app/nps/[patient-id]/[token]/page.tsx"`
    - `grep -q "className=\"light\"\|light" "src/app/nps/[patient-id]/[token]/page.tsx"` (forced light theme)
    - `grep -q "aria-pressed" src/components/crc/NpsPublicForm.tsx`
    - `grep -q "submitNpsPublic" src/components/crc/NpsPublicForm.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5; grep -q "aria-pressed" src/components/crc/NpsPublicForm.tsx && echo A11Y_OK</automated>
  </verify>
  <done>Public single-field NPS form, single-use, forced light theme, accessible; thank-you + error states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify public NPS link → form → submit → panel classification</name>
  <action>Manual verification only — no code changes. Walk through the public link→form→submit→panel flow described below.</action>
  <what-built>Public NPS token form (mobile-first, single-use) and the internal panel that classifies promotores/neutros/detratores and alerts on detractors.</what-built>
  <read_first>.planning/phases/18-crc-marketing/18-VALIDATION.md §"Manual-Only" (Formulário público de NPS via token)</read_first>
  <how-to-verify>
    1. Trigger an invite: run the nps-scan cron locally (GET /api/cron/nps-scan with the CRON auth header) against a clinic that has a 'concluido' appointment, OR insert a test nps_responses row; copy its /nps/{patientId}/{token} URL.
    2. Open the URL on a phone (or mobile viewport): confirm forced light theme, 0-10 buttons tappable, submit disabled until a score is chosen.
    3. Submit a score (e.g. 9) + comment → thank-you screen appears.
    4. Reload the same URL → "Link de Avaliação Inválido" (single-use enforced).
    5. Submit a 0-6 score from another token → in /clinica/crc/nps the detractor banner appears and the response is classified "Detrator"; mark it treated and confirm the banner count drops.
    6. Confirm the score card shows the correct +N and breakdown.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public (no auth) → NPS form | Token is the only credential |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-31 | Spoofing | forged/guessed token | mitigate | Server reads by token+patient_id; submit atomic single-use (Plan 06); render check is not the boundary. |
| T-18-32 | Information Disclosure | revealing detractor status to patient | mitigate | Public form uses no classification coloring; 0-6 selection looks identical to 7-10; classify is internal only. |
| T-18-33 | Tampering | double submit / replay | mitigate | submitNpsPublic atomic UPDATE WHERE token_used_at IS NULL AND score IS NULL. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Human checkpoint: public form single-use + panel classification approved
</verification>

<success_criteria>
NPS panel + single-use public form live; detractor loop closable; patient never sees classification.
</success_criteria>

<output>
After completion, create `.planning/phases/18-crc-marketing/18-10-SUMMARY.md`
</output>
