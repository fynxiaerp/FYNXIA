---
phase: 19-relat-rios-or-amento-bi
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/proxy.ts
  - src/components/shell/nav-config.ts
  - src/components/shell/nav-icons.tsx
  - src/components/relatorios/charts.tsx
  - src/__tests__/rbac-phase19.test.ts
autonomous: true
requirements: [REP-01, REP-02, REP-03, BI-01, BI-02]
must_haves:
  truths:
    - "New ModuleKeys relatorios/orcamento/societario exist with correct role access (D-09/D-14/D-24)"
    - "socio has WRITE (not readOnly) on orcamento (D-14); readOnly on relatorios/societario/bi"
    - "Route prefixes /clinica/relatorios, /clinica/orcamento, /clinica/societario resolve to their modules (most-specific-first)"
    - "Sidebar shows 4 new menu entries: Relatórios, Orçamento, Societário, BI (D-41)"
    - "New non-prototipos chart primitives exist (KpiCard/ChartCard/LineChart/BarChart/DonutChart/OccupancyBar) (D-37)"
  artifacts:
    - path: "src/components/relatorios/charts.tsx"
      provides: "Production chart primitives outside the prototipos namespace (D-37)"
      exports: ["KpiCard", "ChartCard", "LineChart", "BarChart", "DonutChart", "OccupancyBar", "DeltaBadge"]
    - path: "src/proxy.ts"
      provides: "RBAC entries for the 4 new modules"
      contains: "relatorios"
  key_links:
    - from: "src/app/(dashboard)/clinica/relatorios/page.tsx (Plan 10)"
      to: "src/components/relatorios/charts.tsx"
      via: "import { KpiCard }"
      pattern: "components/relatorios/charts"
    - from: "proxy routeToModule"
      to: "MODULE_PERMISSIONS"
      via: "ROUTE_MODULE_MAP prefixes"
      pattern: "/clinica/relatorios"
---

<objective>
Lay the UI foundation shared by all 4 screens: RBAC ModuleKeys + route map + role matrix (honoring D-09/D-14/D-24), sidebar nav entries (D-41), and a new production chart-primitive namespace (D-37, copied technique from prototipos, zero new deps). Single-owner of the shared files (proxy.ts, nav) so Wave-3 UI plans stay parallel.

Purpose: Prevent the socio×financeiro readOnly conflict (Pitfall 1) and give UI plans ready-to-import chart components.
Output: proxy.ts + nav-config.ts + nav-icons.tsx + charts.tsx + rbac test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md
@.planning/phases/19-relat-rios-or-amento-bi/19-RESEARCH.md
@.planning/phases/19-relat-rios-or-amento-bi/19-UI-SPEC.md

<interfaces>
Current proxy (src/proxy.ts): ModuleKey union line 14; MODULE_PERMISSIONS lines 21-33; ROUTE_MODULE_MAP lines 37-52 (most-specific-first). Existing socio row: `socio: { financeiro:{allowed:true,readOnly:true}, bi:{allowed:true,readOnly:true}, config:{...readOnly}, ... }`.
Existing /bi ModuleKey is ALREADY wired (line 14/49) with admin/superadmin full + socio/auditor/dpo readOnly — reuse as-is, do NOT re-add.
Nav (src/components/shell/nav-config.ts): NavIconKey union + ALL_NAV_ITEMS array; nav-icons.tsx maps keys → Lucide.
Chart source to copy (D-37, reference-only — do NOT import from here): src/components/prototipos/charts.tsx (KpiCard, ChartCard, LineChart, BarChart, DonutChart, DeltaBadge — pure SVG/CSS, tokens chart-1..chart-5/primary).
OccupancyBar pattern (UI-SPEC): `<div className="h-1.5 w-20 rounded-full bg-muted overflow-hidden"><div className="h-full rounded-full bg-primary" style={{width}}/></div>`.
rbac test style: src/__tests__/rbac.test.ts (asserts isPathAllowed/isReadOnly).
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-14 | Elevation of Privilege | socio blocked from budget write due to reused financeiro readOnly (Pitfall 1) | mitigate | dedicated `orcamento` ModuleKey with socio NOT readOnly (D-14) |
| T-19-15 | Elevation of Privilege | dentist/receptionist reaching financial screens | mitigate | new modules grant access only to admin/superadmin(+socio); others unlisted → denied |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: RBAC modules + route map + rbac test</name>
  <files>src/proxy.ts, src/__tests__/rbac-phase19.test.ts</files>
  <read_first>
    - src/proxy.ts (full — ModuleKey union, MODULE_PERMISSIONS, ROUTE_MODULE_MAP)
    - src/__tests__/rbac.test.ts (assertion style)
    - .planning/phases/19-relat-rios-or-amento-bi/19-UI-SPEC.md (Access Control Summary table)
  </read_first>
  <action>
    Edit src/proxy.ts:
    1. Add to the ModuleKey union: `| 'relatorios' | 'orcamento' | 'societario'` (keep existing 'bi').
    2. In MODULE_PERMISSIONS add these keys to the relevant roles:
       - superadmin: relatorios:{allowed:true}, orcamento:{allowed:true}, societario:{allowed:true}
       - admin: relatorios:{allowed:true}, orcamento:{allowed:true}, societario:{allowed:true}
       - socio: relatorios:{allowed:true, readOnly:true}, orcamento:{allowed:true}  (NOT readOnly — D-14), societario:{allowed:true, readOnly:true}
       (Do NOT grant these modules to dentist/receptionist/auditor/dpo/ti/implantacao/aluno — D-09/D-39. auditor/dpo may keep read-only if desired but CONTEXT restricts to Admin/Sócio/Superadmin — leave them OFF for these 3 new modules.)
    3. In ROUTE_MODULE_MAP add (most-specific-first, BEFORE the `/clinica` catch-all entry):
       { prefix: '/clinica/relatorios', module: 'relatorios' },
       { prefix: '/clinica/orcamento', module: 'orcamento' },
       { prefix: '/clinica/societario', module: 'societario' },
       (The existing `/bi` entry stays; BI screen uses the pre-wired top-level /bi module.)

    Create src/__tests__/rbac-phase19.test.ts asserting:
    - isPathAllowed('admin','/clinica/relatorios') === true; isPathAllowed('dentist','/clinica/relatorios') === false
    - isPathAllowed('socio','/clinica/orcamento') === true AND isReadOnly('socio','/clinica/orcamento') === false (D-14)
    - isReadOnly('socio','/clinica/relatorios') === true; isReadOnly('socio','/clinica/societario') === true
    - isPathAllowed('receptionist','/clinica/societario') === false
    - isPathAllowed('admin','/bi') === true (pre-wired module still works)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/rbac-phase19.test.ts src/__tests__/rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/rbac-phase19.test.ts src/__tests__/rbac.test.ts` exits 0 (new + existing rbac tests pass — no regression)
    - proxy.ts ModuleKey union contains `'relatorios'`, `'orcamento'`, `'societario'`
    - proxy.ts socio row: orcamento has `allowed:true` with NO `readOnly:true` on that entry
    - ROUTE_MODULE_MAP contains `/clinica/relatorios`, `/clinica/orcamento`, `/clinica/societario` BEFORE `/clinica`
  </acceptance_criteria>
  <done>Four modules gated correctly; socio can write budgets; existing rbac tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar nav entries + new chart primitives</name>
  <files>src/components/shell/nav-config.ts, src/components/shell/nav-icons.tsx, src/components/relatorios/charts.tsx</files>
  <read_first>
    - src/components/shell/nav-config.ts (NavIconKey + ALL_NAV_ITEMS)
    - src/components/shell/nav-icons.tsx (key→Lucide mapping)
    - src/components/prototipos/charts.tsx (technique to copy — pure SVG/CSS, do NOT import it)
  </read_first>
  <action>
    Edit nav-config.ts: add NavIconKey values `'relatorios' | 'orcamento' | 'societario' | 'bi'`. Add to ALL_NAV_ITEMS (adminOnly: true — these are admin/socio/superadmin screens; the sidebar's adminOnly flag hides them from dentist/receptionist, RBAC is the real gate):
    - { href: '/clinica/relatorios', label: 'Relatórios', icon: 'relatorios', adminOnly: true }
    - { href: '/clinica/orcamento', label: 'Orçamento', icon: 'orcamento', adminOnly: true }
    - { href: '/clinica/societario', label: 'Societário', icon: 'societario', adminOnly: true }
    - { href: '/bi', label: 'BI', icon: 'bi', adminOnly: true }
    Edit nav-icons.tsx: map the 4 new keys to Lucide icons (relatorios→FileBarChart, orcamento→Target, societario→PieChart, bi→LineChart or TrendingUp).

    Create src/components/relatorios/charts.tsx: copy the pure-SVG/CSS technique from prototipos/charts.tsx (NEW code, NOT an import from prototipos — D-37). Export KpiCard, ChartCard, LineChart, BarChart, DonutChart, DeltaBadge with the SAME props/behavior as the prototype versions, PLUS a new `OccupancyBar({ value }: { value: number })` using the UI-SPEC pattern (`bg-muted` track, `bg-primary` fill, width = value%). Use only the declared design tokens (chart-1..chart-5/primary/muted). Follow the Typography contract: KPI value = Display 24px/600 tabular-nums .font-display; do NOT introduce font-medium(500) or font-bold(700).
  </action>
  <verify>
    <automated>grep -q "'/clinica/relatorios'" src/components/shell/nav-config.ts && grep -q "'/bi'" src/components/shell/nav-config.ts && grep -q "export function OccupancyBar" src/components/relatorios/charts.tsx && grep -q "export function KpiCard" src/components/relatorios/charts.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - nav-config.ts ALL_NAV_ITEMS contains hrefs `/clinica/relatorios`, `/clinica/orcamento`, `/clinica/societario`, `/bi`
    - nav-config.ts NavIconKey union contains `'relatorios'`, `'orcamento'`, `'societario'`, `'bi'`
    - nav-icons.tsx maps all 4 new keys
    - charts.tsx exports KpiCard, ChartCard, LineChart, BarChart, DonutChart, OccupancyBar, DeltaBadge
    - charts.tsx does NOT contain `from '@/components/prototipos` (D-37 — new namespace, no reuse)
    - charts.tsx does NOT contain `font-bold` or `font-medium`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>4 nav entries added; production chart namespace exists with OccupancyBar, no prototipos import.</done>
</task>

</tasks>

<verification>
- rbac tests green; `npx tsc --noEmit` passes
- charts.tsx is self-contained (no prototipos import)
</verification>

<success_criteria>
Shared UI foundation ready: modules gated per CONTEXT, nav present, chart primitives importable by all 4 screen plans.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-09-SUMMARY.md`.
</output>
