---
phase: 19-relat-rios-or-amento-bi
plan: 12
type: execute
wave: 3
depends_on: [06, 09]
files_modified:
  - src/app/(dashboard)/clinica/societario/page.tsx
  - src/components/relatorios/PartnerDistribution.tsx
  - src/components/relatorios/PartnerShareFormDialog.tsx
  - src/app/api/societario/pdf/route.ts
  - src/components/relatorios/SocietarioPdf.tsx
autonomous: true
requirements: [REP-03]
must_haves:
  truths:
    - "Each sócio row shows R$ calculado (percentual × resultado); negative prejuízo shows in destructive with a minus sign (D-23/D-27)"
    - "Admin/Superadmin see all sócios; a sócio sees only their own cota as a single Card (D-24)"
    - "'Nova vigência' opens a form that blocks save unless percentuais sum to 100% (D-22)"
    - "'Encerrar vigência' is a destructive confirmation (D-20 history)"
    - "Distribution is purely informative — no financial posting (D-26); Exportar PDF available (D-40)"
  artifacts:
    - path: "src/app/(dashboard)/clinica/societario/page.tsx"
      provides: "Societário screen (REP-03)"
    - path: "src/components/relatorios/PartnerShareFormDialog.tsx"
      provides: "Nova vigência form with 100% validation"
  key_links:
    - from: "src/app/(dashboard)/clinica/societario/page.tsx"
      to: "src/actions/partner-shares.ts"
      via: "import getPartnerDistribution / createPartnerShareVigencia"
      pattern: "getPartnerDistribution"
---

<objective>
Build the Societário screen (REP-03): per-sócio R$ distribution (negatives preserved), admin-all vs sócio-own-row view, "Nova vigência" form with the blocking 100% validation, "Encerrar vigência" destructive confirm, vigência history, and PDF export.

Purpose: Surface the Plan 06 partner-share actions per the UI-SPEC.
Output: societario page + PartnerDistribution + PartnerShareFormDialog + PDF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-UI-SPEC.md
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md

<interfaces>
Actions (Plan 06): src/actions/partner-shares.ts → listSocios, listPartnerShares, createPartnerShareVigencia, closePartnerShareVigencia, getPartnerDistribution.
UI-SPEC Societário specifics: per-sócio R$ = Display 24px/600 tabular-nums; negative → destructive token + leading minus (D-27); sócio own-row-only → single Card (no table chrome, D-24); vigência history = read-only expandable list (edit = create new vigência); "Nova vigência" = Plus icon (admin/superadmin only); destructive "Encerrar vigência" copy: "Encerrar vigência de {nome_sócio}? A cota deixará de valer a partir de hoje. Esta ação não pode ser desfeita." Inline validation copy: "A soma dos percentuais deve ser exatamente 100%. Ajuste os valores antes de salvar." (blocks save).
Role of viewer: read x-user-role header in the RSC page; when role='socio' render the single-Card own-row view.
Dialog/AlertDialog: installed shadcn primitives (@base-ui Button render-prop convention — see any existing *FormDialog, e.g. CostCenterFormDialog).
PDF: same template as Plan 10/11.
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Information Disclosure | sócio seeing other sócios in UI | mitigate | RLS already filters (Plan 03); UI renders single Card for socio, no table implying hidden rows |
| T-19-09 | Integrity | saving a non-100% vigência | mitigate | client-side sum check disables save + server-side assertSharesValid (Plan 06) returns the inline error |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: societario page + PartnerDistribution + PartnerShareFormDialog</name>
  <files>src/app/(dashboard)/clinica/societario/page.tsx, src/components/relatorios/PartnerDistribution.tsx, src/components/relatorios/PartnerShareFormDialog.tsx</files>
  <read_first>
    - src/actions/partner-shares.ts (Plan 06 signatures)
    - src/components/financeiro (an existing *FormDialog for the @base-ui Button render-prop + Dialog pattern, e.g. CostCenterFormDialog)
    - src/components/relatorios/charts.tsx (KpiCard for R$ anchor if needed)
  </read_first>
  <action>
    Create RSC page src/app/(dashboard)/clinica/societario/page.tsx: PageHeader (title 'Societário', actions = período selector + "Exportar PDF"; plus "Nova vigência" button (Plus icon) shown ONLY when role ∈ ['admin','superadmin']). Read role from x-user-role header. Call getPartnerDistribution({ from, to }) + listPartnerShares() + listSocios(). If role='socio' pass a flag to render the single-Card own view.
    Create client component src/components/relatorios/PartnerDistribution.tsx:
    - Admin/superadmin: a list/table of sócios each showing name + percentual Badge (variant="secondary") + R$ calculado (Display 24px/600 tabular-nums). Negative valor → destructive color + leading '-' (D-27), never hidden/zeroed.
    - socio own-row: a single Card (no table chrome) with the same fields (D-24).
    - Vigência history: read-only expandable list of past vigências (vigencia_inicio/fim). Each open vigência has an "Encerrar vigência" destructive AlertDialog (admin/superadmin) with the exact copy above → closePartnerShareVigencia.
    - Empty state: heading "Nenhuma cota societária cadastrada" / body "Cadastre os sócios e seus percentuais para visualizar a distribuição de lucro."
    Create src/components/relatorios/PartnerShareFormDialog.tsx ("Nova vigência"): a form listing sócios (from listSocios) with percentual inputs + a vigenciaInicio date. Compute the running sum client-side; DISABLE "Salvar" (primary) while sum ≠ 100% and show inline "A soma dos percentuais deve ser exatamente 100%. Ajuste os valores antes de salvar." On submit → createPartnerShareVigencia (server re-validates — D-22 defense-in-depth).
    Typography: 2 weights only.
  </action>
  <verify>
    <automated>grep -q "getPartnerDistribution" "src/app/(dashboard)/clinica/societario/page.tsx" && grep -q "A soma dos percentuais deve ser exatamente 100%" src/components/relatorios/PartnerShareFormDialog.tsx && grep -q "Encerrar vigência" src/components/relatorios/PartnerDistribution.tsx && grep -q "Nenhuma cota societária cadastrada" src/components/relatorios/PartnerDistribution.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports from `@/actions/partner-shares`; "Nova vigência" gated to admin/superadmin (role check present)
    - PartnerShareFormDialog.tsx disables save unless sum is 100% and shows the exact 100% copy
    - PartnerDistribution.tsx renders negative valores with the destructive token + minus sign (contains a negative-branch using `--destructive`/`text-destructive`)
    - PartnerDistribution.tsx renders a single Card path for role='socio' (no table for that branch)
    - PartnerDistribution.tsx contains the "Encerrar vigência" AlertDialog copy
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Societário distribution + vigência form/close + own-row privacy per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: SocietarioPdf + societario/pdf route</name>
  <files>src/components/relatorios/SocietarioPdf.tsx, src/app/api/societario/pdf/route.ts</files>
  <read_first>
    - src/app/api/estoque/anvisa-pdf/route.ts (route template)
    - src/actions/partner-shares.ts (getPartnerDistribution)
  </read_first>
  <action>
    Create src/components/relatorios/SocietarioPdf.tsx (@react-pdf/renderer, Flexbox only, Roboto): período + per-sócio percentual + R$ calculado table (negatives shown with minus).
    Create src/app/api/societario/pdf/route.ts: runtime nodejs, getUser → tenant_id + role; gate — admin/superadmin get full distribution; a socio only their own row (call getPartnerDistribution which is already RLS-scoped, so the socio's PDF naturally contains only their row). Others 403. renderToBuffer, Cache-Control no-store.
  </action>
  <verify>
    <automated>grep -q "runtime = 'nodejs'" src/app/api/societario/pdf/route.ts && grep -q "no-store" src/app/api/societario/pdf/route.ts && grep -q "getPartnerDistribution" src/app/api/societario/pdf/route.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - societario/pdf/route.ts contains `runtime = 'nodejs'` + `no-store`
    - route denies dentist/receptionist (role gate present)
    - SocietarioPdf.tsx uses `@react-pdf/renderer`, no `display: 'grid'`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Societário PDF works, respects per-sócio scope, no-store.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes; `npm test` green
- Manual (19-VALIDATION.md): sócio session shows only own cota
</verification>

<success_criteria>
REP-03 screen shipped matching the UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-12-SUMMARY.md`.
</output>
