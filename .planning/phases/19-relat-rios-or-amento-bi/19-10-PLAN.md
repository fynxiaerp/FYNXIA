---
phase: 19-relat-rios-or-amento-bi
plan: 10
type: execute
wave: 3
depends_on: [04, 09]
files_modified:
  - src/app/(dashboard)/clinica/relatorios/page.tsx
  - src/components/relatorios/DreView.tsx
  - src/components/relatorios/DrePdf.tsx
  - src/app/api/relatorios/dre-pdf/route.ts
autonomous: true
requirements: [REP-01]
must_haves:
  truths:
    - "Gestor selects período (mês/ano or custom range) and unidade (Todas or one) and sees the DRE (D-01/D-02/D-03)"
    - "Consolidated view shows a per-unit ranking table (D-04)"
    - "Each line shows % of revenue (D-08), expands to cost centers (D-06) and drills to transactions (D-05)"
    - "YoY comparison shows or degrades to 'comparação indisponível' (D-11)"
    - "Exportar PDF produces a DRE PDF (D-07)"
  artifacts:
    - path: "src/app/(dashboard)/clinica/relatorios/page.tsx"
      provides: "DRE screen (REP-01)"
    - path: "src/app/api/relatorios/dre-pdf/route.ts"
      provides: "DRE PDF export route"
  key_links:
    - from: "src/app/(dashboard)/clinica/relatorios/page.tsx"
      to: "src/actions/dre.ts"
      via: "import getDre / getDreByUnit"
      pattern: "getDre"
---

<objective>
Build the DRE screen (REP-01) per the UI-SPEC: period/unit selectors, KPI row (Faturamento/Despesa/Resultado/Margem), consolidated per-unit ranking, accordion drill-down + cost-center expansion, % of revenue, YoY, and PDF export.

Purpose: Surface the Plan 04 DRE actions in a production screen matching the design contract.
Output: relatorios page + DreView + DrePdf + PDF route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-UI-SPEC.md
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md

<interfaces>
Actions (Plan 04): src/actions/dre.ts → getDre, getDreByUnit, getDreYoY, getDreDrilldown.
Charts (Plan 09): src/components/relatorios/charts.tsx → KpiCard, ChartCard, LineChart, OccupancyBar, DeltaBadge.
PDF template to mirror: src/app/api/estoque/anvisa-pdf/route.ts + src/components/estoque/AnvisaReportPdf.tsx (Font.register Roboto, Flexbox-only, runtime nodejs, getUser+tenant_id, renderToBuffer, Cache-Control no-store).
Layout contract (UI-SPEC): PageHeader (existing) + `<div className="p-6 max-w-6xl mx-auto w-full space-y-6">`; KPI row `grid grid-cols-2 lg:grid-cols-4 gap-4`; table wrapper `rounded-xl bg-card ring-1 ring-foreground/10 overflow-hidden` with `px-4 py-3 border-b` header. Accordion (installed) for drill-down/cost-center. nuqs for period/unit URL state. Empty/error copy per UI-SPEC Per-screen table (Relatórios row).
Access: server component reads x-user-role header / actor role — page already RBAC-gated by proxy (Plan 09).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: relatorios page + DreView (selectors, KPI row, ranking, drill-down)</name>
  <files>src/app/(dashboard)/clinica/relatorios/page.tsx, src/components/relatorios/DreView.tsx</files>
  <read_first>
    - src/actions/dre.ts (Plan 04 action signatures)
    - src/components/relatorios/charts.tsx (Plan 09 primitives)
    - src/app/(dashboard)/clinica/prototipos/relatorios/page.tsx (reference UX only — do not import)
    - src/app/(dashboard)/clinica/prototipos/dashboard-franquias/page.tsx (ranking table reference)
  </read_first>
  <action>
    Create the RSC page src/app/(dashboard)/clinica/relatorios/page.tsx: PageHeader (title 'Relatórios', breadcrumbs [{label:'Relatórios'}], actions slot = period+unit selector + "Exportar PDF" button linking to /api/relatorios/dre-pdf with the current period/unit query). Read period/unit from searchParams (nuqs on the client selector). Call getDre + getDreByUnit (when unit='Todas') server-side and pass to DreView.
    Create the client component src/components/relatorios/DreView.tsx:
    - KPI row (Faturamento, Despesa, Resultado, Margem) using KpiCard (Display 24px value, Resultado is the primary anchor). Positive resultado uses primary emphasis; negative uses --destructive.
    - When unit='Todas': render the per-unit ranking table below (position, unidade name + sub-line, Receita/Despesa/Resultado/Margem, OccupancyBar for ocupação if available, DeltaBadge) — mirror dashboard-franquias column set.
    - DRE lines table: each line shows R$ + `· {pct}% da receita` (Label 12px muted tabular-nums, D-08). Use Accordion to expand a line into its cost centers (D-06) and, on click, fetch getDreDrilldown to show the underlying transactions (D-05).
    - YoY: render getDreYoY result via DeltaBadge; when available:false show "comparação indisponível" text (D-11), never crash.
    - Empty state: heading "Nenhum lançamento neste período" / body "Ajuste o período ou a unidade selecionada para visualizar o DRE." Error state with "Tentar novamente".
    Use only declared tokens/typography (no font-bold/medium; 2 weights only).
  </action>
  <verify>
    <automated>grep -q "getDre" "src/app/(dashboard)/clinica/relatorios/page.tsx" && grep -q "da receita" src/components/relatorios/DreView.tsx && grep -q "Nenhum lançamento neste período" src/components/relatorios/DreView.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports from `@/actions/dre` and `@/components/relatorios/charts` (NOT from prototipos)
    - DreView.tsx contains `da receita` (% of revenue, D-08)
    - DreView.tsx contains the empty-state heading `Nenhum lançamento neste período`
    - DreView.tsx contains `comparação indisponível`
    - DreView.tsx uses `Accordion`
    - No `font-bold`/`font-medium` in DreView.tsx
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>DRE screen renders selectors, KPIs, ranking, drill-down, %, YoY, with correct copy/tokens.</done>
</task>

<task type="auto">
  <name>Task 2: DrePdf component + dre-pdf route</name>
  <files>src/components/relatorios/DrePdf.tsx, src/app/api/relatorios/dre-pdf/route.ts</files>
  <read_first>
    - src/components/estoque/AnvisaReportPdf.tsx (Font.register + Flexbox StyleSheet)
    - src/app/api/estoque/anvisa-pdf/route.ts (route: runtime nodejs, getUser+tenant_id, renderToBuffer, no-store)
    - src/actions/dre.ts (getDre for the PDF data)
  </read_first>
  <action>
    Create src/components/relatorios/DrePdf.tsx (@react-pdf/renderer, Flexbox only, Roboto registered for pt-BR): A4 doc rendering clinicName, período label, unidade, the DRE lines (conta, R$, % receita) and totals (Faturamento/Despesa/Resultado/Margem).
    Create src/app/api/relatorios/dre-pdf/route.ts: `export const runtime='nodejs'`, `maxDuration=30`. GET: supabase.auth.getUser() → resolve users.tenant_id + role; enforce role ∈ ['admin','socio','superadmin'] (401/403 otherwise, D-09); read from/to/unitId from query; call getDre; renderToBuffer(createElement(DrePdf, props)); return Response with Content-Type application/pdf, Content-Disposition attachment filename="dre.pdf", Cache-Control 'no-store, no-cache, must-revalidate'.
  </action>
  <verify>
    <automated>grep -q "runtime = 'nodejs'" src/app/api/relatorios/dre-pdf/route.ts && grep -q "no-store" src/app/api/relatorios/dre-pdf/route.ts && grep -q "renderToBuffer" src/app/api/relatorios/dre-pdf/route.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - dre-pdf/route.ts contains `export const runtime = 'nodejs'`
    - dre-pdf/route.ts contains `Cache-Control` with `no-store`
    - dre-pdf/route.ts enforces role in ['admin','socio','superadmin']
    - DrePdf.tsx uses `@react-pdf/renderer` and does NOT use CSS Grid (no `display: 'grid'`)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>DRE PDF export works, role-gated, no-store, Flexbox-only.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes; `npm test` green
- Manual (per 19-VALIDATION.md): drill-down opens filtered transactions
</verification>

<success_criteria>
REP-01 screen shipped matching the UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-10-SUMMARY.md`.
</output>
