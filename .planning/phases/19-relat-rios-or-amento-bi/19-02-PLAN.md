---
phase: 19-relat-rios-or-amento-bi
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/bi/forecast-math.ts
  - src/lib/bi/__tests__/forecast-math.test.ts
  - src/__tests__/governance/bi-forecast-agent.test.ts
autonomous: true
requirements: [BI-01, BI-02]
must_haves:
  truths:
    - "Linear trend (OLS) computes slope/intercept/projectedNext matching known values"
    - "insufficientData is true when fewer than 3 monthly points are supplied (D-32)"
    - "Decline-vs-trend detection fires only when actual is >15% below projection (D-33b)"
    - "A source-inspection guard asserts the BI agent (built in Plan 08) calls withAgentPolicy per-clinic, never aggregate"
  artifacts:
    - path: "src/lib/bi/forecast-math.ts"
      provides: "Pure linear-regression / trend functions"
      exports: ["computeLinearTrend", "isDecliningVsTrend"]
    - path: "src/__tests__/governance/bi-forecast-agent.test.ts"
      provides: "RED source-inspection guard for Plan 08's agent (per-clinic withAgentPolicy)"
  key_links:
    - from: "src/lib/agents/bi-forecast-agent.ts (Plan 08)"
      to: "src/lib/bi/forecast-math.ts"
      via: "import computeLinearTrend"
      pattern: "computeLinearTrend"
---

<objective>
Create the pure BI forecasting math (ordinary least-squares linear trend + decline detection) test-first, and lay down a RED source-inspection guard that the Plan 08 forecast agent will satisfy (per-clinic `withAgentPolicy`, never aggregate). Zero new dependencies (hand-rolled OLS per RESEARCH "Deliberate exception").

Purpose: Give the nightly BI cron (Plan 08) a tested, dependency-free trend calculator, and pre-commit the governance contract before the agent exists.
Output: One pure lib + test (green) + one governance test (RED until Plan 08 lands).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-RESEARCH.md
@.planning/phases/19-relat-rios-or-amento-bi/19-VALIDATION.md

<interfaces>
Reference pure-function + test style: src/lib/financeiro/payout-math.ts + its __tests__.
Existing governance/regression test style to mirror: src/__tests__/governance/policy.test.ts and src/__tests__/governance/approvals.test.ts (they read a source file with fs.readFileSync and assert on its contents).

OLS formula (from RESEARCH Code Examples — implement exactly):
slope = Σ((x-x̄)(y-ȳ)) / Σ((x-x̄)²), intercept = ȳ - slope·x̄, projectedNext = slope·n + intercept, x = 0..n-1.
insufficientData = points.length < 3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OLS linear trend + decline detection (forecast-math.ts)</name>
  <files>src/lib/bi/forecast-math.ts, src/lib/bi/__tests__/forecast-math.test.ts</files>
  <read_first>
    - src/lib/financeiro/payout-math.ts (pure-function convention)
    - .planning/phases/19-relat-rios-or-amento-bi/19-RESEARCH.md (Code Examples: computeLinearTrend, isDecliningVsTrend)
  </read_first>
  <behavior>
    - computeLinearTrend(points: TrendPoint[]): TrendResult
      - Test: perfectly increasing y=[10,20,30,40] (4 points) → slope=10, intercept=10, projectedNext=50, insufficientData=false
      - Test: flat y=[100,100,100] → slope=0, projectedNext=100
      - Test: 2 points → insufficientData=true, projectedNext = last value (no regression run)
      - Test: 0 points → insufficientData=true, projectedNext=0
    - isDecliningVsTrend(actual: number, projected: number, thresholdPct=15): boolean
      - Test: projected=100, actual=80 → 20% below → true
      - Test: projected=100, actual=90 → 10% below → false
      - Test: projected=100, actual=110 → above trend → false
      - Test: projected=0 → false (guard divide-by-zero)
  </behavior>
  <action>
    RED first: write src/lib/bi/__tests__/forecast-math.test.ts covering all cases; run; confirm fail.
    GREEN: create src/lib/bi/forecast-math.ts (pure, no I/O, no 'use server'). Export exactly (copy from RESEARCH Code Examples):
    - `export interface TrendPoint { month: string; value: number }`
    - `export interface TrendResult { slope: number; intercept: number; projectedNext: number; insufficientData: boolean }`
    - `export function computeLinearTrend(points: TrendPoint[]): TrendResult` — if points.length < 3 return { slope:0, intercept: last?.value ?? 0, projectedNext: last?.value ?? 0, insufficientData:true }. Otherwise OLS as in <interfaces>.
    - `export const DECLINE_THRESHOLD_PCT = 15`
    - `export function isDecliningVsTrend(actual: number, projected: number, thresholdPct = DECLINE_THRESHOLD_PCT): boolean` — projected <= 0 ? false : ((projected-actual)/projected)*100 > thresholdPct.
    Run; confirm green.
  </action>
  <verify>
    <automated>npx vitest run src/lib/bi/__tests__/forecast-math.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/bi/__tests__/forecast-math.test.ts` exits 0
    - forecast-math.ts contains `export function computeLinearTrend`
    - forecast-math.ts contains `export function isDecliningVsTrend`
    - forecast-math.ts does NOT contain `'use server'` and does NOT contain `from '@/lib/supabase`
  </acceptance_criteria>
  <done>Trend + decline functions pass every case; file is pure.</done>
</task>

<task type="auto">
  <name>Task 2: RED governance guard for the BI forecast agent</name>
  <files>src/__tests__/governance/bi-forecast-agent.test.ts</files>
  <read_first>
    - src/__tests__/governance/approvals.test.ts (fs.readFileSync source-inspection style to mirror)
    - src/lib/agents/stock-agent.ts (the per-clinic withAgentPolicy convention the agent must follow)
    - .planning/phases/19-relat-rios-or-amento-bi/19-RESEARCH.md (Pattern 4, Anti-Pattern: never call withAgentPolicy at aggregate/null clinicId)
  </read_first>
  <action>
    Create a source-inspection test at src/__tests__/governance/bi-forecast-agent.test.ts that reads the (future) agent file with fs.readFileSync and asserts its contract. Because src/lib/agents/bi-forecast-agent.ts does not exist yet, guard the read with existsSync using an ABSOLUTE path (do NOT use @-alias — TS2307 before the module exists, per D-144 convention). When the file is absent, the test must FAIL with a clear message "bi-forecast-agent.ts not yet created (Plan 08)" so it stays RED until Plan 08.
    Assert (once the file exists) that the source text:
    - contains `withAgentPolicy`
    - contains `agentKey: 'bi_forecast'` (single-quote or double-quote tolerant)
    - contains `clinicId` referenced INSIDE the withAgentPolicy call (per-row, not a literal null) — assert the source does NOT contain `clinicId: null` as the withAgentPolicy clinicId
    - contains `approval_requests` (the agent's only mutation target is the approval queue, never budget_targets directly — assert source does NOT contain `.from('budget_targets').update` / `.from("budget_targets").update`)
    Use path.join(process.cwd(), 'src/lib/agents/bi-forecast-agent.ts').
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/governance/bi-forecast-agent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - bi-forecast-agent.test.ts contains `existsSync`
    - bi-forecast-agent.test.ts contains `withAgentPolicy`
    - bi-forecast-agent.test.ts contains `bi_forecast`
    - Running the test now exits NON-zero (RED — agent file absent). This is the expected Wave-0 state; it turns green in Plan 08.
  </acceptance_criteria>
  <done>Governance guard exists and is RED (fails because the agent is not built yet); documents the contract Plan 08 must meet.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/bi/__tests__/forecast-math.test.ts` green
- Governance guard is intentionally RED until Plan 08 (record this in SUMMARY)
</verification>

<success_criteria>
Forecasting math tested and pure; the per-clinic governance contract for the BI agent is committed as an executable guard.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-02-SUMMARY.md` and note the RED governance guard explicitly.
</output>
