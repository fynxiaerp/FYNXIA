---
phase: 19-relat-rios-or-amento-bi
plan: 14
type: execute
wave: 4
depends_on: [10, 11, 12, 13]
files_modified:
  - src/app/(dashboard)/clinica/prototipos/relatorios/page.tsx
  - src/app/(dashboard)/clinica/prototipos/dashboard-franquias/page.tsx
  - src/app/(dashboard)/clinica/prototipos/page.tsx
autonomous: false
requirements: [REP-01, REP-02, REP-03, BI-01, BI-02]
must_haves:
  truths:
    - "The 4 real screens (Relatórios, Orçamento, Societário, BI) are human-verified working"
    - "The prototype pages /clinica/prototipos/relatorios and /dashboard-franquias are removed (D-42)"
    - "The prototipos index no longer links to the removed pages; build still passes"
  artifacts:
    - path: "src/app/(dashboard)/clinica/prototipos/page.tsx"
      provides: "Updated prototipos index without removed links"
  key_links:
    - from: "prototipos index"
      to: "removed pages"
      via: "links removed"
      pattern: "dashboard-franquias"
---

<objective>
Verify the 4 production screens work, then remove the superseded prototype pages (D-42): `/clinica/prototipos/relatorios` and `/clinica/prototipos/dashboard-franquias`, and clean their links from the prototipos index. Do NOT touch `prototipos/convenios`, `prototipos/nfse`, or `src/components/prototipos/charts.tsx` (still imported by production convenios/nfse pages — out of scope, Pitfall 5).

Purpose: Avoid mock + real versions coexisting once the real screens ship.
Output: 2 prototype page dirs deleted + prototipos index updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md
@.planning/phases/19-relat-rios-or-amento-bi/19-UI-SPEC.md

<interfaces>
Prototype pages to REMOVE: src/app/(dashboard)/clinica/prototipos/relatorios/, src/app/(dashboard)/clinica/prototipos/dashboard-franquias/.
Prototype pages to KEEP: convenios/, nfse/ (unrelated modules).
KEEP: src/components/prototipos/charts.tsx and src/lib/prototipos/mock-data.ts if still referenced by convenios/nfse — grep before deleting anything from mock-data.ts.
Prototipos index: src/app/(dashboard)/clinica/prototipos/page.tsx — remove the cards/links pointing to relatorios and dashboard-franquias only.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the 4 real screens before deleting prototypes</name>
  <what-built>Relatórios (DRE), Orçamento, Societário, and BI production screens (Plans 10-13).</what-built>
  <read_first>
    - src/app/(dashboard)/clinica/relatorios/page.tsx (DRE screen — Plan 10)
    - src/app/(dashboard)/clinica/orcamento/page.tsx (Orçamento screen — Plan 11)
    - src/app/(dashboard)/clinica/societario/page.tsx (Societário screen — Plan 12)
    - src/app/(dashboard)/clinica/bi/page.tsx (BI screen — Plan 13; route may be /bi — confirm actual path via grep before testing)
    - .planning/phases/19-relat-rios-or-amento-bi/19-UI-SPEC.md (expected copy, tokens, per-screen empty/error states to check against)
  </read_first>
  <action>Manually exercise the 4 production screens as admin and as sócio per the verification steps below. Do NOT delete any prototype files until the human confirms all screens work — this checkpoint gates Task 2.</action>
  <how-to-verify>
    As admin: visit /clinica/relatorios (DRE renders, period/unit selectors work, Exportar PDF downloads), /clinica/orcamento (12-month grid, save + copiar do ano anterior, locked past months), /clinica/societario (per-sócio R$, Nova vigência blocks non-100%), /bi (4 tabs + fixed Alertas & Previsões, Exportar PDF).
    As sócio: /clinica/societario shows only own cota; /clinica/orcamento is writable; /bi and /clinica/relatorios are read-only.
    Confirm no console errors on any screen.
  </how-to-verify>
  <acceptance_criteria>
    Confirm each concrete, checkable condition (fail any → do NOT approve, fix the owning plan first):

    Relatórios / DRE (Plan 10):
    - GET `/api/relatorios/dre-pdf?from=<YYYY-MM-DD>&to=<YYYY-MM-DD>` returns HTTP 200 with `Content-Type: application/pdf` when called as admin; returns 401/403 when called as dentista.
    - `/clinica/relatorios` renders the KPI row + DRE lines without a client-side error; changing período/unidade via the selectors updates the numbers (URL query changes, no full crash).
    - Clicking a DRE line expands cost centers and, on drill-down, lists the underlying transactions (D-05/D-06).
    - YoY (D-11): with ≥12 months of history a YoY delta is shown; with <12 months the screen shows "comparação indisponível" and does NOT crash.

    Orçamento (Plan 11):
    - Saving the 12-month grid round-trips without a 500 (reload shows the saved values).
    - "Copiar do ano anterior" populates the grid; a past/closed month is read-only (D-18); semaphore colors render.
    - `/api/orcamento/*-pdf` (or the Exportar PDF route) returns HTTP 200 + `application/pdf`.

    Societário (Plan 12):
    - As admin: per-sócio R$ values render for the selected period; "Nova vigência" is blocked (validation error, no insert) when the percentuais do not sum to exactly 100%.
    - As sócio: only the own cota row is visible; other sócios' rows are absent (D-24).
    - Negative-result period shows negative R$ per sócio, not hidden/zeroed (D-27).

    BI (Plan 13):
    - The BI screen loads with the 4 dimension tabs; "Alertas & Previsões" stays fixed at the top across all tab switches (D-38).
    - Exportar PDF route returns HTTP 200 + `application/pdf`.

    Cross-cutting:
    - No console errors on any of the 4 screens (as admin and as sócio).
    - Dentista is denied access to /clinica/relatorios and /bi (D-09/D-39).
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with prototype removal, or describe issues to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remove prototype pages + clean index</name>
  <files>src/app/(dashboard)/clinica/prototipos/relatorios/page.tsx, src/app/(dashboard)/clinica/prototipos/dashboard-franquias/page.tsx, src/app/(dashboard)/clinica/prototipos/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/prototipos/page.tsx (index links to remove)
  </read_first>
  <action>
    1. grep to confirm nothing in production imports the two prototype PAGE modules (only the index links them). Confirm src/components/prototipos/charts.tsx and src/lib/prototipos/mock-data.ts are still imported by convenios/nfse (if so, KEEP them).
    2. Delete the directories src/app/(dashboard)/clinica/prototipos/relatorios/ and src/app/(dashboard)/clinica/prototipos/dashboard-franquias/ (use `rm -rf` on those two dirs only).
    3. Edit src/app/(dashboard)/clinica/prototipos/page.tsx: remove the two cards/links pointing to `relatorios` and `dashboard-franquias`. Leave convenios/nfse links intact.
    4. Run build/typecheck — no dangling imports.
  </action>
  <verify>
    <automated>test ! -d "src/app/(dashboard)/clinica/prototipos/relatorios" && test ! -d "src/app/(dashboard)/clinica/prototipos/dashboard-franquias" && ! grep -rq "prototipos/dashboard-franquias\|prototipos/relatorios" "src/app/(dashboard)/clinica/prototipos/page.tsx" && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Directory `src/app/(dashboard)/clinica/prototipos/relatorios` no longer exists
    - Directory `src/app/(dashboard)/clinica/prototipos/dashboard-franquias` no longer exists
    - prototipos/page.tsx no longer references `relatorios` or `dashboard-franquias`
    - `src/components/prototipos/charts.tsx` still exists (KEEP — convenios/nfse depend on it)
    - `npx tsc --noEmit` passes (no dangling imports)
  </acceptance_criteria>
  <done>Superseded prototype pages removed; index cleaned; build green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes; `npm test` green
- App builds with no broken links to removed prototypes
</verification>

<success_criteria>
D-42 satisfied: mock DRE/franquias pages gone after the real screens are verified; convenios/nfse prototypes untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-14-SUMMARY.md`.
</output>
