---
phase: 19-relat-rios-or-amento-bi
plan: 06
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - src/lib/financeiro/partner-share-schema.ts
  - src/actions/partner-shares.ts
  - src/actions/__tests__/partner-shares.test.ts
autonomous: true
requirements: [REP-03]
must_haves:
  truths:
    - "A new vigência is only saved when active percentuais sum to exactly 100% (D-22)"
    - "Saving a new vigência closes prior open vigências (vigencia_fim set), preserving history (D-20)"
    - "getPartnerDistribution returns R$ per sócio = percentual × consolidated resultado for the period (D-21/D-23)"
    - "Negative consolidated resultado distributes as negative per-sócio values (D-27)"
    - "A sócio is a users row with role='socio' (D-25); partner_shares write is admin/superadmin only (A1)"
  artifacts:
    - path: "src/actions/partner-shares.ts"
      provides: "Partner-share vigência CRUD + distribution Server Actions"
      exports: ["listPartnerShares", "createPartnerShareVigencia", "closePartnerShareVigencia", "getPartnerDistribution", "listSocios"]
    - path: "src/lib/financeiro/partner-share-schema.ts"
      provides: "Zod schema for a partner-share vigência set"
      exports: ["partnerShareSetSchema"]
  key_links:
    - from: "src/app/(dashboard)/clinica/societario/page.tsx (Plan 12)"
      to: "src/actions/partner-shares.ts"
      via: "import getPartnerDistribution"
      pattern: "getPartnerDistribution"
    - from: "src/actions/partner-shares.ts"
      to: "src/lib/financeiro/partner-share-math.ts"
      via: "import validateSharesSumTo100 / distributeResult"
      pattern: "validateSharesSumTo100"
---

<objective>
Build the Societário Server Actions (REP-03): manage partner-share vigências (with the blocking 100%-sum validation and history preservation), list sócios, and compute the R$ distribution per sócio over the consolidated period result. Uses the consolidated DRE resultado (all units) as the base (D-21).

Purpose: Data layer for the Societário screen (Plan 12). Distribution is purely informative (D-26) — no financial_transactions written.
Output: partner-share-schema.ts + partner-shares.ts + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md
@.planning/phases/19-relat-rios-or-amento-bi/19-RESEARCH.md

<interfaces>
Pure math (Plan 01): src/lib/financeiro/partner-share-math.ts
```typescript
export function resolveActiveShares(rows: ShareRow[], data: string): ShareRow[]
export function validateSharesSumTo100(rows: ShareRow[], data: string): { valid: boolean; sum: number }
export function distributeResult(rows: ShareRow[], data: string, resultado: number): Array<{ user_id; percentual; valor }>
```
Consolidated resultado (D-21): compute INLINE in this file — query financial_transactions for the period with NO cost_center filter (all units, includes NULL rows), sum(receita) - sum(despesa). Do NOT import getDre (keeps this plan decoupled from Plan 04 / wave 2). financial_transactions tenant column is `tenant_id` (Pitfall 4).
getActor: src/actions/transactions.ts.
partner_shares columns (Plan 03): clinic_id, user_id, percentual NUMERIC(5,4), vigencia_inicio DATE, vigencia_fim DATE (NULL=vigente).
RLS (Plan 03): admin/superadmin read all + write; socio reads own row only, NO write. Enforce write gate in action too (defense-in-depth, A1).
role='socio' users: query public.users WHERE role='socio' (D-25).
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Information Disclosure | sócio viewing another sócio's share via API tampering | mitigate | RLS self-row (user_id=auth.uid()) at DB (Plan 03) + action returns only caller-visible rows |
| T-19-09 | Integrity | saving a vigência set that doesn't sum to 100% | mitigate | validateSharesSumTo100 blocks the write (D-22) before any INSERT |
| T-19-10 | Elevation of Privilege | socio creating/closing vigências | mitigate | action-layer gate `['admin','superadmin']` + RLS write policy excludes socio |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: partner-share-schema.ts + vigência CRUD with 100% validation</name>
  <files>src/lib/financeiro/partner-share-schema.ts, src/actions/partner-shares.ts, src/actions/__tests__/partner-shares.test.ts</files>
  <read_first>
    - src/lib/financeiro/partner-share-math.ts (Plan 01 pure functions)
    - src/actions/transactions.ts (getActor)
    - supabase/migrations/20260621000400_tax_tables.sql (vigência close semantics)
  </read_first>
  <behavior>
    - The vigência-save validation is pure and testable: given a proposed set of {user_id, percentual} for a start date, validateSharesSumTo100 must reject 0.6+0.3 (sum 0.9) and accept 0.6+0.4. Test the action's validation branch by extracting a pure `assertSharesValid(shares, data)` helper and unit-testing it (valid/invalid/rounding cases mirroring Plan 01).
    - Close-prior semantics are pure/decidable: given existing open vigências and a new vigencia_inicio D, the prior open rows get vigencia_fim = D minus one day. Unit-test a pure `priorCloseDate(newInicio: string): string` returns the day before.
  </behavior>
  <action>
    Create src/lib/financeiro/partner-share-schema.ts exporting `partnerShareSetSchema` (Zod v3, no .default()): { vigenciaInicio: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), shares: z.array(z.object({ userId: z.string().uuid(), percentual: z.number().gt(0).max(1) })).min(1) }.

    Create src/actions/partner-shares.ts ('use server'). getActor. Constant `const SHARE_WRITE_ROLES = ['admin','superadmin'] as const`. Pure helpers `priorCloseDate(newInicio)` and `assertSharesValid(shares, data)` (wraps validateSharesSumTo100).

    Export:
    - `listSocios()` → users WHERE role='socio' in the tenant (id, name/email). (D-25)
    - `listPartnerShares()` → all partner_shares rows for the tenant, ordered by vigencia_inicio desc (RLS returns only what the caller may see — admin all, socio own).
    - `createPartnerShareVigencia(input)` → validate partnerShareSetSchema; role gate SHARE_WRITE_ROLES else error; call assertSharesValid(shares, vigenciaInicio) — if invalid return { success:false, error:'A soma dos percentuais deve ser exatamente 100%. Ajuste os valores antes de salvar.' }; in sequence: set vigencia_fim = priorCloseDate(vigenciaInicio) on all currently-open rows (vigencia_fim IS NULL) for this clinic, then INSERT the new rows (vigencia_inicio=vigenciaInicio, vigencia_fim=NULL). clinic_id from actor.tenant_id.
    - `closePartnerShareVigencia(params:{ vigenciaInicio:string })` (destructive "Encerrar vigência") → role gate; set vigencia_fim = today for the open rows of that vigência.

    Write src/actions/__tests__/partner-shares.test.ts covering assertSharesValid (valid/invalid/rounding) and priorCloseDate. RED then GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/actions/__tests__/partner-shares.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/actions/__tests__/partner-shares.test.ts` exits 0
    - partner-shares.ts contains `'use server'`
    - partner-shares.ts contains `const SHARE_WRITE_ROLES` = admin/superadmin (does NOT contain `'socio'` in that constant)
    - partner-shares.ts contains `validateSharesSumTo100`
    - partner-shares.ts contains `export async function createPartnerShareVigencia`
    - partner-share-schema.ts does NOT contain `.default(`
  </acceptance_criteria>
  <done>Vigência CRUD enforces 100% sum and history preservation; sócio listing works; write gated to admin/superadmin.</done>
</task>

<task type="auto">
  <name>Task 2: getPartnerDistribution over consolidated result</name>
  <files>src/actions/partner-shares.ts</files>
  <read_first>
    - src/actions/transactions.ts (financial_transactions query + getActor; tenant_id column)
    - src/lib/financeiro/partner-share-math.ts (distributeResult)
  </read_first>
  <action>
    Add `getPartnerDistribution(params:{ from:string; to:string })` to partner-shares.ts (D-21/D-23/D-27):
    1. getActor. Read partner_shares (RLS scopes: admin sees all, socio sees own).
    2. Compute the consolidated resultado INLINE: query financial_transactions select `type, amount` gte from lte to (NO cost_center filter — all units incl. NULL rows, D-21); resultado = sum(amount where type='receita') - sum(amount where type='despesa'). (Do NOT import getDre — keeps 06 in wave 2.)
    3. Call distributeResult(shares, from, resultado) — use the period-start date as the vigência resolution date. Return per-sócio { user_id, percentual, valor } (valor may be negative — D-27, do not zero/hide).
    4. Attach sócio display names via listSocios join.
    Note: admin/superadmin get the full list; a socio session naturally receives only its own row because RLS filtered partner_shares. Do NOT add extra client-trusted filtering.
  </action>
  <verify>
    <automated>grep -q "export async function getPartnerDistribution" src/actions/partner-shares.ts && grep -q "distributeResult" src/actions/partner-shares.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - partner-shares.ts contains `export async function getPartnerDistribution`
    - partner-shares.ts computes resultado from financial_transactions with no cost_center filter (consolidated)
    - partner-shares.ts calls `distributeResult`
    - No `.from('financial_transactions').insert` anywhere in the file (D-26 — purely informative)
  </acceptance_criteria>
  <done>Distribution returns R$ per sócio from the consolidated result, negatives preserved, no financial write.</done>
</task>

</tasks>

<verification>
- `npm test` green
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
REP-03 data layer complete: vigência history with 100% gate, consolidated distribution with negatives, sócio self-row privacy, no auto financial posting.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-06-SUMMARY.md`.
</output>
