---
phase: 19-relat-rios-or-amento-bi
plan: 13
type: execute
wave: 3
depends_on: [07, 08, 09]
files_modified:
  - src/app/(dashboard)/bi/page.tsx
  - src/components/relatorios/BiDashboard.tsx
  - src/components/relatorios/BiAlertsSection.tsx
  - src/components/relatorios/BiPdf.tsx
  - src/app/api/bi/pdf/route.ts
autonomous: true
requirements: [BI-01, BI-02]
must_haves:
  truths:
    - "BI panel shows tabs Operacional | Profissionais | CRC | Estoque/TISS, each with a KPI row meta×realizado (D-38/D-29)"
    - "'Alertas & Previsões' section is fixed above the tabs, visible across all tabs (D-38)"
    - "Alert cards show severity color + LLM narrative; concrete-action alerts link 'Revisar sugestão' → ApprovalInbox (D-35)"
    - "Insufficient-data notice appears when <3 months history; KPIs still show (D-32)"
    - "Exportar PDF per tab available (D-40)"
  artifacts:
    - path: "src/app/(dashboard)/bi/page.tsx"
      provides: "BI dashboard (BI-01/BI-02) at top-level /bi"
    - path: "src/components/relatorios/BiAlertsSection.tsx"
      provides: "Fixed Alertas & Previsões section"
  key_links:
    - from: "src/app/(dashboard)/bi/page.tsx"
      to: "src/actions/bi-kpis.ts / src/actions/bi-alerts.ts"
      via: "import getBiKpis / listBiAlerts"
      pattern: "getBiKpis"
---

<objective>
Build the BI dashboard at /bi (BI-01/BI-02): fixed "Alertas & Previsões" section on top + 4 KPI dimension tabs with meta×realizado, plus PDF export. Reads pre-computed data from Plan 07 (KPIs, alerts) — no client-side forecasting.

Purpose: Surface KPIs and the nightly forecast alerts per the UI-SPEC (tabs + fixed alerts + ApprovalInbox link).
Output: bi page + BiDashboard + BiAlertsSection + BiPdf + PDF route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-UI-SPEC.md
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md

<interfaces>
Actions: src/actions/bi-kpis.ts → getBiKpis (dimensions operacional/profissionais/crc/estoque_tiss); src/actions/bi-alerts.ts → listBiAlerts (severity/trigger_type/narrative/approval_request_id).
Charts (Plan 09): src/components/relatorios/charts.tsx → KpiCard, ChartCard, LineChart, BarChart, DonutChart, OccupancyBar, DeltaBadge.
Tabs: shadcn Tabs (installed). "Alertas & Previsões" must render OUTSIDE/ABOVE <Tabs> (a fixed section, NOT a 5th tab) so it stays visible on tab switch (D-38).
ApprovalInbox link target: the conformidade approval inbox route (grep for the route rendering ApprovalInbox, e.g. /conformidade). "Revisar sugestão" link only when alert.approval_request_id present (D-35).
UI-SPEC BI copy: two empty states — insufficient forecast ("Dados insuficientes para previsão" / "Previsões aparecem após pelo menos 3 meses de histórico. Os KPIs atuais continuam disponíveis normalmente.") and no alerts ("Nenhum alerta no momento" / "Os indicadores estão dentro do esperado."). Error: "Não foi possível carregar os indicadores. Tente novamente em instantes."
Alert severity colors: verde `text-green-700 dark:text-green-400`, amarelo `text-amber-600 dark:text-amber-400`, vermelho destructive token, info neutral `Info` icon.
KPI row per tab: `grid grid-cols-2 lg:grid-cols-5 gap-4`. Never a bare number — every KPI ships a label.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: bi page + BiDashboard tabs + BiAlertsSection</name>
  <files>src/app/(dashboard)/bi/page.tsx, src/components/relatorios/BiDashboard.tsx, src/components/relatorios/BiAlertsSection.tsx</files>
  <read_first>
    - src/actions/bi-kpis.ts + src/actions/bi-alerts.ts (Plan 07 signatures)
    - src/components/relatorios/charts.tsx (Plan 09 primitives)
    - src/components/conformidade/ApprovalInbox.tsx (link target for "Revisar sugestão")
    - src/app/(dashboard)/clinica/prototipos/dashboard-franquias/page.tsx (reference UX only)
  </read_first>
  <action>
    Create RSC page src/app/(dashboard)/bi/page.tsx: PageHeader (title 'BI', actions = período/unit selector + "Exportar PDF"). Call getBiKpis + listBiAlerts server-side, pass to components.
    Create src/components/relatorios/BiAlertsSection.tsx (fixed, above tabs): render alert cards (ChartCard-style wrapper) with severity-colored left border/leading icon + narrative text (Body 14px/400) + a "Revisar sugestão" link (→ ApprovalInbox route) ONLY when approval_request_id is set (D-35). Insufficient-data notice uses neutral `Info` icon + copy "Dados insuficientes para previsão" / "...após pelo menos 3 meses...". No-alerts empty: "Nenhum alerta no momento" / "Os indicadores estão dentro do esperado."
    Create src/components/relatorios/BiDashboard.tsx: `<Tabs>` with 4 triggers "Operacional | Profissionais | CRC | Estoque/TISS". Each TabsContent renders a KPI row (`grid grid-cols-2 lg:grid-cols-5 gap-4`) of KpiCard (value = Display anchor, with meta×realizado via DeltaBadge/atingimento) + relevant charts (LineChart/BarChart/DonutChart/OccupancyBar) from getBiKpis. Every KPI shows its label. BiAlertsSection is rendered by the page ABOVE <BiDashboard>/<Tabs> (fixed, D-38).
    Error state copy on load failure. Typography 2 weights; no font-bold/medium.
  </action>
  <verify>
    <automated>grep -q "getBiKpis" "src/app/(dashboard)/bi/page.tsx" && grep -q "Operacional" src/components/relatorios/BiDashboard.tsx && grep -q "Dados insuficientes para previsão" src/components/relatorios/BiAlertsSection.tsx && grep -q "Revisar sugestão" src/components/relatorios/BiAlertsSection.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports from `@/actions/bi-kpis` and `@/actions/bi-alerts` (NOT prototipos)
    - BiDashboard.tsx uses shadcn `Tabs` with the 4 dimension labels
    - BiAlertsSection renders OUTSIDE the Tabs tree (page.tsx places it before <BiDashboard/>) — grep page.tsx shows BiAlertsSection before BiDashboard
    - BiAlertsSection contains `Dados insuficientes para previsão`, `Nenhum alerta no momento`, and a conditional `Revisar sugestão` link on approval_request_id
    - charts imported from `@/components/relatorios/charts` (not prototipos)
    - No `font-bold`/`font-medium`; `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>BI panel: fixed alerts + 4 KPI tabs, correct copy, ApprovalInbox link only for concrete actions.</done>
</task>

<task type="auto">
  <name>Task 2: BiPdf + bi/pdf route</name>
  <files>src/components/relatorios/BiPdf.tsx, src/app/api/bi/pdf/route.ts</files>
  <read_first>
    - src/app/api/estoque/anvisa-pdf/route.ts (route template)
    - src/actions/bi-kpis.ts (getBiKpis for PDF data)
  </read_first>
  <action>
    Create src/components/relatorios/BiPdf.tsx (@react-pdf/renderer, Flexbox only, Roboto): KPI summary per dimension (label + atual + meta).
    Create src/app/api/bi/pdf/route.ts: runtime nodejs, getUser → tenant_id + role gate ['admin','socio','superadmin'] (D-39), read período/unit/dimension query, call getBiKpis, renderToBuffer(BiPdf), Cache-Control no-store.
  </action>
  <verify>
    <automated>grep -q "runtime = 'nodejs'" src/app/api/bi/pdf/route.ts && grep -q "no-store" src/app/api/bi/pdf/route.ts && grep -q "renderToBuffer" src/app/api/bi/pdf/route.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - bi/pdf/route.ts contains `runtime = 'nodejs'` + `no-store`
    - route enforces role in ['admin','socio','superadmin']
    - BiPdf.tsx uses `@react-pdf/renderer`, no `display: 'grid'`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>BI PDF export works, role-gated, Flexbox-only.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes; `npm test` green
- Manual (19-VALIDATION.md): tabs switch, alerts section stays visible
</verification>

<success_criteria>
BI-01/BI-02 screen shipped matching the UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-13-SUMMARY.md`.
</output>
