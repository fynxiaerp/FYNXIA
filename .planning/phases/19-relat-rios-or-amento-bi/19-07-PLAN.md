---
phase: 19-relat-rios-or-amento-bi
plan: 07
type: execute
wave: 2
depends_on: [03]
files_modified:
  - src/lib/financeiro/kpi-target-schema.ts
  - src/actions/kpi-targets.ts
  - src/actions/bi-kpis.ts
  - src/actions/bi-alerts.ts
autonomous: true
requirements: [BI-01]
must_haves:
  truths:
    - "KPI targets are saved per kpi_key + unit (D-30), gated to admin/superadmin"
    - "getBiKpis returns each KPI's atual value + meta + attainment across the 4 dimensions (D-29)"
    - "KPIs draw from real sources: nps_responses, campaigns/leads, stock_alerts, tiss_guide_items, payables/receivables overdue (D-29)"
    - "listBiAlerts returns active bi_alerts for the panel's fixed Alertas & Previsões section"
    - "BI data actions are gated to admin/socio/superadmin (D-39)"
  artifacts:
    - path: "src/actions/bi-kpis.ts"
      provides: "Multi-dimension KPI aggregation (Operacional/Profissionais/CRC/Estoque-TISS)"
      exports: ["getBiKpis"]
    - path: "src/actions/kpi-targets.ts"
      provides: "KPI target CRUD"
      exports: ["listKpiTargets", "saveKpiTarget"]
    - path: "src/actions/bi-alerts.ts"
      provides: "Read active bi_alerts for the BI panel"
      exports: ["listBiAlerts"]
  key_links:
    - from: "src/app/(dashboard)/bi/page.tsx (Plan 13)"
      to: "src/actions/bi-kpis.ts"
      via: "import getBiKpis"
      pattern: "getBiKpis"
    - from: "src/app/(dashboard)/bi/page.tsx (Plan 13)"
      to: "src/actions/bi-alerts.ts"
      via: "import listBiAlerts"
      pattern: "listBiAlerts"
---

<objective>
Build the BI KPI data layer (BI-01): KPI-target CRUD, multi-dimension KPI aggregation (Operacional, Profissionais, CRC, Estoque/TISS) with meta×realizado, and reading of the active bi_alerts the nightly cron produces. All gated to admin/socio/superadmin (D-39).

Purpose: Feeds the BI dashboard (Plan 13). This plan only READS existing Phase 15/17/18 data + writes kpi_targets; the forecast/alert WRITES are Plan 08.
Output: kpi-target-schema.ts + kpi-targets.ts + bi-kpis.ts + bi-alerts.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md
@.planning/phases/19-relat-rios-or-amento-bi/19-RESEARCH.md

<interfaces>
getActor: src/actions/transactions.ts (replicate; gate = ['admin','socio','superadmin']).
kpi_targets / bi_alerts columns: Plan 03 (kpi_key TEXT, meta_valor NUMERIC; bi_alerts.severity/trigger_type/narrative/status).

Data sources (verified in RESEARCH — read the schema before querying):
- CRC: nps_responses (score 0-10 → promotor/neutro/detrator), campaigns (CPL/CAC via payables.campaign_id), leads (conversão by stage). See src/actions in Phase 18 (roi actions, nps panel action) for existing aggregation to reuse — grep `export async function listConversionByOrigin` and NPS/ROI actions.
- Estoque/Compliance: stock_alerts (count tipo='minimo' active), tiss_guide_items.valor_glosado ÷ total (glosa rate), payables/receivables overdue (due_date < today AND status pendente = atraso).
- Operacional: appointments (ocupação = booked vs available slots; consultas/mês = count; ticket médio = receita ÷ consultas). Reuse existing appointment/financial queries.
- Profissionais: faturamento/procedimentos per dentist (join appointments/service_orders to professionals).

KPI kpi_key catalogue (must match bi_alerts/kpi_targets kpi_key values): 'ocupacao','ticket_medio','consultas_mes','nps','cpl','cac','conversao_leads','glosa_taxa','atraso_pagamento','faturamento_profissional'.
Zod convention: no .default() (D-133).
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | non-privileged role reading BI aggregates | mitigate | action-layer gate `['admin','socio','superadmin']` on getBiKpis/listBiAlerts (D-39) + route gate (Plan 09) |
| T-19-11 | Elevation of Privilege | socio/others writing kpi_targets | mitigate | saveKpiTarget gate `['admin','superadmin']` + RLS (Plan 03) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: kpi-target-schema.ts + kpi-targets CRUD + bi-alerts read</name>
  <files>src/lib/financeiro/kpi-target-schema.ts, src/actions/kpi-targets.ts, src/actions/bi-alerts.ts</files>
  <read_first>
    - src/actions/transactions.ts (getActor)
    - src/lib/financeiro/transaction-schema.ts (Zod style)
    - src/components/conformidade/ApprovalInbox.tsx (how approval_request_id links — for bi_alerts action-link shape)
  </read_first>
  <action>
    Create src/lib/financeiro/kpi-target-schema.ts exporting `kpiTargetSchema` (Zod v3, no .default()): { kpiKey: z.string().min(1), unitId: z.string().uuid().nullable().optional(), metaValor: z.number() }.

    Create src/actions/kpi-targets.ts ('use server'). getActor. `const KPI_READ_ROLES = ['admin','socio','superadmin']`, `const KPI_WRITE_ROLES = ['admin','superadmin']`.
    - `listKpiTargets(params?:{ unitId?:string })` → kpi_targets rows (read gate KPI_READ_ROLES).
    - `saveKpiTarget(input)` → validate kpiTargetSchema; gate KPI_WRITE_ROLES; upsert into kpi_targets (clinic_id from actor.tenant_id, onConflict appropriate unique index).

    Create src/actions/bi-alerts.ts ('use server'). getActor gate KPI_READ_ROLES.
    - `listBiAlerts()` → bi_alerts WHERE status='active' for the tenant, newest first, returning { id, kpi_key, severity, trigger_type, narrative, projected_value, actual_value, approval_request_id, created_at }. approval_request_id present ⇒ the UI renders the "Revisar sugestão" link (D-35).
  </action>
  <verify>
    <automated>grep -q "export const kpiTargetSchema" src/lib/financeiro/kpi-target-schema.ts && grep -q "export async function saveKpiTarget" src/actions/kpi-targets.ts && grep -q "export async function listBiAlerts" src/actions/bi-alerts.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - kpi-target-schema.ts contains `export const kpiTargetSchema` and no `.default(`
    - kpi-targets.ts contains `const KPI_WRITE_ROLES` = admin/superadmin (no `'socio'`)
    - bi-alerts.ts contains `export async function listBiAlerts` and filters `status` = `'active'`
    - kpi-targets.ts + bi-alerts.ts both contain `'use server'`
  </acceptance_criteria>
  <done>KPI targets CRUD gated correctly; active alerts readable for the panel.</done>
</task>

<task type="auto">
  <name>Task 2: getBiKpis multi-dimension aggregation</name>
  <files>src/actions/bi-kpis.ts</files>
  <read_first>
    - src/actions/transactions.ts (getActor + financial aggregation pattern)
    - Phase 18 ROI/NPS actions (grep `listConversionByOrigin` and NPS aggregation) — reuse, do not recompute from scratch
    - supabase/migrations/20260620000300_faturamento_tiss_tables.sql (tiss_guide_items.valor_glosado)
    - supabase/migrations/20260621000100_payables_tables.sql (payables/installments due_date for atraso)
  </read_first>
  <action>
    Create src/actions/bi-kpis.ts ('use server'). getActor gate ['admin','socio','superadmin'].
    Export `getBiKpis(params:{ from:string; to:string; unitId?:string })` returning KPIs grouped by dimension:
    - dimension 'operacional': { ocupacao, ticket_medio, consultas_mes } computed from appointments + financial_transactions for the period/unit.
    - dimension 'profissionais': array of { professionalId, nome, faturamento, procedimentos } from appointments/service_orders joined to professionals.
    - dimension 'crc': { nps, cpl, cac, conversao_leads } — reuse Phase 18 aggregation actions where they exist; nps from nps_responses (promotores-detratores)/total*100.
    - dimension 'estoque_tiss': { alertas_minimo (count stock_alerts tipo='minimo' active), glosa_taxa (Σ valor_glosado / Σ valor guias), atraso_pagamento (count payables/installments overdue) }.
    For each numeric KPI attach its meta from kpi_targets (match kpi_key) and `atingimento` = atual/meta when meta present. Every KPI value ships with a label (Copywriting Contract — never a bare number).
    Keep each dimension's computation in its own small internal helper for readability. If a source table is empty, return 0/null gracefully (no throw).
  </action>
  <verify>
    <automated>grep -q "export async function getBiKpis" src/actions/bi-kpis.ts && grep -q "operacional" src/actions/bi-kpis.ts && grep -q "estoque_tiss" src/actions/bi-kpis.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - bi-kpis.ts contains `export async function getBiKpis`
    - bi-kpis.ts references all 4 dimension keys: `operacional`, `profissionais`, `crc`, `estoque_tiss`
    - bi-kpis.ts references `stock_alerts` and `nps_responses` and `valor_glosado`
    - bi-kpis.ts contains `'use server'`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>All 4 KPI dimensions aggregate from real sources with meta×realizado, gated to BI roles.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm test` green
</verification>

<success_criteria>
BI-01 data layer complete: KPI targets + 4-dimension KPI aggregation + active-alert reads, all role-gated.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-07-SUMMARY.md`.
</output>
