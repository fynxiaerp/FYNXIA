---
phase: 19-relat-rios-or-amento-bi
plan: 05
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - src/lib/financeiro/budget-schema.ts
  - src/actions/budget-targets.ts
  - src/actions/__tests__/budget-targets.test.ts
autonomous: true
requirements: [REP-02]
must_haves:
  truths:
    - "Budget targets are saved per account + unit + ano with 12 editable monthly values (D-12/D-13)"
    - "getBudgetVsRealizado returns meta vs realizado + semaphore per account/month (D-15)"
    - "copyBudgetFromPreviousYear clones prior-year values into a new year (D-17)"
    - "Editing a past month (before current month, America/Sao_Paulo) is rejected (D-18)"
    - "Budget write is gated to admin/socio/superadmin (D-14)"
  artifacts:
    - path: "src/actions/budget-targets.ts"
      provides: "Budget CRUD + orçado×realizado + copy-from-year Server Actions"
      exports: ["listBudgetTargets", "saveBudgetTargets", "copyBudgetFromPreviousYear", "getBudgetVsRealizado"]
    - path: "src/lib/financeiro/budget-schema.ts"
      provides: "Zod schema for budget target rows"
      exports: ["budgetTargetSchema"]
  key_links:
    - from: "src/app/(dashboard)/clinica/orcamento/page.tsx (Plan 11)"
      to: "src/actions/budget-targets.ts"
      via: "import saveBudgetTargets / getBudgetVsRealizado"
      pattern: "getBudgetVsRealizado"
    - from: "src/actions/budget-targets.ts"
      to: "src/lib/financeiro/dre-math.ts"
      via: "import budgetDeviationSemaphore"
      pattern: "budgetDeviationSemaphore"
---

<objective>
Build the Orçamento Server Actions (REP-02): save/list 12-month budget targets per account+unit+year, copy from previous year, and orçado×realizado with the deviation semaphore. Enforce the D-18 past-month lock and the D-14 admin/socio/superadmin write gate.

Purpose: Data layer for the Orçamento screen (Plan 11). Realizado is computed read-time from financial_transactions (same source as DRE, D-10).
Output: budget-schema.ts + budget-targets.ts + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md
@.planning/phases/19-relat-rios-or-amento-bi/19-RESEARCH.md

<interfaces>
getActor + unit→cost-center resolution: src/actions/transactions.ts (replicate).
Semaphore (Plan 01): src/lib/financeiro/dre-math.ts → `budgetDeviationSemaphore(realizado, meta): 'verde'|'amarelo'|'vermelho'`.
Zod convention (NO .default() — D-133): src/lib/financeiro/transaction-schema.ts, src/lib/financeiro/cost-center schema.
America/Sao_Paulo month bound helper to mirror: `saoPauloDayBoundsUTC` in src/lib/agents/stock-agent.ts (Pitfall 6 — month-lock uses wall-clock SP, not UTC).
budget_targets columns (Plan 03): clinic_id, unit_id (nullable), account_id, ano, mes (1-12), valor NUMERIC(12,2).
RLS already allows socio write on budget_targets (Plan 03) — but ALSO enforce the role gate in the action (defense-in-depth).
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | editing a locked past-month budget | mitigate | `updateBudgetTarget`/`saveBudgetTargets` reject writes to months strictly before current month (SP wall-clock, D-18) |
| T-19-08 | Elevation of Privilege | non-privileged role writing budgets | mitigate | action-layer gate `['admin','socio','superadmin']` + RLS write policy (Plan 03) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: budget-schema.ts + list/save/copy actions</name>
  <files>src/lib/financeiro/budget-schema.ts, src/actions/budget-targets.ts</files>
  <read_first>
    - src/actions/transactions.ts (getActor pattern)
    - src/lib/financeiro/transaction-schema.ts (Zod style, no .default())
    - src/lib/agents/stock-agent.ts (saoPauloDayBoundsUTC — SP wall-clock)
  </read_first>
  <action>
    Create src/lib/financeiro/budget-schema.ts exporting `budgetTargetSchema` (Zod v3, no .default()): { accountId: z.string().uuid(), unitId: z.string().uuid().nullable().optional(), ano: z.number().int().min(2020).max(2100), meses: z.array(z.object({ mes: z.number().int().min(1).max(12), valor: z.number().min(0) })).length(12) }. Friendly pt-BR messages.

    Create src/actions/budget-targets.ts ('use server'). Replicate getActor. Constant `const BUDGET_WRITE_ROLES = ['admin','socio','superadmin'] as const`. Add a pure helper `currentMonthSP(): { ano:number; mes:number }` deriving current year/month in America/Sao_Paulo (UTC-3 fixed, mirror stock-agent's offset approach) and `isMonthLocked(ano, mes): boolean` = the (ano,mes) is strictly before currentMonthSP (D-18).

    Export:
    - `listBudgetTargets(params:{ ano:number; unitId?:string })` → returns rows grouped by account_id with the 12 monthly valores. Role gate = BUDGET_WRITE_ROLES (read allowed to same set + already RLS-scoped).
    - `saveBudgetTargets(input)` → validate with budgetTargetSchema; role gate BUDGET_WRITE_ROLES else error; for each month, if isMonthLocked(ano,mes) SKIP that month (do not update locked past months, D-18) — never reject the whole save, just skip locked cells; upsert the non-locked months into budget_targets (clinic_id from actor.tenant_id, onConflict the appropriate unique index). Return count saved + count skipped.
    - `copyBudgetFromPreviousYear(params:{ ano:number; unitId?:string })` (D-17) → read all budget_targets for ano-1 (same unit scope), insert equivalents for ano (only for months not locked). Role gate. Return count copied.
  </action>
  <verify>
    <automated>grep -q "budgetTargetSchema" src/lib/financeiro/budget-schema.ts && grep -q "export async function saveBudgetTargets" src/actions/budget-targets.ts && grep -q "export async function copyBudgetFromPreviousYear" src/actions/budget-targets.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - budget-schema.ts contains `export const budgetTargetSchema` and does NOT contain `.default(`
    - budget-targets.ts contains `'use server'`
    - budget-targets.ts contains `const BUDGET_WRITE_ROLES` with `'socio'`
    - budget-targets.ts contains `isMonthLocked`
    - budget-targets.ts contains `export async function saveBudgetTargets` and `export async function copyBudgetFromPreviousYear`
  </acceptance_criteria>
  <done>Budget CRUD + copy implemented with role gate and month-lock skip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getBudgetVsRealizado + month-lock test</name>
  <files>src/actions/budget-targets.ts, src/actions/__tests__/budget-targets.test.ts</files>
  <read_first>
    - src/lib/financeiro/dre-math.ts (budgetDeviationSemaphore)
    - src/actions/transactions.ts (unit→cost-center resolution for realizado)
  </read_first>
  <behavior>
    - isMonthLocked(ano, mes): a month strictly before the current SP month → true; current month → false; a future month → false. Test with a fixed reference by injecting `now` (add an optional param to the pure helper for testability), e.g. now=2026-07-15 → (2026,6) locked, (2026,7) not locked, (2026,8) not locked.
    - The realizado-vs-meta shaping is pure: given meta=1000 and realizado=1100 → semaphore 'amarelo' (10%); wire budgetDeviationSemaphore. Test the shaping function directly with injected numbers.
  </behavior>
  <action>
    Add `getBudgetVsRealizado(params:{ ano:number; unitId?:string })` to budget-targets.ts (D-15/D-16 data): for the year, read budget_targets (meta per account/month) and compute realizado per account/month from financial_transactions (unit→cost-center resolution, same as DRE), attach `semaphore = budgetDeviationSemaphore(realizado, meta)` per cell. Role gate BUDGET_WRITE_ROLES. Return a structure the Orçamento grid can render (account rows × 12 months, each with meta/realizado/semaphore/locked).

    Extract the month-lock decision and the per-cell semaphore shaping as pure, injectable-`now` functions so they are unit-testable without Supabase. Write src/actions/__tests__/budget-targets.test.ts covering the <behavior> cases. RED then GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/actions/__tests__/budget-targets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/actions/__tests__/budget-targets.test.ts` exits 0
    - budget-targets.ts contains `export async function getBudgetVsRealizado`
    - budget-targets.ts contains `budgetDeviationSemaphore`
    - Test asserts a past month is locked and current/future are not
  </acceptance_criteria>
  <done>Orçado×realizado with semaphore returns correctly; month-lock verified by test.</done>
</task>

</tasks>

<verification>
- `npm test` green
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
REP-02 data layer complete: 12-month budgets, copy-from-year, realizado comparison with semaphore, D-18 lock, D-14 gate.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-05-SUMMARY.md`.
</output>
