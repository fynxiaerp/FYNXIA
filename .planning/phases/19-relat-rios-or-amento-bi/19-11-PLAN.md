---
phase: 19-relat-rios-or-amento-bi
plan: 11
type: execute
wave: 3
depends_on: [05, 09]
files_modified:
  - src/app/(dashboard)/clinica/orcamento/page.tsx
  - src/components/relatorios/BudgetGrid.tsx
  - src/components/relatorios/BudgetPdf.tsx
  - src/app/api/orcamento/pdf/route.ts
autonomous: true
requirements: [REP-02]
must_haves:
  truths:
    - "Screen shows metas vs realizado per account across 12 months with the deviation semaphore (D-15)"
    - "12 monthly meta values are editable; past-month cells are locked with a lock icon (D-13/D-18)"
    - "'Copiar do ano anterior' pre-fills the year from the previous year (D-17)"
    - "'Salvar metas' persists edits; a cross-link to the DRE exists (D-16)"
    - "Exportar PDF produces an Orçamento PDF (D-19/D-40)"
  artifacts:
    - path: "src/app/(dashboard)/clinica/orcamento/page.tsx"
      provides: "Orçamento screen (REP-02)"
    - path: "src/app/api/orcamento/pdf/route.ts"
      provides: "Orçamento PDF export route"
  key_links:
    - from: "src/app/(dashboard)/clinica/orcamento/page.tsx"
      to: "src/actions/budget-targets.ts"
      via: "import getBudgetVsRealizado / saveBudgetTargets"
      pattern: "getBudgetVsRealizado"
---

<objective>
Build the Orçamento screen (REP-02): 12-month editable grid per account with meta/realizado/semaphore, past-month lock, copy-from-year, save, DRE cross-link, and PDF export.

Purpose: Surface the Plan 05 budget actions per the UI-SPEC (semaphore colors, lock icon).
Output: orcamento page + BudgetGrid + BudgetPdf + PDF route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-UI-SPEC.md
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md

<interfaces>
Actions (Plan 05): src/actions/budget-targets.ts → listBudgetTargets, saveBudgetTargets, copyBudgetFromPreviousYear, getBudgetVsRealizado (returns account rows × 12 months with meta/realizado/semaphore/locked).
Semaphore colors (UI-SPEC): verde `text-green-700 dark:text-green-400`, amarelo `text-amber-600 dark:text-amber-400`, vermelho `--destructive` token. Text/badge only, no big fills.
Locked cell (D-18): disabled Input `bg-muted text-muted-foreground` + `Lock` icon (lucide, size 3.5).
Charts (Plan 09): KpiCard for the summary KPI row.
PDF template: same as Plan 10 (anvisa-pdf route + AnvisaReportPdf).
Layout contract + copy: UI-SPEC Orçamento row (Primary CTA "Salvar metas" + secondary "Copiar do ano anterior"; empty state "Nenhum orçamento cadastrado para {ano}").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: orcamento page + BudgetGrid (12-month editable, semaphore, lock)</name>
  <files>src/app/(dashboard)/clinica/orcamento/page.tsx, src/components/relatorios/BudgetGrid.tsx</files>
  <read_first>
    - src/actions/budget-targets.ts (Plan 05 signatures + locked flag)
    - src/components/estoque/StockAlertBanner.tsx (amber-warning convention reuse)
    - src/components/relatorios/charts.tsx (KpiCard)
  </read_first>
  <action>
    Create RSC page src/app/(dashboard)/clinica/orcamento/page.tsx: PageHeader (title 'Orçamento', actions = ano/unit selector + "Exportar PDF" + a cross-link button "Ver DRE" → /clinica/relatorios, D-16). Call getBudgetVsRealizado({ ano, unitId }) server-side; pass to BudgetGrid.
    Create client component src/components/relatorios/BudgetGrid.tsx:
    - Summary KPI row (Σ meta, Σ realizado, desvio) using KpiCard (Display anchor).
    - Grid: rows = contas, columns = 12 meses; each cell shows an editable meta Input (RHF) + Realizado (read-only) + deviation % colored by semaphore (verde `text-green-700 dark:text-green-400`, amarelo `text-amber-600 dark:text-amber-400`, vermelho destructive token). Locked past-month cells: `disabled` Input `bg-muted text-muted-foreground` + a `Lock` icon (lucide size 3.5) — never silently disabled (D-18).
    - Buttons: "Salvar metas" (primary → saveBudgetTargets), "Copiar do ano anterior" (secondary → copyBudgetFromPreviousYear then refresh).
    - Empty state: heading "Nenhum orçamento cadastrado para {ano}" / body "Crie metas mensais por conta contábil ou copie os valores do ano anterior para começar." Error: "Não foi possível salvar as metas. Verifique os valores e tente novamente."
    Typography: 2 weights only; no font-bold/medium.
  </action>
  <verify>
    <automated>grep -q "getBudgetVsRealizado" "src/app/(dashboard)/clinica/orcamento/page.tsx" && grep -q "Copiar do ano anterior" src/components/relatorios/BudgetGrid.tsx && grep -q "text-amber-600" src/components/relatorios/BudgetGrid.tsx && grep -q "Lock" src/components/relatorios/BudgetGrid.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports from `@/actions/budget-targets`
    - page.tsx contains a cross-link to `/clinica/relatorios` (D-16)
    - BudgetGrid.tsx contains `Salvar metas` and `Copiar do ano anterior`
    - BudgetGrid.tsx contains `text-green-700`, `text-amber-600` (semaphore) and uses the destructive token for vermelho
    - BudgetGrid.tsx contains `Lock` icon for locked cells
    - BudgetGrid.tsx empty heading `Nenhum orçamento cadastrado para`
    - No `font-bold`/`font-medium`; `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Orçamento grid editable with semaphore + lock + copy + save + DRE cross-link.</done>
</task>

<task type="auto">
  <name>Task 2: BudgetPdf + orcamento/pdf route</name>
  <files>src/components/relatorios/BudgetPdf.tsx, src/app/api/orcamento/pdf/route.ts</files>
  <read_first>
    - src/app/api/estoque/anvisa-pdf/route.ts (route template)
    - src/components/estoque/AnvisaReportPdf.tsx (PDF component template)
    - src/actions/budget-targets.ts (getBudgetVsRealizado for PDF data)
  </read_first>
  <action>
    Create src/components/relatorios/BudgetPdf.tsx (@react-pdf/renderer, Flexbox only, Roboto): table of contas × 12 meses meta vs realizado with desvio.
    Create src/app/api/orcamento/pdf/route.ts: runtime nodejs, maxDuration 30, getUser → tenant_id + role gate ['admin','socio','superadmin'], read ano/unitId query, call getBudgetVsRealizado, renderToBuffer(BudgetPdf), return PDF with Cache-Control no-store.
  </action>
  <verify>
    <automated>grep -q "runtime = 'nodejs'" src/app/api/orcamento/pdf/route.ts && grep -q "no-store" src/app/api/orcamento/pdf/route.ts && grep -q "renderToBuffer" src/app/api/orcamento/pdf/route.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - orcamento/pdf/route.ts contains `export const runtime = 'nodejs'` and `no-store`
    - route enforces role in ['admin','socio','superadmin']
    - BudgetPdf.tsx uses `@react-pdf/renderer`, no `display: 'grid'`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Orçamento PDF export works, role-gated, Flexbox-only.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes; `npm test` green
</verification>

<success_criteria>
REP-02 screen shipped matching the UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-11-SUMMARY.md`.
</output>
