---
phase: 19-relat-rios-or-amento-bi
plan: 04
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - src/actions/dre.ts
  - src/actions/__tests__/dre.test.ts
autonomous: true
requirements: [REP-01]
must_haves:
  truths:
    - "getDre returns receita/despesa/resultado/margem for a period, scoped by unit or consolidated (D-01/D-02/D-03)"
    - "Unit filter resolves unitId → cost_center_ids; 'Todas' (consolidated) includes NULL-cost-center rows (Pitfall 3/A4)"
    - "Each DRE line exposes the underlying financial_transactions for drill-down (D-05)"
    - "getDre is gated to admin/socio/superadmin at the action layer (D-09, Pitfall 2)"
    - "YoY comparison returns the same period last year when ≥12 months of history exist, else 'indisponível' (D-11)"
  artifacts:
    - path: "src/actions/dre.ts"
      provides: "DRE read-time aggregation Server Actions"
      exports: ["getDre", "getDreByUnit", "getDreYoY", "getDreDrilldown"]
  key_links:
    - from: "src/app/(dashboard)/clinica/relatorios/page.tsx (Plan 10)"
      to: "src/actions/dre.ts"
      via: "import getDre"
      pattern: "getDre"
    - from: "src/actions/dre.ts"
      to: "src/lib/financeiro/dre-math.ts"
      via: "import aggregateDre"
      pattern: "aggregateDre"
---

<objective>
Build the DRE read-time aggregation Server Actions (REP-01): period + unit selection, consolidated vs per-unit, per-unit ranking, cost-center expansion, drill-down to transactions, YoY comparison, and the admin/socio/superadmin gate. Thin wrappers over the pure `aggregateDre` from Plan 01.

Purpose: All DRE data the UI (Plan 10) renders comes from here — server-side aggregation only (D-10, LGPD: never ship raw financial rows to client beyond drill-down).
Output: src/actions/dre.ts + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md
@.planning/phases/19-relat-rios-or-amento-bi/19-RESEARCH.md

<interfaces>
Unit→cost-center resolution + getActor pattern to replicate EXACTLY (verified source):
src/actions/transactions.ts — listTransactions() lines 159-268 (costCenterIds resolution, month-range derivation, getActor).

Pure aggregation (Plan 01): src/lib/financeiro/dre-math.ts
```typescript
export function aggregateDre(rows: DreTxRow[]): DreResult
export interface DreTxRow { amount: number; type: 'receita'|'despesa'; account_id: string|null; account_name: string|null; account_type: 'grupo'|'receita'|'despesa'|null; cost_center_id: string|null }
export interface DreResult { receitaTotal: number; despesaTotal: number; resultado: number; margem: number; lines: DreLine[] }
```

CRITICAL facts:
- financial_transactions has NO unit_id column and its tenant column is `tenant_id` (not clinic_id) — Pitfall 4.
- financial_transactions RLS grants SELECT to ALL tenant roles (no role filter) — enforce D-09 in the ACTION (Pitfall 2), mirroring COST_ROLES in setLabOrderCost (Phase 13).
- account/type comes from join `chart_of_accounts(name, type, parent_id)` on account_id.
- 'Todas'/consolidated = NO cost_center_id filter at all (includes NULL rows, A4). Specific unit = cost_center_id IN (unit's cost centers), excludes NULL rows.
- units list: src/actions (existing listUnits) — use it for per-unit ranking; find it via grep for `export async function listUnits`.
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure / Elevation of Privilege | getDre exposing financials to non-privileged roles | mitigate | Action-layer role gate `['admin','socio','superadmin']` before any query (RLS alone insufficient — Pitfall 2) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getDre + consolidated/per-unit aggregation + role gate</name>
  <files>src/actions/dre.ts, src/actions/__tests__/dre.test.ts</files>
  <read_first>
    - src/actions/transactions.ts (getActor + listTransactions unit→cost-center resolution — replicate exactly)
    - src/lib/financeiro/dre-math.ts (aggregateDre — Plan 01)
    - src/actions/__tests__ (an existing financeiro action test for mock style, e.g. any *.test.ts there)
  </read_first>
  <behavior>
    - The unit-filter helper (pure or extracted) resolves unitId → cost_center_ids and, for consolidated, applies no filter. Unit-test the pure resolution decision: given opts with no unitId → no cost-center filter; with unitId and 2 cost centers → filter IN those 2 ids; with unitId and 0 cost centers → empty result.
    - Role gate: getActor role not in ['admin','socio','superadmin'] → returns { success:false, error:'Permissão insuficiente' } without running a query.
    - YoY availability (D-11): extract the pure decision "is a YoY comparison available?" into a testable helper — given the earliest transaction_date and the requested period, return available:true only when ≥12 months of history precede the period start, else available:false. Unit-test both branches: earliest ≥12 months before `from` → available:true (and the shifted period is exactly one year earlier: from/to minus 1 year); earliest <12 months before `from` → available:false with no shifted-period query attempted.
    (Query-touching paths are covered by the Manual-Only verification in 19-VALIDATION.md; unit tests cover the pure resolution + gate + YoY-availability logic.)
  </behavior>
  <action>
    Create src/actions/dre.ts ('use server'). Replicate getActor from transactions.ts. Add role constant `const DRE_ROLES = ['admin','socio','superadmin'] as const`.

    Export `getDre(params: { from: string; to: string; unitId?: string })`:
    1. getActor; if role not in DRE_ROLES → { success:false, error:'Permissão insuficiente para visualizar o DRE' }.
    2. Validate from/to are 'YYYY-MM-DD' (regex) — reject malformed.
    3. Resolve unitId → costCenterIds via cost_centers WHERE unit_id (same as listTransactions). If unitId given and 0 cost centers → return empty DreResult.
    4. Query financial_transactions select `id, type, amount, transaction_date, account_id, cost_center_id, chart_of_accounts(name, type, parent_id)` gte from lte to. Apply `.in('cost_center_id', costCenterIds)` ONLY when a specific unit is selected; consolidated applies no cost_center filter (A4).
    5. Map rows → DreTxRow[] (account_name/account_type from the join, handling array|object join shape like listTransactions does for financial_categories), call aggregateDre, return { success:true, dre: DreResult }.

    Export `getDreByUnit(params: { from: string; to: string })` (D-04 ranking): for each unit from listUnits(), compute its DreResult (receita/despesa/resultado/margem) and return an array sorted by resultado desc for the consolidated ranking table. Same role gate.

    Export `getDreYoY(params: { from: string; to: string; unitId?: string })` (D-11): compute the same period one year earlier; only return a comparison when the clinic has ≥12 months of transaction history (derive earliest transaction_date via the pure availability helper), else return { success:true, available:false }. When available, return { success:true, available:true, dre: <prior-year DreResult>, resultadoDelta } so the UI can render a real YoY delta (not just the fallback string). Same role gate.

    Export `getDreDrilldown(params: { from: string; to: string; unitId?: string; accountId: string })` (D-05): return the raw financial_transactions rows for one account_id within the period/unit (this is the only path that ships row-level data to the client, and only on explicit drill-down). Same role gate.

    RED first for the pure resolution + gate + YoY-availability behavior, then implement.
  </action>
  <verify>
    <automated>npx vitest run src/actions/__tests__/dre.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/actions/__tests__/dre.test.ts` exits 0
    - dre.ts contains `'use server'`
    - dre.ts contains `const DRE_ROLES` with `'admin'`, `'socio'`, `'superadmin'`
    - dre.ts contains `export async function getDre`
    - dre.ts contains `export async function getDreByUnit`
    - dre.ts contains `export async function getDreYoY`
    - dre.ts contains `export async function getDreDrilldown`
    - dre.ts contains `aggregateDre`
    - dre.test.ts covers YoY availability both branches: ≥12 months → available:true with the period shifted exactly one year earlier; <12 months → available:false (grep the test for `getDreYoY` / an `available` assertion)
    - dre.ts references `tenant_id` when querying financial_transactions (NOT clinic_id) — grep `financial_transactions` present; grep confirms no `.eq('clinic_id'` against financial_transactions
  </acceptance_criteria>
  <done>DRE actions return correct aggregates, enforce the role gate, resolve unit scoping, and expose drill-down + YoY (real prior-year value + fallback) + per-unit ranking.</done>
</task>

</tasks>

<verification>
- `npm test` full suite green
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
REP-01 data layer complete: server-side DRE with period/unit/consolidated, ranking, drill-down, YoY, role-gated.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-04-SUMMARY.md`.
</output>
