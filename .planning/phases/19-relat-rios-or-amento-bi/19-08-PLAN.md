---
phase: 19-relat-rios-or-amento-bi
plan: 08
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - src/lib/agents/bi-forecast-agent.ts
  - src/app/api/cron/bi-previsoes/route.ts
  - src/actions/approval-actions.ts
  - vercel.json
autonomous: true
requirements: [BI-02]
must_haves:
  truths:
    - "A nightly cron computes per-clinic trends and inserts bi_alerts rows (D-36)"
    - "Trend forecasting requires ≥3 months of data; below that no forecast alert is emitted (D-32)"
    - "Alert triggers: budget deviation, revenue decline vs trend, KPI off-target, payment delay (D-33)"
    - "When a budget deviation is persistent, the agent creates an approval_requests suggestion (never edits budget_targets directly) (D-34)"
    - "withAgentPolicy is called per-clinic (real clinicId), never aggregate (governance guard from Plan 02)"
    - "Approving a bi_forecast suggestion applies the budget_targets UPDATE via a dedicated handler (D-34)"
    - "LLM narrative degrades to static pt-BR text when AI_GATEWAY_API_KEY is absent"
  artifacts:
    - path: "src/lib/agents/bi-forecast-agent.ts"
      provides: "L1 forecast agent — suggests budget adjustments via approval_requests"
      exports: ["runBiForecastForClinic"]
    - path: "src/app/api/cron/bi-previsoes/route.ts"
      provides: "Nightly cron computing trends/alerts per clinic"
    - path: "src/actions/approval-actions.ts"
      provides: "approveBudgetAdjustment handler (extends existing file)"
      exports: ["approveBudgetAdjustment"]
  key_links:
    - from: "src/app/api/cron/bi-previsoes/route.ts"
      to: "src/lib/agents/bi-forecast-agent.ts"
      via: "import runBiForecastForClinic"
      pattern: "runBiForecastForClinic"
    - from: "src/lib/agents/bi-forecast-agent.ts"
      to: "src/lib/bi/forecast-math.ts"
      via: "import computeLinearTrend"
      pattern: "computeLinearTrend"
---

<objective>
Build the BI forecasting agent (BI-02, D-31/D-34), its nightly cron (D-36), the LLM narrative, and the dedicated approve-budget-adjustment handler. The agent SUGGESTS budget-target adjustments through the existing ApprovalInbox — it never mutates budget_targets or partner_shares directly. Turns the Plan 02 governance guard GREEN.

Purpose: The panel (Plan 13) only reads pre-computed bi_alerts; all forecasting/LLM cost happens nightly server-side (D-36).
Output: bi-forecast-agent.ts + cron route + approveBudgetAdjustment + vercel.json cron entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-relat-rios-or-amento-bi/19-CONTEXT.md
@.planning/phases/19-relat-rios-or-amento-bi/19-RESEARCH.md

<interfaces>
Agent template to replicate EXACTLY (structure + per-clinic withAgentPolicy + approval_requests insert): src/lib/agents/stock-agent.ts (runStockReplenishmentAgent).
Cron template: src/app/api/cron/estoque-validade/route.ts (runtime nodejs, isCronAuthorized fail-closed, createAdminClient, per-row try/catch).
LLM narrative template (generateText + AI Gateway + key-absent static fallback): src/lib/agents/collection-agent.ts.
Trend math (Plan 02): src/lib/bi/forecast-math.ts → computeLinearTrend, isDecliningVsTrend.
withAgentPolicy signature: src/lib/ai/policy.ts (line 52) — { clinicId, agentKey, actorId, action, actionSensitivity } + async callback.
approval_requests columns: type='ai_action', agent_key='bi_forecast', payload JSONB, required_role, requested_by=null (system), status='pending'.
Existing approve/reject: src/actions/approval-actions.ts (approveRequest/rejectRequest only flip status — a budget mutation needs a dedicated handler, mirrors approveCampaignAndDispatch, Phase 18).
Governance guard to satisfy (Plan 02, currently RED): src/__tests__/governance/bi-forecast-agent.test.ts.
vercel.json existing crons occupy 05/06/07/08/11/12/13/23 UTC — use 04:00 UTC (free, A3).
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering / Elevation of Privilege | agent silently editing budget_targets | mitigate | agent writes ONLY approval_requests + bi_alerts; budget UPDATE happens only in approveBudgetAdjustment after human approval (D-34) |
| T-19-12 | Spoofing | unauthorized cron trigger | mitigate | isCronAuthorized Bearer CRON_SECRET fail-closed 401 before any work |
| T-19-13 | Information Disclosure | PII sent to LLM | mitigate | narrative prompt carries only KPI numbers + kpi_key + clinic display name (no patient/financial-row PII), ZDR via AI Gateway (collection-agent pattern) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: bi-forecast-agent.ts (trend + alert + suggestion) with LLM narrative</name>
  <files>src/lib/agents/bi-forecast-agent.ts</files>
  <read_first>
    - src/lib/agents/stock-agent.ts (per-clinic withAgentPolicy + approval_requests insert + insertAlert resilience)
    - src/lib/agents/collection-agent.ts (generateText + AI_GATEWAY_API_KEY-absent static fallback)
    - src/lib/bi/forecast-math.ts (computeLinearTrend, isDecliningVsTrend)
    - src/__tests__/governance/bi-forecast-agent.test.ts (the contract this file must satisfy)
  </read_first>
  <action>
    Create src/lib/agents/bi-forecast-agent.ts with `import 'server-only'`. Export `runBiForecastForClinic(params:{ clinicId:string; unitId?:string|null })`:
    1. Build the last-12-months monthly series per tracked KPI (receita/margem, plus budget realizado per account) using createAdminClient. If a series has <3 points, skip forecasting for it (D-32) — do NOT emit a forecast alert (current KPIs are still shown by the panel independently).
    2. For each series with ≥3 points: computeLinearTrend; if isDecliningVsTrend(actual, projectedNext) → build a 'revenue_decline' bi_alerts row (severity 'amarelo'/'vermelho' by magnitude). Also evaluate budget deviation (via budgetDeviationSemaphore from dre-math) → 'budget_deviation' alerts; KPI-off-target → 'kpi_off_target'; overdue payments → 'payment_delay'.
    3. Generate the pt-BR narrative for each alert via generateText (model 'anthropic/claude-sonnet-4.6') — pass ONLY numbers + kpi_key + clinic display name (no PII). If AI_GATEWAY_API_KEY absent → static neutral pt-BR text (mirror collection-agent fallback).
    4. Insert bi_alerts rows (service role). Apply daily dedup per clinic+kpi_key+trigger_type (mirror insertStockAlert's SP-day-bounds pre-check + 23505 catch is NOT needed here — use a simple pre-check on today's rows).
    5. For a PERSISTENT budget deviation (D-34): call withAgentPolicy({ clinicId, agentKey: 'bi_forecast', actorId: null, action: 'agent.bi.suggest_budget_adjustment', actionSensitivity: 'reversible' }, async () => {...}). Inside: INSERT approval_requests { clinic_id: clinicId, type: 'ai_action', agent_key: 'bi_forecast', payload: { budget_target_id, month, current_value, suggested_value, reason }, required_role: 'admin', requested_by: null, status: 'pending' }, then INSERT the corresponding bi_alerts row with approval_request_id set (D-35 concrete action). NEVER `.from('budget_targets').update(...)` here.
    CRITICAL: withAgentPolicy is called with the REAL per-clinic clinicId (never null/aggregate) — satisfies the Plan 02 governance guard.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/governance/bi-forecast-agent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/governance/bi-forecast-agent.test.ts` exits 0 (guard now GREEN)
    - bi-forecast-agent.ts contains `import 'server-only'`
    - bi-forecast-agent.ts contains `agentKey: 'bi_forecast'`
    - bi-forecast-agent.ts contains `computeLinearTrend`
    - bi-forecast-agent.ts contains `approval_requests`
    - bi-forecast-agent.ts does NOT contain `.from('budget_targets').update` nor `.from("budget_targets").update`
    - bi-forecast-agent.ts contains `AI_GATEWAY_API_KEY` (fallback guard)
  </acceptance_criteria>
  <done>Agent computes trends, emits alerts with narrative, and suggests budget adjustments only via approval_requests; governance guard green.</done>
</task>

<task type="auto">
  <name>Task 2: bi-previsoes cron + vercel.json + approveBudgetAdjustment</name>
  <files>src/app/api/cron/bi-previsoes/route.ts, vercel.json, src/actions/approval-actions.ts</files>
  <read_first>
    - src/app/api/cron/estoque-validade/route.ts (cron template)
    - src/actions/approval-actions.ts (approveRequest structure — mirror for approveBudgetAdjustment)
    - vercel.json (existing cron slots)
  </read_first>
  <action>
    Create src/app/api/cron/bi-previsoes/route.ts: `export const runtime = 'nodejs'`, `export const maxDuration = 60`. GET handler: isCronAuthorized(authorization) fail-closed 401; createAdminClient(); fetch all active clinics; for each clinic (per-row try/catch so one failure doesn't abort the batch) call runBiForecastForClinic({ clinicId: clinic.id }). Return Response.json({ ok:true, clinics_processed:n }).

    Add to vercel.json crons array: { "path": "/api/cron/bi-previsoes", "schedule": "0 4 * * *" } (04:00 UTC — free slot, A3).

    Extend src/actions/approval-actions.ts with `export async function approveBudgetAdjustment(requestId: string)` (mirrors approveRequest + approveCampaignAndDispatch precedent): getActor; assertNotReadOnly + alçada (canApprove) checks; read the approval_requests row (type='ai_action', agent_key='bi_forecast'); idempotency guard (WHERE status='pending' AND executed_at IS NULL); on approve, apply the budget_targets UPDATE from payload { budget_target_id, suggested_value } — this is the ONLY place budget_targets is written by the agent flow; set status='approved', executed_at=now(); logBusinessEvent. Reject path reuses the generic rejectRequest.
  </action>
  <verify>
    <automated>grep -q "runBiForecastForClinic" src/app/api/cron/bi-previsoes/route.ts && grep -q "bi-previsoes" vercel.json && grep -q "export async function approveBudgetAdjustment" src/actions/approval-actions.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - bi-previsoes/route.ts contains `export const runtime = 'nodejs'` and `isCronAuthorized`
    - bi-previsoes/route.ts calls `runBiForecastForClinic`
    - vercel.json contains `"/api/cron/bi-previsoes"` with schedule `"0 4 * * *"`
    - approval-actions.ts contains `export async function approveBudgetAdjustment`
    - approveBudgetAdjustment contains `.from('budget_targets')` update (the only place the agent flow mutates it)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Cron registered + wired; approve handler applies the suggested budget change only after human approval.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/governance/bi-forecast-agent.test.ts` green
- `npm test` full suite green
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
BI-02 complete: nightly forecasting → alerts + LLM narrative, governed L1 budget-adjustment suggestions via ApprovalInbox, per-clinic policy enforced, applied only on approval.
</success_criteria>

<output>
After completion, create `.planning/phases/19-relat-rios-or-amento-bi/19-08-SUMMARY.md`.
</output>
