---
phase: 11-profissionais-recursos
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - src/types/database.types.ts
autonomous: false
requirements: [PRO-01, RES-01, RES-03]
tags: [database, migration, blocking, realtime]

must_haves:
  truths:
    - "The six Phase 11 migrations are applied to the live Supabase project jqjwyqlbbuqnrffdnlpp"
    - "src/types/database.types.ts contains professionals, professional_availability, professional_availability_exceptions, resources types + appointments resource_id/presence_status/timestamps"
    - "appointments is in the supabase_realtime publication on the live DB"
    - "tsc stays green after type regeneration"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "regenerated Supabase types including the four new tables + appointment columns"
      contains: "professionals"
  key_links:
    - from: "supabase/migrations/20260617000100..000600"
      to: "live DB jqjwyqlbbuqnrffdnlpp"
      via: "supabase db push"
      pattern: "professionals|resources|presence_status|supabase_realtime"
---

<objective>
Apply the six Phase 11 migrations to the live Supabase project and regenerate TypeScript types. This is the SINGLE [BLOCKING] checkpoint of the phase — it requires a human because the Supabase CLI is frequently logged into the wrong account (the nexus-* org) and must be re-authenticated against org kczvihafddupruvsrrsc / project jqjwyqlbbuqnrffdnlpp before push.

Purpose: Make professionals, professional_availability(+exceptions), resources, the appointment extension columns (resource_id + presence_status + 4 timestamps), and the supabase_realtime publication real so the Wave 4 cadastro UIs, check-in actions, and the /painel TV panel can query/subscribe against the live DB with typed clients.
Output: Migrations applied; src/types/database.types.ts regenerated with the four new tables + the appointment columns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-profissionais-recursos/11-RESEARCH.md
@supabase/migrations/20260617000100_professionals.sql
@supabase/migrations/20260617000200_professionals_rls.sql
@supabase/migrations/20260617000300_resources.sql
@supabase/migrations/20260617000400_resources_rls.sql
@supabase/migrations/20260617000500_appointment_resource_checkin.sql
@supabase/migrations/20260617000600_appointments_realtime.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Re-auth, supabase db push, regenerate types (truncation guard)</name>
  <files>src/types/database.types.ts</files>
  <action>
    Human runs the Supabase re-auth + db push + gen types sequence detailed in how-to-verify below.
    This is a checkpoint:human-action because the Supabase CLI is frequently logged into the wrong
    account; the human MUST confirm project jqjwyqlbbuqnrffdnlpp is active before pushing DDL, then
    apply the six Phase 11 migrations and regenerate src/types/database.types.ts using the temp-file
    truncation guard (write temp → verify size + new tables present → only then overwrite).
  </action>
  <what-built>
    Six migrations written in Plans 02/03 (professionals, professionals_rls, resources, resources_rls,
    appointment_resource_checkin, appointments_realtime) are ready to apply. The appointment extension
    only ADDs nullable columns and the realtime migration only ADDs appointments to the publication —
    the GIST no_overlap constraint and the status CHECK are untouched.
  </what-built>
  <how-to-verify>
    Run these from the repo root, in order. Stop and report if any step fails.

    1. Confirm the CLI is on the RIGHT account/project (recurring gotcha — MEMORY.md / Pitfall 9):
         supabase projects list
       The output MUST show project `jqjwyqlbbuqnrffdnlpp` with a ● (linked/active marker).
       If it is NOT shown (CLI logged into a nexus-* account), re-authenticate:
         supabase login
       then re-run `supabase projects list` and confirm jqjwyqlbbuqnrffdnlpp appears with ●.

    2. Apply the migrations to the live DB:
         supabase db push
       Expect the six 20260617000100..000600 migrations to apply with no error.
       (The appointment extension ADDs nullable columns only — no backfill, no NOT NULL. The
       realtime migration adds appointments to supabase_realtime; if it errors because the table is
       already in the publication, report it — do NOT force; the booking + panel still function.)

    3. Regenerate the typed schema WITH THE TRUNCATION GUARD (CLAUDE.md / Pitfall 10):
         supabase gen types typescript --project-id jqjwyqlbbuqnrffdnlpp > src/types/database.types.t
       Then verify the temp file BEFORE overwriting:
         - it has > 1000 lines:           wc -l src/types/database.types.t
         - it contains the new tables:    grep -E "professionals|professional_availability|resources|presence_status" src/types/database.types.t
       ONLY if both checks pass, overwrite atomically:
         mv src/types/database.types.t src/types/database.types.ts
       If either check fails, DELETE the temp file and report — do NOT overwrite the good types file.

    4. Verify types + build:
         npx tsc --noEmit            (must exit 0)
       and confirm src/types/database.types.ts now contains `professionals`, `professional_availability`,
       `professional_availability_exceptions`, `resources`, and the appointments `resource_id` +
       `presence_status` columns.
  </how-to-verify>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `supabase projects list` shows jqjwyqlbbuqnrffdnlpp ● before push.
    - db push applies all six migrations with no error.
    - Temp-file guard satisfied (>1000 lines + new tables present) before mv overwrite.
    - src/types/database.types.ts contains professionals, professional_availability, professional_availability_exceptions, resources + appointments resource_id/presence_status.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <resume-signal>Type "approved" once db push succeeded, types regenerated via the guard, and tsc is green. Report any error otherwise.</resume-signal>
  <done>Six migrations applied to jqjwyqlbbuqnrffdnlpp; types include the four new tables + appointment columns; appointments in supabase_realtime; tsc exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → live DB | db push applies DDL to production project; wrong-account push is the primary risk |
| gen types temp write → build | partial type write can break the Vercel build; truncation guard prevents overwrite of good types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-18 | Tampering | db push to wrong project/org | mitigate | Mandatory `supabase projects list` verification of jqjwyqlbbuqnrffdnlpp ● before push; human gate |
| T-11-19 | Denial of Service | partial/truncated gen types breaks build | mitigate | temp-file (.t) → size + table-presence check → atomic mv only on success |
| T-11-20 | Denial of Service | failed/partial migration | accept | Migrations are additive (new tables + nullable columns + publication add); no data migration on appointments; re-runnable |
| T-11-21 | Tampering | realtime publication exposes appointments broadly | mitigate | RLS on appointments still governs Realtime reads; panel uses a tenant-isolated read path (Plan 08) |
</threat_model>

<verification>
- `supabase projects list` shows jqjwyqlbbuqnrffdnlpp ●
- `supabase db push` applies the six migrations
- temp-file guard passes; `src/types/database.types.ts` regenerated, contains the four tables + appointment columns
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Live DB has professionals, professional_availability(+exceptions), resources with RLS + the appointment extension columns + appointments in supabase_realtime.
- Types regenerated via the truncation guard; tsc green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-profissionais-recursos/11-05-SUMMARY.md`
</output>
