---
phase: 11-profissionais-recursos
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/migrations/phase11.test.ts
  - src/__tests__/professionals/professionals.test.ts
  - src/__tests__/professionals/availability.test.ts
  - src/__tests__/resources/resources.test.ts
  - src/__tests__/resources/waiting-room.test.ts
autonomous: true
requirements: [PRO-01, PRO-02, PRO-03, RES-01, RES-02, RES-03]
tags: [professionals, resources, scheduling, realtime, testing]

must_haves:
  truths:
    - "Running the Phase 11 test files fails with clear RED errors (missing migrations/modules), not import/parse crashes"
    - "Every Phase 11 requirement (PRO-01..03, RES-01..03) has at least one assertion in a scaffold"
    - "A regression assertion locks the existing appointments EXCLUDE GIST + status CHECK as UNCHANGED"
  artifacts:
    - path: "src/__tests__/migrations/phase11.test.ts"
      provides: "Source-inspection of new tables/columns + realtime publication + proxy public route + GIST-regression guard"
    - path: "src/__tests__/professionals/professionals.test.ts"
      provides: "professionals/professional_availability schema + commission Zod shape assertions"
    - path: "src/__tests__/professionals/availability.test.ts"
      provides: "isSlotWithinAvailability pure-unit cases + booking-integration source-inspection"
    - path: "src/__tests__/resources/resources.test.ts"
      provides: "resources schema + isResourceAvailable pure-unit + booking resource-block source-inspection"
    - path: "src/__tests__/resources/waiting-room.test.ts"
      provides: "presence_status transitions + waitingMinutes pure-unit + /painel page source-inspection"
  key_links:
    - from: "test files"
      to: "supabase/migrations + src/lib/scheduling/* + src/actions/* + src/app/painel/* + src/proxy.ts"
      via: "readFileSync source-inspection + dynamic import with existsSync guard"
      pattern: "readFileSync|existsSync|toMatch"
---

<objective>
Create the Wave 0 RED test scaffolds for all of Phase 11 — professionals + availability, resources + maintenance-block, and the waiting-room check-in / TV panel. These tests assert the artifacts that Waves 1–4 will produce; they are RED now (target files do not exist) and turn GREEN incrementally as each plan lands. They also lock a REGRESSION GUARD on the existing appointments EXCLUDE GIST + status CHECK so no later wave can silently break the v1 agenda.

Purpose: Establish the verification contract before implementation (Nyquist). Every PRO/RES requirement gets at least one automated assertion; the test style is locked to the source-inspection + pure-unit convention used in Phases 5–10.
Output: 5 test files under `src/__tests__/{migrations,professionals,resources}/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-profissionais-recursos/11-RESEARCH.md
@.planning/phases/11-profissionais-recursos/11-VALIDATION.md
@supabase/migrations/20260605000100_clinical_tables.sql

<interfaces>
<!-- Established test conventions the executor MUST mirror. No exploration needed. -->

Source-inspection helpers (from src/__tests__/migrations/phase10.test.ts):
```typescript
import { readFileSync, readdirSync, existsSync } from 'node:fs'
import { join, resolve } from 'node:path'
const MIGRATIONS_DIR = join(process.cwd(), 'supabase/migrations')
// M(suffix): read a migration file by its filename suffix
function M(suffix: string): string {
  const files = readdirSync(MIGRATIONS_DIR)
  const match = files.find(f => f.endsWith(suffix))
  if (!match) throw new Error(`Migration ending '${suffix}' not found`)
  return readFileSync(join(MIGRATIONS_DIR, match), 'utf8')
}
// MM(suffix): like M but returns '' if no migration matches (assert RED on content, not on throw)
function MM(suffix: string): string {
  const files = readdirSync(MIGRATIONS_DIR)
  const match = files.find(f => f.endsWith(suffix))
  return match ? readFileSync(join(MIGRATIONS_DIR, match), 'utf8') : ''
}
// SRC(relPath): read a source file, return '' if missing (assert on content, not ENOENT)
function SRC(rel: string): string {
  const p = resolve(process.cwd(), rel)
  return existsSync(p) ? readFileSync(p, 'utf8') : ''
}
```

server-only mock pattern (from connectors.test.ts):
```typescript
import { vi } from 'vitest'
vi.mock('server-only', () => ({}))
```

Absolute-path dynamic-import gotcha (STATE.md D-144 / D-162): use `resolve(process.cwd(),'src/lib/scheduling/availability.ts')` + existsSync guard before `await import(...)` — `@`-alias causes TS2307 on not-yet-created modules.

ES2017 regex gotcha (STATE.md D-163): the `s` (dotAll) flag is NOT supported under the repo's tsconfig target. For multi-line SQL assertions, use multiple separate `.toMatch()` calls, never one `/.../is` across lines.

Phase 11 target migration suffixes (Plans 02/03 create these):
- `_professionals.sql`, `_professionals_rls.sql`, `_resources.sql`, `_resources_rls.sql`,
  `_appointment_resource_checkin.sql`, `_appointments_realtime.sql`

Phase 11 target source files:
- `src/lib/scheduling/availability.ts` (isSlotWithinAvailability — PURE)
- `src/lib/scheduling/resources.ts` (isResourceAvailable — PURE)
- `src/lib/scheduling/waiting.ts` (waitingMinutes — PURE)
- `src/lib/validators/professional.ts`, `src/lib/validators/resource.ts` (Zod v3)
- `src/actions/professionals.ts`, `src/actions/resources.ts`, `src/actions/checkin.ts`
- `src/actions/appointments.ts` + `src/actions/public-booking.ts` (gain availability + resource guards)
- `src/app/painel/[clinic-slug]/page.tsx` (TV panel, public)
- `src/proxy.ts` (gains `/painel` in isPublicRoute)

Existing appointments GIST + status CHECK (regression target — must stay UNCHANGED):
```sql
status TEXT NOT NULL DEFAULT 'agendado'
  CHECK (status IN ('agendado','confirmado','em_atendimento','concluido','cancelado')),
CONSTRAINT no_overlap EXCLUDE USING GIST (
  tenant_id  WITH =,
  dentist_id WITH =,
  tstzrange(start_time, end_time, '[)') WITH &&
) WHERE (status NOT IN ('cancelado'))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + professionals scaffolds (phase11.test.ts, professionals.test.ts, availability.test.ts)</name>
  <files>src/__tests__/migrations/phase11.test.ts, src/__tests__/professionals/professionals.test.ts, src/__tests__/professionals/availability.test.ts</files>
  <read_first>
    - src/__tests__/migrations/phase10.test.ts (M/MM/SRC helpers + describe/it structure + the NO-FOR-policy + GIST regression assertion idioms)
    - src/__tests__/integrations/connectors.test.ts (server-only mock + SRC pattern)
    - supabase/migrations/20260605000100_clinical_tables.sql (exact existing GIST + status CHECK text to lock as regression)
    - .planning/phases/11-profissionais-recursos/11-RESEARCH.md §Architecture Patterns (Pattern 1/2 columns to assert) + §Code Examples (commission_rules shape)
  </read_first>
  <action>
Create `src/__tests__/migrations/phase11.test.ts` (source-inspection on supabase/migrations via M/MM; use MM for new Phase 11 suffixes so the suite is RED-on-content, not crash-on-throw):
  - `_professionals.sql`: asserts `CREATE TABLE public.professionals`, `clinic_id`, `unit_id`, `user_id` (NULLABLE FK → users — assert the column has a `REFERENCES public.users` but NO `NOT NULL`), `cro`, `cro_uf`, `especialidades` (TEXT[]), `vinculo` CHECK `IN ('clt', 'pj', 'autonomo')`, `commission_rules JSONB`, `deleted_at`, `idx_professionals_clinic_id`, a partial unique index matching `professionals(clinic_id, user_id)` with `WHERE` … `user_id IS NOT NULL` and `deleted_at IS NULL`, and `professional_availability` table with `weekday`, `start_time`, `end_time`, plus `professional_availability_exceptions` with `exception_type` CHECK `IN ('folga', 'extra')`. (PRO-01)
  - `_professionals_rls.sql`: asserts `ENABLE ROW LEVEL SECURITY` on all three professional tables, a SELECT policy `clinic_id = get_my_tenant_id()`, and a write policy with BOTH `USING` and `WITH CHECK` restricted to `get_my_role() IN ('admin'` (and `superadmin`). (Access Control)
  - **REGRESSION GUARD (CRITICAL):** read `20260605000100_clinical_tables.sql` via M and assert it STILL contains the exact `CONSTRAINT no_overlap EXCLUDE USING GIST`, `tenant_id  WITH =`, `dentist_id WITH =`, `WHERE (status NOT IN ('cancelado'))`, and the status CHECK `IN ('agendado','confirmado','em_atendimento','concluido','cancelado')`. Add a describe block named `'REGRESSION: appointments GIST + status CHECK unchanged'`. Also assert NO Phase 11 migration file (scan all `*.sql`) contains `DROP CONSTRAINT no_overlap` or `ALTER COLUMN status` on appointments.

Create `src/__tests__/professionals/professionals.test.ts`:
  - `vi.mock('server-only', () => ({}))`.
  - SRC source-inspection on `src/actions/professionals.ts`: matches `assertNotReadOnly`, `get_my_tenant_id` is NOT used client-side (it's a Server Action — assert it references `createClient` from `@/lib/supabase/server`), and `logBusinessEvent`. (PRO-01)
  - Dynamic-import (absolute-path + existsSync guard) the commission Zod schema from `src/lib/validators/professional.ts`. PURE unit on the `commissionRulesSchema` (or the professional schema's `commission_rules` field): an array of `{ type: 'flat_pct', pct: 40 }` parses OK; `{ type: 'service_pct', service_id: '<uuid>', pct: 35 }` parses OK; `pct: 150` (out of 0–100) FAILS; a missing `type` FAILS. (PRO-03 — STORE only, shape validated)
  - SRC on `src/actions/professionals.ts`: assert a backfill/seed reference exists OR (acceptable) assert the migration test covers backfill — to keep PRO-01 "backfill existing dentists" traceable, assert `_professionals.sql` (via MM) contains an `INSERT INTO public.professionals` … `FROM public.users` … `role = 'dentist'` backfill block here in professionals.test.ts.

Create `src/__tests__/professionals/availability.test.ts`:
  - `vi.mock('server-only', () => ({}))`. Dynamic-import `isSlotWithinAvailability` from `src/lib/scheduling/availability.ts` (absolute-path + existsSync guard; skip the pure asserts cleanly with a guarded `if (!fn) return` so tsc stays green while RED).
  - PURE unit cases on `isSlotWithinAvailability(grade, exceptions, slot)` where `grade` = weekly windows `[{weekday, start_time, end_time}]`, `exceptions` = `[{exception_date, exception_type, start_time?, end_time?}]`, `slot` = `{start: Date|ISO, end: Date|ISO}`:
    - recurring window covers slot (weekday match, window.start ≤ slot.start && window.end ≥ slot.end) → true
    - slot outside all windows → false
    - a `folga` exception on the slot's date → false even if a recurring window would cover it
    - an `extra` exception covering the slot on a day with no recurring window → true
    - empty grade + no exceptions → false
  - SRC source-inspection on `src/actions/appointments.ts`: assert `createAppointment` references `isSlotWithinAvailability` (or imports from `@/lib/scheduling/availability`) and returns a `disponibilidade` rejection message. (PRO-02)
  - SRC on `src/actions/public-booking.ts`: assert `getBookedSlots` (or a new `getAvailableSlots`) references availability, and `createPublicAppointment` references `isSlotWithinAvailability`. (PRO-02)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/migrations/phase11.test.ts src/__tests__/professionals/professionals.test.ts src/__tests__/professionals/availability.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and the suite RUNS (no import/parse crash) — new-artifact assertions fail RED (intended Wave 0 state).
    - The REGRESSION GUARD describe block PASSES now (it reads the existing, unchanged clinical_tables migration).
    - `npx tsc --noEmit` stays clean (existsSync/SRC guards prevent TS2307 on absent modules).
  </acceptance_criteria>
  <done>phase11.test.ts, professionals.test.ts, availability.test.ts exist; new-artifact asserts RED; GIST-regression asserts GREEN; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Resources + waiting-room scaffolds (resources.test.ts, waiting-room.test.ts)</name>
  <files>src/__tests__/resources/resources.test.ts, src/__tests__/resources/waiting-room.test.ts</files>
  <read_first>
    - src/__tests__/migrations/phase10.test.ts (M/MM/SRC helpers + RLS USING+WITH CHECK assertion idiom)
    - src/proxy.ts (isPublicRoute list shape for the /painel assertion)
    - .planning/phases/11-profissionais-recursos/11-RESEARCH.md §Pattern 3 (resources schema) + §Pattern 4 (presence_status) + §Pattern 5 (realtime publication) + §Pitfall 3 (LGPD initials-only)
  </read_first>
  <action>
Create `src/__tests__/resources/resources.test.ts`:
  - `vi.mock('server-only', () => ({}))`.
  - Migration source-inspection via MM:
    - `_resources.sql`: asserts `CREATE TABLE public.resources`, `clinic_id`, `unit_id`, `tipo` CHECK `IN ('sala', 'cadeira', 'equipamento')`, `patrimonio`, `numero_serie`, `status` CHECK `IN ('ativo', 'manutencao', 'inativo')`, `deleted_at`, `idx_resources_clinic_id`, `idx_resources_unit_id`. (RES-01)
    - `_resources_rls.sql`: `ENABLE ROW LEVEL SECURITY`, SELECT `clinic_id = get_my_tenant_id()`, write policy with BOTH USING + WITH CHECK + admin/superadmin role gate.
    - `_appointment_resource_checkin.sql`: asserts `ALTER TABLE public.appointments` `ADD COLUMN resource_id UUID REFERENCES public.resources(id)`, `idx_appointments_resource_id`, AND that it does NOT contain `DROP CONSTRAINT no_overlap` (resource is app-level only — RES-02). (RES-02)
  - Dynamic-import `isResourceAvailable` from `src/lib/scheduling/resources.ts` (guarded). PURE unit: `isResourceAvailable('ativo') === true`; `'manutencao' === false`; `'inativo' === false`; `null/undefined === false`.
  - SRC on `src/actions/appointments.ts`: assert it references `resource` status check and a `manutenção`/`indisponível` rejection message; SRC on `src/actions/resources.ts`: matches `assertNotReadOnly` + `logBusinessEvent`. (RES-02 / Access Control)

Create `src/__tests__/resources/waiting-room.test.ts`:
  - `vi.mock('server-only', () => ({}))`.
  - Migration source-inspection via MM:
    - `_appointment_resource_checkin.sql`: asserts `presence_status` column with CHECK `IN ('aguardando', 'chamado', 'em_atendimento', 'finalizado')` (assert it is a SEPARATE column — NOT folded into `status`), plus `arrived_at`, `called_at`, `started_at`, `finished_at` TIMESTAMPTZ columns. (RES-03)
    - `_appointments_realtime.sql`: asserts `ALTER PUBLICATION supabase_realtime ADD TABLE public.appointments`. (RES-03)
  - Dynamic-import `waitingMinutes` from `src/lib/scheduling/waiting.ts` (guarded). PURE unit:
    - `waitingMinutes(arrivedAt, calledAt)` where called 25 min after arrived → 25
    - still waiting (calledAt null) → minutes from arrived to "now" ≥ 0 (use a fixed `now` arg or assert ≥ 0)
    - `arrivedAt` null → null
  - PURE unit on a presence transition helper (assert an exported `isValidPresenceTransition(from, to)` or `PRESENCE_FLOW` ordering): `aguardando→chamado` valid; `chamado→em_atendimento` valid; `em_atendimento→finalizado` valid; `aguardando→finalizado` invalid (skips). If the executor models this as a plain ordered array, assert the array `['aguardando','chamado','em_atendimento','finalizado']` exists.
  - SRC on `src/actions/checkin.ts`: matches `assertNotReadOnly`, sets `arrived_at`/`called_at`/`started_at`/`finished_at`, and `presence_status`. (RES-03)
  - SRC on `src/proxy.ts`: assert `isPublicRoute` references `/painel` (TV panel is public). (RES-03)
  - SRC on `src/app/painel/[clinic-slug]/page.tsx`: assert it does NOT select `cpf` and does NOT select `full_name` directly into the panel payload (LGPD initials-only — Pitfall 3); assert it references `presence_status` and an `initials`/`iniciais` transform. Use guarded SRC (RED until Plan 08).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/resources/resources.test.ts src/__tests__/resources/waiting-room.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and the suite RUNS; new-artifact assertions are RED for not-yet-created targets.
    - `isResourceAvailable` and `waitingMinutes` pure-unit asserts are written so they PASS the moment Plans 03/02 create the libs.
    - `npx tsc --noEmit` clean; full suite still runs (no crash).
  </acceptance_criteria>
  <done>resources.test.ts, waiting-room.test.ts exist; RED for missing artifacts; tsc green; full suite runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → source files | Tests only READ source; no privilege boundary crossed. Scaffolds encode the security + LGPD + regression contract that later waves must satisfy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | accidental GIST/status break | mitigate | phase11.test.ts REGRESSION GUARD asserts no_overlap GIST + status CHECK unchanged; no DROP CONSTRAINT/ALTER COLUMN status in any Phase 11 migration |
| T-11-02 | Information Disclosure | /painel leaking patient PII | mitigate | waiting-room.test.ts asserts panel page selects NO cpf/full_name + uses initials transform (LGPD) |
| T-11-03 | Elevation of Privilege | non-admin mutating professionals/resources | mitigate | scaffolds assert admin/superadmin role gate in RLS write policy + assertNotReadOnly in actions |
| T-11-04 | Tampering | resource maintenance bypass | mitigate | resources.test.ts asserts isResourceAvailable + booking rejection message |
</threat_model>

<verification>
- `npx vitest run src/__tests__/migrations/phase11.test.ts src/__tests__/professionals/ src/__tests__/resources/` runs without import crash (RED on new-artifact assertions is expected; GIST-regression block GREEN).
- `npx tsc --noEmit` clean.
- Existing suites unaffected: `npx vitest run src/__tests__/agenda/ src/__tests__/actions/ src/__tests__/proxy/` still green.
</verification>

<success_criteria>
- 5 scaffold files created covering all 6 requirements with grep-verifiable assertions.
- A passing regression guard locks the appointments GIST + status CHECK.
- Test style matches the Phase 5–10 source-inspection + pure-unit convention; tsc green; existing tests untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/11-profissionais-recursos/11-01-SUMMARY.md`
</output>
