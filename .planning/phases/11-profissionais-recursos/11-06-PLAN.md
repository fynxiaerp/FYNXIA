---
phase: 11-profissionais-recursos
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified:
  - src/actions/professionals.ts
  - src/components/professionals/ProfessionalForm.tsx
  - src/components/professionals/AvailabilityGrid.tsx
  - src/app/(dashboard)/clinica/profissionais/page.tsx
  - src/app/(dashboard)/clinica/profissionais/[id]/page.tsx
autonomous: true
requirements: [PRO-01, PRO-03]
tags: [professionals, ui, forms, availability]

must_haves:
  truths:
    - "Admin can create/edit a professional with CRO+UF, especialidades, vínculo, and a weekly availability grade + exceptions"
    - "Commission rules (% flat or per-service) are captured and STORED as JSONB (no calculation)"
    - "Mutations are blocked for read-only roles (assertNotReadOnly) and gated to admin/superadmin"
  artifacts:
    - path: "src/actions/professionals.ts"
      provides: "create/update/delete professional + availability + exceptions Server Actions"
      exports: ["createProfessional", "updateProfessional"]
    - path: "src/components/professionals/ProfessionalForm.tsx"
      provides: "tabbed RHF+Zod form: Ficha + Horários + Comissão"
    - path: "src/app/(dashboard)/clinica/profissionais/page.tsx"
      provides: "professionals list (RSC)"
  key_links:
    - from: "src/components/professionals/ProfessionalForm.tsx"
      to: "src/actions/professionals.ts"
      via: "form submit → Server Action"
      pattern: "createProfessional|updateProfessional"
---

<objective>
Build the professional cadastro (PRO-01 + PRO-03): a Server Action layer (`createProfessional`/`updateProfessional`/`deleteProfessional` + nested availability grade + exceptions writes), a tabbed RHF+Zod v3 form (Ficha = CRO/UF/especialidades/vínculo; Horários = weekly AvailabilityGrid + exceptions; Comissão = commission_rules editor stored as JSONB), and the list + edit RSC pages under `/clinica/profissionais`.

Purpose: Let admins register the rich clinical professional record that the booking availability gate (Plan 04) and Phase 16 commission consume. Uses the design-system conventions (PageHeader, tokens, @base-ui render-prop, RHF+Zod v3, pt-BR).
Output: 1 action file + 2 components + 2 pages. Self-contained UI; no overlap with Plans 07/08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-profissionais-recursos/11-RESEARCH.md
@src/lib/validators/professional.ts
@src/lib/auth/guards.ts
@src/app/(dashboard)/clinica/agenda/page.tsx
@src/components/shell/nav-config.ts

<interfaces>
<!-- Contracts the executor uses. -->

From src/lib/validators/professional.ts (Plan 02):
```typescript
professionalSchema, type ProfessionalInput
commissionRulesSchema           // discriminatedUnion flat_pct | service_pct
availabilityWindowSchema, availabilityExceptionSchema
```

Server Action conventions (STATE.md): `'use server'` files export ASYNC functions only — NO sync const/enum exports (D-138). Constants/types live in non-'use server' files. Call `await assertNotReadOnly()` at the TOP of every mutation. Use `createClient` from `@/lib/supabase/server`; resolve actor tenant/role like src/actions/appointments.ts getActor(). Log via `logBusinessEvent`.

RSC page pattern (from agenda/page.tsx): `await headers()` → `x-user-id`, fetch tenant_id, render `PageHeader` + content; wrap client form in `NuqsAdapter` only if it uses nuqs.

UI primitives: shadcn Dialog/Select/Input/Label/Button already exist under src/components/ui/*; Button is the @base-ui primitive (render-prop, NEVER asChild — STATE.md). Tokens only (no raw slate/gray). Tabs: use shadcn Tabs if present (run `shadcn add tabs` if missing) — verify under src/components/ui/tabs.tsx first.

professionals table columns (post-push, Plan 05): clinic_id, unit_id, user_id (nullable), full_name, cro, cro_uf, especialidades TEXT[], vinculo, commission_rules JSONB, ativo, deleted_at.
professional_availability: professional_id, clinic_id, weekday (0..6), start_time, end_time.
professional_availability_exceptions: professional_id, clinic_id, exception_date, exception_type (folga|extra), start_time, end_time, reason.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: professionals Server Actions (CRUD + availability + exceptions)</name>
  <files>src/actions/professionals.ts</files>
  <read_first>
    - src/actions/appointments.ts (getActor pattern, role gate, logBusinessEvent, 'use server' async-only)
    - src/lib/auth/guards.ts (assertNotReadOnly)
    - src/lib/validators/professional.ts (schemas to validate against)
    - .planning/phases/11-profissionais-recursos/11-RESEARCH.md §RLS for professionals (admin write)
  </read_first>
  <action>
Create `src/actions/professionals.ts` (`'use server'`, async exports only):
  - `createProfessional(input: ProfessionalInput & { availability?: AvailabilityWindow[]; exceptions?: AvailabilityException[] })`: `await assertNotReadOnly()`; resolve actor (tenant_id + role); role-gate to admin/superadmin; `professionalSchema.safeParse`; insert the professional row (clinic_id = actor.tenant_id, commission_rules from input); then insert availability rows + exception rows with `professional_id` + denormalized `clinic_id`. Return `{ success, id?, error? }`. `logBusinessEvent` with only IDs (no PII).
  - `updateProfessional(id, input)`: same guards; tenant-scoped update (`.eq('id', id).eq('clinic_id', actor.tenant_id)`); replace availability grade (delete existing rows for the professional + reinsert) inside the action; log.
  - `deleteProfessional(id)`: soft delete (`deleted_at = now()`), tenant-scoped; log.
  - Keep all queries tenant-scoped via `clinic_id = actor.tenant_id` (defense in depth atop RLS).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/professionals/professionals.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - professionals.test.ts action source-inspection asserts (assertNotReadOnly, createClient server, logBusinessEvent) GREEN.
    - `npx tsc --noEmit` exits 0 against the regenerated types (Plan 05).
    - Only async exports in the 'use server' file.
  </acceptance_criteria>
  <done>professionals.ts exports create/update/delete with availability + exceptions writes; admin-gated; assertNotReadOnly; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: ProfessionalForm (tabbed) + AvailabilityGrid + list/edit pages</name>
  <files>src/components/professionals/ProfessionalForm.tsx, src/components/professionals/AvailabilityGrid.tsx, src/app/(dashboard)/clinica/profissionais/page.tsx, src/app/(dashboard)/clinica/profissionais/[id]/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/agenda/page.tsx (RSC tenant resolution + PageHeader + EmptyState pattern)
    - src/components/agenda/AgendaCalendar.tsx (client component + shadcn Select/Dialog/Button usage + tokens)
    - src/lib/validators/professional.ts (form schema + commission shape)
    - src/components/shell/PageHeader.tsx (header API)
  </read_first>
  <action>
Create `src/components/professionals/AvailabilityGrid.tsx` (`'use client'`): a weekly editor — 7 weekday rows (Domingo..Sábado), each with add/remove time windows (start/end `<input type="time">`), plus an exceptions sub-section (date + folga/extra + optional start/end + reason). Controlled value `{ windows: AvailabilityWindow[]; exceptions: AvailabilityException[] }` via props/onChange so the parent RHF form owns the state. pt-BR labels; tokens only.
Create `src/components/professionals/ProfessionalForm.tsx` (`'use client'`): RHF + `zodResolver(professionalSchema)`; defaultValues supply all fields (NO `.default()` in schema — D-133). Three tabs:
  - Ficha: full_name, cro, cro_uf (2-char uppercase), especialidades (multi via comma/tag input), vinculo (Select clt/pj/autonomo), unit_id (Select of units), optional user_id (Select of role='dentist' users to link login — or "sem login").
  - Horários: render `<AvailabilityGrid>` bound to form state.
  - Comissão: commission_rules editor — list of rules, each `flat_pct` (single pct) or `service_pct` (service_id + pct); store as the JSONB array. Add helper text: "Regra armazenada; o cálculo do repasse ocorre no módulo financeiro (Fase 16)." (PRO-03 — store only, no calc).
  On submit call `createProfessional`/`updateProfessional`; show shadcn Alert on error (pt-BR); on success router.push back to the list.
Create `src/app/(dashboard)/clinica/profissionais/page.tsx` (RSC): resolve tenant via `headers()` x-user-id; fetch `professionals WHERE clinic_id = tenant AND deleted_at IS NULL`; render `PageHeader title="Profissionais"` + a table/list (nome, CRO/UF, vínculo, especialidades, ativo) with an EmptyState + "Novo profissional" CTA linking to `[id]/page.tsx?new` or a create route. Read `x-read-only` to hide mutation CTAs for read-only roles.
Create `src/app/(dashboard)/clinica/profissionais/[id]/page.tsx` (RSC): fetch one professional + its availability + exceptions; render `<ProfessionalForm>` with the loaded values for edit (and a `new` mode when id is a sentinel like `novo`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx next build` completes clean (no 'use server' sync-export error, no RSC function-across-boundary error).
    - Form uses RHF + zodResolver(professionalSchema); commission editor stores the JSONB array; AvailabilityGrid bound to form state.
    - Read-only roles see no mutation CTAs (x-read-only respected).
  </acceptance_criteria>
  <done>Tabbed ProfessionalForm + AvailabilityGrid + list/edit pages render; build green; PRO-01 + PRO-03 cadastro complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → professionals Server Actions | untrusted input; Zod-validated + admin-gated + tenant-scoped |
| read-only role → mutation action | assertNotReadOnly blocks at the action layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-22 | Elevation of Privilege | read-only role mutating professionals | mitigate | assertNotReadOnly() at top of create/update/delete + admin/superadmin role gate |
| T-11-23 | Spoofing | cross-tenant professional write | mitigate | clinic_id = actor.tenant_id on every query + RLS USING+WITH CHECK |
| T-11-24 | Input Validation | malformed commission_rules | mitigate | commissionRulesSchema bounds pct + uuid before storage |
| T-11-25 | Tampering | invalid availability window times | mitigate | availabilityWindowSchema validates HH:MM + weekday range |
</threat_model>

<verification>
- `npx vitest run src/__tests__/professionals/` GREEN; `npx tsc --noEmit` clean; `npx next build` green.
- Existing suites unaffected.
</verification>

<success_criteria>
- Admin creates/edits a professional (Ficha + Horários + Comissão); availability persists; commission_rules stored as JSONB (no calc).
- Read-only roles cannot mutate; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-profissionais-recursos/11-06-SUMMARY.md`
</output>
