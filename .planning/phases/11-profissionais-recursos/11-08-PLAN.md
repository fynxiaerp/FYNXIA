---
phase: 11-profissionais-recursos
plan: 08
type: execute
wave: 4
depends_on: [05]
files_modified:
  - src/actions/checkin.ts
  - src/lib/scheduling/panel.ts
  - src/components/painel/WaitingPanel.tsx
  - src/app/painel/[clinic-slug]/page.tsx
  - src/components/agenda/CheckinControls.tsx
autonomous: true
requirements: [RES-03]
tags: [waiting-room, checkin, realtime, panel, lgpd]

must_haves:
  truths:
    - "Reception can move a patient through aguardando→chamado→em_atendimento→finalizado with timestamps"
    - "The /painel/[clinic-slug] TV page shows called patients in real time via Supabase Realtime"
    - "The panel exposes ONLY presence_status + patient INITIALS + waiting time — never full_name/CPF (LGPD)"
    - "The panel is tenant-isolated: it resolves the clinic by slug and only shows that clinic's appointments"
  artifacts:
    - path: "src/actions/checkin.ts"
      provides: "check-in/call/start/finish Server Actions setting presence_status + timestamps; getPanelRows public read (initials-only)"
      exports: ["markArrived", "callPatient", "startTreatment", "finishTreatment", "getPanelRows"]
    - path: "src/lib/scheduling/panel.ts"
      provides: "toInitials + panel row shaping (no PII) — pure"
      exports: ["toInitials"]
    - path: "src/app/painel/[clinic-slug]/page.tsx"
      provides: "public TV panel RSC resolving clinic by slug, initials-only"
  key_links:
    - from: "src/components/painel/WaitingPanel.tsx"
      to: "supabase Realtime appointments channel"
      via: ".channel().on('postgres_changes') → invalidateQueries"
      pattern: "postgres_changes|channel"
---

<objective>
Build the waiting-room flow (RES-03): the check-in Server Actions (`markArrived`/`callPatient`/`startTreatment`/`finishTreatment`) that set `presence_status` + the four timestamps, a small agenda/reception control to drive them, and the public `/painel/[clinic-slug]` TV panel that subscribes to Supabase Realtime and renders called patients with INITIALS ONLY + waiting time. The panel is strictly tenant-isolated (resolves clinic by slug) and leaks NO PII beyond initials (LGPD — Pitfall 3).

Purpose: Deliver the real-time waiting-room display + waiting-time measurement. Reuses `waitingMinutes`/`PRESENCE_FLOW` (Plan 03) and the CLAUDE.md Realtime→invalidateQueries pattern.
Output: 1 action file + 1 pure panel lib + 1 client panel + 1 public RSC page + 1 reception control. No file overlap with Plans 06/07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-profissionais-recursos/11-RESEARCH.md
@src/lib/scheduling/waiting.ts
@src/lib/auth/guards.ts
@src/actions/public-booking.ts
@src/lib/supabase/client.ts
@src/proxy.ts

<interfaces>
<!-- Contracts the executor uses. -->

From src/lib/scheduling/waiting.ts (Plan 03): `waitingMinutes(arrivedAt, calledAt, now?)`, `PRESENCE_FLOW`, `isValidPresenceTransition(from, to)`.

appointments check-in columns (post-push, Plan 05): presence_status ('aguardando'|'chamado'|'em_atendimento'|'finalizado'|null), arrived_at, called_at, started_at, finished_at; plus patient_id → patients(full_name).

Realtime client (from src/lib/supabase/client.ts): `createClient()` returns a browser client with `.channel(name).on('postgres_changes', { event:'*', schema:'public', table:'appointments', filter:'tenant_id=eq.<clinicId>' }, cb).subscribe()`; cleanup via `supabase.removeChannel(channel)`. CLAUDE.md prescribes Realtime → `queryClient.invalidateQueries`. CSP already allows wss://*.supabase.co (security-headers test).

Public flow precedent (src/actions/public-booking.ts): resolve clinic by slug via `createAdminClient()` (service role) — the panel page is public (no session) so it uses the same admin-client + slug resolution, then selects ONLY non-PII columns + computes initials server-side. proxy.ts already lists /painel as public (Plan 04).

LGPD contract (Pitfall 3): the panel payload sent to the client MUST contain ONLY { id, presence_status, initials, arrived_at, called_at } — NEVER full_name or cpf. Initials computed server-side from full_name (e.g. "João da Silva" → "J.S.").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: check-in Server Actions + reception control + toInitials pure lib</name>
  <files>src/actions/checkin.ts, src/lib/scheduling/panel.ts, src/components/agenda/CheckinControls.tsx</files>
  <read_first>
    - src/actions/appointments.ts (getActor + role gate + logBusinessEvent + tenant scope + 'use server' async-only)
    - src/lib/auth/guards.ts (assertNotReadOnly)
    - src/lib/scheduling/waiting.ts (PRESENCE_FLOW + isValidPresenceTransition)
    - src/__tests__/resources/waiting-room.test.ts (checkin action source-inspection expectations)
  </read_first>
  <action>
Create `src/lib/scheduling/panel.ts` (PURE — no server-only): export `toInitials(fullName: string | null | undefined): string` → uppercase first letter of up to the first 2 name tokens joined with '.', trailing '.' (e.g. "João da Silva" → "J.S."; empty → "?"). Export a `PanelRow` type `{ id, presence_status, initials, arrived_at, called_at }` (NO full_name/cpf — the LGPD shape).
Create `src/actions/checkin.ts` (`'use server'`, async-only):
  - `markArrived(appointmentId)`: `await assertNotReadOnly()`; resolve actor; role gate (admin/dentist/receptionist/superadmin); tenant-scoped update setting `presence_status='aguardando'`, `arrived_at=now()`; log.
  - `callPatient(appointmentId)`: guards; set `presence_status='chamado'`, `called_at=now()`; log.
  - `startTreatment(appointmentId)`: guards; set `presence_status='em_atendimento'`, `started_at=now()`; log.
  - `finishTreatment(appointmentId)`: guards; set `presence_status='finalizado'`, `finished_at=now()`; log.
  - Each update is `.eq('id', appointmentId).eq('tenant_id', actor.tenant_id)`. Optionally validate the transition with `isValidPresenceTransition` and reject illegal jumps with a pt-BR error.
Create `src/components/agenda/CheckinControls.tsx` (`'use client'`): given an appointment's current presence_status, render the next-step button(s) (Chegou / Chamar / Iniciar / Finalizar) calling the matching action; pt-BR; tokens; disable when read-only. (This is a reception-side control; it can be embedded by the agenda or a reception list — keep it self-contained and prop-driven so it does not require editing AgendaCalendar.tsx.)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/resources/waiting-room.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - waiting-room.test.ts checkin action source-inspection asserts (assertNotReadOnly + presence_status + the four timestamps) GREEN.
    - `npx tsc --noEmit` exits 0; panel.ts has no server-only import (importable by the client panel).
  </acceptance_criteria>
  <done>checkin.ts (4 transitions) + panel.ts (toInitials) + CheckinControls render; admin/reception-gated; tsc clean; target asserts GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: public /painel TV page + WaitingPanel (Realtime, initials-only, tenant-isolated)</name>
  <files>src/app/painel/[clinic-slug]/page.tsx, src/components/painel/WaitingPanel.tsx</files>
  <read_first>
    - src/actions/public-booking.ts (createAdminClient + resolve clinic by slug pattern for the public flow)
    - src/lib/supabase/client.ts (browser client → Realtime .channel)
    - src/lib/scheduling/panel.ts + src/lib/scheduling/waiting.ts (toInitials + waitingMinutes)
    - .planning/phases/11-profissionais-recursos/11-RESEARCH.md §Pattern 5 (Realtime) + §Pitfall 3 (LGPD initials) + §Pitfall 6 (publication) + §Open Question 3 (unit query param)
  </read_first>
  <action>
Create `src/app/painel/[clinic-slug]/page.tsx` (RSC, PUBLIC — no auth; runtime nodejs):
  - Resolve clinic by slug via `createAdminClient()` (same as public-booking); if not found → render a neutral "Painel indisponível" (no tenant info leak).
  - Fetch today's appointments for that clinic with `presence_status IN ('aguardando','chamado','em_atendimento')`, selecting ONLY `id, presence_status, arrived_at, called_at, patient:patients(full_name)`. Map to `PanelRow[]` via `toInitials(full_name)` SERVER-SIDE so full_name NEVER reaches the client. Pass `clinicId` + the initial `PanelRow[]` to `<WaitingPanel>`.
  - Optionally read `?unit=<uuid>` to scope by unit (Open Question 3) — filter the query by unit_id when present.
  - Do NOT select `cpf`; do NOT pass `full_name` to the client component (assert in waiting-room.test.ts).
Create `src/components/painel/WaitingPanel.tsx` (`'use client'`): TV-optimized layout (large type, high contrast, tokens — dark/neon brand ok). Show the "chamado" patients prominently (initials + waiting time via `waitingMinutes(arrived_at, called_at)`), plus an aguardando queue. Subscribe to Supabase Realtime on mount: `createClient().channel('painel:'+clinicId).on('postgres_changes', { event:'*', schema:'public', table:'appointments', filter:'tenant_id=eq.'+clinicId }, () => refetch())`. On event, re-fetch the panel rows through a small public Server Action `getPanelRows(clinicSlug, unitId?)` — ADD IT TO `src/actions/checkin.ts` (already in files_modified; do NOT create a new file). It uses the admin client + the SAME initials-only shaping; returns PanelRow[] only. Use TanStack Query `invalidateQueries`/refetch per CLAUDE.md. Clean up the channel on unmount. A periodic 30s tick re-renders waiting-time counters.
  - The Server Action `getPanelRows` MUST mirror the page's initials-only shaping (never returns full_name/cpf).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - waiting-room.test.ts panel-page source-inspection asserts GREEN: page references presence_status + an initials/iniciais transform; does NOT select cpf; does NOT pass full_name to the client.
    - WaitingPanel references `postgres_changes` / `.channel(` and a tenant filter `tenant_id=eq.`; `npx tsc --noEmit` + `npx next build` clean.
    - /painel reachable without auth (proxy public route from Plan 04).
  </acceptance_criteria>
  <done>Public /painel renders called patients in real time with initials + waiting time; tenant-isolated; no PII beyond initials; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon → /painel public page | no auth; must be tenant-isolated by slug and leak no PII beyond initials |
| Realtime channel → browser | postgres_changes stream; RLS on appointments + tenant_id filter scope it |
| reception → check-in actions | authenticated; assertNotReadOnly + role gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-29 | Information Disclosure | panel leaking patient name/CPF | mitigate | server-side toInitials shaping; page/action select NO cpf, NEVER pass full_name to client; PanelRow type excludes PII |
| T-11-30 | Information Disclosure | cross-tenant Realtime subscription | mitigate | channel filter `tenant_id=eq.<clinicId>` + clinic resolved by slug; RLS on appointments governs reads |
| T-11-31 | Elevation of Privilege | read-only/anon driving check-in | mitigate | check-in actions require auth + assertNotReadOnly + staff role gate; panel is read-only display |
| T-11-32 | Tampering | illegal presence transition | mitigate | isValidPresenceTransition guards the state machine in checkin actions |
| T-11-33 | Denial of Service | Realtime not firing (table not in publication) | accept | publication migration (Plan 03/05) adds appointments; verified at db push |
</threat_model>

<verification>
- `npx vitest run src/__tests__/resources/waiting-room.test.ts` GREEN (checkin + panel initials-only asserts).
- `npx tsc --noEmit` clean; `npx next build` green.
- Manual UAT (documented in 11-VALIDATION): reception calls a patient → /painel updates live; panel shows initials only.
</verification>

<success_criteria>
- Reception drives presence_status through the flow with timestamps; waiting time measured.
- Public /painel updates in real time, tenant-isolated, initials-only (LGPD); build green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-profissionais-recursos/11-08-SUMMARY.md`
</output>
