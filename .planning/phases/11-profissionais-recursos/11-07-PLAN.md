---
phase: 11-profissionais-recursos
plan: 07
type: execute
wave: 4
depends_on: [05]
files_modified:
  - src/actions/resources.ts
  - src/components/resources/ResourceForm.tsx
  - src/app/(dashboard)/clinica/recursos/page.tsx
  - src/app/(dashboard)/clinica/recursos/[id]/page.tsx
autonomous: true
requirements: [RES-01]
tags: [resources, ui, forms]

must_haves:
  truths:
    - "Admin can create/edit a resource (sala/cadeira/equipamento) with patrimônio/série and status (ativo/manutencao/inativo)"
    - "Setting a resource to 'manutencao' is the data that blocks booking (gate lives in Plan 04)"
    - "Mutations are blocked for read-only roles (assertNotReadOnly) and gated to admin/superadmin"
  artifacts:
    - path: "src/actions/resources.ts"
      provides: "create/update/delete resource Server Actions"
      exports: ["createResource", "updateResource"]
    - path: "src/components/resources/ResourceForm.tsx"
      provides: "RHF+Zod resource form"
    - path: "src/app/(dashboard)/clinica/recursos/page.tsx"
      provides: "resources list (RSC) with status badges"
  key_links:
    - from: "src/components/resources/ResourceForm.tsx"
      to: "src/actions/resources.ts"
      via: "form submit → Server Action"
      pattern: "createResource|updateResource"
---

<objective>
Build the resources cadastro (RES-01): a Server Action layer (`createResource`/`updateResource`/`deleteResource`), an RHF+Zod v3 form (nome, tipo, unidade, patrimônio, número de série, status, manutenção prevista), and the list + edit RSC pages under `/clinica/recursos`. The booking-time maintenance gate already lives in Plan 04 (`isResourceAvailable`); this plan delivers the cadastro that produces that data.

Purpose: Let admins register physical resources whose `status='manutencao'` blocks the agenda. Design-system conventions (PageHeader, tokens, @base-ui render-prop, RHF+Zod v3, pt-BR).
Output: 1 action file + 1 component + 2 pages. No file overlap with Plans 06/08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-profissionais-recursos/11-RESEARCH.md
@src/lib/validators/resource.ts
@src/lib/scheduling/resources.ts
@src/lib/auth/guards.ts
@src/app/(dashboard)/clinica/agenda/page.tsx

<interfaces>
<!-- Contracts the executor uses. -->

From src/lib/validators/resource.ts (Plan 03): `resourceSchema`, `type ResourceInput`.
From src/lib/scheduling/resources.ts (Plan 03): `RESOURCE_STATUS` = ['ativo','manutencao','inativo'], `RESOURCE_TYPES` = ['sala','cadeira','equipamento'].

resources table columns (post-push, Plan 05): clinic_id, unit_id (NOT NULL), nome, tipo, patrimonio, numero_serie, status, manutencao_prevista, deleted_at.

Server Action conventions: `'use server'` async-only exports; `await assertNotReadOnly()` at top; admin/superadmin role gate; `createClient` from `@/lib/supabase/server`; tenant-scope via clinic_id = actor.tenant_id; `logBusinessEvent` IDs only.

RSC + UI conventions: PageHeader + EmptyState + tokens; shadcn Select/Input/Label/Button (Button = @base-ui render-prop, NEVER asChild); status rendered as a colored badge using tokens (ativo = positive, manutencao = warning, inativo = muted). Read `x-read-only` to hide mutation CTAs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: resources Server Actions (CRUD)</name>
  <files>src/actions/resources.ts</files>
  <read_first>
    - src/actions/appointments.ts (getActor + role gate + logBusinessEvent + 'use server' async-only)
    - src/lib/auth/guards.ts (assertNotReadOnly)
    - src/lib/validators/resource.ts (resourceSchema)
  </read_first>
  <action>
Create `src/actions/resources.ts` (`'use server'`, async exports only):
  - `createResource(input: ResourceInput)`: `await assertNotReadOnly()`; resolve actor; role-gate admin/superadmin; `resourceSchema.safeParse`; insert row with `clinic_id = actor.tenant_id`; `logBusinessEvent` (IDs only). Return `{ success, id?, error? }`.
  - `updateResource(id, input)`: same guards; tenant-scoped update (`.eq('id', id).eq('clinic_id', actor.tenant_id)`); log. (status changes to 'manutencao' are the booking-block trigger consumed by Plan 04.)
  - `deleteResource(id)`: soft delete (`deleted_at = now()`), tenant-scoped; log.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/resources/resources.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - resources.test.ts action source-inspection asserts (assertNotReadOnly + logBusinessEvent) GREEN.
    - `npx tsc --noEmit` exits 0 against regenerated types; only async exports in the 'use server' file.
  </acceptance_criteria>
  <done>resources.ts exports create/update/delete; admin-gated; assertNotReadOnly; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: ResourceForm + list/edit pages</name>
  <files>src/components/resources/ResourceForm.tsx, src/app/(dashboard)/clinica/recursos/page.tsx, src/app/(dashboard)/clinica/recursos/[id]/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/agenda/page.tsx (RSC tenant resolution + PageHeader + EmptyState)
    - src/components/agenda/AgendaCalendar.tsx (shadcn Select/Input/Button + tokens usage)
    - src/lib/validators/resource.ts + src/lib/scheduling/resources.ts (schema + status/type consts)
  </read_first>
  <action>
Create `src/components/resources/ResourceForm.tsx` (`'use client'`): RHF + `zodResolver(resourceSchema)`; defaultValues supply all fields (no `.default()` in schema). Fields: nome (Input), tipo (Select from RESOURCE_TYPES, pt-BR labels Sala/Cadeira/Equipamento), unit_id (Select of units), patrimonio (Input), numero_serie (Input), status (Select from RESOURCE_STATUS, labels Ativo/Manutenção/Inativo), manutencao_prevista (`<input type="date">`, optional). Submit → `createResource`/`updateResource`; Alert on error (pt-BR); router.push to list on success.
Create `src/app/(dashboard)/clinica/recursos/page.tsx` (RSC): resolve tenant via headers x-user-id; fetch `resources WHERE clinic_id = tenant AND deleted_at IS NULL`; PageHeader title="Recursos" + list/table (nome, tipo, patrimônio, status badge); EmptyState + "Novo recurso" CTA; hide CTA for read-only roles (x-read-only).
Create `src/app/(dashboard)/clinica/recursos/[id]/page.tsx` (RSC): fetch one resource; render `<ResourceForm>` for edit; `novo` sentinel for create mode.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx next build` clean.
    - Form uses RHF + zodResolver(resourceSchema); status Select includes manutencao; status badge uses tokens (no raw slate/gray).
    - Read-only roles see no mutation CTAs.
  </acceptance_criteria>
  <done>ResourceForm + list/edit pages render; build green; RES-01 cadastro complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → resources Server Actions | untrusted input; Zod-validated + admin-gated + tenant-scoped |
| read-only role → mutation action | assertNotReadOnly blocks at the action layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-26 | Elevation of Privilege | read-only role mutating resources | mitigate | assertNotReadOnly() + admin/superadmin role gate |
| T-11-27 | Spoofing | cross-tenant resource write | mitigate | clinic_id = actor.tenant_id on every query + RLS USING+WITH CHECK |
| T-11-28 | Input Validation | invalid tipo/status | mitigate | resourceSchema enum-validates tipo + status before storage |
</threat_model>

<verification>
- `npx vitest run src/__tests__/resources/` GREEN; `npx tsc --noEmit` clean; `npx next build` green.
- Existing suites unaffected.
</verification>

<success_criteria>
- Admin creates/edits a resource with status; 'manutencao' status is captured (booking-block gate in Plan 04 consumes it).
- Read-only roles cannot mutate; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-profissionais-recursos/11-07-SUMMARY.md`
</output>
