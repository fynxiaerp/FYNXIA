---
phase: 11-profissionais-recursos
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - src/actions/appointments.ts
  - src/actions/public-booking.ts
  - src/lib/validators/appointment.ts
  - src/proxy.ts
  - src/components/shell/nav-config.ts
  - src/components/shell/nav-icons.ts
autonomous: true
requirements: [PRO-02, RES-02]
tags: [scheduling, booking, agenda, proxy, navigation]

must_haves:
  truths:
    - "Internal createAppointment rejects a slot outside the professional's availability and rejects a resource in 'manutencao'"
    - "Public createPublicAppointment rejects out-of-availability slots; getBookedSlots reports out-of-availability slots as unavailable"
    - "/painel is registered as a public route in proxy.ts; profissionais + recursos appear in nav (admin-only)"
    - "The existing GIST double-booking path and 23P01 handling remain intact (regression-safe)"
  artifacts:
    - path: "src/actions/appointments.ts"
      provides: "availability + resource pre-flight guards before insert (GIST stays the backstop)"
      contains: "isSlotWithinAvailability"
    - path: "src/actions/public-booking.ts"
      provides: "availability gate in public booking + getAvailableSlots/getBookedSlots availability awareness"
      contains: "isSlotWithinAvailability"
    - path: "src/proxy.ts"
      provides: "/painel in isPublicRoute"
      contains: "/painel"
  key_links:
    - from: "src/actions/appointments.ts"
      to: "src/lib/scheduling/availability.ts + resources.ts"
      via: "import + pre-insert guard"
      pattern: "isSlotWithinAvailability|isResourceAvailable"
    - from: "src/actions/public-booking.ts"
      to: "professional availability rows"
      via: "fetch grade+exceptions, filter slots"
      pattern: "professional_availability"
---

<objective>
Wire the Wave 1 availability + resource logic into the existing booking paths WITHOUT rewriting the agenda or touching the GIST: (1) inject `isSlotWithinAvailability` + `isResourceAvailable` pre-flight guards into the internal `createAppointment` Server Action, (2) inject the availability gate into the public `createPublicAppointment` and make `getBookedSlots` (or a new `getAvailableSlots`) availability-aware so the public form stops offering out-of-hours slots, and (3) register the `profissionais`/`recursos` nav modules and add `/painel` to the public-route list in proxy.

Purpose: Deliver PRO-02 (availability gates the agenda) and RES-02 (maintenance blocks booking) as application-level pre-flight guards. The GIST remains the atomic backstop; a race that passes the soft check still hits 23P01 (already handled). Source-inspection-verifiable before the live db push (Plan 05).
Output: 5 edited files. Turns availability.test.ts + resources.test.ts booking source-inspection asserts GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-profissionais-recursos/11-RESEARCH.md
@src/actions/appointments.ts
@src/actions/public-booking.ts
@src/proxy.ts
@src/components/shell/nav-config.ts
@src/components/shell/nav-icons.ts

<interfaces>
<!-- Existing contracts the executor extends — already in the codebase. -->

From src/lib/scheduling/availability.ts (Plan 02):
```typescript
function isSlotWithinAvailability(grade: AvailabilityWindow[], exceptions: AvailabilityException[], slot: Slot): boolean
```
From src/lib/scheduling/resources.ts (Plan 03):
```typescript
function isResourceAvailable(status: string | null | undefined): boolean
```

Existing createAppointment (src/actions/appointments.ts): parses appointmentSchema, gets actor, role-gates, inserts with tenant_id/dentist_id, catches 23P01. The new guards go AFTER validation + actor resolution, BEFORE the insert. Resolve professional via `professionals WHERE user_id = dentist_id AND clinic_id = actor.tenant_id`. If a professional row exists, load its availability grade + exceptions and gate. If `input.resource_id` is present, load resource status + check overlap.

NOTE (RESEARCH Open Question 2 — intentional divergence): Phase 11 does NOT add an `appointments.professional_id` FK column. The professional↔appointment link is resolved at QUERY TIME via `professionals WHERE user_id = dentist_id` (above). `dentist_id` (FK→users.id) stays the single column in the sacred GIST; adding/migrating a `professional_id` FK is deferred to a future phase. This satisfies PRO-02 without referential-integrity coupling and keeps the GIST untouched.

Existing public-booking (src/actions/public-booking.ts): getBookedSlots(clinicSlug, dentistId, date) returns occupied start_time ISO strings using createAdminClient; createPublicAppointment validates publicBookingSchema then inserts via admin client. Both resolve the dentist via `users WHERE role='dentist'`. The professional row is resolved the same way (user_id = dentistId).

proxy.ts isPublicRoute currently lists /invite, /agendar, /anamnese. ROUTE_MODULE_MAP: /clinica/profissionais and /clinica/recursos resolve to module 'clinica' automatically (prefix match) — NO change to ROUTE_MODULE_MAP needed.

nav-config.ts: NavIconKey is a string-union; ALL_NAV_ITEMS entries have {href,label,icon,adminOnly?}. nav-icons.ts maps keys → Lucide components (client-only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Availability + resource guards in internal + public booking actions</name>
  <files>src/actions/appointments.ts, src/actions/public-booking.ts</files>
  <read_first>
    - src/actions/appointments.ts (createAppointment structure, getActor, 23P01 handling — insert guards without altering the insert or the 23P01 branch)
    - src/actions/public-booking.ts (getBookedSlots + createPublicAppointment admin-client flow)
    - src/lib/scheduling/availability.ts + src/lib/scheduling/resources.ts (Wave 1 pure fns)
    - .planning/phases/11-profissionais-recursos/11-RESEARCH.md §Code Examples (createAppointment extension) + §Pitfall 2 (race acceptable) + §Pitfall 5 (slots must reflect availability)
  </read_first>
  <action>
Edit `src/actions/appointments.ts` — inside `createAppointment`, AFTER `getActor()` + role gate, BEFORE the `.insert`:
  - Add `resource_id` to the destructured `parsed.data` (extend `appointmentSchema`/`AppointmentInput` if needed to accept an optional `resource_id: z.string().uuid().optional()` — edit src/lib/validators/appointment.ts to add the optional field; keep it optional so existing callers are unaffected).
  - Resolve professional: `supabase.from('professionals').select('id').eq('user_id', dentist_id).eq('clinic_id', actor.tenant_id).is('deleted_at', null).maybeSingle()`.
  - If a professional row exists: fetch its availability grade (`professional_availability` rows: weekday/start_time/end_time) + exceptions (`professional_availability_exceptions`) for the slot's date, then call `isSlotWithinAvailability(grade, exceptions, { start: start_time, end: end_time })`. If false → `return { success: false, error: 'Horário fora da disponibilidade do profissional.' }`. (PRO-02)
  - If `resource_id` provided: fetch `resources.status` for that id scoped to `actor.tenant_id`; if `!isResourceAvailable(status)` → `return { success: false, error: 'Recurso em manutenção ou indisponível.' }`. Then run an app-level overlap check: any non-cancelled appointment with the same `resource_id` overlapping [start,end) → `return { success: false, error: 'Recurso já reservado neste horário.' }`. (RES-02)
  - Include `resource_id` in the insert payload (nullable). Do NOT modify the 23P01 branch or the GIST-dependent insert structure.
  - Mirror the same availability guard into `updateAppointment` when start_time/end_time/dentist_id change (resolve professional from the new/effective dentist_id).

Edit `src/actions/public-booking.ts`:
  - In `createPublicAppointment`, AFTER the dentist verification, BEFORE the insert: resolve professional via `professionals WHERE user_id = data.dentist_id AND clinic_id = clinic.id`; if present, fetch grade + exceptions and call `isSlotWithinAvailability`; if false → `return { success: false, error: 'Horário fora da disponibilidade do profissional.' }`. (PRO-02)
  - Make slot offering availability-aware (Pitfall 5): add an exported `getAvailableSlots(clinicSlug, dentistId, date)` that returns the set of valid 30-min slot start_times within the professional's availability for that date (or, simpler and matching the test: extend `getBookedSlots` to ALSO return out-of-availability slot starts so the form disables them). Reuse the admin client; resolve the professional the same way. Keep returning `[]` on any error (never leak details — same as existing).
  - Import the pure fns from `@/lib/scheduling/availability`. Do NOT change the 23P01 friendly-error branch.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/professionals/availability.test.ts src/__tests__/resources/resources.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - availability.test.ts source-inspection asserts (createAppointment + public-booking reference isSlotWithinAvailability + a disponibilidade rejection) GREEN.
    - resources.test.ts source-inspection asserts (createAppointment references resource status check + manutenção message) GREEN.
    - 23P01 branches unchanged; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both actions gate on availability + resource; resource_id flows into the insert; 23P01/GIST path intact; tsc clean; target asserts GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: /painel public route + profissionais/recursos nav modules</name>
  <files>src/proxy.ts, src/components/shell/nav-config.ts, src/components/shell/nav-icons.ts</files>
  <read_first>
    - src/proxy.ts (isPublicRoute list — add /painel only; ROUTE_MODULE_MAP needs NO change)
    - src/components/shell/nav-config.ts (NavIconKey union + ALL_NAV_ITEMS + adminOnly)
    - src/components/shell/nav-icons.ts (client-only Lucide map)
    - .planning/phases/11-profissionais-recursos/11-RESEARCH.md §Pattern 7 (module registration + public route)
  </read_first>
  <action>
Edit `src/proxy.ts`:
  - Add `|| pathname.startsWith('/painel')` to the `isPublicRoute` expression (TV panel has no auth — RES-03). Add a comment: `/painel: public TV waiting-room display (tenant-isolated by slug, initials-only — LGPD)`.
  - Do NOT add `/painel` to ROUTE_MODULE_MAP (it is fully public; routeToModule returning null is correct for a public route).

Edit `src/components/shell/nav-config.ts`:
  - Extend `NavIconKey` union with `'profissionais'` and `'recursos'`.
  - Add to `ALL_NAV_ITEMS` (after Equipe): `{ href: '/clinica/profissionais', label: 'Profissionais', icon: 'profissionais', adminOnly: true }` and `{ href: '/clinica/recursos', label: 'Recursos', icon: 'recursos', adminOnly: true }`.

Edit `src/components/shell/nav-icons.ts`:
  - Import `Stethoscope` and `Armchair` from `lucide-react`; add `profissionais: Stethoscope, recursos: Armchair` to `NAV_ICONS`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/resources/waiting-room.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - waiting-room.test.ts `/painel in isPublicRoute` assertion GREEN.
    - nav-config NavIconKey includes profissionais + recursos; NAV_ICONS maps both keys; `npx tsc --noEmit` exits 0.
    - Existing proxy + rbac tests still green (no RBAC matrix change — both routes resolve to 'clinica', already admin-accessible).
  </acceptance_criteria>
  <done>/painel public; profissionais + recursos nav entries (admin-only) with icons; tsc clean; proxy/rbac suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public booking payload → createPublicAppointment | untrusted; availability gate is additive to existing strict Zod + dentist-belongs-to-clinic check |
| anon → /painel | public route; must stay tenant-isolated + initials-only (enforced in Plan 08) |
| booking guard vs GIST | soft pre-flight; GIST is the atomic backstop for double-booking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Tampering | booking outside availability to overbook | mitigate | isSlotWithinAvailability pre-flight in BOTH internal + public actions; GIST backstop for the race (Pitfall 2) |
| T-11-15 | Tampering | booking a resource in maintenance | mitigate | isResourceAvailable check + app-level overlap check before insert |
| T-11-16 | Information Disclosure | /painel reachable but un-isolated | mitigate | added to isPublicRoute here; tenant-isolation + initials enforced in Plan 08 panel page + RLS |
| T-11-17 | Spoofing | foreign dentist_id in public booking | accept | existing dentist-belongs-to-clinic check retained unchanged |
</threat_model>

<verification>
- `npx vitest run src/__tests__/professionals/ src/__tests__/resources/` GREEN for booking + nav + public-route asserts.
- `npx tsc --noEmit` clean; `npx vitest run src/__tests__/proxy/ src/__tests__/rbac/ src/__tests__/agenda/ src/__tests__/actions/` still green (no regression).
- 23P01 friendly-error branches in both actions byte-identical to before (regression-safe).
</verification>

<success_criteria>
- Availability + resource guards live in internal + public booking; getBookedSlots/getAvailableSlots availability-aware.
- /painel public; profissionais + recursos nav modules registered.
- GIST + 23P01 path untouched; tsc green; existing suites green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-profissionais-recursos/11-04-SUMMARY.md`
</output>
