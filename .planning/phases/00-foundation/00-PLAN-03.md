---
phase: 00-foundation
plan: 03
type: execute
wave: 3
depends_on: ["00-foundation/01", "00-foundation/02"]
files_modified:
  - src/types/database.types.ts
  - supabase/tests/rls-checks.sql
  - .planning/phases/00-foundation/VERIFY.md
autonomous: false
requirements: [INFRA-02, INFRA-04, INFRA-06, INFRA-07]
user_setup:
  - service: supabase
    why: "Schema push needs a linked project; Custom Access Token Hook registration is dashboard-only; region (sa-east-1) and Pro plan must be confirmed"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (enables non-interactive supabase db push)"
    dashboard_config:
      - task: "Confirm project region is South America (Sao Paulo) sa-east-1"
        location: "Supabase Dashboard -> Project Settings -> General -> Region"
      - task: "Confirm Supabase Pro plan is active (required for Auth Hooks)"
        location: "Supabase Dashboard -> Project Settings -> Billing"
      - task: "Register Custom Access Token Hook -> public.custom_access_token_hook"
        location: "Supabase Dashboard -> Authentication -> Hooks -> Custom Access Token"

must_haves:
  truths:
    - "All three migrations are pushed to the linked Supabase project (schema is live)"
    - "Querying public.users as an authenticated role returns rows, NOT a stack-depth recursion error (C-1 closed live)"
    - "A JWT issued after login contains tenant_id and user_role claims (INFRA-07 verified live)"
    - "Generated database types reflect tenants, users, audit_logs"
    - "Supabase project is confirmed in sa-east-1 (INFRA-02)"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "Generated TypeScript types from the live schema"
      contains: "tenants"
    - path: "supabase/tests/rls-checks.sql"
      provides: "Repeatable SQL script to verify RLS recursion-free + tenant isolation"
      contains: "get_my_tenant_id"
    - path: ".planning/phases/00-foundation/VERIFY.md"
      provides: "Manual verification checklist (region, Pro plan, hook registration, JWT decode)"
      contains: "jwt"
  key_links:
    - from: "supabase db push"
      to: "linked Supabase project (sa-east-1)"
      via: "Supabase CLI + SUPABASE_ACCESS_TOKEN"
      pattern: "db push"
    - from: "src/types/database.types.ts"
      to: "live schema"
      via: "supabase gen types typescript --linked"
      pattern: "gen types"
---

<objective>
Push the three Phase 0 migrations to the linked Supabase project, generate TypeScript types from the live schema, create a repeatable RLS verification SQL script, and run the manual verifications that cannot be automated (region confirmation, Pro plan, Custom Access Token Hook registration, and live JWT claim inspection).

Purpose: Migrations that are written but not pushed produce a false-positive verification state — `tsc`/`build` pass from config types, not the live database. This plan is the BLOCKING gate that makes the schema real and proves the security invariants (C-1 recursion-free, INFRA-07 JWT claims) on the actual project before any feature phase begins.

Output: Live schema in Supabase, generated types, an RLS test script, and a completed manual verification checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/research/PITFALLS.md
@.planning/phases/00-foundation/00-VALIDATION.md

<interfaces>
<!-- Consumes the migrations authored in Plan 02 -->
supabase/migrations/20260603000000_initial_schema.sql
supabase/migrations/20260603000100_rls_policies.sql
supabase/migrations/20260603000200_custom_access_token_hook.sql
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 1: Confirm Supabase region + Pro plan and link the CLI</name>
  <action>Pause for the user to: (1) confirm the Supabase project region is sa-east-1 (immutable — INFRA-02; if wrong, a new project must be created), (2) confirm the Pro plan is active (Auth Hooks unavailable on Free), (3) set SUPABASE_ACCESS_TOKEN for non-interactive push, (4) link the CLI with `npx supabase link --project-ref <ref>`. Do NOT proceed to Task 1 (the push) until the resume signal is received.</action>
  <what-built>Plans 01-02 produced the project scaffold and three SQL migrations. Before pushing, the Supabase project must be confirmed as sa-east-1 (region is immutable after creation) and on the Pro plan (Auth Hooks require Pro), and the CLI must be linked.</what-built>
  <how-to-verify>
    1. In the Supabase Dashboard, open Project Settings -> General -> Region. Confirm it reads "South America (Sao Paulo)" / sa-east-1 (INFRA-02). If it does NOT, STOP — a new project in sa-east-1 must be created (region cannot be changed).
    2. Project Settings -> Billing: confirm the Pro plan is active (Auth Hooks are unavailable on Free).
    3. Generate a Supabase access token (Account -> Access Tokens) and provide it as `SUPABASE_ACCESS_TOKEN` so the push runs non-interactively.
    4. Provide the project ref (Project Settings -> General -> Reference ID) so the CLI can link: `npx supabase link --project-ref <ref>`.
  </how-to-verify>
  <verify>Manual: region=sa-east-1 confirmed, Pro plan active, SUPABASE_ACCESS_TOKEN set, project linked.</verify>
  <done>User confirms region, Pro plan, token, and link are in place via the resume signal.</done>
  <resume-signal>Type "linked" once region=sa-east-1 is confirmed, Pro plan is active, SUPABASE_ACCESS_TOKEN is set, and the project is linked. Provide the project ref.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: [BLOCKING] Push migrations to Supabase and generate database types</name>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md (Pattern 9 migration workflow lines 636-659)
    - supabase/migrations/20260603000000_initial_schema.sql
    - supabase/migrations/20260603000100_rls_policies.sql
    - supabase/migrations/20260603000200_custom_access_token_hook.sql
  </read_first>
  <action>
    This task is MANDATORY and BLOCKING — the phase cannot pass verification without it (build/type checks pass from config, not the live DB, so the push is the only thing that makes the schema real).

    1. Confirm the project is linked (from the preceding checkpoint). With `SUPABASE_ACCESS_TOKEN` exported, run non-interactively: `npx supabase db push`.
       - If push reports the migration order is wrong, ensure files apply in timestamp order: 000000 (schema+function) -> 000100 (RLS) -> 000200 (hook). The schema migration MUST apply before RLS (RLS references the tables) and before the hook (hook reads users).
       - If `db push` requires an interactive confirmation that cannot be suppressed, halt and flag for manual run (`npx supabase db push` in the user's terminal) — this plan is already `autonomous: false`.
    2. After a successful push, generate types: `npx supabase gen types typescript --linked > src/types/database.types.ts`.
    3. Verify the generated file references the three tables.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='src/types/database.types.ts'; if(!fs.existsSync(p)){console.error('FAIL: types not generated'); process.exit(1)} const s=fs.readFileSync(p,'utf8'); for(const t of ['tenants','users','audit_logs']){ if(!s.includes(t)){console.error('FAIL: missing table '+t+' in generated types'); process.exit(1)} } console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completed without error (migrations applied in 000000 -> 000100 -> 000200 order)
    - File `src/types/database.types.ts` exists
    - Generated types contain `tenants`, `users`, and `audit_logs`
    - `npx tsc --noEmit` exits 0 with the generated types in place
    - Verify node script prints `OK`
  </acceptance_criteria>
  <done>All three migrations live on the linked Supabase project; database types generated and compiling.</done>
</task>

<task type="auto">
  <name>Task 2: Create repeatable RLS verification SQL script and the manual VERIFY.md checklist</name>
  <read_first>
    - .planning/phases/00-foundation/00-VALIDATION.md (Manual-Only Verifications table lines 70-75; Phase Requirements to Test Map)
    - .planning/phases/00-foundation/00-RESEARCH.md (Validation Architecture lines 924-962; Verifying JWT Claims lines 830-852)
    - .planning/research/PITFALLS.md (C-1 detection lines 57-59; Phase 0 must-complete checklist lines 799-806)
  </read_first>
  <action>
    1. Create `supabase/tests/rls-checks.sql` — a runnable script (Supabase SQL Editor) that proves the security invariants. Include, with explanatory comments:
       - C-1 recursion test: a `SET LOCAL ROLE authenticated;` block selecting `SELECT * FROM public.users LIMIT 1;` documented to return rows (or empty), NOT `stack depth limit exceeded`.
       - Function presence: `SELECT proname, prosecdef FROM pg_proc WHERE proname IN ('get_my_tenant_id','custom_access_token_hook');` (prosecdef must be true — SECURITY DEFINER).
       - Policy inspection: `SELECT tablename, policyname, qual, with_check FROM pg_policies WHERE schemaname='public' ORDER BY tablename;` with a comment that no `qual`/`with_check` may contain `FROM users` (C-1).
       - TIMESTAMPTZ check (SEC-07): `SELECT table_name, column_name FROM information_schema.columns WHERE data_type='timestamp without time zone' AND table_schema='public';` documented to return 0 rows.
       - RLS-enabled check: `SELECT relname FROM pg_class WHERE relrowsecurity=true AND relnamespace='public'::regnamespace;` must list tenants, users, audit_logs.
    2. Create `.planning/phases/00-foundation/VERIFY.md` — the manual verification checklist (mirrors 00-VALIDATION.md Manual-Only table) with explicit, numbered steps and checkboxes for:
       - [ ] Supabase region = sa-east-1 (INFRA-02)
       - [ ] Supabase Pro plan active
       - [ ] Custom Access Token Hook registered & enabled in dashboard (INFRA-07)
       - [ ] After test login, decode the JWT at jwt.io and confirm `tenant_id` and `user_role` claims are present (INFRA-07) — note: claims are read via `user.app_metadata` in the JS client per RESEARCH.md A2
       - [ ] Run `supabase/tests/rls-checks.sql` in SQL Editor; record results (no recursion, prosecdef=true, 0 bare-timestamp columns, RLS on all 3 tables)
       - [ ] After `npm run build`, run the post-build secret check and record CLEAN (C-2): `grep -r "service_role" .next/static/` returns nothing
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r='supabase/tests/rls-checks.sql'; const v='.planning/phases/00-foundation/VERIFY.md'; if(!fs.existsSync(r)){console.error('FAIL: rls-checks.sql missing'); process.exit(1)} if(!fs.existsSync(v)){console.error('FAIL: VERIFY.md missing'); process.exit(1)} const rs=fs.readFileSync(r,'utf8'); if(!/get_my_tenant_id/.test(rs)||!/pg_policies/.test(rs)){console.error('FAIL: rls-checks.sql incomplete'); process.exit(1)} const vs=fs.readFileSync(v,'utf8').toLowerCase(); for(const t of ['jwt','sa-east-1','hook']){ if(!vs.includes(t)){console.error('FAIL: VERIFY.md missing '+t); process.exit(1)} } console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/tests/rls-checks.sql` exists and contains `get_my_tenant_id` AND `pg_policies` AND `information_schema.columns`
    - File `.planning/phases/00-foundation/VERIFY.md` exists
    - VERIFY.md contains (case-insensitive) `jwt`, `sa-east-1`, and `hook`
    - VERIFY.md has a checklist item for the post-build `.next/static/` secret check (C-2)
    - Verify node script prints `OK`
  </acceptance_criteria>
  <done>RLS test script and manual verification checklist created, covering C-1, SEC-07, INFRA-02, INFRA-07, and C-2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: Register auth hook, inspect live JWT, run RLS + build-secret checks</name>
  <action>Pause for the user to register the Custom Access Token Hook in the dashboard, obtain and decode a real JWT to confirm tenant_id + user_role claims, run `supabase/tests/rls-checks.sql`, and run the post-build `.next/static/` secret check. Collect results into VERIFY.md before resuming.</action>
  <what-built>Schema is pushed live, types generated, RLS test script and VERIFY.md created. The remaining checks require a human: registering the auth hook and inspecting a real JWT.</what-built>
  <how-to-verify>
    1. Supabase Dashboard -> Authentication -> Hooks -> Custom Access Token: select `public.custom_access_token_hook`, enable it (INFRA-07).
    2. Create a test user (Authentication -> Users -> Add user) and insert a matching `public.users` row with a `tenant_id` (create a `public.tenants` row first) and a `role`. Sign in as that user (or use the SQL Editor / a quick script) to obtain an access token.
    3. Decode the access token at jwt.io and confirm `tenant_id` and `user_role` appear (under `app_metadata` per RESEARCH.md A2).
    4. Open the Supabase SQL Editor and run `supabase/tests/rls-checks.sql`. Confirm: no "stack depth limit exceeded" (C-1), `prosecdef=true` for both functions, 0 rows of bare-timestamp columns (SEC-07), and RLS enabled on tenants/users/audit_logs.
    5. Run `npm run build`, then `grep -r "service_role" .next/static/` and confirm it returns nothing (C-2).
    6. Tick every box in `.planning/phases/00-foundation/VERIFY.md` and record the JWT decode result.
  </how-to-verify>
  <verify>Manual: hook registered, JWT shows tenant_id + user_role, rls-checks.sql passes (no recursion), build output free of service_role key.</verify>
  <done>User confirms via the resume signal that all manual checks pass and VERIFY.md is fully ticked.</done>
  <resume-signal>Type "verified" once the hook is registered, the JWT shows tenant_id + user_role, the RLS script passes (no recursion), and the build output is free of the service_role key. Report any failures instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → Supabase project | Migration push uses an access token; applies privileged DDL |
| Supabase Auth → issued JWT | Live confirmation that the hook injects the correct, non-spoofable claims |
| build output → public web | Confirms no secret crosses into the static bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-12 | Denial of Service (C-1) | live users RLS | mitigate | rls-checks.sql executes an authenticated `SELECT FROM public.users` and asserts no stack-depth recursion — proves get_my_tenant_id() resolves the policy without looping. |
| T-00-13 | Spoofing / EoP (INFRA-07, C-5) | live JWT claims | mitigate | Manual JWT decode confirms tenant_id + user_role originate from the hook (public.users), establishing the trusted tenant context all RLS depends on. |
| T-00-14 | Information Disclosure (C-2) | .next/static build output | mitigate | Post-build grep for `service_role` in `.next/static/` must return nothing before sign-off. |
| T-00-15 | Tampering | migration apply order | accept | Timestamped filenames enforce 000000 -> 000100 -> 000200 ordering; push is idempotent and re-runnable; low risk on a fresh project. |
| T-00-16 | Information Disclosure (INFRA-02/LGPD residency) | data region | mitigate | Region confirmed = sa-east-1 before any data is written; blocking checkpoint halts if wrong. |
</threat_model>

<verification>
- `supabase db push` applied all three migrations (INFRA-04 live)
- `src/types/database.types.ts` exists and references tenants/users/audit_logs
- `supabase/tests/rls-checks.sql` runs in SQL Editor with: no recursion (C-1/INFRA-06), prosecdef=true for both functions, 0 bare-timestamp columns (SEC-07), RLS on all 3 tables
- Decoded JWT shows tenant_id + user_role (INFRA-07)
- Supabase region confirmed sa-east-1 (INFRA-02)
- `.next/static/` contains no service_role key (C-2)
</verification>

<success_criteria>
- Schema is live on the linked sa-east-1 project (INFRA-02, INFRA-04)
- RLS verified recursion-free against the live database (INFRA-06, C-1 closed)
- JWT carries tenant_id + user_role via the registered hook (INFRA-07)
- Generated types compile; manual VERIFY.md checklist completed and recorded
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-03-SUMMARY.md` documenting: the push result, the project ref/region confirmation, the exact JWT claim path observed (resolving assumption A2), the rls-checks.sql results, the C-2 build-output result, and confirmation that all 6 critical pitfalls (C-1..C-6) are resolved at phase end.
</output>
