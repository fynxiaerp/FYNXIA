---
phase: 12-receitu-rio-teleodontologia
plan: 07
type: execute
wave: 4
depends_on: [05]
files_modified:
  - src/components/teleconsultation/TeleconsultationForm.tsx
  - src/components/teleconsultation/SoapEditor.tsx
  - src/app/(dashboard)/clinica/teleodontologia/page.tsx
  - src/app/(dashboard)/clinica/teleodontologia/[id]/page.tsx
autonomous: true
requirements: [TEL-01, TEL-02]
tags: [teleodontologia, ui, forms, soap, consent, prontuario]

must_haves:
  truths:
    - "Dentist creates a teleconsultation with CFO consent capture + external meeting link; starting requires consent; start/end set the session timestamps"
    - "Dentist writes a SOAP record (S/O/A/P) from the teleconsultation that links teleconsultation_id + appointment_id (vinculado ao atendimento + prontuário)"
    - "Read-only roles see no mutation CTAs"
  artifacts:
    - path: "src/components/teleconsultation/TeleconsultationForm.tsx"
      provides: "RHF+Zod consent + external link + start/end controls"
    - path: "src/components/teleconsultation/SoapEditor.tsx"
      provides: "RHF+Zod SOAP S/O/A/P editor"
    - path: "src/app/(dashboard)/clinica/teleodontologia/page.tsx"
      provides: "teleconsultations list (RSC) + create CTA"
  key_links:
    - from: "src/components/teleconsultation/TeleconsultationForm.tsx"
      to: "src/actions/teleconsultations.ts"
      via: "form submit → createTeleconsultation / start / end / createSoapRecord"
      pattern: "createTeleconsultation|createSoapRecord"
---

<objective>
Build the teleodontologia UI (TEL-01/02): (1) a TeleconsultationForm capturing the CFO session consent (checkbox + explanatory text) + the external meeting link (Meet/Zoom/Jitsi) and the iniciar/encerrar controls (start requires consent), and (2) a SoapEditor (S/O/A/P fields) that creates a `soap_records` row linked to the teleconsultation + the atendimento, plus the list + session RSC pages under `/clinica/teleodontologia`. Module registration (proxy + nav) is done in Plan 06 — this plan only adds its own components + pages (no shared-file overlap, so it runs parallel to Plan 06).

Purpose: Deliver the teleconsultation session workflow (external link + consent registered, D-03) and the SOAP record into the prontuário timeline (TEL-02), reusing the Plan 04 Server Actions. Design-system v1 (PageHeader, tokens, @base-ui Button render-prop NEVER asChild, RHF+Zod v3 no .default(), pt-BR).
Output: 2 components + 2 pages. Self-contained UI; no overlap with Plan 06's files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-receitu-rio-teleodontologia/12-RESEARCH.md
@src/lib/validators/teleconsultation.ts
@src/app/(dashboard)/clinica/documentos/page.tsx
@src/lib/auth/guards.ts

<interfaces>
<!-- Contracts the executor uses. -->

From src/actions/teleconsultations.ts (Plan 04):
```typescript
createTeleconsultation(input: TeleconsultationInput): Promise<{ success; id?; error? }>
startTeleconsultation(id: string): Promise<{ success; error? }>   // requires consent_given
endTeleconsultation(id: string): Promise<{ success; error? }>
createSoapRecord(input: SoapInput): Promise<{ success; id?; error? }>
listTeleconsultations(patientId?: string): Promise<{ success; data?; error? }>
```
From src/lib/validators/teleconsultation.ts (Plan 03): `teleconsultationSchema`, `TeleconsultationInput`, `soapSchema`, `SoapInput`.

teleconsultations row shape (post-push): clinic_id, patient_id, appointment_id?, professional_id?, external_link, consent_given, consent_given_at, status ('agendada'|'em_andamento'|'concluida'|'cancelada'), started_at, ended_at.
soap_records row shape: patient_id, appointment_id?, teleconsultation_id?, soap_subjective, soap_objective, soap_assessment, soap_plan.

UI primitives: shadcn Select/Input/Label/Textarea/Checkbox/Alert/Button under src/components/ui/*; Button is @base-ui (render-prop, NEVER asChild). Tokens only. RSC page pattern from documentos/page.tsx: headers() x-user-id → tenant; PageHeader + content; read x-read-only to hide mutation CTAs.

NOTE: consent_ip + consent_given_at are set SERVER-SIDE in createTeleconsultation (Plan 04) — the form only sends consent_given (boolean) + external_link. Do NOT collect or send an IP from the client.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TeleconsultationForm + SoapEditor components</name>
  <files>src/components/teleconsultation/TeleconsultationForm.tsx, src/components/teleconsultation/SoapEditor.tsx</files>
  <read_first>
    - src/lib/validators/teleconsultation.ts (teleconsultationSchema + soapSchema — form shapes)
    - src/actions/teleconsultations.ts (create/start/end/createSoapRecord signatures)
    - src/components/agenda/AgendaCalendar.tsx (client component + shadcn Select/Dialog/Button usage + tokens reference)
    - .planning/phases/12-receitu-rio-teleodontologia/12-RESEARCH.md §Pattern 4 + §Open Question 2 (consent = checkbox + server-side IP/time)
  </read_first>
  <action>
Create `src/components/teleconsultation/TeleconsultationForm.tsx` (`'use client'`): RHF + `zodResolver(teleconsultationSchema)`; defaultValues supply all fields (NO `.default()`). Fields:
  - patient selector (Select of clinic patients passed via props), optional appointment link (Select), optional professional.
  - `external_link` (Input type=url) with helper text "Cole o link da reunião (Google Meet, Zoom, Jitsi). O FYNXIA não hospeda o vídeo."
  - `consent_given` checkbox with the CFO consent text: "O paciente foi informado e consente com a realização da teleconsulta (Resolução CFO 226/2020). O consentimento e o horário são registrados." Submit calls `createTeleconsultation`; on success, surface the session controls.
  - Session controls (when a session exists): "Iniciar teleconsulta" button → `startTeleconsultation(id)` (disabled / shows error if consent not given), "Encerrar" → `endTeleconsultation(id)`; show current `status` + started_at/ended_at. @base-ui Button render-prop, NEVER asChild. shadcn Alert on errors (pt-BR).

Create `src/components/teleconsultation/SoapEditor.tsx` (`'use client'`): RHF + `zodResolver(soapSchema)`; props include `patientId`, optional `teleconsultationId`, optional `appointmentId` (passed from the session page so the SOAP record links to both). Four textareas: Subjetivo (soap_subjective), Objetivo (soap_objective), Avaliação (soap_assessment), Plano (soap_plan), each with a pt-BR label + short helper. Submit calls `createSoapRecord({ patient_id, teleconsultation_id, appointment_id, soap_* })`; on success show a confirmation and that the registro foi vinculado ao prontuário + atendimento. Errors via shadcn Alert.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both components compile; TeleconsultationForm uses zodResolver(teleconsultationSchema) + external_link url field + consent_given checkbox + start/end controls (start gated on consent); SoapEditor uses zodResolver(soapSchema) + 4 SOAP fields + links teleconsultation_id + appointment_id.
    - @base-ui Button render-prop (no asChild); tokens only; NO client-side IP collection.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>TeleconsultationForm + SoapEditor render; consent + external link + start/end + SOAP S/O/A/P linked to session + atendimento; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: teleodontologia list + session RSC pages</name>
  <files>src/app/(dashboard)/clinica/teleodontologia/page.tsx, src/app/(dashboard)/clinica/teleodontologia/[id]/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/documentos/page.tsx (RSC tenant resolution + PageHeader + list + x-read-only gating)
    - src/components/teleconsultation/TeleconsultationForm.tsx + SoapEditor.tsx (the components rendered here)
    - src/actions/teleconsultations.ts (listTeleconsultations)
  </read_first>
  <action>
Create `src/app/(dashboard)/clinica/teleodontologia/page.tsx` (RSC): resolve tenant via headers() x-user-id; fetch teleconsultations (via listTeleconsultations or a tenant-scoped query) ordered created_at desc; also fetch the clinic patients list (+ appointments for linking) to pass to the form. Render `PageHeader title="Teleodontologia"` + a table (paciente, status, consentimento sim/não, início/fim, data) with EmptyState + "Nova teleconsulta" CTA → `[id]/page.tsx?novo`. Read x-read-only to hide the create CTA for read-only roles.

Create `src/app/(dashboard)/clinica/teleodontologia/[id]/page.tsx` (RSC): when id is `novo`, render `<TeleconsultationForm>` in create mode (patients/appointments props). When id is a real session, fetch it (tenant-scoped) + render the session detail (link, consent status, start/end controls via the form bound to the existing session) AND a `<SoapEditor patientId teleconsultationId={id} appointmentId={session.appointment_id ?? undefined} />` so the dentist writes the SOAP for that session (TEL-02 — linked to session + atendimento). Read x-read-only to hide controls for read-only roles.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx next build` completes clean (no RSC function-across-boundary error).
    - List page renders teleconsultations + create CTA (x-read-only respected); session page renders the form + SoapEditor with teleconsultation_id + appointment_id wired.
    - Start control gated on consent (UI mirrors the action guard).
  </acceptance_criteria>
  <done>teleodontologia list + session pages render; create + start/end + SOAP linked; build green; TEL-01/02 UI complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → teleconsultation actions | untrusted; Zod-validated + dentist/admin-gated + tenant-scoped (Plan 04); consent_ip/time set server-side |
| read-only role → create/start/SOAP CTA | x-read-only hides CTAs; assertNotReadOnly blocks at the action layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-30 | Repudiation | consent captured client-side only | mitigate | form sends only consent_given boolean; consent_ip + consent_given_at are server-set in createTeleconsultation (Plan 04); UI collects no IP |
| T-12-31 | Elevation of Privilege | non-clinical role authoring SOAP/session | mitigate | teleodontologia module gated (Plan 06 MODULE_PERMISSIONS) + assertNotReadOnly + role gate in the action |
| T-12-32 | Tampering | starting a session without consent | mitigate | start button disabled when consent not given; startTeleconsultation action also returns an error if consent_given is false |
| T-12-33 | Information Disclosure | SOAP clinical notes cross-tenant | mitigate | list/read pages tenant-scoped; soap_records RLS clinic_id = get_my_tenant_id() (Plan 03) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx next build` green.
- `npx vitest run src/__tests__/teleodontologia/` green; existing suites unaffected.
</verification>

<success_criteria>
- Dentist creates a teleconsultation (consent + external link), starts (consent-gated) + ends it, and writes a SOAP record linked to the session + atendimento.
- Read-only roles blocked; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/12-receitu-rio-teleodontologia/12-07-SUMMARY.md`
</output>
</content>
