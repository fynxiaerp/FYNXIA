---
phase: 12-receitu-rio-teleodontologia
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - src/components/pdf/ReceituarioPDF.tsx
  - src/components/pdf/AtestadoPDF.tsx
  - src/components/pdf/ExamePDF.tsx
  - src/actions/clinical-documents.ts
  - src/actions/teleconsultations.ts
autonomous: true
requirements: [RX-01, RX-02, RX-03, TEL-01, TEL-02]
tags: [receituario, teleodontologia, server-actions, pdf, signing, allergy, soap]

must_haves:
  truths:
    - "issueClinicDocument validates Zod, decrypts patient allergies + reads latest anamnese, runs checkMedicationAllergy, generates atomic doc_number via next_doc_number RPC, and inserts an encrypted-content draft"
    - "signClinicDocument renders the typed PDF, reuses Phase 8 signPdfBuffer, uploads to clinical-documents-pdf, and flips draft→signed under a .is('signature', null) race guard (immutable)"
    - "createTeleconsultation records external_link + consent with server-set consent_ip + consent_given_at; start/endTeleconsultation set timestamps + status; createSoapRecord links teleconsultation_id + appointment_id"
    - "Every mutation calls assertNotReadOnly + role gate + logBusinessEvent + tenant scope; the Phase 8 signing engine is imported, never modified"
  artifacts:
    - path: "src/actions/clinical-documents.ts"
      provides: "issueClinicDocument + signClinicDocument + listClinicDocuments"
      exports: ["issueClinicDocument", "signClinicDocument"]
    - path: "src/actions/teleconsultations.ts"
      provides: "createTeleconsultation + startTeleconsultation + endTeleconsultation + createSoapRecord + listTeleconsultations"
      exports: ["createTeleconsultation", "createSoapRecord"]
    - path: "src/components/pdf/ReceituarioPDF.tsx"
      provides: "receita simples + controle especial PDF (Flexbox only)"
  key_links:
    - from: "src/actions/clinical-documents.ts"
      to: "src/lib/icp/sign-document.ts + src/lib/clinical/allergy-check.ts + next_doc_number"
      via: "import signPdfBuffer + checkMedicationAllergy + supabase.rpc"
      pattern: "signPdfBuffer|checkMedicationAllergy|next_doc_number"
    - from: "src/actions/teleconsultations.ts"
      to: "teleconsultations + soap_records"
      via: "insert with server-set consent_ip + SOAP links"
      pattern: "consent_ip|teleconsultation_id"
---

<objective>
Build the receituário + teleodontologia Server Action + PDF layer (RX-01/02/03 + TEL-01/02): (1) three `@react-pdf/renderer` components (ReceituarioPDF for receita simples + controle especial, AtestadoPDF, ExamePDF) following the Phase 8 DocumentoPDF pattern (Flexbox only, Roboto pt-BR font, deterministic `generatedAt`); (2) `clinical-documents.ts` with `issueClinicDocument` (validate → decrypt allergies + read anamnese → checkMedicationAllergy → next_doc_number → insert encrypted draft) and `signClinicDocument` (render typed PDF → REUSE Phase 8 `signPdfBuffer` → upload to `clinical-documents-pdf` → atomic `.is('signature', null)` flip to signed, immutable); (3) `teleconsultations.ts` with `createTeleconsultation` (CFO consent + external link + server-side `consent_ip`/`consent_given_at`), `start`/`endTeleconsultation`, and `createSoapRecord` (links `teleconsultation_id` + `appointment_id`, writes SOAP into the prontuário).

Purpose: Deliver the full emission + signing + teleconsultation backend so the Wave 4 UIs (Plans 06/07) just render forms over these actions. The allergy decrypt happens HERE (server-side) and feeds plaintext to the pure check; signing reuses the verified Phase 8 engine without modifying it; signed clinical docs are immutable via the same race guard as DOC-03.
Output: 3 PDF components + 2 Server Action files. Turns clinical-documents.test.ts + teleconsultations.test.ts source-inspection asserts GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-receitu-rio-teleodontologia/12-RESEARCH.md
@src/actions/documents.ts
@src/lib/icp/sign-document.ts
@src/lib/documents/document-types.ts
@src/components/pdf/DocumentoPDF.tsx
@src/lib/crypto.ts
@src/lib/validators/anamnesis.ts

<interfaces>
<!-- Contracts the executor uses. All verified from the codebase. -->

Phase 8 signing engine (REUSE — never modify src/lib/icp/sign-document.ts):
```typescript
function signPdfBuffer(pdfBuffer: Buffer, certStoragePath: string, certPasswordEnc: string): Promise<SignatureResult>
// SignatureResult: { sha256Hex, signatureB64, signedAt, certSubjectCn, certThumbprintSha1, certNotAfter, certPem }
```

Phase 8 signing flow to MIRROR (from src/actions/documents.ts signDocument):
1. getActor() → role gate (dentist/admin/superadmin)
2. admin = createAdminClient(); fetch row via admin (bypasses REVOKE on storage_path)
3. cross-tenant guard: row.clinic_id === actor.tenant_id
4. if (row.signature) return already-signed; if status==='signed' return already-signed
5. fetch certificates row: select('storage_path, cert_password_enc, ...').eq('clinic_id', ...).is('deleted_at', null).order('created_at',{ascending:false}).limit(1).single() → if missing return 'Certificado ICP-Brasil não encontrado para esta clínica'
6. decrypt content if encrypted; render FINAL PDF with generatedAt = row.created_at (deterministic — Pitfall 1/WR-04)
7. const pdfBuffer = await renderToBuffer(createElement(PDFComp, props))
8. sigResult = await signPdfBuffer(pdfBuffer, cert.storage_path, cert.cert_password_enc)
9. upload to bucket: admin.storage.from('clinical-documents-pdf').upload(`${clinic_id}/${docId}.pdf`, new Uint8Array(pdfBuffer), {contentType:'application/pdf', upsert:false})
10. atomic update .eq('id', docId).is('signature', null).select('id') → if 0 rows, remove uploaded PDF + return 'já foi assinada'
11. logBusinessEvent

getActor() helper (copy the exact shape from src/actions/documents.ts): returns { actor: { id, tenant_id, role, full_name } } | { error }.

From src/lib/clinical/allergy-check.ts (Plan 02): `checkMedicationAllergy(params): { hasAlert, reasons }` — PURE. Pass DECRYPTED allergies.
From src/lib/clinical/doc-number.ts (Plan 02): `formatDocNumber`, `DOC_TYPE_PREFIX`, `ClinicalDocType`.
From src/lib/validators/clinical-document.ts (Plan 02): `clinicalDocumentSchema`, `ClinicalDocumentInput`, `medicationLineSchema`.
From src/lib/validators/teleconsultation.ts (Plan 03): `teleconsultationSchema`, `soapSchema`, `TeleconsultationInput`, `SoapInput`.
From src/lib/crypto.ts: `encrypt(plaintext)`, `decrypt(ciphertext)` (server-only).
From src/lib/audit: `logBusinessEvent({ tenantId, actorId, action, details })`.
From src/lib/auth/guards.ts: `assertNotReadOnly()`.

patients.allergies is AES-encrypted (decrypt only if non-null/non-empty — Pitfall 2). Latest anamnese: query `anamneses WHERE patient_id = ... ORDER BY created_at DESC LIMIT 1`, read `responses.alergia_medicamento` / `responses.alergia_anestesia` (default false if no anamnese).

Server-side consent IP: read from `headers()` (next/headers) — `x-forwarded-for` (first IP) or `x-real-ip`. NEVER trust a client-supplied IP. next/headers is server-only — fine inside 'use server'.

CLAUDE.md constraints: 'use server' files export ASYNC functions ONLY (no const/type exports — D-138; types come from the validator files). @react-pdf/renderer Flexbox only (no CSS Grid). nodejs runtime (PDF). Roboto font for pt-BR.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReceituarioPDF + AtestadoPDF + ExamePDF (@react-pdf/renderer, Flexbox only)</name>
  <files>src/components/pdf/ReceituarioPDF.tsx, src/components/pdf/AtestadoPDF.tsx, src/components/pdf/ExamePDF.tsx</files>
  <read_first>
    - src/components/pdf/DocumentoPDF.tsx (Font.register Roboto block + StyleSheet Flexbox patterns + signatureBlock + formatPtBR helper + header/footer — copy the structure)
    - src/lib/clinical/doc-number.ts (ClinicalDocType union for the title mapping)
    - .planning/phases/12-receitu-rio-teleodontologia/12-RESEARCH.md §Pattern 5 (signing flow) + §Pitfall 1 (deterministic generatedAt)
  </read_first>
  <action>
Create the three PDF components mirroring DocumentoPDF.tsx (Font.register Roboto with the SAME two Google Fonts URLs; `StyleSheet.create` Flexbox-only — NEVER `display:'grid'`; header with clinic name + doc number + `Gerado em: formatPtBR(generatedAt)`; the same `signatureBlock?` prop shape `{ signerCn, signedAt, thumbprintSha1, sha256Hex }` rendering the green "Assinado digitalmente com certificado ICP-Brasil" block, else the amber "RASCUNHO — não assinado" label; footer with page numbers). All are server-only (NO 'use client'); nodejs runtime is set by the calling action's module.

`ReceituarioPDF.tsx` — `ReceituarioPDFProps { clinicName: string; professionalName: string; professionalCro: string; patientName: string; isControleEspecial: boolean; medications: { medication_name: string; posologia: string; quantidade?: string }[]; observacoes?: string; documentNumber: string; generatedAt: string; signatureBlock?: {...} }`. Title = `isControleEspecial ? 'Receituário de Controle Especial' : 'Receita'`. Render patient name, then each medication as a numbered block (nome — posologia — quantidade), then observações. For controle especial, add a notice line (duas vias). Show `professionalName` + `CRO: {professionalCro}` near the signature area.

`AtestadoPDF.tsx` — `AtestadoPDFProps { clinicName; professionalName; professionalCro; patientName; motivo: string; dias?: number; documentNumber; generatedAt; signatureBlock? }`. Title `'Atestado'`. Body: standard atestado text mentioning the patient, the motivo, and `dias` (afastamento) when present.

`ExamePDF.tsx` — `ExamePDFProps { clinicName; professionalName; professionalCro; patientName; solicitacao: string; documentNumber; generatedAt; signatureBlock? }`. Title `'Solicitação de Exame'`. Body: the requested exams text.

Each component references `generatedAt` directly in the header (NO `new Date()` inside — deterministic, Pitfall 1).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/receituario/clinical-documents.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - clinical-documents.test.ts PDF asserts GREEN: each PDF imports @react-pdf/renderer, contains NO display:'grid', references generatedAt.
    - `npx tsc --noEmit` exits 0; components are server-only (no 'use client').
  </acceptance_criteria>
  <done>Three PDF components render receita/atestado/exame with Flexbox + Roboto + deterministic generatedAt + signature block; tsc clean; PDF asserts GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: clinical-documents.ts Server Actions (issue + sign + list)</name>
  <files>src/actions/clinical-documents.ts</files>
  <read_first>
    - src/actions/documents.ts (getActor helper, generateDocument + signDocument FULL flow, atomic .is('signature', null) guard, createAdminClient cert fetch, encrypt/decrypt usage, logBusinessEvent — mirror exactly)
    - src/lib/icp/sign-document.ts (signPdfBuffer signature — IMPORT, never modify)
    - src/lib/clinical/allergy-check.ts + src/lib/clinical/doc-number.ts + src/lib/validators/clinical-document.ts (Plan 02 contracts)
    - src/lib/validators/anamnesis.ts (responses.alergia_medicamento/alergia_anestesia keys)
    - .planning/phases/12-receitu-rio-teleodontologia/12-RESEARCH.md §Code Example 1 (issueClinicDocument outline) + §Pattern 5 (sign flow) + §Pitfall 1/2/7
  </read_first>
  <action>
Create `src/actions/clinical-documents.ts` (`'use server'`, async exports ONLY — types come from the validator files, D-138):
  - Copy the `getActor()` helper from src/actions/documents.ts.
  - `issueClinicDocument(input: ClinicalDocumentInput): Promise<{ success; documentId?; docNumber?; allergyAlert?: { reasons: string[] }; error? }>`:
    1. `await assertNotReadOnly()`; getActor; role gate `['admin','superadmin','dentist']`.
    2. `clinicalDocumentSchema.safeParse(input)`; on fail return validation error. Enforce doc-type requiredness in code: receita_* needs `medications.length >= 1`; atestado needs `atestado_motivo`; solicitacao_exame needs `exame_solicitacao` — else return a pt-BR error.
    3. ALLERGY (RX-02, receita_* only): fetch patient via `createAdminClient()` select `allergies` (encrypted) scoped to `clinic_id = actor.tenant_id`; `const allergiesPlain = patient.allergies ? decrypt(patient.allergies) : null` (Pitfall 2 guard). Fetch latest anamnese: `anamneses WHERE patient_id ORDER BY created_at DESC LIMIT 1` → `flags = { alergia_medicamento: !!responses?.alergia_medicamento, alergia_anestesia: !!responses?.alergia_anestesia }` (both false if no row). For each medication line, look up its `therapeutic_class` + `allergen_tags` from `medications` (by `medication_id`), call `checkMedicationAllergy({ medicationName, therapeuticClass, allergenTags, patientAllergiesPlaintext: allergiesPlain, anamnesisFlags: flags })`, accumulate reasons. Set `allergyAlert = { reasons }` if any. NON-BLOCKING (D-02): do NOT abort — return the alert alongside success.
    4. NUMBER (RX-01): `const { data: docNumber } = await supabase.rpc('next_doc_number', { p_clinic_id: actor.tenant_id, p_doc_type: input.doc_type })`; on error return 'Erro ao gerar número do documento'.
    5. Build `content_json` object (medications/posologia/motivo/dias/solicitacao/observacoes per type) → `const encryptedContent = encrypt(JSON.stringify(contentJson))` (Pitfall 7).
    6. Insert `clinical_documents` row: status 'draft', `clinic_id = actor.tenant_id`, `patient_id`, `appointment_id ?? null`, `professional_id ?? null` (resolve from input or from professionals WHERE user_id = actor.id if absent), `doc_type`, `doc_number`, `content_json: encryptedContent`, `portal_visible: input.portal_visible ?? false`, `created_by: actor.id`. Use `createClient()` (RLS) for the insert. Return `{ success: true, documentId, docNumber, allergyAlert }`.
    7. `logBusinessEvent({ tenantId, actorId, action: 'clinical_document.issued', details: { document_id, doc_type, doc_number } })` — IDs only, no PII.
  - `signClinicDocument(documentId: string): Promise<{ success; signerCn?; signedAt?; thumbprint?; error? }>` — mirror signDocument EXACTLY but on `clinical_documents`:
    1. assertNotReadOnly; getActor; role gate.
    2. admin = createAdminClient(); fetch the clinical_documents row via admin (select content_json + doc_type + professional_id + patient_id + status + signature + created_at + clinic_id). If `row.signature` or `status==='signed'` → return already-signed. Cross-tenant guard `row.clinic_id === actor.tenant_id`.
    3. Fetch certificates row (same query as documents.ts) → if missing return 'Certificado ICP-Brasil não encontrado para esta clínica'.
    4. Decrypt content_json; fetch clinic name + professional (name + cro/cro_uf from professionals) + patient full_name via admin. `const generatedAt = row.created_at` (deterministic — Pitfall 1).
    5. Pick the PDF component by `doc_type` (receita_simples/receita_controle_especial → ReceituarioPDF with isControleEspecial flag; atestado → AtestadoPDF; solicitacao_exame → ExamePDF); `pdfBuffer = await renderToBuffer(createElement(Comp, props))`.
    6. `sigResult = await signPdfBuffer(pdfBuffer, cert.storage_path, cert.cert_password_enc)`.
    7. Upload to `clinical-documents-pdf` bucket at `${row.clinic_id}/${documentId}.pdf` (upsert:false).
    8. Atomic update `.eq('id', documentId).is('signature', null).select('id')` setting content_hash/signature/storage_path/cert_pem/signer_cn/cert_thumbprint/cert_not_after/signed_at/signed_by + `status='signed'`. If 0 rows → remove uploaded PDF + return 'já foi assinada por outra requisição simultânea'.
    9. logBusinessEvent 'clinical_document.signed'. Return signerCn/signedAt/thumbprint.
  - `listClinicDocuments(patientId?: string)`: `createClient()` RLS read of `clinical_documents` (NEVER select storage_path/cert_pem), tenant-scoped, ordered created_at desc; optional patient filter. Return display metadata.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/receituario/clinical-documents.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - clinical-documents.test.ts action asserts GREEN: imports decrypt from @/lib/crypto + checkMedicationAllergy; references next_doc_number + .rpc(; imports signPdfBuffer from @/lib/icp/sign-document; uses .is('signature', null) + clinical-documents-pdf bucket; assertNotReadOnly + createClient + createAdminClient + logBusinessEvent + portal_visible; no MAX(.
    - issueClinicDocument + signClinicDocument exported async; sign-document.ts UNCHANGED.
    - `npx tsc --noEmit` exits 0 against regenerated types? (Note: types not yet pushed — see acceptance below.)
  </acceptance_criteria>
  <done>clinical-documents.ts exports issue/sign/list; allergy decrypt server-side; atomic numbering + immutable signing reusing Phase 8 engine; action asserts GREEN. tsc may show only the new-table type errors resolved after Plan 05 push — see note.</done>
</task>

<task type="auto">
  <name>Task 3: teleconsultations.ts Server Actions (create + start/end + SOAP)</name>
  <files>src/actions/teleconsultations.ts</files>
  <read_first>
    - src/actions/documents.ts (getActor helper + 'use server' async-only + logBusinessEvent)
    - src/lib/validators/teleconsultation.ts (teleconsultationSchema + soapSchema — Plan 03)
    - src/__tests__/teleodontologia/teleconsultations.test.ts (the exact source-inspection asserts to satisfy: consent_ip from headers, both export names, SOAP links)
    - .planning/phases/12-receitu-rio-teleodontologia/12-RESEARCH.md §Pattern 4 + §Open Question 2
  </read_first>
  <action>
Create `src/actions/teleconsultations.ts` (`'use server'`, async exports ONLY):
  - Copy `getActor()` from documents.ts.
  - `createTeleconsultation(input: TeleconsultationInput)`: assertNotReadOnly; getActor; role gate `['admin','superadmin','dentist']`; `teleconsultationSchema.safeParse`. Capture consent server-side: `const h = await headers(); const fwd = h.get('x-forwarded-for'); const consentIp = fwd ? fwd.split(',')[0].trim() : (h.get('x-real-ip') ?? null)`. Insert `teleconsultations` row via `createClient()`: clinic_id = actor.tenant_id, patient_id, appointment_id ?? null, professional_id ?? null, external_link, consent_given: input.consent_given, `consent_given_at: input.consent_given ? new Date().toISOString() : null`, `consent_ip: input.consent_given ? consentIp : null`, status 'agendada', created_by: actor.id. logBusinessEvent 'teleconsultation.created' (IDs only — consent_ip is NOT logged). Return { success, id?, error? }.
  - `startTeleconsultation(id: string)`: assertNotReadOnly; getActor; tenant-scoped update `.eq('id', id).eq('clinic_id', actor.tenant_id)` setting `status='em_andamento'`, `started_at = now()`. Guard: only if consent_given is true (else return 'Consentimento da sessão é obrigatório'). log.
  - `endTeleconsultation(id: string)`: assertNotReadOnly; tenant-scoped update `status='concluida'`, `ended_at = now()`. log.
  - `createSoapRecord(input: SoapInput)`: assertNotReadOnly; getActor; role gate; `soapSchema.safeParse`. Insert `soap_records`: clinic_id = actor.tenant_id, patient_id, `appointment_id: input.appointment_id ?? null`, `teleconsultation_id: input.teleconsultation_id ?? null`, dentist_id = actor.id, the 4 soap_* fields. log 'soap_record.created'. Return { success, id?, error? }. (TEL-02: links the SOAP note to BOTH the teleconsultation and the atendimento.)
  - `listTeleconsultations(patientId?: string)`: createClient() RLS read, tenant-scoped, ordered created_at desc; optional patient filter.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/teleodontologia/teleconsultations.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - teleconsultations.test.ts action asserts GREEN: createTeleconsultation references external_link/consent_given/consent_given_at + consent_ip read from headers (x-forwarded-for); start/endTeleconsultation set started_at/ended_at + status; createSoapRecord references teleconsultation_id + appointment_id + the 4 SOAP fields; assertNotReadOnly + createClient + role gate (dentist) + logBusinessEvent.
    - All exports async; `npx tsc --noEmit` exits 0 (post-Plan-05 push for the new-table types — see note).
  </acceptance_criteria>
  <done>teleconsultations.ts exports create/start/end/SOAP/list; consent_ip + consent_given_at server-set; SOAP links both FKs; action asserts GREEN.</done>
</task>

</tasks>

<note_on_tsc>
The two Server Action files (Tasks 2/3) query the new tables (clinical_documents/medications/teleconsultations/soap_records) which do NOT yet exist in src/types/database.types.ts until the Plan 05 [BLOCKING] db push regenerates types. If tsc reports "table not found in Database type" errors ONLY for these four tables/the next_doc_number rpc, that is EXPECTED at Wave 2 and is resolved by Plan 05. Use `.from('clinical_documents' as never)` casts ONLY if the executor cannot otherwise keep the build staged — but PREFER leaving the typed calls and letting Plan 05's gen types resolve them, validating instead with `npx vitest run` (source-inspection, which does not type-check the Supabase generics). Do NOT introduce `any` across the file. The source-inspection test suites (the <verify> commands) are the binding gate for this plan; full `npx next build` green is gated at Plan 05/06 after the push.
</note_on_tsc>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → issue/sign/teleconsult actions | untrusted; Zod-validated + role-gated + tenant-scoped; allergy decrypt + consent IP set server-side |
| clinical_documents draft → signed | one-way immutable transition guarded by .is('signature', null) (Phase 8 race guard) |
| Phase 8 signing engine | imported read-only; signClinicDocument must not modify sign-document.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Tampering | client pre-checks/overrides allergy result | mitigate | allergy check runs server-side in issueClinicDocument; client input has no allergy-override field; decrypt + checkMedicationAllergy in the action only |
| T-12-17 | Tampering | re-signing an already-signed clinical doc | mitigate | .is('signature', null) atomic guard + status==='signed' early return + rollback of uploaded PDF on 0-row update |
| T-12-18 | Repudiation | forged teleconsultation consent IP/time | mitigate | consent_ip read from x-forwarded-for/x-real-ip headers server-side; consent_given_at = server now(); client cannot set them |
| T-12-19 | Information Disclosure | content_json/allergy plaintext leak | mitigate | content_json encrypted before insert; allergy plaintext stays in the action (never returned to client — only non-identifying reason strings) |
| T-12-20 | Elevation of Privilege | read-only role issuing/signing | mitigate | assertNotReadOnly() + role gate ['admin','superadmin','dentist'] at the top of every mutation |
| T-12-21 | Information Disclosure | storage_path returned to client | mitigate | listClinicDocuments never selects storage_path/cert_pem (REVOKE + explicit column list) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/receituario/ src/__tests__/teleodontologia/` GREEN for all action + PDF source-inspection asserts; Plan 01 regression guard still GREEN.
- `npx vitest run` full suite runs without crash.
- sign-document.ts byte-identical to before (regression-safe — Plan 01 asserts it still exports signPdfBuffer).
</verification>

<success_criteria>
- issueClinicDocument (validate + decrypt allergies + checkMedicationAllergy + next_doc_number + encrypted draft) + signClinicDocument (typed PDF → signPdfBuffer reuse → upload → immutable signed) + list.
- createTeleconsultation (consent + server-set IP/time) + start/end + createSoapRecord (linked to session + atendimento) + list.
- Every mutation: assertNotReadOnly + role gate + logBusinessEvent + tenant scope; Phase 8 engine reused unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/12-receitu-rio-teleodontologia/12-04-SUMMARY.md`
</output>
</content>
