---
phase: 12-receitu-rio-teleodontologia
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - src/types/database.types.ts
autonomous: false
requirements: [RX-01, RX-03, TEL-01, TEL-02]
tags: [database, migration, blocking, gen-types]

must_haves:
  truths:
    - "The five Phase 12 migrations are applied to the live Supabase project jqjwyqlbbuqnrffdnlpp"
    - "src/types/database.types.ts contains medications, clinical_documents, document_seq_counters, teleconsultations, soap_records types"
    - "next_doc_number RPC is callable on the live DB and the clinical-documents-pdf bucket exists"
    - "tsc stays green after type regeneration (the Plan 04 action files type-check against the new tables)"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "regenerated Supabase types including the five new tables + next_doc_number rpc"
      contains: "clinical_documents"
  key_links:
    - from: "supabase/migrations/20260618000100..000500"
      to: "live DB jqjwyqlbbuqnrffdnlpp"
      via: "supabase db push"
      pattern: "medications|clinical_documents|teleconsultations|soap_records"
---

<objective>
Apply the five Phase 12 migrations to the live Supabase project and regenerate TypeScript types. This is the SINGLE [BLOCKING] checkpoint of the phase — it requires a human because the Supabase CLI is frequently logged into the wrong account (the nexus-* org) and must be re-authenticated against org `kczvihafddupruvsrrsc` / project `jqjwyqlbbuqnrffdnlpp` before push.

Purpose: Make `medications` (+seed), `clinical_documents`, `document_seq_counters` + `next_doc_number()`, the `clinical-documents-pdf` bucket, `teleconsultations`, and `soap_records` real so the Wave 4 receituário + teleodontologia UIs (Plans 06/07) can query against the live DB with typed clients, and so the Plan 04 Server Actions type-check against the regenerated schema.
Output: Migrations applied; src/types/database.types.ts regenerated with the five new tables + the next_doc_number rpc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-receitu-rio-teleodontologia/12-RESEARCH.md
@supabase/migrations/20260618000100_clinical_documents.sql
@supabase/migrations/20260618000200_clinical_documents_rls.sql
@supabase/migrations/20260618000300_clinical_documents_bucket.sql
@supabase/migrations/20260618000400_teleconsultations.sql
@supabase/migrations/20260618000500_teleconsultations_rls.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Re-auth, supabase db push, regenerate types (truncation guard)</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - .planning/phases/11-profissionais-recursos/11-05-PLAN.md (the exact re-auth + db push + temp-file gen-types guard sequence — mirror it for Phase 12)
    - supabase/migrations/20260618000100_clinical_documents.sql ... 20260618000500_teleconsultations_rls.sql (the five files being applied)
  </read_first>
  <action>
    Human runs the Supabase re-auth + db push + gen types sequence detailed in how-to-verify below.
    This is a checkpoint:human-action because the Supabase CLI is frequently logged into the wrong
    account; the human MUST confirm project jqjwyqlbbuqnrffdnlpp is active before pushing DDL, then
    apply the five Phase 12 migrations and regenerate src/types/database.types.ts using the temp-file
    truncation guard (write temp → verify size + new tables present → only then overwrite).
  </action>
  <what-built>
    Five migrations written in Plans 02/03 (clinical_documents [+medications +document_seq_counters
    +next_doc_number], clinical_documents_rls, clinical_documents_bucket, teleconsultations [+soap_records],
    teleconsultations_rls) are ready to apply. They ONLY ADD new tables, a global reference table, a function,
    a storage bucket, and RLS — the appointments GIST no_overlap constraint, the status CHECK, medical_records,
    dental_records, and the Phase 8 signing engine are all untouched (Plan 01 regression guard backs this).
  </what-built>
  <how-to-verify>
    Run these from the repo root, in order. Stop and report if any step fails.

    1. Confirm the CLI is on the RIGHT account/project (recurring gotcha — MEMORY.md):
         supabase projects list
       The output MUST show project `jqjwyqlbbuqnrffdnlpp` with a ● (linked/active marker).
       If it is NOT shown (CLI logged into a nexus-* account), re-authenticate:
         supabase login
       then re-run `supabase projects list` and confirm jqjwyqlbbuqnrffdnlpp appears with ●.

    2. Apply the migrations to the live DB:
         supabase db push
       Expect the five 20260618000100..000500 migrations to apply with no error.
       (All are additive: new tables + medications seed INSERTs + next_doc_number function + the
       clinical-documents-pdf bucket + RLS. No data migration on appointments. If the bucket insert
       errors because it already exists, the ON CONFLICT DO NOTHING handles it — report any other error.)

    3. Regenerate the typed schema WITH THE TRUNCATION GUARD (CLAUDE.md):
         supabase gen types typescript --project-id jqjwyqlbbuqnrffdnlpp > src/types/database.types.t
       Then verify the temp file BEFORE overwriting:
         - it has > 1000 lines:           wc -l src/types/database.types.t
         - it contains the new tables:    grep -E "medications|clinical_documents|document_seq_counters|teleconsultations|soap_records" src/types/database.types.t
         - it contains the rpc:           grep "next_doc_number" src/types/database.types.t
       ONLY if all checks pass, overwrite atomically:
         mv src/types/database.types.t src/types/database.types.ts
       If any check fails, DELETE the temp file and report — do NOT overwrite the good types file.

    4. Verify types + build:
         npx tsc --noEmit            (must exit 0 — the Plan 04 action files now type-check against the new tables)
       and confirm src/types/database.types.ts now contains `medications`, `clinical_documents`,
       `document_seq_counters`, `teleconsultations`, `soap_records`, and the `next_doc_number` function.
  </how-to-verify>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `supabase projects list` shows jqjwyqlbbuqnrffdnlpp ● before push.
    - db push applies all five migrations with no error (medications seed present).
    - Temp-file guard satisfied (>1000 lines + five new tables + next_doc_number present) before mv overwrite.
    - src/types/database.types.ts contains medications, clinical_documents, document_seq_counters, teleconsultations, soap_records + next_doc_number rpc.
    - `npx tsc --noEmit` exits 0 (Plan 04 actions resolve against the new types).
  </acceptance_criteria>
  <resume-signal>Type "approved" once db push succeeded, types regenerated via the guard, and tsc is green. Report any error otherwise.</resume-signal>
  <done>Five migrations applied to jqjwyqlbbuqnrffdnlpp; types include the five new tables + next_doc_number; clinical-documents-pdf bucket exists; tsc exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → live DB | db push applies DDL to production project; wrong-account push is the primary risk |
| gen types temp write → build | partial type write can break the Vercel build; truncation guard prevents overwrite of good types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-22 | Tampering | db push to wrong project/org | mitigate | Mandatory `supabase projects list` verification of jqjwyqlbbuqnrffdnlpp ● before push; human gate |
| T-12-23 | Denial of Service | partial/truncated gen types breaks build | mitigate | temp-file (.t) → size + table-presence + rpc-presence check → atomic mv only on success |
| T-12-24 | Denial of Service | failed/partial migration | accept | Migrations are additive (new tables + seed + function + bucket + RLS); no data migration on appointments; re-runnable (ON CONFLICT DO NOTHING on bucket) |
| T-12-25 | Information Disclosure | medications seed exposes nothing sensitive | accept | medications is global non-PII reference data; global SELECT policy is intentional (Pitfall 4) |
</threat_model>

<verification>
- `supabase projects list` shows jqjwyqlbbuqnrffdnlpp ●
- `supabase db push` applies the five migrations
- temp-file guard passes; `src/types/database.types.ts` regenerated, contains the five tables + next_doc_number
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Live DB has medications(+seed), clinical_documents, document_seq_counters + next_doc_number(), the clinical-documents-pdf bucket, teleconsultations, soap_records with RLS.
- Types regenerated via the truncation guard; tsc green; Plan 04 actions type-check.
</success_criteria>

<output>
After completion, create `.planning/phases/12-receitu-rio-teleodontologia/12-05-SUMMARY.md`
</output>
</content>
