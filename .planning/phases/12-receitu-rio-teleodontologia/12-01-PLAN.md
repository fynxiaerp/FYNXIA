---
phase: 12-receitu-rio-teleodontologia
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/receituario/allergy-check.test.ts
  - src/__tests__/receituario/clinical-documents.test.ts
  - src/__tests__/receituario/migrations-phase12-rx.test.ts
  - src/__tests__/teleodontologia/teleconsultations.test.ts
  - src/__tests__/teleodontologia/migrations-phase12-tel.test.ts
autonomous: true
requirements: [RX-01, RX-02, RX-03, TEL-01, TEL-02]
tags: [receituario, teleodontologia, clinical-documents, testing, scaffold]

must_haves:
  truths:
    - "Running the 5 Phase 12 test files fails with clear RED errors (missing migrations/modules/libs), not import/parse crashes"
    - "Every Phase 12 requirement (RX-01..03, TEL-01..02) has at least one assertion in a scaffold"
    - "A regression guard locks the appointments EXCLUDE GIST + status CHECK as UNCHANGED and asserts the Phase 8 signing engine src/lib/icp/sign-document.ts is untouched"
  artifacts:
    - path: "src/__tests__/receituario/allergy-check.test.ts"
      provides: "checkMedicationAllergy pure-unit cases (RX-02): tag/free-text match, anamnese flags, accent/case-insensitivity, no-match"
    - path: "src/__tests__/receituario/clinical-documents.test.ts"
      provides: "issueClinicDocument + signClinicDocument source-inspection (RX-01/RX-03): allergy decrypt, next_doc_number, signPdfBuffer, .is('signature', null) guard"
    - path: "src/__tests__/receituario/migrations-phase12-rx.test.ts"
      provides: "medications + clinical_documents + document_seq_counters + next_doc_number migration source-inspection (RX-01/RX-03) + GIST/Phase-8 regression guard"
    - path: "src/__tests__/teleodontologia/teleconsultations.test.ts"
      provides: "createTeleconsultation + createSoapRecord source-inspection (TEL-01/TEL-02): external_link/consent_given/consent_ip + teleconsultation_id+appointment_id link"
    - path: "src/__tests__/teleodontologia/migrations-phase12-tel.test.ts"
      provides: "teleconsultations + soap_records migration source-inspection (TEL-01/TEL-02): status CHECK + 4 SOAP columns"
  key_links:
    - from: "test files"
      to: "supabase/migrations + src/lib/clinical/* + src/actions/clinical-documents.ts + src/actions/teleconsultations.ts"
      via: "readFileSync source-inspection + absolute-path dynamic import with existsSync guard"
      pattern: "readFileSync|existsSync|toMatch"
---

<objective>
Create the Wave 0 RED test scaffolds for all of Phase 12 — receituário (medications base + clinical documents + allergy check + ICP signing reuse + atomic numbering) and teleodontologia (teleconsultation session + SOAP record). These tests assert the artifacts that Waves 1–4 will produce; they are RED now (target files do not exist) and turn GREEN incrementally as each plan lands. They also lock a REGRESSION GUARD on (a) the existing appointments EXCLUDE GIST + status CHECK and (b) the Phase 8 signing engine `src/lib/icp/sign-document.ts`, so no later wave can silently break the v1 agenda or the DOC-02 signing path that RX-03 reuses.

Purpose: Establish the verification contract before implementation (Nyquist). Every RX/TEL requirement gets at least one automated assertion; the test style is locked to the source-inspection + pure-unit convention used in Phases 8–11.
Output: 5 test files under `src/__tests__/{receituario,teleodontologia}/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-receitu-rio-teleodontologia/12-RESEARCH.md
@.planning/phases/12-receitu-rio-teleodontologia/12-VALIDATION.md
@.planning/phases/11-profissionais-recursos/11-01-PLAN.md
@supabase/migrations/20260605000100_clinical_tables.sql
@src/lib/icp/sign-document.ts

<interfaces>
<!-- Established test conventions the executor MUST mirror. No exploration needed. -->

Source-inspection helpers (copy verbatim from src/__tests__/migrations/phase11.test.ts):
```typescript
import { readFileSync, readdirSync, existsSync } from 'node:fs'
import { join, resolve } from 'node:path'
const MIGRATIONS_DIR = join(process.cwd(), 'supabase/migrations')
// M(suffix): read a migration file by its filename suffix (throws if absent)
function M(suffix: string): string {
  const files = readdirSync(MIGRATIONS_DIR)
  const match = files.find(f => f.endsWith(suffix))
  if (!match) throw new Error(`Migration ending '${suffix}' not found`)
  return readFileSync(join(MIGRATIONS_DIR, match), 'utf8')
}
// MM(suffix): like M but returns '' if no migration matches (assert RED on content, not on throw)
function MM(suffix: string): string {
  const files = readdirSync(MIGRATIONS_DIR)
  const match = files.find(f => f.endsWith(suffix))
  return match ? readFileSync(join(MIGRATIONS_DIR, match), 'utf8') : ''
}
// ALL(): concatenation of every *.sql migration (for the "no DROP/ALTER appointments" scan)
function ALL(): string {
  return readdirSync(MIGRATIONS_DIR).filter(f => f.endsWith('.sql'))
    .map(f => readFileSync(join(MIGRATIONS_DIR, f), 'utf8')).join('\n')
}
// SRC(relPath): read a source file, return '' if missing (assert on content, not ENOENT)
function SRC(rel: string): string {
  const p = resolve(process.cwd(), rel)
  return existsSync(p) ? readFileSync(p, 'utf8') : ''
}
```

server-only mock pattern (top of each source-inspection test):
```typescript
import { vi } from 'vitest'
vi.mock('server-only', () => ({}))
```

Absolute-path dynamic-import gotcha (STATE.md D-144/D-162/D-168): for the PURE allergy-check unit, use
`resolve(process.cwd(),'src/lib/clinical/allergy-check.ts')` + existsSync guard before `await import(...)` —
the `@`-alias causes TS2307 when the target module does not yet exist. Guard the pure asserts with
`if (!fn) return` so tsc stays green while RED.

ES2017 regex gotcha (STATE.md D-163/D-169): the `s` (dotAll) flag is NOT supported under the repo tsconfig
target. For multi-line SQL assertions, use MULTIPLE separate `.toMatch()` calls — never one `/.../is`.

Phase 12 target migration suffixes (Plans 02/03 create these):
- `_clinical_documents.sql` (medications + clinical_documents + document_seq_counters + next_doc_number)
- `_clinical_documents_rls.sql`
- `_clinical_documents_bucket.sql` (clinical-documents-pdf private bucket)
- `_teleconsultations.sql` (teleconsultations + soap_records)
- `_teleconsultations_rls.sql`

Phase 12 target source files (Plans 02/03/04 create these):
- `src/lib/clinical/allergy-check.ts` (checkMedicationAllergy — PURE, no 'use server')
- `src/lib/clinical/doc-number.ts` (formatDocNumber helper — PURE)
- `src/lib/validators/clinical-document.ts`, `src/lib/validators/teleconsultation.ts` (Zod v3)
- `src/actions/clinical-documents.ts` (issueClinicDocument, signClinicDocument)
- `src/actions/teleconsultations.ts` (createTeleconsultation, start/endTeleconsultation, createSoapRecord)
- `src/components/pdf/ReceituarioPDF.tsx`, `AtestadoPDF.tsx`, `ExamePDF.tsx`

checkMedicationAllergy signature (Plan 02 implements — assert against this exact shape):
```typescript
function checkMedicationAllergy(params: {
  medicationName: string
  therapeuticClass: string
  allergenTags: string[]
  patientAllergiesPlaintext: string | null
  anamnesisFlags: { alergia_medicamento: boolean; alergia_anestesia: boolean }
}): { hasAlert: boolean; reasons: string[] }
```

Existing appointments GIST + status CHECK (regression target — must stay UNCHANGED, in 20260605000100_clinical_tables.sql):
```sql
status TEXT NOT NULL DEFAULT 'agendado'
  CHECK (status IN ('agendado','confirmado','em_atendimento','concluido','cancelado')),
CONSTRAINT no_overlap EXCLUDE USING GIST (
  tenant_id  WITH =,
  dentist_id WITH =,
  tstzrange(start_time, end_time, '[)') WITH &&
) WHERE (status NOT IN ('cancelado'))
```

Phase 8 signing engine (regression target — must stay UNCHANGED): `src/lib/icp/sign-document.ts`
exports `signPdfBuffer(pdfBuffer, certStoragePath, certPasswordEnc): Promise<SignatureResult>` and
`verifyPdfSignature(...)`. RX-03 REUSES this; no Phase 12 file may modify it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Receituário scaffolds (allergy-check.test.ts, clinical-documents.test.ts, migrations-phase12-rx.test.ts)</name>
  <files>src/__tests__/receituario/allergy-check.test.ts, src/__tests__/receituario/clinical-documents.test.ts, src/__tests__/receituario/migrations-phase12-rx.test.ts</files>
  <read_first>
    - src/__tests__/migrations/phase11.test.ts (M/MM/SRC helpers + describe/it structure + the GIST regression assertion idioms + ALL-files scan idiom)
    - src/__tests__/professionals/professionals.test.ts (server-only mock + SRC source-inspection pattern + guarded dynamic-import)
    - supabase/migrations/20260605000100_clinical_tables.sql (exact existing GIST + status CHECK text to lock as regression; patients.allergies encrypted column)
    - src/lib/icp/sign-document.ts (signPdfBuffer signature + the regression-target file path)
    - .planning/phases/12-receitu-rio-teleodontologia/12-RESEARCH.md §Pattern 1/2/3/6 (clinical_documents + seq counter + allergy check + medications columns) + §Code Example 2 (checkMedicationAllergy)
    - .planning/phases/12-receitu-rio-teleodontologia/12-VALIDATION.md §Per-Requirement Validation Map (exact REQ→assertion mapping)
  </read_first>
  <action>
Create `src/__tests__/receituario/allergy-check.test.ts` (PURE-unit, RX-02):
  - `vi.mock('server-only', () => ({}))`.
  - Dynamic-import `checkMedicationAllergy` from `src/lib/clinical/allergy-check.ts` via absolute-path + existsSync guard; guard the pure asserts with `if (!checkMedicationAllergy) return` so tsc + suite stay green while RED.
  - PURE cases against the signature in the interfaces block:
    - allergen tag present in free-text: `allergenTags=['penicilina']`, `patientAllergiesPlaintext='Alergia a Penicilina'` → `hasAlert===true` and a reason mentions `penicilina`. (asserts accent/CASE-insensitive match: also test `'PENICILINA'` and `'penicilína'` variants → true)
    - anamnese flag: `anamnesisFlags.alergia_medicamento=true`, no tag match → `hasAlert===true`, reason matches `/anamnese/i`.
    - anestesia: `therapeuticClass='anestesico_local'`, `anamnesisFlags.alergia_anestesia=true` → `hasAlert===true`, reason matches `/anestesia/i`. And with `therapeuticClass='analgesico'` + only `alergia_anestesia=true` → that anestesia reason is NOT present.
    - no match: empty tags, `patientAllergiesPlaintext=null`, both flags false → `hasAlert===false`, `reasons.length===0`.
    - never throws on null: calling with `patientAllergiesPlaintext=null` does not throw.
  - SRC source-inspection on `src/lib/clinical/allergy-check.ts`: assert it does NOT contain `'use server'` and does NOT import `server-only` (must be client-importable PURE). (RX-02 architecture)

Create `src/__tests__/receituario/clinical-documents.test.ts` (source-inspection, RX-01/RX-03):
  - `vi.mock('server-only', () => ({}))`.
  - SRC on `src/actions/clinical-documents.ts`:
    - RX-02 wiring: imports `decrypt` from `@/lib/crypto` (match `from '@/lib/crypto'` and `decrypt`) AND references `checkMedicationAllergy` (match `checkMedicationAllergy`). (Allergy decrypted server-side, never against ciphertext.)
    - RX-01 numbering: references the atomic counter — match `next_doc_number` AND `.rpc(`. (No `MAX(` + `+ 1` doc-number computation in the action — assert the action does NOT match `/MAX\(/`.)
    - RX-03 signing reuse: references `signPdfBuffer` and imports from `@/lib/icp/sign-document`.
    - RX-03 immutability race guard: matches `.is('signature', null)` AND uploads to bucket `clinical-documents-pdf`.
    - Security: matches `assertNotReadOnly`, references `createClient` from `@/lib/supabase/server`, references `createAdminClient`, and `logBusinessEvent`. Role gate present (match `dentist` near a role array).
    - `issueClinicDocument` and `signClinicDocument` are exported async functions (match `export async function issueClinicDocument` and `export async function signClinicDocument`).
    - `portal_visible` flag referenced (RX-03 portal). Match `portal_visible`.
  - SRC on the PDF components `src/components/pdf/ReceituarioPDF.tsx`, `AtestadoPDF.tsx`, `ExamePDF.tsx`: each is a `@react-pdf/renderer` component — match `@react-pdf/renderer`; assert NONE contains `display: 'grid'` or `display:"grid"` (Flexbox-only — CLAUDE.md). Assert they reference `generatedAt` (deterministic timestamp — Pitfall 1).
  - Dynamic-import `formatDocNumber` from `src/lib/clinical/doc-number.ts` (guarded). PURE: `formatDocNumber('receita_simples', 42, 2026)` matches `/^REC-2026-0042$/` (prefix per type + zero-pad to 4); `'atestado'` → prefix `ATE`; `'solicitacao_exame'` → `EXM`; `'receita_controle_especial'` → `RCC`.

Create `src/__tests__/receituario/migrations-phase12-rx.test.ts` (migration source-inspection, RX-01/RX-03):
  - `_clinical_documents.sql` (via MM):
    - `CREATE TABLE public.medications` with `name`, `generic_name`, `therapeutic_class`, `allergen_tags` (match `allergen_tags TEXT[]` allowing whitespace variance via separate `.toMatch(/allergen_tags/)` + `.toMatch(/TEXT\[\]/)`), `requires_special_control BOOLEAN`, and at least one seed `INSERT INTO public.medications`. (RX-01 base curada D-01)
    - `CREATE TABLE public.clinical_documents` with `clinic_id`, `patient_id`, `professional_id`, `appointment_id`, `doc_type` with a CHECK constraint listing the 4 values `receita_simples`, `receita_controle_especial`, `atestado`, `solicitacao_exame` (assert each value with a separate `.toMatch`), `status` CHECK `IN ('draft','signed')` (assert `draft` + `signed`), `content_json` (PII — assert encrypted-at-rest by checking the column is `TEXT` not `JSONB`: match `content_json TEXT`), `doc_number`, `storage_path`, `signature`, `portal_visible BOOLEAN`, `deleted_at`, `idx_clinical_docs_clinic` (match `idx_clinical_docs` + `clinic_id`). (RX-01/RX-03)
    - `CREATE TABLE public.document_seq_counters` with `UNIQUE (clinic_id, doc_type)` (assert `clinic_id` + `doc_type` + `UNIQUE`). (RX-03)
    - `CREATE OR REPLACE FUNCTION public.next_doc_number` and the atomic upsert `ON CONFLICT` + `DO UPDATE SET last_seq` (assert `next_doc_number`, `ON CONFLICT`, `last_seq`). Assert it does NOT use `MAX(` (race-safe). (RX-03 Pitfall 3)
    - REVOKE on sensitive column: match `REVOKE` + `storage_path`. (Security — Phase 8 pattern)
  - `_clinical_documents_rls.sql` (via MM): `ENABLE ROW LEVEL SECURITY` on `clinical_documents`; SELECT policy `clinic_id = get_my_tenant_id()`; write policy with BOTH `USING` and `WITH CHECK` and a role gate `get_my_role() IN ('admin'` (and `dentist`/`superadmin`); `medications` SELECT policy open to authenticated (assert a policy on `public.medications` that does NOT filter by `get_my_tenant_id` — match a `medications` policy line + that the medications block lacks `get_my_tenant_id`). (Access Control + Pitfall 4)
  - **REGRESSION GUARD (CRITICAL) — describe block `'REGRESSION: appointments GIST + status CHECK + Phase 8 signing unchanged'`:**
    - read `20260605000100_clinical_tables.sql` via M and assert it STILL contains `CONSTRAINT no_overlap EXCLUDE USING GIST`, `tenant_id  WITH =`, `dentist_id WITH =`, `WHERE (status NOT IN ('cancelado'))`, and the status CHECK values `agendado`/`confirmado`/`em_atendimento`/`concluido`/`cancelado` (separate `.toMatch` each).
    - assert `ALL()` (every migration) does NOT contain `DROP CONSTRAINT no_overlap` and does NOT contain `ALTER COLUMN status` on appointments (match `/ALTER COLUMN status/` → expect not).
    - assert `SRC('src/lib/icp/sign-document.ts')` STILL exports `signPdfBuffer` (match `export async function signPdfBuffer`) and that NO Phase 12 source file edits it (this scaffold only reads it). Add a comment that Plan 04 must IMPORT, never modify, this file.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/receituario/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and the suite RUNS (no import/parse crash) — new-artifact assertions fail RED (intended Wave 0 state).
    - The REGRESSION GUARD describe block PASSES now (reads the unchanged clinical_tables migration + the unchanged sign-document.ts).
    - `npx tsc --noEmit` stays clean (existsSync/SRC guards prevent TS2307 on absent modules).
  </acceptance_criteria>
  <done>allergy-check.test.ts, clinical-documents.test.ts, migrations-phase12-rx.test.ts exist; new-artifact asserts RED; GIST + Phase-8-signing regression asserts GREEN; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Teleodontologia scaffolds (teleconsultations.test.ts, migrations-phase12-tel.test.ts)</name>
  <files>src/__tests__/teleodontologia/teleconsultations.test.ts, src/__tests__/teleodontologia/migrations-phase12-tel.test.ts</files>
  <read_first>
    - src/__tests__/migrations/phase11.test.ts (M/MM/SRC helpers + RLS USING+WITH CHECK assertion idiom)
    - src/__tests__/receituario/migrations-phase12-rx.test.ts (the file Task 1 just created — mirror its helper imports + structure)
    - .planning/phases/12-receitu-rio-teleodontologia/12-RESEARCH.md §Pattern 4 (teleconsultations + soap_records schema) + §Open Question 2 (CFO consent fields)
    - .planning/phases/12-receitu-rio-teleodontologia/12-VALIDATION.md §Per-Requirement Validation Map (TEL-01/TEL-02 rows)
  </read_first>
  <action>
Create `src/__tests__/teleodontologia/teleconsultations.test.ts` (source-inspection, TEL-01/TEL-02):
  - `vi.mock('server-only', () => ({}))`.
  - SRC on `src/actions/teleconsultations.ts`:
    - TEL-01: `createTeleconsultation` exported async (match `export async function createTeleconsultation`); references `external_link`, `consent_given`, `consent_given_at`; and records `consent_ip` SERVER-SIDE — assert it reads the IP from headers, not from client input (match `consent_ip` AND a `headers(` reference like `x-forwarded-for` or `headers()`). (Consent forgery mitigation — server-side IP/timestamp)
    - `startTeleconsultation` / `endTeleconsultation` exported async (match both); set `started_at` / `ended_at` and `status`.
    - TEL-02: `createSoapRecord` exported async; references BOTH `teleconsultation_id` AND `appointment_id` (links the SOAP note to the session and the atendimento) and the 4 SOAP fields `soap_subjective`/`soap_objective`/`soap_assessment`/`soap_plan`.
    - Security: matches `assertNotReadOnly`, `createClient` from `@/lib/supabase/server`, role gate referencing `dentist`, and `logBusinessEvent`. Tenant scope via `clinic_id`.
  - SRC on `src/lib/validators/teleconsultation.ts`: assert exported Zod schemas `teleconsultationSchema` + `soapSchema`; `external_link` validated as a URL (match `url(`); NO `.default(` anywhere (D-133/D-158). (Input validation)

Create `src/__tests__/teleodontologia/migrations-phase12-tel.test.ts` (migration source-inspection, TEL-01/TEL-02):
  - `_teleconsultations.sql` (via MM):
    - `CREATE TABLE public.teleconsultations` with `clinic_id`, `patient_id`, `professional_id`, `appointment_id`, `external_link`, `consent_given BOOLEAN`, `consent_given_at`, `consent_ip` (match `consent_ip`), `started_at`, `ended_at`, `status` with CHECK listing `agendada`/`em_andamento`/`concluida`/`cancelada` (separate `.toMatch` each), `deleted_at`, and `idx_teleconsultations_clinic` (match index + `clinic_id`). (TEL-01)
    - `CREATE TABLE public.soap_records` with `clinic_id`, `patient_id`, `appointment_id`, `teleconsultation_id`, and the 4 SOAP columns `soap_subjective`, `soap_objective`, `soap_assessment`, `soap_plan` (assert each), `deleted_at`, `idx_soap_records_clinic`. (TEL-02)
  - `_teleconsultations_rls.sql` (via MM): `ENABLE ROW LEVEL SECURITY` on `teleconsultations` AND `soap_records`; SELECT policy `clinic_id = get_my_tenant_id()`; write policy with BOTH `USING` + `WITH CHECK`. (Access Control)
  - REGRESSION re-assert (lightweight): `ALL()` contains NO `DROP CONSTRAINT no_overlap` and the Phase 12 tel migrations do NOT touch `public.appointments` GIST (assert the tel migration text does NOT match `EXCLUDE USING GIST`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/teleodontologia/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and the suite RUNS; new-artifact assertions are RED for not-yet-created targets.
    - The regression re-assert PASSES (no GIST/appointments tampering in any migration).
    - `npx tsc --noEmit` clean; full suite still runs (no crash).
  </acceptance_criteria>
  <done>teleconsultations.test.ts, migrations-phase12-tel.test.ts exist; RED for missing artifacts; tsc green; full suite runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → source files | Tests only READ source; no privilege boundary crossed. Scaffolds encode the security + LGPD + regression contract that later waves must satisfy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | accidental GIST/status break or Phase 8 signing engine edit | mitigate | migrations-phase12-rx.test.ts REGRESSION GUARD asserts no_overlap GIST + status CHECK unchanged, no DROP CONSTRAINT/ALTER COLUMN status in any migration, and sign-document.ts still exports signPdfBuffer |
| T-12-02 | Tampering | allergy check run against ciphertext (false negatives) | mitigate | clinical-documents.test.ts asserts the action imports decrypt from @/lib/crypto and calls checkMedicationAllergy; allergy-check.ts asserted PURE (no server-only) |
| T-12-03 | Tampering | re-signing an already-signed clinical document | mitigate | clinical-documents.test.ts asserts `.is('signature', null)` atomic guard (Phase 8 pattern) |
| T-12-04 | Repudiation | teleconsultation consent forgery | mitigate | teleconsultations.test.ts asserts consent_ip + consent_given_at recorded server-side from headers, not client input |
| T-12-05 | Information Disclosure | storage_path/PII exposure | mitigate | migration test asserts REVOKE on storage_path + content_json stored encrypted (TEXT not JSONB) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/receituario/ src/__tests__/teleodontologia/` runs without import crash (RED on new-artifact assertions is expected; regression blocks GREEN).
- `npx tsc --noEmit` clean.
- Existing suites unaffected: `npx vitest run src/__tests__/migrations/ src/__tests__/professionals/ src/__tests__/documents/` still green.
</verification>

<success_criteria>
- 5 scaffold files created covering all 5 requirements (RX-01..03, TEL-01..02) with grep-verifiable assertions.
- Passing regression guard locks the appointments GIST + status CHECK AND the Phase 8 signing engine.
- Test style matches the Phase 8–11 source-inspection + pure-unit convention; tsc green; existing tests untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/12-receitu-rio-teleodontologia/12-01-SUMMARY.md`
</output>
</content>
</invoke>
