---
phase: 07-sistema-multiunidade-pap-is
plan: 06
type: execute
wave: 3
depends_on: [04]
files_modified:
  - src/lib/validators/certificate.ts
  - src/actions/certificate.ts
  - src/actions/ai-agent-config.ts
  - src/app/(dashboard)/config/certificado/page.tsx
  - src/app/(dashboard)/config/perfis/page.tsx
  - src/app/(dashboard)/config/ia/page.tsx
  - src/components/config/CertificateUpload.tsx
  - src/components/config/AiAutonomyForm.tsx
  - src/components/config/PerfisMatrix.tsx
  - src/__tests__/config/certificate.test.ts
autonomous: false
requirements: [SYS-02, SYS-03, SYS-04, ROLE-02]

must_haves:
  truths:
    - "Admin uploads an ICP-Brasil A1 .pfx; the system extracts + displays metadata and rejects invalid/expired certs before saving"
    - "The .pfx bytes go to the private bucket via service role; the password is AES-256 encrypted; neither leaves the server"
    - "Admin sets autonomy level L0–L4 per AI agent and it persists to ai_agent_config"
    - "Perfis screen shows the role×module permission matrix (server-side source of truth)"
    - "auditor/dpo/socio cannot mutate cert or AI config (server-side gate)"
  artifacts:
    - path: "src/actions/certificate.ts"
      provides: "uploadCertificate Server Action: validate + extract + encrypt + store"
      contains: "extractPfxMetadata"
    - path: "src/actions/ai-agent-config.ts"
      provides: "get/save AI autonomy level per agent (admin-gated)"
      contains: "autonomy_level"
    - path: "src/app/(dashboard)/config/certificado/page.tsx"
      provides: "SYS-02 cert upload + metadata view"
      contains: "PageHeader"
    - path: "src/app/(dashboard)/config/ia/page.tsx"
      provides: "SYS-04 AI autonomy config screen"
      contains: "PageHeader"
  key_links:
    - from: "src/actions/certificate.ts"
      to: "icp-certificates bucket"
      via: "createAdminClient().storage.from('icp-certificates').upload"
      pattern: "icp-certificates"
    - from: "src/actions/certificate.ts"
      to: "src/lib/crypto.ts"
      via: "encrypt(password) → cert_password_enc"
      pattern: "encrypt\\("
---

<objective>
Deliver SYS-02 (ICP-Brasil A1 certificate upload + validation + secure keystore), SYS-04 (AI autonomy L0–L4 config per agent), and the SYS-03 perfis surface (read-only view of the role×module matrix that proxy.ts enforces). The cert flow validates the .pfx via node-forge, rejects expired/invalid certs, stores bytes in the private `icp-certificates` bucket via service role, and AES-encrypts the password — nothing sensitive ever reaches the client. All mutations are admin-gated and read-only-guarded.

Purpose: Completes the Phase 7 config surfaces. Cert keystore is consumed by Fase 8 (signing); AI config by Fase 10 (enforcement).
Output: certificado / ia / perfis routes + components, certificate + ai-agent-config Server Actions, validator, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-sistema-multiunidade-pap-is/07-CONTEXT.md
@.planning/phases/07-sistema-multiunidade-pap-is/07-RESEARCH.md

<interfaces>
<!-- Reuse EXACTLY. -->

node-forge extractor (src/lib/icp/pfx-metadata.ts — Plan 03):
```typescript
export function extractPfxMetadata(pfxBuffer: Buffer, password: string): CertificateMetadata
// { subject_cn, cnpj|null, cpf|null, not_before: Date, not_after: Date, thumbprint_sha1, serial_number, issuer_cn }
// throws Error('Certificado inválido ou senha incorreta.') on parse/password failure
```

crypto (src/lib/crypto.ts):
```typescript
import { encrypt } from '@/lib/crypto'   // encrypt(password) → certificates.cert_password_enc
```

admin client for Storage (src/lib/supabase/admin.ts):
```typescript
import { createAdminClient } from '@/lib/supabase/admin'   // service role; only reader/writer of icp-certificates
// path convention: `${clinicId}/${crypto.randomUUID()}.pfx`; upsert:false; contentType 'application/x-pkcs12'
```

Server Action role-gate (src/actions/collection-ruler.ts): getActor → ['admin','superadmin'] gate → logBusinessEvent.
Read-only guard (src/lib/auth/guards.ts): `await assertNotReadOnly()` first in mutations. For cert/ti also allow 'ti'.

RBAC matrix source of truth (src/proxy.ts — Plan 03): `MODULE_PERMISSIONS`, `isReadOnly`, NETWORK_ROLES/OPERATIONAL_ROLES/READ_ONLY_ROLES — import for the perfis display.

ai_agent_config (Plan 03/04): columns clinic_id, unit_id(nullable), agent_key ('confirmation'|'collection'), autonomy_level ('L0'..'L4'), enabled, limits jsonb, updated_by. UNIQUE(clinic_id, agent_key, unit_id). Seeded both agents at L0.

PageHeader + page role-gate + RHF/Zod + design tokens: same patterns referenced in Plan 05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Certificate validator + upload Server Action (validate, extract, encrypt, store)</name>
  <read_first>
    - src/lib/icp/pfx-metadata.ts (extractPfxMetadata contract + thrown error)
    - src/lib/crypto.ts (encrypt) + src/lib/supabase/admin.ts (createAdminClient)
    - src/actions/collection-ruler.ts (getActor + gate + logBusinessEvent)
    - src/lib/auth/guards.ts (assertNotReadOnly)
    - .planning/phases/07-sistema-multiunidade-pap-is/07-RESEARCH.md §"Pattern 6" (upload) + §"Security Domain" (5 MB limit, never plaintext password, never public URL)
  </read_first>
  <behavior>
    - certificateSchema validates: file present, MIME/extension .pfx or .p12, size ≤ 5 MB, password non-empty.
    - uploadCertificate(formData): assertNotReadOnly → getActor → gate ['admin','superadmin','ti'] → read file into Buffer → extractPfxMetadata(buffer, password) (throws → return friendly error) → reject if `not_after < now()` ("Certificado expirado") → upload bytes to icp-certificates via createAdminClient at `${tenant_id}/${uuid}.pfx` → INSERT into certificates {clinic_id: actor.tenant_id, subject_cn, cnpj, cpf, issuer_cn, serial_number, not_before, not_after, thumbprint_sha1, storage_path, cert_password_enc: encrypt(password), uploaded_by} → logBusinessEvent('certificate.uploaded', {subject_cn, thumbprint_sha1, not_after}) (no password/path in audit details).
    - getCertificate(): returns latest non-deleted certificate METADATA only for the tenant (never password/path to client beyond the storage_path being omitted from the returned shape).
  </behavior>
  <action>
    Create `src/lib/validators/certificate.ts` (zod) and `src/actions/certificate.ts` (`'use server'`) per the behavior block. Use `crypto.randomUUID()` for the storage path. The password is encrypted with `encrypt()` BEFORE insert — never store plaintext. Never return `storage_path` or `cert_password_enc` from `getCertificate()`.
  </action>
  <acceptance_criteria>
    - `src/actions/certificate.ts` contains `assertNotReadOnly`, `extractPfxMetadata`, `encrypt(`, `icp-certificates`, `createAdminClient`, and a `not_after < ` / expiry check
    - certificateSchema enforces a 5 MB max and a .pfx/.p12 type check
    - getCertificate return type does NOT include `cert_password_enc` or `storage_path`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Cert upload action validates, extracts metadata, rejects expired, stores bytes via service role, encrypts password, audits without secrets.</done>
</task>

<task type="auto">
  <name>Task 2: AI autonomy config action + certificado/ia/perfis pages + components</name>
  <read_first>
    - src/app/(dashboard)/clinica/financeiro/regua-de-cobranca/page.tsx (Server Component role gate + PageHeader + Alert)
    - src/components/financeiro/CollectionRulerForm.tsx (RHF + zodResolver + sonner toast)
    - src/proxy.ts (MODULE_PERMISSIONS / isReadOnly / role consts — for PerfisMatrix display)
    - CLAUDE.md §UI primitives (@base-ui render-prop, NO asChild; shadcn first; design tokens only)
    - .planning/phases/07-sistema-multiunidade-pap-is/07-CONTEXT.md §D-05 (autonomy stored only; enforcement Fase 10)
  </read_first>
  <action>
    Create `src/actions/ai-agent-config.ts` (`'use server'`): `listAiAgentConfig()` (tenant-scoped SELECT of confirmation+collection rows) and `saveAiAgentConfig(agentKey, autonomyLevel, enabled)` — zod (`autonomyLevel` enum L0..L4) → `await assertNotReadOnly()` → getActor → gate ['admin','superadmin'] → upsert on (clinic_id, agent_key, unit_id=null) → logBusinessEvent('ai_agent_config.saved', {agent_key, autonomy_level}).

    Create `src/app/(dashboard)/config/certificado/page.tsx` (Server Component, admin gate): PageHeader "Certificado ICP-Brasil"; load getCertificate(); render `<CertificateUpload current={metadata} />`.
    Create `src/app/(dashboard)/config/ia/page.tsx` (admin gate): PageHeader "Autonomia da IA"; load listAiAgentConfig(); render `<AiAutonomyForm agents={...} />`. Add a note that L0–L4 enforcement arrives in Fase 10.
    Create `src/app/(dashboard)/config/perfis/page.tsx` (admin gate): PageHeader "Perfis de Acesso"; render `<PerfisMatrix />` — a read-only table of MODULE_PERMISSIONS (role rows × module columns, marking allowed / read-only). Note: editable per-action permissions are deferred (D-03); this surface displays the enforced matrix.

    Create `src/components/config/CertificateUpload.tsx` (`'use client'`): file input (.pfx) + password field, RHF + certificateSchema, submit calls uploadCertificate (FormData), shows extracted metadata card (titular/CNPJ/validade/thumbprint) on success, sonner toast on error (incl. "Certificado expirado"/"inválido"). Design tokens only.
    Create `src/components/config/AiAutonomyForm.tsx` (`'use client'`): per agent (confirmation, collection) a Select L0–L4 + enabled toggle calling saveAiAgentConfig; sonner toast. Show human labels (L0 = sugere … L4 = executa).
    Create `src/components/config/PerfisMatrix.tsx` (`'use client'` or server): import MODULE_PERMISSIONS from @/proxy and render a shadcn table; allowed = check, readOnly = "somente leitura" badge. No raw slate/gray/white.
    Use @base-ui only where shadcn lacks a component (render-prop, never asChild).
  </action>
  <acceptance_criteria>
    - `src/actions/ai-agent-config.ts` contains `assertNotReadOnly`, `autonomy_level`, the L0..L4 enum, and admin gate
    - certificado/ia/perfis pages each contain `PageHeader` and a role gate (`Acesso restrito`)
    - `AiAutonomyForm.tsx` contains `saveAiAgentConfig` and L0..L4 options; `CertificateUpload.tsx` contains `uploadCertificate` and renders metadata; `PerfisMatrix.tsx` imports `MODULE_PERMISSIONS`
    - No raw `slate-`/`gray-`/`text-white`/`bg-white` in the 3 new components (grep none)
    - `npx tsc --noEmit` passes and `npx next build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>Certificado / IA / Perfis screens render in the v1 design system, admin-gated, wired to actions; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests for certificate validation + AI config gate + perfis matrix</name>
  <read_first>
    - src/__tests__/collection/ruler-config.test.ts (test style)
    - src/lib/validators/certificate.ts + src/actions/certificate.ts + src/actions/ai-agent-config.ts
    - src/proxy.ts (MODULE_PERMISSIONS shape)
  </read_first>
  <behavior>
    - certificateSchema rejects a >5 MB file and a non-.pfx type; accepts a valid .pfx ≤5 MB.
    - Source-inspection: certificate.ts contains `assertNotReadOnly`, `encrypt(`, `icp-certificates`, an expiry rejection; ai-agent-config.ts contains `assertNotReadOnly` + L0..L4 enum + admin gate.
    - MODULE_PERMISSIONS: every READ_ONLY_ROLES member that has a module has `readOnly: true`; PerfisMatrix data derives from it (assert all 11 roles present).
  </behavior>
  <action>
    Create `src/__tests__/config/certificate.test.ts` per the behavior block (zod validation + readFileSync source-inspection on the two action files + import MODULE_PERMISSIONS for the read-only-flag assertions).
  </action>
  <acceptance_criteria>
    - `src/__tests__/config/certificate.test.ts` exists
    - `npx vitest run src/__tests__/config/certificate.test.ts` GREEN
    - Asserts size/type rejection, presence of `assertNotReadOnly`+`encrypt`+expiry check in certificate.ts, L0..L4 + admin gate in ai-agent-config.ts, and read-only flags on auditor/dpo/socio in MODULE_PERMISSIONS
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/config/certificate.test.ts</automated>
  </verify>
  <done>Cert validation, AI-config gating, and read-only matrix covered; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client upload → Server Action | .pfx + password are untrusted; processed server-side only |
| Server Action → private Storage | only service role touches icp-certificates; bytes never reach client |
| extracted cert metadata | attacker-controlled certificate subject |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-18 | Information Disclosure | .pfx bytes or password leaking to client | mitigate | bytes uploaded via service-role admin client to public=false bucket; password AES-256 encrypted before insert; getCertificate omits storage_path + cert_password_enc |
| T-07-19 | Elevation of Privilege | read-only/non-admin uploading cert or raising AI autonomy | mitigate | `assertNotReadOnly()` + admin/superadmin(/ti for cert) gate in both actions; RLS write policy (Plan 03) backstop |
| T-07-20 | Denial of Service | oversized .pfx upload | mitigate | zod 5 MB size cap rejected before forwarding to Storage |
| T-07-21 | Tampering | malicious certificate subject (CNPJ) used downstream | mitigate | extracted metadata is display-only, parameterized inserts; expired certs rejected (`not_after < now()`) |
| T-07-22 | Spoofing | invalid cert / wrong password accepted | mitigate | extractPfxMetadata throws on bad password/parse → upload aborted with friendly error |
</threat_model>

<verification>
- `npx vitest run src/__tests__/config/certificate.test.ts` GREEN; `npx tsc --noEmit` + `npx next build` clean.
- Cert action: validate → extract → reject expired → service-role upload → AES password → audit (no secrets).
- AI config + perfis admin-gated; matrix shows read-only roles.
- Manual UAT: valid A1 accepted (metadata shown), expired rejected; autonomy persists; both themes (07-VALIDATION manual list).
</verification>

<success_criteria>
- Admin uploads/validates ICP A1 cert; secrets never leave the server.
- Admin sets L0–L4 per agent (persisted).
- Perfis matrix visible; read-only roles cannot mutate.
- Build + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sistema-multiunidade-pap-is/07-06-SUMMARY.md`
</output>
