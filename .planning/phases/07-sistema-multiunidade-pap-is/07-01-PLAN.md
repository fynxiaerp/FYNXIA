---
phase: 07-sistema-multiunidade-pap-is
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - src/lib/auth/guards.ts
  - src/__tests__/migrations/phase7.test.ts
  - src/__tests__/rbac/matrix.test.ts
  - src/__tests__/icp/pfx-metadata.test.ts
  - src/__tests__/icp/fixtures/test-cert.pfx
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03, SYS-04, SYS-05, ROLE-01, ROLE-02]
user_setup: []

must_haves:
  truths:
    - "node-forge + @types/node-forge are installed and importable server-side"
    - "Source-inspection test scaffolds exist and FAIL (RED) against not-yet-written migrations/proxy"
    - "assertNotReadOnly() guard helper exists and throws when x-read-only header is true"
    - "A synthetic PKCS12 test fixture exists for node-forge extraction unit tests"
  artifacts:
    - path: "src/lib/auth/guards.ts"
      provides: "assertNotReadOnly() server-only guard for mutation Server Actions"
      contains: "export function assertNotReadOnly"
    - path: "src/__tests__/migrations/phase7.test.ts"
      provides: "Source-inspection RED tests for all Phase 7 migrations"
      contains: "Phase 7 migrations"
    - path: "src/__tests__/rbac/matrix.test.ts"
      provides: "RED tests for MODULE_PERMISSIONS + isReadOnly"
      contains: "MODULE_PERMISSIONS"
    - path: "src/__tests__/icp/pfx-metadata.test.ts"
      provides: "RED unit tests for extractPfxMetadata"
      contains: "extractPfxMetadata"
  key_links:
    - from: "src/lib/auth/guards.ts"
      to: "next/headers"
      via: "headers().get('x-read-only')"
      pattern: "x-read-only"
---

<objective>
Wave 0 foundation: install the one new dependency (`node-forge`), create the `assertNotReadOnly()` server-side guard that downstream Server Actions will call, and write the source-inspection / unit test scaffolds (RED) that subsequent waves turn GREEN. Per 07-VALIDATION.md, the Nyquist contract requires tests to exist BEFORE the migrations/proxy/lib they validate.

Purpose: Establish the test contracts and security guard so every later task has a target to satisfy and the executor never explores the codebase to learn the contract.
Output: `node-forge` installed, `src/lib/auth/guards.ts`, three test files (intentionally RED), and a synthetic `.pfx` fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sistema-multiunidade-pap-is/07-RESEARCH.md
@.planning/phases/07-sistema-multiunidade-pap-is/07-VALIDATION.md

<interfaces>
<!-- Contracts the test scaffolds assert against. These are the targets for Waves 1–3. -->
<!-- Executor: write tests AGAINST these names; do NOT invent different signatures. -->

Existing test convention (src/__tests__/migrations/schema.test.ts):
```typescript
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
const M = (f: string) => readFileSync(join(process.cwd(), 'supabase/migrations', f), 'utf8')
// then: expect(M('file.sql')).toMatch(/CREATE TABLE public\.x/i)
```

Existing crypto (src/lib/crypto.ts — reuse, do NOT re-implement):
```typescript
export function encrypt(plaintext: string): string   // "iv:authTag:ciphertext" hex
export function decrypt(ciphertext: string): string
```

Target signatures created in later waves (assert these names exist):
```typescript
// src/lib/auth/guards.ts (THIS plan creates it)
export function assertNotReadOnly(): void   // throws if headers().get('x-read-only') === 'true'

// src/proxy.ts (Plan 03 creates these — FULL 7-module set, RESEARCH Pattern 3)
type ModuleKey = 'clinica'|'config'|'superadmin'|'paciente'|'financeiro'|'ia'|'bi'
export const MODULE_PERMISSIONS: Record<AppRole, Partial<Record<ModuleKey, ModuleAccess>>>
export function isReadOnly(role: string, pathname: string): boolean   // most-specific module wins
export function isPathAllowed(role: string, pathname: string): boolean

// src/lib/icp/pfx-metadata.ts (Plan 03 creates it)
export function extractPfxMetadata(pfxBuffer: Buffer, password: string): CertificateMetadata
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install node-forge dependency</name>
  <read_first>
    - package.json (confirm node-forge / @types/node-forge are NOT already present)
    - .planning/phases/07-sistema-multiunidade-pap-is/07-RESEARCH.md §"New Dependency Required" (node-forge 1.4.0, pure JS, Vercel Node.js runtime)
  </read_first>
  <action>
    Run `npm install node-forge` then `npm install --save-dev @types/node-forge`.
    node-forge 1.4.0 is pure JS (no native binaries) — verified safe for Vercel Fluid Compute (Node.js runtime). Used in Plan 03 for PKCS12 (.pfx) metadata extraction.
    Do NOT add any other dependency. Do NOT touch unrelated package.json fields.
  </action>
  <acceptance_criteria>
    - `npm ls node-forge` exits 0 and shows node-forge in the tree
    - package.json `dependencies` contains a `node-forge` entry
    - package.json `devDependencies` contains a `@types/node-forge` entry
    - `npx tsc --noEmit` still passes (no broken types from install)
  </acceptance_criteria>
  <verify>
    <automated>npm ls node-forge && npx tsc --noEmit</automated>
  </verify>
  <done>node-forge and @types/node-forge are in package.json and resolvable; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create assertNotReadOnly() server guard</name>
  <read_first>
    - src/lib/crypto.ts (the `import 'server-only'` pattern at top of server-only modules)
    - src/proxy.ts (how `x-user-role` request header is set — `x-read-only` will be set the same way in Plan 03)
    - .planning/phases/07-sistema-multiunidade-pap-is/07-RESEARCH.md §"Pitfall 5: Server Actions Bypass Middleware Read-Only Gate"
  </read_first>
  <action>
    Create `src/lib/auth/guards.ts`:
    - First line: `import 'server-only'`
    - Import `headers` from `next/headers`.
    - Export `export async function assertNotReadOnly(): Promise<void>` that reads `(await headers()).get('x-read-only')` and, if it equals the string `'true'`, throws `new Error('Acesso somente leitura: este papel não pode alterar dados.')`.
    - Add a JSDoc explaining: call this at the TOP of every mutation Server Action touched by auditor/dpo/socio modules. Middleware sets the header (Plan 03); Server Actions are direct POSTs and bypass middleware redirect, so the guard is mandatory at the action layer.
    - Note in JSDoc: in Next.js 15 `headers()` is async — must be awaited.
  </action>
  <acceptance_criteria>
    - File `src/lib/auth/guards.ts` exists
    - Contains `import 'server-only'`
    - Contains `export async function assertNotReadOnly`
    - Contains the string `x-read-only`
    - Throws when header value is `'true'` (string literal check)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Guard module exists, server-only, reads x-read-only header, throws on read-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED test scaffolds for migrations + RBAC matrix</name>
  <read_first>
    - src/__tests__/migrations/schema.test.ts (the `M(f)` readFileSync + toMatch convention — copy exactly)
    - src/__tests__/proxy/rbac.test.ts (existing isPathAllowed test style + the vi.mock('server-only') / vi.mock supabase blocks)
    - .planning/phases/07-sistema-multiunidade-pap-is/07-RESEARCH.md §"Pattern 3" (the AUTHORITATIVE 7-module matrix) + §"Validation Architecture → Phase Requirements → Test Map"
    - .planning/phases/07-sistema-multiunidade-pap-is/07-VALIDATION.md §"Per-Requirement Validation Map"
  </read_first>
  <behavior>
    These tests MUST be RED now (files they assert don't exist yet) and GREEN after Waves 1–3.
    NOTE: this plan creates exactly TWO test files — `src/__tests__/migrations/phase7.test.ts` (ALL migration assertions) and `src/__tests__/rbac/matrix.test.ts` (ALL RBAC assertions). 07-VALIDATION.md references these two paths (not per-requirement split filenames). Cover:
    - migrations/phase7.test.ts (source-inspection on migration files Plan 02 + Plan 03 write):
      - `units` table: `CREATE TABLE public.units`, `clinic_id` FK `REFERENCES public.clinics`, `is_default`, `deleted_at`, index on `clinic_id`
      - `clinics.regime_tributario` (Plan 02 migration `20260614000150_clinics_regime.sql`): `ADD COLUMN regime_tributario`, CHECK with `simples_nacional`, `lucro_presumido`, `lucro_real`, `mei`
      - `user_units` N:N: `CREATE TABLE public.user_units`, `UNIQUE (user_id, unit_id)`, indexes on user_id/unit_id/clinic_id
      - `get_my_unit_ids()`: `RETURNS UUID\[\]`, `SECURITY DEFINER`, `SET search_path = public`, `REVOKE EXECUTE`
      - role expansion: CHECK on `public.users` includes all of `dpo|auditor|socio|ti|implantacao|aluno`; SAME on `public.invitations`
      - unit_id backfill: `ADD COLUMN unit_id`, an `UPDATE` backfill, then `SET NOT NULL` on appointments AND charges AND receivables
      - `certificates` table: `CREATE TABLE public.certificates`, columns `subject_cn`, `cnpj`, `not_before`, `not_after`, `thumbprint_sha1`, `storage_path`, `cert_password_enc`
      - `icp-certificates` private bucket: `INSERT INTO storage.buckets`, `'icp-certificates'`, `public` set to `false`
      - `ai_agent_config` table: `CREATE TABLE public.ai_agent_config`, `agent_key`, `autonomy_level` CHECK `L0|L1|L2|L3|L4`, `enabled`, `limits` jsonb, `updated_by`, and a PARTIAL unique index `WHERE unit_id IS NULL` (INFO 8)
    - rbac/matrix.test.ts (import from @/proxy):
      - `MODULE_PERMISSIONS` defines all 11 roles (admin, superadmin, dentist, receptionist, patient, dpo, auditor, socio, ti, implantacao, aluno)
      - read-only contract (ROLE-02 — these are REQUIRED and currently the regression target):
        - `isReadOnly('auditor', '/clinica/financeiro')` === true
        - `isReadOnly('socio', '/clinica/financeiro')`   === true
        - `isReadOnly('socio', '/bi')`                     === true
        - `isReadOnly('auditor', '/clinica')`              === true
        - `isReadOnly('admin', '/clinica')`               === false
      - `isPathAllowed('ti', '/config')` === true; `isPathAllowed('dentist', '/config')` === false
      - `isPathAllowed('socio', '/clinica/financeiro')` === true
    Reuse the vi.mock blocks from rbac.test.ts so importing @/proxy works in the test env.
  </behavior>
  <action>
    Create `src/__tests__/migrations/phase7.test.ts` and `src/__tests__/rbac/matrix.test.ts` exactly per the behavior block. Use the `M()` helper for migration tests. Reference migration filenames Plan 02/03 will create (units `20260614000100`, clinics_regime `20260614000150`, user_units/get_my_unit_ids `20260614000300`, roles `20260614000400`, certificates `20260614000500`, ai_agent_config `20260614000600`, operational unit_id `20260614000700`). It is OK and EXPECTED that these tests fail now (RED).
  </action>
  <acceptance_criteria>
    - Both test files exist (exactly these two paths)
    - `npx vitest run src/__tests__/rbac/matrix.test.ts` runs and FAILS (RED — MODULE_PERMISSIONS not yet exported)
    - `npx vitest run src/__tests__/migrations/phase7.test.ts` runs and FAILS (RED — migration files not yet written)
    - migrations test contains literal assertions for: `public.units`, `regime_tributario`, `public.user_units`, `get_my_unit_ids`, `public.certificates`, `ai_agent_config`, all 6 new role strings, and the partial unique index `WHERE unit_id IS NULL`
    - matrix test asserts `isReadOnly('auditor','/clinica/financeiro') === true` AND `isReadOnly('socio','/clinica/financeiro') === true` AND references `MODULE_PERMISSIONS`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/rbac/matrix.test.ts src/__tests__/migrations/phase7.test.ts; if [ $? -ne 0 ]; then echo "RED as expected"; fi</automated>
  </verify>
  <done>Migration + RBAC test scaffolds exist and are RED, targeting the exact contracts Waves 1–3 deliver (incl. regime_tributario + financeiro read-only).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Generate .pfx fixture + RED pfx-metadata test</name>
  <read_first>
    - src/__tests__/migrations/schema.test.ts (test file structure)
    - .planning/phases/07-sistema-multiunidade-pap-is/07-RESEARCH.md §"Pattern 4: node-forge PKCS12 Metadata Extraction" (the CertificateMetadata shape + extractPfxMetadata signature)
  </read_first>
  <behavior>
    - Generate a self-signed PKCS12 test fixture at `src/__tests__/icp/fixtures/test-cert.pfx` with a known password (e.g. `test1234`) using openssl. This is a SYNTHETIC cert (no real ICP-Brasil OIDs required) — sufficient to assert subject CN, validity dates, and thumbprint extraction.
    - Write `src/__tests__/icp/pfx-metadata.test.ts` that imports `extractPfxMetadata` from `@/lib/icp/pfx-metadata` (created in Plan 03) and asserts: returns an object with `subject_cn` (string), `not_before` / `not_after` (Date), `thumbprint_sha1` (hex string), and tolerates `cnpj === null` for a synthetic cert. This test is RED now.
  </behavior>
  <action>
    Generate fixture (run from repo root; requires openssl which is available on the platform):
    `openssl req -x509 -newkey rsa:2048 -keyout /tmp/k.pem -out /tmp/c.pem -days 365 -nodes -subj "/CN=FYNXIA Teste/O=FYNXIA"`
    then `openssl pkcs12 -export -in /tmp/c.pem -inkey /tmp/k.pem -out src/__tests__/icp/fixtures/test-cert.pfx -passout pass:test1234`.
    (On Windows PowerShell use the equivalent openssl invocation; openssl ships with Git.)
    Create the test file `src/__tests__/icp/pfx-metadata.test.ts` reading the fixture with `readFileSync(..., null)` to get a Buffer and calling `extractPfxMetadata(buffer, 'test1234')`. Assert the shape per the behavior block. It is OK that it is RED now.
  </action>
  <acceptance_criteria>
    - File `src/__tests__/icp/fixtures/test-cert.pfx` exists and is non-empty (`> 0` bytes)
    - File `src/__tests__/icp/pfx-metadata.test.ts` exists and imports `extractPfxMetadata`
    - `npx vitest run src/__tests__/icp/pfx-metadata.test.ts` runs and FAILS (RED — pfx-metadata.ts not yet written)
    - Test asserts `subject_cn`, `not_before`, `not_after`, `thumbprint_sha1` and `cnpj` nullable
  </acceptance_criteria>
  <verify>
    <automated>test -s src/__tests__/icp/fixtures/test-cert.pfx && (npx vitest run src/__tests__/icp/pfx-metadata.test.ts; echo "RED expected")</automated>
  </verify>
  <done>Synthetic .pfx fixture exists; pfx-metadata test is RED against the Plan 03 extractor contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action ← client POST | Mutation actions are invoked directly; middleware cannot redirect them |
| test fixture | Synthetic .pfx never contains real ICP-Brasil private keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | mutation Server Actions reachable by read-only roles | mitigate | `assertNotReadOnly()` created here; Plan 03 wires header; Plans 05/06 call it in every mutation action |
| T-07-02 | Information Disclosure | test .pfx fixture committed to repo | accept | Synthetic self-signed cert with throwaway password `test1234`; no production secret value |
| T-07-03 | Tampering | node-forge supply-chain | accept | Pinned version 1.4.0, pure-JS, widely used; lockfile committed |
</threat_model>

<verification>
- `npm ls node-forge` resolves; tsc clean.
- `src/lib/auth/guards.ts` exports `assertNotReadOnly` (server-only, reads x-read-only).
- Three test files exist and are RED (their targets are written in Waves 1–3).
- `.pfx` fixture exists and is non-empty.
</verification>

<success_criteria>
- node-forge + @types installed; tsc passes.
- Guard helper in place.
- All Phase 7 test scaffolds present and intentionally failing (RED), matching exact contracts for later waves (incl. regime_tributario + financeiro/bi read-only).
</success_criteria>

<output>
After completion, create `.planning/phases/07-sistema-multiunidade-pap-is/07-01-SUMMARY.md`
</output>
