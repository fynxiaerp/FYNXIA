---
phase: 07-sistema-multiunidade-pap-is
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - src/lib/validators/empresa.ts
  - src/lib/validators/unit.ts
  - src/actions/empresa.ts
  - src/actions/units.ts
  - src/app/(dashboard)/config/empresa/page.tsx
  - src/components/config/EmpresaForm.tsx
  - src/components/config/UnitsManager.tsx
  - src/__tests__/config/empresa.test.ts
autonomous: false
requirements: [SYS-01, ROLE-02]

must_haves:
  truths:
    - "Admin edits the rede (CNPJ/CPF validated + regime tributário) and it persists to public.clinics"
    - "Admin creates/edits/deactivates units (filiais) with CNPJ + slug + endereço"
    - "Non-admin and read-only roles cannot mutate empresa/units (server-side gate)"
  artifacts:
    - path: "src/actions/units.ts"
      provides: "createUnit/updateUnit/listUnits Server Actions (admin-gated)"
      contains: "assertNotReadOnly"
    - path: "src/app/(dashboard)/config/empresa/page.tsx"
      provides: "SYS-01 company + units config screen"
      contains: "PageHeader"
    - path: "src/lib/validators/empresa.ts"
      provides: "Zod schema: CNPJ/CPF + regime tributário validation"
      contains: "regime"
  key_links:
    - from: "src/actions/units.ts"
      to: "public.units"
      via: "supabase.from('units').insert/update"
      pattern: "from\\('units'\\)"
    - from: "src/components/config/EmpresaForm.tsx"
      to: "src/actions/empresa.ts"
      via: "RHF onSubmit → saveEmpresa server action"
      pattern: "saveEmpresa"
---

<objective>
Deliver SYS-01: the company/rede configuration screen where an admin edits the rede record (CNPJ/CPF with validation + regime tributário) and manages the network of units (filiais) — create, edit, activate/deactivate. Mutations are gated server-side (admin/superadmin only + `assertNotReadOnly()`), satisfying ROLE-02 read-only enforcement at the action layer.

Purpose: First user-facing multiunidade surface; persists to the `public.clinics` (rede) + `public.units` tables landed in Plan 02 and now live (Plan 04).
Output: empresa config route, EmpresaForm + UnitsManager, validators, Server Actions, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-sistema-multiunidade-pap-is/07-CONTEXT.md
@.planning/phases/07-sistema-multiunidade-pap-is/07-RESEARCH.md

<interfaces>
<!-- Reuse these EXACT patterns. The v1 design system + action conventions. -->

PageHeader (src/components/shell/PageHeader.tsx):
```typescript
<PageHeader title="Empresa & Unidades" breadcrumbs={[{label:'Configurações', href:'/config'},{label:'Empresa'}]} />
```

Server Action + role-gate pattern (src/actions/collection-ruler.ts — COPY this structure):
```typescript
'use server'
import { z } from 'zod'
import { createClient } from '@/lib/supabase/server'
import { logBusinessEvent } from '@/lib/audit'
// getActor(): reads auth.getUser() then public.users {id, tenant_id, role}
// mutation: zod safeParse → getActor → role gate (['admin','superadmin']) → supabase mutation → logBusinessEvent
```

Read-only guard (src/lib/auth/guards.ts — Plan 01):
```typescript
import { assertNotReadOnly } from '@/lib/auth/guards'
await assertNotReadOnly()   // call FIRST in every mutation action (throws for auditor/dpo/socio)
```

CNPJ/CPF validation (cpf-cnpj-validator — already installed):
```typescript
import { cnpj, cpf } from 'cpf-cnpj-validator'
cnpj.isValid('11222333000181')  // boolean
```

Page role-gate UI pattern (src/app/(dashboard)/clinica/financeiro/regua-de-cobranca/page.tsx):
```typescript
// Server Component: getUser → read users.role → non-admin renders <Alert>Acesso restrito</Alert> (NO redirect)
```

clinics (rede) columns available: id, name, slug, cnpj, phone, address, specialty, logo_url.
  → regime tributário is NEW. Store it: add it to clinics via this plan's first action? NO — schema is frozen post-Plan 04.
  → Store regime tributário in the existing `clinics.specialty`? NO. Instead: this plan adds a small migration is NOT allowed in Wave 3.
  → DECISION: regime tributário is persisted on `clinics` using a column that MUST already exist. Since it does not, store it inside the existing encrypted/塊? 
  → CORRECT APPROACH: regime tributário column belongs in Plan 02's clinics expansion. See action notes — if missing, this plan is BLOCKED and must request a schema addition. Verify column presence against src/types/database.types.ts FIRST.
units columns: id, clinic_id, name, cnpj, slug, phone, address, is_default, ativo, deleted_at.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validators + Server Actions for empresa + units</name>
  <read_first>
    - src/types/database.types.ts (CONFIRM the `clinics` row type and whether a `regime_tributario` column exists; CONFIRM the `units` row type from Plan 04)
    - src/actions/collection-ruler.ts (getActor + role-gate + logBusinessEvent pattern — copy)
    - src/lib/auth/guards.ts (assertNotReadOnly signature)
    - .planning/phases/07-sistema-multiunidade-pap-is/07-CONTEXT.md §D-01 (units fields) + §Claude's Discretion (CNPJ/regime validation)
  </read_first>
  <action>
    FIRST verify in database.types.ts whether `clinics` has a column for regime tributário. The Plan 02 clinics expansion did NOT add one. To keep SYS-01 at full fidelity ("regime tributário"), store regime tributário as a new `clinics` column. Since schema is frozen after Plan 04, add a SMALL migration `supabase/migrations/20260614000800_clinics_regime.sql` (`ALTER TABLE public.clinics ADD COLUMN regime_tributario TEXT CHECK (regime_tributario IN ('simples_nacional','lucro_presumido','lucro_real','mei'));`) AND re-run `supabase db push` + `supabase gen types` at the start of this task (re-auth gotcha applies — if push needs human re-auth, surface it). This keeps SYS-01 complete rather than dropping regime.
    NOTE: add this migration filename to files_modified mentally; it is a small additive column.

    Create `src/lib/validators/empresa.ts`: Zod schema `{ name: string min 2, cnpj_or_cpf: string (validate via cpf-cnpj-validator — accept either), regime_tributario: enum(['simples_nacional','lucro_presumido','lucro_real','mei']), phone?, address? }`. Export a `.refine` that runs cnpj.isValid OR cpf.isValid on the document.
    Create `src/lib/validators/unit.ts`: Zod schema `{ name: string min 2, cnpj?: string (cnpj.isValid if present), slug: string regex /^[a-z0-9-]+$/, phone?, address?, ativo: boolean }`.

    Create `src/actions/empresa.ts` (`'use server'`): `getEmpresa()` (reads clinics row for actor.tenant_id) and `saveEmpresa(input)` — zod parse → `await assertNotReadOnly()` → getActor → gate role ∈ ['admin','superadmin'] → `supabase.from('clinics').update({...}).eq('id', actor.tenant_id)` → logBusinessEvent('empresa.saved', non-PII fields).
    Create `src/actions/units.ts` (`'use server'`): `listUnits()` (tenant-scoped SELECT), `createUnit(input)` and `updateUnit(id, input)` — each: zod parse → `await assertNotReadOnly()` → getActor → gate ['admin','superadmin'] → insert/update with `clinic_id = actor.tenant_id` (NEVER trust a client clinic_id) → logBusinessEvent. Deactivation = updateUnit with `ativo:false`. Prevent deactivating the `is_default` unit (return error).
  </action>
  <acceptance_criteria>
    - `src/lib/validators/empresa.ts` contains `regime` and a cpf/cnpj `.refine`
    - `src/lib/validators/unit.ts` contains a slug regex and `ativo`
    - `src/actions/empresa.ts` + `src/actions/units.ts` each contain `assertNotReadOnly`, a role gate on `['admin','superadmin']`, and `logBusinessEvent`
    - units actions set `clinic_id` from `actor.tenant_id` (grep `actor.tenant_id`), never from input
    - `npx tsc --noEmit` passes (compiles against regenerated types)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Validators + admin-gated, read-only-guarded Server Actions for empresa + units, persisting to clinics/units with tenant scoping.</done>
</task>

<task type="auto">
  <name>Task 2: Empresa page + EmpresaForm + UnitsManager UI</name>
  <read_first>
    - src/app/(dashboard)/clinica/financeiro/regua-de-cobranca/page.tsx (Server Component role-gate + PageHeader + Alert "Acesso restrito" pattern)
    - src/components/financeiro/CollectionRulerForm.tsx (RHF + Zod resolver + @base-ui render-prop / shadcn form pattern — copy)
    - src/components/shell/PageHeader.tsx
    - CLAUDE.md §"UI Component Primitives" (@base-ui render-prop, NO asChild; shadcn first) + design tokens (no raw slate/gray/white)
  </read_first>
  <action>
    Create `src/app/(dashboard)/config/empresa/page.tsx` (Server Component): auth + read users.role; non-admin → in-page `<Alert>Acesso restrito...</Alert>` (NO redirect, per v1 convention). Admin → `<PageHeader title="Empresa & Unidades" breadcrumbs={[{label:'Configurações', href:'/config'},{label:'Empresa'}]} />`, load `getEmpresa()` + `listUnits()`, render `<EmpresaForm initial={...} />` and `<UnitsManager units={...} />`.
    Create `src/components/config/EmpresaForm.tsx` (`'use client'`): RHF + zodResolver(empresaSchema), fields name / documento (CNPJ-CPF masked) / regime tributário (shadcn Select) / phone / address; submit calls `saveEmpresa`; toast via sonner on success/error. Use design tokens only (bg-background, text-foreground, border-border) — NO raw slate/gray/white.
    Create `src/components/config/UnitsManager.tsx` (`'use client'`): a shadcn table/list of units (name, cnpj, slug, ativo badge, default badge) + a dialog form (RHF + unitSchema) to create/edit a unit calling createUnit/updateUnit; an "ativo" toggle calling updateUnit. Disable deactivation on the default unit. nuqs/Zustand not required here; local state is fine for the dialog.
    Reuse @base-ui primitives only where shadcn lacks an equivalent (per CLAUDE.md); use render-prop, never `asChild`.
  </action>
  <acceptance_criteria>
    - `src/app/(dashboard)/config/empresa/page.tsx` contains `PageHeader`, a role gate (`Acesso restrito`), `getEmpresa`, `listUnits`
    - `EmpresaForm.tsx` contains `zodResolver`, `saveEmpresa`, `regime`, and a sonner toast
    - `UnitsManager.tsx` contains `createUnit`, `updateUnit`, an `ativo` control, and disables deactivating `is_default`
    - No raw `slate-`/`gray-`/`text-white`/`bg-white` Tailwind classes (grep returns none in the 3 new UI files)
    - `npx tsc --noEmit` passes and `npx next build` succeeds (catches `'use server'`/`'use client'` boundary errors that tsc/vitest miss)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>Empresa/Unidades screen renders in the v1 design system, admin-gated, wired to the Server Actions; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests for empresa/units validation + read-only gate</name>
  <read_first>
    - src/__tests__/collection/ruler-config.test.ts (validation + action-shape test style)
    - src/lib/validators/empresa.ts + src/lib/validators/unit.ts
    - src/actions/empresa.ts + src/actions/units.ts
  </read_first>
  <behavior>
    - empresaSchema rejects an invalid CNPJ/CPF and an unknown regime; accepts a valid CNPJ + 'simples_nacional'.
    - unitSchema rejects an invalid slug ('Bad Slug!') and accepts 'unidade-centro'.
    - Source-inspection: both action files contain `assertNotReadOnly` and a `['admin','superadmin']` gate, and units actions reference `actor.tenant_id` (not an input clinic_id).
  </behavior>
  <action>
    Create `src/__tests__/config/empresa.test.ts` per the behavior block. Import the zod schemas directly (pure validation — no DB). Use readFileSync source-inspection for the action-file guard assertions (mirrors v1 convention).
  </action>
  <acceptance_criteria>
    - `src/__tests__/config/empresa.test.ts` exists
    - `npx vitest run src/__tests__/config/empresa.test.ts` GREEN
    - Test asserts invalid-document rejection, slug rejection, and presence of `assertNotReadOnly` + admin gate in both action files
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/config/empresa.test.ts</automated>
  </verify>
  <done>Validation + read-only gate covered; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → Server Action | All empresa/unit input is untrusted; validated + role-gated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Elevation of Privilege | non-admin or read-only role mutating empresa/units | mitigate | `assertNotReadOnly()` + `getActor()` role gate `['admin','superadmin']` in every mutation action; RLS write policy (Plan 02) is the DB backstop |
| T-07-16 | Tampering | client supplying a clinic_id for another tenant on unit create | mitigate | Actions set `clinic_id = actor.tenant_id` from the session, ignore any client-supplied clinic_id; RLS WITH CHECK enforces tenant |
| T-07-17 | Input Validation | malformed CNPJ/regime persisted | mitigate | zod schema + cpf-cnpj-validator refine; regime constrained to enum + DB CHECK |
</threat_model>

<verification>
- `npx vitest run src/__tests__/config/empresa.test.ts` GREEN; `npx tsc --noEmit` + `npx next build` clean.
- Mutations call assertNotReadOnly + admin gate; clinic_id sourced from session.
- Manual UAT: admin edits empresa + adds a unit in both themes (07-VALIDATION manual list).
</verification>

<success_criteria>
- Admin manages rede + units with validated CNPJ/CPF + regime tributário, persisted to clinics/units.
- Non-admin/read-only blocked server-side.
- Build + tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sistema-multiunidade-pap-is/07-05-SUMMARY.md`
</output>
