---
phase: 07-sistema-multiunidade-pap-is
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - src/types/database.types.ts
autonomous: false
requirements: [SYS-01, SYS-02, SYS-04, SYS-05, ROLE-01]

must_haves:
  truths:
    - "All Phase 7 migrations are applied to the live Supabase project jqjwyqlbbuqnrffdnlpp"
    - "src/types/database.types.ts is regenerated from the live DB and includes units, user_units, certificates, ai_agent_config"
    - "Existing operational rows have non-null unit_id after the backfill ran live"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "Regenerated DB types reflecting Phase 7 schema"
      contains: "units"
  key_links:
    - from: "supabase db push"
      to: "Supabase project jqjwyqlbbuqnrffdnlpp"
      via: "supabase CLI linked to org kczvihafddupruvsrrsc"
      pattern: "jqjwyqlbbuqnrffdnlpp"
---

<objective>
[BLOCKING] Apply all Phase 7 migrations to the live Supabase project and regenerate `src/types/database.types.ts` from the live schema. Per 07-VALIDATION.md, build + tsc pass WITHOUT this push (types are stale/config-derived), creating a false-positive — so the live push is mandatory and gated on human Supabase re-auth.

Purpose: Prove the schema is real, the backfills ran on production data, and downstream UI plans (05/06) compile against the actual generated types.
Output: Migrations applied; database.types.ts regenerated; post-push data checks confirm backfill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-sistema-multiunidade-pap-is/07-RESEARCH.md
@.planning/phases/07-sistema-multiunidade-pap-is/07-VALIDATION.md

<interfaces>
<!-- The Supabase CLI auth gotcha (STATE.md + 07-RESEARCH Pitfall 6). -->
Project ref: jqjwyqlbbuqnrffdnlpp
Org:         kczvihafddupruvsrrsc (sa-east-1)
Gotcha:      CLI often stays logged into the wrong org (nexus-*) → re-login + re-link before push.
Type gen:    supabase gen types typescript --project-id jqjwyqlbbuqnrffdnlpp > src/types/database.types.ts
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Supabase CLI re-auth to org kczvihafddupruvsrrsc / project jqjwyqlbbuqnrffdnlpp</name>
  <action>
    Human re-authenticates and links the Supabase CLI to the correct project BEFORE the live db push. Claude cannot complete the browser login or confirm the active org. Follow the verify steps below, then resume.
  </action>
  <what-built>
    Plans 02 + 03 wrote 7 migration files (units, units_rls, user_units + get_my_unit_ids, role_expansion, certificates + icp-certificates bucket, ai_agent_config, operational unit_id backfill). They are NOT yet applied to the live DB.
  </what-built>
  <how-to-verify>
    Human must re-authenticate the Supabase CLI to the correct org/project before the push (documented gotcha — CLI frequently logged into org `nexus-*`):
    1. Run `supabase login` (browser auth) if a token prompt appears.
    2. Run `supabase projects list` and CONFIRM project `jqjwyqlbbuqnrffdnlpp` shows the active marker (●) under org `kczvihafddupruvsrrsc`.
    3. Run `supabase link --project-ref jqjwyqlbbuqnrffdnlpp` if not already linked.
    Reply once `supabase projects list` shows `jqjwyqlbbuqnrffdnlpp` as active.
  </how-to-verify>
  <resume-signal>Type "linked" once projects list confirms jqjwyqlbbuqnrffdnlpp is active under kczvihafddupruvsrrsc</resume-signal>
</task>

<task type="auto">
  <name>Task 1: supabase db push + regenerate types + post-push data checks</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - supabase/migrations/20260614000100_units_table.sql .. 20260614000700_operational_unit_id.sql (the 7 Phase 7 files — confirm they are all present and ordered)
    - .planning/phases/07-sistema-multiunidade-pap-is/07-RESEARCH.md §"Pitfall 6" (re-auth) + §"Runtime State Inventory" (gen types command)
    - .planning/phases/07-sistema-multiunidade-pap-is/07-VALIDATION.md §"Sampling Rate → migration checkpoint"
  </read_first>
  <action>
    1. Run `supabase db push` (applies all unapplied Phase 7 migrations). If it fails on a CHECK constraint name (`users_role_check` not found — Pitfall 3), run `supabase db push --debug`, query the actual constraint name, fix the DROP CONSTRAINT line in 20260614000400, and retry.
    2. Regenerate types: `supabase gen types typescript --project-id jqjwyqlbbuqnrffdnlpp > src/types/database.types.ts`.
    3. Post-push DB sanity checks (via `supabase db execute` or the SQL editor — record results in the SUMMARY):
       - `SELECT count(*) FROM public.units WHERE is_default = true;` equals the count of non-deleted clinics.
       - `SELECT count(*) FROM public.appointments WHERE unit_id IS NULL;` returns 0 (same for charges, receivables).
       - `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid='public.users'::regclass AND contype='c';` shows the 11-value role list.
       - `SELECT id, public FROM storage.buckets WHERE id = 'icp-certificates';` → public = false.
    4. Run `npx tsc --noEmit` against the regenerated types and `npx vitest run` (full suite) — all GREEN.
  </action>
  <acceptance_criteria>
    - `supabase db push` reports all Phase 7 migrations applied (no pending)
    - `src/types/database.types.ts` contains `units`, `user_units`, `certificates`, `ai_agent_config` type entries
    - Post-push checks: zero NULL unit_id rows on appointments/charges/receivables; one default unit per clinic; icp-certificates bucket public=false; role CHECK has 11 values (results recorded in SUMMARY)
    - `npx tsc --noEmit` GREEN and `npx vitest run` full suite GREEN
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Schema live, types regenerated, backfills confirmed on production data, full suite + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → production DB | db push mutates the live schema; wrong-org push could corrupt another project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering | push applied to the wrong Supabase project | mitigate | Blocking human checkpoint confirms `jqjwyqlbbuqnrffdnlpp` active under `kczvihafddupruvsrrsc` before push (Pitfall 6) |
| T-07-14 | Denial of Service | partial migration leaving operational rows with NULL unit_id | mitigate | Post-push check asserts zero NULL unit_id before declaring done; three-step backfill ordering (Plan 02) |
</threat_model>

<verification>
- `supabase db push` clean; no pending migrations.
- database.types.ts regenerated and contains the 4 new tables.
- Post-push data checks pass (recorded in SUMMARY).
- Full vitest suite + tsc GREEN against live-generated types.
</verification>

<success_criteria>
- All Phase 7 schema live on jqjwyqlbbuqnrffdnlpp.
- Types regenerated; backfills verified on real data; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sistema-multiunidade-pap-is/07-04-SUMMARY.md`
</output>
