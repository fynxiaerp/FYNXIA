---
phase: 05-ai-agents
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/components/ui/sheet.tsx
  - src/components/ui/scroll-area.tsx
  - src/lib/stores/copilot-store.ts
  - src/components/copilot/CopilotTrigger.tsx
  - src/components/copilot/CopilotSidebar.tsx
  - src/components/copilot/MessageList.tsx
  - src/components/copilot/MessageBubble.tsx
  - src/components/copilot/SuggestedPrompts.tsx
  - src/components/copilot/CopilotInput.tsx
  - src/app/(dashboard)/clinica/layout.tsx
autonomous: false
requirements: [AI-01]

must_haves:
  truths:
    - "A copilot trigger button is present on every /clinica/* page (mounted in clinica/layout.tsx)"
    - "Clicking the trigger opens a right-side Sheet hosting the useChat conversation"
    - "User can type a question, send it, and see the streamed assistant answer in the message list"
    - "Empty state shows 3 context-aware suggested prompts based on pathname"
    - "Limpar conversa clears the in-memory conversation (no server mutation — D-05)"
  artifacts:
    - path: "src/components/copilot/CopilotSidebar.tsx"
      provides: "Sheet + useChat (v6) host"
      contains: "useChat"
    - path: "src/components/copilot/CopilotTrigger.tsx"
      provides: "fixed bottom-right trigger (Bot icon)"
      contains: "fixed bottom"
    - path: "src/app/(dashboard)/clinica/layout.tsx"
      provides: "mounts CopilotTrigger on all clinica pages"
      contains: "CopilotTrigger"
  key_links:
    - from: "src/components/copilot/CopilotSidebar.tsx"
      to: "/api/copilot"
      via: "useChat transport"
      pattern: "/api/copilot"
    - from: "src/app/(dashboard)/clinica/layout.tsx"
      to: "CopilotTrigger"
      via: "import + render"
      pattern: "CopilotTrigger"
---

<objective>
Build the copilot sidebar UI (AI-01) per 05-UI-SPEC: a fixed trigger on every /clinica/* page, a
right-side Sheet hosting the AI SDK v6 `useChat` conversation, message bubbles with streaming, context
suggested prompts, and the input area.

Purpose: This is the only user-facing surface of AI-01 — it makes the copilot "available em toda tela".
It consumes the `POST /api/copilot` contract built in 05-02. Read-only (D-05): no action buttons.
Output: shadcn Sheet+ScrollArea, copilot components, zustand open/closed store, clinica/layout.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-ai-agents/05-UI-SPEC.md
@.planning/phases/05-ai-agents/05-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- AI SDK v6 useChat — CRITICAL: 05-UI-SPEC §AI SDK Integration Contract lists v4 API names
     (handleSubmit, handleInputChange, isLoading, toDataStreamResponse). Those are OUTDATED.
     Use the v6 API below (RESEARCH §State of the Art + Pitfalls 1,9). Honor the VISUAL contract
     from UI-SPEC; override only the hook API names. -->

import { useChat } from '@ai-sdk/react'   // NOT 'ai/react', NOT 'ai'

const [input, setInput] = useState('')     // v6: component owns input state
const { messages, sendMessage, status, error, setMessages } = useChat({ api: '/api/copilot' })
const isLoading = status === 'submitted' || status === 'streaming'
// send: sendMessage({ text: input }); setInput('')   (NOT handleSubmit)
// clear: setMessages([])
// render: messages[].parts (array) — render parts where part.type === 'text' → part.text
//         (NOT messages[].content string)

shadcn components to add (official registry — no vetting gate): `npx shadcn@latest add sheet scroll-area`
  (Sheet may already exist from Phase 2 — only add if missing; ScrollArea is new).
@base-ui Button is at src/components/ui/button.tsx (render-prop, no asChild — CLAUDE.md convention).
Icons: lucide-react (Bot, X, Send/ArrowUp). Existing /clinica pages use bg-background; layout is a Sheet
overlay that does NOT reflow content.

zustand store (client-only open/closed UI state per CLAUDE.md state rules):
  src/lib/stores/copilot-store.ts → useCopilotStore with { open: boolean; setOpen; toggle } — conversation
  is NOT in the store (lives in useChat; resets on navigation per UI-SPEC).

Visual contract highlights (05-UI-SPEC): panel w-96 desktop / w-80 tablet / w-full mobile; trigger
fixed bottom-6 right-6 z-40 h-12 w-12 rounded-full bg-primary, Bot icon → X when open; header "Copiloto
FYNXIA"; user bubble bg-muted right-aligned, assistant no-fill left-aligned; streaming cursor span
aria-hidden; typing indicator role="status"; MessageList aria-live="polite"; textarea Enter submits
(Shift+Enter newline); "Perguntar" + "Limpar conversa"; all copy pt-BR per §Copywriting Contract.
Suggested prompts: 3 chips by pathname (/clinica/agenda, /clinica/financeiro*, else) per §Suggested Prompts.
Error: render error as assistant bubble with text-destructive using the §Error States copy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn primitives + zustand store + trigger + sidebar shell</name>
  <read_first>
    - src/components/ui/button.tsx (@base-ui render-prop convention)
    - src/app/(dashboard)/clinica/page.tsx (current /clinica content; confirm no existing layout.tsx)
    - 05-UI-SPEC.md §Layout Patterns, §Component Inventory, §Copywriting Contract
    - 05-RESEARCH.md Pattern 2 (useChat wiring)
  </read_first>
  <action>
1. `npx shadcn@latest add scroll-area` (and `sheet` if not already present in src/components/ui/sheet.tsx).
2. `src/lib/stores/copilot-store.ts` — `'use client'`-free zustand store: `useCopilotStore` with
   `open`, `setOpen(v)`, `toggle()`. (Per UI-SPEC: open/closed persists across pages; conversation does not.)
3. `src/components/copilot/CopilotTrigger.tsx` — `'use client'`. Fixed button `fixed bottom-6 right-6 z-40`
   (mobile `bottom-4 right-4`), `h-12 w-12 rounded-full bg-primary shadow-lg`, lucide `Bot` icon
   `size-5 text-primary-foreground`; when `open` show `X`. aria-label toggles "Abrir copiloto"/"Fechar
   copiloto"; aria-expanded bound to `open`. onClick `toggle()`. Renders `<CopilotSidebar />`.
4. `src/components/copilot/CopilotSidebar.tsx` — `'use client'`. Sheet (`side="right"`) controlled by
   `useCopilotStore` open/onOpenChange. SheetContent width `w-96 sm:w-80 lg:w-96 max-sm:w-full` (follow
   UI-SPEC breakpoints). Hosts `useChat({ api: '/api/copilot' })` (v6 API above) + own `useState('')` input.
   Layout: header (title "Copiloto FYNXIA" + subtitle "Pergunte sobre sua clínica ou peça ajuda"), then
   `<MessageList .../>` (flex-1), then `<CopilotInput .../>`. "Limpar conversa" calls `setMessages([])`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/scroll-area.tsx` exists (shadcn); `src/components/ui/sheet.tsx` present
    - `copilot-store.ts` exports `useCopilotStore` with `open`/`setOpen`/`toggle`
    - `CopilotSidebar.tsx` imports `useChat` from `@ai-sdk/react` and posts to `/api/copilot`; uses `sendMessage`/`status`/`setMessages` (NOT handleSubmit/isLoading)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Primitives added; store + trigger + sidebar shell compile with v6 useChat.</done>
</task>

<task type="auto">
  <name>Task 2: MessageList + MessageBubble + SuggestedPrompts + CopilotInput</name>
  <read_first>
    - src/components/copilot/CopilotSidebar.tsx (Task 1 — props it passes down)
    - 05-UI-SPEC.md §Color (bubble split), §Typography (streaming cursor), §Interaction Contracts, §Accessibility, §Copywriting Contract
  </read_first>
  <action>
- `src/components/copilot/MessageBubble.tsx` — renders one message. User variant: `ml-auto max-w-[80%]
  bg-muted px-3 py-2 rounded-xl rounded-br-sm`. Assistant variant: `mr-auto max-w-[85%] px-3 py-2` (no fill).
  Render `message.parts` filtering `part.type === 'text'`. Streaming cursor: `<span aria-hidden="true"
  className="inline-block w-[2px] h-[14px] bg-foreground align-middle animate-pulse" />` appended when this
  is the last assistant message and `isLoading`. Error bubble: assistant variant with `text-destructive`.
- `src/components/copilot/MessageList.tsx` — ScrollArea, `aria-live="polite"` `aria-label="Conversa com o
  copiloto"`. Maps messages → MessageBubble. Auto-scroll to bottom on new message (ref + useEffect). When
  `messages.length === 0` render empty state ("Olá, como posso ajudar?" + body copy) and `<SuggestedPrompts/>`.
  Typing indicator (`role="status" aria-label="Copiloto digitando…"`, 3 animate-bounce dots) shown when
  isLoading and last message is the user's (awaiting first token).
- `src/components/copilot/SuggestedPrompts.tsx` — 3 chips by `usePathname()` per UI-SPEC §Suggested Prompts
  table. Chip styling per spec (`rounded-full border bg-muted px-3 py-2 text-sm`). Clicking a chip calls a
  passed `onPick(text)` that submits immediately. Hidden once messages exist.
- `src/components/copilot/CopilotInput.tsx` — controlled Textarea (`aria-label="Mensagem para o copiloto"`,
  `min-h-[40px] max-h-[120px] resize-none`, placeholder "Faça uma pergunta…") + send Button ("Perguntar",
  `aria-label="Enviar mensagem"`, disabled when isLoading or empty) + "Limpar conversa" ghost button.
  Enter submits, Shift+Enter newline (onKeyDown). All copy pt-BR.
- Wire submit in CopilotSidebar: `function submit(text){ if(!text.trim())return; sendMessage({text}); setInput('') }`,
  passed to CopilotInput (onSubmit) and SuggestedPrompts (onPick).
- D-05: NO action/mutation buttons anywhere in the panel.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 4 component files exist under src/components/copilot/
    - MessageBubble renders `message.parts` (not `.content`); streaming cursor span has `aria-hidden`
    - SuggestedPrompts switches chips by pathname; hidden when messages exist
    - CopilotInput: Enter submits / Shift+Enter newline; send button disabled when empty or streaming
    - `npx tsc --noEmit` exit 0 and `npx next build` clean
  </acceptance_criteria>
  <done>Message list, bubbles, prompts, input render the full conversation UX; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Mount trigger in clinica/layout.tsx</name>
  <read_first>
    - src/app/(dashboard)/clinica/page.tsx and src/app/(dashboard)/layout.tsx (existing shell)
    - 05-UI-SPEC.md §Dashboard Shell — Copilot Integration Point
  </read_first>
  <action>
Create `src/app/(dashboard)/clinica/layout.tsx` (does not currently exist) as a Server Component that
renders `{children}` and mounts `<CopilotTrigger />` (client component) once so it appears on every
`/clinica/*` route. Do not reflow page content — the trigger is `position: fixed` and the Sheet overlays.
If a parent `(dashboard)/layout.tsx` already wraps content, keep this layout minimal (fragment + children +
trigger). Ensure no SSR error from the client store (CopilotTrigger is `'use client'`).
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/clinica/layout.tsx` exists and renders `{children}` + `<CopilotTrigger />`
    - grep: layout.tsx imports CopilotTrigger
    - `npx next build` clean (all /clinica/* routes build with the trigger)
  </acceptance_criteria>
  <done>Copilot trigger present on every /clinica/* page; build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual + functional verification of copilot sidebar</name>
  <files>src/components/copilot/CopilotSidebar.tsx, src/app/(dashboard)/clinica/layout.tsx</files>
  <action>No new code — this is a human verification gate after Tasks 1-3. Run the dev server and follow how-to-verify to confirm the copilot sidebar mounts on every /clinica page, opens/streams (or degrades gracefully without a key), shows context prompts, clears conversation, and exposes no write actions (D-05).</action>
  <what-built>The full copilot sidebar UI mounted on every /clinica page, wired to POST /api/copilot (AI SDK v6 useChat).</what-built>
  <how-to-verify>
Requires AI_GATEWAY_API_KEY set locally for a live answer (otherwise the route returns 503 — the UI should
show the gateway error bubble gracefully, which is also a valid pass for the UI wiring).
1. `npm run dev`, log in, visit `/clinica/agenda`.
2. Confirm the round Bot trigger appears bottom-right. Click it → Sheet slides in from the right.
3. Empty state shows "Olá, como posso ajudar?" + 3 agenda-context chips.
4. (If key set) Click "Quais consultas tenho hoje?" → streamed answer appears; no raw CPF/health data shown.
   (If no key) The gateway-unreachable error bubble appears in pt-BR — acceptable.
5. Type a how-to question ("Como cadastro um paciente?") → useful answer (or graceful error w/o key).
6. Click "Limpar conversa" → messages clear, chips reappear, no confirmation dialog.
7. Navigate to /clinica/financeiro → trigger still present; chips now financeiro-context.
8. Confirm there are NO action buttons (Confirmar/Remarcar/Gerar cobrança) inside the panel (D-05).
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/functional issues.</resume-signal>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Trigger visible on all /clinica/* pages; Sheet opens/closes
    - Suggested prompts are context-aware by pathname; clear conversation works
    - No mutation/action buttons in the panel (D-05)
    - Full suite GREEN + tsc clean
  </acceptance_criteria>
  <done>Copilot sidebar verified working (live or graceful-degraded); read-only confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser(client UI)→/api/copilot | Chat input submitted from client; server enforces auth/RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-ui-eop | Elevation of Privilege | client UI offering write actions | mitigate | D-05 — panel has NO action buttons; all writes happen via normal UI flows; server registers only read tools (05-02) |
| T-5-ui-disc | Information Disclosure | rendering unmasked PII in bubbles | mitigate | UI renders only what tools return (already masked in 05-02); no extra client fetch of patient PII |
| T-5-ui-auth | Spoofing | unauthenticated access to copilot | accept | /clinica/* is already behind the Phase 1 auth middleware; the route also 401s independently |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0; `npx next build` clean
- Full suite `npx vitest run` GREEN (tools + chat-route tests from 05-02 remain GREEN)
- Manual: trigger on all /clinica pages, Sheet opens, streaming/graceful-error, read-only confirmed
</verification>

<success_criteria>
- Copilot trigger on every /clinica/* page; Sheet sidebar hosts useChat (v6) → /api/copilot
- Context suggested prompts; streaming bubbles; Limpar conversa (in-memory); no action buttons (D-05)
- tsc + next build clean; full suite GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-agents/05-03-SUMMARY.md`
</output>
