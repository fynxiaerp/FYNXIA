---
phase: 05-ai-agents
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/migrations/ai.test.ts
  - src/__tests__/ai/tools.test.ts
  - src/__tests__/ai/chat-route.test.ts
  - src/__tests__/ai/whatsapp-inbound.test.ts
  - src/__tests__/ai/collection-agent.test.ts
  - supabase/migrations/20260610000100_agent_outreach_log.sql
  - supabase/migrations/20260610000200_agent_outreach_log_rls.sql
  - supabase/migrations/20260610000300_whatsapp_inbound_events.sql
  - src/types/database.types.ts
autonomous: false
requirements: [AI-01, AI-02, AI-03]
user_setup:
  - service: supabase-cli
    why: "db push requires CLI logged into FYNXIA org (recurring re-auth gotcha)"
    dashboard_config:
      - task: "supabase login into org kczvihafddupruvsrrsc (project jqjwyqlbbuqnrffdnlpp) before db push"
        location: "Supabase CLI"

must_haves:
  truths:
    - "agent_outreach_log table exists in Supabase sa-east-1 with tenant_id + agent_type + status + whatsapp_message_id + intent_result"
    - "whatsapp_inbound_events table exists with wamid UNIQUE for idempotent dedup"
    - "RLS on agent_outreach_log enforces tenant_id = get_my_tenant_id() (SELECT); no client write policy"
    - "5 Wave 0 test scaffolds exist and run (ai.test.ts GREEN after push; 4 ai/* RED-by-design)"
    - "database.types.ts regenerated includes agent_outreach_log + whatsapp_inbound_events"
  artifacts:
    - path: "supabase/migrations/20260610000100_agent_outreach_log.sql"
      provides: "agent_outreach_log table + tenant_id index"
      contains: "CREATE TABLE public.agent_outreach_log"
    - path: "supabase/migrations/20260610000200_agent_outreach_log_rls.sql"
      provides: "RLS SELECT policy via get_my_tenant_id(); no client write"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "supabase/migrations/20260610000300_whatsapp_inbound_events.sql"
      provides: "wamid UNIQUE dedup table (service-role only, no RLS)"
      contains: "wamid        TEXT UNIQUE NOT NULL"
    - path: "src/__tests__/migrations/ai.test.ts"
      provides: "SQL assertions for both migrations"
      contains: "agent_outreach_log"
  key_links:
    - from: "agent_outreach_log.tenant_id"
      to: "public.clinics(id)"
      via: "FK REFERENCES"
      pattern: "REFERENCES public\\.clinics\\(id\\)"
    - from: "whatsapp_inbound_events.wamid"
      to: "dedup constraint"
      via: "UNIQUE"
      pattern: "wamid\\s+TEXT UNIQUE NOT NULL"
---

<objective>
Lay the Phase 5 database foundation and the Nyquist Wave 0 test scaffolds.

Creates two new tables — `agent_outreach_log` (AI-02/03 audit + the `/clinica/ia/agentes` UI surface) and `whatsapp_inbound_events` (AI-02 inbound dedup) — plus the five RED-by-design test files that downstream plans (05-02..05-05) turn GREEN. Ends with the [BLOCKING] `supabase db push` + type regeneration so every later plan compiles against live types.

Purpose: Downstream copilot/webhook/agent plans need these tables and types to exist, and the Wave 0 tests define the behavioral contract (PII masking, HMAC verify, dedup, real Asaas link) before any implementation.
Output: 3 migration files, 5 test scaffolds, regenerated `database.types.ts`, tables live in Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-agents/05-RESEARCH.md
@.planning/phases/05-ai-agents/05-VALIDATION.md

<interfaces>
<!-- Existing patterns the executor mirrors — extracted from the live codebase. -->

Tenant table is `public.clinics` (renamed from `tenants` in Phase 1) — NOT `public.tenants`.
RESEARCH.md migration drafts reference `public.tenants(id)` — that is WRONG; use `public.clinics(id)`.

Appointment status values (from 20260605000100_clinical_tables.sql):
  status IN ('agendado','confirmado','em_atendimento','concluido','cancelado')
  → AI-02 maps CONFIRM → 'confirmado', CANCEL → 'cancelado' (Portuguese, NOT 'confirmed'/'cancelled').

Patient name column is `full_name` (NOT `name`). Patients table has: full_name, cpf (plaintext),
phone, email, medical_history/allergies/medications (AES-256 — NEVER to LLM), deleted_at, is_anonymized.

RLS reference pattern (from 20260606000200_financial_rls.sql):
  ALTER TABLE public.X ENABLE ROW LEVEL SECURITY;
  CREATE POLICY "X_tenant_read" ON public.X FOR SELECT USING (tenant_id = get_my_tenant_id());
  -- staff-write FOR ALL with get_my_role() IN ('admin','dentist','receptionist','superadmin')

webhook_events (Phase 3) has NO RLS — service-role only. Mirror that for whatsapp_inbound_events.

Migration test pattern (from src/__tests__/migrations/clinical.test.ts):
  const M = (f) => readFileSync(join(process.cwd(), 'supabase/migrations', f), 'utf8')
  expect(M('file.sql')).toMatch(/.../i)

Reusable modules downstream plans will import (paths confirmed to exist):
  - src/lib/messaging/queue.ts → getOutboxQueue(admin), EnqueueOptions { tenantId, channel, idempotencyKey, payload }
  - src/lib/messaging/worker.ts → drainOutbox(admin)
  - src/lib/asaas/gateway.ts → gateway.getInvoiceUrl(chargeId): Promise<string|null>
  - src/lib/audit.ts → logBusinessEvent({ tenantId, actorId, action, details })
  - src/lib/supabase/server.ts → createClient() (user RLS session)
  - src/lib/supabase/admin.ts → createAdminClient() (service role)
  - src/lib/whatsapp/client.ts → sendTemplateMessage (call-time creds)
  - src/lib/whatsapp/templates.ts → TEMPLATE_APPOINTMENT_REMINDER, TEMPLATE_COLLECTION, buildAppointmentReminderComponents (payloads CONFIRM_APPOINTMENT / CANCEL_APPOINTMENT — no id suffix yet), buildCollectionComponents
  - src/lib/cron-auth.ts → isCronAuthorized(authHeader)
  - src/lib/phone.ts → toE164(raw)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — author the 5 RED test scaffolds</name>
  <read_first>
    - src/__tests__/migrations/clinical.test.ts and src/__tests__/migrations/comms.test.ts (readFileSync + toMatch pattern)
    - .planning/phases/05-ai-agents/05-VALIDATION.md (§Wave 0 Requirements — the 5 files + exact behaviors)
    - .planning/phases/05-ai-agents/05-RESEARCH.md (§Validation Architecture → Test Map, §Code Examples)
    - src/lib/asaas/gateway.ts (getInvoiceUrl signature), src/lib/messaging/queue.ts (getOutboxQueue)
  </read_first>
  <action>
Create five Vitest files. Source-inspection style for files not yet written (use a helper that
readFileSync's the target source and `existsSync`-guards so the test is RED-with-clear-message until
the source exists, GREEN once written). Logic/function tests where the module is pure.

1. `src/__tests__/migrations/ai.test.ts` (will be GREEN after Task 2 db push — it only reads SQL files):
   - `20260610000100_agent_outreach_log.sql` matches: /CREATE TABLE public\.agent_outreach_log/i,
     /tenant_id\s+UUID NOT NULL REFERENCES public\.clinics\(id\)/i, /agent_type\s+TEXT NOT NULL/i,
     /CHECK \(agent_type IN \('confirmation', 'collection'\)\)/i, /whatsapp_message_id TEXT/i,
     /intent_result TEXT/i, /idx_agent_outreach_log_tenant_created/i.
   - `20260610000200_agent_outreach_log_rls.sql` matches: /ENABLE ROW LEVEL SECURITY/i,
     /FOR SELECT\s+USING \(tenant_id = get_my_tenant_id\(\)\)/i, and asserts NO "FOR INSERT"/"FOR UPDATE"
     client policy (expect(sql).not.toMatch(/FOR (INSERT|UPDATE|DELETE)/i)).
   - `20260610000300_whatsapp_inbound_events.sql` matches: /CREATE TABLE public\.whatsapp_inbound_events/i,
     /wamid\s+TEXT UNIQUE NOT NULL/i, /processed\s+BOOLEAN NOT NULL DEFAULT FALSE/i, and asserts
     NO "ENABLE ROW LEVEL SECURITY" (service-role only, mirrors webhook_events).

2. `src/__tests__/ai/tools.test.ts` (RED until 05-02):
   - reads src/lib/ai/masking.ts: maskCPF('123.456.789-00') === '***.***.***-00'; maskPhone masks digits.
   - reads src/lib/ai/tools.ts source: must import from '@/lib/supabase/server' (createClient), must NOT
     import '@/lib/supabase/admin' (no createAdminClient in copilot tools); source must NOT contain the
     strings 'medical_history', 'allergies', 'medications', 'anamnes' (health columns never selected);
     must contain `tool(` definitions and a `searchHelpDocs` tool (D-03). Assert there is NO mutation
     verb in any tool's supabase chain: source must NOT match /\.(insert|update|delete|upsert)\(/ inside tools.ts.

3. `src/__tests__/ai/chat-route.test.ts` (RED until 05-02):
   - reads src/app/api/copilot/route.ts source: matches /export const runtime = 'nodejs'/,
     /process\.env\.AI_GATEWAY_API_KEY/ (call-time read — assert it appears INSIDE `export async function POST`,
     not at module top), /'anthropic\/claude-sonnet-4\.6'/, /zeroDataRetention:\s*true/,
     /streamText/, /toUIMessageStreamResponse/. Assert it does NOT use removed v4/v5 APIs:
     not.toMatch(/maxSteps/), not.toMatch(/convertToCoreMessages/).

4. `src/__tests__/ai/whatsapp-inbound.test.ts` (RED until 05-04):
   - import the pure HMAC verifier `verifyWhatsAppSignature` from src/lib/whatsapp/verify-signature.ts:
     valid sha256 hmac over a known body+secret returns true; tampered body returns false; missing
     'sha256=' prefix returns false.
   - import the pure intent->status mapper from src/lib/ai/whatsapp-intent.ts (e.g. `buttonPayloadToStatus`):
     'CONFIRM_APPOINTMENT_abc' → { appointmentId:'abc', status:'confirmado' };
     'CANCEL_APPOINTMENT_abc' → { appointmentId:'abc', status:'cancelado' }; unknown → null.
   - reads src/app/api/webhooks/whatsapp/route.ts source: matches /export const runtime = 'nodejs'/,
     a GET handler echoing /hub\.challenge/ guarded by /WHATSAPP_WEBHOOK_VERIFY_TOKEN/, a POST that calls
     /request\.text\(\)/ BEFORE any /JSON\.parse/ (HMAC on raw body), references /whatsapp_inbound_events/
     (dedup) and /WHATSAPP_APP_SECRET/. Assert ambiguous free-text path does NOT update status
     (source contains a comment/branch where intent==='ambiguous' → no appointments update).

5. `src/__tests__/ai/collection-agent.test.ts` (RED until 05-05):
   - reads src/lib/agents/collection-agent.ts source: must reference /getInvoiceUrl/ and must NOT contain
     a hardcoded link literal (not.toMatch(/asaas\.com\/i\//) and not.toMatch(/https:\/\/[^\s'"]*\$\{/) —
     i.e. no fabricated/templated URL); must reference /getOutboxQueue/ (enqueue, not direct send) and
     /logBusinessEvent/ and /agent_outreach_log/. Also assert the confirmation-agent file
     src/lib/agents/confirmation-agent.ts references /TEMPLATE_APPOINTMENT_REMINDER/ and /agent_outreach_log/.

Each ai/* test that reads a not-yet-existing source must fail with a readable message (e.g.
`expect(existsSync(path), 'src/lib/ai/tools.ts not yet created (05-02)').toBe(true)`), making the RED
state intentional and self-documenting.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/migrations/ai.test.ts src/__tests__/ai/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `src/__tests__/migrations/ai.test.ts`, `src/__tests__/ai/tools.test.ts`, `src/__tests__/ai/chat-route.test.ts`, `src/__tests__/ai/whatsapp-inbound.test.ts`, `src/__tests__/ai/collection-agent.test.ts`
    - `ai.test.ts` is RED before Task 1 (migration files absent) and the 4 `ai/*` tests are RED-by-design with readable "not yet created" messages
    - No watch-mode flags; each test runs and reports in < 10s
  </acceptance_criteria>
  <done>5 test files committed; migrations test RED (pre-Task-1); ai/* tests RED-by-design with clear messages.</done>
</task>

<task type="auto">
  <name>Task 1: Author agent_outreach_log + whatsapp_inbound_events migrations</name>
  <read_first>
    - supabase/migrations/20260606000100_financial_tables.sql (column style, TIMESTAMPTZ, gen_random_uuid)
    - supabase/migrations/20260606000200_financial_rls.sql (RLS USING/WITH CHECK pattern)
    - supabase/migrations/20260603000100_rls_policies.sql (webhook_events no-RLS precedent — find via Phase 3 financial_tables webhook_events block)
    - .planning/phases/05-ai-agents/05-RESEARCH.md §Data Model (correct the tenants→clinics, patients/appointments/receivables FK names)
  </read_first>
  <action>
Create THREE migration files (note: tenant table is `public.clinics`, NOT `tenants`):

`supabase/migrations/20260610000100_agent_outreach_log.sql`:
```sql
CREATE TABLE public.agent_outreach_log (
  id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id           UUID NOT NULL REFERENCES public.clinics(id) ON DELETE CASCADE,
  agent_type          TEXT NOT NULL CHECK (agent_type IN ('confirmation', 'collection')),
  patient_id          UUID REFERENCES public.patients(id),
  appointment_id      UUID REFERENCES public.appointments(id),
  receivable_id       UUID REFERENCES public.receivables(id),
  status              TEXT NOT NULL DEFAULT 'sent'
                      CHECK (status IN ('sent','delivered','responded','failed','ambiguous')),
  whatsapp_message_id TEXT,
  intent_result       TEXT,          -- AI-02: 'confirm' | 'cancel' | 'ambiguous'
  error_message       TEXT,
  created_at          TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at          TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_agent_outreach_log_tenant_created
  ON public.agent_outreach_log(tenant_id, created_at DESC);
```

`supabase/migrations/20260610000200_agent_outreach_log_rls.sql`:
```sql
-- Tenant members read their own log (powers /clinica/ia/agentes). Writes are service-role
-- only (cron/webhook via createAdminClient) — mirrors message_outbox decision (no client write).
ALTER TABLE public.agent_outreach_log ENABLE ROW LEVEL SECURITY;
CREATE POLICY "agent_outreach_log_tenant_read" ON public.agent_outreach_log
  FOR SELECT USING (tenant_id = get_my_tenant_id());
```
(Deliberately NO INSERT/UPDATE/DELETE policy.)

`supabase/migrations/20260610000300_whatsapp_inbound_events.sql`:
```sql
-- Inbound WhatsApp dedup. Service-role only (webhook uses createAdminClient) — NO RLS,
-- mirrors webhook_events (Phase 3). UNIQUE(wamid) is the idempotency constraint.
CREATE TABLE public.whatsapp_inbound_events (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  wamid       TEXT UNIQUE NOT NULL,
  from_phone  TEXT NOT NULL,
  payload     JSONB NOT NULL,
  processed   BOOLEAN NOT NULL DEFAULT FALSE,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
```
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/migrations/ai.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/migrations/ai.test.ts` is GREEN (all SQL assertions pass)
    - `agent_outreach_log.sql` contains `REFERENCES public.clinics(id)` (NOT `public.tenants`)
    - `agent_outreach_log_rls.sql` contains `ENABLE ROW LEVEL SECURITY` and a SELECT-only policy; grep shows no `FOR INSERT|UPDATE|DELETE`
    - `whatsapp_inbound_events.sql` contains `wamid        TEXT UNIQUE NOT NULL` and NO `ENABLE ROW LEVEL SECURITY`
  </acceptance_criteria>
  <done>Three migration files exist; ai.test.ts GREEN.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push + regenerate types</name>
  <files>supabase/migrations/20260610000100_agent_outreach_log.sql, supabase/migrations/20260610000200_agent_outreach_log_rls.sql, supabase/migrations/20260610000300_whatsapp_inbound_events.sql, src/types/database.types.ts</files>
  <action>Apply the 3 migrations to Supabase sa-east-1 via supabase db push, then regenerate src/types/database.types.ts. Human re-authenticates the Supabase CLI into the FYNXIA org first (recurring gotcha); orchestrator runs the push and type-gen. See how-to-verify for exact steps.</action>
  <what-built>Two new tables (`agent_outreach_log`, `whatsapp_inbound_events`) defined in 3 migration files, ready to apply to Supabase sa-east-1 (project jqjwyqlbbuqnrffdnlpp).</what-built>
  <how-to-verify>
RECURRING GOTCHA: the Supabase CLI login does not persist across days and frequently defaults to the
wrong account (nexus-*). Re-authenticate into the FYNXIA org (kczvihafddupruvsrrsc) BEFORE pushing.

1. Human: `supabase login` (or `supabase projects list` to confirm jqjwyqlbbuqnrffdnlpp is visible).
2. Orchestrator runs: `supabase db push` (applies 20260610000100/200/300).
3. Orchestrator runs: `supabase gen types typescript --linked > src/types/database.types.ts`
   (regenerates types — must now include agent_outreach_log + whatsapp_inbound_events).
4. Verify: `npx tsc --noEmit` exits 0; `npx vitest run src/__tests__/migrations/ai.test.ts` GREEN.
  </how-to-verify>
  <resume-signal>Type "pushed" once db push succeeded and database.types.ts regenerated, or describe the error.</resume-signal>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/__tests__/migrations/ai.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `database.types.ts` contains `agent_outreach_log` and `whatsapp_inbound_events`
    - `npx tsc --noEmit` exits 0
    - Both tables visible in Supabase (human confirms via dashboard or `supabase db push` success output)
  </acceptance_criteria>
  <done>Tables live in sa-east-1; types regenerated; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client(staff)→agent_outreach_log | Tenant members read agent audit rows via RLS |
| webhook/cron→whatsapp_inbound_events | Service-role-only writes (no client path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Information Disclosure | agent_outreach_log cross-tenant read | mitigate | RLS `FOR SELECT USING (tenant_id = get_my_tenant_id())`; FK to clinics(id) |
| T-5-02 | Tampering | client forging/altering outreach audit rows | mitigate | NO client INSERT/UPDATE/DELETE policy — service-role writes only (mirrors message_outbox) |
| T-5-03 | Tampering | duplicate inbound WhatsApp → double status update | mitigate | `whatsapp_inbound_events.wamid` UNIQUE — 23505 = idempotent skip (consumed by 05-04) |
| T-5-04 | Spoofing | direct client write to dedup table | accept | whatsapp_inbound_events has NO RLS and no PostgREST-exposed client path; service-role only (mirrors webhook_events, T-3-04 accepted) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/migrations/ai.test.ts` GREEN after Task 1
- `npx tsc --noEmit` exit 0 after Task 2 type regen
- Full suite `npx vitest run` still GREEN (4 ai/* RED-by-design are acceptable WITHIN this plan only — they go GREEN in 05-02..05-05)
</verification>

<success_criteria>
- agent_outreach_log + whatsapp_inbound_events tables live in Supabase sa-east-1
- RLS on agent_outreach_log (SELECT tenant-scoped, no client write); whatsapp_inbound_events no-RLS service-role only
- 5 Wave 0 scaffolds committed; ai.test.ts GREEN; database.types.ts regenerated
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-agents/05-01-SUMMARY.md`
</output>
