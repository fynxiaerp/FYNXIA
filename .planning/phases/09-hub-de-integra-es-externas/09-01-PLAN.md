---
phase: 09-hub-de-integra-es-externas
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/integrations/connectors.test.ts
  - src/__tests__/integrations/webhooks.test.ts
  - src/__tests__/integrations/health.test.ts
autonomous: true
requirements: [INT-01, INT-02, INT-03]
must_haves:
  truths:
    - "Running the new integration test files fails RED (asserting artifacts that do not yet exist)"
    - "Test files compile under tsc strict (no type errors)"
    - "Each INT requirement has at least one source-inspection or unit test"
  artifacts:
    - path: "src/__tests__/integrations/connectors.test.ts"
      provides: "INT-01 RED scaffold: migration columns + REVOKE + RLS + action shape + AES round-trip + masking"
      min_lines: 60
    - path: "src/__tests__/integrations/webhooks.test.ts"
      provides: "INT-02 RED scaffold: integration_events migration + additive logToHub in both handlers"
      min_lines: 50
    - path: "src/__tests__/integrations/health.test.ts"
      provides: "INT-03 RED scaffold: deriveHealth unit + drainIntegrationEvents CAS + cron route"
      min_lines: 50
  key_links:
    - from: "src/__tests__/integrations/connectors.test.ts"
      to: "supabase/migrations/*_integration_connectors.sql"
      via: "readFileSync suffix-match (M() helper, mirrors phase8.test.ts)"
      pattern: "_integration_connectors\\.sql"
    - from: "src/__tests__/integrations/webhooks.test.ts"
      to: "src/app/api/webhooks/asaas/route.ts + whatsapp/route.ts"
      via: "readFileSync + toMatch(/logToHub/)"
      pattern: "logToHub"
---

<objective>
Create the RED test scaffolds for Phase 9 (integration hub) following the established
source-inspection + unit test style from Phase 7/8 (`phase8.test.ts`). These tests assert
the artifacts that Plans 02–05 will create. They MUST fail RED now (files/columns/calls do
not exist yet) and turn GREEN as the implementation lands wave by wave.

Purpose: Nyquist compliance — every INT requirement gets an automated check that exists
BEFORE the implementation, so no behavior ships untested.
Output: 3 test files under `src/__tests__/integrations/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-hub-de-integra-es-externas/09-RESEARCH.md
@.planning/phases/09-hub-de-integra-es-externas/09-VALIDATION.md

# Test style to mirror EXACTLY (M() suffix-match helper, source-inspection):
@src/__tests__/migrations/phase8.test.ts

# Artifacts under test (read so the assertions match the real shapes Plan 02-03-05 will produce):
@src/lib/crypto.ts
@src/app/api/webhooks/asaas/route.ts
@src/app/api/webhooks/whatsapp/route.ts

<interfaces>
<!-- Contracts the implementation plans MUST satisfy — assert these exact shapes. -->

Migrations (Plan 02 creates; suffix-match via M()):
- `_integration_connectors.sql`  — table public.integration_connectors
- `_integration_events.sql`      — table public.integration_events
- `_integration_revoke.sql`      — REVOKE SELECT (credential_enc) ... FROM authenticated, anon

integration_connectors columns: id, clinic_id (NULLABLE — system sentinel), type TEXT,
  config JSONB, credential_enc TEXT, status TEXT CHECK ('enabled','disabled'),
  created_at, updated_at. Index idx_integration_connectors_clinic on (clinic_id).
  RLS: USING (clinic_id = get_my_tenant_id()) + WITH CHECK + role gate admin/superadmin/ti.

integration_events columns: id, clinic_id (NULLABLE), connector_id (NULLABLE FK SET NULL),
  direction TEXT CHECK ('inbound','outbound'), status TEXT CHECK ('received','pending','processed','failed'),
  event_type TEXT, external_event_id TEXT, payload_ref TEXT, attempts INT DEFAULT 0,
  max_attempts INT DEFAULT 3, last_error TEXT, processed_at, created_at, updated_at.
  Indexes: idx_integration_events_clinic, idx_integration_events_connector, idx_integration_events_status.

src/lib/integrations/health.ts:
  export function deriveHealth(recentEvents): 'ok' | 'degraded' | 'failed' | 'unknown'
  - [] → 'unknown'; 0 failed in 24h → 'ok'; failed/total >= 0.5 → 'failed'; else 'degraded'

src/lib/integrations/worker.ts:
  export async function drainIntegrationEvents(admin?, batchSize?): Promise<{processed,failed,skipped}>
  - atomic claim: .update(...).eq('status','pending').eq('attempts', row.attempts) (CAS)

src/actions/integration-connectors.ts:
  export type ConnectorPublic = Omit<ConnectorRow, 'credential_enc'>
  export async function createConnector(formData): contains assertNotReadOnly() + encrypt(
  export function maskCredential(plaintextLastFour): string  → returns '••••••' + last4

src/app/api/cron/integration-retry/route.ts:
  export const runtime = 'nodejs'; uses isCronAuthorized; calls drainIntegrationEvents
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: connectors.test.ts (INT-01) — migration + REVOKE + RLS + action + AES + masking</name>
  <files>src/__tests__/integrations/connectors.test.ts</files>
  <read_first>src/__tests__/migrations/phase8.test.ts (copy the M() suffix-match helper verbatim), src/lib/crypto.ts</read_first>
  <action>
Create `src/__tests__/integrations/connectors.test.ts`. Copy the `M(suffix)` helper from
phase8.test.ts (readdirSync + endsWith + readFileSync against `supabase/migrations`). Add an
`SRC(relPath)` helper that reads a source file from `process.cwd()` and returns '' if missing
(so the assertion fails on content, not on a thrown ENOENT — use try/catch returning '').

Assertions (all RED now):
  - migration `_integration_connectors.sql`:
    * toMatch /CREATE TABLE public\.integration_connectors/i
    * toMatch /credential_enc\s+TEXT/i
    * toMatch /clinic_id/i  (column present, NULLABLE — assert NOT toMatch /clinic_id\s+UUID\s+NOT NULL/i)
    * toMatch /status\s+TEXT/i and /CHECK/i with 'enabled' and 'disabled'
    * toMatch /idx_integration_connectors_clinic/i
    * toMatch /ENABLE ROW LEVEL SECURITY/i, /get_my_tenant_id\(\)/i, /WITH CHECK/i
    * role gate: toMatch /'admin'/ and /'ti'/ and /'superadmin'/
  - migration `_integration_revoke.sql`:
    * toMatch /REVOKE SELECT \(credential_enc\)/i and /FROM authenticated, anon/i
  - action `src/actions/integration-connectors.ts` (via SRC):
    * toMatch /assertNotReadOnly\(\)/
    * toMatch /encrypt\(/      (encrypts before insert)
    * toMatch /Omit<[^>]*,\s*'credential_enc'>/  (ConnectorPublic excludes secret)
    * NOT toMatch a return of raw credential_enc to client — assert toMatch /maskCredential/ used or Omit applied
  - unit (AES round-trip — import { encrypt, decrypt } from '@/lib/crypto'):
    * expect(decrypt(encrypt('api-key-1234'))).toBe('api-key-1234')
  - unit (masking — import { maskCredential } from '@/actions/integration-connectors' OR from a util; if action is 'use server', import from a separate '@/lib/integrations/mask'): assert maskCredential('1234') === '••••••1234' and never reveals full secret. Decide the import path now and document it in the test so Plan 02 follows it. RECOMMEND a pure util at `src/lib/integrations/mask.ts` (NOT 'use server') so it can be imported by both the action and a client component.
Set ENCRYPTION_KEY in the test via a beforeAll if not already set by setup.ts (64-char hex) — check existing icp tests for how the key is provided; reuse that mechanism.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/integrations/connectors.test.ts 2>&1 | grep -E "fail|FAIL|✗" || echo "EXPECTED RED — assertions reference not-yet-created artifacts"</automated>
  </verify>
  <done>File exists, compiles (npx tsc --noEmit clean for this file), runs and fails RED on the missing migration/action artifacts. AES round-trip assertion passes (crypto.ts exists). min 60 lines.</done>
</task>

<task type="auto">
  <name>Task 2: webhooks.test.ts (INT-02) — integration_events migration + additive logToHub in handlers</name>
  <files>src/__tests__/integrations/webhooks.test.ts</files>
  <read_first>src/app/api/webhooks/asaas/route.ts, src/app/api/webhooks/whatsapp/route.ts, src/__tests__/migrations/phase8.test.ts</read_first>
  <action>
Create `src/__tests__/integrations/webhooks.test.ts` with the same M() + SRC() helpers.
Assertions (RED now):
  - migration `_integration_events.sql`:
    * toMatch /CREATE TABLE public\.integration_events/i
    * columns: /connector_id/i, /direction\s+TEXT/i with /CHECK/ ('inbound','outbound'),
      /status\s+TEXT/i with /CHECK/ ('received','pending','processed','failed'),
      /attempts\s+INT/i, /max_attempts\s+INT/i, /last_error/i, /external_event_id/i, /payload_ref/i
    * connector_id FK: toMatch /REFERENCES public\.integration_connectors\(id\) ON DELETE SET NULL/i
    * clinic_id NULLABLE: assert NOT toMatch /clinic_id\s+UUID\s+NOT NULL/i
    * indexes: /idx_integration_events_clinic/i, /idx_integration_events_connector/i, /idx_integration_events_status/i
    * MUST NOT reuse message_outbox ENUMs: assert NOT toMatch /message_status/ and NOT toMatch /message_channel/
  - asaas handler (SRC src/app/api/webhooks/asaas/route.ts):
    * toMatch /logToHub\(/  (additive call present)
    * toMatch /\.catch\(/   (fire-and-forget — followed by catch)
    * REGRESSION-SAFE: assert the existing critical lines still present:
      /asaas-access-token/, /ignoreDuplicates: true/, /new Response\('', \{ status: 200 \}\)/,
      /processWebhookEvent\(/
  - whatsapp handler (SRC src/app/api/webhooks/whatsapp/route.ts):
    * toMatch /logToHub\(/  and /\.catch\(/
    * REGRESSION-SAFE: /verifyWhatsAppSignature/, /'23505'/, /processInbound\(/, /export const runtime = 'nodejs'/
  - hub-log lib (SRC src/lib/integrations/hub-log.ts):
    * toMatch /export async function logToHub/
    * toMatch /import 'server-only'/
    * toMatch /integration_events/  (inserts into the events table)
    * null-safe connector: assert toMatch /connector_id/  and a comment or code path tolerating NULL (assert NOT toMatch /throw new Error\('connector not found/)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/integrations/webhooks.test.ts 2>&1 | grep -E "fail|FAIL|✗" || echo "EXPECTED RED"</automated>
  </verify>
  <done>File exists, tsc-clean, runs RED (logToHub not yet added to handlers; migration absent). Regression-safe assertions documented. min 50 lines.</done>
</task>

<task type="auto">
  <name>Task 3: health.test.ts (INT-03) — deriveHealth unit + drainIntegrationEvents CAS + cron route</name>
  <files>src/__tests__/integrations/health.test.ts</files>
  <read_first>src/lib/messaging/worker.ts (atomic-claim CAS pattern to mirror), src/lib/cron-auth.ts</read_first>
  <action>
Create `src/__tests__/integrations/health.test.ts` with M() + SRC() helpers.
Assertions:
  - UNIT deriveHealth (import { deriveHealth } from '@/lib/integrations/health'):
    * deriveHealth([]) === 'unknown'
    * 3 events all status 'processed' within last hour → 'ok'
    * 2 of 2 events 'failed' within 24h → 'failed'
    * 1 failed of 4 within 24h (25%) → 'degraded'
    * an event older than 24h is excluded from the window (build events with created_at = now-25h)
    Build event objects matching the IntegrationEventRow shape (status, created_at ISO).
  - SOURCE drainIntegrationEvents (SRC src/lib/integrations/worker.ts):
    * toMatch /export async function drainIntegrationEvents/
    * toMatch /import 'server-only'/
    * CAS atomic claim: toMatch /\.eq\('status', 'pending'\)/ AND /\.eq\('attempts'/
    * selects only pending: toMatch /\.eq\('status', 'pending'\)/  (re-assert in select)
    * does not re-send processed: assert NOT toMatch /\.eq\('status', 'processed'\)/ in the fetch
  - SOURCE cron route (SRC src/app/api/cron/integration-retry/route.ts):
    * toMatch /export const runtime = 'nodejs'/
    * toMatch /isCronAuthorized\(/
    * toMatch /drainIntegrationEvents\(/
    * toMatch /status: 401/  (fail-closed unauthorized response)
  - SOURCE vercel.json: toMatch /integration-retry/  and a cron schedule string
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/integrations/health.test.ts 2>&1 | grep -E "fail|FAIL|✗" || echo "EXPECTED RED"</automated>
  </verify>
  <done>File exists, tsc-clean, runs RED (health.ts/worker.ts/cron route not yet created). deriveHealth unit assertions are precise and reference the documented thresholds. min 50 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → source files | Tests only READ source/migrations; no secrets in fixtures. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | test fixtures | mitigate | Use a throwaway plaintext like 'api-key-1234' for the AES round-trip; never a real secret. ENCRYPTION_KEY for tests comes from setup.ts (same mechanism as icp tests), not hardcoded. |
| T-09-02 | Tampering | regression assertions | mitigate | webhooks.test.ts asserts the existing 200-response + dedup lines still exist, so a later additive edit that accidentally removes them turns the test RED. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (all 3 test files compile under strict).
- `npx vitest run src/__tests__/integrations/` runs (RED is expected — implementation not built).
- AES round-trip + deriveHealth unit assertions are GREEN immediately (crypto.ts exists; deriveHealth import is RED until Plan 02).
</verification>

<success_criteria>
3 test files exist under src/__tests__/integrations/, compile clean, and fail RED against
not-yet-created artifacts. Every INT-01/02/03 behavior from 09-VALIDATION.md maps to at least
one assertion here.
</success_criteria>

<output>
After completion, create `.planning/phases/09-hub-de-integra-es-externas/09-01-SUMMARY.md`
</output>
