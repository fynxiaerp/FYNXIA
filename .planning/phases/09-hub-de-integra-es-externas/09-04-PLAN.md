---
phase: 09-hub-de-integra-es-externas
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified:
  - src/types/database.types.ts
autonomous: false
requirements: [INT-01, INT-02, INT-03]
must_haves:
  truths:
    - "The integration_connectors and integration_events tables exist in the live Supabase project (jqjwyqlbbuqnrffdnlpp / org kczvihafddupruvsrrsc)"
    - "credential_enc is NOT SELECTable by the authenticated/anon roles in the live DB (column REVOKE applied)"
    - "src/types/database.types.ts contains the new tables and was regenerated WITHOUT truncation"
    - "RLS is enabled on both new tables in the live DB"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "regenerated Supabase types including integration_connectors + integration_events"
      contains: "integration_connectors"
  key_links:
    - from: "supabase/migrations/*_integration_connectors.sql"
      to: "live Supabase project"
      via: "supabase db push (after re-auth on correct org/project)"
      pattern: "integration_connectors"
    - from: "live Supabase schema"
      to: "src/types/database.types.ts"
      via: "supabase gen types typescript (temp-file truncation guard)"
      pattern: "integration_events"
---

<objective>
[BLOCKING] Push the three Phase 9 migrations (`integration_connectors`, `integration_events`,
`integration_revoke`) to the live Supabase project, then regenerate `database.types.ts` with the
temp-file truncation guard, then run post-push DB checks. This is the ONLY plan in Phase 9 that
runs `supabase db push` — every migration written in Plan 02 lands here, BEFORE the UI panel
(Plan 05) consumes the new tables/types.

Purpose: Make the credential vault (INT-01), event log (INT-02), and health/retry tables (INT-03)
real in the database so the UI can read them and `gen types` exposes them to TypeScript.
Output: live tables + REVOKE + RLS verified; `src/types/database.types.ts` regenerated, un-truncated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-hub-de-integra-es-externas/09-VALIDATION.md
@./CLAUDE.md

# Migrations to push (written by Plan 02 — DO NOT modify here, only push):
@supabase/migrations/20260615000400_integration_connectors.sql
@supabase/migrations/20260615000500_integration_events.sql
@supabase/migrations/20260615000600_integration_revoke.sql

# Recurring re-auth gotcha + project/org IDs:
# project jqjwyqlbbuqnrffdnlpp, org kczvihafddupruvsrrsc, region sa-east-1.
# CLI/MCP is often logged into the WRONG account (nexus-*) → re-login before db push.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] supabase db push + gen types (truncation guard) + post-push DB checks</name>
  <files>src/types/database.types.ts</files>
  <read_first>09-VALIDATION.md §"Sampling Rate" (the [BLOCKING] db push step + re-auth gotcha), supabase/migrations/20260615000400_integration_connectors.sql, supabase/migrations/20260615000600_integration_revoke.sql</read_first>
  <what-built>
Plan 02 wrote three migrations (NOT yet pushed). Plan 03 wired the handlers/worker/cron/proxy
against those table shapes. Nothing has touched the live DB yet. This task pushes the migrations
and regenerates types. It is a checkpoint because `supabase db push` may require an interactive
re-login (recurring gotcha: the CLI is frequently authenticated against the wrong `nexus-*`
account instead of the FYNXIA org).
  </what-built>
  <action>
Execute the following IN ORDER. Stop and surface to the human at the first failure.

1. VERIFY CORRECT ACCOUNT/PROJECT (re-auth gotcha):
   - Run `supabase projects list`.
   - Confirm the output shows a `●` (linked) on project `jqjwyqlbbuqnrffdnlpp` under org
     `kczvihafddupruvsrrsc` (region sa-east-1).
   - If the linked project is wrong, or the project is absent (logged into a `nexus-*` account):
     run `supabase login` (human completes the browser auth), then `supabase link --project-ref
     jqjwyqlbbuqnrffdnlpp`, then re-run `supabase projects list` to confirm the `●`.

2. PUSH MIGRATIONS:
   - Run `supabase db push`.
   - Confirm all three Phase 9 migrations apply: `20260615000400_integration_connectors`,
     `20260615000500_integration_events`, `20260615000600_integration_revoke`.
   - If push errors (e.g. dependency on get_my_tenant_id/get_my_role or clinics FK), surface the
     exact error to the human — do NOT edit the migration files here (they are Plan 02's artifacts;
     a fix routes back to a Plan 02 revision).

3. REGENERATE TYPES WITH TRUNCATION GUARD (never overwrite the real file blindly):
   - Generate to a TEMP file:
     `supabase gen types typescript --project-id jqjwyqlbbuqnrffdnlpp --schema public > /tmp/db.types.gen.ts`
   - GUARD A (size): the temp file MUST have > 1000 lines:
     `[ "$(wc -l < /tmp/db.types.gen.ts)" -gt 1000 ] || { echo 'ABORT: gen types truncated (<1000 lines)'; exit 1; }`
   - GUARD B (content): the temp file MUST contain BOTH new tables:
     `grep -q 'integration_connectors:' /tmp/db.types.gen.ts && grep -q 'integration_events:' /tmp/db.types.gen.ts || { echo 'ABORT: new tables missing from gen output'; exit 1; }`
   - ONLY if BOTH guards pass: overwrite `src/types/database.types.ts` with the temp file
     (`cp /tmp/db.types.gen.ts src/types/database.types.ts`).
   - If either guard fails: leave `src/types/database.types.ts` UNCHANGED, surface to the human
     (likely wrong project ref or a transient CLI truncation — re-run gen types).

4. POST-PUSH DB CHECKS (read-only confirmations against the live DB; use `supabase db` SQL or the
   Supabase MCP SQL tool against project jqjwyqlbbuqnrffdnlpp):
   - Both tables exist:
     `SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('integration_connectors','integration_events');` → 2 rows.
   - REVOKE applied (credential_enc NOT selectable by authenticated/anon):
     `SELECT grantee, privilege_type FROM information_schema.column_privileges WHERE table_name='integration_connectors' AND column_name='credential_enc' AND grantee IN ('authenticated','anon');` → 0 rows for SELECT.
   - RLS enabled on both:
     `SELECT relname, relrowsecurity FROM pg_class WHERE relname IN ('integration_connectors','integration_events');` → relrowsecurity = true for both.
   - Seed sentinels present:
     `SELECT type FROM public.integration_connectors WHERE clinic_id IS NULL;` → asaas, whatsapp, email.

5. Run `npx tsc --noEmit` — must be clean against the regenerated types.
  </action>
  <how-to-verify>
1. `supabase projects list` shows `●` on jqjwyqlbbuqnrffdnlpp (org kczvihafddupruvsrrsc).
2. `supabase db push` applied the three 20260615000400/500/600 migrations with no error.
3. `wc -l src/types/database.types.ts` > 1000 AND `grep integration_connectors src/types/database.types.ts` AND `grep integration_events src/types/database.types.ts` both match.
4. The 4 post-push SQL checks return the expected rows (2 tables / 0 SELECT grants on credential_enc / RLS true / 3 sentinels).
5. `npx tsc --noEmit` clean.
  </how-to-verify>
  <resume-signal>Type "pushed" once all five steps pass, or paste the failing step's output.</resume-signal>
  <verify>
    <automated>grep -q "integration_connectors" "src/types/database.types.ts" && grep -q "integration_events" "src/types/database.types.ts" && [ "$(wc -l < src/types/database.types.ts)" -gt 1000 ] && echo "TYPES OK" || echo "TYPES MISSING/TRUNCATED — push+gen not yet done"</automated>
  </verify>
  <done>Live DB has both tables with RLS on + REVOKE on credential_enc + 3 system sentinels. database.types.ts regenerated, > 1000 lines, contains both new tables, tsc clean. No migration file edited in this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local CLI → live Supabase project | `supabase db push` mutates the production schema; must target the correct org/project. |
| live schema → committed types file | `gen types` output overwrites a source-controlled file; truncation could silently break the build. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Tampering | wrong-project db push (nexus-* account) | mitigate | Step 1 mandates `supabase projects list` confirms the `●` on jqjwyqlbbuqnrffdnlpp/org kczvihafddupruvsrrsc BEFORE push; re-login if not. |
| T-09-17 | Denial of Service | truncated gen types breaks compile | mitigate | Temp-file generation + GUARD A (>1000 lines) + GUARD B (both tables present); real file overwritten ONLY when both guards pass. |
| T-09-18 | Information Disclosure | credential_enc readable post-push | mitigate | Post-push check 4b asserts 0 SELECT grants on credential_enc for authenticated/anon (REVOKE landed). |
| T-09-19 | Elevation of Privilege | RLS not actually enabled in live DB | mitigate | Post-push check 4c asserts relrowsecurity=true on both tables. |
</threat_model>

<verification>
- `supabase db push` applied all three migrations; `supabase migration list` shows them remote.
- Post-push SQL checks: 2 tables, 0 credential_enc SELECT grants, RLS true ×2, 3 sentinels.
- `src/types/database.types.ts` > 1000 lines + contains both tables (guard-verified).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
The Phase 9 schema is live and the regenerated types are committed un-truncated, unblocking the
UI panel (Plan 05). credential_enc is provably non-selectable by tenant roles; RLS is on.
</success_criteria>

<output>
After completion, create `.planning/phases/09-hub-de-integra-es-externas/09-04-SUMMARY.md`
</output>
</content>
</invoke>
