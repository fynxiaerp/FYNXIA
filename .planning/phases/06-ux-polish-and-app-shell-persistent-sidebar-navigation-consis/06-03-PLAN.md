---
phase: 06-ux-polish-and-app-shell
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - package.json
  - package-lock.json
  - public/fynxia-logo.png
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/avatar.tsx
  - src/hooks/useSidebarStore.ts
  - src/components/shell/ThemeToggle.tsx
  - src/components/shell/SidebarNavClient.tsx
  - src/components/shell/SidebarFooter.tsx
  - src/components/shell/AppSidebar.tsx
  - src/components/shell/AppShellClient.tsx
  - src/app/(dashboard)/clinica/layout.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every /clinica/* page renders inside a persistent left sidebar (~240px)"
    - "The sidebar shows the FYNXIA logo, role-gated module nav, and a footer with theme toggle + Sair"
    - "The active module is highlighted via usePathname"
    - "The sidebar collapses to a 56px icon rail and the content margin tracks it"
    - "The CopilotTrigger still floats independently"
  artifacts:
    - path: "src/components/shell/AppSidebar.tsx"
      provides: "Server Component reading role + clinic; renders header/nav/footer"
      min_lines: 40
    - path: "src/components/shell/SidebarNavClient.tsx"
      provides: "Client nav with usePathname active detection (role-gated items as prop)"
    - path: "src/components/shell/ThemeToggle.tsx"
      provides: "next-themes Sun/Moon toggle with mounted guard"
    - path: "src/hooks/useSidebarStore.ts"
      provides: "Zustand isCollapsed toggle"
    - path: "src/app/(dashboard)/clinica/layout.tsx"
      provides: "Shell: AppSidebar + margin-tracking content area + CopilotTrigger"
  key_links:
    - from: "src/app/(dashboard)/clinica/layout.tsx"
      to: "src/components/shell/AppSidebar.tsx"
      via: "import + render in flex shell"
      pattern: "AppSidebar"
    - from: "src/components/shell/SidebarNavClient.tsx"
      to: "next/navigation"
      via: "usePathname active item"
      pattern: "usePathname"
    - from: "src/components/shell/AppSidebar.tsx"
      to: "supabase users.role"
      via: "server-side role read mirrors clinica/page.tsx"
      pattern: "role"
---

<objective>
Build the persistent app shell (D-02). Install shadcn `dropdown-menu` + `avatar`, move the logo to
`public/`, create the Zustand collapse store, the `ThemeToggle`, the role-gated `SidebarNavClient`,
the `SidebarFooter` (clinic/user + ThemeToggle + Sair), and the `AppSidebar` Server Component, then
replace the passthrough `clinica/layout.tsx` with a real shell that mounts the sidebar, a
margin-tracking content area, and the existing `CopilotTrigger`.

Purpose: the single highest-leverage audit fix (UI-REVIEW Priority #1 — no persistent shell).
Output: every authenticated /clinica page gains a sidebar with wayfinding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-RESEARCH.md
@src/app/(dashboard)/clinica/layout.tsx
@src/app/(dashboard)/clinica/page.tsx
@CLAUDE.md

<interfaces>
<!-- Role read pattern (copy verbatim from clinica/page.tsx) — AppSidebar is a Server Component: -->
<!--   const supabase = await createClient()
       const { data: { user } } = await supabase.auth.getUser()
       const { data: me } = user ? await supabase.from('users').select('role').eq('id', user.id).single() : { data: null }
       const role = me?.role ?? 'receptionist'
       const { data: clinic } = await supabase.from('clinics').select('name').single()
       const isAdmin = role === 'admin' || role === 'superadmin' -->
<!-- CopilotTrigger import (existing): import { CopilotTrigger } from '@/components/copilot/CopilotTrigger' -->
<!-- signOut action (existing): import { signOut } from '@/actions/auth' -->
<!-- @base-ui Button uses render-prop, NOT asChild (CLAUDE.md). For nav use plain next/link <Link>. -->
<!-- shadcn sidebar Tailwind-v4 width bug (06-RESEARCH Pitfall 1): DO NOT install shadcn `sidebar`. -->
<!--   Use a custom fixed-width layout (w-[240px]/w-[56px]) + Zustand, per 06-RESEARCH Pattern 4. -->

Nav items (06-UI-SPEC lines 364-370), lucide icons:
  Agenda      Calendar     always       /clinica/agenda
  Pacientes   Users        always       /clinica/pacientes
  Financeiro  DollarSign   always       /clinica/financeiro
  Equipe      UserCog      admin only   /clinica/equipe
  IA / Agentes BrainCircuit always      /clinica/ia/agentes

ThemeToggle (06-RESEARCH Code Examples lines 516-540): mounted guard; aria-label
  light->'Ativar tema escuro', dark->'Ativar tema claro'.

SidebarNavClient active detection (06-RESEARCH lines 587-622): pathname.startsWith(href);
  active = bg-sidebar-primary text-sidebar-primary-foreground; aria-current="page".

useSidebarStore (06-RESEARCH lines 289-302): create<...>((set)=>({ isCollapsed:false, toggle }))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deps, logo, store, ThemeToggle, SidebarNavClient</name>
  <files>package.json, package-lock.json, public/fynxia-logo.png, src/components/ui/dropdown-menu.tsx, src/components/ui/avatar.tsx, src/hooks/useSidebarStore.ts, src/components/shell/ThemeToggle.tsx, src/components/shell/SidebarNavClient.tsx</files>
  <read_first>
    - 06-RESEARCH.md (Standard Stack installs; Pattern 4 Zustand store; Code Examples ThemeToggle + SidebarNavClient; Pitfall 1 — skip shadcn sidebar)
    - 06-UI-SPEC.md (SidebarNav lines 360-385; ThemeToggle line 394)
    - CLAUDE.md (shadcn first; @base-ui render-prop)
  </read_first>
  <action>
    1. Run `npx shadcn@latest add dropdown-menu avatar` (skip `sidebar` — Tailwind-v4 width bug, 06-RESEARCH Pitfall 1).
    2. Move the logo: copy `.firecrawl/fynxia-logo.png` → `public/fynxia-logo.png` (use Bash copy; do not delete the source). The logo has a dark navy background baked in — the dark-chip treatment makes it legible in both themes.
    3. Create `src/hooks/useSidebarStore.ts` exactly per 06-RESEARCH Pattern 4 (Zustand `create`; `isCollapsed: false`; `toggle`).
    4. Create `src/components/shell/ThemeToggle.tsx` (`'use client'`) per 06-RESEARCH Code Examples — `useTheme` + `mounted` guard; renders `Sun` in dark / `Moon` in light; classes `h-8 w-8 rounded-md hover:bg-sidebar-accent flex items-center justify-center transition-colors`; dynamic `aria-label`.
    5. Create `src/components/shell/SidebarNavClient.tsx` (`'use client'`) per 06-RESEARCH Code Examples — props `{ items: NavItem[]; isCollapsed: boolean }`; `usePathname()`; active item `bg-sidebar-primary text-sidebar-primary-foreground rounded-md`; inactive `text-sidebar-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground`; `aria-current="page"` on active; in icon-rail mode show icon only + `aria-label` on the `<Link>` and a shadcn `Tooltip` (right side) with the module name. Items carry `{ href, label, icon }` — icons passed from the server component as lucide components.
  </action>
  <verify>
    <automated>npx tsc --noEmit; node -e "if(!require('fs').existsSync('public/fynxia-logo.png'))process.exit(1)"</automated>
  </verify>
  <done>dropdown-menu.tsx + avatar.tsx present; public/fynxia-logo.png exists; store + ThemeToggle + SidebarNavClient created; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: SidebarFooter + AppSidebar (Server Component)</name>
  <files>src/components/shell/SidebarFooter.tsx, src/components/shell/AppSidebar.tsx</files>
  <read_first>
    - 06-UI-SPEC.md (SidebarHeader lines 343-358 logo chip; SidebarFooter lines 387-396; AppSidebar contract lines 324-342)
    - src/app/(dashboard)/clinica/page.tsx (role + clinic read pattern to copy)
    - src/components/shell/ThemeToggle.tsx, src/components/shell/SidebarNavClient.tsx (just created)
  </read_first>
  <action>
    1. Create `src/components/shell/SidebarFooter.tsx`. Receives `{ clinicName, userEmail, isCollapsed }` (passed down) OR reads nothing (server passes props). Layout per 06-UI-SPEC lines 387-396: clinic name `text-sm font-semibold truncate`; user email `text-xs text-muted-foreground truncate`; a row with `<ThemeToggle />` and the Sair button. Sair = `<form action={signOut}>` ghost-variant submit button, label "Sair" (`text-sm`), `aria-label="Sair da conta"` (keep label + aria-label per audit). In icon-rail mode collapse to icons only. Use shadcn `Avatar` for the user initials chip.
    2. Create `src/components/shell/AppSidebar.tsx` as a **Server Component** (no `'use client'`). Read role + clinic name server-side using the exact pattern from `clinica/page.tsx` (interfaces block). Build the nav items array with the 5 modules; gate Equipe behind `isAdmin`. Render the container per 06-UI-SPEC line 336: `fixed inset-y-0 left-0 z-30 flex flex-col bg-sidebar border-r border-sidebar-border transition-[width] duration-200` with the collapse-driven width handled by the client wrapper (Task 3) via a shared CSS var or by passing `isCollapsed` — since collapse is client state, AppSidebar renders the static structure and the **AppShellClient** (Task 3) owns the width/collapse state. Concretely: AppSidebar renders SidebarHeader (logo chip per 06-UI-SPEC lines 347-353 — `rounded-xl overflow-hidden bg-[hsl(240_20%_8%)] flex items-center gap-2 px-3 py-2` — note `py-2` not `py-1.5` per audit fix; `<Image src="/fynxia-logo.png" alt="FYNXIA" width={28} height={28}>` + wordmark `text-white font-display font-bold text-sm tracking-tight`), then `<SidebarNavClient items={...} isCollapsed={...}/>`, then `<SidebarFooter .../>`. Pass `isCollapsed` from the client wrapper.
    Note: because `isCollapsed` is client state, AppSidebar is rendered as a child of AppShellClient which provides the collapse value via a prop or context; keep AppSidebar a Server Component by having AppShellClient read the store and pass `isCollapsed` down as a prop to AppSidebar (Server Components accept serializable props from Client parents).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/shell.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>AppSidebar (Server) renders logo chip + role-gated nav + footer (ThemeToggle + Sair); shell.test.ts AppSidebar assertions PASS; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: AppShellClient + replace clinica/layout.tsx</name>
  <files>src/components/shell/AppShellClient.tsx, src/app/(dashboard)/clinica/layout.tsx</files>
  <read_first>
    - 06-UI-SPEC.md (Shell Layout Contract lines 296-322; Layout Patterns lines 546-572; Responsive lines 847-855)
    - 06-RESEARCH.md (Pattern 3 shell layout; Pitfall 5 margin tracking; Pitfall 4 server/client split)
    - src/app/(dashboard)/clinica/layout.tsx (current passthrough)
  </read_first>
  <action>
    1. Create `src/components/shell/AppShellClient.tsx` (`'use client'`). Reads `useSidebarStore()` for `isCollapsed`. Renders a flex shell: a sidebar slot and a content slot. It receives `sidebar` and `children` as props (so AppSidebar can stay a Server Component rendered by the layout and passed in). Apply width/margin tracking: when collapsed, content uses `ml-[56px]`, else `ml-[240px]`, with `transition-[margin] duration-200`. The sidebar slot wrapper applies `w-[240px]`/`w-[56px]` accordingly. Also render the mobile behavior hook-up: at `<md` the fixed sidebar is hidden (`hidden md:flex`) — the mobile Sheet drawer is opened from the PageHeader hamburger (built in 06-04); AppShellClient exposes the same nav via a Sheet is OPTIONAL here — minimum: hide sidebar `<md` and let content go full width (`ml-0`). Pass `isCollapsed` into the sidebar subtree via a prop on the rendered AppSidebar (the layout wires this).
    2. Replace `src/app/(dashboard)/clinica/layout.tsx` (Server Component) so it renders:
       ```
       <AppShellClient sidebar={<AppSidebar />}>
         {children}
       </AppShellClient>
       <CopilotTrigger />
       ```
       Wrap in a `<div className="flex h-screen overflow-hidden">` per 06-UI-SPEC line 561. The content area is `<div className="flex flex-col flex-1 min-w-0 ..."><main id="main-content" className="flex-1 overflow-y-auto">{children}</main></div>` — actual `<main>` padding (p-6 vs p-0 for agenda) is owned by each page in later waves; here just establish the scroll container with `id="main-content"`.
       Keep `CopilotTrigger` import + render (it stays a floating FAB, independent of the sidebar).
    Resolve the Server/Client split so `isCollapsed` reaches AppSidebar: AppShellClient reads the store and clones/wraps the passed `sidebar` element with the `isCollapsed` prop, OR pass a render function. Simplest: AppShellClient renders `<aside className={cn('hidden md:flex shrink-0 transition-[width] duration-200', isCollapsed ? 'w-[56px]' : 'w-[240px]')}>{sidebar}</aside>` and AppSidebar reads collapse via the store directly is NOT possible (Server Component) — therefore make the sidebar's INNER widths driven by AppSidebar receiving `isCollapsed`. Decision for executor: pass `isCollapsed` to AppSidebar by making the layout a thin client wrapper is disallowed (needs server role read). Use this contract: AppShellClient takes `sidebar` (the AppSidebar element already rendered with role data) and only controls the OUTER `<aside>` width + content `ml`; AppSidebar internally renders nav labels conditionally by reading `useSidebarStore` **inside its client children** (SidebarNavClient/SidebarFooter/ThemeToggle are already client) — so AppSidebar passes NO isCollapsed and instead SidebarNavClient + SidebarFooter call `useSidebarStore()` themselves. Update Task-1/Task-2 components accordingly if needed (they may read the store directly rather than receive isCollapsed as a prop).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/shell.test.ts; npx tsc --noEmit; npx next build</automated>
  </verify>
  <done>clinica/layout.tsx mounts AppSidebar via AppShellClient + keeps CopilotTrigger; content has id="main-content"; shell.test.ts PASS; tsc exits 0; next build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Server (AppSidebar role/clinic read via Supabase RLS) → client rendering. Auth boundary unchanged from prior phases.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Elevation of privilege | role-gated nav (Equipe admin-only) | accept | Nav visibility is UI-only defense-in-depth; the real boundary remains server-side route protection + Supabase RLS (unchanged). Hiding the Equipe link does NOT grant/deny access. |
| T-06-05 | Information disclosure | SidebarFooter (clinic name + user email) | accept | Both already visible post-auth (hub page); RLS-scoped; no new PII surface. |
| T-06-06 | Information disclosure | public/fynxia-logo.png | accept | Brand asset intentionally public; no sensitive data. |
</threat_model>

<verification>
- shell.test.ts GREEN.
- `npx tsc --noEmit` exits 0; `npx next build` green.
- Manual (UAT, deferred): collapse to icon-rail, active highlight, mobile drawer, theme toggle persistence.
</verification>

<success_criteria>
/clinica/* pages render inside a persistent ~240px sidebar (logo + role-gated nav + footer theme toggle/Sair) that collapses to a 56px rail; CopilotTrigger still floats; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-03-SUMMARY.md`
</output>
