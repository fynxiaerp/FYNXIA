---
phase: 06-ux-polish-and-app-shell
plan: 08
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - src/app/(dashboard)/clinica/equipe/page.tsx
  - src/app/(dashboard)/clinica/equipe/loading.tsx
  - src/app/(dashboard)/clinica/equipe/error.tsx
  - src/app/(dashboard)/clinica/ia/agentes/page.tsx
  - src/app/(dashboard)/clinica/ia/agentes/loading.tsx
  - src/app/(dashboard)/clinica/ia/agentes/error.tsx
  - src/components/copilot/AgentOutreachLog.tsx
  - src/components/copilot/CopilotSidebar.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The Equipe page is rebuilt on design tokens (no slate/gray/bg-white) and works in dark mode"
    - "Equipe and IA/Agentes use PageHeader + shadcn Table (no raw HTML tables)"
    - "Empty states use lucide icons; the stale 'Fase 2' note is removed"
    - "The CopilotSidebar header Bot icon is text-muted-foreground (not accent)"
  artifacts:
    - path: "src/app/(dashboard)/clinica/equipe/page.tsx"
      provides: "Full token rewrite: PageHeader + shadcn Table + Badge + EmptyState"
      min_lines: 40
    - path: "src/components/copilot/AgentOutreachLog.tsx"
      provides: "shadcn Table (replaces raw HTML table)"
    - path: "src/components/copilot/CopilotSidebar.tsx"
      provides: "Bot icon text-muted-foreground (decorative, not accent)"
  key_links:
    - from: "src/app/(dashboard)/clinica/equipe/page.tsx"
      to: "design tokens + shadcn Table"
      via: "bg-card/text-foreground/border-border replace slate/gray; <Table>"
      pattern: "bg-card"
    - from: "src/components/copilot/AgentOutreachLog.tsx"
      to: "src/components/ui/table"
      via: "shadcn Table import"
      pattern: "@/components/ui/table"
---

<objective>
Sweep the team, AI-log, and copilot surfaces (D-04). Fully rewrite `equipe/page.tsx` on design tokens
(the worst design island — raw slate/gray/bg-white, broken dark mode) using `PageHeader` + shadcn
`Table`/`Badge`/`Breadcrumb` + `EmptyState`, removing the stale "Fase 2" note. Convert
`AgentOutreachLog` from a raw HTML `<table>` to shadcn `Table`, add `PageHeader` to the IA/Agentes
log page, and change the `CopilotSidebar` header Bot icon from accent to muted.

Purpose: closes UI-REVIEW Priority #3 (Equipe design island) + the third-table-pattern + copilot
Bot-icon accent findings.
Output: a token-consistent team/AI surface across both themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md
@.planning/reports/UI-REVIEW-v1.0.md
@src/app/(dashboard)/clinica/equipe/page.tsx
@src/components/copilot/AgentOutreachLog.tsx
@src/components/copilot/CopilotSidebar.tsx
@CLAUDE.md

<interfaces>
<!-- Reuse Wave 2: PageHeader, EmptyState, ErrorState, skeletons. shadcn Table/Badge/Breadcrumb -->
<!-- already installed. error.tsx = 3-line ErrorState wrapper. -->
<!-- Token swap map (06-UI-SPEC lines 198-200, 688): -->
<!--   bg-gray-50/bg-white -> bg-background/bg-card; text-slate-900 -> text-foreground; -->
<!--   text-slate-400/500/600 -> text-muted-foreground; border-slate-200 -> border-border. -->
<!-- Role badge: shadcn Badge variant secondary OR bg-muted text-muted-foreground (NOT bg-yellow-100). -->

Equipe (06-UI-SPEC lines 682-693):
  PageHeader title "Equipe", breadcrumb "Clínica > Equipe", actions [Convidar Membro];
  shadcn Table; remove "← Voltar" (use Breadcrumb); remove stale Fase 2 note; max-w-4xl mx-auto;
  empty Users "Nenhum membro na equipe" CTA "Convidar Membro" (06-UI-SPEC line 503).
  No text-lg/text-base/font-bold/font-medium.
IA/Agentes (06-UI-SPEC lines 695-699):
  PageHeader title "Ações dos Agentes IA", breadcrumb "Clínica > IA > Agentes", no actions; max-w-4xl mx-auto;
  AgentOutreachLog -> shadcn Table; empty BrainCircuit "Nenhuma ação registrada ainda".
Copilot (06-UI-SPEC lines 701-706):
  CopilotSidebar header Bot icon text-primary -> text-muted-foreground. No other functional change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Equipe full rewrite on tokens</name>
  <files>src/app/(dashboard)/clinica/equipe/page.tsx, src/app/(dashboard)/clinica/equipe/loading.tsx, src/app/(dashboard)/clinica/equipe/error.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/equipe/page.tsx (current — full slate/gray rewrite target)
    - 06-UI-SPEC.md (Equipe lines 682-693; Fix lines 198-200; Empty State line 503; skeleton line 449)
    - UI-REVIEW-v1.0.md (Team Management section — 22 raw color violations)
  </read_first>
  <action>
    Rewrite `equipe/page.tsx` from the ground up (preserve the existing data fetching + invite/role
    server-action wiring — change ONLY presentation):
    - Replace `<header>`+`<main>` semantic layout with: `<PageHeader title="Equipe" breadcrumbs={[{label:'Clínica',href:'/clinica'},{label:'Equipe'}]} actions={<Button>Convidar Membro</Button>} mobileMenuTrigger={...}/>` then `<main>` content `p-6 max-w-4xl mx-auto w-full`.
    - Remove the "← Voltar" anchor (Breadcrumb replaces it). Remove the stale "Gestão completa… Fase 2" note entirely.
    - Replace every raw color class: `bg-gray-50`/`bg-white` → `bg-background`/`bg-card`; `text-slate-900` → `text-foreground`; `text-slate-400/500/600` → `text-muted-foreground`; `border-slate-200` → `border-border`; `bg-amber-50 border-amber-200` (pending-invite warning) → `bg-muted border-border` or a shadcn Alert.
    - Replace the raw `<table>` with shadcn `Table`/`TableHeader`/`TableBody`/`TableRow`/`TableHead`/`TableCell`. Column headers `text-sm font-semibold`.
    - Role badge: replace `bg-yellow-100 text-yellow-800` with shadcn `Badge variant="secondary"` (or `bg-muted text-muted-foreground`).
    - Typography: no `text-lg`/`text-base`/`font-bold`/`font-medium` — section headings `text-xl font-semibold` or labels `text-sm font-semibold`.
    - Empty state (no team members): `Users` EmptyState "Nenhum membro na equipe" + CTA "Convidar Membro".
    Create `loading.tsx` (PageHeaderSkeleton + 3 TableRowsSkeleton) and `error.tsx` (ErrorState wrapper).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/typography.test.ts src/__tests__/ui/page-pattern.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>Equipe uses tokens (zero slate/gray/bg-white) + PageHeader + shadcn Table + Badge; no stale note; no text-lg/base/font-bold/medium; loading/error exist; typography.test.ts equipe assertion PASS; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: IA/Agentes — PageHeader + shadcn Table + empty/loading/error</name>
  <files>src/app/(dashboard)/clinica/ia/agentes/page.tsx, src/app/(dashboard)/clinica/ia/agentes/loading.tsx, src/app/(dashboard)/clinica/ia/agentes/error.tsx, src/components/copilot/AgentOutreachLog.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/ia/agentes/page.tsx + src/components/copilot/AgentOutreachLog.tsx (current — raw <table>)
    - 06-UI-SPEC.md (IA/Agentes lines 695-699; Empty State line 504; skeleton line 450)
  </read_first>
  <action>
    1. `ia/agentes/page.tsx`: add `<PageHeader title="Ações dos Agentes IA" breadcrumbs={[{label:'Clínica',href:'/clinica'},{label:'IA',href:'/clinica/ia/agentes'},{label:'Agentes'}]} mobileMenuTrigger={...}/>` (no actions). Content `p-6 max-w-4xl mx-auto w-full`. Empty state (no log entries): `BrainCircuit` EmptyState "Nenhuma ação registrada ainda", body per 06-UI-SPEC line 504, no CTA.
    2. `AgentOutreachLog.tsx`: replace the raw HTML `<table>` with shadcn `Table` primitives (establishes the single table pattern). Preserve the existing masked patient-name rendering + status display (presentation-only change). Column headers `text-sm font-semibold`.
    3. Create `loading.tsx` (PageHeaderSkeleton + 5 TableRowsSkeleton) and `error.tsx` (ErrorState wrapper).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/page-pattern.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>IA/Agentes uses PageHeader; AgentOutreachLog uses shadcn Table; empty/loading/error present; masking preserved; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Copilot Bot-icon polish</name>
  <files>src/components/copilot/CopilotSidebar.tsx</files>
  <read_first>
    - src/components/copilot/CopilotSidebar.tsx (current — header Bot icon text-primary, ~line 58)
    - 06-UI-SPEC.md (Copilot lines 701-706 — Bot icon decorative, verify bg-card)
    - UI-REVIEW-v1.0.md (AI/Copilot module — Bot icon finding)
  </read_first>
  <action>
    In `CopilotSidebar.tsx`, change the header `Bot` icon class from `text-primary` to
    `text-muted-foreground` (decorative, not accent — accent reserved list). Confirm the Panel
    background uses `bg-card` in both themes (06-UI-SPEC line 705). No functional changes — Phase 5
    copilot logic is the best-quality module and must not be altered beyond this single icon class +
    background-token confirmation.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/; npx tsc --noEmit; npx next build</automated>
  </verify>
  <done>CopilotSidebar header Bot icon is text-muted-foreground; Panel uses bg-card; full ui test suite GREEN; tsc exits 0; next build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Equipe (invite/role management) and AgentOutreachLog go through existing RLS-scoped server actions. Copilot data path unchanged.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Information disclosure | AgentOutreachLog patient names | accept | Existing `maskPatientName()` (FirstName L.) rendering preserved in the Table rewrite; no unmasking. |
| T-06-16 | Elevation of privilege | Equipe invite/role actions | mitigate | Presentation rewrite only — invite + role-change server actions and their admin authorization are left untouched; do not alter the action layer. |
| T-06-17 | Information disclosure | Equipe member emails | accept | Already visible to admins post-auth; RLS-scoped; no new PII surface. |
</threat_model>

<verification>
- Full `npx vitest run src/__tests__/ui/` GREEN (this is the last Wave-3 plan — all source-inspection contracts should now pass).
- `npx tsc --noEmit` exits 0; `npx next build` green.
- Manual (UAT, deferred): Equipe in dark mode, IA log table, copilot icon, both themes.
</verification>

<success_criteria>
Equipe is rebuilt on tokens (works in dark mode) with PageHeader + shadcn Table; IA/Agentes uses PageHeader + shadcn Table; stale note removed; copilot Bot icon muted; full UI test suite + build green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-08-SUMMARY.md`
</output>
