---
phase: 06-ux-polish-and-app-shell
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/ui/theme.test.ts
  - src/__tests__/ui/contrast.test.ts
  - src/__tests__/ui/shell.test.ts
  - src/__tests__/ui/page-pattern.test.ts
  - src/__tests__/ui/typography.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "The 5 source-inspection test files from 06-VALIDATION.md exist and run under vitest"
    - "Tests are RED-by-design (assert contracts not yet implemented) but tsc stays green"
    - "Contrast test is a pure unit test asserting light --primary #007a85 on white >= 4.5:1"
  artifacts:
    - path: "src/__tests__/ui/theme.test.ts"
      provides: "Asserts globals.css :root+.dark token blocks + layout.tsx theme/font wiring"
    - path: "src/__tests__/ui/contrast.test.ts"
      provides: "Pure WCAG contrast fn asserting key token pairs >= AA"
    - path: "src/__tests__/ui/shell.test.ts"
      provides: "Asserts clinica/layout.tsx mounts AppSidebar + sidebar structure"
    - path: "src/__tests__/ui/page-pattern.test.ts"
      provides: "Asserts PageHeader exists + representative pages use it + loading/error files"
    - path: "src/__tests__/ui/typography.test.ts"
      provides: "Asserts no off-contract font-bold/medium/text-3xl/lg/base in audited screens"
  key_links:
    - from: "vitest"
      to: "src/__tests__/ui/*.test.ts"
      via: "npx vitest run src/__tests__/ui/"
      pattern: "describe\\(|it\\("
---

<objective>
Author the 5 Wave-0 source-inspection test scaffolds from 06-VALIDATION.md. These define the
executable contracts for the rest of Phase 6: theme tokens, contrast, app shell, page pattern,
and typography normalization. Tests use `readFileSync` + `toMatch` (source inspection) and pure
functions only — never importing application components — so `npx tsc --noEmit` stays green even
while the contracts are unimplemented (RED-by-design).

Purpose: lock the acceptance criteria for every downstream wave into runnable tests before any
implementation, per the Nyquist validation strategy.
Output: 5 test files under `src/__tests__/ui/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-VALIDATION.md
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md
@vitest.config.ts

<interfaces>
<!-- Source-inspection style confirmed from Phase 5 scaffolds (STATE.md): tests read files with -->
<!-- readFileSync + existsSync guards and assert with toMatch. They NEVER import app components -->
<!-- (keeps tsc green even when the file under test is broken/unwritten). -->
<!-- Vitest 4.1.8 is already installed. Run command: npx vitest run src/__tests__/ui/ -->

Pattern to use in every test file:
```ts
import { describe, it, expect } from 'vitest'
import { readFileSync, existsSync } from 'node:fs'
import { resolve } from 'node:path'

function read(rel: string): string {
  const p = resolve(process.cwd(), rel)
  return existsSync(p) ? readFileSync(p, 'utf8') : ''
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: theme.test.ts + contrast.test.ts</name>
  <files>src/__tests__/ui/theme.test.ts, src/__tests__/ui/contrast.test.ts</files>
  <read_first>
    - .planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-VALIDATION.md (Wave 0 Requirements — theme + contrast bullets)
    - .planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md (globals.css Token Block lines 865-956; layout.tsx font wiring lines 246-261)
    - .planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-RESEARCH.md (Critical Token Issue section — 26% = 5.12:1)
  </read_first>
  <action>
    Create `theme.test.ts` (source inspection of `src/app/globals.css` and `src/app/layout.tsx`):
    - globals.css contains a `:root {` block AND a `.dark {` block.
    - globals.css light `:root` declares `--primary: hsl(185 100% 26%)` — assert `toMatch(/--primary:\s*hsl\(185 100% 26%\)/)`. Also assert it does NOT contain `185 100% 30%`, `185 100% 38%`, or `185 100% 50%` inside the `:root` block (the AA-failing values). (Allow `185 100% 50%` ONLY inside `.dark`.)
    - globals.css declares `--background`, `--foreground`, `--card`, `--sidebar`, `--sidebar-primary` in both blocks; assert at least 16 `--sidebar` occurrences total (`(css.match(/--sidebar/g)||[]).length` >= 16).
    - globals.css `@theme inline` maps `--font-display: var(--font-space-grotesk)` and `--font-sans: var(--font-inter)`.
    - globals.css `@layer base` maps headings to the display font (`toMatch(/h1[\s\S]*--font-display/)` or a `.font-display` rule referencing `--font-display`).
    - layout.tsx imports `Space_Grotesk` and `Inter` from `next/font/google`; declares `variable: '--font-space-grotesk'` and `variable: '--font-inter'`.
    - layout.tsx `<html>` has `lang="pt-BR"` and `suppressHydrationWarning`.
    - layout.tsx references `ThemeProvider`.

    Create `contrast.test.ts` (PURE unit test — no file reads, no app imports):
    - Implement a local `contrastRatio(hexA, hexB)` using the WCAG relative-luminance formula (sRGB → linearize → L → (L1+0.05)/(L2+0.05)).
    - Assert `contrastRatio('#007a85', '#ffffff')` >= 4.5 (light --primary on white; expect ~5.12).
    - Assert `contrastRatio('#ffffff', '#007a85')` >= 4.5 (white text on primary fill — same ratio).
    - Assert the dark pair: cyan `#00e5ff` (hsl 185 100% 50%) on navy `#0d0d14` (hsl 240 20% 6%) >= 4.5 (expect ~13).
    - Add a regression assertion documenting the rejected value: `contrastRatio('#008c99','#ffffff')` < 4.5 (the 30% value that FAILS AA).
    These pure assertions PASS immediately (they validate the chosen token values, not the implementation).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/theme.test.ts src/__tests__/ui/contrast.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>theme.test.ts runs and currently FAILS (globals.css still oklch) — RED-by-design. contrast.test.ts PASSES (pure value validation). tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: shell.test.ts + page-pattern.test.ts</name>
  <files>src/__tests__/ui/shell.test.ts, src/__tests__/ui/page-pattern.test.ts</files>
  <read_first>
    - .planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-VALIDATION.md (shell + page-pattern bullets)
    - .planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md (App Shell Architecture lines 292-396; PageHeader lines 400-431; Loading/Error/Empty lines 434-507)
    - src/app/(dashboard)/clinica/layout.tsx (current passthrough — the file under test)
  </read_first>
  <action>
    Create `shell.test.ts` (source inspection):
    - `src/app/(dashboard)/clinica/layout.tsx` references `AppSidebar` and is NOT a bare passthrough (assert it does NOT match the exact fragment-only body — i.e. it imports AppSidebar).
    - `src/components/shell/AppSidebar.tsx` exists and contains the role-gate (`admin`), the nav module labels `Agenda`, `Pacientes`, `Financeiro`, `Equipe`, `IA`, and references `SidebarFooter`/footer + `ThemeToggle` + `Sair`.
    - `src/components/shell/SidebarNavClient.tsx` exists and references `usePathname`.
    - `src/components/shell/ThemeToggle.tsx` exists, has `'use client'`, references `useTheme` and `next-themes`.
    - `src/components/providers/ThemeProvider.tsx` exists and references `next-themes`.
    - `src/hooks/useSidebarStore.ts` exists and references `zustand` (`create`) + `isCollapsed`.
    Use `existsSync` guards so a missing file yields an empty string and a clean failed assertion (not a thrown error).

    Create `page-pattern.test.ts` (source inspection):
    - `src/components/shell/PageHeader.tsx` exists and declares props `title`, `breadcrumbs`, `actions` (assert `toMatch(/title/)`, `/breadcrumbs/`, `/actions/`).
    - A representative page from each module imports/uses `PageHeader`: assert each of these contains `PageHeader` —
      `src/app/(dashboard)/clinica/pacientes/page.tsx`,
      `src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx`,
      `src/app/(dashboard)/clinica/equipe/page.tsx`,
      `src/app/(dashboard)/clinica/ia/agentes/page.tsx`.
    - `loading.tsx` exists for the key routes: `pacientes`, `agenda`, `financeiro/fluxo-de-caixa`, `financeiro/contas-a-receber`, `equipe`, `ia/agentes` (existsSync each).
    - `error.tsx` exists for the same key routes.
    - A shared `src/components/shell/EmptyState.tsx` exists and references a lucide icon prop + `title` + `description`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/shell.test.ts src/__tests__/ui/page-pattern.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>Both files run and FAIL (shell/PageHeader/loading/error not yet built) — RED-by-design. tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: typography.test.ts</name>
  <files>src/__tests__/ui/typography.test.ts</files>
  <read_first>
    - .planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-VALIDATION.md (typography bullet)
    - .planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md (Typography lines 263-289 — 2-weight rule + brand wordmark exception)
  </read_first>
  <action>
    Create `typography.test.ts` (source inspection across audited app screens — NOT `src/components/ui/**`).
    Define a list of audited files (the per-module screens from 06-UI-SPEC Per-Module Design Direction):
      hub `src/app/(dashboard)/clinica/page.tsx`, auth forms (`src/components/auth/LoginForm.tsx`, `SignupForm.tsx`, `ForgotPasswordForm.tsx`), `src/app/(auth)/reset-password/page.tsx`,
      `src/app/(dashboard)/clinica/equipe/page.tsx`, `src/app/invite/[token]/page.tsx`,
      `src/app/agendar/[clinic-slug]/page.tsx`, `src/app/anamnese/[patient-id]/[token]/page.tsx`,
      `src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx`, `src/app/(dashboard)/clinica/financeiro/contas-a-receber/page.tsx`.
    For each file (existsSync-guarded):
    - Assert NO `font-medium` (`expect(src).not.toMatch(/font-medium/)`).
    - Assert NO `text-3xl` and NO `text-lg` (`/text-3xl/`, `/text-lg/`).
    - `font-bold` is allowed ONLY on the brand wordmark line: assert that every `font-bold` occurrence co-occurs with `font-display` on the same logical usage. Pragmatic encoding: count `font-bold` not paired with `font-display` must be 0 — implement by removing `font-bold font-display`/`font-display font-bold` combos from the source string, then asserting the residue has no remaining `font-bold`.
    Document in a comment: this enforces the 2-weight system (400/600) with the single brand-wordmark exception.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/typography.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>typography.test.ts runs and FAILS against current sources (font-bold/medium/text-3xl present) — RED-by-design. tsc exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None crossed. Test files run only in the local/CI test runner; they read source files and compute pure functions. No data paths, no secrets, no network.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | test scaffolds | accept | Tests read only source files already in the repo; no PII, no secrets referenced. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/ui/` runs all 5 files (contrast PASS; theme/shell/page-pattern/typography RED-by-design).
- `npx tsc --noEmit` exits 0 (source-inspection style, no app imports).
</verification>

<success_criteria>
5 test files exist under `src/__tests__/ui/`; contrast.test.ts passes; the other four fail against the current (unimplemented) sources without throwing; tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-01-SUMMARY.md`
</output>
