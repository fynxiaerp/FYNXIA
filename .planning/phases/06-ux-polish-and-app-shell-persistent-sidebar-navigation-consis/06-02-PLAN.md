---
phase: 06-ux-polish-and-app-shell
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/providers/ThemeProvider.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "App renders in a light clinical theme by default with FYNXIA brand tokens"
    - "A dark brand theme is available via the .dark class with cyan/magenta/purple tokens"
    - "Headings render in Space Grotesk; body/UI renders in Inter"
    - "Theme preference persists with no FOUC on production build"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Dual-theme HSL token tables (:root light + .dark brand) + font mapping"
      contains: "--primary: hsl(185 100% 26%)"
    - path: "src/app/layout.tsx"
      provides: "next/font Space Grotesk + Inter, ThemeProvider, lang=pt-BR, suppressHydrationWarning"
    - path: "src/components/providers/ThemeProvider.tsx"
      provides: "next-themes wrapper (attribute=class, defaultTheme=light, storageKey=fynxia-theme)"
  key_links:
    - from: "src/app/layout.tsx"
      to: "next-themes ThemeProvider"
      via: "<ThemeProvider> wraps <body> children"
      pattern: "ThemeProvider"
    - from: "src/app/globals.css @theme inline"
      to: "--font-space-grotesk / --font-inter from layout.tsx"
      via: "--font-display / --font-sans mapping"
      pattern: "--font-display:\\s*var\\(--font-space-grotesk\\)"
---

<objective>
Lay the visual foundation: install `next-themes`, migrate `globals.css` from the current oklch
grayscale tokens to the dual-theme FYNXIA HSL token tables (light clinical `:root` + dark brand
`.dark`), replace the Geist fonts with Space Grotesk (display) + Inter (body) via `next/font`, and
mount a `ThemeProvider`. This implements D-01.

Purpose: every downstream wave (shell, page pattern, per-module sweep) depends on these tokens,
fonts, and theme classes existing first.
Output: brand-aware dual theme, correct fonts, persistent theme toggle infrastructure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-RESEARCH.md
@src/app/globals.css
@src/app/layout.tsx
@CLAUDE.md

<interfaces>
<!-- CRITICAL contrast correction (06-RESEARCH §Critical Token Issue): light --primary MUST be -->
<!-- hsl(185 100% 26%) (#007a85, 5.12:1 AA). NOT 30% (#008c99, 4.02:1, FAILS). Re-verify before commit. -->
<!-- next-themes 0.4.6 supports React 19 — no --legacy-peer-deps needed. -->
<!-- The existing @custom-variant dark (&:is(.dark *)) line is correct — keep it unchanged. -->
<!-- Keep Geist_Mono for --font-geist-mono (low risk; @theme inline references it). -->

Exact globals.css token block to apply (from 06-UI-SPEC lines 865-956) — light :root + dark .dark:
- light --primary/--ring/--sidebar-primary/--sidebar-ring: hsl(185 100% 26%)
- light --accent: hsl(185 60% 92%); --accent-foreground: hsl(185 100% 28%)
- dark --primary: hsl(185 100% 50%); --secondary: hsl(300 100% 60%); --accent: hsl(250 100% 65%)
- dark named brand tokens + gradients + glows (lines 944-954)

layout.tsx font wiring (06-UI-SPEC lines 246-261, 06-RESEARCH Pattern 5):
  Inter   -> variable: '--font-inter'        weights ['400','600']
  Space_Grotesk -> variable: '--font-space-grotesk' weights ['400','600','700']

ThemeProvider (06-RESEARCH Pattern 1):
  attribute="class" defaultTheme="light" enableSystem={false} storageKey="fynxia-theme"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-themes + ThemeProvider</name>
  <files>package.json, package-lock.json, src/components/providers/ThemeProvider.tsx</files>
  <read_first>
    - 06-RESEARCH.md (Pattern 1 — ThemeProvider wrapper code; Standard Stack — install command)
    - CLAUDE.md (state management — theme via next-themes, not Zustand)
  </read_first>
  <action>
    Run `npm install next-themes` (0.4.6 supports React 19; do NOT pass --legacy-peer-deps).
    Create `src/components/providers/ThemeProvider.tsx` exactly per 06-RESEARCH Pattern 1:
    `'use client'`; import `ThemeProvider as NextThemesProvider` from `next-themes`; export a
    `ThemeProvider` wrapper passing `attribute="class"`, `defaultTheme="light"`,
    `enableSystem={false}`, `storageKey="fynxia-theme"` and rendering `{children}`.
  </action>
  <verify>
    <automated>npm ls next-themes; npx tsc --noEmit</automated>
  </verify>
  <done>next-themes present in package.json deps; ThemeProvider.tsx exports the wrapper; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate globals.css to dual-theme HSL tokens + font mapping</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current oklch token blocks — being replaced)
    - 06-UI-SPEC.md (globals.css Token Block lines 865-956; Color Token Tables lines 74-157)
    - 06-RESEARCH.md (Critical Token Issue — 26% not 30%; Pitfall 3 — --font-display required)
  </read_first>
  <action>
    In `src/app/globals.css`:
    1. Keep the top imports and the `@custom-variant dark (&:is(.dark *));` line unchanged.
    2. In the `@theme inline` block: change `--font-sans: var(--font-sans)` to `--font-sans: var(--font-inter)`; replace `--font-heading: var(--font-sans)` with `--font-display: var(--font-space-grotesk)`; keep `--font-mono: var(--font-geist-mono)`; keep ALL existing `--color-*` and `--radius-*` mappings (they map tokens to utilities — sidebar color mappings already present, do not remove).
    3. Replace the entire `:root {…}` block with the light clinical HSL tokens from 06-UI-SPEC lines 882-912 (every value wrapped in `hsl(...)`). `--primary: hsl(185 100% 26%)`. Include all `--sidebar*` tokens and `--radius: 0.75rem`.
    4. Replace the entire `.dark {…}` block with the dark brand HSL tokens from 06-UI-SPEC lines 914-955, including the named brand tokens (`--cyan`, `--magenta`, `--purple`, `--dark-navy`, `--navy`, `--navy-light`), `--gradient-primary`, `--gradient-secondary`, `--glow-cyan`, `--glow-magenta`.
    5. In `@layer base`: keep the existing `*`, `body`, `html` rules; ADD a heading rule:
       `h1, h2, h3, h4, h5, h6, .font-display { font-family: var(--font-display), sans-serif; }`
       and add a skip-to-content utility rule is optional (handled in layout). The `html { @apply font-sans }` stays (maps to Inter).
    6. The current file has `--chart-*` tokens — preserve them (light + dark) so existing `@theme inline` chart mappings don't break the build.
    Re-verify `#007a85` on white >= 4.5:1 with a contrast checker before saving (06-RESEARCH confirms 5.12:1).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/theme.test.ts src/__tests__/ui/contrast.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>theme.test.ts token assertions PASS (both blocks present, --primary 26%, >=16 --sidebar, --font-display mapped); contrast.test.ts PASS; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Replace fonts + mount ThemeProvider in root layout</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current Geist setup — being replaced)
    - 06-RESEARCH.md (Pattern 1 root layout; Pattern 5 font migration; Pitfall 2 dev-mode FOUC)
    - 06-UI-SPEC.md (Accessibility Contract lines 843 — skip-to-content)
  </read_first>
  <action>
    Rewrite `src/app/layout.tsx`:
    - Import `Inter, Space_Grotesk` from `next/font/google` (and keep `Geist_Mono` for `--font-geist-mono`).
    - `const inter = Inter({ variable: '--font-inter', subsets: ['latin'], weight: ['400','600'] })`.
    - `const spaceGrotesk = Space_Grotesk({ variable: '--font-space-grotesk', subsets: ['latin'], weight: ['400','600','700'] })`.
    - Set `metadata` title to "FYNXIA" / description in pt-BR ("ERP odontológico FYNXIA").
    - `<html lang="pt-BR" suppressHydrationWarning className={`${inter.variable} ${spaceGrotesk.variable} ${geistMono.variable} h-full antialiased`}>`.
    - `<body className="min-h-full flex flex-col">` wraps children in `<ThemeProvider>` (import from `@/components/providers/ThemeProvider`).
    - Add a skip-to-content link as the first child inside `<body>`: `<a href="#main-content" className="sr-only focus:not-sr-only focus:absolute focus:z-50 focus:p-2 focus:bg-background">Ir para conteúdo</a>` (the main content `id="main-content"` is added in the shell wave — fine to land the anchor now).
    Note for executor: dev-mode theme flash is expected (06-RESEARCH Pitfall 2); FOUC correctness is only validated via `npm run build && npm run start`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/theme.test.ts; npx tsc --noEmit; npx next build</automated>
  </verify>
  <done>theme.test.ts layout assertions PASS (Space_Grotesk + Inter + ThemeProvider + lang pt-BR + suppressHydrationWarning); tsc exits 0; next build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Client ↔ browser storage (next-themes localStorage). No server/data boundary touched in this plan.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | next-themes localStorage (`fynxia-theme`) | accept | Stores only `'light'`/`'dark'` string; no innerHTML injection; no security decision derives from it. |
| T-06-03 | Information disclosure | globals.css / layout.tsx | accept | Presentation-only; no secrets, no PII. Logo not referenced here. |
</threat_model>

<verification>
- theme.test.ts + contrast.test.ts GREEN.
- `npx tsc --noEmit` exits 0.
- `npx next build` green.
- Manual (UAT, deferred): toggle behavior + FOUC verified in a later wave once ThemeToggle exists.
</verification>

<success_criteria>
globals.css carries both light + dark FYNXIA token tables (light --primary = hsl(185 100% 26%)); layout.tsx loads Space Grotesk + Inter and mounts ThemeProvider with pt-BR + suppressHydrationWarning; build is green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-02-SUMMARY.md`
</output>
