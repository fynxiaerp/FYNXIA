---
phase: 06-ux-polish-and-app-shell
plan: 06
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - src/app/(dashboard)/clinica/agenda/page.tsx
  - src/app/(dashboard)/clinica/agenda/loading.tsx
  - src/app/(dashboard)/clinica/agenda/error.tsx
  - src/components/agenda/AgendaCalendar.tsx
  - src/app/(dashboard)/clinica/pacientes/page.tsx
  - src/app/(dashboard)/clinica/pacientes/loading.tsx
  - src/app/(dashboard)/clinica/pacientes/error.tsx
  - src/components/patients/PatientTable.tsx
  - src/app/(dashboard)/clinica/pacientes/[id]/page.tsx
  - src/app/(dashboard)/clinica/pacientes/[id]/loading.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Agenda and Pacientes pages use PageHeader with title/breadcrumb/actions"
    - "The agenda empty state fires on appointments.length === 0 (not dentists.length === 0)"
    - "The dentist filter offers a 'Todos os dentistas' option"
    - "The patient table has clickable sort headers with aria-sort and a single Eye action"
    - "Patient detail Prontuário/Odontograma tabs render content inline (no redirect cards)"
  artifacts:
    - path: "src/components/patients/PatientTable.tsx"
      provides: "Sortable headers (aria-sort, sort icons, active text-primary); Eye-only action"
    - path: "src/app/(dashboard)/clinica/agenda/page.tsx"
      provides: "PageHeader + full-viewport calendar + fixed empty-state trigger"
    - path: "src/app/(dashboard)/clinica/pacientes/[id]/page.tsx"
      provides: "Inline tab content (no redirect stub cards)"
  key_links:
    - from: "src/components/patients/PatientTable.tsx"
      to: "TanStack getSortedRowModel"
      via: "onClick TableHead toggles sort"
      pattern: "aria-sort"
    - from: "src/app/(dashboard)/clinica/agenda/page.tsx"
      to: "EmptyState"
      via: "appointments.length === 0 trigger"
      pattern: "appointments\\.length"
---

<objective>
Sweep the clinical module — Agenda and Pacientes (D-04). Add `PageHeader` + `loading.tsx`/`error.tsx`
to each route, fix the agenda empty-state trigger and dentist filter "all" option, make the patient
table sortable (clickable headers + `aria-sort` + sort icons) with a single Eye action (remove the
redundant Pencil), and render the patient-detail Prontuário/Odontograma tabs inline instead of as
redirect stub cards. Replace the `Salvando...` ASCII dots with `…`.

Purpose: closes UI-REVIEW Experience findings (no shell context, agenda empty-state, dentist filter,
patient sort, redundant actions, tab stubs).
Output: a coherent, navigable clinical module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md
@.planning/reports/UI-REVIEW-v1.0.md
@src/app/(dashboard)/clinica/pacientes/[id]/page.tsx
@src/components/patients/PatientTable.tsx
@CLAUDE.md

<interfaces>
<!-- Reuse Wave 2 primitives: PageHeader, EmptyState, ErrorState, skeletons. -->
<!-- error.tsx pattern: 'use client'; import { ErrorState } from '@/components/shell/ErrorState'; -->
<!--   export default function Error({ reset }: { error: Error; reset: () => void }) { return <ErrorState reset={reset}/> } -->
<!-- Agenda main uses p-0 + h-[calc(100vh-64px)] (full-viewport calendar, 64px PageHeader). -->
<!-- Pacientes main uses p-6 max-w-5xl mx-auto. Patient detail fills content area (no max-width). -->
<!-- @base-ui Button render-prop (no asChild). PDF button: shadcn Button + Loader2 spinner. -->

Agenda (06-UI-SPEC lines 632-639):
  PageHeader title "Agenda", actions [+ Nova Consulta]; empty state CalendarX "Nenhuma consulta esta semana"
  fire on appointments.length===0; add "Todos os dentistas" as first Select item.
Pacientes list (06-UI-SPEC lines 640-645):
  PageHeader title "Pacientes", breadcrumb "Clínica > Pacientes", actions [+ Novo Paciente];
  sort indicators ↑/↓ + aria-sort; active sort header text-primary; remove Pencil, keep Eye.
Patient detail (06-UI-SPEC lines 647-652, 607-608):
  PageHeader title=patient name, breadcrumb "Clínica > Pacientes > [Nome]", actions [Baixar PDF][...];
  Prontuário + Odontograma tabs render content inline (eliminate redirect cards); tab content p-4.
AgendaCalendar (06-UI-SPEC line 76 audit): 'Salvando...' -> 'Salvando…'.
Empty-state icons: Pacientes UserX "Nenhum paciente cadastrado" CTA "Novo Paciente".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agenda — PageHeader, empty-state fix, dentist filter, loading/error</name>
  <files>src/app/(dashboard)/clinica/agenda/page.tsx, src/app/(dashboard)/clinica/agenda/loading.tsx, src/app/(dashboard)/clinica/agenda/error.tsx, src/components/agenda/AgendaCalendar.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/agenda/page.tsx + src/components/agenda/AgendaCalendar.tsx (current)
    - 06-UI-SPEC.md (Agenda lines 632-639; Empty State agenda row line 499; skeleton line 443)
    - UI-REVIEW-v1.0.md (Clinical module — empty-state trigger; dentist filter)
  </read_first>
  <action>
    1. `agenda/page.tsx`: add `<PageHeader title="Agenda" actions={<Button>Nova Consulta</Button>} mobileMenuTrigger={...} />`. Wrap the calendar in `<div className="h-[calc(100vh-64px)] p-0">` (full-viewport, 64px PageHeader). Fix the empty-state condition: render the `CalendarX` `EmptyState` ("Nenhuma consulta esta semana", body per 06-UI-SPEC line 499, no CTA) when `appointments.length === 0` — NOT when `dentists.length === 0`. If there are dentists but no appointments, still render the calendar with the empty-state messaging as appropriate (do not hide the calendar when dentists exist).
    2. In the dentist filter (Select): add "Todos os dentistas" as the FIRST `SelectItem` (value maps to null/all) so the user can return to the all-dentists view (06-UI-SPEC line 636).
    3. `AgendaCalendar.tsx`: change the button label `'Salvando...'` → `'Salvando…'` (ellipsis char). No other functional change.
    4. Create `loading.tsx` (PageHeaderSkeleton + calendar-grid skeleton rows from skeletons.tsx) and `error.tsx` (3-line ErrorState wrapper).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/page-pattern.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>Agenda uses PageHeader; empty state keys on appointments.length; "Todos os dentistas" present; 'Salvando…'; loading/error exist; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Pacientes list — PageHeader, sortable table, Eye-only, loading/error</name>
  <files>src/app/(dashboard)/clinica/pacientes/page.tsx, src/app/(dashboard)/clinica/pacientes/loading.tsx, src/app/(dashboard)/clinica/pacientes/error.tsx, src/components/patients/PatientTable.tsx</files>
  <read_first>
    - src/components/patients/PatientTable.tsx (current — Pencil+Eye; no sort onClick)
    - 06-UI-SPEC.md (Pacientes lines 640-645; skeleton line 444; Empty State line 500)
    - UI-REVIEW-v1.0.md (patient table redundant actions + sort)
  </read_first>
  <action>
    1. `pacientes/page.tsx`: add `<PageHeader title="Pacientes" breadcrumbs={[{label:'Clínica',href:'/clinica'},{label:'Pacientes'}]} actions={<Button>Novo Paciente</Button>} mobileMenuTrigger={...} />`. Content wrapper `p-6 max-w-5xl mx-auto w-full`. Empty state (when zero patients): `UserX` EmptyState "Nenhum paciente cadastrado" + CTA "Novo Paciente" (06-UI-SPEC line 500).
    2. `PatientTable.tsx`: wire sorting — add `onClick` to sortable `<TableHead>` calling the TanStack column `toggleSorting` (the table already has `getSortedRowModel()`); render `↑`/`↓` (`ArrowUp`/`ArrowDown` lucide, `size-3.5`) based on sort direction; set `aria-sort="ascending|descending|none"` on the header; active sort header gets `text-primary` (accent reserved list item 6). Remove the redundant `Pencil` action button; keep only the `Eye` (View) button → `/clinica/pacientes/[id]`.
    3. Create `loading.tsx` (PageHeaderSkeleton + 5 TableRowsSkeleton) and `error.tsx` (ErrorState wrapper).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/page-pattern.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>Pacientes list uses PageHeader; sortable headers with aria-sort + icons + active text-primary; Pencil removed (Eye only); loading/error exist; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Patient detail — PageHeader, inline tabs, PDF button, loading</name>
  <files>src/app/(dashboard)/clinica/pacientes/[id]/page.tsx, src/app/(dashboard)/clinica/pacientes/[id]/loading.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/pacientes/[id]/page.tsx (current — Prontuário/Odontograma stub redirect cards; PDF plain <a>)
    - 06-UI-SPEC.md (Patient Detail lines 647-652; Patient Detail Tabs Fix lines 607-608; skeleton line 445)
    - UI-REVIEW-v1.0.md (tab stubs; PDF download anchor)
  </read_first>
  <action>
    1. `[id]/page.tsx`: add `<PageHeader title={patient.name} breadcrumbs={[{label:'Clínica',href:'/clinica'},{label:'Pacientes',href:'/clinica/pacientes'},{label:patientName}]} actions={<PdfButton/>} />`. Tab content area `p-4`.
    2. Eliminate the Prontuário and Odontograma redirect stub cards. Render the tab content INLINE: import and render the existing prontuário and odontograma content components directly within the tab panels (the content already exists at the sub-routes `/prontuario` and `/odontograma` — lift the rendering component into the tab, OR render the sub-route's content component inline). Decision per 06-UI-SPEC line 608: inline content, no redirect card. If the sub-route pages contain inline-able server components, render them in the tab panels; otherwise keep the sub-routes but make the tabs `router.push` to the sub-route while keeping tab visual context. Prefer true inline rendering of the content component.
    3. PDF download: replace the plain `<a>` anchor with a shadcn `Button` (label "Baixar Prontuário PDF") that shows a `Loader2` spinner during generation — on click set loading → fetch the prontuário PDF endpoint → trigger download → clear loading. This is a `'use client'` sub-component.
    4. Create `loading.tsx` (patient header skeleton + 4 tab skeletons + content skeleton per 06-UI-SPEC line 445).
    Presentation-only — do not change PDF generation server logic, only the trigger UX.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/page-pattern.test.ts src/__tests__/ui/typography.test.ts; npx tsc --noEmit; npx next build</automated>
  </verify>
  <done>Patient detail uses PageHeader; Prontuário/Odontograma render inline (no stub cards); PDF button shows spinner; loading.tsx exists; tsc exits 0; next build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
All reads are RLS-scoped server reads (patients, appointments). PDF endpoint auth unchanged.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Information disclosure | inline prontuário/odontograma tab content | accept | Renders the same RLS-scoped clinical data already served at the sub-routes; no new data path; CPF/health masking unchanged. |
| T-06-12 | Information disclosure | PatientTable masked columns | accept | Existing CPF/contact masking logic is left untouched (presentation sweep only). |
</threat_model>

<verification>
- page-pattern.test.ts + typography.test.ts GREEN for clinical files.
- `npx tsc --noEmit` exits 0; `npx next build` green.
- Manual (UAT, deferred): sort interaction, empty-state copy, inline tabs, PDF spinner, both themes.
</verification>

<success_criteria>
Agenda + Pacientes adopt PageHeader + loading/error; agenda empty-state and dentist filter fixed; patient table sortable with Eye-only action; patient-detail tabs render inline; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-06-SUMMARY.md`
</output>
