---
phase: 06-ux-polish-and-app-shell
plan: 05
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - src/app/(dashboard)/clinica/page.tsx
  - src/app/(dashboard)/clinica/loading.tsx
  - src/app/(auth)/layout.tsx
  - src/components/auth/LoginForm.tsx
  - src/components/auth/SignupForm.tsx
  - src/components/auth/ForgotPasswordForm.tsx
  - src/app/(auth)/reset-password/page.tsx
  - src/app/invite/[token]/page.tsx
  - src/app/agendar/[clinic-slug]/page.tsx
  - src/app/anamnese/[patient-id]/[token]/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The hub is a dashboard overview (greeting + quick stats + shortcuts), not a nav menu"
    - "Auth pages show the FYNXIA brand chip and work in both themes with the 2-weight system"
    - "Invite page uses design tokens (no slate/gray), matching the rest of the app"
    - "Public pages use tokens, correct title copy, and 44px touch targets"
  artifacts:
    - path: "src/app/(dashboard)/clinica/page.tsx"
      provides: "Hub redesign: greeting + 3 stat cards + shortcut grid; no role/plan slug line"
    - path: "src/app/invite/[token]/page.tsx"
      provides: "Token-based invite page rewritten on design tokens"
    - path: "src/app/(dashboard)/clinica/loading.tsx"
      provides: "Hub skeleton (3-col card grid)"
  key_links:
    - from: "src/app/invite/[token]/page.tsx"
      to: "design tokens"
      via: "bg-background/text-foreground/border-border replace slate/gray"
      pattern: "bg-background"
    - from: "src/app/anamnese/[patient-id]/[token]/page.tsx"
      to: "copy contract"
      via: "title 'Anamnese Odontológica'"
      pattern: "Anamnese Odontológica"
---

<objective>
Sweep the hub, auth, and public pages (D-04). Redesign the `/clinica` hub into a dashboard overview
(time-based greeting + 3 quick-stat cards + condensed action shortcuts, removing the raw role/plan
slug line). Apply brand tokens + the 2-weight typography system to the auth forms and `(auth)/layout`.
Fully rewrite `/invite/[token]` on design tokens. Token-correct the public `/agendar` and `/anamnese`
pages, fix the anamnesis title copy, and confirm 44px touch targets.

Purpose: closes UI-REVIEW findings — hub role/plan slug, auth `font-bold`/`text-3xl` wordmark, invite
design-island, anamnese title mismatch, public token usage.
Output: branded, consistent hub/auth/public surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md
@.planning/reports/UI-REVIEW-v1.0.md
@src/app/(dashboard)/clinica/page.tsx
@src/app/(auth)/layout.tsx
@CLAUDE.md

<interfaces>
<!-- Reuse from Wave 2: EmptyState, skeletons (CardGridSkeleton). The hub is INSIDE the shell now -->
<!-- (06-03), so it must NOT render its own Sair button or "FYNXIA"/role/plan header — the sidebar -->
<!-- footer owns user identity. Hub uses PageHeader title "Início", no breadcrumb (top-level). -->
<!-- Auth + public pages are OUTSIDE the shell — they keep their own layouts, no PageHeader. -->
<!-- 2-weight rule: headings/labels font-semibold; body font-normal. Brand wordmark exception: -->
<!--   "Fynxia" uses text-2xl font-bold font-display (NOT text-3xl). -->
<!-- Logo chip (both themes): rounded-xl bg-[hsl(240_20%_8%)] with /fynxia-logo.png. -->

Hub content (06-UI-SPEC lines 594-604, 625-631, Copywriting lines 766-777):
  greeting "Bom dia/Boa tarde/Boa noite, [firstName]" (time-based) text-2xl font-semibold font-display;
  3 stat cards: "Consultas hoje", "Pacientes ativos", "Recebíveis em aberto" (server-fetched);
  shortcut grid (2 cols): "Nova Consulta", "Novo Paciente", "Emitir Cobrança", "Abrir Copiloto";
  preserve public booking link section ("Link de agendamento público").
  Nav/shortcut card icons: text-muted-foreground default, group-hover:text-primary.

Invite rewrite (06-UI-SPEC lines 711): bg-background/text-foreground/border-border;
  wordmark text-2xl font-bold font-display text-primary.
Public (06-UI-SPEC lines 708-713): tokens; "Anamnese Digital" -> "Anamnese Odontológica";
  FYNXIA chip text-primary; min-h-[44px] min-w-[44px] touch targets.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hub redesign + loading.tsx</name>
  <files>src/app/(dashboard)/clinica/page.tsx, src/app/(dashboard)/clinica/loading.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/page.tsx (current hub — role/plan slug + Sair to remove)
    - 06-UI-SPEC.md (Hub redesign lines 594-604; Dashboard Hub lines 625-631; Copywriting lines 766-777)
    - 06-RESEARCH.md (no new data — reuse existing reads; keep server-side fetches RLS-scoped)
    - src/components/shell/PageHeader.tsx, EmptyState.tsx, skeletons.tsx (Wave 2)
  </read_first>
  <action>
    Rewrite `src/app/(dashboard)/clinica/page.tsx` (stays a Server Component):
    - Remove the `<form action={signOut}>` Sair button and the "FYNXIA"/`Perfil: {role} · Plano: {plan}` header block (the sidebar footer owns identity — audit fix).
    - Render `<PageHeader title="Início" />` (no breadcrumb — top-level), with a `mobileMenuTrigger` slot passing the role-gated nav items (so the hamburger works on `<md`).
    - Greeting line: derive first name from the user; time-based "Bom dia/Boa tarde/Boa noite, [firstName]" — `text-2xl font-semibold font-display`.
    - 3 quick-stat cards (server-fetched, RLS-scoped — reuse existing query patterns; if a count query is trivial use `supabase.from(...).select('*', { count: 'exact', head: true })`): "Consultas hoje", "Pacientes ativos", "Recebíveis em aberto". Card amounts `text-2xl font-semibold tabular-nums`. Do NOT add new tables/migrations — counts only.
    - Shortcut grid (2 cols): "Nova Consulta" (→ /clinica/agenda), "Novo Paciente" (→ /clinica/pacientes/novo), "Emitir Cobrança" (→ /clinica/financeiro/nova-cobranca), "Abrir Copiloto" (→ /clinica, opens copilot — link to /clinica is fine if no deep link). Card icons `text-muted-foreground group-hover:text-primary`.
    - Preserve the public booking link card; replace its inner `text-primary font-medium` link with `font-semibold` (no font-medium).
    - Page wrapper: `<main>` already provided by shell; render content in `p-6 max-w-5xl mx-auto w-full space-y-8` INSIDE the page (the shell's main is the scroll container).
    Create `src/app/(dashboard)/clinica/loading.tsx` using `CardGridSkeleton` (3 cards) from skeletons.tsx (06-UI-SPEC line 442).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/typography.test.ts src/__tests__/ui/page-pattern.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>Hub shows greeting + stats + shortcuts via PageHeader; no role/plan slug; loading.tsx exists; typography.test.ts hub assertion PASS (no font-medium/text-3xl/text-lg); tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Auth pages — brand chip + 2-weight system</name>
  <files>src/app/(auth)/layout.tsx, src/components/auth/LoginForm.tsx, src/components/auth/SignupForm.tsx, src/components/auth/ForgotPasswordForm.tsx, src/app/(auth)/reset-password/page.tsx</files>
  <read_first>
    - 06-UI-SPEC.md (Auth Module lines 614-624; Auth Layout lines 574-592)
    - src/components/auth/LoginForm.tsx (current — wordmark text-3xl font-bold to fix)
    - src/app/(auth)/reset-password/page.tsx (raw <label> font-medium to fix)
    - UI-REVIEW-v1.0.md (Auth Module section)
  </read_first>
  <action>
    1. `(auth)/layout.tsx`: ensure `bg-background` (light) with a dark-theme brand radial blob per 06-UI-SPEC line 592 (`before:` radial-gradient at low opacity — only manifests in `.dark`). Center content; render the FYNXIA logo chip (`rounded-xl bg-[hsl(240_20%_8%)]` + `/fynxia-logo.png`) above the card slot if the layout owns the brand header; otherwise leave per-form.
    2. In each auth form (Login/Signup/ForgotPassword): change the FYNXIA wordmark from `text-3xl font-bold` to `text-2xl font-bold font-display tracking-tight` (brand exception — documented, paired with font-display). Keep `text-primary`. Replace any `font-medium` with `font-semibold` (labels) or `font-normal` (body). Form card: `bg-card rounded-xl border border-border p-8 shadow-md`. Loading button labels use `…`: "Entrando…", "Enviando…".
    3. `reset-password/page.tsx`: replace raw `<label className="...font-medium">` with the shadcn `Form` + `FormField` + `Label` pattern matching `LoginForm.tsx`; loading label "Salvando…". Wordmark same `text-2xl font-bold font-display`.
    Verify no `text-3xl`, no `font-medium`, and every `font-bold` co-occurs with `font-display`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/typography.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>Auth forms use text-2xl font-bold font-display wordmark + token card; reset-password uses Form pattern; typography.test.ts auth assertions PASS; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Invite rewrite + public pages token sweep</name>
  <files>src/app/invite/[token]/page.tsx, src/app/agendar/[clinic-slug]/page.tsx, src/app/anamnese/[patient-id]/[token]/page.tsx</files>
  <read_first>
    - src/app/invite/[token]/page.tsx (current — slate/gray/bg-white to replace)
    - 06-UI-SPEC.md (Public Pages lines 708-713; Responsive public note line 855)
    - UI-REVIEW-v1.0.md (Public Pages + invite findings)
  </read_first>
  <action>
    1. `invite/[token]/page.tsx`: full token rewrite — replace `bg-gray-50`→`bg-background`, `bg-white`→`bg-card`, `text-slate-900`→`text-foreground`, `text-slate-500/600`→`text-muted-foreground`, `border-slate-200`→`border-border`. FYNXIA wordmark: `text-2xl font-bold font-display text-primary` (not `text-3xl font-bold text-slate-900`). Remove any `font-medium`. Keep the existing token-acceptance logic untouched (presentation-only change).
    2. `anamnese/[patient-id]/[token]/page.tsx`: change the page title copy "Anamnese Digital" → "Anamnese Odontológica" (06-UI-SPEC line 713). Ensure tokens (no slate/gray), the FYNXIA chip uses `text-primary`, and interactive controls have `min-h-[44px] min-w-[44px]`. Progress indicator fill may use `text-primary`/`bg-primary` (accent reserved list item 7).
    3. `agendar/[clinic-slug]/page.tsx`: confirm tokens (no raw slate/gray), FYNXIA chip `text-primary`, all buttons/slots `min-h-[44px]`. Replace any `font-bold` heading not paired with `font-display`, and any `font-medium`.
    Do not change data fetching / server actions — presentation only.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/typography.test.ts; npx tsc --noEmit; npx next build</automated>
  </verify>
  <done>invite uses tokens (no slate/gray); anamnese title is "Anamnese Odontológica"; public pages token-correct with 44px targets; typography.test.ts invite/public/anamnese assertions PASS; tsc exits 0; next build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Public pages (`/invite`, `/agendar`, `/anamnese`) handle untrusted tokens at the server-action layer (unchanged). Hub reads are RLS-scoped server reads.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information disclosure | hub quick-stat counts | accept | Counts are RLS-scoped to the tenant (same as existing hub queries); no new data path, no PII added. |
| T-06-10 | Tampering | invite/anamnese token flows | mitigate | Presentation-only change; token validation/single-use logic in the existing server actions is left untouched — do not alter the auth/token boundary. |
</threat_model>

<verification>
- typography.test.ts GREEN for hub/auth/invite/public files.
- `npx tsc --noEmit` exits 0; `npx next build` green.
- Manual (UAT, deferred): hub greeting/stats in both themes; auth brand moment; invite/public legibility.
</verification>

<success_criteria>
Hub is an overview (greeting + stats + shortcuts) with no role/plan slug; auth + invite + public pages use brand tokens + the 2-weight system; anamnese title corrected; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-05-SUMMARY.md`
</output>
