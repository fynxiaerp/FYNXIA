---
phase: 06-ux-polish-and-app-shell
plan: 04
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/components/shell/PageHeader.tsx
  - src/components/shell/MobileMenuTrigger.tsx
  - src/components/shell/EmptyState.tsx
  - src/components/shell/ErrorState.tsx
  - src/components/shell/skeletons.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "A shared PageHeader renders a Space Grotesk title + optional breadcrumb + actions slot"
    - "PageHeader accepts a client mobile-menu slot for the hamburger on <md"
    - "A shared EmptyState renders a lucide icon + heading + body + optional CTA"
    - "Reusable skeleton + error primitives exist for loading.tsx/error.tsx per route"
  artifacts:
    - path: "src/components/shell/PageHeader.tsx"
      provides: "title + breadcrumbs[] + actions + mobileMenuTrigger props"
      exports: ["PageHeader"]
    - path: "src/components/shell/EmptyState.tsx"
      provides: "icon + title + description + optional cta"
      exports: ["EmptyState"]
    - path: "src/components/shell/skeletons.tsx"
      provides: "Reusable skeleton building blocks (rows, cards) for loading.tsx"
    - path: "src/components/shell/ErrorState.tsx"
      provides: "Reusable error body for error.tsx (icon + heading + retry)"
      exports: ["ErrorState"]
  key_links:
    - from: "src/components/shell/PageHeader.tsx"
      to: "src/components/ui/breadcrumb"
      via: "Breadcrumb composition"
      pattern: "Breadcrumb"
    - from: "src/components/shell/MobileMenuTrigger.tsx"
      to: "src/components/ui/sheet"
      via: "Sheet drawer opened by hamburger"
      pattern: "Sheet"
---

<objective>
Build the shared page-pattern primitives (D-03): `PageHeader` (Server Component — title +
breadcrumb + actions + a client mobile-menu slot), `MobileMenuTrigger` (client hamburger → Sheet
drawer for `<md`), `EmptyState` (lucide icon + heading + body + optional CTA), `ErrorState`
(reusable body for `error.tsx`), and a `skeletons.tsx` toolkit of layout-mimicking building blocks
for `loading.tsx` files.

Purpose: these primitives are imported by every per-module sweep plan (Wave 3). Building them once
here keeps the sweep plans consistent and conflict-free.
Output: the reusable page-pattern component set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-RESEARCH.md
@src/components/ui/breadcrumb.tsx
@src/components/ui/sheet.tsx
@src/components/ui/skeleton.tsx
@CLAUDE.md

<interfaces>
<!-- PageHeader code from 06-RESEARCH lines 545-583. Add mobileMenuTrigger?: React.ReactNode slot -->
<!-- (06-RESEARCH Open Question 3): rendered on the left at md:hidden so a 'use client' hamburger -->
<!-- can live inside a Server Component header. -->
<!-- EmptyState anatomy from 06-UI-SPEC lines 485-495; table of contexts lines 497-506. -->
<!-- ErrorState body from 06-UI-SPEC lines 462-477 (AlertTriangle + h2 + p + Button reset). -->
<!-- @base-ui Button render-prop (no asChild); for breadcrumb use shadcn Breadcrumb primitives. -->

PageHeaderProps:
  title: string
  breadcrumbs?: { label: string; href?: string }[]
  actions?: React.ReactNode
  mobileMenuTrigger?: React.ReactNode   // md:hidden left slot

EmptyStateProps:
  icon: LucideIcon
  title: string
  description: string
  cta?: React.ReactNode

Loading/Error copy (06-UI-SPEC Copywriting lines 781-787):
  loading aria-label "Carregando…"; error heading "Algo deu errado";
  error body "Não foi possível carregar esta página. Tente novamente."; retry "Tentar novamente".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PageHeader + MobileMenuTrigger</name>
  <files>src/components/shell/PageHeader.tsx, src/components/shell/MobileMenuTrigger.tsx</files>
  <read_first>
    - 06-RESEARCH.md (PageHeader code lines 545-583; Open Question 3 mobile slot)
    - 06-UI-SPEC.md (PageHeader contract lines 400-431; Responsive lines 847-855; copy lines 763)
    - src/components/ui/breadcrumb.tsx, src/components/ui/sheet.tsx
  </read_first>
  <action>
    1. Create `src/components/shell/PageHeader.tsx` (Server Component — no `'use client'`) per 06-RESEARCH lines 545-583. Container: `<header className="h-16 px-6 border-b border-border bg-background flex items-center justify-between shrink-0">`. Left column: render `mobileMenuTrigger` (when provided) at `md:hidden`, then optional `Breadcrumb` (`text-sm text-muted-foreground`), then `<h1 className="text-xl font-semibold font-display leading-tight">{title}</h1>`. Right: `actions` in `flex items-center gap-2`. Add the `mobileMenuTrigger?: React.ReactNode` prop to the interface (06-RESEARCH Open Question 3).
    2. Create `src/components/shell/MobileMenuTrigger.tsx` (`'use client'`). Renders a hamburger `Menu` icon button (`aria-label="Abrir menu"`, `md:hidden`) that opens a shadcn `Sheet` (`side="left"`, content `w-[240px]`). The Sheet content renders the same nav modules — accept the nav items + role decision as props (`items: {href,label,icon}[]`) so the page (server) can pass the role-gated list. Use shadcn `Sheet` primitives; the drawer gets `role="dialog"` focus trap by default. Inside, render the nav links (reuse the active-link styling convention; a simple `<Link>` list with `usePathname` highlight is fine) + a `<ThemeToggle/>` + Sair form is OPTIONAL (nav links are the minimum).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/page-pattern.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>PageHeader exports the component with title/breadcrumbs/actions/mobileMenuTrigger; MobileMenuTrigger opens a left Sheet; page-pattern.test.ts PageHeader assertion PASS; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: EmptyState + ErrorState + skeletons toolkit</name>
  <files>src/components/shell/EmptyState.tsx, src/components/shell/ErrorState.tsx, src/components/shell/skeletons.tsx</files>
  <read_first>
    - 06-UI-SPEC.md (Empty State lines 481-507; Error States lines 456-477; Loading lines 434-452)
    - src/components/ui/skeleton.tsx, src/components/ui/button.tsx
  </read_first>
  <action>
    1. Create `src/components/shell/EmptyState.tsx` per 06-UI-SPEC lines 485-495. Props `{ icon: LucideIcon; title; description; cta? }`. Markup: `<div className="flex flex-col items-center justify-center py-16 gap-4 text-center">` → `<Icon className="size-10 text-muted-foreground" />` (icon is NOT text-primary — accent reserved list), heading `<h3 className="text-xl font-semibold font-display">`, body `<p className="text-sm text-muted-foreground max-w-sm">`, optional `cta`. No `font-medium`/`text-base` (audit fix).
    2. Create `src/components/shell/ErrorState.tsx` (`'use client'`) per 06-UI-SPEC lines 462-477. Props `{ reset: () => void }`. `AlertTriangle` icon (`size-10 text-muted-foreground`), `<h2 className="text-xl font-semibold font-display">Algo deu errado</h2>`, body "Não foi possível carregar esta página. Tente novamente.", `<Button onClick={reset}>Tentar novamente</Button>`. Add `useEffect` to focus the `<h2>` on mount for screen-reader announcement (06-UI-SPEC line 842). This component is imported by each route's `error.tsx` (built in Wave 3) so each segment's error.tsx is a 3-line `'use client'` wrapper.
    3. Create `src/components/shell/skeletons.tsx` — reusable building blocks using shadcn `Skeleton` (`animate-pulse bg-muted rounded-md`), each wrapped with `aria-busy="true" aria-label="Carregando…"` at the container level where used. Export at least: `PageHeaderSkeleton`, `CardGridSkeleton({ count })` (h-28 cards), `TableRowsSkeleton({ rows })`, `TotalsCardsSkeleton` (3 × min-h-[72px]). These map to the per-page skeleton table in 06-UI-SPEC lines 441-449. Wave-3 `loading.tsx` files compose these.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/page-pattern.test.ts; npx tsc --noEmit; npx next build</automated>
  </verify>
  <done>EmptyState (icon+title+description+cta), ErrorState (icon+heading+retry+focus), skeletons toolkit exported; page-pattern.test.ts EmptyState assertion PASS; tsc exits 0; next build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None new. All primitives are presentational; ErrorState/MobileMenuTrigger are client components with no data access.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information disclosure | EmptyState/ErrorState copy | accept | Generic pt-BR copy only; ErrorState never renders `error.message` to the user (only the static body), preventing stack/detail leakage. |
| T-06-08 | Tampering | MobileMenuTrigger nav items | accept | Nav links are UI-only; route protection remains server-side (mirrors T-06-04). |
</threat_model>

<verification>
- page-pattern.test.ts PageHeader + EmptyState assertions GREEN.
- `npx tsc --noEmit` exits 0; `npx next build` green.
</verification>

<success_criteria>
PageHeader (with mobile slot), MobileMenuTrigger, EmptyState, ErrorState, and a skeletons toolkit exist and compile; build green. Wave-3 plans can import all of them.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-04-SUMMARY.md`
</output>
