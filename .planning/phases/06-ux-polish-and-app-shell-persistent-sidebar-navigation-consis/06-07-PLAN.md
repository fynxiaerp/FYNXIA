---
phase: 06-ux-polish-and-app-shell
plan: 07
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - src/app/(dashboard)/clinica/financeiro/page.tsx
  - src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx
  - src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/loading.tsx
  - src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/error.tsx
  - src/app/(dashboard)/clinica/financeiro/contas-a-receber/page.tsx
  - src/app/(dashboard)/clinica/financeiro/contas-a-receber/loading.tsx
  - src/app/(dashboard)/clinica/financeiro/contas-a-receber/error.tsx
  - src/app/(dashboard)/clinica/financeiro/nova-cobranca/page.tsx
  - src/app/(dashboard)/clinica/financeiro/regua-de-cobranca/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every financeiro screen uses PageHeader with consistent breadcrumbs + actions"
    - "Page widths are standardized (max-w-5xl hub/lists; max-w-2xl/xl forms)"
    - "Fluxo de Caixa and Contas a Receber have layout-mimicking loading.tsx + error.tsx"
    - "Empty states use lucide icons + font-semibold headings (no font-medium)"
    - "Sub-module card icons default to text-muted-foreground"
  artifacts:
    - path: "src/app/(dashboard)/clinica/financeiro/page.tsx"
      provides: "Financeiro hub with PageHeader + token-correct sub-module cards"
    - path: "src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/loading.tsx"
      provides: "3 totals cards + 6 transaction row skeletons"
    - path: "src/app/(dashboard)/clinica/financeiro/contas-a-receber/loading.tsx"
      provides: "filter bar + 5 table row skeletons"
  key_links:
    - from: "src/app/(dashboard)/clinica/financeiro/*/page.tsx"
      to: "src/components/shell/PageHeader.tsx"
      via: "PageHeader import"
      pattern: "PageHeader"
    - from: "src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx"
      to: "EmptyState"
      via: "font-semibold empty-state heading"
      pattern: "EmptyState"
---

<objective>
Sweep the financial module (D-04): apply `PageHeader` + standardized widths to the Financeiro hub,
Fluxo de Caixa, Contas a Receber, Nova Cobrança, and Régua de Cobrança; add layout-mimicking
`loading.tsx`/`error.tsx` to the two data-heavy list screens; replace empty-state `font-medium`
headings with `font-semibold` + lucide icons; and set sub-module card icons to
`text-muted-foreground` (accent only on hover).

Purpose: closes UI-REVIEW findings — max-width mismatch, missing skeletons/error, empty-state
`font-medium`, nav-icon accent overuse.
Output: a consistent, well-stated financial module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-UI-SPEC.md
@.planning/reports/UI-REVIEW-v1.0.md
@src/app/(dashboard)/clinica/financeiro/page.tsx
@CLAUDE.md

<interfaces>
<!-- Reuse Wave 2: PageHeader, EmptyState, ErrorState, skeletons (TotalsCardsSkeleton, TableRowsSkeleton). -->
<!-- error.tsx 3-line wrapper around ErrorState. Financial status badges keep amber/green/red Tailwind -->
<!-- classes (06-UI-SPEC lines 192-196) — NOT tokens. financial amounts text-2xl font-semibold tabular-nums. -->
<!-- Régua de Cobrança is admin-only (Settings2 card) — preserve existing role gating. -->

Per-screen (06-UI-SPEC lines 654-681):
  Hub: title "Financeiro", breadcrumb "Clínica > Financeiro", actions [Nova Cobrança];
    sub-module cards TrendingUp/Receipt/FilePlus/Settings2; icons text-muted-foreground group-hover:text-primary.
  Fluxo de Caixa: title "Fluxo de Caixa", breadcrumb "Financeiro > Fluxo de Caixa",
    actions [+ Lançamento] + month nav; max-w-5xl mx-auto; empty Receipt "Nenhum lançamento em [mês]".
  Contas a Receber: title "Contas a Receber", breadcrumb "Financeiro > Contas a Receber",
    actions [Emitir Cobrança]; max-w-5xl mx-auto; empty ReceiptText "Nenhum recebível cadastrado" CTA "Emitir Cobrança".
  Nova Cobrança: title "Nova Cobrança", breadcrumb "Financeiro > Nova Cobrança", no actions; max-w-2xl mx-auto.
  Régua: title "Régua de Cobrança", breadcrumb "Financeiro > Régua de Cobrança", no actions; max-w-xl mx-auto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Financeiro hub + Nova Cobrança + Régua</name>
  <files>src/app/(dashboard)/clinica/financeiro/page.tsx, src/app/(dashboard)/clinica/financeiro/nova-cobranca/page.tsx, src/app/(dashboard)/clinica/financeiro/regua-de-cobranca/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/financeiro/page.tsx (current — icon text-primary to change)
    - 06-UI-SPEC.md (Financeiro Hub lines 654-658; Nova Cobrança 672-675; Régua 677-680)
    - UI-REVIEW-v1.0.md (nav-icon accent overuse)
  </read_first>
  <action>
    1. `financeiro/page.tsx`: add `<PageHeader title="Financeiro" breadcrumbs={[{label:'Clínica',href:'/clinica'},{label:'Financeiro'}]} actions={<Button>Nova Cobrança</Button>} mobileMenuTrigger={...}/>`. Content `p-6 max-w-5xl mx-auto w-full`. Sub-module cards: icons `TrendingUp` (Fluxo de Caixa), `Receipt` (Contas a Receber), `FilePlus` (Nova Cobrança), `Settings2` (Régua — admin only, preserve gating). Change every card icon from `text-primary` to `text-muted-foreground` with `group-hover:text-primary` (accent reserved). No `font-medium`.
    2. `nova-cobranca/page.tsx`: add `<PageHeader title="Nova Cobrança" breadcrumbs={[{label:'Financeiro',href:'/clinica/financeiro'},{label:'Nova Cobrança'}]} mobileMenuTrigger={...}/>` (no actions). Content `max-w-2xl mx-auto w-full p-6`. Normalize typography (no font-medium/text-3xl/text-lg).
    3. `regua-de-cobranca/page.tsx`: add `<PageHeader title="Régua de Cobrança" breadcrumbs={[{label:'Financeiro',href:'/clinica/financeiro'},{label:'Régua de Cobrança'}]} mobileMenuTrigger={...}/>` (no actions). Content `max-w-xl mx-auto w-full p-6`. Preserve admin gating. Normalize typography.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/page-pattern.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>Hub/Nova Cobrança/Régua use PageHeader + standardized widths; hub icons text-muted-foreground; page-pattern assertion progresses; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Fluxo de Caixa — PageHeader, width, empty-state, loading/error</name>
  <files>src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx, src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/loading.tsx, src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/error.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx (current — max-w-4xl, font-medium empty heading)
    - 06-UI-SPEC.md (Fluxo de Caixa lines 660-664; Empty State line 501; skeleton line 447)
  </read_first>
  <action>
    1. Add `<PageHeader title="Fluxo de Caixa" breadcrumbs={[{label:'Financeiro',href:'/clinica/financeiro'},{label:'Fluxo de Caixa'}]} actions={<>[+ Lançamento button] [month nav]</>} mobileMenuTrigger={...}/>`. Standardize content to `max-w-5xl mx-auto w-full p-6` (was max-w-4xl).
    2. Replace the empty-state block (currently `text-sm font-medium`) with the `Receipt` `EmptyState`: heading "Nenhum lançamento em [mês]" (font-semibold via EmptyState), body per 06-UI-SPEC line 501, no CTA.
    3. Keep `CashFlowTotals` (amounts `text-2xl font-semibold tabular-nums`) and status badges (amber/green/red) untouched.
    4. Create `loading.tsx` (PageHeaderSkeleton + TotalsCardsSkeleton + 6 TableRowsSkeleton) and `error.tsx` (ErrorState wrapper).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/page-pattern.test.ts src/__tests__/ui/typography.test.ts; npx tsc --noEmit</automated>
  </verify>
  <done>Fluxo de Caixa uses PageHeader + max-w-5xl + icon EmptyState (no font-medium); loading/error exist; typography.test.ts fluxo assertion PASS; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Contas a Receber — PageHeader, width, empty-state, loading/error</name>
  <files>src/app/(dashboard)/clinica/financeiro/contas-a-receber/page.tsx, src/app/(dashboard)/clinica/financeiro/contas-a-receber/loading.tsx, src/app/(dashboard)/clinica/financeiro/contas-a-receber/error.tsx</files>
  <read_first>
    - src/app/(dashboard)/clinica/financeiro/contas-a-receber/page.tsx (current — font-medium empty heading)
    - 06-UI-SPEC.md (Contas a Receber lines 666-670; Empty State line 502; skeleton line 448)
  </read_first>
  <action>
    1. Add `<PageHeader title="Contas a Receber" breadcrumbs={[{label:'Financeiro',href:'/clinica/financeiro'},{label:'Contas a Receber'}]} actions={<Button>Emitir Cobrança</Button>} mobileMenuTrigger={...}/>`. Standardize content to `max-w-5xl mx-auto w-full p-6`.
    2. Replace the empty-state (`font-medium`) with the `ReceiptText` `EmptyState`: heading "Nenhum recebível cadastrado", body per 06-UI-SPEC line 502, CTA "Emitir Cobrança".
    3. Keep `ReceivablesTable` + the client-side vencido derivation + status badges untouched.
    4. Create `loading.tsx` (PageHeaderSkeleton + filter-bar skeleton + 5 TableRowsSkeleton per 06-UI-SPEC line 448) and `error.tsx` (ErrorState wrapper).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ui/page-pattern.test.ts src/__tests__/ui/typography.test.ts; npx tsc --noEmit; npx next build</automated>
  </verify>
  <done>Contas a Receber uses PageHeader + max-w-5xl + icon EmptyState (no font-medium); loading/error exist; typography.test.ts contas assertion PASS; tsc exits 0; next build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
All financial reads/writes go through existing RLS-scoped server actions / Asaas adapter. No boundary changed.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Information disclosure | financeiro amounts/receivables | accept | Same RLS-scoped financial data; presentation sweep only — no query/server-action changes. |
| T-06-14 | Elevation of privilege | Régua de Cobrança (admin-only) | accept | Existing admin gating preserved; nav/card visibility is UI-only; route protection unchanged. |
</threat_model>

<verification>
- page-pattern.test.ts + typography.test.ts GREEN for financeiro files.
- `npx tsc --noEmit` exits 0; `npx next build` green.
- Manual (UAT, deferred): skeletons during slow fetch, empty states, both themes, dense-table dark legibility.
</verification>

<success_criteria>
All five financeiro screens use PageHeader with standardized widths; the two list screens have layout-mimicking loading/error; empty states use icons + font-semibold; sub-module icons are muted; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-and-app-shell-persistent-sidebar-navigation-consis/06-07-SUMMARY.md`
</output>
