---
phase: 02-clinical-mvp
plan: "04"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/lib/validators/anamnesis.ts
  - src/actions/anamneses.ts
  - src/actions/public-booking.ts
  - src/components/anamnesis/SignatureCanvas.tsx
  - src/components/anamnesis/AnamnesisForm.tsx
  - src/app/anamnese/[patient-id]/[token]/page.tsx
  - src/app/agendar/[clinic-slug]/page.tsx
  - src/components/booking/PublicBookingForm.tsx
  - package.json
  - src/__tests__/anamnesis/signature.test.ts
  - src/__tests__/proxy/rbac.test.ts
autonomous: true
requirements: [CLINIC-08, CLINIC-09, SEC-04]
user_setup: []

must_haves:
  truths:
    - "Paciente assina anamnese em canvas; SHA-256 do PNG + timestamp ISO + IP + user-agent registrados (CLINIC-08, D-16)"
    - "Dois fluxos: link público /anamnese/[patient-id]/[token] e presencial (D-17)"
    - "Botão 'Assinar e Enviar' desabilitado até a assinatura ser confirmada (D-19)"
    - "Token de anamnese é single-use e expira (72h); reuso/expirado é rejeitado com mesma mensagem genérica (D-20, Pitfall 5)"
    - "Registro de anamnese é imutável após assinatura — sem UPDATE (D-20)"
    - "Paciente usa link público /agendar/[clinic-slug] para agendar sem login; slot bloqueado imediatamente via GIST (CLINIC-09, D-04)"
    - "Race condition no agendamento público retorna mensagem amigável ao capturar 23P01 (Pitfall 7)"
  artifacts:
    - path: "src/actions/anamneses.ts"
      provides: "submitAnamnesis com SHA-256, IP, user-agent, token single-use via service role"
      contains: "createHash('sha256')"
    - path: "src/components/anamnesis/SignatureCanvas.tsx"
      provides: "wrapper signature_pad v5 com cleanup no unmount"
      contains: "signature_pad"
    - path: "src/actions/public-booking.ts"
      provides: "createPublicAppointment via service role com captura de 23P01"
      contains: "23P01"
    - path: "src/app/anamnese/[patient-id]/[token]/page.tsx"
      provides: "página pública de anamnese com validação de token"
      contains: "token"
  key_links:
    - from: "src/actions/anamneses.ts"
      to: "public.anamneses token_used_at"
      via: "UPDATE condicional single-use (token_used_at IS NULL AND token_expires_at > now())"
      pattern: "token_used_at"
    - from: "src/components/anamnesis/AnamnesisForm.tsx"
      to: "SignatureCanvas onSign"
      via: "habilita 'Assinar e Enviar' só após assinatura confirmada"
      pattern: "disabled"
    - from: "src/actions/public-booking.ts"
      to: "23P01 exclusion_violation"
      via: "captura de conflito de slot no agendamento público"
      pattern: "23P01"
---

<objective>
Entregar a anamnese digital com assinatura manuscrita (CLINIC-08) e o link de agendamento público (CLINIC-09). Inclui: validador Zod da anamnese CFO, Server Action `submitAnamnesis` com SHA-256 do PNG + IP + user-agent + token single-use, o wrapper `SignatureCanvas` (signature_pad v5) e o `AnamnesisForm` (questionário CFO fixo + assinatura obrigatória), a página pública de anamnese com validação de token, e o fluxo de agendamento público `/agendar/[clinic-slug]` com bloqueio imediato de slot via GIST e tratamento de race condition (23P01).

Purpose: Fecha o fluxo clínico completo do dentista (consultar → cadastrar → documentar → odontograma → assinar anamnese) e abre o canal de aquisição de pacientes (link público de agendamento). Depende de pacientes/agendamentos (Plan 02) e das tabelas anamneses/appointments (Plan 01) — Wave 3.

Output: Server Actions de anamnese e agendamento público, componentes de assinatura e formulário CFO, páginas públicas /anamnese e /agendar, e testes Vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-clinical-mvp/02-CONTEXT.md
@.planning/phases/02-clinical-mvp/02-RESEARCH.md
@.planning/phases/02-clinical-mvp/02-UI-SPEC.md
@.planning/phases/02-clinical-mvp/02-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Padrões e contratos existentes que o executor DEVE replicar. -->

src/lib/audit.ts: logBusinessEvent({ tenantId, actorId, action, details }) — details só com IDs.
src/lib/supabase/admin.ts: createAdminClient() — bypass RLS (server-only). USAR para fluxos públicos
  sem sessão (anamnese por token, agendamento público) — mesmo padrão de acceptInvitation/patient self-register.
src/lib/supabase/server.ts: createClient() — RLS via JWT (fluxo presencial autenticado).

Padrão de token single-use (de src/actions/invitations.ts acceptInvitation — UPDATE atômico condicional):
  UPDATE ... SET token_used_at = now() WHERE token = $1 AND token_used_at IS NULL
    AND token_expires_at > now() RETURNING id;
  Se retorna 0 linhas → inválido/expirado/usado → rejeitar com mensagem genérica.

proxy.ts: '/agendar' e '/anamnese'... — '/agendar' JÁ é rota pública (isPublicRoute, linha 36).
  '/anamnese' NÃO está na lista de isPublicRoute atualmente — ADICIONAR nesta fase.

DB (de Plan 01):
  anamneses(id, tenant_id, patient_id, responses JSONB, signature_hash TEXT NOT NULL, signature_url,
    signed_at, ip_address INET, user_agent, flow 'presencial'|'link_publico', token UUID UNIQUE,
    token_expires_at, token_used_at, created_at) — imutável após insert (D-20).
  appointments(... source 'interno'|'publico', constraint no_overlap GIST) — agendamento público usa source='publico'.
  clinics(id, slug, name) — resolver clinic por slug no agendamento público.

SHA-256 do PNG (de 02-RESEARCH.md Padrão 6, Node.js built-in):
  const base64 = dataUrl.replace(/^data:image\/png;base64,/, '')
  const buf = Buffer.from(base64, 'base64')
  const hash = createHash('sha256').update(buf).digest('hex')
  IP: headers().get('x-forwarded-for') ?? headers().get('x-real-ip') ?? 'unknown'

signature_pad v5.1.3 (headless): new SignaturePad(canvas, { penColor: '#111827' });
  pad.isEmpty(); pad.toDataURL('image/png'); pad.clear(); pad.off() no cleanup (M-8).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0) + validador: testes RED de assinatura/token + Zod CFO</name>
  <read_first>
    - .planning/phases/02-clinical-mvp/02-VALIDATION.md
    - .planning/phases/02-clinical-mvp/02-RESEARCH.md (Padrão 6; Pitfall 5)
    - src/__tests__/proxy/rbac.test.ts
    - src/proxy.ts
  </read_first>
  <behavior>
    - sha256OfPngDataUrl: determinístico — mesmo dataUrl produz o mesmo hash hex (64 chars)
    - isTokenValid(row, now): true só se token_used_at IS NULL E token_expires_at > now; false se usado ou expirado
    - anamnesisSchema valida as respostas CFO fixas (booleans) e exige signature presente
    - rbac.test.ts: '/anamnese' é tratada como rota pública (não exige sessão)
  </behavior>
  <files>src/lib/validators/anamnesis.ts, src/__tests__/anamnesis/signature.test.ts, src/__tests__/proxy/rbac.test.ts</files>
  <action>
    Instalar signature_pad (RESEARCH instalação): `npm install signature_pad`.

    Criar `src/lib/validators/anamnesis.ts` (Zod v3): `anamnesisSchema` com o questionário CFO fixo (D-18) — campos boolean para: alergia_medicamento, alergia_anestesia, hipertensao, diabetes, problema_cardiaco, gravidez, uso_medicamento_continuo, problema_coagulacao, etc. (pré-definidos CFO), mais campo opcional de observações. Exportar `type AnamnesisInput` e a lista `CFO_QUESTIONS` (array de { key, label pt-BR }) para a UI. Exportar função pura `sha256OfPngDataUrl(dataUrl: string): string` (usa node:crypto createHash) e `isTokenValid(row: { token_used_at: string|null; token_expires_at: string|null }, now: Date): boolean`.

    Criar `src/__tests__/anamnesis/signature.test.ts` (deve passar GREEN ao fim desta task):
    - `sha256OfPngDataUrl` determinístico (mesmo input = mesmo hash; comprimento 64 hex)
    - `isTokenValid`: usado → false; expirado → false; não-usado e dentro do prazo → true
    - anamnesisSchema rejeita submissão sem assinatura

    Estender `src/__tests__/proxy/rbac.test.ts`: adicionar assertion de que pathname iniciando com '/anamnese' é público (depende da alteração de proxy.ts na Task 3 — pode ficar RED até lá; documentar no teste).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/anamnesis/signature.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "createHash" src/lib/validators/anamnesis.ts` (ou import de node:crypto) presente
    - `CFO_QUESTIONS` exportado com ao menos 6 perguntas pt-BR
    - `isTokenValid` e `sha256OfPngDataUrl` exportados
    - signature.test.ts GREEN nas partes de hash/token/schema
  </acceptance_criteria>
  <done>Validador CFO + funções puras de hash/token criados; signature.test.ts GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Server Actions de anamnese (presencial + token público)</name>
  <read_first>
    - src/actions/invitations.ts (padrão de token single-use atômico em acceptInvitation)
    - src/lib/audit.ts
    - src/lib/validators/anamnesis.ts
    - .planning/phases/02-clinical-mvp/02-RESEARCH.md (Padrão 6; Pitfall 5; Security Domain V13)
  </read_first>
  <behavior>
    - createAnamnesisToken(patientId): admin/dentist/receptionist gera token UUID + token_expires_at = now()+72h; flow 'link_publico'; retorna URL
    - submitAnamnesis (presencial): staff autenticado; calcula SHA-256, IP, user-agent; insert flow 'presencial'; imutável
    - submitAnamnesisPublic (token): valida token single-use atômico via service role; calcula hash/IP/UA; marca token_used_at; rejeita reuso/expirado com mensagem genérica
  </behavior>
  <files>src/actions/anamneses.ts</files>
  <action>
    Criar `src/actions/anamneses.ts` ('use server'):
    - `createAnamnesisToken(patientId)`: getUser → actor (tenant_id, role); validar staff; criar linha de anamnese parcial? NÃO — em vez disso gerar um token e inserir uma linha pendente seria mutável. Abordagem: inserir uma linha de anamnese "pendente" com token, token_expires_at=now()+72h, flow='link_publico', signature_hash placeholder NÃO permitido (NOT NULL). Portanto: armazenar o token numa linha de anamnese só é possível no submit. Em vez disso, gerar o token e guardá-lo numa estrutura leve: criar a linha de anamnese SOMENTE no submit; o token é gerado aqui e embutido na URL, e validade é derivada de uma tabela. Para MVP sem nova tabela: inserir a linha de anamnese no submit e validar o token contra uma linha "convite" reutilizando o padrão — porém o schema do Plan 01 coloca token NA tabela anamneses. SOLUÇÃO ALINHADA AO SCHEMA: ao gerar, inserir a linha de anamnese com `responses='{}'`, `signature_hash=''`... mas signature_hash é NOT NULL. Para respeitar o schema do Plan 01 sem alterá-lo, gerar o token e persistir a linha somente no submit; a validade do token é checada por existência+prazo. Como o schema exige a linha conter o token, ajuste de fluxo: `createAnamnesisToken` insere a linha com signature_hash = 'PENDING' (string não-vazia satisfaz NOT NULL), token, token_expires_at; o submit faz UPDATE atômico condicional (token_used_at IS NULL AND token_expires_at>now()) setando hash/responses/ip/ua/token_used_at. NOTA D-20 (imutabilidade): a imutabilidade vale APÓS a assinatura — o UPDATE de PENDING→assinado é a transição de assinatura, permitida uma única vez via o gate atômico; após token_used_at setado, nenhum outro UPDATE passa. Registrar essa nuance no SUMMARY.
      Implementação: usar createClient() (staff autenticado, RLS anamneses_staff_insert permite). Retornar `{ url: ${SITE_URL}/anamnese/${patientId}/${token} }`.
    - `submitAnamnesisPublic(patientId, token, responses, signatureDataUrl)`: usar `createAdminClient()` (sem sessão). Calcular hash via sha256OfPngDataUrl; IP/UA de headers(). UPDATE atômico:
      `update anamneses set signature_hash=hash, responses=..., ip_address=ip, user_agent=ua, signed_at=now(), token_used_at=now() where token=$ and token_used_at is null and token_expires_at > now() and signature_hash='PENDING' returning id`.
      Se 0 linhas → `{ success:false, error:'Este link de anamnese expirou ou já foi utilizado. Solicite um novo link à sua clínica.' }` (mesma msg para expirado/usado — Pitfall 5/Security). Audita 'anamnesis.signed' (apenas patient_id, anamnesis_id) usando o tenant_id da linha.
    - `submitAnamnesisPresencial(patientId, responses, signatureDataUrl)`: createClient() (staff); calcular hash/IP/UA; INSERT direto com flow='presencial', signature_hash=hash, signed_at=now(). Imutável. Audita 'anamnesis.signed'.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/anamnesis/signature.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "createHash('sha256')" src/actions/anamneses.ts` OU uso de `sha256OfPngDataUrl` presente
    - UPDATE atômico de submit contém `token_used_at is null` E `token_expires_at > now()` (single-use + expiry)
    - Fluxo público usa `createAdminClient()`; fluxo presencial usa `createClient()`
    - Mensagem de token inválido é idêntica para expirado e usado (não distingue — segurança)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Server Actions de anamnese (token público single-use + presencial) completas; tipos limpos.</done>
</task>

<task type="auto">
  <name>Task 2: SignatureCanvas + AnamnesisForm + página pública de anamnese</name>
  <read_first>
    - .planning/phases/02-clinical-mvp/02-RESEARCH.md (Padrão 6 — signature_pad + cleanup M-8)
    - .planning/phases/02-clinical-mvp/02-UI-SPEC.md (Public Anamnesis Page Layout; Anamnesis Canvas Interactions; Accessibility; Copywriting; Error States; Responsive — touch 44px)
    - src/lib/validators/anamnesis.ts
    - src/actions/anamneses.ts
  </read_first>
  <files>src/components/anamnesis/SignatureCanvas.tsx, src/components/anamnesis/AnamnesisForm.tsx, src/app/anamnese/[patient-id]/[token]/page.tsx</files>
  <action>
    Garantir shadcn checkbox: `npx shadcn@latest add checkbox card alert button`.

    `src/components/anamnesis/SignatureCanvas.tsx` ('use client'): conforme 02-RESEARCH.md Padrão 6. useRef<HTMLCanvasElement>; new SignaturePad no useEffect; `padRef.current?.off()` no cleanup (M-8). Botões "Limpar" (clear + reset onClear) e "Confirmar Assinatura" (se !isEmpty, chama onSign(toDataURL('image/png'))). canvas 600×200, `touchAction: 'none'`, `aria-label="Área de assinatura"` (Accessibility). Texto adjacente "Desenhe sua assinatura no campo acima."

    `src/components/anamnesis/AnamnesisForm.tsx` ('use client'): renderiza CFO_QUESTIONS como grupos de Checkbox (yes/no). Inclui `<SignatureCanvas onSign={setSignature} onClear={() => setSignature(null)} />`. Botão "Assinar e Enviar" `disabled={!signature}` (D-19). No submit chama a action apropriada (pública ou presencial) recebida via props. Touch targets min 44px (Responsive). Em erro mostra Alert.

    `src/app/anamnese/[patient-id]/[token]/page.tsx` (rota pública): Server Component. Validar o token: buscar a linha de anamnese por token via createAdminClient() e checar `isTokenValid` + `signature_hash === 'PENDING'`. Se inválido/expirado/usado → renderizar estado de erro full-page "Link expirado" / "Este link de anamnese expirou ou já foi utilizado. Solicite um novo link à sua clínica." (UI-SPEC Error States) — NÃO renderizar o formulário. Se válido → renderizar `<AnamnesisForm mode="public" patientId={...} token={...} />` que chama submitAnamnesisPublic. Layout conforme Public Anamnesis Page Layout (logo, título, progress, CFO, assinatura, CTA). max-w-lg mx-auto, responsivo mobile.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - `grep "signature_pad" src/components/anamnesis/SignatureCanvas.tsx` retorna match
    - `grep ".off()" src/components/anamnesis/SignatureCanvas.tsx` (cleanup M-8) presente
    - `grep "disabled" src/components/anamnesis/AnamnesisForm.tsx` — botão ligado ao estado de assinatura (D-19)
    - Página de anamnese renderiza estado de erro quando token inválido (não mostra formulário)
    - `aria-label="Área de assinatura"` presente
    - `npx tsc --noEmit` exit 0; suite Vitest GREEN
  </acceptance_criteria>
  <done>UI de anamnese (canvas + formulário CFO + página pública) completa com gate de assinatura e estado de token inválido.</done>
</task>

<task type="auto">
  <name>Task 3: Agendamento público (/agendar) + ativar rota pública /anamnese no proxy</name>
  <read_first>
    - .planning/phases/02-clinical-mvp/02-RESEARCH.md (Pitfall 7 — race condition agendamento público)
    - .planning/phases/02-clinical-mvp/02-UI-SPEC.md (Public Booking Page Layout; Copywriting; Error States; Responsive)
    - src/proxy.ts
    - src/actions/appointments.ts
    - src/lib/validators/appointment.ts
  </read_first>
  <files>src/actions/public-booking.ts, src/app/agendar/[clinic-slug]/page.tsx, src/components/booking/PublicBookingForm.tsx, src/proxy.ts</files>
  <action>
    `src/proxy.ts`: adicionar `/anamnese` à condição `isPublicRoute` (atualmente apenas '/invite' e '/agendar'). Alterar a linha para:
    ```typescript
    const isPublicRoute =
      pathname.startsWith('/invite') ||
      pathname.startsWith('/agendar') ||
      pathname.startsWith('/anamnese')
    ```

    `src/actions/public-booking.ts` ('use server'): `createPublicAppointment(clinicSlug, input)`:
    - usar `createAdminClient()` (sem sessão — fluxo público).
    - resolver clinic por slug: `from('clinics').select('id').eq('slug', clinicSlug).single()`; se não achar → erro genérico.
    - opcional: criar/achar paciente mínimo pelos dados do formulário (nome, telefone, email) — para MVP, inserir paciente com cpf placeholder? NÃO usar placeholder de CPF (viola unique + não é real). Em vez disso: o agendamento público cria appointment com `patient_id = null` e os dados do solicitante em `notes` (nome/telefone), `source='publico'`, status='agendado'. A recepção vincula o paciente depois. Documentar no SUMMARY.
    - inserir appointment com tenant_id=clinic.id, dentist_id (do formulário/seleção), start_time/end_time, source='publico'. CRÍTICO: capturar `error.code === '23P01'` → `{ success:false, error:'Este horário acabou de ser reservado. Por favor, escolha outro horário.' }` (Pitfall 7 / UI-SPEC). Audita 'appointment.public_created' (tenant_id da clinic, actorId null/sistema, details com appointment_id).

    `src/app/agendar/[clinic-slug]/page.tsx` (rota pública): Server Component. Resolver clinic por slug; se inexistente → 404. Buscar dentistas da clínica via createAdminClient(). Renderizar `<PublicBookingForm clinicSlug={...} dentists={...} />`. Layout conforme Public Booking Page Layout (logo, "Agendar Consulta" Display, steps: dentista → data → slot → dados). max-w-lg mx-auto, touch 44px.

    `src/components/booking/PublicBookingForm.tsx` ('use client'): Select de dentista, shadcn Calendar para data, grid de Cards de slots disponíveis, Inputs de nome/telefone/email, CTA "Confirmar Agendamento" (D-04). Em erro 23P01 mostra Alert "Horário indisponível" + botão [Recarregar horários] que rebusca slots. Empty state "Sem horários disponíveis".
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/proxy/rbac.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "/anamnese" src/proxy.ts` aparece na condição isPublicRoute
    - `grep "23P01" src/actions/public-booking.ts` retorna match
    - public-booking usa `createAdminClient()` e `source: 'publico'`
    - PublicBookingForm tem CTA "Confirmar Agendamento" e estado de erro de slot tomado
    - rbac.test.ts GREEN (assertion de /anamnese pública agora passa); `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Agendamento público funcional com bloqueio GIST e tratamento de race condition; /anamnese ativada como rota pública.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet anônima → /anamnese/[token] | request sem autenticação com token na URL |
| internet anônima → /agendar/[slug] | agendamento público sem login (flooding, enumeration) |
| service role → anamneses/appointments | escrita privilegiada em fluxos públicos sem sessão |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-07 | Spoofing | token de anamnese compartilhado/reutilizado | high | mitigate | UPDATE atômico single-use (token_used_at IS NULL AND token_expires_at>now() AND signature_hash='PENDING'); mensagem genérica não distingue usado/expirado |
| T-2-01 | Tampering | double-booking via race no agendamento público | high | mitigate | createPublicAppointment captura 23P01; constraint GIST do Plan 01 é atômico; source='publico' coberto pelo mesmo constraint |
| T-2-14 | Tampering | alteração de anamnese assinada (validade jurídica) | high | mitigate | D-20 imutável: após token_used_at setado nenhum UPDATE passa o gate; sem policy de UPDATE para fluxo presencial (INSERT-only) |
| T-2-15 | Denial of Service | flooding do endpoint público de agendamento/anamnese | medium | accept | token de anamnese expira em 72h; agendamento limitado por GIST; rate limiting de borda deferido — risco baixo no MVP, sem PII exposta no flood |
| T-2-16 | Information Disclosure | IDOR — enumerar patient-id/clinic-slug no link público | medium | mitigate | token UUID não-adivinhável obrigatório para anamnese; agendamento público só expõe slots disponíveis, nunca dados de paciente; clinic-slug é público por design |
| T-2-08 | Privacy | dados de saúde/IP em logs | medium | mitigate | logBusinessEvent só com IDs; IP/UA gravados na linha de anamnese (evidência LGPD/CFO), nunca em audit details |
</threat_model>

<verification>
- `npx vitest run src/__tests__/` — GREEN (signature.test.ts + rbac.test.ts)
- `npx tsc --noEmit` exit 0
- `next build` sem erros
- Manual (UI-SPEC): fluxo público de anamnese completo (preencher CFO, assinar, verificar imutabilidade + reuso bloqueado); /agendar bloqueia slot imediatamente; canvas funciona em touch
</verification>

<success_criteria>
- Anamnese assinada com SHA-256 + IP + user-agent + timestamp (CLINIC-08, D-16)
- Dois fluxos: público (token single-use 72h) + presencial (D-17)
- "Assinar e Enviar" desabilitado até assinatura (D-19); registro imutável (D-20)
- Agendamento público bloqueia slot via GIST com tratamento de race (CLINIC-09, D-04)
- /anamnese ativada como rota pública no proxy
</success_criteria>

<output>
After completion, create `.planning/phases/02-clinical-mvp/02-04-SUMMARY.md`
</output>
