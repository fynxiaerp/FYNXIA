---
phase: 02-clinical-mvp
plan: "05"
type: execute
wave: 4
gap_closure: true
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - src/actions/public-booking.ts
  - src/actions/anamneses.ts
  - src/components/booking/PublicBookingForm.tsx
  - src/components/anamnesis/AnamnesisList.tsx
  - src/app/(dashboard)/clinica/pacientes/[id]/page.tsx
  - src/__tests__/actions/public-booking-availability.test.ts
  - src/__tests__/anamnesis/list.test.ts
requirements: [CLINIC-08, CLINIC-09]
source: [02-VERIFICATION.md, 02-HUMAN-UAT.md]
user_setup: []

must_haves:
  truths:
    - "getBookedSlots(clinicSlug, dentistId, date) returns occupied start_times for that dentist/day excluding status='cancelado', via service-role client, tenant-scoped by clinic slug"
    - "PublicBookingForm fetches booked slots on date select and renders already-booked slots as disabled/unavailable (not selectable)"
    - "PublicBookingForm sends timezone-aware datetimes (America/Sao_Paulo -03:00 offset) so they satisfy the publicBookingSchema datetime({offset:true}) — booking no longer fails validation on naive datetimes"
    - "listAnamneses(patientId) returns the patient's anamneses (id, flow, signed_at, status: pending|signed, created_at) via RLS-aware client, tenant-isolated"
    - "Patient detail page Anamneses tab renders the real list (AnamnesisList) plus a 'Gerar link de anamnese' action — the 'Disponível após Plano 04' stub is removed"
    - "AnamnesisList distinguishes signed (has signature_hash != 'PENDING' / signed_at) from pending public links (signature_hash='PENDING', not yet used)"
  artifacts:
    - path: "src/actions/public-booking.ts"
      provides: "getBookedSlots service-role action returning occupied ranges; brToIsoWithOffset helper or schema relaxation"
      contains: "getBookedSlots"
    - path: "src/actions/anamneses.ts"
      provides: "listAnamneses RLS-aware action"
      contains: "export async function listAnamneses"
    - path: "src/components/booking/PublicBookingForm.tsx"
      provides: "availability-aware slot grid + offset-correct datetimes"
      contains: "getBookedSlots"
    - path: "src/components/anamnesis/AnamnesisList.tsx"
      provides: "anamneses list UI for patient detail tab"
      contains: "AnamnesisList"
  key_links:
    - from: "PublicBookingForm date select"
      to: "getBookedSlots"
      via: "Server Action call marking booked slots unavailable"
      pattern: "getBookedSlots"
    - from: "patient detail Anamneses tab"
      to: "listAnamneses"
      via: "AnamnesisList render"
      pattern: "AnamnesisList"

<objective>
Fechar os dois gaps de produto da Fase 2 levantados na verificação (02-HUMAN-UAT itens 6 e 7), em vez de adiá-los:

1. **CLINIC-09 — disponibilidade no agendamento público:** a grade de horários do `PublicBookingForm` é gerada client-side sem consultar a agenda, então slots já ocupados aparecem como selecionáveis. Adicionar uma Server Action `getBookedSlots` (service-role, escopo por slug da clínica) que retorna os horários ocupados do dentista no dia, e marcar esses slots como indisponíveis na UI. Corrigir também o bug latente de datetime: o form envia datetime sem offset, mas `publicBookingSchema` exige `datetime({offset:true})`, o que faz toda submissão falhar na validação — enviar datetime com offset -03:00 (Brasil, sem DST).

2. **CLINIC-08 — aba Anamneses:** a aba de anamneses no detalhe do paciente ainda é um stub "Disponível após Plano 04". Adicionar `listAnamneses(patientId)` (RLS-aware) e o componente `AnamnesisList`, além do botão "Gerar link de anamnese" (reusa `createAnamnesisToken`). Remover o stub.

Purpose: entregar a UX completa de CLINIC-08 e CLINIC-09 (não só a camada de segurança), fechando os itens 6 e 7 do UAT humano.
Output: 2 novas Server Actions, 1 componente, atualização do form e da página de detalhe, testes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-clinical-mvp/02-VERIFICATION.md
@.planning/phases/02-clinical-mvp/02-04-SUMMARY.md
@CLAUDE.md

<interfaces>
Reuse existing patterns — do NOT reinvent:
- src/actions/appointments.ts → getActor() helper, 23P01 capture, tenant scoping.
- src/actions/medical-records.ts → listMedicalRecords (the list/order pattern to mirror for listAnamneses).
- src/actions/public-booking.ts → createPublicAppointment: createAdminClient(), resolve clinic by slug, publicBookingSchema (datetime({offset:true})), dentist tenant-validation (CR-01).
- src/actions/anamneses.ts → createAnamnesisToken (already exists; the tab's "Gerar link" button calls it).
- appointments columns: tenant_id, dentist_id, patient_id, start_time, end_time, status ('agendado'|'confirmado'|'em_atendimento'|'concluido'|'cancelado'), source.
- anamneses columns: id, tenant_id, patient_id, responses JSONB, signature_hash (TEXT NOT NULL; 'PENDING' = aguardando assinatura), signed_at, flow ('presencial'|'link_publico'), token, token_expires_at, token_used_at, created_at.
- UI: shadcn components already installed (Tabs, Badge, Alert, etc.). Brazil timezone is America/Sao_Paulo, fixed -03:00 (no DST since 2019).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (RED): availability + anamnesis-list test scaffolds</name>
  <read_first>
    - src/__tests__/anamnesis/signature.test.ts
    - src/__tests__/actions/medical-records.test.ts
    - vitest.config.ts
  </read_first>
  <files>src/__tests__/actions/public-booking-availability.test.ts, src/__tests__/anamnesis/list.test.ts</files>
  <action>
    Criar dois arquivos de teste seguindo o estilo dos testes existentes (mock do Supabase client como nos testes de actions atuais). Cobrir:
    - public-booking-availability: getBookedSlots retorna apenas start_times de appointments NÃO-cancelados do dentista no dia; helper de datetime gera string com offset -03:00 que passa em `z.string().datetime({offset:true})`.
    - anamnesis/list: listAnamneses retorna linhas mapeadas com status derivado (signature_hash==='PENDING' && token_used_at==null → 'pending'; senão 'signed'); rejeita sem autenticação.
    Os testes devem falhar RED (funções ainda não existem).
  </action>
  <verify><automated>npx vitest run src/__tests__/actions/public-booking-availability.test.ts src/__tests__/anamnesis/list.test.ts</automated></verify>
  <acceptance_criteria>
    - Ambos os arquivos existem e rodam
    - Falham RED (símbolos ainda não exportados)
  </acceptance_criteria>
  <done>Scaffolds RED commitados.</done>
</task>

<task type="auto">
  <name>Task 1: getBookedSlots + datetime offset fix (CLINIC-09 item 6)</name>
  <read_first>
    - src/actions/public-booking.ts
    - src/components/booking/PublicBookingForm.tsx
  </read_first>
  <files>src/actions/public-booking.ts, src/components/booking/PublicBookingForm.tsx</files>
  <action>
    Em public-booking.ts: adicionar `getBookedSlots(clinicSlug, dentistId, date)` usando createAdminClient(): resolver clínica por slug (deleted_at null), validar dentista pertence à clínica (mesmo padrão CR-01), e consultar appointments com tenant_id=clinic.id, dentist_id, start_time no intervalo [date 00:00, date+1 00:00) (offset -03:00), status != 'cancelado'. Retornar array de start_time ISO ocupados. Bounds: validar date (YYYY-MM-DD) e dentistId (uuid) com Zod; retornar [] em erro (não vazar detalhes).

    Em PublicBookingForm.tsx:
    - Ao selecionar data (handleDateSelect), chamar getBookedSlots via useTransition e guardar o set de start_times ocupados.
    - Em generateSlots, gerar start_time/end_time com offset -03:00 (ex.: `${date}T${HH}:${MM}:00-03:00`) para satisfazer o schema. Normalizar a comparação com os start_times ocupados retornados (comparar pelo instante, ex.: new Date(...).getTime(), pois o banco pode devolver em UTC).
    - Marcar slots ocupados como disabled (cursor-not-allowed, opacity, sem onClick), com rótulo/aria "indisponível". Mostrar loading enquanto busca. Manter o tratamento de 23P01 como rede de segurança final.
  </action>
  <verify><automated>npx vitest run src/__tests__/actions/public-booking-availability.test.ts</automated></verify>
  <acceptance_criteria>
    - `grep "getBookedSlots" src/actions/public-booking.ts` retorna a definição exportada
    - `grep "getBookedSlots" src/components/booking/PublicBookingForm.tsx` retorna a chamada
    - Form gera datetimes com `-03:00`
    - Slots ocupados ficam disabled
    - Teste de disponibilidade GREEN
  </acceptance_criteria>
  <done>getBookedSlots entregue; form consulta disponibilidade e envia datetime com offset.</done>
</task>

<task type="auto">
  <name>Task 2: listAnamneses + AnamnesisList + aba real (CLINIC-08 item 7)</name>
  <read_first>
    - src/actions/anamneses.ts
    - src/actions/medical-records.ts
    - src/app/(dashboard)/clinica/pacientes/[id]/page.tsx
  </read_first>
  <files>src/actions/anamneses.ts, src/components/anamnesis/AnamnesisList.tsx, src/app/(dashboard)/clinica/pacientes/[id]/page.tsx</files>
  <action>
    Em anamneses.ts: adicionar `listAnamneses(patientId)` RLS-aware (createClient, getActor pattern) retornando linhas do paciente ordenadas por created_at DESC: id, flow, signed_at, created_at, e status derivado ('pending' se signature_hash==='PENDING' e token_used_at==null; 'expired' se pending e token_expires_at < now; senão 'signed'). NÃO retornar responses/signature_hash crus para a lista. Tenant isolation via RLS (já garante).

    Criar AnamnesisList.tsx (client ou server component simples): recebe a lista e renderiza cada anamnese com data (signed_at ou created_at), tipo (Presencial / Link público), e Badge de status (Assinada / Aguardando assinatura / Link expirado). Estado vazio: "Nenhuma anamnese registrada". Inclui um botão "Gerar link de anamnese" que chama createAnamnesisToken(patientId) e exibe a URL gerada para copiar (com fallback de erro).

    Em [id]/page.tsx: substituir o stub da TabsContent "anamneses" — chamar listAnamneses(patient.id) no server component e renderizar <AnamnesisList ... />. Atualizar o comentário desatualizado sobre "Plano 04".
  </action>
  <verify><automated>npx vitest run src/__tests__/anamnesis/list.test.ts</automated></verify>
  <acceptance_criteria>
    - `grep "export async function listAnamneses" src/actions/anamneses.ts` retorna 1
    - AnamnesisList.tsx existe e é usado em [id]/page.tsx
    - Stub "Disponível após Plano 04" removido
    - Teste de lista GREEN
  </acceptance_criteria>
  <done>Aba Anamneses real entregue com listagem + gerar link.</done>
</task>

<task type="auto">
  <name>Task 3: full verification</name>
  <action>Rodar a suíte completa e o type-check; corrigir o que quebrar.</action>
  <verify><automated>npx vitest run && npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Toda a suíte GREEN
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Fase 2 com itens 6 e 7 fechados; suíte verde.</done>
</task>

</tasks>

<verification>
- npx vitest run — toda a suíte GREEN (incluindo os 2 novos arquivos)
- npx tsc --noEmit exit 0
- getBookedSlots exportada e usada; datetimes com offset -03:00
- listAnamneses exportada; AnamnesisList renderizada na aba; stub removido
</verification>

<success_criteria>
- Agendamento público mostra slots ocupados como indisponíveis e envia datetime válido (item 6 fechado)
- Aba Anamneses lista as anamneses do paciente e permite gerar link (item 7 fechado)
</success_criteria>

<output>
After completion, create `.planning/phases/02-clinical-mvp/02-05-SUMMARY.md`
</output>
