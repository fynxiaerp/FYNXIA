---
phase: 02-clinical-mvp
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/lib/validators/patient.ts
  - src/lib/validators/appointment.ts
  - src/actions/patients.ts
  - src/actions/appointments.ts
  - src/app/(dashboard)/clinica/pacientes/page.tsx
  - src/app/(dashboard)/clinica/pacientes/novo/page.tsx
  - src/app/(dashboard)/clinica/pacientes/[id]/page.tsx
  - src/app/(dashboard)/clinica/agenda/page.tsx
  - src/components/patients/PatientForm.tsx
  - src/components/patients/PatientTable.tsx
  - src/components/agenda/AgendaCalendar.tsx
  - src/proxy.ts
  - package.json
  - src/__tests__/actions/patients.test.ts
  - src/__tests__/agenda/calendar.test.ts
autonomous: true
requirements: [CLINIC-01, CLINIC-02, CLINIC-03, CLINIC-04, SEC-04]
user_setup: []

must_haves:
  truths:
    - "Recepcionista cadastra paciente com nome, CPF, nascimento, telefone, email, endereço, histórico de saúde, alergias, medicamentos (CLINIC-03)"
    - "Campos medical_history/allergies/medications são criptografados AES-256 antes do INSERT e descriptografados na exibição (D-07)"
    - "Recepcionista edita ficha de paciente existente (CLINIC-04)"
    - "Excluir paciente anonimiza PII (nome/CPF/telefone/email) e seta deleted_at + is_anonymized, preservando prontuários (SEC-04, D-08)"
    - "Usuário visualiza agenda semanal por dentista selecionado em dropdown (CLINIC-01, D-01)"
    - "Criar/editar/cancelar agendamento; double-booking é rejeitado com mensagem amigável ao capturar 23P01 (CLINIC-02, D-03)"
    - "Rotas /clinica/pacientes e /clinica/agenda acessíveis por admin/dentist/receptionist via proxy.ts"
  artifacts:
    - path: "src/actions/patients.ts"
      provides: "createPatient, updatePatient, anonymizePatient Server Actions com encrypt/decrypt e logBusinessEvent"
      contains: "encrypt("
    - path: "src/actions/appointments.ts"
      provides: "createAppointment, updateAppointment, cancelAppointment com captura de 23P01"
      contains: "23P01"
    - path: "src/components/agenda/AgendaCalendar.tsx"
      provides: "FullCalendar timeGridWeek com dropdown de dentista e filtro por dentistId"
      contains: "timeGridWeek"
    - path: "src/components/patients/PatientForm.tsx"
      provides: "Formulário RHF + Zod de cadastro/edição de paciente"
      contains: "useForm"
  key_links:
    - from: "src/actions/patients.ts"
      to: "src/lib/crypto.ts"
      via: "encrypt() nos campos sensíveis antes do insert"
      pattern: "encrypt\\("
    - from: "src/actions/appointments.ts"
      to: "PostgreSQL 23P01 exclusion_violation"
      via: "captura de erro de constraint GIST"
      pattern: "23P01"
    - from: "src/components/agenda/AgendaCalendar.tsx"
      to: "appointments por dentista"
      via: "filtro events por dentistId selecionado no dropdown"
      pattern: "dentistId"
---

<objective>
Entregar o CRUD completo de pacientes (CLINIC-03, CLINIC-04, SEC-04) e a agenda semanal por dentista com criação/edição/cancelamento de agendamentos sem conflito (CLINIC-01, CLINIC-02). Inclui: validadores Zod, Server Actions com criptografia seletiva AES-256 e auditoria, formulário e tabela de pacientes (shadcn + RHF + TanStack Table), fluxo de anonimização LGPD, e o componente FullCalendar `timeGridWeek` com dropdown de dentista e tratamento do erro de double-booking (23P01).

Purpose: Estes dois módulos formam o núcleo operacional da recepção — sem eles o dentista não tem pacientes nem agenda para atender. Vertical slice independente do prontuário/odontograma (Plan 03), permitindo execução paralela na Wave 2.

Output: Páginas `/clinica/pacientes` (lista, novo, ficha) e `/clinica/agenda`, Server Actions de pacientes e agendamentos, componentes client, rotas adicionadas ao proxy.ts, e testes Vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-clinical-mvp/02-CONTEXT.md
@.planning/phases/02-clinical-mvp/02-RESEARCH.md
@.planning/phases/02-clinical-mvp/02-UI-SPEC.md
@.planning/phases/02-clinical-mvp/02-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Padrões e assinaturas existentes que o executor DEVE replicar. -->

src/lib/crypto.ts (server-only):
  encrypt(plaintext: string): string         // formato iv:authTag:ciphertext; lança se chave ausente
  decrypt(ciphertext: string): string        // GUARD obrigatório: nunca chamar com null/'' (Pitfall 2)
  encryptJSON(data) / decryptJSON<T>()

src/lib/audit.ts (server-only):
  logBusinessEvent({ tenantId: string, actorId: string, action: string, details: Record<string,unknown> }): Promise<void>
  // REGRA: details recebe apenas IDs — NUNCA CPF ou dados de saúde.

src/lib/supabase/server.ts: createClient() — RLS via JWT do usuário (use para reads/writes do usuário)
src/lib/supabase/admin.ts: createAdminClient() — bypass RLS (server-only; use só quando necessário)

Padrão de Server Action (de src/actions/invitations.ts):
  'use server'; getUser() → buscar actor em public.users (id, tenant_id, role) →
  validar role → operar → logBusinessEvent → retornar { success, error? }.
  Rollback compensatório em falhas parciais.

proxy.ts ROLE_ROUTES já permite '/clinica' para admin/dentist/receptionist/superadmin.
  '/clinica/pacientes' e '/clinica/agenda' já caem sob o prefixo '/clinica' — NÃO precisam de nova entrada,
  mas confirme que estão acessíveis. Headers x-user-id e x-user-role são injetados pelo proxy.

DB (de Plan 01): public.patients, public.appointments — ver schema em 02-RESEARCH.md.
  appointments.status: 'agendado'|'confirmado'|'em_atendimento'|'concluido'|'cancelado'
  appointments.source: 'interno'|'publico' (default 'interno')
  Constraint no_overlap dispara PostgreSQL error code '23P01' em conflito.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0) + Validadores: testes RED + schemas Zod</name>
  <read_first>
    - .planning/phases/02-clinical-mvp/02-VALIDATION.md
    - src/lib/validators/invitation.ts
    - src/lib/crypto.ts
    - src/__tests__/auth/invitations.test.ts
  </read_first>
  <behavior>
    - encrypt/decrypt roundtrip: decrypt(encrypt('histórico')) === 'histórico'
    - anonymizePatient produz: full_name='Paciente Excluído', cpf='000.000.000-00', phone='(00) 00000-0000', email='anonimizado@excluido.local', is_anonymized=true, deleted_at setado; campos de saúde → null
    - patientSchema rejeita CPF com formato inválido e nome vazio; aceita campos de saúde opcionais
    - calendar event mapping: dado um appointment, gera um event com id, start, end, dentistId, status; filtro por dentistId retorna apenas eventos daquele dentista
  </behavior>
  <files>src/lib/validators/patient.ts, src/lib/validators/appointment.ts, src/__tests__/actions/patients.test.ts, src/__tests__/agenda/calendar.test.ts</files>
  <action>
    Instalar dependências da fase (RESEARCH instalação):
    `npm install @fullcalendar/react @fullcalendar/daygrid @fullcalendar/timegrid @fullcalendar/interaction date-fns date-fns-tz nuqs @tanstack/react-query @tanstack/react-table zustand`

    Criar `src/lib/validators/patient.ts` — Zod v3 (NUNCA v4). Campos: full_name (min 2), cpf (regex `^\d{3}\.\d{3}\.\d{3}-\d{2}$`), date_of_birth (opcional ISO date string), phone/email/address opcionais, medical_history/allergies/medications opcionais (string). Exportar `patientSchema` e `type PatientInput`.

    Criar `src/lib/validators/appointment.ts` — Zod v3. Campos: dentist_id (uuid), patient_id (uuid opcional), start_time (datetime ISO), end_time (datetime ISO), status (enum dos 5 valores, default 'agendado'), notes (opcional). Refine: end_time > start_time. Exportar `appointmentSchema`, `type AppointmentInput`, e uma função pura `mapAppointmentToEvent(a)` que retorna `{ id, start, end, dentistId, status, title }` e `filterEventsByDentist(events, dentistId)`.

    Criar `src/__tests__/actions/patients.test.ts` — testes RED:
    - encrypt/decrypt roundtrip importando de '@/lib/crypto' (setar `process.env.ENCRYPTION_KEY` a um hex de 64 chars no topo do teste via `beforeAll`)
    - uma função pura `buildAnonymizedPatch()` exportada de '@/actions/patients' que retorna o objeto de anonimização do D-08 — assertion sobre os valores exatos
    - patientSchema.safeParse rejeita CPF inválido

    Criar `src/__tests__/agenda/calendar.test.ts` — testes RED:
    - `mapAppointmentToEvent` mapeia campos corretamente
    - `filterEventsByDentist` retorna apenas eventos do dentistId dado e exclui os de outros dentistas (cobre Pitfall 3 a nível de lógica)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/actions/patients.test.ts src/__tests__/agenda/calendar.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/validators/patient.ts` exporta `patientSchema` e usa `import { z } from 'zod'`
    - `src/lib/validators/appointment.ts` exporta `mapAppointmentToEvent` e `filterEventsByDentist`
    - Os dois arquivos de teste existem; rodam e falham RED nas partes que dependem de patients.ts (buildAnonymizedPatch ainda não existe)
    - As assertions de Zod e de mapeamento de evento passam GREEN (validadores já implementados nesta task)
  </acceptance_criteria>
  <done>Validadores Zod e funções puras de calendário criados; testes de calendário GREEN; testes de patients RED até Task 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Server Actions de pacientes (criar, editar, anonimizar)</name>
  <read_first>
    - src/actions/invitations.ts
    - src/lib/crypto.ts
    - src/lib/audit.ts
    - src/lib/validators/patient.ts
    - .planning/phases/02-clinical-mvp/02-RESEARCH.md (Padrão 3; Pitfall 2; Fluxo LGPD para Exclusão)
  </read_first>
  <behavior>
    - createPatient: criptografa medical_history/allergies/medications (somente se não-vazios), insere com tenant_id do actor, audita 'patient.created' com apenas patient_id
    - updatePatient: re-criptografa campos de saúde alterados; audita 'patient.updated'
    - anonymizePatient: somente admin; aplica buildAnonymizedPatch(); NÃO deleta medical_records/dental_records/anamneses; audita 'patient.anonymized'
    - getPatientDecrypted: descriptografa campos de saúde com guard (decrypt só se valor presente)
  </behavior>
  <files>src/actions/patients.ts</files>
  <action>
    Criar `src/actions/patients.ts` com `'use server'`. Replicar o padrão de `src/actions/invitations.ts`:
    1. Exportar função pura `buildAnonymizedPatch()` (sem 'use server' restriction — é determinística) retornando EXATAMENTE:
       ```typescript
       export function buildAnonymizedPatch() {
         return {
           full_name: 'Paciente Excluído',
           cpf: '000.000.000-00',
           phone: '(00) 00000-0000',
           email: 'anonimizado@excluido.local',
           address: null,
           medical_history: null,
           allergies: null,
           medications: null,
           is_anonymized: true,
           deleted_at: new Date().toISOString(),
         }
       }
       ```
    2. `createPatient(input: PatientInput)`: parse com patientSchema; getUser() → buscar actor (id, tenant_id, role); validar role IN admin/dentist/receptionist/superadmin; criptografar campos de saúde via `encrypt()` SOMENTE se string não-vazia (senão null); insert em `patients` com tenant_id do actor e registered_by=actor.id; em sucesso `logBusinessEvent({ action: 'patient.created', details: { patient_id } })` (NUNCA logar CPF/saúde); retornar `{ success, id?, error? }`. Capturar violação de índice único de CPF (code '23505') e retornar erro "CPF já cadastrado nesta clínica".
    3. `updatePatient(id, input)`: mesma validação; re-criptografar campos de saúde; update scoped por tenant_id; audita 'patient.updated'.
    4. `anonymizePatient(id)`: validar role === 'admin' (ou superadmin); aplicar `buildAnonymizedPatch()` via update scoped por tenant_id; NÃO tocar em medical_records/dental_records/anamneses; `logBusinessEvent({ action: 'patient.anonymized', details: { patient_id: id } })`.
    5. `getPatientDecrypted(id)`: ler paciente scoped por tenant; descriptografar cada campo de saúde com guard `value ? decrypt(value) : ''` (Pitfall 2).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/actions/patients.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "encrypt(" src/actions/patients.ts` retorna matches (campos de saúde)
    - `grep "buildAnonymizedPatch" src/actions/patients.ts` retorna a definição exportada
    - Contém guard de decrypt: `? decrypt(` ou equivalente para evitar decrypt(null) (Pitfall 2)
    - `logBusinessEvent` chamado com `details` contendo apenas `patient_id` (sem cpf/medical_history)
    - Captura code '23505' para CPF duplicado
    - patients.test.ts passa GREEN (roundtrip + buildAnonymizedPatch + schema)
  </acceptance_criteria>
  <done>Server Actions de pacientes completas; patients.test.ts GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Páginas de paciente (lista, novo, ficha) + PatientForm/PatientTable</name>
  <read_first>
    - .planning/phases/02-clinical-mvp/02-UI-SPEC.md (Patient Form Interactions; Component Inventory; Copywriting; Masking Contract; Destructive Actions)
    - src/proxy.ts
    - src/components/ui/button.tsx
    - src/actions/patients.ts
    - src/lib/validators/patient.ts
  </read_first>
  <files>src/app/(dashboard)/clinica/pacientes/page.tsx, src/app/(dashboard)/clinica/pacientes/novo/page.tsx, src/app/(dashboard)/clinica/pacientes/[id]/page.tsx, src/components/patients/PatientForm.tsx, src/components/patients/PatientTable.tsx, src/proxy.ts</files>
  <action>
    Adicionar componentes shadcn necessários (UI-SPEC Component Inventory): `npx shadcn@latest add input label textarea select badge card table separator alert dialog form skeleton`. Usar `@base-ui/react` apenas para Button (já existe) — NÃO instalar mais @base-ui.

    `src/app/(dashboard)/clinica/pacientes/page.tsx` (Server Component): ler `x-user-id`/`x-user-role` de `headers()`; buscar pacientes do tenant via createClient() (RLS aplica) onde `deleted_at IS NULL`; passar para `<PatientTable />` (client). CTA "Novo Paciente" → /clinica/pacientes/novo. Empty state conforme UI-SPEC ("Nenhum paciente cadastrado").

    `src/components/patients/PatientTable.tsx` ('use client'): TanStack Table v8 envolvendo shadcn Table. Colunas: nome, CPF mascarado conforme Masking Contract (`123.***.***-**` para receptionist/patient), telefone mascarado, ações (ver/editar). Linhas linkam para /clinica/pacientes/[id].

    `src/components/patients/PatientForm.tsx` ('use client'): RHF + zodResolver(patientSchema) + shadcn Form. Campos do D-05. CPF com máscara `000.000.000-00` no blur. date_of_birth via shadcn Calendar em Popover. Em submit chama createPatient ou updatePatient; em erro de CPF duplicado mostra Alert inline "CPF já cadastrado". CTA "Cadastrar Paciente" (criar) / "Salvar Alterações" (editar) conforme Copywriting Contract.

    `src/app/(dashboard)/clinica/pacientes/novo/page.tsx`: renderiza `<PatientForm mode="create" />`.

    `src/app/(dashboard)/clinica/pacientes/[id]/page.tsx` (Server Component): chama getPatientDecrypted(id); header do paciente com CPF mascarado; botão "Excluir paciente" → Dialog de anonimização que exige DIGITAR o nome completo do paciente para habilitar o botão destrutivo "Confirmar anonimização" (UI-SPEC Destructive Actions), chamando anonymizePatient. Inclui também navegação para as abas (Prontuário/Odontograma/Anamneses) — os conteúdos das abas vêm de Plan 03/04; aqui criar apenas a estrutura de Tabs com placeholders que dizem "Disponível após Plano 03/04" SOMENTE para abas ainda não implementadas (não simplificar — é dependência de wave, não redução de escopo).

    `src/proxy.ts`: confirmar que `/clinica/pacientes` e `/clinica/agenda` caem sob ROLE_ROUTES['/clinica']. Como já caem sob o prefixo `/clinica`, NÃO é necessária alteração; se por algum motivo precisar de granularidade, manter como está. (Esta task lista proxy.ts em files_modified por garantia de revisão; se nenhuma mudança for necessária, deixar o arquivo intacto.)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - As 3 páginas e os 2 componentes existem
    - `grep "zodResolver" src/components/patients/PatientForm.tsx` retorna match
    - `grep "123.\\*\\*\\*" src/components/patients/PatientTable.tsx` OU lógica de máscara de CPF presente
    - Dialog de anonimização exige digitar o nome (input controlado comparado ao full_name) antes de habilitar o botão
    - `npx tsc --noEmit` exit 0; suite Vitest existente permanece GREEN
  </acceptance_criteria>
  <done>UI de pacientes completa (lista/novo/ficha) com máscara LGPD e fluxo de anonimização; build de tipos limpo.</done>
</task>

<task type="auto">
  <name>Task 3: Agenda — Server Actions de agendamento + AgendaCalendar (FullCalendar)</name>
  <read_first>
    - .planning/phases/02-clinical-mvp/02-RESEARCH.md (Padrão 4; Pitfall 3; Pitfall 7)
    - .planning/phases/02-clinical-mvp/02-UI-SPEC.md (Agenda Page Layout; FullCalendar Configuration Contract; Agenda Calendar Interactions; Calendar Event Status Colors)
    - src/lib/validators/appointment.ts
    - src/actions/patients.ts
  </read_first>
  <files>src/actions/appointments.ts, src/app/(dashboard)/clinica/agenda/page.tsx, src/components/agenda/AgendaCalendar.tsx</files>
  <action>
    `src/actions/appointments.ts` ('use server'): replicar padrão de Server Action.
    - `createAppointment(input: AppointmentInput)`: parse appointmentSchema; getUser → actor (tenant_id, role); validar role staff; insert em appointments com tenant_id, source='interno'. CRÍTICO: capturar erro de constraint GIST — se `error.code === '23P01'` retornar `{ success: false, error: 'Horário indisponível. Este horário foi reservado enquanto você preenchia. Selecione outro horário.' }` (UI-SPEC Error States). Audita 'appointment.created' (apenas IDs).
    - `updateAppointment(id, input)`: update scoped por tenant; mesma captura de 23P01 (cobre drag-to-reschedule). Audita 'appointment.updated'.
    - `cancelAppointment(id)`: update status='cancelado' scoped por tenant (não deleta). Audita 'appointment.cancelled'.

    `src/app/(dashboard)/clinica/agenda/page.tsx` (Server Component): buscar dentistas do tenant (users WHERE role='dentist') e agendamentos da semana via createClient(); passar para `<AgendaCalendar dentists={...} events={...} />`.

    `src/components/agenda/AgendaCalendar.tsx` ('use client'): FullCalendar conforme FullCalendar Configuration Contract da UI-SPEC:
    - plugins: timeGridPlugin, dayGridPlugin, interactionPlugin
    - initialView 'timeGridWeek', locale 'pt-br', slotMinTime '07:00:00', slotMaxTime '20:00:00', allDaySlot false, slotDuration '00:15:00', snapDuration '00:15:00', nowIndicator true, height 'calc(100vh - 112px)'
    - dropdown de dentista (shadcn Select) com estado via `nuqs` `useQueryState('dentist')` — D-01
    - `events` filtrados por dentistId usando `filterEventsByDentist` do validador
    - eventClassNames por status conforme Calendar Event Status Colors (bg-blue-100 etc.)
    - editable=true; `eventDrop` chama updateAppointment; em erro 23P01 reverte o evento (`info.revert()`) e mostra Alert "Horário indisponível"
    - select (clique em slot vazio) abre Dialog "Novo Agendamento" com start/end pré-preenchidos
    - cada evento tem aria-label com status + paciente + horário (Accessibility Contract)
    - useQuery do TanStack Query com queryKey incluindo tenantId (Pitfall 3) se buscar client-side; se os dados vierem do Server Component como props, garantir que a página re-renderiza por semana.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/agenda/calendar.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "23P01" src/actions/appointments.ts` retorna ao menos 2 matches (create + update)
    - `grep "timeGridWeek" src/components/agenda/AgendaCalendar.tsx` retorna match
    - `grep "useQueryState" src/components/agenda/AgendaCalendar.tsx` retorna match (dropdown de dentista via nuqs)
    - AgendaCalendar usa `filterEventsByDentist` e define eventClassNames por status
    - `info.revert()` presente no handler de eventDrop para reverter conflito
    - calendar.test.ts GREEN; `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Agenda funcional com FullCalendar, dropdown de dentista, drag-and-drop e tratamento de double-booking; testes GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → Server Action | input não-confiável de paciente/agendamento cruza para o servidor |
| Server Action → patients/appointments | escrita com tenant_id derivado do actor autenticado (nunca do cliente) |
| React Query cache → calendário | dados de tenant em cache no client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-02 | Information Disclosure | tenant_id forjado no input do formulário | high | mitigate | tenant_id SEMPRE derivado de actor lido de public.users via getUser(); nunca de FormData/input do cliente |
| T-2-03 | Information Disclosure | cross-tenant leak no cache do React Query | high | mitigate | queryKey inclui tenantId (Pitfall 3); filterEventsByDentist opera só sobre eventos já isolados por RLS |
| T-2-01 | Tampering | double-booking via race condition | high | mitigate | captura de '23P01' em createAppointment/updateAppointment retorna erro amigável; constraint GIST do Plan 01 garante atomicidade |
| T-2-06 | Tampering | hard delete de paciente (LGPD) | high | mitigate | anonymizePatient faz soft delete + anonimização; sem DELETE; prontuários preservados |
| T-2-08 | Information Disclosure | CPF/dados de saúde em logs | medium | mitigate | logBusinessEvent recebe apenas patient_id; campos de saúde criptografados antes do INSERT |
| T-2-10 | Information Disclosure | CPF/telefone visíveis a recepcionista em listagem | medium | mitigate | Masking Contract: CPF `123.***.***-**` e telefone mascarados em PatientTable para receptionist/patient |
</threat_model>

<verification>
- `npx vitest run src/__tests__/` — suite GREEN (incluindo patients.test.ts e calendar.test.ts)
- `npx tsc --noEmit` exit 0
- `next build` sem erros
- Manual (UI-SPEC Manual-Only): abrir /clinica/agenda, selecionar dentista, arrastar evento; criar 2º agendamento no mesmo slot e verificar mensagem de erro
</verification>

<success_criteria>
- Paciente cadastrado/editado com todos os campos do D-05; saúde criptografada AES-256 (CLINIC-03, CLINIC-04)
- Anonimização LGPD preserva prontuários (SEC-04)
- Agenda semanal por dentista via dropdown (CLINIC-01)
- Double-booking rejeitado com mensagem amigável (CLINIC-02)
- CPF/telefone mascarados em listagem (SEC-01 reforçado)
</success_criteria>

<output>
After completion, create `.planning/phases/02-clinical-mvp/02-02-SUMMARY.md`
</output>
