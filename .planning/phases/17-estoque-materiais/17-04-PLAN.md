---
phase: 17-estoque-materiais
plan: 04
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - src/lib/agents/stock-agent.ts
  - src/actions/stock-alerts.ts
  - src/app/api/cron/estoque-validade/route.ts
  - vercel.json
autonomous: true
requirements:
  - EST-03

must_haves:
  truths:
    - "Ao detectar saldo <= estoque_minimo, o agente L2 cria rascunho de payable (origem='estoque_agente') + approval_request quando há preferred_supplier_id e estoque_maximo"
    - "Sem preferred_supplier_id ou sem estoque_maximo, o agente apenas insere stock_alert tipo='minimo' (não cria CP)"
    - "withAgentPolicy é chamado com clinicId real (per-tenant), nunca null"
    - "Cron semanal /api/cron/estoque-validade insere alertas tipo='validade' para lotes com validade <= hoje+30d, idempotente por dia"
    - "Cron usa runtime nodejs, isCronAuthorized e createAdminClient"
  artifacts:
    - path: "src/lib/agents/stock-agent.ts"
      provides: "Agente de compras L2 (withAgentPolicy) + insertStockAlert helper"
      exports: ["runStockReplenishmentAgent", "insertStockAlert"]
    - path: "src/actions/stock-alerts.ts"
      provides: "Leitura de alertas ativos para dashboard/banner"
      exports: ["listActiveAlerts", "getAlertCounts"]
    - path: "src/app/api/cron/estoque-validade/route.ts"
      provides: "Cron semanal de validade"
      contains: "isCronAuthorized"
  key_links:
    - from: "src/lib/agents/stock-agent.ts"
      to: "src/lib/ai/policy.ts"
      via: "import { withAgentPolicy }"
      pattern: "withAgentPolicy"
    - from: "src/lib/agents/stock-agent.ts"
      to: "payables + approval_requests tables"
      via: "insert origem='estoque_agente' + approval_request"
      pattern: "estoque_agente|stock_replenishment|approval_requests"
    - from: "src/app/api/cron/estoque-validade/route.ts"
      to: "product_batches table"
      via: "createAdminClient scan validade <= hoje+30d"
      pattern: "data_validade"
---

<objective>
Implementar o agente de compras L2 (withAgentPolicy → rascunho de CP + approval_request), o helper de inserção de alertas, as Server Actions de leitura de alertas para a UI, e o cron semanal de validade.

Purpose: entregar o backend de alertas + agente do EST-03 e tornar GREEN os tests RED de stock-agent e cron-validade plantados no Plan 01.

Output: src/lib/agents/stock-agent.ts + src/actions/stock-alerts.ts + src/app/api/cron/estoque-validade/route.ts + vercel.json atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-estoque-materiais/17-RESEARCH.md
@src/lib/agents/collection-agent.ts
@src/lib/ai/policy.ts
@src/app/api/cron/recorrente/route.ts
@vercel.json
</context>

<interfaces>
<!-- withAgentPolicy(ctx, execute) — retorna resultado de execute OU { _policy, reason }. -->
<!-- ctx: { clinicId, agentKey, actorId, action, actionSensitivity }. clinicId DEVE ser UUID real. -->
<!-- approval_requests.requested_by agora é NULLABLE (Plan 02) → passar null para ator de sistema. -->

Assinaturas a criar:
```typescript
// src/lib/agents/stock-agent.ts (import 'server-only')
export async function runStockReplenishmentAgent(params: {
  clinicId: string; unitId: string; productId: string; productName: string;
  saldoAtual: number; estoqueMinimo: number; estoqueMaximo: number | null;
  preferredSupplierId: string | null;
}): Promise<void>
export async function insertStockAlert(
  clinicId: string, unitId: string, productId: string,
  tipo: 'minimo' | 'validade', batchId?: string | null,
): Promise<void>

// src/actions/stock-alerts.ts ('use server')
export async function listActiveAlerts(unitId?: string): Promise<{ success: boolean; error?: string; data?: AlertRow[] }>
export async function getAlertCounts(unitId?: string): Promise<{ minimo: number; validade: number; negativo: number }>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Agente de compras L2 + helper de alertas</name>
  <files>src/lib/agents/stock-agent.ts</files>
  <read_first>
    - src/lib/agents/collection-agent.ts (padrão withAgentPolicy PER-ROW, createAdminClient, logBusinessEvent, import 'server-only')
    - src/lib/ai/policy.ts (assinatura de withAgentPolicy + retorno { _policy, reason })
    - .planning/phases/17-estoque-materiais/17-RESEARCH.md §"Pattern 4: Agente de Compras L2" (código verbatim) + §"Pitfall 3" (clinicId per-row)
    - src/__tests__/estoque/stock-agent.test.ts (strings: 'withAgentPolicy', 'stock_replenishment', 'estoque_agente', 'approval_requests')
    - supabase/migrations/20260621000100_payables_tables.sql (colunas de payables para o insert)
  </read_first>
  <action>
Criar src/lib/agents/stock-agent.ts com import 'server-only'. Seguir §Pattern 4 de 17-RESEARCH.md verbatim, com ajustes:
- `agentKey: 'stock_replenishment'`, `action: 'agent.stock.create_draft_cp'`, `actionSensitivity: 'reversible'`, `actorId: null`.
- clinicId SEMPRE o params.clinicId real (Pitfall 3 — nunca null/agregado).
- Dentro do execute: se `!preferredSupplierId || !estoqueMaximo` → `await insertStockAlert(clinicId, unitId, productId, 'minimo')` e retorna { _created: false } (D-15).
- Senão: `qtdReposicao = estoqueMaximo - saldoAtual`; insert em payables (createAdminClient) { clinic_id, unit_id, supplier_id: preferredSupplierId, descricao: `Reposição automática: ${productName} (${qtdReposicao} un)`, valor_total: 0, origem: 'estoque_agente', status: 'pendente' } → select id.
- insert em approval_requests { clinic_id, type: 'ai_action', payload: { payable_id, product_id: productId }, agent_key: 'stock_replenishment', required_role: 'admin', requested_by: null, status: 'pending' } (requested_by null = sistema, agora permitido pelo Plan 02).
- Também inserir stock_alert tipo='minimo' para aparecer no banner.
- logBusinessEvent action 'agent.stock.draft_cp_created'.

insertStockAlert: createAdminClient insert em stock_alerts { clinic_id, unit_id, product_id, batch_id: batchId ?? null, tipo }. Usar upsert/onConflict do índice uq_stock_alerts_daily (ignoreDuplicates) OU try/catch do erro 23505 (idempotência diária). NÃO propagar erro que quebre a baixa.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/estoque/stock-agent.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep "withAgentPolicy" src/lib/agents/stock-agent.ts` presente
    - `grep "stock_replenishment" src/lib/agents/stock-agent.ts` presente (agentKey)
    - `grep "estoque_agente" src/lib/agents/stock-agent.ts` presente (origem do payable)
    - `grep "approval_requests" src/lib/agents/stock-agent.ts` presente
    - `grep "requested_by: null\|requested_by:null" src/lib/agents/stock-agent.ts` (ator de sistema)
    - `npx vitest run src/__tests__/estoque/stock-agent.test.ts` GREEN (era RED no Plan 01)
  </acceptance_criteria>
  <done>Agente L2 cria CP+approval quando aplicável, senão só alerta; stock-agent.test.ts GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Server Actions de leitura de alertas</name>
  <files>src/actions/stock-alerts.ts</files>
  <read_first>
    - src/actions/suppliers.ts (getActor pattern)
    - src/types/database.types.ts (Row de stock_alerts, product_batches)
  </read_first>
  <action>
Criar src/actions/stock-alerts.ts com 'use server' + getActor.
- listActiveAlerts(unitId?): SELECT stock_alerts JOIN products(name) WHERE resolvido=false [AND unit_id=unitId] ORDER BY created_at DESC. Retorna id, tipo, product_id, product_name, unit_id, batch_id, created_at.
- getAlertCounts(unitId?): retorna { minimo, validade, negativo }. minimo = count stock_alerts tipo='minimo' resolvido=false; validade = count tipo='validade' resolvido=false; negativo = count de produtos com saldo < 0 (SUM product_batches.saldo_disponivel por product+unit < 0). Filtrar por unitId quando fornecido.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "stock-alerts.ts" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use server'" src/actions/stock-alerts.ts` presente
    - `grep "listActiveAlerts\|getAlertCounts" src/actions/stock-alerts.ts` — ambas exportadas
    - `grep "resolvido" src/actions/stock-alerts.ts` — filtra alertas não resolvidos
    - `npx tsc --noEmit` sem erro novo em stock-alerts.ts
  </acceptance_criteria>
  <done>listActiveAlerts + getAlertCounts prontos para o dashboard/banner.</done>
</task>

<task type="auto">
  <name>Task 3: Cron semanal de validade + registro no vercel.json</name>
  <files>src/app/api/cron/estoque-validade/route.ts, vercel.json</files>
  <read_first>
    - src/app/api/cron/recorrente/route.ts (padrão verbatim: runtime nodejs, maxDuration, isCronAuthorized, createAdminClient, loop por clínica)
    - src/lib/agents/stock-agent.ts (insertStockAlert — reutilizar)
    - src/__tests__/estoque/cron-validade.test.ts (strings: 'isCronAuthorized', "runtime = 'nodejs'", 'createAdminClient', 'data_validade')
    - vercel.json (array de crons existente)
  </read_first>
  <action>
Criar src/app/api/cron/estoque-validade/route.ts seguindo §Pattern 5 de 17-RESEARCH.md:
- `export const runtime = 'nodejs'` e `export const maxDuration = 60` no topo.
- GET(request): `if (!isCronAuthorized(request.headers.get('authorization'))) return new Response('Unauthorized', { status: 401 })`.
- `const admin = createAdminClient()`.
- Calcular thresholdISO = hoje + 30 dias (YYYY-MM-DD).
- SELECT product_batches (id, product_id, clinic_id, unit_id, data_validade, saldo_disponivel) WHERE data_validade <= thresholdISO AND data_validade IS NOT NULL AND saldo_disponivel > 0 AND deleted_at IS NULL.
- Para cada lote: `await insertStockAlert(batch.clinic_id, batch.unit_id, batch.product_id, 'validade', batch.id)` (idempotente por dia via uq_stock_alerts_daily).
- Retornar Response.json({ ok: true, alertas_criados: count }).
- NÃO dispara agente de compras (D-16: vencimento = descarte, não reposição).

Atualizar vercel.json: adicionar `{ "path": "/api/cron/estoque-validade", "schedule": "0 5 * * 1" }` (segunda-feira 05:00) ao array crons.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/estoque/cron-validade.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep "runtime = 'nodejs'" src/app/api/cron/estoque-validade/route.ts` presente
    - `grep "isCronAuthorized" src/app/api/cron/estoque-validade/route.ts` presente
    - `grep "createAdminClient" src/app/api/cron/estoque-validade/route.ts` presente
    - `grep "data_validade" src/app/api/cron/estoque-validade/route.ts` presente
    - `grep "estoque-validade" vercel.json` presente no array crons
    - `npx vitest run src/__tests__/estoque/cron-validade.test.ts` GREEN (era RED no Plan 01)
  </acceptance_criteria>
  <done>Cron de validade insere alertas idempotentes; registrado no vercel.json; cron-validade.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron → estoque-validade | Authorization Bearer CRON_SECRET; fail-closed |
| agente (service role) → payables/approval_requests | Cria rascunho, nunca efetiva sem aprovação humana |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Elevation of Privilege | Agente cria CP sem aprovação | mitigate | withAgentPolicy L2 → approval_request obrigatório antes de efetivar |
| T-17-10 | Spoofing | Cron chamado sem autorização | mitigate | isCronAuthorized (constant-time, fail-closed) antes de qualquer query |
| T-17-11 | Tampering | ai_decision_log clinic_id null | mitigate | withAgentPolicy per-row com clinicId real (Pitfall 3) |
</threat_model>

<verification>
`npx vitest run src/__tests__/estoque/stock-agent.test.ts src/__tests__/estoque/cron-validade.test.ts` GREEN. `npx tsc --noEmit` limpo para os novos arquivos.
</verification>

<success_criteria>
- Agente L2 cria payable estoque_agente + approval_request quando há fornecedor+máximo; senão só alerta
- listActiveAlerts/getAlertCounts prontos para UI
- Cron de validade nodejs + isCronAuthorized + createAdminClient, idempotente, registrado no vercel.json
- stock-agent.test.ts e cron-validade.test.ts (RED do Plan 01) agora GREEN
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-estoque-materiais/17-04-SUMMARY.md`
</output>
