---
phase: 17-estoque-materiais
plan: 06
type: execute
wave: 4
depends_on: [17-03, 17-04, 17-05]
files_modified:
  - src/components/shell/nav-config.ts
  - src/components/shell/nav-icons.ts
  - src/app/(dashboard)/clinica/estoque/page.tsx
  - src/app/(dashboard)/clinica/estoque/produtos/page.tsx
  - src/components/estoque/StockAlertBanner.tsx
  - src/components/estoque/ProductsTable.tsx
  - src/components/estoque/ProductFormDialog.tsx
autonomous: true
requirements:
  - EST-01
  - EST-03

must_haves:
  truths:
    - "Link 'Estoque' aparece no sidebar apontando para /clinica/estoque"
    - "Dashboard /clinica/estoque mostra banner de alertas ativos + 3 cards de KPI + movimentações recentes"
    - "Catálogo /clinica/estoque/produtos lista produtos com saldo, custo médio e status semântico"
    - "Admin cadastra/edita produto via ProductFormDialog com campos condicionais por categoria (ANVISA para implante)"
    - "Filtros de categoria/status/busca persistem na URL (nuqs)"
  artifacts:
    - path: "src/app/(dashboard)/clinica/estoque/page.tsx"
      provides: "Dashboard de alertas (RSC)"
      contains: "getAlertCounts"
    - path: "src/app/(dashboard)/clinica/estoque/produtos/page.tsx"
      provides: "Catálogo de produtos (RSC)"
      contains: "listProducts"
    - path: "src/components/estoque/ProductFormDialog.tsx"
      provides: "Dialog de cadastro/edição com campos condicionais"
      contains: "productSchema"
  key_links:
    - from: "src/components/shell/nav-config.ts"
      to: "/clinica/estoque"
      via: "ALL_NAV_ITEMS + NavIconKey 'estoque'"
      pattern: "estoque"
    - from: "src/app/(dashboard)/clinica/estoque/produtos/page.tsx"
      to: "src/actions/products.ts"
      via: "listProducts"
      pattern: "listProducts"
---

<objective>
Adicionar o item de navegação 'Estoque' ao sidebar e construir o dashboard de alertas e o catálogo de produtos (com dialog de cadastro/edição), conforme o UI-SPEC da fase.

Purpose: entregar a UI de entrada do módulo (EST-01 catálogo + EST-03 alertas visíveis).

Output: nav atualizado + /clinica/estoque + /clinica/estoque/produtos + 3 componentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-estoque-materiais/17-UI-SPEC.md
@src/components/shell/nav-config.ts
@src/components/shell/nav-icons.ts
@src/lib/validators/product.ts
@src/actions/products.ts
@src/actions/stock-alerts.ts
</context>

<interfaces>
<!-- Consumir: listProducts, createProduct, updateProduct, deactivateProduct (Plan 03); -->
<!-- listActiveAlerts, getAlertCounts (Plan 04); listStockDraws/listStockEntries p/ movimentações (Plans 03/05). -->
<!-- Padrão UI: @base-ui Button usa render-prop (NÃO asChild); DropdownMenuTrigger com pointer-events-none no ícone. -->
<!-- Forms: RHF v7 + zodResolver(productSchema); campos condicionais via watch('category'); sem .default() no Zod. -->
<!-- Filtros: nuqs useQueryState('q'|'categoria'|'status'). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Item de navegação 'Estoque' no sidebar</name>
  <files>src/components/shell/nav-config.ts, src/components/shell/nav-icons.ts</files>
  <read_first>
    - src/components/shell/nav-config.ts (NavIconKey union + ALL_NAV_ITEMS)
    - src/components/shell/nav-icons.ts (mapa NavIconKey → componente Lucide)
    - .planning/phases/17-estoque-materiais/17-RESEARCH.md §"Pitfall 7" (adicionar ao union + import Lucide)
  </read_first>
  <action>
Em nav-config.ts: adicionar `| 'estoque'` ao union NavIconKey. Adicionar em ALL_NAV_ITEMS: `{ href: '/clinica/estoque', label: 'Estoque', icon: 'estoque' }` (visível a todos os papéis — leitura permitida a dentista/recep D-18; escrita gated na UI/action).
Em nav-icons.ts: importar `Package` de 'lucide-react' e mapear `estoque: Package` no objeto NAV_ICONS.
  </action>
  <verify>
    <automated>grep -n "estoque" src/components/shell/nav-config.ts src/components/shell/nav-icons.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'estoque'" src/components/shell/nav-config.ts` presente no union e em ALL_NAV_ITEMS
    - `grep "Package" src/components/shell/nav-icons.ts` importado e mapeado para 'estoque'
    - `npx tsc --noEmit` sem erro em nav-config.ts/nav-icons.ts
  </acceptance_criteria>
  <done>Link 'Estoque' com ícone Package no sidebar.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard de alertas /clinica/estoque</name>
  <files>src/app/(dashboard)/clinica/estoque/page.tsx, src/components/estoque/StockAlertBanner.tsx</files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-UI-SPEC.md §"1. /clinica/estoque — Dashboard de Alertas" + §Copywriting Contract
    - src/actions/stock-alerts.ts (listActiveAlerts, getAlertCounts)
    - src/actions/stock-draws.ts / src/actions/stock-entries.ts (listStockDraws/listStockEntries para movimentações recentes)
    - Uma page RSC existente com PageHeader (ex.: src/app/(dashboard)/clinica/financeiro/*/page.tsx) para o padrão de layout
  </read_first>
  <action>
Criar src/app/(dashboard)/clinica/estoque/page.tsx (RSC). Layout: `PageHeader` + `<main className="p-6 max-w-5xl mx-auto w-full space-y-6">`.
- Chamar getAlertCounts() e listActiveAlerts() server-side.
- Renderizar <StockAlertBanner alerts={...} /> (client) apenas quando há alertas.
- 3 cards KPI em `grid grid-cols-1 gap-4 sm:grid-cols-3`: "Alertas de Mínimo" (text-amber-600), "Próximos do Vencimento" (text-orange-600), "Saldo Negativo" (text-red-600). Estrutura CardHeader>CardTitle(text-sm font-semibold text-muted-foreground) + CardContent>p(text-2xl font-semibold leading-tight tabular-nums).
- Seção "Movimentações Recentes": h2 font-display, Table shadcn com últimas 10 (entradas+baixas combinadas, ordenadas created_at desc), ícone ArrowDownCircle(entrada)/ArrowUpCircle(baixa), produto, qtd, data dd/MM/yyyy.
- Empty state (sem alertas): "Estoque sob controle" / "Nenhum alerta ativo no momento."

Criar src/components/estoque/StockAlertBanner.tsx ('use client'): usa Alert shadcn; variante destructive para negativo, padrão para validade/minimo; ícone AlertTriangle (crítico/negativo) / Clock (validade). Copy exata do §Copywriting Contract (banner mínimo/validade/negativo com contagem X).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "estoque/page.tsx|StockAlertBanner" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "getAlertCounts" "src/app/(dashboard)/clinica/estoque/page.tsx"` presente
    - `grep "grid-cols-1 gap-4 sm:grid-cols-3" "src/app/(dashboard)/clinica/estoque/page.tsx"` — 3 KPI cards
    - `grep "'use client'" src/components/estoque/StockAlertBanner.tsx` presente
    - `grep "AlertTriangle\|Clock" src/components/estoque/StockAlertBanner.tsx` presente
    - `npx tsc --noEmit` sem erro nos novos arquivos
  </acceptance_criteria>
  <done>Dashboard com banner + KPIs + movimentações recentes conforme UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Catálogo de produtos + ProductFormDialog</name>
  <files>src/app/(dashboard)/clinica/estoque/produtos/page.tsx, src/components/estoque/ProductsTable.tsx, src/components/estoque/ProductFormDialog.tsx</files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-UI-SPEC.md §"2. /clinica/estoque/produtos" (colunas, filtros, dialog, campos condicionais, cores de status)
    - src/actions/products.ts (listProducts, createProduct, updateProduct, deactivateProduct)
    - src/actions/suppliers.ts (para popular Select de fornecedor preferido — usar listSuppliers se existir)
    - src/lib/validators/product.ts (productSchema, PRODUCT_CATEGORIES, UNIDADES_MEDIDA)
    - src/components/financeiro/PayablesTable.tsx (padrão DropdownMenuTrigger + pointer-events-none, badge de status)
  </read_first>
  <action>
Criar produtos/page.tsx (RSC): PageHeader com ação primária (só admin/superadmin) "Cadastrar Produto" que abre ProductFormDialog. Chamar listProducts() server-side e passar para ProductsTable. Derivar role via supabase.auth.getUser()+users.role (padrão da prontuario page).

Criar ProductsTable.tsx ('use client'): tabela shadcn com colunas Produto(nome+sku), Categoria(Badge outline), Saldo Atual(vermelho se negativo), Custo Médio(formatBRL tabular-nums), Est. Mínimo, Status(Badge semântico com classes do UI-SPEC §Color), Ações(DropdownMenu: Editar, Registrar Entrada, Baixa Manual, Histórico — com pointer-events-none no ícone). Filtros nuqs: categoria, status, q (debounce 300ms). Empty state: ícone Package, "Nenhum produto cadastrado" + CTA (admin). Badges de status usar classes exatas: baixo=amber, critico=orange, negativo=red, vencido=muted, normal=secondary.

Criar ProductFormDialog.tsx ('use client'): Dialog shadcn + RHF + zodResolver(productSchema), defaultValues (sem .default no Zod). Campos: Nome, SKU(opcional), Categoria(Select), Unidade Medida(Select un/ml/g/cx/fr), Estoque Mínimo(number), Estoque Máximo(number opcional), Fornecedor Preferido(Select suppliers). Campo condicional: quando `watch('category')==='implante'` exibir "Número ANVISA" (Input obrigatório). Submit chama createProduct/updateProduct; botão "Salvar Produto"/"Atualizar Produto" com Loader2 durante mutação. Erros inline text-sm text-destructive. Mensagem ANVISA: "Número ANVISA é obrigatório para implantes."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "produtos/page.tsx|ProductsTable|ProductFormDialog" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "listProducts" "src/app/(dashboard)/clinica/estoque/produtos/page.tsx"` presente
    - `grep "useQueryState" src/components/estoque/ProductsTable.tsx` — filtros nuqs
    - `grep "pointer-events-none" src/components/estoque/ProductsTable.tsx` no ícone do dropdown
    - `grep "productSchema" src/components/estoque/ProductFormDialog.tsx` no zodResolver
    - `grep "watch('category')\|watch(\"category\")" src/components/estoque/ProductFormDialog.tsx` — condicional ANVISA
    - `npx tsc --noEmit` sem erro nos novos arquivos
  </acceptance_criteria>
  <done>Catálogo filtral + dialog com campos condicionais funcionando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI form → createProduct action | Validação client (RHF) é UX; validação real é no Server Action (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-15 | Elevation of Privilege | Botão de escrita visível a dentista/recep | mitigate | CTA condicionado a role admin/superadmin na UI; gate real no Server Action |
| T-17-16 | Information Disclosure | Saldo de outra unidade | accept | listProducts filtra por unit do contexto; RLS por clinic_id |
</threat_model>

<verification>
`npx tsc --noEmit` limpo. Item 'Estoque' no sidebar. Dashboard e catálogo renderizam (checagem visual em UAT).
</verification>

<success_criteria>
- Link Estoque no sidebar
- Dashboard: banner + 3 KPIs + movimentações
- Catálogo: tabela filtrável + status semântico + dialog condicional (ANVISA para implante)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-estoque-materiais/17-06-SUMMARY.md`
</output>
