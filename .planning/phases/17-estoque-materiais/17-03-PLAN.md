---
phase: 17-estoque-materiais
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - src/lib/stock/custo-medio.ts
  - src/actions/products.ts
  - src/actions/product-batches.ts
  - src/actions/stock-entries.ts
autonomous: true
requirements:
  - EST-01

must_haves:
  truths:
    - "Admin cadastra produto com categoria, unidade de medida, estoque mínimo/máximo e fornecedor preferido"
    - "Entrada de estoque cria um product_batch e recalcula o custo médio móvel via fórmula D-02"
    - "Custo médio no primeiro lote (saldo anterior 0) é o próprio custo unitário (guard divisão por zero)"
    - "Entrada de implante exige numero_anvisa_lote e data_validade (validado por stockEntrySchema)"
    - "Saldo por unidade = SUM(product_batches.saldo_disponivel) filtrado por product_id + unit_id"
  artifacts:
    - path: "src/lib/stock/custo-medio.ts"
      provides: "Função pura calcularCustoMedioMovel (testável sem DB)"
      exports: ["calcularCustoMedioMovel"]
    - path: "src/actions/products.ts"
      provides: "CRUD de produtos (EST-01)"
      exports: ["createProduct", "updateProduct", "listProducts", "deactivateProduct"]
    - path: "src/actions/stock-entries.ts"
      provides: "Registro de entrada + custo médio móvel + criação de lote"
      exports: ["createStockEntry", "listStockEntries"]
    - path: "src/actions/product-batches.ts"
      provides: "Leitura de lotes por produto/unidade"
      exports: ["listProductBatches"]
  key_links:
    - from: "src/actions/stock-entries.ts"
      to: "src/lib/stock/custo-medio.ts"
      via: "import { calcularCustoMedioMovel }"
      pattern: "calcularCustoMedioMovel"
    - from: "src/actions/products.ts"
      to: "src/lib/validators/product.ts"
      via: "import { productSchema }"
      pattern: "productSchema"
    - from: "src/actions/stock-entries.ts"
      to: "product_batches table"
      via: "insert com saldo_disponivel + custo_unitario"
      pattern: "custo_medio_movel|custo_medio_apos"
---

<objective>
Implementar a lib pura de custo médio móvel e as Server Actions de cadastro de produtos, leitura de lotes e registro de entradas de estoque (com recálculo de custo médio e criação de lote FIFO).

Purpose: entregar o backend do EST-01 (cadastro + custo médio) e tornar GREEN os tests RED de stock-entries plantados no Plan 01.

Output: src/lib/stock/custo-medio.ts + src/actions/products.ts + src/actions/product-batches.ts + src/actions/stock-entries.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-estoque-materiais/17-CONTEXT.md
@.planning/phases/17-estoque-materiais/17-RESEARCH.md
@src/lib/validators/product.ts
@src/actions/suppliers.ts
</context>

<interfaces>
<!-- Padrão de Server Action do projeto (verbatim de suppliers.ts): -->
<!-- getActor() lê users(id, tenant_id, role); WRITER_ROLES = ['admin','superadmin']. -->
<!-- Retorno: { success: boolean; error?: string; data?: T }. revalidatePath após mutação. -->

Assinaturas a criar:

```typescript
// src/lib/stock/custo-medio.ts (SEM 'use server' — lib pura importável por action + test)
export function calcularCustoMedioMovel(
  saldoAtual: number,      // saldo da UNIDADE antes da entrada (NUMERIC 12,4)
  custoAnterior: number,   // custo médio antes da entrada
  qtdEntrada: number,
  custoUnitario: number,
): number  // guard: se saldoAtual <= 0 → retorna custoUnitario; arredondar a 4 casas

// src/actions/products.ts ('use server')
export async function createProduct(input: ProductInput): Promise<{ success: boolean; error?: string; id?: string }>
export async function updateProduct(id: string, input: Partial<ProductInput>): Promise<{ success: boolean; error?: string }>
export async function listProducts(opts?: { unitId?: string; categoria?: string; status?: string; q?: string }): Promise<{ success: boolean; error?: string; data?: ProductWithSaldo[] }>
export async function deactivateProduct(id: string): Promise<{ success: boolean; error?: string }>

// src/actions/product-batches.ts ('use server')
export async function listProductBatches(productId: string, unitId: string): Promise<{ success: boolean; error?: string; data?: BatchRow[] }>

// src/actions/stock-entries.ts ('use server')
export async function createStockEntry(input: StockEntryInput & { unit_id: string }): Promise<{ success: boolean; error?: string; id?: string }>
export async function listStockEntries(opts?: { productId?: string; from?: string; to?: string }): Promise<{ success: boolean; error?: string; data?: EntryRow[] }>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lib pura de custo médio móvel</name>
  <files>src/lib/stock/custo-medio.ts, src/__tests__/estoque/custo-medio.test.ts</files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-RESEARCH.md §"Pattern 3: Custo Médio Móvel" + §"Pitfall 6" (divisão por zero)
    - src/__tests__/estoque/stock-entries.test.ts (RED test do Plan 01 que source-inspeciona 'calcularCustoMedioMovel' nesta lib)
  </read_first>
  <behavior>
    - calcularCustoMedioMovel(0, 0, 10, 5) === 5 (primeiro lote → custo unitário)
    - calcularCustoMedioMovel(10, 5, 10, 7) === 6 ((10*5 + 10*7)/20)
    - calcularCustoMedioMovel(-3, 5, 10, 7) === 7 (saldo negativo → guard retorna custoUnitario)
    - calcularCustoMedioMovel(3, 4, 2, 9) arredondado a 4 casas === Number(((3*4+2*9)/5).toFixed(4)) === 6
    - resultado nunca NaN nem Infinity
  </behavior>
  <action>
Criar src/lib/stock/custo-medio.ts (SEM 'use server' nem 'server-only' — lib pura):
```typescript
export function calcularCustoMedioMovel(
  saldoAtual: number,
  custoAnterior: number,
  qtdEntrada: number,
  custoUnitario: number,
): number {
  if (saldoAtual <= 0) return custoUnitario
  const novoSaldo = saldoAtual + qtdEntrada
  if (novoSaldo <= 0) return custoUnitario
  const custo = (saldoAtual * custoAnterior + qtdEntrada * custoUnitario) / novoSaldo
  return Number(custo.toFixed(4))
}
```
Criar src/__tests__/estoque/custo-medio.test.ts com os casos do bloco <behavior>.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/estoque/custo-medio.test.ts src/__tests__/estoque/stock-entries.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/estoque/custo-medio.test.ts` PASS (todos os casos)
    - src/__tests__/estoque/stock-entries.test.ts assertion de 'calcularCustoMedioMovel' na lib agora GREEN
    - `grep "'use server'" src/lib/stock/custo-medio.ts` retorna 0 (lib pura)
  </acceptance_criteria>
  <done>Lib pura testada e GREEN; source-inspection do Plan 01 sobre custo-medio.ts satisfeita.</done>
</task>

<task type="auto">
  <name>Task 2: Server Actions de produtos e lotes</name>
  <files>src/actions/products.ts, src/actions/product-batches.ts</files>
  <read_first>
    - src/actions/suppliers.ts (padrão getActor, WRITER_ROLES, Zod parse, revalidatePath — copiar verbatim o helper getActor)
    - src/lib/validators/product.ts (productSchema — usar no parse)
    - src/types/database.types.ts (Row types de products, product_batches)
  </read_first>
  <action>
Criar src/actions/products.ts com 'use server' + import 'server-only'. Copiar o helper getActor de suppliers.ts. WRITER_ROLES = ['admin','superadmin'] (D-18: operacional não existe no enum — Pitfall 8).

- createProduct: `getActor()` → gate WRITER_ROLES → `productSchema.parse(input)` → insert em products com clinic_id = actor.tenant_id → `revalidatePath('/clinica/estoque/produtos')` → retorna { success, id }.
- updateProduct: gate WRITER_ROLES → parse parcial → update WHERE id AND clinic_id → revalidatePath.
- deactivateProduct: gate → update ativo=false, deleted_at=now() WHERE id AND clinic_id (soft delete D-133/LGPD) → revalidatePath.
- listProducts(opts): SELECT de products (clinic_id via RLS) + saldo por unidade. Saldo = subquery/agregação de product_batches.saldo_disponivel WHERE product_id=p.id AND unit_id=opts.unitId AND deleted_at IS NULL. Derivar status: 'negativo' se saldo<0; 'critico' se saldo<=0 e não negativo OR saldo <= estoque_minimo*0.5; 'baixo' se saldo <= estoque_minimo; senão 'normal'. Aplicar filtros categoria/status/q (nome ou sku ILIKE). Ordenar por criticidade depois nome.

Criar src/actions/product-batches.ts com 'use server':
- listProductBatches(productId, unitId): SELECT de product_batches WHERE product_id AND unit_id AND deleted_at IS NULL ORDER BY created_at ASC (FIFO). Retorna id, numero_lote, numero_anvisa, data_validade, qtd_inicial, saldo_disponivel, custo_unitario.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "products.ts|product-batches.ts" | head; echo "exit-check"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use server'" src/actions/products.ts` presente
    - `grep "productSchema" src/actions/products.ts` presente (validação antes do DB)
    - `grep "get_my_role\|WRITER_ROLES\|admin.*superadmin" src/actions/products.ts` — gate de escrita presente
    - `grep "saldo_disponivel" src/actions/products.ts` — saldo derivado de batches
    - `npx tsc --noEmit` não reporta erro novo em products.ts nem product-batches.ts
  </acceptance_criteria>
  <done>CRUD de produtos + leitura de lotes compilando; escrita restrita a admin/superadmin; saldo por unidade derivado de batches.</done>
</task>

<task type="auto">
  <name>Task 3: Server Action de entrada de estoque (custo médio + criação de lote)</name>
  <files>src/actions/stock-entries.ts</files>
  <read_first>
    - src/lib/stock/custo-medio.ts (calcularCustoMedioMovel — Task 1)
    - src/lib/validators/product.ts (stockEntrySchema)
    - src/actions/product-batches.ts (padrão de leitura de saldo)
    - src/__tests__/estoque/stock-entries.test.ts (strings source-inspecionadas: 'custo_medio_movel', 'calcularCustoMedioMovel', 'product_batches', "'use server'")
  </read_first>
  <action>
Criar src/actions/stock-entries.ts com 'use server' + import 'server-only'. Copiar getActor. Gate WRITER_ROLES.

createStockEntry(input):
1. `stockEntrySchema.parse(input)` (input inclui categoria_produto para validação condicional; unit_id vem separado).
2. Ler saldo atual da unidade = SUM(product_batches.saldo_disponivel) WHERE product_id AND unit_id AND deleted_at IS NULL. Ler products.custo_medio atual.
3. `const novoCustoMedio = calcularCustoMedioMovel(saldoAtual, custoAnterior, input.qtd, input.custo_unitario)` — comentar a linha com `// custo_medio_movel D-02`.
4. Insert em product_batches: { clinic_id, unit_id, product_id, numero_lote, numero_anvisa: input.numero_anvisa_lote, data_validade, qtd_inicial: input.qtd, saldo_disponivel: input.qtd, custo_unitario: input.custo_unitario }.
5. Insert em stock_entries: { clinic_id, unit_id, product_id, batch_id, supplier_id, qtd, custo_unitario, custo_medio_apos: novoCustoMedio, nota_fiscal, created_by: actor.id }.
6. Update products SET custo_medio = novoCustoMedio WHERE id (denormalizado network-level; nota: multi-unidade usa custo_medio_apos por unidade — aceitável para display, ver Claude's Discretion do CONTEXT).
7. revalidatePath('/clinica/estoque/entradas') e '/clinica/estoque/produtos'.
Usar createAdminClient? NÃO — entrada é ação de usuário autenticado admin; usar createClient() (RLS admin_write permite). Retorna { success, id }.

listStockEntries(opts): SELECT stock_entries JOIN products(name) JOIN suppliers(name) LEFT, filtros productId/from/to, ORDER BY created_at DESC.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/estoque/stock-entries.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep "custo_medio_movel" src/actions/stock-entries.ts` presente (comentário/identificador)
    - `grep "calcularCustoMedioMovel" src/actions/stock-entries.ts` importado e usado
    - `grep "product_batches" src/actions/stock-entries.ts` — insert de lote presente
    - `grep "'use server'" src/actions/stock-entries.ts` presente
    - `npx vitest run src/__tests__/estoque/stock-entries.test.ts` GREEN (era RED no Plan 01)
    - `npx tsc --noEmit` sem erro novo em stock-entries.ts
  </acceptance_criteria>
  <done>Entrada cria lote + recalcula custo médio; stock-entries.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → createProduct/createStockEntry | Zod valida antes do DB; role gate + RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Elevation of Privilege | createProduct/createStockEntry por role não-admin | mitigate | Gate WRITER_ROLES no Server Action + RLS admin_write |
| T-17-07 | Injection | numero_lote/numero_anvisa | mitigate | Zod .max() no schema + queries parametrizadas Supabase |
| T-17-08 | Tampering | custo médio incorreto (drift) | mitigate | NUMERIC(12,4) + toFixed(4); guard divisão por zero |
</threat_model>

<verification>
`npx vitest run src/__tests__/estoque/` — custo-medio.test.ts + stock-entries.test.ts GREEN. `npx tsc --noEmit` limpo para os novos arquivos.
</verification>

<success_criteria>
- calcularCustoMedioMovel pura, testada, cobre primeiro lote e saldo negativo
- createProduct/updateProduct/listProducts/deactivateProduct funcionais, escrita restrita admin/superadmin
- createStockEntry cria lote + recalcula custo médio; listStockEntries com filtros
- stock-entries.test.ts (RED do Plan 01) agora GREEN
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-estoque-materiais/17-03-SUMMARY.md`
</output>
