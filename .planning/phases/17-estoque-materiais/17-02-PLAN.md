---
phase: 17-estoque-materiais
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260703000100_estoque_tables.sql
  - supabase/migrations/20260703000200_estoque_alters.sql
  - supabase/migrations/20260703000300_estoque_rls.sql
  - supabase/migrations/20260703000400_estoque_seed.sql
  - src/types/database.types.ts
autonomous: false
requirements:
  - EST-01
  - EST-02
  - EST-03
user_setup:
  - service: supabase
    why: "Push das migrations da Fase 17 ao projeto jqjwyqlbbuqnrffdnlpp; a CLI costuma estar logada na conta errada (nexus-*) per project memory"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (para push não-TTY)"
    dashboard_config:
      - task: "Confirmar que a CLI está logada na conta correta antes do push (re-login se nexus-*)"
        location: "supabase login / supabase projects list"

must_haves:
  truths:
    - "As 6 tabelas novas (products, product_batches, stock_entries, stock_draws, service_material_templates, stock_alerts) existem no banco ao vivo"
    - "payables.origem aceita 'estoque_agente' sem violar CHECK"
    - "approval_requests.requested_by é nullable (NULL = ator de sistema/cron)"
    - "ai_agent_config tem seed do agente 'stock_replenishment' em L2 para todas as clínicas ativas"
    - "src/types/database.types.ts regenerado inclui as 6 tabelas novas"
  artifacts:
    - path: "supabase/migrations/20260703000100_estoque_tables.sql"
      provides: "DDL das 6 tabelas de estoque com índices clinic_id + unit_id"
      contains: "CREATE TABLE public.products"
    - path: "supabase/migrations/20260703000200_estoque_alters.sql"
      provides: "ALTER payables.origem CHECK + approval_requests.requested_by nullable"
      contains: "estoque_agente"
    - path: "supabase/migrations/20260703000300_estoque_rls.sql"
      provides: "RLS USING+WITH CHECK nas 6 tabelas"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "supabase/migrations/20260703000400_estoque_seed.sql"
      provides: "Seed do agente stock_replenishment L2"
      contains: "stock_replenishment"
    - path: "src/types/database.types.ts"
      provides: "Tipos regenerados com as tabelas de estoque"
      contains: "product_batches"
  key_links:
    - from: "supabase db push"
      to: "live DB jqjwyqlbbuqnrffdnlpp"
      via: "aplicação das migrations em ordem de timestamp 000100→000400"
      pattern: "stock_draws"
    - from: "src/actions/products.ts"
      to: "products table"
      via: "database.types.ts Row type"
      pattern: "products"
---

<objective>
Criar as 6 tabelas do módulo de estoque, os ALTERs de integração (payables.origem + approval_requests.requested_by), a migration de RLS, o seed do agente de compras, aplicar tudo no banco ao vivo via [BLOCKING] db push e regenerar os tipos TypeScript.

Purpose: tornar o schema real e tipado ANTES de qualquer Server Action/UI. Sem o push, tsc/build passariam em tipos obsoletos e a verificação seria falso-positivo (per schema_push_requirement). Todas as Waves 2–4 dependem das tabelas ao vivo + tipos regenerados.

Output: 4 migrations aplicadas + src/types/database.types.ts regenerado incluindo products, product_batches, stock_entries, stock_draws, service_material_templates, stock_alerts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-estoque-materiais/17-CONTEXT.md
@.planning/phases/17-estoque-materiais/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever as 4 migrations (tabelas + alters + RLS + seed)</name>
  <files>
    supabase/migrations/20260703000100_estoque_tables.sql,
    supabase/migrations/20260703000200_estoque_alters.sql,
    supabase/migrations/20260703000300_estoque_rls.sql,
    supabase/migrations/20260703000400_estoque_seed.sql
  </files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-RESEARCH.md §"Schema das 5 Novas Tabelas" (linhas ~485-603 — DDL verbatim, note que são 6 com stock_alerts)
    - supabase/migrations/20260621000100_payables_tables.sql (CHECK de payables.origem linha ~78; padrão de suppliers FK)
    - supabase/migrations/20260616000200_approval_requests.sql (requested_by NOT NULL — será alterado)
    - supabase/migrations/20260614000400_role_expansion.sql (D-18: verificar se papel 'operacional' existe no enum de roles — se existir, incluir nas políticas admin_write de products, product_batches, stock_entries, service_material_templates)
    - supabase/migrations/20260614000600_ai_agent_config.sql OU o arquivo *ai_agent_config* (padrão de seed com ON CONFLICT no índice parcial WHERE unit_id IS NULL)
    - supabase/migrations/20260620000200_faturamento_os_tables.sql (appointment_procedures — sem unit_id; FK alvo de stock_draws.appointment_procedure_id)
  </read_first>
  <action>
Criar 20260703000100_estoque_tables.sql com as 6 tabelas EXATAMENTE conforme o DDL de 17-RESEARCH.md §"Schema das 5 Novas Tabelas" (products, product_batches, stock_entries, stock_draws, service_material_templates, stock_alerts), com estas correções obrigatórias:

1. products: manter `custo_medio NUMERIC(12,4) NOT NULL DEFAULT 0` (custo médio denormalizado network-level; per-unit derivado de stock_entries.custo_medio_apos). Manter `numero_anvisa TEXT` (obrigatório só para implante — enforcement no Zod/Server Action, não no CHECK).
2. Colunas monetárias em NUMERIC(12,2) NÃO se aplicam aqui: qtd e custos de estoque usam NUMERIC(12,4) (padrão do projeto para qtd fracionada + precisão de custo médio). Manter conforme DDL.
3. stock_draws.appointment_procedure_id REFERENCES public.appointment_procedures(id) ON DELETE RESTRICT (Pitfall 5). stock_draws.motivo TEXT (obrigatório apenas p/ manual — enforce no Server Action). stock_draws.tipo CHECK IN ('automatico','manual').
4. stock_alerts: substituir a UNIQUE inline por índice de expressão (Postgres não aceita DATE() em UNIQUE de tabela):
   `CREATE UNIQUE INDEX uq_stock_alerts_daily ON public.stock_alerts (product_id, unit_id, clinic_id, tipo, (created_at::date));`
   Manter coluna `resolvido BOOLEAN NOT NULL DEFAULT false` e CHECK tipo IN ('minimo','validade').
5. Todos os índices clinic_id + unit_id conforme DDL. Todas as tabelas com deleted_at TIMESTAMPTZ (exceto stock_draws e stock_alerts que são append-only — sem deleted_at, conforme DDL).

Criar 20260703000200_estoque_alters.sql:
```sql
-- EST-03: nova origem de CP gerada pelo agente de compras L2
ALTER TABLE public.payables DROP CONSTRAINT IF EXISTS payables_origem_check;
ALTER TABLE public.payables ADD CONSTRAINT payables_origem_check
  CHECK (origem IN ('manual', 'recorrente', 'lab', 'repasse', 'tributo', 'estoque_agente'));

-- Open Question 2: agente cron não tem sessão de usuário → requested_by nullable (NULL = sistema)
ALTER TABLE public.approval_requests ALTER COLUMN requested_by DROP NOT NULL;
COMMENT ON COLUMN public.approval_requests.requested_by IS 'NULL = ação de sistema/agente cron (sem sessão de usuário)';
```

Criar 20260703000300_estoque_rls.sql seguindo o padrão de 20260621000600_phase16_rls.sql. IMPORTANTE (Open Question 1 / Pitfall 8): o papel `operacional` NÃO existe no enum de roles → usar SOMENTE `admin`/`superadmin` nas políticas de escrita. Para cada tabela:
- ENABLE ROW LEVEL SECURITY;
- SELECT policy `<tabela>_tenant_read`: `USING (clinic_id = get_my_tenant_id())`
- products, product_batches, stock_entries, service_material_templates → write policy `<tabela>_admin_write`: `FOR ALL USING (clinic_id = get_my_tenant_id() AND get_my_role() IN ('admin','superadmin')) WITH CHECK (clinic_id = get_my_tenant_id() AND get_my_role() IN ('admin','superadmin'))`
- stock_draws e stock_alerts → SEM política de INSERT/UPDATE para authenticated (escrita SOMENTE via createAdminClient nas Server Actions/cron). Apenas o tenant_read SELECT.

Criar 20260703000400_estoque_seed.sql:
```sql
-- Seed do agente 'stock_replenishment' L2 (network-level) para clínicas ativas
INSERT INTO public.ai_agent_config (clinic_id, agent_key, autonomy_level, enabled)
SELECT c.id, 'stock_replenishment', 'L2', true
FROM public.clinics c
WHERE c.deleted_at IS NULL
ON CONFLICT (clinic_id, agent_key) WHERE unit_id IS NULL DO NOTHING;
```
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE public.\(products\|product_batches\|stock_entries\|stock_draws\|service_material_templates\|stock_alerts\)" supabase/migrations/20260703000100_estoque_tables.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE" supabase/migrations/20260703000100_estoque_tables.sql` == 6
    - `grep "estoque_agente" supabase/migrations/20260703000200_estoque_alters.sql` retorna a linha do novo CHECK
    - `grep "requested_by DROP NOT NULL" supabase/migrations/20260703000200_estoque_alters.sql` presente
    - `grep -c "get_my_role() IN ('admin','superadmin')" supabase/migrations/20260703000300_estoque_rls.sql` >= 4
    - `grep -c "operacional" supabase/migrations/20260703000300_estoque_rls.sql` == 0 (papel não existe)
    - `grep "stock_replenishment" supabase/migrations/20260703000400_estoque_seed.sql` presente
    - `grep "uq_stock_alerts_daily" supabase/migrations/20260703000100_estoque_tables.sql` usa (created_at::date)
  </acceptance_criteria>
  <done>4 migrations escritas, sem db push ainda (Task 3). Timestamps > 20260625000100 (última existente).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Verificar login Supabase na conta correta</name>
  <files>(nenhum — verificação de login)</files>
  <read_first>
    - Project memory: conta jqjwyqlbbuqnrffdnlpp (org kczvihafddupruvsrrsc, sa-east-1); gotcha: CLI/MCP costuma estar logado na conta errada (nexus-*) → re-login antes de db push
  </read_first>
  <what-built>4 migrations staged: 20260703000100..000400. Antes do push, a CLI deve apontar para a org dona de jqjwyqlbbuqnrffdnlpp (NÃO nexus-*).</what-built>
  <action>
CHECKPOINT HUMANO — não fazer push até confirmar:
  1. Rodar `supabase projects list` e confirmar que `jqjwyqlbbuqnrffdnlpp` aparece.
  2. Se NÃO aparecer (CLI logada em nexus-*), rodar `supabase login` e autenticar com a conta FYNXIA, OU exportar `SUPABASE_ACCESS_TOKEN` para não-TTY.
  3. Confirmar linkado: `supabase link --project-ref jqjwyqlbbuqnrffdnlpp` se ainda não estiver.
Isto protege contra aplicar schema no projeto errado (gotcha da project memory).
  </action>
  <how-to-verify>
    1. `supabase projects list` mostra jqjwyqlbbuqnrffdnlpp.
    2. Se ausente (logado em nexus-*), rodar `supabase login` e autenticar com a conta FYNXIA, OU exportar SUPABASE_ACCESS_TOKEN para não-TTY.
    3. `supabase link --project-ref jqjwyqlbbuqnrffdnlpp` se ainda não linkado.
  </how-to-verify>
  <resume-signal>Digite "logado" quando `supabase projects list` mostrar jqjwyqlbbuqnrffdnlpp.</resume-signal>
  <acceptance_criteria>
    - `supabase projects list` inclui jqjwyqlbbuqnrffdnlpp
  </acceptance_criteria>
  <done>CLI autenticada na conta FYNXIA; jqjwyqlbbuqnrffdnlpp linkado e visível.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] db push + regenerar tipos</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - supabase/migrations/20260703000100_estoque_tables.sql
    - supabase/migrations/20260703000200_estoque_alters.sql
    - supabase/migrations/20260703000300_estoque_rls.sql
    - supabase/migrations/20260703000400_estoque_seed.sql
    - CLAUDE.md §"Type generation" (gen types em CI; guard de truncamento)
  </read_first>
  <action>
Da raiz do projeto:
```
supabase db push
```
Aplica as 4 migrations em ordem de timestamp (000100 tables → 000200 alters → 000400 seed). Se o push reportar migrations parcialmente aplicadas, NÃO re-editar arquivos — resolver com `supabase migration repair` apenas se a CLI instruir explicitamente, depois re-rodar push.

Após push com sucesso, regenerar tipos para o arquivo canônico do projeto (src/types/database.types.ts):
```
supabase gen types typescript --project-id jqjwyqlbbuqnrffdnlpp --schema public > src/types/database.types.ts
```
Guard de truncamento (padrão fases 14/15/16): se o arquivo gerado for MENOR que a versão anterior OU não contiver `product_batches` e `stock_draws`, a saída foi truncada — re-rodar o comando gen. Confirmar que o arquivo termina em chave de fechamento completa.

Se a CLI `supabase` estiver indisponível, fallback: Supabase MCP `apply_migration` por arquivo em ordem, depois gen types via MCP; PREFERIR a CLI.
  </action>
  <verify>
    <automated>grep -c "product_batches\|stock_draws\|stock_entries\|service_material_templates\|stock_alerts\|products" src/types/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "product_batches" src/types/database.types.ts` >= 1
    - `grep -c "stock_draws" src/types/database.types.ts` >= 1
    - `grep -c "service_material_templates" src/types/database.types.ts` >= 1
    - `grep "estoque_agente" src/types/database.types.ts` (payables.origem union atualizado)
    - database.types.ts NÃO truncado (termina em `}`; tamanho >= arquivo anterior)
  </acceptance_criteria>
  <done>4 migrations aplicadas no banco ao vivo; src/types/database.types.ts regenerado com as 6 tabelas + origem estoque_agente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → tabelas de estoque | RLS isola por clinic_id; escrita restrita por role |
| cron/agent (service role) → stock_draws/stock_alerts | Sem política authenticated; escrita só via createAdminClient server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Information Disclosure | Cross-tenant stock read | mitigate | RLS `USING (clinic_id = get_my_tenant_id())` em todas as 6 tabelas |
| T-17-04 | Elevation of Privilege | Escrita direta em stock_draws pelo cliente | mitigate | Sem política INSERT/UPDATE authenticated; escrita só via service role |
| T-17-05 | Tampering | payables via origem inválida | accept | CHECK constraint restringe origem a lista fechada incluindo estoque_agente |
</threat_model>

<verification>
`supabase db push` sem erro; `grep product_batches src/types/database.types.ts` retorna linhas; RLS habilitado nas 6 tabelas (`grep -c "ENABLE ROW LEVEL SECURITY" 20260703000300_estoque_rls.sql` == 6).
</verification>

<success_criteria>
- 6 tabelas criadas no banco ao vivo com índices clinic_id + unit_id
- payables.origem aceita 'estoque_agente'; approval_requests.requested_by nullable
- ai_agent_config seed do stock_replenishment L2 aplicado
- src/types/database.types.ts regenerado e não truncado
- RLS: escrita admin/superadmin apenas; stock_draws/stock_alerts sem escrita authenticated
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-estoque-materiais/17-02-SUMMARY.md`
</output>
