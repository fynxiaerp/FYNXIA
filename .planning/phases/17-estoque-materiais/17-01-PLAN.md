---
phase: 17-estoque-materiais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validators/product.ts
  - src/__tests__/estoque/stock-entries.test.ts
  - src/__tests__/estoque/stock-draws.test.ts
  - src/__tests__/estoque/stock-agent.test.ts
  - src/__tests__/estoque/cron-validade.test.ts
autonomous: true
requirements:
  - EST-01
  - EST-02
  - EST-03

must_haves:
  truths:
    - "Zod schemas validam campos obrigatórios por categoria (implante exige ANVISA, medicamento/implante exigem validade)"
    - "Tests de source-inspection existem e estão RED (falham) para stock-entries, stock-draws, stock-agent e cron"
    - "productSchema rejeita implante sem numero_anvisa_produto"
    - "stockEntrySchema rejeita implante sem numero_lote e data_validade"
    - "stockDrawSchema rejeita motivo ausente quando tipo='manual'"
  artifacts:
    - path: "src/lib/validators/product.ts"
      provides: "Zod v3 schemas para produto, lote, entrada e baixa de estoque"
      exports: ["productSchema", "stockEntrySchema", "stockDrawSchema", "serviceMaterialTemplateSchema"]
    - path: "src/__tests__/estoque/stock-entries.test.ts"
      provides: "RED test — source-inspect stock-entries.ts para 'custo_medio_movel'"
    - path: "src/__tests__/estoque/stock-draws.test.ts"
      provides: "RED test — source-inspect stock-draws.ts para 'saldo_disponivel' e 'FIFO'"
    - path: "src/__tests__/estoque/stock-agent.test.ts"
      provides: "RED test — source-inspect stock-agent.ts para 'withAgentPolicy' e 'stock_replenishment'"
    - path: "src/__tests__/estoque/cron-validade.test.ts"
      provides: "RED test — source-inspect cron route para 'isCronAuthorized' e 'estoque-validade'"
  key_links:
    - from: "src/lib/validators/product.ts"
      to: "src/actions/products.ts"
      via: "import { productSchema } from '@/lib/validators/product'"
      pattern: "productSchema|stockEntrySchema|stockDrawSchema"
    - from: "src/__tests__/estoque/stock-entries.test.ts"
      to: "src/actions/stock-entries.ts"
      via: "SRC() source-inspection"
      pattern: "custo_medio_movel"
---

<objective>
Criar os Zod schemas de validação (productSchema, stockEntrySchema, stockDrawSchema, serviceMaterialTemplateSchema) e quatro test stubs RED que source-inspecionam as Server Actions e Agent ainda não existentes.

Purpose: Estabelecer os contratos de validação que todas as Server Actions da Fase 17 consumirão, e plantar os tests RED que se tornarão GREEN quando as implementações forem criadas nos Plans 05–06.

Output: src/lib/validators/product.ts (schemas concretos + testados) + 4 arquivos de test RED em src/__tests__/estoque/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-estoque-materiais/17-CONTEXT.md
@.planning/phases/17-estoque-materiais/17-RESEARCH.md
@src/lib/validators/service.ts
@src/lib/validators/service-order.ts
</context>

<interfaces>
<!-- Schemas Zod v3 que serão criados neste plan — outros plans importam daqui -->
<!-- Padrão de validators do projeto: Zod v3, sem .default(), usar defaultValues no useForm -->
<!-- D-133 do STATE.md: NUNCA usar .default() em schemas Zod — RHF v7 type mismatch -->

Contratos a criar em src/lib/validators/product.ts:

```typescript
// productSchema — validação de produto de estoque (EST-01)
// Campo condicional: numero_anvisa_produto obrigatório apenas para categoria='implante'
export const productSchema: z.ZodObject<...>
// Campos: name (string, 1-200), sku (string, opcional), category ('insumo'|'medicamento'|'implante'),
// unidade_medida ('un'|'ml'|'g'|'cx'|'fr'), estoque_minimo (number, ≥0), estoque_maximo (number opcional),
// preferred_supplier_id (uuid opcional), numero_anvisa_produto (string opcional — obrigatório para implante via .superRefine)

// stockEntrySchema — validação de entrada de estoque (D-10)
// Campos condicionais: numero_lote obrigatório sempre; data_validade obrigatória para implante/medicamento
export const stockEntrySchema: z.ZodObject<...>
// Campos: product_id (uuid), supplier_id (uuid opcional), numero_lote (string, 1-100),
// data_validade (string date opcional — obrigatório para implante/medicamento via .superRefine),
// qtd (number, >0), custo_unitario (number, ≥0), nota_fiscal (string opcional),
// numero_anvisa_lote (string opcional — obrigatório para implante via .superRefine),
// categoria_produto ('insumo'|'medicamento'|'implante') — passed context for conditional validation

// stockDrawSchema — validação de baixa manual (D-19)
export const stockDrawSchema: z.ZodObject<...>
// Campos: product_id (uuid), qtd (number, >0), motivo ('perda'|'quebra'|'vencimento'|'ajuste_inventario'),
// observacao (string opcional)

// serviceMaterialTemplateSchema — validação de template de consumo (D-07)
export const serviceMaterialTemplateSchema: z.ZodObject<...>
// Campos: service_id (uuid), product_id (uuid), qtd_padrao (number, >0)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod v3 schemas para produto, lote, entrada e baixa</name>
  <files>src/lib/validators/product.ts, src/__tests__/estoque/produto-schema.test.ts</files>
  <read_first>
    - src/lib/validators/service.ts (padrão de schema: Zod v3, sem .default(), .superRefine para condicionais)
    - src/lib/validators/service-order.ts (padrão de uuid(), enums, mensagens de erro)
    - .planning/phases/17-estoque-materiais/17-CONTEXT.md §D-03 (campos por categoria)
  </read_first>
  <behavior>
    - productSchema.parse({ name:'x', category:'implante', unidade_medida:'un', estoque_minimo:0 }) → deve FALHAR (numero_anvisa_produto ausente)
    - productSchema.parse({ name:'x', category:'implante', unidade_medida:'un', estoque_minimo:0, numero_anvisa_produto:'12345.678901/2024-001' }) → deve PASSAR
    - productSchema.parse({ name:'x', category:'insumo', unidade_medida:'un', estoque_minimo:5 }) → deve PASSAR (sem ANVISA)
    - stockEntrySchema.parse({ product_id: uuid, numero_lote:'L001', qtd:10, custo_unitario:5.5, categoria_produto:'implante' }) → deve FALHAR (numero_anvisa_lote e data_validade ausentes)
    - stockEntrySchema.parse({ product_id: uuid, numero_lote:'L001', qtd:10, custo_unitario:5.5, categoria_produto:'insumo' }) → deve PASSAR
    - stockDrawSchema.parse({ product_id: uuid, qtd:0, motivo:'perda' }) → deve FALHAR (qtd deve ser >0)
    - stockDrawSchema.parse({ product_id: uuid, qtd:2, motivo:'perda' }) → deve PASSAR
    - serviceMaterialTemplateSchema.parse({ service_id: uuid, product_id: uuid, qtd_padrao:0 }) → deve FALHAR
  </behavior>
  <action>
Criar src/lib/validators/product.ts — NÃO usar 'use server' (módulo importado por client e server).

```typescript
import { z } from 'zod'

export const PRODUCT_CATEGORIES = ['insumo', 'medicamento', 'implante'] as const
export const UNIDADES_MEDIDA = ['un', 'ml', 'g', 'cx', 'fr'] as const
export const DRAW_MOTIVOS = ['perda', 'quebra', 'vencimento', 'ajuste_inventario'] as const

export const productSchema = z
  .object({
    name: z.string({ required_error: 'Nome obrigatório' }).min(1, 'Nome obrigatório').max(200),
    sku: z.string().max(100).optional().nullable(),
    category: z.enum(PRODUCT_CATEGORIES, { errorMap: () => ({ message: 'Categoria inválida' }) }),
    unidade_medida: z.enum(UNIDADES_MEDIDA, { errorMap: () => ({ message: 'Unidade inválida' }) }),
    estoque_minimo: z.number({ required_error: 'Estoque mínimo obrigatório', invalid_type_error: 'Deve ser número' }).min(0, 'Deve ser ≥ 0'),
    estoque_maximo: z.number().min(0).optional().nullable(),
    preferred_supplier_id: z.string().uuid().optional().nullable(),
    numero_anvisa_produto: z.string().max(50).optional().nullable(),
  })
  .superRefine((val, ctx) => {
    if (val.category === 'implante' && !val.numero_anvisa_produto) {
      ctx.addIssue({ code: z.ZodIssueCode.custom, message: 'Número ANVISA é obrigatório para implantes.', path: ['numero_anvisa_produto'] })
    }
  })

export type ProductInput = z.infer<typeof productSchema>

export const stockEntrySchema = z
  .object({
    product_id: z.string({ required_error: 'Produto obrigatório' }).uuid(),
    categoria_produto: z.enum(PRODUCT_CATEGORIES),
    supplier_id: z.string().uuid().optional().nullable(),
    numero_lote: z.string({ required_error: 'Número de lote obrigatório' }).min(1).max(100),
    data_validade: z.string().optional().nullable(),
    qtd: z.number({ required_error: 'Quantidade obrigatória', invalid_type_error: 'Deve ser número' }).positive('A quantidade deve ser maior que zero.'),
    custo_unitario: z.number({ required_error: 'Custo unitário obrigatório', invalid_type_error: 'Deve ser número' }).min(0),
    nota_fiscal: z.string().max(100).optional().nullable(),
    numero_anvisa_lote: z.string().max(50).optional().nullable(),
  })
  .superRefine((val, ctx) => {
    if ((val.categoria_produto === 'implante' || val.categoria_produto === 'medicamento') && !val.data_validade) {
      ctx.addIssue({ code: z.ZodIssueCode.custom, message: 'Data de validade obrigatória para esta categoria.', path: ['data_validade'] })
    }
    if (val.categoria_produto === 'implante' && !val.numero_anvisa_lote) {
      ctx.addIssue({ code: z.ZodIssueCode.custom, message: 'Número ANVISA é obrigatório para implantes.', path: ['numero_anvisa_lote'] })
    }
  })

export type StockEntryInput = z.infer<typeof stockEntrySchema>

export const stockDrawSchema = z.object({
  product_id: z.string({ required_error: 'Produto obrigatório' }).uuid(),
  qtd: z.number({ required_error: 'Quantidade obrigatória', invalid_type_error: 'Deve ser número' }).positive('A quantidade deve ser maior que zero.'),
  motivo: z.enum(DRAW_MOTIVOS, { errorMap: () => ({ message: 'Motivo inválido' }) }),
  observacao: z.string().max(500).optional().nullable(),
})

export type StockDrawInput = z.infer<typeof stockDrawSchema>

export const serviceMaterialTemplateSchema = z.object({
  service_id: z.string({ required_error: 'Serviço obrigatório' }).uuid(),
  product_id: z.string({ required_error: 'Produto obrigatório' }).uuid(),
  qtd_padrao: z.number({ required_error: 'Quantidade padrão obrigatória', invalid_type_error: 'Deve ser número' }).positive('A quantidade deve ser maior que zero.'),
})

export type ServiceMaterialTemplateInput = z.infer<typeof serviceMaterialTemplateSchema>
```

Criar src/__tests__/estoque/produto-schema.test.ts com todos os casos do bloco <behavior> acima.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/estoque/produto-schema.test.ts --reporter=verbose</automated>
  </verify>
  <done>Todos os 8 casos de teste passam. src/lib/validators/product.ts exporta productSchema, stockEntrySchema, stockDrawSchema, serviceMaterialTemplateSchema sem erros de tsc.</done>
</task>

<task type="auto">
  <name>Task 2: RED source-inspection tests para actions e cron</name>
  <files>
    src/__tests__/estoque/stock-entries.test.ts,
    src/__tests__/estoque/stock-draws.test.ts,
    src/__tests__/estoque/stock-agent.test.ts,
    src/__tests__/estoque/cron-validade.test.ts
  </files>
  <read_first>
    - src/__tests__/governance/approvals.test.ts (padrão SRC() helper + source-inspection RED)
    - src/__tests__/financeiro16/migrations-phase16.test.ts (padrão de migration source-inspection)
    - .planning/phases/17-estoque-materiais/17-RESEARCH.md §Pattern 1-5 (strings concretas a inspecionar)
  </read_first>
  <action>
Criar 4 arquivos de test RED usando o padrão SRC() helper exatamente como approvals.test.ts:

```typescript
// SRC helper padrão (copiar de approvals.test.ts):
function SRC(relPath: string): string {
  const absPath = resolve(process.cwd(), relPath)
  try { return readFileSync(absPath, 'utf8') } catch { return '' }
}
```

**src/__tests__/estoque/stock-entries.test.ts** — RED até Plan 05 criar src/actions/stock-entries.ts:
- SRC('src/actions/stock-entries.ts') contém 'custo_medio_movel'
- SRC('src/actions/stock-entries.ts') contém 'calcularCustoMedioMovel'
- SRC('src/actions/stock-entries.ts') contém 'product_batches'
- SRC('src/actions/stock-entries.ts') contém "'use server'"
- SRC('src/lib/stock/custo-medio.ts') contém 'calcularCustoMedioMovel' (verifica a lib pura do Plan 03)

**src/__tests__/estoque/stock-draws.test.ts** — RED até Plan 06:
- SRC('src/actions/stock-draws.ts') contém 'saldo_disponivel'
- SRC('src/actions/stock-draws.ts') contém 'FIFO'
- SRC('src/actions/stock-draws.ts') contém 'drawMaterialsForProcedures'
- SRC('src/actions/stock-draws.ts') contém 'logBusinessEvent'
- SRC('src/actions/stock-draws.ts') contém "'use server'"

**src/__tests__/estoque/stock-agent.test.ts** — RED até Plan 06:
- SRC('src/lib/agents/stock-agent.ts') contém 'withAgentPolicy'
- SRC('src/lib/agents/stock-agent.ts') contém 'stock_replenishment'
- SRC('src/lib/agents/stock-agent.ts') contém 'estoque_agente'
- SRC('src/lib/agents/stock-agent.ts') contém 'approval_requests'

**src/__tests__/estoque/cron-validade.test.ts** — RED até Plan 06:
- SRC('src/app/api/cron/estoque-validade/route.ts') contém 'isCronAuthorized'
- SRC('src/app/api/cron/estoque-validade/route.ts') contém "runtime = 'nodejs'"
- SRC('src/app/api/cron/estoque-validade/route.ts') contém 'createAdminClient'
- SRC('src/app/api/cron/estoque-validade/route.ts') contém 'data_validade'

Todos os 4 arquivos devem incluir `vi.mock('server-only', () => ({}))` no topo.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/estoque/stock-entries.test.ts` sai com código != 0 (RED esperado — src/actions/stock-entries.ts não existe ainda)
    - `npx vitest run src/__tests__/estoque/stock-agent.test.ts` sai com código != 0 (RED esperado)
    - Cada arquivo de test tem o helper SRC() e pelo menos 4 assertions de source-inspection
    - Nenhum arquivo usa imports de 'use server' diretos (apenas SRC() helper que não executa o módulo)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/estoque/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>produto-schema.test.ts PASS. Os demais 4 tests estão RED (FAIL) com mensagem de conteúdo ausente, confirmando que as implementations ainda não existem. Nenhum erro de sintaxe TypeScript nos arquivos de test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → Server Action | Zod valida antes de qualquer operação de banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | productSchema | mitigate | .superRefine valida campos obrigatórios por categoria antes do DB; ANVISA ausente em implante rejeitado com mensagem explícita |
| T-17-02 | Tampering | stockDrawSchema | mitigate | motivo obrigatório via z.enum — valores fora da lista rejeitados no parse |
</threat_model>

<verification>
npx vitest run src/__tests__/estoque/ — produto-schema.test.ts PASS; demais 4 RED.
npx tsc --noEmit — src/lib/validators/product.ts sem erros de tipo.
</verification>

<success_criteria>
- src/lib/validators/product.ts existe, exporta 4 schemas, compila sem erros
- productSchema rejeita implante sem numero_anvisa_produto
- stockEntrySchema rejeita implante/medicamento sem data_validade
- stockDrawSchema rejeita qtd ≤ 0
- 4 RED tests existem e falham por conteúdo ausente (não por erro de sintaxe)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-estoque-materiais/17-01-SUMMARY.md`
</output>
