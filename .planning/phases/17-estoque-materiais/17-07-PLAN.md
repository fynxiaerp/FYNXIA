---
phase: 17-estoque-materiais
plan: 07
type: execute
wave: 4
depends_on: [17-03, 17-05]
files_modified:
  - src/app/(dashboard)/clinica/estoque/entradas/page.tsx
  - src/components/estoque/StockEntriesTable.tsx
  - src/components/estoque/StockEntryFormDialog.tsx
  - src/components/estoque/ManualDrawDialog.tsx
autonomous: true
requirements:
  - EST-01

must_haves:
  truths:
    - "Admin registra entrada de estoque via StockEntryFormDialog (produto, lote, validade, qtd, custo, NF)"
    - "Entrada de implante/medicamento exige data de validade; implante exige número ANVISA (validação condicional)"
    - "O dialog mostra 'Custo médio atual' e o 'Novo custo médio' após entrada"
    - "Baixa manual via ManualDrawDialog exige motivo e confirma com aviso de irreversibilidade"
    - "Histórico de entradas lista data, produto, lote, validade, qtd, custos e fornecedor"
  artifacts:
    - path: "src/app/(dashboard)/clinica/estoque/entradas/page.tsx"
      provides: "Página de entradas (RSC)"
      contains: "listStockEntries"
    - path: "src/components/estoque/StockEntryFormDialog.tsx"
      provides: "Dialog de registro de entrada com custo médio"
      contains: "stockEntrySchema"
    - path: "src/components/estoque/ManualDrawDialog.tsx"
      provides: "Dialog de baixa manual com confirmação destructive"
      contains: "stockDrawSchema"
  key_links:
    - from: "src/components/estoque/StockEntryFormDialog.tsx"
      to: "src/actions/stock-entries.ts"
      via: "createStockEntry"
      pattern: "createStockEntry"
    - from: "src/components/estoque/ManualDrawDialog.tsx"
      to: "src/actions/stock-draws.ts"
      via: "createManualDraw"
      pattern: "createManualDraw"
---

<objective>
Construir a página de entradas de estoque (formulário de recebimento + histórico) e o dialog de baixa manual com confirmação destrutiva, conforme o UI-SPEC.

Purpose: entregar a UI de movimentação de estoque (EST-01 entradas + baixa manual D-19).

Output: /clinica/estoque/entradas + StockEntriesTable + StockEntryFormDialog + ManualDrawDialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-estoque-materiais/17-UI-SPEC.md
@src/lib/validators/product.ts
@src/actions/stock-entries.ts
@src/actions/stock-draws.ts
@src/actions/products.ts
</context>

<interfaces>
<!-- Consumir: createStockEntry, listStockEntries (Plan 03); createManualDraw (Plan 05); listProducts (Plan 03). -->
<!-- stockEntrySchema exige categoria_produto p/ validação condicional → passar a categoria do produto selecionado. -->
<!-- Máscara BRL: usar padrão existente (ex.: TransactionModal amountStr string→number no submit). -->
<!-- @base-ui Button render-prop; DropdownMenu pointer-events-none. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Página de entradas + histórico</name>
  <files>src/app/(dashboard)/clinica/estoque/entradas/page.tsx, src/components/estoque/StockEntriesTable.tsx</files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-UI-SPEC.md §"3. /clinica/estoque/entradas" (colunas da tabela, filtros, empty state)
    - src/actions/stock-entries.ts (listStockEntries)
    - src/app/(dashboard)/clinica/estoque/produtos/page.tsx (padrão RSC + derivação de role — Plan 06)
  </read_first>
  <action>
Criar entradas/page.tsx (RSC): PageHeader com ação "Registrar Entrada" (admin/superadmin) abrindo StockEntryFormDialog. Chamar listStockEntries() server-side. Filtros nuqs: produto, from, to (date range). Passar para StockEntriesTable.

Criar StockEntriesTable.tsx ('use client'): Table shadcn com colunas Data(dd/MM/yyyy), Produto, Lote, Validade(dd/MM/yyyy ou "—"), Qtd Recebida(+UM), Custo Unit.(formatBRL), Custo Médio Após(formatBRL), Fornecedor(ou "—"), Registrado por. Filtros nuqs (produto select, date range Popover+Calendar). Empty state: ícone PackagePlus, "Nenhuma entrada registrada" / "Registre o recebimento de produtos para atualizar o estoque." + CTA (admin).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "entradas/page.tsx|StockEntriesTable" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "listStockEntries" "src/app/(dashboard)/clinica/estoque/entradas/page.tsx"` presente
    - `grep "useQueryState" src/components/estoque/StockEntriesTable.tsx` — filtros nuqs
    - `grep "PackagePlus" src/components/estoque/StockEntriesTable.tsx` — empty state
    - `npx tsc --noEmit` sem erro nos novos arquivos
  </acceptance_criteria>
  <done>Página de entradas com histórico filtrável.</done>
</task>

<task type="auto">
  <name>Task 2: StockEntryFormDialog (recebimento + custo médio)</name>
  <files>src/components/estoque/StockEntryFormDialog.tsx</files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-UI-SPEC.md §"Dialog StockEntryFormDialog" (campos, condicional implante, informativo de custo médio)
    - src/lib/validators/product.ts (stockEntrySchema — inclui categoria_produto, numero_anvisa_lote, data_validade condicionais)
    - src/actions/stock-entries.ts (createStockEntry — recebe input + unit_id)
    - src/actions/products.ts (listProducts para o Select; ler custo_medio e category do produto escolhido)
  </read_first>
  <action>
Criar StockEntryFormDialog.tsx ('use client'): Dialog + RHF + zodResolver(stockEntrySchema), defaultValues sem .default. Campos: Produto(Select com busca — ao escolher, setar categoria_produto no form e exibir "Custo médio atual: R$ X,XX"), Fornecedor(Select opcional), Número de Lote(obrigatório), Data de Validade(Input date — obrigatório para implante/medicamento via watch da categoria do produto), Qtd Recebida(number), Custo Unitário(Input BRL máscara → number no submit), Nota Fiscal(opcional). Condicional: se categoria do produto = 'implante' exibir "Número ANVISA" (obrigatório) abaixo do lote. Após submit com sucesso: exibir "Novo custo médio: R$ Y,YY" (retornado/recalculado) antes de fechar. Submit chama createStockEntry({...values, unit_id}) — unit_id do contexto do usuário. Botão "Registrar Entrada" com Loader2.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "StockEntryFormDialog" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "stockEntrySchema" src/components/estoque/StockEntryFormDialog.tsx` no zodResolver
    - `grep "createStockEntry" src/components/estoque/StockEntryFormDialog.tsx` no submit
    - `grep "categoria_produto" src/components/estoque/StockEntryFormDialog.tsx` — setado do produto p/ validação condicional
    - `grep "Custo médio\|custo médio" src/components/estoque/StockEntryFormDialog.tsx` — informativo
    - `npx tsc --noEmit` sem erro no arquivo
  </acceptance_criteria>
  <done>Dialog de entrada com validação condicional + informativo de custo médio.</done>
</task>

<task type="auto">
  <name>Task 3: ManualDrawDialog (baixa manual com confirmação)</name>
  <files>src/components/estoque/ManualDrawDialog.tsx</files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-UI-SPEC.md §"Dialog ManualDrawDialog" + §Copywriting Contract (confirmação de baixa manual)
    - src/lib/validators/product.ts (stockDrawSchema — motivo enum obrigatório)
    - src/actions/stock-draws.ts (createManualDraw)
  </read_first>
  <action>
Criar ManualDrawDialog.tsx ('use client'): Dialog + RHF + zodResolver(stockDrawSchema). Campos: Produto(pré-selecionado se vindo do contexto do dropdown do catálogo), Qtd(number), Motivo(Select: perda/quebra/vencimento/ajuste_inventario), Observação(Textarea opcional). Aviso Alert com AlertDescription: "Esta operação é irreversível e será registrada na trilha de auditoria." Botão submit variant="destructive" "Registrar Baixa" (Loader2 durante mutação). Submit chama createManualDraw({...values, unit_id}). Título "Confirmar Baixa Manual". Botão "Cancelar".
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "ManualDrawDialog" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "stockDrawSchema" src/components/estoque/ManualDrawDialog.tsx` no zodResolver
    - `grep "createManualDraw" src/components/estoque/ManualDrawDialog.tsx` no submit
    - `grep "variant=\"destructive\"" src/components/estoque/ManualDrawDialog.tsx` no botão
    - `grep "irreversível" src/components/estoque/ManualDrawDialog.tsx` — aviso
    - `npx tsc --noEmit` sem erro no arquivo
  </acceptance_criteria>
  <done>Dialog de baixa manual com motivo obrigatório e confirmação destructive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI dialogs → stock actions | Validação client é UX; role gate + Zod reais nos Server Actions (Plans 03/05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-17 | Repudiation | Baixa manual sem confirmação | mitigate | Aviso de irreversibilidade + createManualDraw grava logBusinessEvent (Plan 05) |
| T-17-18 | Tampering | Custo unitário mal formatado | mitigate | Máscara BRL string→number no submit; NUMERIC(12,4) no DB |
</threat_model>

<verification>
`npx tsc --noEmit` limpo. Entrada e baixa manual testadas em UAT (custo médio recalcula, baixa registra motivo).
</verification>

<success_criteria>
- Página de entradas com histórico filtrável
- StockEntryFormDialog com validação condicional + informativo de custo médio
- ManualDrawDialog com motivo obrigatório + confirmação destructive
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-estoque-materiais/17-07-SUMMARY.md`
</output>
