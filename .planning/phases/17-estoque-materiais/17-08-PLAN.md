---
phase: 17-estoque-materiais
plan: 08
type: execute
wave: 4
depends_on: [17-05]
files_modified:
  - src/app/(dashboard)/clinica/estoque/anvisa/page.tsx
  - src/components/estoque/AnvisaReportTable.tsx
  - src/components/estoque/AnvisaReportPdf.tsx
  - src/app/api/estoque/anvisa-pdf/route.ts
autonomous: true
requirements:
  - EST-03

must_haves:
  truths:
    - "Relatório ANVISA lista implantes usados: paciente, data, profissional, procedimento, lote, validade, número ANVISA, qtd"
    - "Filtros por produto/lote/paciente/período persistem na URL (nuqs)"
    - "Export PDF gera documento via @react-pdf/renderer (server, Flexbox only) com cabeçalho e rodapé"
  artifacts:
    - path: "src/app/(dashboard)/clinica/estoque/anvisa/page.tsx"
      provides: "Página do relatório ANVISA (RSC)"
      contains: "listAnvisaTraceability"
    - path: "src/components/estoque/AnvisaReportPdf.tsx"
      provides: "Documento @react-pdf/renderer"
      contains: "@react-pdf/renderer"
    - path: "src/app/api/estoque/anvisa-pdf/route.ts"
      provides: "Endpoint de geração de PDF (nodejs runtime)"
      contains: "renderToBuffer"
  key_links:
    - from: "src/app/(dashboard)/clinica/estoque/anvisa/page.tsx"
      to: "src/actions/stock-draws.ts"
      via: "listAnvisaTraceability"
      pattern: "listAnvisaTraceability"
    - from: "src/app/api/estoque/anvisa-pdf/route.ts"
      to: "src/components/estoque/AnvisaReportPdf.tsx"
      via: "renderToBuffer(<AnvisaReportPdf/>)"
      pattern: "AnvisaReportPdf|renderToBuffer"
---

<objective>
Construir a página do relatório ANVISA de implantes (tabela filtrável) e o export em PDF via @react-pdf/renderer, conforme o UI-SPEC.

Purpose: entregar a rastreabilidade ANVISA visível + exportável (EST-03 D-12/D-13).

Output: /clinica/estoque/anvisa + AnvisaReportTable + AnvisaReportPdf + rota de PDF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-estoque-materiais/17-UI-SPEC.md
@src/actions/stock-draws.ts
</context>

<interfaces>
<!-- Consumir: listAnvisaTraceability(opts) (Plan 05) → colunas: data, paciente, profissional, procedimento, produto, numero_lote, numero_anvisa, data_validade, qtd. -->
<!-- @react-pdf/renderer v4: Flexbox ONLY (sem CSS Grid); fonte Helvetica padrão. renderToBuffer no server. -->
<!-- Buscar um exemplo de uso de @react-pdf/renderer no projeto se existir (grep 'react-pdf'). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Página + tabela do relatório ANVISA</name>
  <files>src/app/(dashboard)/clinica/estoque/anvisa/page.tsx, src/components/estoque/AnvisaReportTable.tsx</files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-UI-SPEC.md §"4. /clinica/estoque/anvisa" (colunas, filtros, empty state)
    - src/actions/stock-draws.ts (listAnvisaTraceability)
    - src/app/(dashboard)/clinica/estoque/produtos/page.tsx (padrão RSC — Plan 06)
  </read_first>
  <action>
Criar anvisa/page.tsx (RSC): PageHeader com ação secundária "Exportar PDF" (Button variant="outline", ícone FileDown) que faz link/fetch para /api/estoque/anvisa-pdf com os filtros atuais na querystring. Chamar listAnvisaTraceability(opts) server-side com os filtros de searchParams. Filtros nuqs: produto (só implantes), lote, paciente, from, to. Passar para AnvisaReportTable.

Criar AnvisaReportTable.tsx ('use client'): Table shadcn com colunas Data Procedimento(dd/MM/yyyy), Paciente, Profissional, Procedimento, Produto(Implante), Nº Lote, Nº ANVISA, Validade(dd/MM/yyyy), Qtd. Filtros nuqs. Empty state: ícone ClipboardList, "Nenhum implante rastreado no período" / "Registre implantes nas entradas de estoque para gerar rastreabilidade ANVISA."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "anvisa/page.tsx|AnvisaReportTable" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "listAnvisaTraceability" "src/app/(dashboard)/clinica/estoque/anvisa/page.tsx"` presente
    - `grep "anvisa-pdf" "src/app/(dashboard)/clinica/estoque/anvisa/page.tsx"` — link de export
    - `grep "useQueryState" src/components/estoque/AnvisaReportTable.tsx` — filtros nuqs
    - `grep "ClipboardList" src/components/estoque/AnvisaReportTable.tsx` — empty state
    - `npx tsc --noEmit` sem erro nos novos arquivos
  </acceptance_criteria>
  <done>Relatório ANVISA filtrável com botão de export.</done>
</task>

<task type="auto">
  <name>Task 2: Documento PDF + rota de geração</name>
  <files>src/components/estoque/AnvisaReportPdf.tsx, src/app/api/estoque/anvisa-pdf/route.ts</files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-UI-SPEC.md §"PDF exportado (AnvisaReportPdf)" (cabeçalho, colunas, rodapé, Flexbox only)
    - src/actions/stock-draws.ts (listAnvisaTraceability)
    - CLAUDE.md §"PDF / Document Generation" (@react-pdf/renderer, Flexbox only, sem Grid)
    - Resultado de `grep -rl "@react-pdf/renderer" src` (reusar padrão existente se houver)
  </read_first>
  <action>
Criar AnvisaReportPdf.tsx: componente @react-pdf/renderer (Document>Page>View). Props: clinicName, periodo, geradoEm, rows[]. Cabeçalho: nome da clínica, período filtrado, data de geração. Tabela via View/flexDirection row (SEM CSS Grid). Colunas iguais à AnvisaReportTable. Rodapé: "Gerado pelo FYNXIA ERP em {data} às {hora}". Fonte Helvetica padrão. Estilos via StyleSheet.create com Flexbox.

Criar src/app/api/estoque/anvisa-pdf/route.ts: `export const runtime = 'nodejs'` (Edge não roda @react-pdf/renderer nem Supabase TCP). GET(request): autenticar via createClient()/getUser (só usuários da clínica); ler filtros da querystring; chamar listAnvisaTraceability(opts); `const buffer = await renderToBuffer(<AnvisaReportPdf .../>)` (import { renderToBuffer } from '@react-pdf/renderer'); retornar new Response(buffer, { headers: { 'Content-Type': 'application/pdf', 'Content-Disposition': 'attachment; filename="relatorio-anvisa.pdf"' } }).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "AnvisaReportPdf|anvisa-pdf/route" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@react-pdf/renderer" src/components/estoque/AnvisaReportPdf.tsx` presente
    - `grep -c "Grid\|display: 'grid'" src/components/estoque/AnvisaReportPdf.tsx` == 0 (Flexbox only)
    - `grep "runtime = 'nodejs'" src/app/api/estoque/anvisa-pdf/route.ts` presente
    - `grep "renderToBuffer" src/app/api/estoque/anvisa-pdf/route.ts` presente
    - `npx tsc --noEmit` sem erro nos novos arquivos
  </acceptance_criteria>
  <done>PDF ANVISA gerado server-side (Flexbox, nodejs runtime), com cabeçalho e rodapé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/estoque/anvisa-pdf | Autenticação obrigatória; dados só da clínica do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-19 | Information Disclosure | PDF vaza dados de paciente cross-tenant | mitigate | Rota autentica getUser + listAnvisaTraceability roda sob RLS clinic_id |
| T-17-20 | DoS | @react-pdf/renderer em Edge | mitigate | runtime nodejs explícito (Edge não suporta) |
</threat_model>

<verification>
`npx tsc --noEmit` limpo. Relatório renderiza; export baixa PDF válido (UAT).
</verification>

<success_criteria>
- Relatório ANVISA filtrável (produto/lote/paciente/período)
- Export PDF via @react-pdf/renderer server-side, Flexbox only, nodejs runtime
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-estoque-materiais/17-08-SUMMARY.md`
</output>
