---
phase: 17-estoque-materiais
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [EST-02]
files_modified:
  - src/components/prontuario/ProntuarioForm.tsx

must_haves:
  truths:
    - "Ao abrir o formulário de registro de atendimento, o dentista vê um seletor 'Serviço realizado' populado pelos serviços ATIVOS da clínica (listServices)"
    - "Ao selecionar um serviço que possui templates de materiais configurados (service_material_templates), a seção 'Materiais Utilizados' (D-22) passa a renderizar, com qtd editável e custo estimado de insumos"
    - "A seção 'Materiais Utilizados' permanece oculta quando nenhum serviço está selecionado ou quando o serviço selecionado não tem templates (comportamento return-null preservado)"
    - "O fluxo existente de registro (createMedicalRecord, refine 'ao menos um campo', gating canWrite) permanece inalterado — a mudança é puramente aditiva"
  artifacts:
    - path: "src/components/prontuario/ProntuarioForm.tsx"
      provides: "Seletor 'Serviço realizado' + estado selectedServiceId que alimenta <MaterialsUsedSection serviceId={selectedServiceId} />"
      contains: "listServices"
  key_links:
    - from: "src/components/prontuario/ProntuarioForm.tsx"
      to: "src/actions/services.ts (listServices)"
      via: "fetch-on-mount em useEffect (carrega serviços ativos para o Select)"
      pattern: "listServices"
    - from: "src/components/prontuario/ProntuarioForm.tsx"
      to: "src/components/estoque/MaterialsUsedSection.tsx"
      via: "prop serviceId alimentada pelo estado local selectedServiceId (não mais pelo prop dormante)"
      pattern: "MaterialsUsedSection serviceId=\\{selectedServiceId"
---

<objective>
Fechar o único gap da verificação da Fase 17: a seção "Materiais Utilizados" (D-22, UI-SPEC §6) está totalmente implementada em `MaterialsUsedSection.tsx` e corretamente renderizada dentro de `ProntuarioForm`, mas é **código morto na aplicação em produção** — `serviceId` nunca é fornecido por nenhum dos dois callers reais de `ProntuarioForm`, então `MaterialsUsedSection` sempre executa `return null`.

Este plano adiciona um seletor "Serviço realizado" (opcional) dentro do próprio `ProntuarioForm`, populado pela Server Action existente `listServices()` (construída na Fase 15, reusada no Plan 17-09). Ao escolher um serviço, o form guarda o `serviceId` em estado local e o repassa para `<MaterialsUsedSection serviceId={selectedServiceId} />`, que então revela os materiais do template. Isso torna D-22 observável para dentistas sem tocar os callers, o schema, nem o fluxo de submit.

Purpose: transformar um recurso já construído (mas inalcançável) em funcionalidade viva, satisfazendo o must-have de Plan 09 e a cobertura de UI de EST-02.
Output: `ProntuarioForm.tsx` modificado — seletor de serviço + estado + wiring. Nenhuma mudança de DB, migração, ou schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-estoque-materiais/17-CONTEXT.md
@.planning/phases/17-estoque-materiais/17-VERIFICATION.md
@.planning/phases/17-estoque-materiais/17-UI-SPEC.md
@.planning/phases/17-estoque-materiais/17-09-SUMMARY.md

<interfaces>
<!-- Contratos exatos que o executor precisa. Extraídos do código atual — NÃO explorar a base. -->

<!-- src/actions/services.ts — 'use server' + 'server-only'. Chamável de client component
     (mesmo padrão que MaterialsTemplateTab usa com listProducts). -->
```typescript
export async function listServices(): Promise<{
  success: boolean
  services?: Array<{
    id: string
    name: string
    code: string | null
    tuss_code: string | null
    description: string | null
    valor_particular: number
    account_id: string | null
    aliquota_iss_override: number | null
    item_lista_servico_override: string | null
    ativo: boolean
  }>
  error?: string
}>
```

<!-- src/components/estoque/MaterialsUsedSection.tsx — já implementado, NÃO alterar.
     Prop service?: string. Auto-oculta (return null) quando serviceId ausente OU
     quando o serviço não tem templates. Faz fetch próprio de listServiceMaterials+listProducts. -->
```typescript
interface MaterialsUsedSectionProps {
  serviceId?: string
  disabled?: boolean
}
export function MaterialsUsedSection({ serviceId, disabled }: MaterialsUsedSectionProps): JSX.Element | null
```

<!-- src/components/ui/select.tsx — wrapper @base-ui/react/select.
     IMPORTANTE (decisão registrada em STATE): base-ui Select.Root onValueChange recebe
     (value: T | null, eventDetails) — o handler deve tratar null.
     Padrão de uso confirmado em MaterialsTemplateTab.tsx (linhas 198-211): -->
```tsx
import {
  Select, SelectContent, SelectItem, SelectTrigger, SelectValue,
} from '@/components/ui/select'

<Select onValueChange={(v) => setX(v ?? undefined)} value={x ?? undefined}>
  <SelectTrigger className="w-full bg-background border-border">
    <SelectValue placeholder="Selecione...">
      {options.find((o) => o.id === x)?.name}
    </SelectValue>
  </SelectTrigger>
  <SelectContent>
    {options.map((o) => (
      <SelectItem key={o.id} value={o.id}>{o.name}</SelectItem>
    ))}
  </SelectContent>
</Select>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seletor "Serviço realizado" + estado selectedServiceId em ProntuarioForm, alimentando MaterialsUsedSection</name>
  <files>src/components/prontuario/ProntuarioForm.tsx</files>

  <read_first>
    - src/components/prontuario/ProntuarioForm.tsx (arquivo a modificar — form atual: RHF + createMedicalRecord + <MaterialsUsedSection serviceId={serviceId} /> na linha ~150)
    - src/components/estoque/MaterialsUsedSection.tsx (consumidor — confirmar prop service?: string e comportamento return-null; NÃO alterar)
    - src/components/estoque/MaterialsTemplateTab.tsx (linhas 29-35 imports do Select; 198-211 padrão de uso do Select com base-ui, incluindo fetch-on-mount de listProducts em client component)
    - src/actions/services.ts (linhas 1-79 — confirmar 'use server' e o shape de retorno de listServices: { success, services?: [{ id, name, ativo, ... }], error? })
  </read_first>

  <action>
    Modificar SOMENTE `src/components/prontuario/ProntuarioForm.tsx`. Mudança 100% aditiva — preservar TODO o comportamento existente (schema prontuarioFormSchema com refine "Ao menos um campo deve ser preenchido", createMedicalRecord no onSubmit, window.location.reload, os três Textarea de diagnosis/treatment_plan/prescription, o Button de submit).

    1. Imports novos no topo:
       - `import { listServices } from '@/actions/services'`
       - `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'`
       - Adicionar `useEffect` ao import existente de `react` (já importa useState, useTransition).

    2. Tipo local para a opção do seletor (logo antes do componente):
       `type ServiceOption = { id: string; name: string }`

    3. Dentro do componente `ProntuarioForm`, adicionar estado:
       - `const [services, setServices] = useState<ServiceOption[]>([])`
       - `const [selectedServiceId, setSelectedServiceId] = useState<string | undefined>(serviceId)`
         (inicializa com o prop `serviceId` existente para preservar retrocompatibilidade — se um caller futuro passar um serviceId, ele vira o valor inicial do seletor).

    4. Carregar serviços ATIVOS on-mount (mesmo padrão fetch-on-mount de MaterialsTemplateTab):
       ```tsx
       useEffect(() => {
         listServices().then((result) => {
           if (result.success) {
             setServices(
               (result.services ?? [])
                 .filter((s) => s.ativo)
                 .map((s) => ({ id: s.id, name: s.name }))
             )
           }
         })
       }, [])
       ```

    5. Renderizar o seletor DENTRO do <form>, logo ANTES do comentário `{/* Materiais Utilizados (D-22) ... */}` e do `<MaterialsUsedSection ... />` (ou seja, após o FormField de "prescription"). Usar um bloco simples (não é um campo RHF — é UI-only, não faz parte do submit):
       ```tsx
       <div className="space-y-1.5">
         <label className="text-sm font-semibold">Serviço realizado (opcional)</label>
         <Select
           value={selectedServiceId ?? undefined}
           onValueChange={(v) => setSelectedServiceId(v ?? undefined)}
         >
           <SelectTrigger className="w-full bg-background border-border">
             <SelectValue placeholder="Selecione o serviço para exibir os materiais">
               {services.find((s) => s.id === selectedServiceId)?.name}
             </SelectValue>
           </SelectTrigger>
           <SelectContent>
             {services.map((s) => (
               <SelectItem key={s.id} value={s.id}>
                 {s.name}
               </SelectItem>
             ))}
           </SelectContent>
         </Select>
       </div>
       ```

    6. Trocar o wiring da seção de materiais para usar o ESTADO em vez do prop dormante:
       - Antes: `<MaterialsUsedSection serviceId={serviceId} />`
       - Depois: `<MaterialsUsedSection serviceId={selectedServiceId} />`

    Não adicionar o serviceId ao payload de createMedicalRecord (D-22: a seção é informativa; a baixa real já ocorre server-side via appointments.ts → stock-draws.ts, verificado como SC #2). Não alterar MaterialsUsedSection.tsx. Não tocar os callers (prontuario/page.tsx nem pacientes/[id]/page.tsx) — o seletor é interno ao form, logo os callers continuam invocando `<ProntuarioForm patientId={id} />` sem mudança.
  </action>

  <acceptance_criteria>
    - `grep -n "import { listServices } from '@/actions/services'" src/components/prontuario/ProntuarioForm.tsx` → 1 match
    - `grep -n "from '@/components/ui/select'" src/components/prontuario/ProntuarioForm.tsx` → 1 match (importa Select, SelectContent, SelectItem, SelectTrigger, SelectValue)
    - `grep -n "Serviço realizado" src/components/prontuario/ProntuarioForm.tsx` → 1 match (label do seletor)
    - `grep -n "selectedServiceId" src/components/prontuario/ProntuarioForm.tsx` → >= 3 matches (estado + onValueChange + prop de MaterialsUsedSection)
    - `grep -nE "MaterialsUsedSection serviceId=\{selectedServiceId" src/components/prontuario/ProntuarioForm.tsx` → 1 match (wiring pelo estado, NÃO mais pelo prop `serviceId` cru)
    - `grep -nE "\.filter\(\(s\) => s\.ativo\)" src/components/prontuario/ProntuarioForm.tsx` → 1 match (apenas serviços ativos no seletor)
    - `grep -n "createMedicalRecord" src/components/prontuario/ProntuarioForm.tsx` → ainda presente (fluxo de submit preservado)
    - `grep -n "Ao menos um campo deve ser preenchido" src/components/prontuario/ProntuarioForm.tsx` → ainda presente (refine preservado)
    - `npx tsc --noEmit 2>&1 | grep -i "prontuario/ProntuarioForm"` → 0 matches (sem novos erros de tipo neste arquivo)
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "ProntuarioForm" || echo "TSC CLEAN for ProntuarioForm"</automated>
  </verify>

  <done>
    ProntuarioForm exibe um seletor "Serviço realizado" com os serviços ativos da clínica; selecionar um serviço com templates faz `MaterialsUsedSection` renderizar (qtd editável + custo estimado); nenhum serviço selecionado ou serviço sem templates mantém a seção oculta; createMedicalRecord/refine/canWrite intactos; `npx tsc --noEmit` sem novos erros no arquivo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (ProntuarioForm) → Server Actions | `listServices()`, e (via MaterialsUsedSection) `listServiceMaterials(serviceId)` + `listProducts()` cruzam o boundary; o `serviceId` é escolhido no cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-30 | Tampering | `serviceId` passado do cliente para `listServiceMaterials` | mitigate | `service_material_templates` tem RLS `USING (clinic_id = get_my_tenant_id())` — um serviceId de outro tenant retorna 0 linhas (MaterialsUsedSection então faz `return null`); nenhum dado cross-tenant vaza. Sem novo código de autorização necessário — herda a RLS já verificada na Fase 17. |
| T-17-31 | Information Disclosure | lista de serviços no seletor | accept | `listServices()` chama `getActor()` e roda sob RLS de `services` (tenant-scoped); só retorna serviços do próprio tenant. Nomes de serviço não são PII. |
| T-17-32 | Tampering / Repudiation | custo de insumos exibido no prontuário | accept | O valor é meramente informativo no cliente (T-17-22 já aceito). A baixa real de estoque NÃO parte desta UI — ocorre server-side em `appointments.ts → drawMaterialsForProcedures` ao concluir o procedimento (SC #2 verificado). Nenhum write path é introduzido por este plano. |
</threat_model>

<verification>
- `npx tsc --noEmit` não introduz novos erros em `src/components/prontuario/ProntuarioForm.tsx` (erros pré-existentes em `__tests__`/`faturamento`/`financeiro` são fora de escopo).
- `npx vitest run` continua verde (nenhum teste referencia diretamente ProntuarioForm; mudança é aditiva e não altera contratos de action).
- Inspeção manual/grep: seção "Materiais Utilizados" agora alcançável — `MaterialsUsedSection` recebe `selectedServiceId` do estado, não mais o prop dormante.
</verification>

<success_criteria>
- Gap da 17-VERIFICATION.md fechado: a truth "A seção 'Materiais Utilizados' no prontuário lista os materiais do template com qtd editável e custo estimado (D-22)" torna-se observável na aplicação viva — um dentista seleciona o serviço realizado e os materiais aparecem.
- Nenhuma regressão no fluxo de registro de atendimento (createMedicalRecord, refine de campo mínimo, gating canWrite).
- Zero mudanças de DB/schema/migração; um único arquivo modificado (`ProntuarioForm.tsx`).
</success_criteria>

<output>
After completion, create `.planning/phases/17-estoque-materiais/17-10-SUMMARY.md`
</output>
