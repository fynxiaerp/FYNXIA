---
phase: 17-estoque-materiais
plan: 09
type: execute
wave: 4
depends_on: [17-03, 17-05]
files_modified:
  - src/app/(dashboard)/config/servicos/page.tsx
  - src/components/config/ServiceForm.tsx
  - src/components/estoque/MaterialsTemplateTab.tsx
  - src/components/estoque/MaterialsUsedSection.tsx
  - src/components/prontuario/ProntuarioForm.tsx
autonomous: true
requirements:
  - EST-02

must_haves:
  truths:
    - "Admin gerencia o catálogo de serviços em /config/servicos (listar, criar, editar)"
    - "O ServiceForm tem uma aba 'Materiais' onde o admin associa produto + qtd padrão ao serviço (D-21)"
    - "A aba Materiais adiciona/remove templates via inline row (não dialog)"
    - "A seção 'Materiais Utilizados' no prontuário lista os materiais do template com qtd editável e custo estimado (D-22)"
    - "Se o serviço não tem templates, a seção de materiais não é renderizada"
  artifacts:
    - path: "src/app/(dashboard)/config/servicos/page.tsx"
      provides: "Catálogo de serviços (RSC)"
      contains: "listServices"
    - path: "src/components/estoque/MaterialsTemplateTab.tsx"
      provides: "Aba Materiais no ServiceForm (D-21)"
      contains: "addServiceMaterial"
    - path: "src/components/estoque/MaterialsUsedSection.tsx"
      provides: "Seção de materiais no prontuário (D-22)"
      contains: "listServiceMaterials"
  key_links:
    - from: "src/components/estoque/MaterialsTemplateTab.tsx"
      to: "src/actions/service-material-templates.ts"
      via: "addServiceMaterial / removeServiceMaterial / listServiceMaterials"
      pattern: "addServiceMaterial|removeServiceMaterial"
    - from: "src/components/estoque/MaterialsUsedSection.tsx"
      to: "src/actions/service-material-templates.ts"
      via: "listServiceMaterials"
      pattern: "listServiceMaterials"
---

<objective>
Criar o catálogo de serviços em /config/servicos com um ServiceForm que inclui a aba "Materiais" (templates de consumo), e adicionar a seção "Materiais Utilizados" ao prontuário.

Purpose: entregar a UI de configuração e visualização dos templates de consumo (EST-02 D-21/D-22), fechando o laço da baixa automática (backend no Plan 05).

Output: /config/servicos + ServiceForm + MaterialsTemplateTab + MaterialsUsedSection + integração no ProntuarioForm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-estoque-materiais/17-UI-SPEC.md
@src/actions/services.ts
@src/actions/service-material-templates.ts
@src/actions/products.ts
@src/components/prontuario/ProntuarioForm.tsx
</context>

<interfaces>
<!-- services.ts (existente): listServices, createService, updateService, deactivateService. -->
<!-- service-material-templates.ts (Plan 05): listServiceMaterials, addServiceMaterial, removeServiceMaterial. -->
<!-- products.ts (Plan 03): listProducts (para o Select de produto na aba Materiais). -->
<!-- NÃO existe /config/servicos nem ServiceForm hoje — criar ambos. -->
<!-- Tabs shadcn; @base-ui Button render-prop; RHF v7 + Zod sem .default(). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Catálogo de serviços /config/servicos + ServiceForm com Tabs</name>
  <files>src/app/(dashboard)/config/servicos/page.tsx, src/components/config/ServiceForm.tsx</files>
  <read_first>
    - src/actions/services.ts (listServices, createService, updateService — assinaturas exatas dos campos do serviço)
    - src/app/(dashboard)/config/unidades/page.tsx + src/components/config/UnitsManager.tsx (padrão de página de config com manager client + dialog)
    - .planning/phases/17-estoque-materiais/17-UI-SPEC.md §"5. Aba Materiais Utilizados" (posição da aba, label "Materiais")
  </read_first>
  <action>
Criar config/servicos/page.tsx (RSC): PageHeader "Serviços". Chamar listServices() server-side, passar para um manager client (pode ser inline no ServiceForm ou uma ServicesManager simples) com botão "Novo Serviço" que abre ServiceForm em Dialog. Derivar role (admin/superadmin write).

Criar ServiceForm.tsx ('use client'): Dialog + Tabs shadcn. Aba 1 "Dados" com os campos do serviço (RHF + zodResolver do schema em src/lib/validators/service.ts — reusar). Aba 2 "Materiais" renderiza <MaterialsTemplateTab serviceId={service?.id} /> (só habilitada em modo edição, quando o serviço já existe e tem id). Submit da aba Dados chama createService/updateService. Nota: em modo create, exibir aviso na aba Materiais "Salve o serviço primeiro para configurar materiais."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "config/servicos/page.tsx|ServiceForm" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "listServices" "src/app/(dashboard)/config/servicos/page.tsx"` presente
    - `grep "Tabs" src/components/config/ServiceForm.tsx` presente
    - `grep "MaterialsTemplateTab" src/components/config/ServiceForm.tsx` renderizada na aba Materiais
    - `grep "createService\|updateService" src/components/config/ServiceForm.tsx` no submit
    - `npx tsc --noEmit` sem erro nos novos arquivos
  </acceptance_criteria>
  <done>Catálogo de serviços com ServiceForm em abas (Dados + Materiais).</done>
</task>

<task type="auto">
  <name>Task 2: MaterialsTemplateTab (aba Materiais)</name>
  <files>src/components/estoque/MaterialsTemplateTab.tsx</files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-UI-SPEC.md §"5. Aba Materiais Utilizados" (mini-tabela, inline row, empty state)
    - src/actions/service-material-templates.ts (listServiceMaterials, addServiceMaterial, removeServiceMaterial)
    - src/actions/products.ts (listProducts para Select)
    - src/lib/validators/product.ts (serviceMaterialTemplateSchema)
  </read_first>
  <action>
Criar MaterialsTemplateTab.tsx ('use client'). Props: serviceId. Ao montar, listServiceMaterials(serviceId). Título "Materiais consumidos neste serviço" (h3 font-display) + descrição "Defina os insumos padrão baixados automaticamente ao concluir este serviço." Mini-tabela: Produto | Qtd Padrão | Ações(remover via removeServiceMaterial). Botão "Adicionar Material" (Button variant outline, ícone Plus) → inline row expansion (NÃO dialog): Select de produto (listProducts) + Input qtd; ao confirmar chama addServiceMaterial({ service_id, product_id, qtd_padrao }) e recarrega a lista. Empty state inline: "Nenhum material configurado. Adicione para habilitar a baixa automática." (sem ícone).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "MaterialsTemplateTab" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "listServiceMaterials" src/components/estoque/MaterialsTemplateTab.tsx` presente
    - `grep "addServiceMaterial" src/components/estoque/MaterialsTemplateTab.tsx` presente
    - `grep "removeServiceMaterial" src/components/estoque/MaterialsTemplateTab.tsx` presente
    - `grep "Nenhum material configurado" src/components/estoque/MaterialsTemplateTab.tsx` — empty state
    - `npx tsc --noEmit` sem erro no arquivo
  </acceptance_criteria>
  <done>Aba Materiais adiciona/remove templates via inline row.</done>
</task>

<task type="auto">
  <name>Task 3: Seção Materiais Utilizados no prontuário</name>
  <files>src/components/estoque/MaterialsUsedSection.tsx, src/components/prontuario/ProntuarioForm.tsx</files>
  <read_first>
    - .planning/phases/17-estoque-materiais/17-UI-SPEC.md §"6. Seção Materiais Utilizados — Prontuário" (estrutura Card, regras de interação, custo estimado)
    - src/actions/service-material-templates.ts (listServiceMaterials)
    - src/actions/products.ts (listProducts para custo_medio e status — custo estimado)
    - src/components/prontuario/ProntuarioForm.tsx (onde inserir a seção)
  </read_first>
  <action>
Criar MaterialsUsedSection.tsx ('use client'). Props: serviceId (opcional), disabled (após confirmação). Ao montar com serviceId, listServiceMaterials(serviceId). Se vazio → NÃO renderiza nada (retorna null). Card shadcn: CardHeader>CardTitle "Materiais Utilizados" (font-display). CardContent space-y-3: uma linha por material — span nome flex-1, Input type="number" w-20 defaultValue={qtd_padrao} min=0 step=0.01, span unidade_medida. Se produto status 'critico'/'negativo' exibir Badge ("Estoque Baixo"/"Saldo Negativo") ao lado do nome. Rodapé: "Custo estimado de insumos: R$ X,XX" (font-semibold tabular-nums), calculado como Σ(qtd × custo_medio). Após confirmação (disabled=true): inputs viram read-only.

Integrar no ProntuarioForm.tsx: renderizar <MaterialsUsedSection serviceId={...} /> na área apropriada do form (após seleção de procedimento/serviço, se aplicável ao fluxo atual). Se o ProntuarioForm hoje não tem seleção de serviço, renderizar a seção de forma condicional a uma prop serviceId passada pelo parent (page), mantendo a seção auto-ocultável quando não há templates. NÃO quebrar o fluxo existente de createMedicalRecord.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "MaterialsUsedSection|ProntuarioForm" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "listServiceMaterials" src/components/estoque/MaterialsUsedSection.tsx` presente
    - `grep "Custo estimado de insumos" src/components/estoque/MaterialsUsedSection.tsx` presente
    - `grep "return null" src/components/estoque/MaterialsUsedSection.tsx` — auto-oculta sem templates
    - `grep "MaterialsUsedSection" src/components/prontuario/ProntuarioForm.tsx` — integrada
    - `npx tsc --noEmit` sem erro; fluxo de prontuário existente intacto
  </acceptance_criteria>
  <done>Seção de materiais no prontuário com qtd editável + custo estimado, auto-ocultável.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → service-material-templates actions | Role gate admin/superadmin no Server Action (Plan 05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-21 | Elevation of Privilege | Não-admin edita templates | mitigate | Gate WRITER_ROLES em addServiceMaterial/removeServiceMaterial (Plan 05) |
| T-17-22 | Tampering | Custo estimado manipulado no client | accept | Valor é informativo (D-22); baixa real usa custo snapshot server-side (Plan 05) |
</threat_model>

<verification>
`npx tsc --noEmit` limpo. Aba Materiais salva templates; seção do prontuário exibe e auto-oculta (UAT).
</verification>

<success_criteria>
- /config/servicos com catálogo + ServiceForm em abas
- Aba Materiais adiciona/remove templates (inline row)
- Seção Materiais Utilizados no prontuário: qtd editável, custo estimado, auto-oculta sem templates
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-estoque-materiais/17-09-SUMMARY.md`
</output>
