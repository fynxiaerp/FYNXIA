---
phase: 08-documentos-assinatura-icp-brasil
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/types/database.types.ts
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "The three Phase 8 migrations are applied to the live Supabase project jqjwyqlbbuqnrffdnlpp"
    - "src/types/database.types.ts contains document_templates, documents, document_versions types"
    - "tsc stays green after type regeneration"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "regenerated Supabase types including the three new tables"
      contains: "document_versions"
  key_links:
    - from: "supabase/migrations/20260615000100_document_tables.sql"
      to: "live DB jqjwyqlbbuqnrffdnlpp"
      via: "supabase db push"
      pattern: "document_versions"
---

<objective>
Apply the Phase 8 migrations to the live Supabase project and regenerate TypeScript types.
This is the SINGLE [BLOCKING] checkpoint of the phase — it requires a human because the
Supabase CLI is frequently logged into the wrong account (the nexus-* org) and must be
re-authenticated against org kczvihafddupruvsrrsc / project jqjwyqlbbuqnrffdnlpp before push.

Purpose: Make the document tables real so the Wave 4 UI/actions can query them with typed
clients. Without this, generateDocument/signDocument cannot run.
Output: Migrations applied; src/types/database.types.ts regenerated with the three new tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-documentos-assinatura-icp-brasil/08-RESEARCH.md
@supabase/migrations/20260615000100_document_tables.sql
@supabase/migrations/20260615000200_document_rls.sql
@supabase/migrations/20260615000300_documents_bucket.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Re-auth, supabase db push, regenerate types</name>
  <files>src/types/database.types.ts</files>
  <action>
    Human runs the Supabase re-auth + db push + gen types sequence detailed in how-to-verify below.
    This is a checkpoint:human-action because the Supabase CLI is frequently logged into the wrong
    account; the human MUST confirm project jqjwyqlbbuqnrffdnlpp is active before pushing DDL, then
    apply the three Phase 8 migrations and regenerate src/types/database.types.ts.
  </action>
  <what-built>
    Three migrations written in Plan 02 (document_templates, documents, document_versions + RLS
    + private documents-pdf bucket) are ready to apply to the live project.
  </what-built>
  <how-to-verify>
    Run these from the repo root, in order. Stop and report if any step fails.

    1. Confirm the CLI is on the RIGHT account/project (recurring gotcha — MEMORY.md):
         supabase projects list
       The output MUST show project `jqjwyqlbbuqnrffdnlpp` with a ● (linked/active marker).
       If it is NOT shown (CLI logged into a nexus-* account), re-authenticate:
         supabase login
       then re-run `supabase projects list` and confirm jqjwyqlbbuqnrffdnlpp appears.

    2. Apply the migrations to the live DB:
         supabase db push
       Expect the three 20260615000100/000200/000300 migrations to apply with no error.

    3. Regenerate the typed schema:
         supabase gen types typescript --project-id jqjwyqlbbuqnrffdnlpp > src/types/database.types.ts

    4. Verify types + grep:
         npx tsc --noEmit            (must exit 0)
       and confirm src/types/database.types.ts now contains `document_templates`, `documents`,
       and `document_versions`.
  </how-to-verify>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <resume-signal>Type "approved" once db push succeeded, types regenerated, and tsc is green. Report any error otherwise.</resume-signal>
  <done>Three migrations applied to jqjwyqlbbuqnrffdnlpp; src/types/database.types.ts includes the three new tables; tsc exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → live DB | db push applies DDL to production project; wrong-account push is the primary risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | db push to wrong project/org | mitigate | Mandatory `supabase projects list` verification of jqjwyqlbbuqnrffdnlpp ● before push; human gate |
| T-08-10 | Denial of Service | failed/partial migration | accept | Migrations are additive (new tables only); no data migration; re-runnable |
</threat_model>

<verification>
- `supabase projects list` shows jqjwyqlbbuqnrffdnlpp ●
- `supabase db push` applies the three migrations
- `src/types/database.types.ts` regenerated, contains the three tables
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Live DB has document_templates, documents, document_versions with RLS + private bucket
- Types regenerated; tsc green
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentos-assinatura-icp-brasil/08-03-SUMMARY.md`
</output>
