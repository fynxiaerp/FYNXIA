---
phase: 08-documentos-assinatura-icp-brasil
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/icp/sign-document.test.ts
  - src/__tests__/documents/template-engine.test.ts
  - src/__tests__/documents/actions.test.ts
  - src/__tests__/migrations/phase8.test.ts
  - src/__tests__/pdf/documento.test.ts
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "Five Phase 8 test files exist and run under vitest"
    - "The real sign→verify crypto test against test-cert.pfx passes immediately (it exercises node-forge, not new code)"
    - "Source-inspection tests for migrations/actions/PDF/template-engine FAIL until Wave 2 implements them (RED)"
  artifacts:
    - path: "src/__tests__/icp/sign-document.test.ts"
      provides: "DOC-02 real RSA sign→verify round-trip against Phase 7 .pfx fixture"
      contains: "test-cert.pfx"
    - path: "src/__tests__/documents/template-engine.test.ts"
      provides: "DOC-01 fillTemplate + detectVariables behavior tests"
      contains: "detectVariables"
    - path: "src/__tests__/documents/actions.test.ts"
      provides: "DOC-01/02/03 Server Action source-inspection"
      contains: "document-templates"
    - path: "src/__tests__/migrations/phase8.test.ts"
      provides: "DOC-01/03 migration source-inspection"
      contains: "document_versions"
    - path: "src/__tests__/pdf/documento.test.ts"
      provides: "DOC-01/02 DocumentoPDF source-inspection"
      contains: "DocumentoPDF"
  key_links:
    - from: "src/__tests__/icp/sign-document.test.ts"
      to: "src/__tests__/icp/fixtures/test-cert.pfx"
      via: "readFileSync of the Phase 7 fixture"
      pattern: "fixtures/test-cert\\.pfx"
---

<objective>
Create the Wave 0 RED test scaffolds for Phase 8. These five test files encode the
expected behavior of the document engine BEFORE any production code exists, satisfying
the Nyquist validation requirement (every DOC requirement has an automated test).

The one exception is the crypto sign→verify test (`sign-document.test.ts`): it exercises
node-forge + the Phase 7 `.pfx` fixture directly, so it is GREEN from the first run —
it locks down the exact signing algorithm that `src/lib/icp/sign-document.ts` (Plan 02)
must match. The four source-inspection tests are RED until Plan 02 writes the migrations,
actions, PDF component, and template engine.

Purpose: Lock the contracts (file paths, exported symbols, SQL shape, signing algorithm)
so Plan 02 implements against a fixed target with no interpretation.
Output: 5 test files; crypto test GREEN, 4 source-inspection tests RED (expected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentos-assinatura-icp-brasil/08-RESEARCH.md
@.planning/phases/08-documentos-assinatura-icp-brasil/08-VALIDATION.md

<interfaces>
<!-- The signing algorithm the test pins (and Plan 02 must match exactly).
     Verified in 08-RESEARCH.md against node-forge 1.4.0 + test-cert.pfx (password 'test1234'). -->

Existing fixture: src/__tests__/icp/fixtures/test-cert.pfx (2048-bit RSA A1 test cert, password 'test1234')
Existing pattern to mirror for PFX load: src/lib/icp/pfx-metadata.ts
  - forge.util.binary.raw.encode(new Uint8Array(pfxBuffer)) → forge.asn1.fromDer → forge.pkcs12.pkcs12FromAsn1(asn1, false, password)
  - CERT_BAG_OID = forge.pki.oids.certBag ?? '1.2.840.113549.1.12.10.1.3'
  - KEY_BAG_OID  = forge.pki.oids.pkcs8ShroudedKeyBag ?? '1.2.840.113549.1.12.10.1.2'
  - thumbprint: forge.pki.certificateToAsn1(cert)  (NOT cert.toAsn1())

Signing contract (Plan 02 src/lib/icp/sign-document.ts must produce these):
  - sha256Hex   = createHash('sha256').update(pdfBuffer).digest('hex')   // 64 hex chars
  - md = forge.md.sha256.create(); md.update(pdfBuffer.toString('binary'), 'raw')
  - signatureB64 = forge.util.encode64(privateKey.sign(md))               // 344 chars (2048-bit RSA)
  - verify: FRESH md2; cert.publicKey.verify(md2.digest().bytes(), sig) === true

Test infra (Phase 7, already present): vitest.config.ts, src/__tests__/setup.ts, __mocks__/server-only.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crypto sign→verify test (GREEN) + template-engine test (RED)</name>
  <files>src/__tests__/icp/sign-document.test.ts, src/__tests__/documents/template-engine.test.ts</files>
  <read_first>
    - src/__tests__/icp/pfx-metadata.test.ts (existing fixture-loading test style + setup)
    - src/lib/icp/pfx-metadata.ts (exact PFX load sequence + OID fallbacks)
    - .planning/phases/08-documentos-assinatura-icp-brasil/08-RESEARCH.md (Code Example 2 + Example 3)
  </read_first>
  <action>
    Create `src/__tests__/icp/sign-document.test.ts`. This test uses node-forge + the
    existing `src/__tests__/icp/fixtures/test-cert.pfx` directly (NOT importing Plan 02 code yet —
    it pins the algorithm). Mirror 08-RESEARCH Code Example 2 exactly:
      - readFileSync(resolve(process.cwd(), 'src/__tests__/icp/fixtures/test-cert.pfx'))
      - load PFX with password 'test1234' via the pfx-metadata.ts sequence
      - extract cert + privateKey via CERT_BAG_OID / KEY_BAG_OID with the documented fallbacks
      - const fakePdf = Buffer.from('%PDF-1.4 fake content')
      - assert createHash('sha256').update(fakePdf).digest('hex') has length 64
      - sign: md.update(fakePdf.toString('binary'),'raw'); sigB64 = forge.util.encode64(privateKey.sign(md)); assert length 344
      - verify with a FRESH md2: cert.publicKey.verify(md2.digest().bytes(), sig) === true
      - thumbprint: forge.pki.certificateToAsn1(cert) → sha1 → assert /^[0-9a-f]{40}$/
      - Add a SECOND it() asserting Node crypto SHA-256 hex === forge SHA-256 hex for the SAME buffer
        (forge: md.update(fakePdf.toString('binary'),'raw'); md.digest().toHex()).
    This file is GREEN from first run — it locks the algorithm Plan 02 must reproduce.

    Create `src/__tests__/documents/template-engine.test.ts` (RED — imports code Plan 02 writes).
    Import `{ fillTemplate, detectVariables }` from `@/lib/documents/template-engine`.
      - detectVariables('Olá {{nome_paciente}}, hoje é {{data}}. {{nome_paciente}} de novo')
        → ['nome_paciente','data'] (deduped, order of first appearance)
      - fillTemplate('Olá {{nome_paciente}}', { nome_paciente: 'Ana' }) → 'Olá Ana'
      - fillTemplate with a missing key leaves the placeholder verbatim:
        fillTemplate('{{x}} {{y}}', { x: 'A' }) → 'A {{y}}'
      - fillTemplate replaces ALL occurrences of the same var (global).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/icp/sign-document.test.ts</automated>
  </verify>
  <done>sign-document.test.ts PASSES (locks algorithm). template-engine.test.ts FAILS with module-not-found on @/lib/documents/template-engine (expected RED).</done>
</task>

<task type="auto">
  <name>Task 2: Source-inspection RED scaffolds (migrations, actions, PDF)</name>
  <files>src/__tests__/migrations/phase8.test.ts, src/__tests__/documents/actions.test.ts, src/__tests__/pdf/documento.test.ts</files>
  <read_first>
    - src/__tests__/migrations (any existing migration source-inspection test, if present, for style)
    - .planning/phases/08-documentos-assinatura-icp-brasil/08-RESEARCH.md (Patterns 4, 5, 6 — exact SQL strings)
  </read_first>
  <action>
    All three use `readFileSync` + `toMatch`/`toContain` against files Plan 02 will create.
    They are RED now (files do not exist) → wrap each readFileSync target so the test fails
    clearly (do NOT use existsSync-guards that skip — the test MUST fail until implemented).

    `src/__tests__/migrations/phase8.test.ts` — read the three migration files
    (use Glob/readdir on supabase/migrations for `*document_tables.sql`, `*document_rls.sql`,
    `*documents_bucket.sql` so timestamp prefixes don't break the test). Assert:
      - document_tables.sql: `CREATE TABLE public.document_templates`, `CREATE TABLE public.documents`,
        `CREATE TABLE public.document_versions`, `idx_document_templates_clinic`, `idx_documents_clinic`,
        `idx_doc_versions_clinic`, `UNIQUE (document_id, version_number)`,
        `status` CHECK with 'draft' and 'signed', `is_content_encrypted`.
      - document_rls.sql: `ENABLE ROW LEVEL SECURITY` for all three tables; document_versions has a
        `FOR INSERT` policy with `WITH CHECK`; assert the file contains NO `FOR UPDATE` and NO
        `FOR DELETE` policy on document_versions (regex: no `ON public.document_versions ... FOR (UPDATE|DELETE)`);
        write policies use `get_my_tenant_id()` and `WITH CHECK`.
      - documents_bucket.sql: `documents-pdf`, `public` false.

    `src/__tests__/documents/actions.test.ts` — read src/actions/document-templates.ts and
    src/actions/documents.ts as text. Assert:
      - both start with `'use server'`
      - document-templates.ts exports async `createTemplate`, `updateTemplate`, `deleteTemplate`, `listTemplates`
      - documents.ts exports async `generateDocument`, `signDocument`, `verifyDocumentSignature`, `listDocumentVersions`
      - every mutation body contains `assertNotReadOnly` (regex count: at least the mutating ones)
      - documents.ts references `createAdminClient`, `decrypt`, `renderToBuffer`, and `documents-pdf`.

    `src/__tests__/pdf/documento.test.ts` — read src/components/pdf/DocumentoPDF.tsx as text. Assert:
      - exports `DocumentoPDF`
      - `Font.register` present
      - uses Flexbox (`flexDirection`) and contains NO `display: 'grid'` / `display:'grid'`
      - no `'use client'`
      - props include a signature block field (`signatureBlock`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/migrations/phase8.test.ts src/__tests__/documents/actions.test.ts src/__tests__/pdf/documento.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All three files exist; tsc has no parse errors in them; they FAIL (RED) because Plan 02 targets don't exist yet. `npx tsc --noEmit` still exits 0 (test files are valid TS; missing runtime files are read at test-time, not import-time — use readFileSync not import for the source-inspection targets).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → fixture file | Reads the Phase 7 test .pfx from disk; fixture is a synthetic non-production cert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | test-cert.pfx in repo | accept | Synthetic test cert (password 'test1234'), no production key material; already committed in Phase 7 |
| T-08-02 | Tampering | sign-document.test.ts pins algorithm | mitigate | Test asserts exact 344-char signature + verify=true, so Plan 02 cannot silently diverge from the verified algorithm |
</threat_model>

<verification>
- `npx vitest run src/__tests__/icp/sign-document.test.ts` → GREEN
- `npx vitest run src/__tests__/documents/ src/__tests__/migrations/phase8.test.ts src/__tests__/pdf/documento.test.ts` → RED (expected, targets not built)
- `npx tsc --noEmit` → exit 0 (test files use readFileSync for unbuilt targets; only template-engine.test.ts imports @/lib/documents/template-engine — that import is allowed to fail at runtime, not typecheck, because it is a test that vitest runs; confirm tsc stays green, and if the unresolved import breaks tsc, add a `// @ts-expect-error not-yet-implemented` on the import line).
</verification>

<success_criteria>
- 5 Phase 8 test files created
- Crypto sign→verify test passes against test-cert.pfx
- 4 source-inspection/behavior tests are RED pending Plan 02
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentos-assinatura-icp-brasil/08-01-SUMMARY.md`
</output>
