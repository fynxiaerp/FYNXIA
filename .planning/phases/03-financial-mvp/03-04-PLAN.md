---
phase: 03-financial-mvp
plan: "04"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - next.config.ts
  - vercel.json
  - src/components/pdf/ReceiboPDF.tsx
  - src/app/api/financeiro/charges/[id]/recibo.pdf/route.ts
  - src/emails/CollectionReminderEmail.tsx
  - src/lib/collection/ruler.ts
  - src/actions/collection-ruler.ts
  - src/app/api/cron/collection-ruler/route.ts
  - src/app/(dashboard)/clinica/financeiro/regua-de-cobranca/page.tsx
  - src/components/financeiro/CollectionRulerForm.tsx
  - src/__tests__/collection/ruler.test.ts
  - src/__tests__/pdf/recibo.test.ts
  - src/__tests__/config/security-headers.test.ts
autonomous: true
requirements: [FIN-07, FIN-08, SEC-06]
user_setup:
  - service: vercel
    why: "Cron secret + cron schedule for the collection ruler (D-09)"
    env_vars:
      - name: CRON_SECRET
        source: "Self-generated; set in Vercel Project → Settings → Environment Variables (Vercel injects as Authorization: Bearer)"

must_haves:
  truths:
    - "A daily Vercel Cron endpoint scans overdue/due receivables and sends a Resend email reminder per active rule"
    - "Reminders are idempotent per (receivable + milestone) — no duplicate sends"
    - "The cron endpoint rejects requests without the correct CRON_SECRET bearer token"
    - "Admin can configure the collection ruler (due-date reminder, overdue cadence N days)"
    - "Receptionist or admin can download a PDF receipt for a paid charge (FIN-08, ROADMAP Phase 3 Success Criterion 4)"
    - "All HTTP responses carry CSP, HSTS, X-Frame-Options, X-Content-Type-Options (SEC-06)"
  artifacts:
    - path: "next.config.ts"
      provides: "Security headers (SEC-06)"
      contains: "Content-Security-Policy"
    - path: "src/app/api/cron/collection-ruler/route.ts"
      provides: "Daily collection ruler cron (FIN-07, D-09)"
      contains: "CRON_SECRET"
    - path: "src/components/pdf/ReceiboPDF.tsx"
      provides: "PDF receipt (FIN-08)"
      contains: "Font.register"
    - path: "src/app/api/financeiro/charges/[id]/recibo.pdf/route.ts"
      provides: "Role-gated PDF receipt route — allows admin, dentist, receptionist (ROADMAP SC-4)"
      contains: "receptionist"
    - path: "src/lib/collection/ruler.ts"
      provides: "Pure ruler-selection + milestone logic (idempotency key)"
      contains: "milestone"
  key_links:
    - from: "src/app/api/cron/collection-ruler/route.ts"
      to: "public.collection_log"
      via: "idempotency check by (receivable_id, milestone, channel)"
      pattern: "collection_log"
    - from: "src/app/api/cron/collection-ruler/route.ts"
      to: "Resend"
      via: "CollectionReminderEmail send"
      pattern: "resend.emails.send|CollectionReminderEmail"
    - from: "vercel.json"
      to: "/api/cron/collection-ruler"
      via: "crons schedule 0 8 * * *"
      pattern: "collection-ruler"
---

<objective>
Build the collection ruler (Vercel Cron + Resend email, idempotent per milestone — D-09/D-10), the PDF receipt (FIN-08, reusing the ProntuarioPDF pattern), and the SEC-06 security headers in next.config.ts. These three concerns are independent of the financial UI (Plan 03) and share no files with it, so they run in the same wave.

Purpose: FIN-07 (automated collection ruler), FIN-08 (PDF receipt), SEC-06 (security headers).
Output: cron endpoint + ruler engine + ruler config UI, ReceiboPDF + route, hardened next.config.ts headers.

ROADMAP authority note: ROADMAP Phase 3 Success Criterion 4 states the **receptionist** can generate and download a PDF receipt. The 03-UI-SPEC.md §PDF Layout Contract earlier restricted the route to admin/dentist only. The ROADMAP is the higher authority (success criteria define phase done-ness), so this plan aligns the recibo.pdf route to allow `admin`, `dentist`, AND `receptionist` — a receptionist at the front desk legitimately issues receipts. UI-SPEC restriction is superseded for this gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-financial-mvp/03-RESEARCH.md
@.planning/phases/03-financial-mvp/03-UI-SPEC.md
@src/lib/resend.ts
@src/lib/audit.ts

<interfaces>
<!-- Reuse targets + new contracts. -->

Resend (src/lib/resend.ts): `import { resend, FROM_EMAIL } from '@/lib/resend'`; `resend.emails.send({ from: FROM_EMAIL, to, subject, react: <Email/> })`.
Email template pattern: src/emails/InviteEmail.tsx (Html/Head/Body/Container/Heading/Text/Button via @react-email/components).
PDF pattern: src/components/pdf/ProntuarioPDF.tsx (Font.register Roboto, Flexbox only, A4) + src/app/api/patients/[id]/prontuario.pdf/route.ts (runtime='nodejs', getActor gate, renderToBuffer → Response with application/pdf).
createAdminClient (src/lib/supabase/admin.ts): cron runs without a session — query across tenants via service role.
collection_rules columns: due_date_reminder_enabled, overdue_reminder_enabled, overdue_interval_days, tenant_id (UNIQUE).
collection_log UNIQUE (receivable_id, milestone, channel) — idempotency.
Vercel Cron: path /api/cron/collection-ruler, schedule "0 8 * * *" (FREE plan = once/day MAX — Pitfall 5). Auth: header `Authorization: Bearer ${CRON_SECRET}` (Pitfall 6).
next.config.ts CSP must include connect-src `wss://*.supabase.co https://api.asaas.com https://api-sandbox.asaas.com` (Pitfall 9).
Role gate for recibo.pdf: `('admin','dentist','receptionist')` per ROADMAP Phase 3 SC-4 (overrides the UI-SPEC admin/dentist-only restriction).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Security headers (SEC-06) + ReceiboPDF + receipt route (FIN-08)</name>
  <files>next.config.ts, src/components/pdf/ReceiboPDF.tsx, src/app/api/financeiro/charges/[id]/recibo.pdf/route.ts, src/__tests__/pdf/recibo.test.ts, src/__tests__/config/security-headers.test.ts</files>
  <behavior>
    - next.config.ts headers() returns CSP, HSTS, X-Frame-Options, X-Content-Type-Options for source '/(.*)'
    - CSP connect-src includes wss://*.supabase.co + both Asaas hosts
    - ReceiboPDF uses Font.register (Roboto), flexDirection (Flexbox only), formatBRL
    - recibo route is runtime='nodejs', getActor-gated to admin/dentist/receptionist (ROADMAP Phase 3 SC-4); any other role 403
  </behavior>
  <read_first>
    - next.config.ts (current — empty config to extend)
    - src/components/pdf/ProntuarioPDF.tsx (Font.register + Flexbox layout to mirror)
    - src/app/api/patients/[id]/prontuario.pdf/route.ts (runtime + getActor + renderToBuffer pattern)
    - .planning/phases/03-financial-mvp/03-RESEARCH.md (§Pattern 4 — CSP code; Pitfalls 7,9)
    - .planning/phases/03-financial-mvp/03-UI-SPEC.md (§PDF Layout Contract — note: role gate superseded by ROADMAP SC-4, see plan objective)
    - .planning/ROADMAP.md (Phase 3 Success Criterion 4 — receptionist can generate/download receipt)
  </read_first>
  <action>
    1. `next.config.ts` — add `async headers()` returning the block from RESEARCH §Pattern 4: source `'/(.*)'` with X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Strict-Transport-Security max-age=63072000; includeSubDomains; preload, and Content-Security-Policy with the cspValue array (default-src 'self'; script-src 'self' 'unsafe-inline' (+'unsafe-eval' in dev); style-src 'self' 'unsafe-inline'; img-src 'self' blob: data: https://fonts.gstatic.com; font-src 'self' https://fonts.gstatic.com; connect-src 'self' https://*.supabase.co wss://*.supabase.co https://api.asaas.com https://api-sandbox.asaas.com; frame-ancestors 'none'; object-src 'none'; base-uri 'self'; form-action 'self'; upgrade-insecure-requests). Keep src/proxy.ts untouched (auth-only).
    2. `src/components/pdf/ReceiboPDF.tsx` — mirror ProntuarioPDF: reuse the same `Font.register` Roboto call, Flexbox `View` layout (NO Grid), A4, margins 40/48. Sections: header (clinic name + "Recibo de Pagamento" + date), patient block (full_name + CPF), charge details (method, amount via formatBRL, date paid, Asaas provider_charge_id), footer ("FYNXIA ERP — Documento confidencial"). Props: clinicName, patientName, patientCpf, billingType, amount, paidAt, providerChargeId.
    3. `src/app/api/financeiro/charges/[id]/recibo.pdf/route.ts` — `export const runtime = 'nodejs'` (Pitfall 7). GET handler: getActor; gate to role in `('admin','dentist','receptionist')` else 403 (ROADMAP Phase 3 SC-4 — receptionist legitimately issues receipts at the front desk; this overrides the UI-SPEC admin/dentist-only restriction); load charge + patient (tenant-scoped via createClient/RLS); `renderToBuffer(createElement(ReceiboPDF, props))`; return Response with `Content-Type: application/pdf` + `Content-Disposition: attachment; filename="recibo-{id}.pdf"`. On error → 500 with friendly message.
    4. `src/__tests__/pdf/recibo.test.ts` — source-inspect ReceiboPDF for `Font.register`, `flexDirection`, `toLocaleString('pt-BR'` (turn GREEN). Also source-inspect the recibo route and assert the role allowlist includes `'receptionist'` (`toMatch(/receptionist/)`) per ROADMAP SC-4.
    5. `src/__tests__/config/security-headers.test.ts` — read next.config.ts source; assert it contains all 4 required headers + `wss://*.supabase.co` + `api-sandbox.asaas.com` (turn GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/pdf/recibo.test.ts src/__tests__/config/security-headers.test.ts && npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Content-Security-Policy" next.config.ts` matches
    - `grep "wss://\*.supabase.co" next.config.ts` matches and `grep "api-sandbox.asaas.com" next.config.ts` matches
    - `grep "runtime = 'nodejs'" src/app/api/financeiro/charges/[id]/recibo.pdf/route.ts` matches
    - `grep "receptionist" src/app/api/financeiro/charges/[id]/recibo.pdf/route.ts` matches (role allowlist per ROADMAP SC-4)
    - `grep "Font.register" src/components/pdf/ReceiboPDF.tsx` matches
    - recibo.test.ts + security-headers.test.ts GREEN; next build succeeds
  </acceptance_criteria>
  <done>Security headers live in next.config.ts; ReceiboPDF + nodejs route render a PDF receipt gated to admin/dentist/receptionist (ROADMAP SC-4); build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collection ruler engine + cron endpoint + Resend reminder email</name>
  <files>src/lib/collection/ruler.ts, src/emails/CollectionReminderEmail.tsx, src/app/api/cron/collection-ruler/route.ts, vercel.json, src/__tests__/collection/ruler.test.ts</files>
  <behavior>
    - selectReminders(rule, receivables, today) returns targets with a milestone key ('due_date' | 'overdue_{N}')
    - milestone is stable so (receivable_id + milestone) dedups re-sends
    - cron endpoint returns 401 without correct CRON_SECRET bearer token
    - cron skips clinics with both reminders disabled
  </behavior>
  <read_first>
    - src/emails/InviteEmail.tsx (react-email template to mirror)
    - src/lib/resend.ts (resend client + FROM_EMAIL)
    - src/lib/supabase/admin.ts (createAdminClient — cron has no session)
    - .planning/phases/03-financial-mvp/03-RESEARCH.md (§Pattern 3 — cron; Open Question 2; Pitfalls 5,6)
  </read_first>
  <action>
    1. `src/lib/collection/ruler.ts` — pure functions (no server-only, unit-testable):
       - `type Rule = { due_date_reminder_enabled: boolean; overdue_reminder_enabled: boolean; overdue_interval_days: number }`
       - `selectReminders(rule, receivables: {id, due_date, status}[], today: Date): { receivableId: string; milestone: string }[]` — for each unpaid receivable: if due_date === today AND due_date_reminder_enabled → milestone 'due_date'; if overdue_reminder_enabled AND daysOverdue = floor((today - due_date)/day) is a positive multiple of overdue_interval_days → milestone `overdue_${daysOverdue}`. Skip paid. Use date-fns.
       This is the idempotency key generator — `(receivableId, milestone)` matches collection_log UNIQUE.
    2. `src/emails/CollectionReminderEmail.tsx` — react-email template mirroring InviteEmail: props {patientName, clinicName, chargeDescription, amount, dueDate, isOverdue}. Subject-friendly body in pt-BR; CTA "Ver detalhes da cobrança". Use formatBRL for amount.
    3. `src/app/api/cron/collection-ruler/route.ts` — `export const runtime = 'nodejs'`. GET: validate `request.headers.get('authorization') === \`Bearer ${process.env.CRON_SECRET}\`` else 401 (Pitfall 6). createAdminClient. Query `collection_rules WHERE due_date_reminder_enabled OR overdue_reminder_enabled` (Open Question 2 — skip disabled). For each tenant rule: load that tenant's unpaid receivables (+ patient email + charge description); `selectReminders(...)`; for each target: attempt INSERT collection_log (tenant_id, receivable_id, milestone, channel 'email') — if 23505 unique violation, skip (already sent, idempotent); else send `resend.emails.send({from: FROM_EMAIL, to: patientEmail, subject, react: CollectionReminderEmail(...)})`. logBusinessEvent per send. Return `Response.json({ processed: count })`.
    4. `vercel.json` — add `"crons": [{ "path": "/api/cron/collection-ruler", "schedule": "0 8 * * *" }]` alongside existing `"regions": ["gru1"]`. Use ONLY once-daily (Pitfall 5).
    5. `src/__tests__/collection/ruler.test.ts` — assert selectReminders: due-today emits 'due_date'; N-days-overdue emits 'overdue_N' at the right cadence; paid receivable emits nothing; disabled flags emit nothing. Source-inspect the cron route for CRON_SECRET + 401 + collection_log. Turn GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/collection/ruler.test.ts && npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CRON_SECRET" src/app/api/cron/collection-ruler/route.ts` matches and `grep "status: 401" src/app/api/cron/collection-ruler/route.ts` matches
    - `grep "collection_log" src/app/api/cron/collection-ruler/route.ts` matches (idempotency)
    - `grep "collection-ruler" vercel.json` matches and `grep "0 8 \* \* \*" vercel.json` matches
    - `grep "selectReminders" src/lib/collection/ruler.ts` matches
    - ruler.test.ts GREEN; next build succeeds
  </acceptance_criteria>
  <done>Daily cron scans active rules, sends idempotent Resend reminders per milestone; CRON_SECRET-gated; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Collection ruler config action + admin-only page + form</name>
  <files>src/actions/collection-ruler.ts, src/app/(dashboard)/clinica/financeiro/regua-de-cobranca/page.tsx, src/components/financeiro/CollectionRulerForm.tsx, src/__tests__/collection/ruler-config.test.ts</files>
  <behavior>
    - saveCollectionRuler gates to admin/superadmin; non-admin returns a failure result, never persists
    - the régua page renders "Acesso restrito" for non-admin roles (no redirect)
    - CollectionRulerForm surfaces the Fase 4 WhatsApp deferral note
  </behavior>
  <read_first>
    - src/actions/appointments.ts (getActor + role gate)
    - src/components/patients/PatientForm.tsx (RHF + zodResolver + Switch usage)
    - .planning/phases/03-financial-mvp/03-UI-SPEC.md (§Régua de Cobrança; §Collection Ruler Interactions; §Accessibility)
    - src/__tests__/migrations/financial.test.ts (Phase 3 source-inspection test pattern to mirror)
  </read_first>
  <action>
    1. `src/actions/collection-ruler.ts` (`'use server'`) — getActor + role gate to 'admin' (and superadmin). `getCollectionRuler()` SELECT collection_rules for tenant (return defaults if none). `saveCollectionRuler({dueDateReminderEnabled, overdueReminderEnabled, overdueIntervalDays})` validated by Zod (intervalDays int 1-30); UPSERT collection_rules on tenant_id (UNIQUE); logBusinessEvent. Non-admin → `{success:false, error:'Permissão insuficiente'}`.
    2. `regua-de-cobranca/page.tsx` — Server Component. getActor; if role !== admin/superadmin → render in-page Alert "Acesso restrito" (no redirect, per UI-SPEC). Else load getCollectionRuler() and render breadcrumb + heading + `CollectionRulerForm`.
    3. `CollectionRulerForm.tsx` ('use client') — RHF + zodResolver: Switch "Lembrete no vencimento" (due_date_reminder_enabled); Switch "Lembretes por atraso" (overdue_reminder_enabled) with useWatch enabling/disabling "Intervalo (dias)" Input (type number min 1 max 30); muted note "O canal WhatsApp será habilitado na Fase 4 após verificação Meta Business." (text-muted-foreground); "Salvar Configurações" → saveCollectionRuler; success/error inline. Each Switch has a `<Label htmlFor>` (accessibility). No auto-save.
    4. `src/__tests__/collection/ruler-config.test.ts` — behavioral source-inspection (Phase 2 readFileSync/toMatch pattern): read `src/actions/collection-ruler.ts` and assert it exports `saveCollectionRuler` and gates on role (`toMatch(/getActor/)` + `toMatch(/admin/)`); read `regua-de-cobranca/page.tsx` and assert `Acesso restrito`; read `CollectionRulerForm.tsx` and assert the `Fase 4` WhatsApp deferral note. GREEN after this task.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/collection/ruler-config.test.ts && npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function saveCollectionRuler" src/actions/collection-ruler.ts` matches
    - `grep "Acesso restrito" src/app/(dashboard)/clinica/financeiro/regua-de-cobranca/page.tsx` matches (admin gate)
    - `grep "Fase 4" src/components/financeiro/CollectionRulerForm.tsx` matches (WhatsApp deferral note)
    - ruler-config.test.ts GREEN; next build succeeds; tsc 0
  </acceptance_criteria>
  <done>Admin can configure the collection ruler (due-date + overdue cadence); non-admins see "Acesso restrito"; ruler-config.test.ts asserts the admin gate, page restriction, and Fase 4 note and is GREEN; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron → /api/cron/collection-ruler | Scheduled invocation under service role across all tenants |
| external attacker → cron endpoint | Public URL must reject non-Vercel callers |
| browser → recibo.pdf route | PDF with patient PII (CPF) — privileged |
| all HTTP responses | Security posture for the whole app (SEC-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-cron-E | Elevation of Privilege | /api/cron/collection-ruler | mitigate | Validate `Authorization: Bearer ${CRON_SECRET}`; 401 on mismatch (Pitfall 6) |
| T-3-cron-T | Tampering / duplicate sends | collection reminders | mitigate | INSERT collection_log with UNIQUE (receivable_id, milestone, channel); 23505 → skip — idempotent (D-10) |
| T-3-cron-I | Information Disclosure | cross-tenant in cron | mitigate | Process per-tenant from collection_rules; query each tenant's receivables explicitly; derive recipient from matched patient only |
| T-3-pdf-I | Information Disclosure | recibo.pdf (CPF) | mitigate | getActor gate to admin/dentist/receptionist only (ROADMAP SC-4 — receptionist legitimately issues receipts at the front desk); charge loaded tenant-scoped via RLS; any other role 403 |
| T-3-sec06-T | Tampering / Clickjacking | all responses | mitigate | X-Frame-Options DENY + frame-ancestors 'none'; HSTS; X-Content-Type-Options nosniff; CSP restricts connect-src to Supabase + Asaas only (Pitfall 9) |
| T-3-sec06-S | Spoofing | CSP unsafe-inline | accept | unsafe-inline accepted for internal ERP (no third-party scripts); nonce approach would force dynamic render on all pages (A3 in RESEARCH) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/collection/ruler.test.ts src/__tests__/collection/ruler-config.test.ts src/__tests__/pdf/recibo.test.ts src/__tests__/config/security-headers.test.ts` GREEN
- `npx tsc --noEmit` exits 0
- `npx next build` succeeds
- Manual: `curl -I {deployed-url}` shows CSP/HSTS/X-Frame-Options/X-Content-Type-Options (SEC-06); cron endpoint 401 without CRON_SECRET; collection email delivered to an overdue test receivable (VALIDATION manual-only); a receptionist account can download recibo.pdf (ROADMAP SC-4)
</verification>

<success_criteria>
- FIN-07 collection ruler: daily Vercel Cron (D-09), Resend email (D-10), idempotent per (receivable + milestone), admin config UI
- FIN-08 PDF receipt via @react-pdf/renderer (Flexbox, nodejs runtime, role-gated to admin/dentist/receptionist per ROADMAP SC-4)
- SEC-06 security headers present on all responses with Supabase WSS + Asaas in connect-src
- Build + types clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-mvp/03-04-SUMMARY.md`
</output>
</content>
