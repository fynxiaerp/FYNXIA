---
phase: 03-financial-mvp
plan: "03"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/lib/format/money.ts
  - src/actions/transactions.ts
  - src/actions/receivables.ts
  - src/app/(dashboard)/clinica/page.tsx
  - src/app/(dashboard)/clinica/financeiro/page.tsx
  - src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx
  - src/app/(dashboard)/clinica/financeiro/contas-a-receber/page.tsx
  - src/app/(dashboard)/clinica/financeiro/nova-cobranca/page.tsx
  - src/components/financeiro/CashFlowTotals.tsx
  - src/components/financeiro/TransactionList.tsx
  - src/components/financeiro/TransactionModal.tsx
  - src/components/financeiro/ReceivablesTable.tsx
  - src/components/financeiro/ChargeForm.tsx
  - src/components/financeiro/PixQRDisplay.tsx
  - src/__tests__/financeiro/money.test.ts
  - src/__tests__/financeiro/receivables.test.ts
  - src/__tests__/financeiro/charge-form.test.ts
autonomous: true
requirements: [FIN-01, FIN-02, FIN-03, FIN-04, FIN-05, FIN-06]

must_haves:
  truths:
    - "Receptionist sees current-month cash flow: entradas, saídas, saldo totals + transaction list, navigable by ?month"
    - "Receptionist can launch a manual transaction (receita/despesa, category, value, date)"
    - "Receptionist sees receivables list with status pendente/pago/vencido (vencido derived at read-time) and due dates"
    - "Installment charges group their parcels (accordion); each parcel shows due date + status"
    - "Receptionist can issue a PIX/boleto/cartão charge from Nova Cobrança and see QR / boleto link inline"
    - "All money renders as R$ 1.234,56 (pt-BR) and a Financeiro card exists on the /clinica hub"
  artifacts:
    - path: "src/lib/format/money.ts"
      provides: "formatBRL + deriveReceivableStatus pure helpers"
      contains: "toLocaleString('pt-BR'"
    - path: "src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx"
      provides: "Cash flow page (FIN-01)"
    - path: "src/components/financeiro/ReceivablesTable.tsx"
      provides: "Receivables table with read-time vencido derivation + installment accordion (FIN-03/06)"
      contains: "deriveReceivableStatus"
    - path: "src/components/financeiro/ChargeForm.tsx"
      provides: "Charge creation form wired to createCharge (FIN-04/05)"
    - path: "src/__tests__/financeiro/receivables.test.ts"
      provides: "Behavioral source-inspection: ReceivablesTable derives vencido client-side + uses Accordion (FIN-03/06)"
    - path: "src/__tests__/financeiro/charge-form.test.ts"
      provides: "Behavioral source-inspection: ChargeForm calls createCharge (FIN-04/05)"
  key_links:
    - from: "src/components/financeiro/ChargeForm.tsx"
      to: "src/actions/charges.ts"
      via: "createCharge Server Action"
      pattern: "createCharge"
    - from: "src/components/financeiro/TransactionModal.tsx"
      to: "src/actions/transactions.ts"
      via: "createTransaction Server Action"
      pattern: "createTransaction"
    - from: "src/app/(dashboard)/clinica/page.tsx"
      to: "/clinica/financeiro"
      via: "navItems Financeiro card"
      pattern: "financeiro"
---

<objective>
Build the financial UI: cash flow view (totals + transaction list with manual entry), accounts receivable list (read-time vencido derivation + installment accordion), and the Nova Cobrança charge form wired to the `createCharge` action from Plan 02. Plus the `Financeiro` hub card and the BRL/pt-BR money formatting helpers.

Purpose: FIN-01 (cash flow), FIN-02 (manual transaction), FIN-03 (receivables + status), FIN-04/05/06 (charge UI surfacing PIX/boleto/installments).
Output: financeiro pages + components + transactions/receivables Server Actions, per 03-UI-SPEC.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-financial-mvp/03-UI-SPEC.md
@.planning/phases/03-financial-mvp/03-RESEARCH.md
@src/actions/appointments.ts

<interfaces>
<!-- From Plan 02 (already built) + Phase 2 patterns. -->

createCharge (src/actions/charges.ts) → `{ success, chargeId?, pix?: {encodedImage, payload}, bankSlipUrl?, error? }`
getActor pattern: src/actions/appointments.ts.
Money rule (UI-SPEC §Money Formatting): `amount.toLocaleString('pt-BR', {style:'currency', currency:'BRL'})` → `R$ 1.234,56`; negative uses U+2212 (−), applied in component.
vencido derivation (UI-SPEC §Status badge, D-04): `status !== 'pago' && isPast(parseISO(dueDate)) ? 'vencido' : status`. date-fns installed.
Hub card pattern: src/app/(dashboard)/clinica/page.tsx navItems array (Agenda/Pacientes/Equipe cards).
Patient lookup: query patients (id, full_name, cpf) tenant-scoped via createClient (RLS).
shadcn to add: `npx shadcn@latest add switch accordion`. Existing: Card, Table, Dialog, Badge, Form, Input, Select, Calendar, Popover, Breadcrumb, Alert, Skeleton.
Status badge classes (UI-SPEC): pendente amber, pago green, vencido red. Amount signs: entrada text-green-700 +, saída text-red-600 −.
Behavioral test pattern (Phase 2): `readFileSync(resolve(...), 'utf8')` then `expect(src).toMatch(/.../)` — source-inspection, since `'use client'` + RSC components are not renderable under Vitest node env.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Money helpers + transactions/receivables Server Actions + Financeiro hub card</name>
  <files>src/lib/format/money.ts, src/actions/transactions.ts, src/actions/receivables.ts, src/app/(dashboard)/clinica/page.tsx, src/app/(dashboard)/clinica/financeiro/page.tsx, src/__tests__/financeiro/money.test.ts</files>
  <behavior>
    - formatBRL(1234.56) === 'R$ 1.234,56' and formatBRL(0) === 'R$ 0,00'
    - deriveReceivableStatus('pendente', pastDate) === 'vencido'; deriveReceivableStatus('pago', pastDate) === 'pago'
    - createTransaction gates role + inserts financial_transactions row scoped to tenant
  </behavior>
  <read_first>
    - src/app/(dashboard)/clinica/page.tsx (navItems card pattern — exact shape)
    - src/actions/appointments.ts (getActor + role gate)
    - .planning/phases/03-financial-mvp/03-UI-SPEC.md (§Money/Date Formatting; §Navigation Hub Integration; §Financeiro Module Hub)
  </read_first>
  <action>
    1. `src/lib/format/money.ts` — pure module (no server-only): `formatBRL(amount: number): string` using `toLocaleString('pt-BR', {style:'currency', currency:'BRL'})`; `formatBRLSigned(amount, direction: 'entrada'|'saida')` prefixing `+`/`−` (U+2212); `deriveReceivableStatus(status: string, dueDate: string): 'pendente'|'pago'|'vencido'` using date-fns isPast/parseISO (D-04).
    2. `src/__tests__/financeiro/money.test.ts` — assert formatBRL and deriveReceivableStatus cases above (this replaces the recibo render approach — pure helpers are unit-testable).
    3. `src/actions/transactions.ts` (`'use server'`) — getActor + role gate (admin/dentist/receptionist/superadmin); `createTransaction({type, categoryId, amount, transactionDate, description})` validates with a Zod schema (type in receita/despesa), INSERT financial_transactions (tenant_id, category_id, type, amount, transaction_date, description, posted_by: actor.id); logBusinessEvent. `listTransactions(month: 'YYYY-MM')` SELECT scoped to month range (RLS handles tenant). `listCategories()` SELECT financial_categories. (This module is what `src/__tests__/actions/transactions.test.ts` from Plan 01 Task 0 turns GREEN — FIN-02.)
    4. `src/actions/receivables.ts` (`'use server'`) — `listReceivables(filters?: {status?, from?, to?})` joins charges + patients (RLS), returns rows with installment grouping data (charge_id, installment_number, value, due_date, status, patient name, description). Do NOT compute vencido server-side — return raw status + due_date (derived client-side per D-04).
    5. `src/app/(dashboard)/clinica/page.tsx` — add Financeiro card to navItems: `{ href:'/clinica/financeiro', title:'Financeiro', description:'Fluxo de caixa, cobranças, recebíveis e recibos.', icon: DollarSign, show: true }` (import DollarSign from lucide-react).
    6. `src/app/(dashboard)/clinica/financeiro/page.tsx` — Server Component hub mirroring /clinica grid with cards: Fluxo de Caixa, Contas a Receber, Nova Cobrança, Régua de Cobrança (Régua card shown to admin only via role check).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro/money.test.ts src/__tests__/actions/transactions.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "toLocaleString('pt-BR'" src/lib/format/money.ts` matches
    - `grep "deriveReceivableStatus" src/lib/format/money.ts` matches
    - `grep "financeiro" src/app/(dashboard)/clinica/page.tsx` matches (hub card)
    - `grep "export async function createTransaction" src/actions/transactions.ts` matches
    - money.test.ts GREEN; transactions.test.ts (FIN-02) now GREEN; tsc 0
  </acceptance_criteria>
  <done>Money/status helpers, transactions + receivables actions, hub card, financeiro hub page present; money.test.ts and transactions.test.ts (FIN-02) GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cash flow page + receivables page + components</name>
  <files>src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx, src/app/(dashboard)/clinica/financeiro/contas-a-receber/page.tsx, src/components/financeiro/CashFlowTotals.tsx, src/components/financeiro/TransactionList.tsx, src/components/financeiro/TransactionModal.tsx, src/components/financeiro/ReceivablesTable.tsx, src/__tests__/financeiro/receivables.test.ts</files>
  <behavior>
    - ReceivablesTable derives vencido client-side (calls deriveReceivableStatus) — never reads a stored 'vencido' value (D-04)
    - ReceivablesTable groups installment charges with an Accordion
    - TransactionModal submits to the createTransaction Server Action
  </behavior>
  <read_first>
    - src/components/patients/PatientTable.tsx (TanStack Table v8 + @base-ui render-prop usage)
    - src/components/patients/PatientForm.tsx (RHF + zodResolver + Dialog pattern)
    - .planning/phases/03-financial-mvp/03-UI-SPEC.md (§Cash Flow Page; §Contas a Receber Page; §Interaction Contracts; §Accessibility)
    - src/lib/format/money.ts (helpers from Task 1)
    - src/__tests__/migrations/financial.test.ts (Phase 3 source-inspection test pattern to mirror)
  </read_first>
  <action>
    Run `npx shadcn@latest add switch accordion` first (idempotent).

    1. `fluxo-de-caixa/page.tsx` — Server Component. Read `?month` via nuqs server-side (default current month `new Date().toISOString().slice(0,7)`). Call listTransactions(month) + listCategories(). Render breadcrumb, month nav (Prev/Next links updating ?month), `CashFlowTotals`, `TransactionList`, and the "+ Lançamento" trigger. Empty state copy from UI-SPEC.
    2. `CashFlowTotals.tsx` ('use client') — 3 cards (entradas/saídas/saldo), Display-size `tabular-nums` amounts via formatBRL; entrada green, saída red, saldo neutral/red-if-negative; `aria-label` with full amount (UI-SPEC accessibility).
    3. `TransactionList.tsx` ('use client') — TanStack Table v8: Data | Descrição | Categoria | Tipo | Valor (text-right tabular-nums, signed). nuqs `?tipo` + `?category` client-side filters. Empty state.
    4. `TransactionModal.tsx` ('use client') — Dialog + RHF + zodResolver: tipo (Select receita/despesa), categoria (Select from listCategories), valor (BRL-masked Input, raw numeric in RHF state per UI-SPEC), data (Calendar-in-Popover), descrição. On submit → createTransaction; on success close + router.refresh(); on error inline Alert.
    5. `contas-a-receber/page.tsx` — Server Component. listReceivables(filters from nuqs ?status/?from/?to). Breadcrumb + "Contas a Receber" header + "+ Cobrança" link to /nova-cobranca + `ReceivablesTable`. Empty/filter-empty states.
    6. `ReceivablesTable.tsx` ('use client') — TanStack Table v8. Columns: Paciente | Descrição | Parcelas | Vencimento | Valor | Status | Ações. Group rows with installment_count>1 into shadcn Accordion (parent shows "N de M pagas", parcels expand). Status badge derived via `deriveReceivableStatus` (client-side, D-04) with UI-SPEC badge classes + `aria-label="Status: {status}"`. nuqs `?status` filter. "Baixar Recibo" button on pago rows linking GET `/api/financeiro/charges/[id]/recibo.pdf` (route built in Plan 04).
    7. `src/__tests__/financeiro/receivables.test.ts` — behavioral source-inspection (Phase 2 readFileSync/toMatch pattern): read `src/components/financeiro/ReceivablesTable.tsx` and assert it imports/calls `deriveReceivableStatus` (client-side vencido derivation, D-04), that it does NOT contain a stored `'vencido'` literal status from the DB (`expect(src).not.toMatch(/status.*===.*['"]vencido['"]/)` style — derivation only), and that it uses `Accordion` for installment grouping (`toMatch(/Accordion/)`). GREEN after this task.
    Use `@base-ui/react` render-prop pattern (not asChild) per Phase 2 lesson.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro/receivables.test.ts && npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "deriveReceivableStatus" src/components/financeiro/ReceivablesTable.tsx` matches
    - `grep "createTransaction" src/components/financeiro/TransactionModal.tsx` matches
    - `grep -r "Accordion" src/components/financeiro/ReceivablesTable.tsx` matches (installment grouping)
    - receivables.test.ts GREEN; `next build` succeeds; tsc 0
  </acceptance_criteria>
  <done>Cash flow page (totals + list + manual entry) and receivables page (status + installment accordion) build cleanly; receivables.test.ts asserts client-side vencido derivation + Accordion usage and is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Nova Cobrança page + ChargeForm + PixQRDisplay</name>
  <files>src/app/(dashboard)/clinica/financeiro/nova-cobranca/page.tsx, src/components/financeiro/ChargeForm.tsx, src/components/financeiro/PixQRDisplay.tsx, src/__tests__/financeiro/charge-form.test.ts</files>
  <behavior>
    - ChargeForm submits to the createCharge Server Action
    - ChargeForm toggles installment fields via a Switch
    - PixQRDisplay renders the Asaas base64 QR as an inline data URL
  </behavior>
  <read_first>
    - src/components/patients/PatientForm.tsx (RHF + zodResolver + Popover patient lookup style)
    - src/lib/validators/charge.ts (chargeSchema from Plan 02)
    - src/actions/charges.ts (createCharge return shape from Plan 02)
    - .planning/phases/03-financial-mvp/03-UI-SPEC.md (§Nova Cobrança Page; §Charge Creation Interactions; §Accessibility)
    - src/__tests__/migrations/financial.test.ts (Phase 3 source-inspection test pattern to mirror)
  </read_first>
  <action>
    1. `nova-cobranca/page.tsx` — Server Component. Breadcrumb + "Emitir Cobrança" heading + `ChargeForm` (pass tenant patient list or let form fetch via action). Role gate read: page accessible to admin/dentist/receptionist.
    2. `ChargeForm.tsx` ('use client') — RHF + zodResolver(chargeSchema): Paciente (Popover search by name/CPF → patient id), Descrição (Input), Método (Select PIX/BOLETO/CREDIT_CARD), Valor total (BRL-masked Input, raw numeric in state, `inputMode="decimal"`, `aria-label="Valor em reais"`), Parcelado? (Switch — useWatch toggles Nº de parcelas Select 2/3/6/12), Vencimento (Calendar). Submit → createCharge; disable button + spinner during submit. On success render result block: for PIX → `PixQRDisplay` + "Baixar Recibo PDF" link; for BOLETO → clickable bankSlipUrl anchor (target="_blank") + barcode in font-mono. On error → Alert surfacing Asaas description (UI-SPEC error states). Credit card: render as billingType but note tokenization handled by Asaas (no raw card capture in this form).
    3. `PixQRDisplay.tsx` ('use client') — 200×200 QR `<img src={`data:image/png;base64,${encodedImage}`} alt="Código QR para pagamento via Pix do paciente {name}" />` + copia-e-cola string + "Copiar código Pix" button (`navigator.clipboard.writeText`, label → "Copiado!" 2s, `aria-label="Copiar código Pix"`).
    4. `src/__tests__/financeiro/charge-form.test.ts` — behavioral source-inspection (Phase 2 readFileSync/toMatch pattern): read `src/components/financeiro/ChargeForm.tsx` and assert it calls `createCharge` (`toMatch(/createCharge\(/)`), uses a `Switch` for parcelamento, and reads `src/components/financeiro/PixQRDisplay.tsx` to assert it contains `data:image/png;base64`. GREEN after this task.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro/charge-form.test.ts && npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "createCharge" src/components/financeiro/ChargeForm.tsx` matches
    - `grep "Switch" src/components/financeiro/ChargeForm.tsx` matches (parcelamento toggle)
    - `grep "data:image/png;base64" src/components/financeiro/PixQRDisplay.tsx` matches
    - charge-form.test.ts GREEN; `next build` succeeds; tsc 0
  </acceptance_criteria>
  <done>Nova Cobrança form issues PIX/boleto/cartão via createCharge and renders QR / boleto link inline; charge-form.test.ts asserts createCharge wiring + Switch + base64 QR and is GREEN; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → createCharge / createTransaction | Authenticated staff input crossing into Server Actions |
| browser → financial pages | Tenant-scoped data rendered; RLS enforces isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-ui-I | Information Disclosure | financeiro pages | mitigate | All reads via createClient (RLS USING tenant_id = get_my_tenant_id()); no cross-tenant rows |
| T-3-ui-E | Elevation of Privilege | createTransaction / Régua hub card | mitigate | getActor role gate in actions; Régua card shown to admin only; action-level gates are the real enforcement |
| T-3-ui-V | Input Validation | TransactionModal / ChargeForm | mitigate | Zod schemas on both Server Actions; amount parsed as number (not string) |
| T-3-ui-xss | XSS | Asaas error / patient names in UI | mitigate | React escapes by default; surface only Asaas `description` string; no innerHTML |
</threat_model>

<verification>
- `npx vitest run src/__tests__/financeiro/money.test.ts src/__tests__/actions/transactions.test.ts src/__tests__/financeiro/receivables.test.ts src/__tests__/financeiro/charge-form.test.ts` GREEN
- `npx tsc --noEmit` exits 0
- `npx next build` succeeds (catches `'use server'` async-export + RSC boundary errors)
- Manual: /clinica shows Financeiro card; cash flow totals + transaction list render; receivables show derived vencido + installment accordion; Nova Cobrança renders PIX QR (verified live in Plan 02 checkpoint)
</verification>

<success_criteria>
- FIN-01 cash flow (totals + list, ?month nav), FIN-02 manual transaction, FIN-03 receivables with derived vencido, FIN-06 installment accordion, FIN-04/05 charge form surfacing PIX QR + boleto link
- All money pt-BR formatted; Financeiro hub card present
- Build + types clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-mvp/03-03-SUMMARY.md`
</output>
</content>
