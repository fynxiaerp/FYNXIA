---
phase: 16
plan: 06
type: execute
wave: 3
depends_on: [16-04, 16-05]
files_modified:
  - src/actions/suppliers.ts
  - src/actions/payables.ts
  - src/actions/recorrente.ts
  - src/app/api/cron/recorrente/route.ts
autonomous: true
requirements: [FOP-01]
user_setup: []

must_haves:
  truths:
    - "createSupplier/linkProfessionalSupplier/linkLabSupplier create and link suppliers; professionals.supplier_id is set on link (D-01)"
    - "createPayable inserts a payable + N payable_installments (equal split, incrementing due dates) scoped to tenant (D-02a/D-04)"
    - "baixarPayable picks a bank_account, inserts a financial_transaction type='despesa', debits bank_accounts.saldo_atual, supports partial baixa, and is idempotent via CAS on installment.status (D-03/D-04)"
    - "createPayableFromLabOrder (origem='lab', D-02c) and createPayableFromRepasse (origem='repasse', D-02d) and createTributoPayable (origem='tributo', D-20) create system payables"
    - "generateRecorrentePayables creates one payable per active recorrente_template for a competência, idempotent per (template, competência) (D-02b)"
  artifacts:
    - path: "src/actions/suppliers.ts"
      provides: "listSuppliers, createSupplier, updateSupplier, linkProfessionalSupplier, linkLabSupplier"
      exports: ["listSuppliers", "createSupplier", "linkProfessionalSupplier"]
    - path: "src/actions/payables.ts"
      provides: "createPayable, baixarPayable, listPayables, getPayable, cancelarPayable, attachPayableDocument, createPayableFromLabOrder, createPayableFromRepasse, createTributoPayable"
      exports: ["createPayable", "baixarPayable", "listPayables"]
    - path: "src/actions/recorrente.ts"
      provides: "createRecorrenteTemplate, listRecorrenteTemplates, generateRecorrentePayables"
      exports: ["generateRecorrentePayables"]
  key_links:
    - from: "baixarPayable"
      to: "financial_transactions insert + bank_accounts.saldo_atual debit"
      via: "supabase insert despesa + saldo update"
      pattern: "type: 'despesa'"
    - from: "baixarPayable"
      to: "payable_installments CAS"
      via: ".eq('status', 'pendente') / .eq('status','parcial')"
      pattern: "\\.neq\\('status', 'pago'\\)|\\.in\\('status'"
    - from: "linkProfessionalSupplier"
      to: "professionals.supplier_id"
      via: "supabase update professionals"
      pattern: "supplier_id"
---

<objective>
Wave 3 — construir o núcleo de Contas a Pagar: cadastro de fornecedores (com link profissional/laboratório → supplier, D-01), criação de CP com parcelas, baixa que move o caixa e o saldo bancário, as quatro origens de CP (manual/recorrente/lab/repasse) + a origem 'tributo' das retenções, e o template recorrente + Cron de geração por competência. Depende do db push de 16-05 (todo o schema está vivo). Espelha exatamente os padrões de `src/actions/receivables.ts` e `src/actions/transactions.ts` (getActor, RLS via createClient, zod parse, return serializável).

Purpose: FOP-01 — operar contas a pagar (vencimentos, baixa) integradas a fornecedores/laboratório.
Output: 4 arquivos de Server Action + uma rota de Cron.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-CONTEXT.md

<interfaces>
Mirror EXACTLY (getActor + RLS via createClient): src/actions/receivables.ts (getActor at top), src/actions/transactions.ts (zod safeParse → friendly first error → role gate → insert → revalidatePath).

Validators (Plan 04, already live): src/lib/validators/payable.ts exports `payableSchema` (supplierId, descricao, accountId, costCenterId, unitId?, valorTotal isMoney2dp, dueDate, parcelas int≥1, origem, notes?, documentId?) and `baixaSchema` (installmentId, bankAccountId, valorPago isMoney2dp, dataPagamento, comprovanteDocumentId?). NO Zod .default() (D-133).

Schema live after 16-05 (16-RESEARCH §"Schema Design"):
  - public.suppliers (id, clinic_id, name, tipo CHECK('laboratorio','material','servico','autonomo','pj','outro'), cnpj_cpf, pix_key, banco, agencia, conta, vinculo CHECK('clt','pj','autonomo'), professional_id, lab_id, ativo, deleted_at)
  - public.payables (id, clinic_id, unit_id, supplier_id, account_id, cost_center_id, bank_account_id, descricao, valor_total NUMERIC(12,2), origem CHECK('manual','recorrente','lab','repasse','tributo') DEFAULT 'manual', lab_order_id, payout_id, status CHECK('pendente','parcial','pago','cancelado') DEFAULT 'pendente', recorrente_template_id, competencia, notes, document_id, created_by)
  - public.payable_installments (id, clinic_id, payable_id, numero, valor NUMERIC(12,2), due_date, paid_at, valor_pago, financial_transaction_id, status CHECK('pendente','parcial','pago','cancelado') DEFAULT 'pendente')
  - public.recorrente_templates (id, clinic_id, unit_id, supplier_id, account_id, cost_center_id, descricao, valor NUMERIC(12,2), dia_vencimento SMALLINT 1..28, ativo)
  - public.financial_transactions (existing + new reconciliation_status DEFAULT 'pendente', statement_line_id) — insert despesa with: tenant_id, type 'despesa', account_id, cost_center_id, bank_account_id, amount, transaction_date, description, reconciliation_status 'baixado'
  - public.bank_accounts (existing + new saldo_atual NUMERIC(12,2) DEFAULT 0, data_abertura)
  - public.professionals (existing + new supplier_id)
  - public.lab_orders (Fase 13 — lab_order_id source for D-02c)

financial_transactions column name for tenant: confirm via read (Fase 3 uses tenant_id). Use the SAME column name found in src/actions/transactions.ts insert.
Phase 10 alçada (estorno D-24): src/actions/approval-actions.ts (createApprovalRequest) + audit via src/lib/audit (logBusinessEvent). cancelarPayable on a PAID payable MUST route through alçada, not delete directly.
Cron pattern to mirror: src/lib/messaging/reminder-scan.ts + an existing src/app/api/cron/*/route.ts (runtime='nodejs', CRON_SECRET header check). Inspect one existing cron route for the auth gate idiom.
Roles (D-23): writers = ['admin','superadmin']; the proxy already marks auditor/socio financeiro readOnly. Add an inline role gate in each write action mirroring transactions.ts; reject non-writers with an error mentioning 'permissão'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: suppliers.ts — CRUD + link profissional/laboratório</name>
  <files>src/actions/suppliers.ts</files>
  <read_first>
    - src/actions/receivables.ts (getActor helper to copy verbatim at top of file)
    - src/actions/transactions.ts (zod safeParse → friendly error → role gate → insert → revalidatePath structure)
    - src/lib/validators/payable.ts (NOT a supplier schema — define a small inline supplierSchema here, Zod v3, no .default())
    - 16-RESEARCH.md §"Schema Design" suppliers DDL (lines ~440-460) + §"professionals ALTER" (supplier_id)
    - CLAUDE.md (RLS via createClient authenticated session; 'use server' async-only; service role server-only)
  </read_first>
  <action>
`'use server'` + `import 'server-only'`. Copy the `getActor()` helper from receivables.ts verbatim. Define inline `supplierSchema` (Zod v3, no .default()): name min 1, tipo enum ('laboratorio','material','servico','autonomo','pj','outro'), cnpjCpf optional, pixKey optional, banco/agencia/conta optional, vinculo enum optional ('clt','pj','autonomo'), professionalId uuid optional, labId uuid optional, ativo boolean optional. Export inferred `SupplierInput`.

Implement (each: getActor → if 'error' return {success:false,error}; role gate writers ['admin','superadmin'] for writes, return error 'Sem permissão para esta operação' if not; supabase = await createClient(); RLS isolates tenant):
  - `listSuppliers(filters?: { tipo?: string|null; ativo?: boolean|null })` → { success, suppliers } selecting id, name, tipo, cnpj_cpf, vinculo, professional_id, lab_id, ativo; order by name; filter `.is('deleted_at', null)`.
  - `createSupplier(input)` → validate supplierSchema; insert into public.suppliers with clinic_id = actor.tenant_id (NEVER from input). Return { success, supplierId }.
  - `updateSupplier(id, input)` → validate; update by id (RLS scopes tenant).
  - `deactivateSupplier(id)` → set ativo=false (soft, keep row for fiscal trail).
  - `linkProfessionalSupplier(professionalId, supplierId)` (D-01): writer gate; update public.suppliers SET professional_id=professionalId WHERE id=supplierId; AND update public.professionals SET supplier_id=supplierId WHERE id=professionalId (both tenant-scoped via RLS). Return {success}.
  - `linkLabSupplier(labId, supplierId)` (D-01): update suppliers SET lab_id=labId WHERE id=supplierId.
`revalidatePath('/clinica/financeiro/contas-a-pagar')` after writes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "actions/suppliers" | head && grep -c "import 'server-only'" src/actions/suppliers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function createSupplier" src/actions/suppliers.ts` present
    - `grep "export async function linkProfessionalSupplier" src/actions/suppliers.ts` present and updates BOTH suppliers.professional_id and professionals.supplier_id
    - `grep "actor.tenant_id" src/actions/suppliers.ts` present (clinic_id never from input)
    - role gate: `grep -E "admin'|permissão" src/actions/suppliers.ts` present in write paths
    - `grep -c "import 'server-only'" src/actions/suppliers.ts` returns 1
    - tsc clean for the file
  </acceptance_criteria>
  <done>suppliers CRUD + bidirectional professional/lab link; tenant-scoped; writer-gated.</done>
</task>

<task type="auto">
  <name>Task 2: payables.ts — createPayable + baixarPayable (CAS, partial, saldo debit) + system origins + cancel via alçada</name>
  <files>src/actions/payables.ts</files>
  <read_first>
    - src/__tests__/financeiro16/payables.test.ts (RED specs — baixa despesa insert + saldo debit + partial + idempotent re-baixa + role gate; implement to GREEN)
    - src/actions/transactions.ts (createTransaction insert shape: confirm exact column names tenant_id/type/account_id/cost_center_id/bank_account_id/amount/transaction_date)
    - src/actions/receivables.ts (getActor; estorno/baixa idempotency style if present)
    - src/actions/lab-orders.ts (lab_order → cost link; TOCTOU re-fetch guard before money mutation)
    - src/actions/approval-actions.ts (createApprovalRequest by alçada — for cancelarPayable on PAID payables, D-24)
    - src/lib/validators/payable.ts (payableSchema, baixaSchema)
    - 16-RESEARCH.md §"Schema Design" payables + payable_installments DDL + §"Code Examples"; §"Common Pitfalls" Pitfall 5 (fee=despesa sign) for context
  </read_first>
  <action>
`'use server'` + `import 'server-only'`. getActor helper. Writer gate ['admin','superadmin'] on all writes.

  - `createPayable(rawInput)` (D-02a/D-04): parse payableSchema. Insert public.payables (clinic_id=actor.tenant_id, supplier_id, account_id, cost_center_id, unit_id, descricao, valor_total, origem from input or 'manual', status 'pendente', notes, document_id, created_by=actor.id). Then generate `parcelas` installments: equal integer-cent split (last installment absorbs the rounding remainder so sum === valor_total exactly), `numero` 1..N, `due_date` = dueDate + (numero-1) months (use date math, day-clamped ≤28 safe), status 'pendente'. Insert all payable_installments rows (clinic_id, payable_id). Return { success, payableId }. revalidatePath.

  - `baixarPayable(rawInput)` (D-03/D-04) — the money mutation:
    1. Parse baixaSchema (installmentId, bankAccountId, valorPago, dataPagamento, comprovanteDocumentId?).
    2. Re-fetch the installment by id (TOCTOU) with its payable (for account_id/cost_center_id, descricao). 
    3. Idempotency/CAS: if installment.status === 'pago' → return { success: true } (idempotent no-op, do NOT re-insert). 
    4. Insert ONE public.financial_transactions row: { tenant_id: actor.tenant_id, type: 'despesa', account_id: payable.account_id, cost_center_id: payable.cost_center_id, bank_account_id: bankAccountId, amount: valorPago, transaction_date: dataPagamento, description: payable.descricao, reconciliation_status: 'baixado' }. Capture inserted id (ftId).
    5. Debit bank_accounts.saldo_atual: fetch current saldo_atual, update to (saldo_atual − valorPago) (integer-cent). (Acceptable: a single UPDATE … SET saldo_atual = saldo_atual - valorPago via rpc or read-modify-write; read-modify-write is fine at clinic volumes.)
    6. Determine new installment status: compute saldo restante = installment.valor − (installment.valor_pago ?? 0) − valorPago. If saldo restante ≤ 0.005 → status 'pago', paid_at=now; else status 'parcial'. Update payable_installments SET valor_pago = (prev + valorPago), status, paid_at, financial_transaction_id=ftId WHERE id=installmentId AND status != 'pago' (CAS guard — `.neq('status','pago')`). If 0 rows affected → a concurrent baixa won; return { success:false, error:'Baixa concorrente detectada' } and (best-effort) compensate the inserted FT or rely on the CAS being checked BEFORE the FT insert — PREFER: do the CAS claim on the installment FIRST (update status to a transient/intended state) then insert FT; structure so the FT is only inserted after the CAS claim succeeds.
    7. Roll up payable.status from its installments: if all 'pago' → payable 'pago'; if any 'parcial'/'pago' mix → 'parcial'; update public.payables.status.
    8. Return { success: true, financialTransactionId: ftId }. revalidatePath('/clinica/financeiro/contas-a-pagar') + '/fluxo-de-caixa'.
    Reorder steps 4↔6 if needed so the installment CAS claim happens before the FT insert (prevents double-debit on double-click — T-16 money mutation rule).

  - `createPayableFromLabOrder(labOrderId, supplierId, valorTotal, dueDate)` (D-02c): system-invoked (called from lab-orders action when prótese concluída); insert payable origem='lab', lab_order_id=labOrderId + single installment. Idempotent: if a payable with this lab_order_id already exists → return existing id (no duplicate). Export so Plan/Fase 13 hook can call it; also safe to call internally.

  - `createPayableFromRepasse(payoutId, supplierId, valorRepasse, competencia, dueDate)` (D-02d/D-15): insert payable origem='repasse', payout_id=payoutId, competencia + single installment; idempotent per payout_id. Returns payableId (caller in Plan 08 sets professional_payouts.payable_id).

  - `createTributoPayable({ supplierId|null, descricao, valor, dueDate, competencia, accountId, costCenterId, documentId? })` (D-20): insert payable origem='tributo' + single installment (used by RPA to lance INSS/IRRF/ISS a recolher). Returns payableId.

  - `listPayables(filters?: { status?, supplierId?, from?, to?, unitId? })` → { success, payables } joining supplier name + aggregated installment data (valor_total, valor_pago sum, próximo vencimento, derived nothing — return raw status + due_date; 'vencido' derived client-side mirroring receivables D-04). 
  - `getPayable(id)` → payable + installments + supplier.
  - `attachPayableDocument(payableId, documentId)` (D-27): update payables.document_id (writer gate).
  - `cancelarPayable(payableId, motivo)` (D-24): if payable.status === 'pago' OR has any 'pago' installment → route through `createApprovalRequest` (alçada) + audit log; do NOT directly cancel. If fully 'pendente' → set status 'cancelado' + cascade installments 'cancelado' directly. 
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro16/payables.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - payables.test.ts fully GREEN (despesa insert + saldo debit + partial baixa status='parcial' + idempotent re-baixa keeps insert count at 1 + role gate)
    - `grep "type: 'despesa'" src/actions/payables.ts` present
    - `grep -E "saldo_atual" src/actions/payables.ts` present (bank debit)
    - `grep -E "\\.neq\\('status', 'pago'\\)|\\.in\\('status'" src/actions/payables.ts` present (installment CAS guard)
    - exports createPayable, baixarPayable, listPayables, getPayable, cancelarPayable, attachPayableDocument, createPayableFromLabOrder, createPayableFromRepasse, createTributoPayable
    - cancelarPayable references createApprovalRequest for paid path (grep)
    - `grep "actor.tenant_id" src/actions/payables.ts` present; clinic_id never from input
    - `grep -c "import 'server-only'" src/actions/payables.ts` returns 1
  </acceptance_criteria>
  <done>Full CP lifecycle: create+parcelas, idempotent baixa with saldo debit + CAS, 4 system origins, cancel via alçada; payables.test.ts GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: recorrente.ts + Cron route — recurring CP generation by competência</name>
  <files>src/actions/recorrente.ts, src/app/api/cron/recorrente/route.ts</files>
  <read_first>
    - src/lib/messaging/reminder-scan.ts (scan-all-tenants Cron worker pattern)
    - one existing src/app/api/cron/*/route.ts (runtime='nodejs' + CRON_SECRET / Authorization header gate idiom — replicate exactly)
    - src/actions/payables.ts (Task 2 — generateRecorrentePayables creates payables via the same insert path; reuse createPayable internals or a shared insert)
    - 16-RESEARCH.md §"Schema Design" recorrente_templates DDL + §"Padrão 6" (competência)
    - CLAUDE.md (Vercel cron; nodejs runtime; service role server-only for cross-tenant cron)
  </read_first>
  <action>
**src/actions/recorrente.ts** (`'use server'` + `import 'server-only'`, getActor, writer gate):
  - Inline `recorrenteSchema` (Zod v3, no .default()): supplierId uuid optional, accountId uuid, costCenterId uuid, unitId uuid optional, descricao min 1, valor isMoney2dp positive, diaVencimento int 1..28.
  - `createRecorrenteTemplate(input)` → insert public.recorrente_templates (clinic_id=actor.tenant_id, ativo=true).
  - `listRecorrenteTemplates()` → { success, templates } clinic-scoped, ativo + inactive.
  - `updateRecorrenteTemplate(id, input)` / `deactivateRecorrenteTemplate(id)`.
  - `generateRecorrentePayables(competencia: string, clinicId?: string)` (D-02b): for the given competência ('YYYY-MM'), for each ativo template (scoped to clinicId when passed by cron, else actor.tenant_id), create ONE payable origem='recorrente', recorrente_template_id=template.id, competencia, due_date = competência + dia_vencimento (clamp ≤28), single installment. IDEMPOTENT: skip a template if a payable with (recorrente_template_id, competencia) already exists (query first). Return { success, created, skipped }.

**src/app/api/cron/recorrente/route.ts**:
  - `export const runtime = 'nodejs'` and `export const maxDuration = 60`.
  - `export async function GET(request: Request)` (Vercel cron uses GET): verify the CRON_SECRET / Authorization header EXACTLY like the existing cron route read in read_first. Reject 401 if missing/mismatch.
  - Compute current competência = `new Date().toISOString().slice(0,7)` (YYYY-MM).
  - Using `createAdminClient` (service role — cross-tenant), iterate clinics that have ≥1 ativo recorrente_template; call `generateRecorrentePayables(competencia, clinicId)` per clinic.
  - Return JSON { ok: true, competencia, totals: { created, skipped } }.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "recorrente" | head && grep -E "runtime = 'nodejs'" src/app/api/cron/recorrente/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - recorrente.ts exports createRecorrenteTemplate, listRecorrenteTemplates, generateRecorrentePayables
    - generateRecorrentePayables is idempotent per (recorrente_template_id, competencia) — grep shows an existence check before insert
    - cron route: `grep "runtime = 'nodejs'" src/app/api/cron/recorrente/route.ts` present
    - cron route verifies CRON_SECRET/Authorization header (grep for CRON_SECRET or authorization)
    - cron uses createAdminClient for cross-tenant iteration
    - tsc clean for both files
  </acceptance_criteria>
  <done>Recurring CP template + idempotent per-competência generation + nodejs Cron with secret gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client form → createPayable/baixarPayable | Untrusted CP/baixa input crosses into money mutations |
| cron trigger → generateRecorrentePayables | Cross-tenant batch via service role; must verify secret |
| double-click baixar → financial side-effect | Retry must not double-debit |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-20 | Tampering | double-click baixarPayable → double FT + double saldo debit | mitigate | installment CAS `.neq('status','pago')` claim BEFORE FT insert; idempotent re-baixa returns success without re-insert |
| T-16-21 | Spoofing | tenant_id injected via input on createPayable | mitigate | clinic_id always = actor.tenant_id, never from client; RLS WITH CHECK backstop |
| T-16-22 | Elevation of Privilege | non-writer (auditor/socio/recepção) baixa/cria CP | mitigate | role gate ['admin','superadmin'] in every write action; proxy readOnly for auditor/socio |
| T-16-23 | Repudiation | cancel a PAID payable with no trail | mitigate | cancelarPayable on paid routes through approval_requests alçada + audit (D-24) |
| T-16-24 | Elevation of Privilege | unauthenticated hit on /api/cron/recorrente | mitigate | CRON_SECRET / Authorization header check returns 401; nodejs runtime |
| T-16-25 | Tampering | duplicate recorrente payable on cron re-run | mitigate | generateRecorrentePayables idempotent per (template, competência) |
</threat_model>

<verification>
- `npx vitest run src/__tests__/financeiro16/payables.test.ts` GREEN.
- `npx tsc --noEmit` clean for suppliers/payables/recorrente/cron.
- Manual: createPayable(2 parcelas) → baixarPayable parcial → installment 'parcial', saldo_atual debitado, FT despesa criado.
</verification>

<success_criteria>
FOP-01 delivered: fornecedores + 4 origens de CP + parcelas + baixa idempotente que move caixa e saldo bancário + cancel via alçada + recorrente Cron. payables.test.ts GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-06-SUMMARY.md`
</output>
