---
phase: 16
plan: 02
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - supabase/migrations/20260621000100_payables_tables.sql
  - supabase/migrations/20260621000200_reconciliation_tables.sql
  - supabase/migrations/20260621000400_tax_tables.sql
  - supabase/migrations/20260621000700_phase16_seed.sql
autonomous: true
requirements: [FOP-01, FOP-02, FOP-03, TRIB-02]
user_setup: []

must_haves:
  truths:
    - "suppliers/payables/payable_installments/recorrente_templates tables exist with clinic_id index + NUMERIC(12,2) money + origem/status enums (D-01/02/04)"
    - "bank_statements/statement_lines exist with partial UNIQUE(bank_account_id,fitid) and UNIQUE(bank_account_id,fitid_fallback) for FITID idempotency (D-11)"
    - "inss/irrf/iss tax tables exist with vigencia_inicio/vigencia_fim + index, seeded with 2026 brackets (D-17)"
  artifacts:
    - path: "supabase/migrations/20260621000100_payables_tables.sql"
      provides: "suppliers, payables, payable_installments, recorrente_templates"
      contains: "CREATE TABLE public.payables"
    - path: "supabase/migrations/20260621000200_reconciliation_tables.sql"
      provides: "bank_statements, statement_lines with FITID idempotency"
      contains: "fitid_fallback"
    - path: "supabase/migrations/20260621000400_tax_tables.sql"
      provides: "inss/irrf/iss tax tables versioned by vigência"
      contains: "inss_tax_tables"
    - path: "supabase/migrations/20260621000700_phase16_seed.sql"
      provides: "2026 INSS/IRRF brackets + ISS default + supplier types seed"
      contains: "INSERT INTO public.inss_tax_tables"
  key_links:
    - from: "payable_installments.financial_transaction_id"
      to: "financial_transactions(id)"
      via: "FK ON DELETE SET NULL"
      pattern: "financial_transaction_id"
    - from: "statement_lines (fitid uniqueness)"
      to: "idempotent OFX import"
      via: "partial UNIQUE indexes"
      pattern: "UNIQUE \\(bank_account_id, fitid\\)"
---

<objective>
Wave 1 — escrever as migrations do lado "pagar + conciliar + tributo-seed": fornecedores, contas a pagar com parcelas, templates recorrentes, extratos bancários com idempotência FITID, e as tabelas de tributo versionadas por vigência com seed 2026. NÃO faz db push (Plan 05 [BLOCKING] faz). As tabelas são escritas verbatim a partir do DDL de 16-RESEARCH.md (linhas ~439-707). Roda em paralelo com 16-03 (sem overlap de arquivos) e 16-04.

Purpose: FOP-01 (CP/fornecedores/parcelas), FOP-02 (extrato/idempotência), TRIB-02 (seed das faixas de imposto).
Output: 4 arquivos de migration (3 de tabelas + 1 de seed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-CONTEXT.md

<interfaces>
Existing schema to reference (do NOT recreate — FK targets):
- public.clinics(id), public.units(id), public.users(id)
- public.bank_accounts(id) (cols: id, clinic_id, name, banco, agencia, conta, saldo_inicial, ativo) — 20260619001100
- public.chart_of_accounts(id), public.cost_centers(id) — 20260619001100
- public.financial_transactions(id) — 20260606000100 (NUMERIC(12,2), tenant_id)
- public.professionals(id), public.prosthetic_labs(id), public.lab_orders(id) — Phases 11/13
Established conventions (STATE.md): tenant column for new financial tables uses clinic_id; index every clinic_id; NUMERIC(12,2) money; deferred-FK pattern (D-14-15-02): forward references (payables.payout_id, rpa_records.reinf_event_id) are added in 20260621000500_phase16_alters.sql (Plan 03), NOT here.
2026 seed values (16-RESEARCH §"Brazilian Tax Withholding"): INSS brackets table + IRRF brackets table.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: payables migration (suppliers + payables + installments + recorrente_templates)</name>
  <files>supabase/migrations/20260621000100_payables_tables.sql</files>
  <read_first>
    - supabase/migrations/20260619001100_financial_cadastros_tables.sql (column/index/NUMERIC conventions to mirror)
    - supabase/migrations/20260620000200_faturamento_os_tables.sql (clinic_id + unit_id + created_by pattern, deferred-FK comment style)
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md §"Schema Design" lines ~439-510 + 667-683 (suppliers, payables, payable_installments, recorrente_templates DDL)
    - src/__tests__/financeiro16/migrations-phase16.test.ts (RED assertions this migration must satisfy)
  </read_first>
  <action>
Create `supabase/migrations/20260621000100_payables_tables.sql` with the 4 tables EXACTLY per RESEARCH DDL:

`public.suppliers` (D-01): id UUID PK; clinic_id UUID NOT NULL REFERENCES clinics(id) ON DELETE CASCADE; name TEXT NOT NULL; tipo TEXT NOT NULL CHECK (tipo IN ('laboratorio','material','servico','autonomo','pj','outro')); cnpj_cpf TEXT; pix_key TEXT; banco TEXT; agencia TEXT; conta TEXT; vinculo TEXT CHECK (vinculo IN ('clt','pj','autonomo')); professional_id UUID REFERENCES professionals(id) ON DELETE SET NULL; lab_id UUID REFERENCES prosthetic_labs(id) ON DELETE SET NULL; modalidade_inss TEXT DEFAULT '11pct' CHECK (modalidade_inss IN ('11pct','progressivo')); iss_retido_fonte BOOLEAN NOT NULL DEFAULT false; iss_override NUMERIC(5,4); ativo BOOLEAN NOT NULL DEFAULT true; deleted_at TIMESTAMPTZ; created_at/updated_at TIMESTAMPTZ NOT NULL DEFAULT now(). Indexes: idx_suppliers_clinic ON (clinic_id); idx_suppliers_cnpj_cpf ON (cnpj_cpf) WHERE cnpj_cpf IS NOT NULL.
  (Note: modalidade_inss/iss_retido_fonte/iss_override added per RESEARCH Open Questions 1/2 — configurable, not hardcoded.)

`public.payables` (D-02/D-04) — copy RESEARCH lines 463-490 verbatim: clinic_id, unit_id, supplier_id REFERENCES suppliers ON DELETE RESTRICT, account_id REFERENCES chart_of_accounts ON DELETE RESTRICT, cost_center_id REFERENCES cost_centers ON DELETE RESTRICT, bank_account_id REFERENCES bank_accounts ON DELETE SET NULL, descricao TEXT NOT NULL, valor_total NUMERIC(12,2) NOT NULL, origem TEXT NOT NULL DEFAULT 'manual' CHECK (origem IN ('manual','recorrente','lab','repasse','tributo')), lab_order_id UUID REFERENCES lab_orders(id) ON DELETE SET NULL, payout_id UUID (forward-ref — FK added in Plan 03 alters; add an explicit comment `-- FK to professional_payouts added in 20260621000500`), status TEXT NOT NULL DEFAULT 'pendente' CHECK (status IN ('pendente','parcial','pago','cancelado')), recorrente_template_id UUID, competencia TEXT, notes TEXT, document_id UUID, created_by UUID REFERENCES users(id), created_at/updated_at. Indexes: idx_payables_clinic, idx_payables_supplier, idx_payables_status ON (clinic_id,status), idx_payables_unit.

`public.payable_installments` (D-04) — copy RESEARCH lines 493-510 verbatim: clinic_id, payable_id REFERENCES payables ON DELETE CASCADE, numero INT NOT NULL DEFAULT 1, valor NUMERIC(12,2) NOT NULL, due_date DATE NOT NULL, paid_at TIMESTAMPTZ, valor_pago NUMERIC(12,2), financial_transaction_id UUID REFERENCES financial_transactions(id) ON DELETE SET NULL, status TEXT NOT NULL DEFAULT 'pendente' CHECK (status IN ('pendente','parcial','pago','cancelado')), created_at/updated_at. Indexes: idx_payable_inst_clinic, idx_payable_inst_payable, idx_payable_inst_due ON (clinic_id,due_date).

`public.recorrente_templates` (D-02b) — copy RESEARCH lines 668-682 verbatim: clinic_id, unit_id, supplier_id REFERENCES suppliers ON DELETE RESTRICT, account_id REFERENCES chart_of_accounts ON DELETE RESTRICT, cost_center_id REFERENCES cost_centers ON DELETE RESTRICT, descricao TEXT NOT NULL, valor NUMERIC(12,2) NOT NULL, dia_vencimento SMALLINT NOT NULL CHECK (dia_vencimento BETWEEN 1 AND 28), ativo BOOLEAN NOT NULL DEFAULT true, created_at/updated_at. Index idx_recorrente_clinic.

Do NOT enable RLS here (Plan 03 writes 20260621000600_phase16_rls.sql for ALL phase-16 tables in one file).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro16/migrations-phase16.test.ts -t "payables|suppliers|installment|recorrente" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File contains `CREATE TABLE public.suppliers`, `public.payables`, `public.payable_installments`, `public.recorrente_templates`
    - payables origem CHECK matches `/('manual', 'recorrente', 'lab', 'repasse', 'tributo')/` and status CHECK `/('pendente', 'parcial', 'pago', 'cancelado')/`
    - suppliers tipo CHECK matches the 6-value enum; modalidade_inss + iss_retido_fonte + iss_override columns present
    - Every table has a `CREATE INDEX ... (clinic_id)`
    - valor_total + valor are NUMERIC(12,2)
    - payables.payout_id has a comment noting forward-ref FK deferred to Plan 03
    - The migrations-phase16 assertions for these 4 tables go GREEN
  </acceptance_criteria>
  <done>payables migration written verbatim from RESEARCH; source-inspection rows for these 4 tables GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: reconciliation migration (bank_statements + statement_lines) + tax tables migration</name>
  <files>supabase/migrations/20260621000200_reconciliation_tables.sql, supabase/migrations/20260621000400_tax_tables.sql</files>
  <read_first>
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md §"Schema Design" lines ~512-553 (bank_statements/statement_lines) + §"Padrão 1" lines 175-192 (inss DDL) + lines 655-665 (iss_tax_tables) + Pitfall 1 (partial unique)
    - src/__tests__/financeiro16/migrations-phase16.test.ts (RED assertions: fitid uniqueness, vigencia index)
  </read_first>
  <action>
**20260621000200_reconciliation_tables.sql** — copy RESEARCH lines 513-553 verbatim:

`public.bank_statements` (D-05): clinic_id, bank_account_id REFERENCES bank_accounts ON DELETE CASCADE, fonte TEXT NOT NULL DEFAULT 'ofx' CHECK (fonte IN ('ofx','open_finance')), periodo_inicio DATE NOT NULL, periodo_fim DATE NOT NULL, filename TEXT, imported_by UUID REFERENCES users(id), created_at. Indexes idx_bank_statements_clinic, idx_bank_statements_account.

`public.statement_lines` (D-05/D-11): clinic_id, bank_account_id REFERENCES bank_accounts ON DELETE CASCADE, bank_statement_id REFERENCES bank_statements ON DELETE CASCADE, fitid TEXT, fitid_fallback TEXT, transaction_date DATE NOT NULL, amount NUMERIC(12,2) NOT NULL (positive=crédito, negative=débito), memo TEXT, check_number TEXT, reconciliation_status TEXT NOT NULL DEFAULT 'pendente' CHECK (reconciliation_status IN ('pendente','conciliado','ignorado')), matched_transaction_ids UUID[], fee_transaction_id UUID REFERENCES financial_transactions(id) ON DELETE SET NULL, created_at/updated_at.
  CRITICAL (Pitfall 1): the FITID uniqueness MUST be PARTIAL unique indexes, NOT table constraints, because NULL fitid would otherwise duplicate:
  `CREATE UNIQUE INDEX uq_statement_line_fitid ON public.statement_lines(bank_account_id, fitid) WHERE fitid IS NOT NULL;`
  `CREATE UNIQUE INDEX uq_statement_line_fitid_fallback ON public.statement_lines(bank_account_id, fitid_fallback) WHERE fitid_fallback IS NOT NULL;`
  (Override the RESEARCH table-level CONSTRAINT form — use partial indexes per Pitfall 1.)
  Indexes: idx_statement_lines_clinic, idx_statement_lines_account, idx_statement_lines_date ON (bank_account_id,transaction_date), idx_statement_lines_recon ON (clinic_id,reconciliation_status) WHERE reconciliation_status='pendente'.

**20260621000400_tax_tables.sql** (D-17) — copy RESEARCH §"Padrão 1" + lines 655-665:

`public.inss_tax_tables`: id UUID PK; vigencia_inicio DATE NOT NULL; vigencia_fim DATE; faixa_min NUMERIC(12,2) NOT NULL; faixa_max NUMERIC(12,2); aliquota NUMERIC(5,4) NOT NULL; parcela_deduzir NUMERIC(12,2) NOT NULL DEFAULT 0; teto NUMERIC(12,2); created_at. Index idx_inss_tax_vigencia ON (vigencia_inicio, vigencia_fim).

`public.irrf_tax_tables`: same columns as inss plus `formula_desconto TEXT` (NULLABLE — for the gradual band formula). Index idx_irrf_tax_vigencia ON (vigencia_inicio, vigencia_fim).

`public.iss_tax_tables` (lines 655-665): id, vigencia_inicio DATE NOT NULL, vigencia_fim DATE, codigo_ibge TEXT NOT NULL, municipio TEXT NOT NULL, aliquota NUMERIC(5,4) NOT NULL, servico_lc116 TEXT, created_at. Index idx_iss_tax_municipio ON (codigo_ibge, vigencia_inicio).

These tax tables are global reference data (no clinic_id — shared like glosa_motivos NULL-clinic seed in Phase 15). RLS for them (read-all, no tenant write) is handled in Plan 03's rls file.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro16/migrations-phase16.test.ts -t "statement|fitid|inss|irrf|iss|vigencia" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - reconciliation file contains `CREATE TABLE public.bank_statements` and `public.statement_lines`
    - statement_lines has TWO partial unique indexes: `WHERE fitid IS NOT NULL` and `WHERE fitid_fallback IS NOT NULL` (Pitfall 1)
    - tax file contains `public.inss_tax_tables`, `public.irrf_tax_tables`, `public.iss_tax_tables`
    - inss/irrf tables have vigencia_inicio + vigencia_fim + idx_*_tax_vigencia; irrf has formula_desconto column
    - iss_tax_tables has codigo_ibge + municipio + aliquota
    - migrations-phase16 rows for statement/fitid/tax GREEN
  </acceptance_criteria>
  <done>reconciliation + tax-table migrations written; FITID partial uniqueness + vigência indexes verified.</done>
</task>

<task type="auto">
  <name>Task 3: phase-16 seed (2026 tax brackets + supplier defaults)</name>
  <files>supabase/migrations/20260621000700_phase16_seed.sql</files>
  <read_first>
    - supabase/migrations/20260619001300_financial_cadastros_seed.sql (seed idempotency style — ON CONFLICT DO NOTHING)
    - supabase/migrations/20260620000500_faturamento_seed.sql (reference-data seed pattern for NULL-clinic rows)
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md §"Brazilian Tax Withholding" (INSS 2026 table lines 196-201, IRRF 2026 lines 209-213) + Pitfall 3/4
  </read_first>
  <action>
Create `supabase/migrations/20260621000700_phase16_seed.sql` seeding the 2026 tax brackets (D-17). Use `INSERT ... ON CONFLICT DO NOTHING` or guard with NOT EXISTS so re-push is idempotent.

`inss_tax_tables` — INSS progressivo 2026 (RESEARCH lines 196-201), 4 rows, vigencia_inicio='2026-01-01', vigencia_fim=NULL:
  (0.00, 1621.00, 0.0750, 0.00, NULL); (1621.01, 2902.84, 0.0900, 24.32, NULL); (2902.85, 4354.27, 0.1200, 111.40, NULL); (4354.28, 8475.55, 0.1400, 198.49, 8475.55).
  Plus document in a SQL comment that '11pct' modalidade uses teto faixa_max 8475.55 × 0.11 (handled in computeInss; the teto value lives in the last bracket's faixa_max/teto).

`irrf_tax_tables` — IRRF 2026 Lei 15.270/2025 (RESEARCH lines 209-213), vigencia_inicio='2026-01-01':
  (0.00, 5000.00, 0.0000, 0.00, NULL); (5000.01, 7350.00, 0.2750, 0.00, '978.62 - 0.133145 * base'); (7350.01, NULL, 0.2750, 908.73, NULL).
  (formula_desconto column holds the gradual-band string for the middle bracket.)

`iss_tax_tables` — seed a single default reference row for the most common service (RESEARCH §ISS): codigo_ibge '0000000' as a generic fallback, municipio 'Padrão', aliquota 0.0500 (5%), servico_lc116 '14.01', vigencia_inicio='2026-01-01'. Add a comment noting per-município rows are added by the admin / future migration; computeIss falls back to suppliers.iss_override when no município match (RESEARCH ISS step 3).

Optionally seed nothing for suppliers (per-tenant created via UI); add only a comment that supplier `tipo` values are seeded implicitly by the CHECK enum.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro16/migrations-phase16.test.ts -t "seed|2026" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - File contains `INSERT INTO public.inss_tax_tables` with 4 bracket rows and `2026-01-01`
    - File contains `INSERT INTO public.irrf_tax_tables` with the isento (5000) + gradual + 27.5% rows; gradual row has the formula string in formula_desconto
    - File contains `INSERT INTO public.iss_tax_tables` with a default 5% LC116 14.01 row
    - Seed uses ON CONFLICT DO NOTHING or NOT EXISTS guard (idempotent re-push)
    - migrations-phase16 seed assertions GREEN
  </acceptance_criteria>
  <done>2026 INSS/IRRF/ISS brackets seeded idempotently; tax source-inspection rows GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| migration DDL → tenant data store | New financial/PII tables (CNPJ/CPF, bank data) define the isolation surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | suppliers CNPJ/CPF + bank data | mitigate | clinic_id on suppliers + RLS (Plan 03) tenant-isolates; index on cnpj_cpf is partial, no cross-tenant exposure |
| T-16-04 | Tampering | duplicate OFX line via NULL fitid | mitigate | partial UNIQUE indexes on (bank_account_id,fitid) WHERE NOT NULL + fitid_fallback (Pitfall 1) |
| T-16-05 | Spoofing | tenant writes shared tax reference rows | mitigate | tax tables have no clinic_id; RLS (Plan 03) makes them read-only to tenants |
</threat_model>

<verification>
- `npx vitest run src/__tests__/financeiro16/migrations-phase16.test.ts` rows for suppliers/payables/installments/recorrente/bank_statements/statement_lines/inss/irrf/iss/seed all GREEN.
- No db push here (Plan 05 [BLOCKING]).
</verification>

<success_criteria>
4 migration files written verbatim from RESEARCH DDL; FITID idempotency + vigência-versioned tax tables + 2026 seed in place; source-inspection GREEN; no overlap with Plan 03 files.
</success_criteria>

<output>
After completion, create `.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-02-SUMMARY.md`
</output>
