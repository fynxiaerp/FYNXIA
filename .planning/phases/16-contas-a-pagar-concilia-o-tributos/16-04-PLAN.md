---
phase: 16
plan: 04
type: tdd
wave: 1
depends_on: [16-01]
files_modified:
  - src/lib/financeiro/tax-tables.ts
  - src/lib/financeiro/payout-math.ts
  - src/lib/financeiro/reconciliation.ts
  - src/lib/financeiro/ofx-parser.ts
  - src/lib/reinf/types.ts
  - src/lib/reinf/stub.ts
  - src/lib/reinf/index.ts
  - src/lib/integrations/types.ts
  - src/lib/validators/payable.ts
  - src/lib/validators/rpa.ts
autonomous: true
requirements: [FOP-02, TRIB-01, TRIB-02, TRIB-03]
user_setup: []

must_haves:
  truths:
    - "computeInss/computeIrrf/computeIss/computeRpaWithholdings are pure functions matching the RED specs (INSS teto, INSS-before-IRRF base, IRRF gradual band) (TRIB-02)"
    - "computePayout applies service>wildcard precedence, sem_regra→0%+alerta, applyDeductions subtracts only named deductions (TRIB-01)"
    - "matchExact/matchFuzzy/matchNToOne implement the 3-stage algorithm with fee=despesa (FOP-02)"
    - "parseOfxBuffer extracts FITID/date/amount/memo from OFX SGML (FOP-02)"
    - "StubReinfProvider + getReinfProvider factory mirror getFiscalProvider; ConnectorType gains 'reinf'/'open_finance' (TRIB-03)"
  artifacts:
    - path: "src/lib/financeiro/tax-tables.ts"
      provides: "computeInss, computeIrrf, computeIss, computeRpaWithholdings, selectBracketsByVigencia"
      exports: ["computeInss", "computeIrrf", "computeIss"]
    - path: "src/lib/financeiro/reconciliation.ts"
      provides: "matchExact, matchFuzzy, matchNToOne"
      exports: ["matchExact", "matchFuzzy", "matchNToOne"]
    - path: "src/lib/reinf/index.ts"
      provides: "getReinfProvider credential-gated factory"
      exports: ["getReinfProvider"]
  key_links:
    - from: "computeRpaWithholdings"
      to: "computeInss + computeIrrf (INSS-deducted base)"
      via: "irrf base = bruto − inss"
      pattern: "valorBruto - .*inss"
    - from: "getReinfProvider"
      to: "integration_connectors type=reinf"
      via: "credential_enc gate → StubReinfProvider"
      pattern: "StubReinfProvider"
---

<objective>
Wave 1 — implementar todas as funções PURAS de imposto, repasse, conciliação e OFX, mais o ReinfProvider (types/stub/factory) e os Zod validators, levando os scaffolds RED de 16-01 para GREEN. Código puro em `src/lib/financeiro/` para testabilidade máxima (Nyquist Dimension 8). Roda em paralelo com 16-02/16-03 (sem overlap de arquivos). NÃO toca DB (não depende do db push — testa via mocks/imports puros).

Purpose: TRIB-02 (tax math), TRIB-01 (payout math), FOP-02 (matching + OFX parse), TRIB-03 (ReinfProvider stub).
Output: 4 libs financeiro + 3 libs reinf + ConnectorType extension + 2 validators.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md

<interfaces>
RED specs to satisfy (Plan 01): src/lib/financeiro/__tests__/{tax-tables,payout-math,reconciliation,ofx-parser}.test.ts, src/lib/reinf/__tests__/reinf.test.ts.
Existing reference: src/lib/fiscal/iss.ts (computeIss integer-cent), src/lib/fiscal/index.ts (getFiscalProvider credential-gated factory pattern to mirror), src/lib/fiscal/stub.ts (StubFiscalProvider shape), src/lib/fiscal/types.ts (provider interface shape).
ofx-data-extractor API (RESEARCH §"OFX Parsing"): `import { Ofx } from 'ofx-data-extractor'`; `Ofx.fromBuffer(buffer)`; `.getTransactionsSummary()` → [{ FITID, DTPOSTED, TRNAMT, MEMO, NAME, CHECKNUM }]; `.getWarnings()`.
createAdminClient: src/lib/supabase/admin (service role) — used by getReinfProvider exactly like getFiscalProvider.
Zod v3 (pinned); no .default() in schemas (D-133); isMoney2dp pattern from src/lib/validators/charge.ts.
ConnectorType (src/lib/integrations/types.ts line 7) currently: 'asaas'|'whatsapp'|'email'|'nfse'|'banco'|'tiss' → add 'reinf'|'open_finance'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tax-tables + payout-math pure libs</name>
  <files>src/lib/financeiro/tax-tables.ts, src/lib/financeiro/payout-math.ts</files>
  <behavior>
    - computeInss(5000,'11pct',brackets) → {valor:550,aliquota:0.11}; computeInss(100000,'11pct',brackets) → valor 932.31 (teto)
    - computeInss(3000,'progressivo',brackets) → 248.60 (12% − 111.40)
    - computeIrrf(4000,irrf) → {valor:0}; computeIrrf(6000,irrf) gradual; computeIrrf(10000,irrf) → 27.5%−908.73
    - computeRpaWithholdings(6000,'11pct',inss,irrf,0): IRRF base = 6000−660 (INSS deducted, Pitfall 4)
    - computeIss(1200,0.05) === 60
    - computePayout precedence service>wildcard; sem_regra→0%+alerta:'sem_regra'; applyDeductions named-only
  </behavior>
  <read_first>
    - src/lib/financeiro/__tests__/tax-tables.test.ts + payout-math.test.ts (RED specs — implement to GREEN)
    - src/lib/fiscal/iss.ts (computeIss integer-cent reference — reuse or re-export)
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md §"Brazilian Tax Withholding" (computeInss/computeIrrf sketches) + §"Repasse Profissional" (computePayout)
  </read_first>
  <action>
**src/lib/financeiro/tax-tables.ts** (pure, NO 'use server'; importable by tests + actions):
  - Export interfaces `InssBracket` (faixa_min, faixa_max|null, aliquota, parcela_deduzir, teto|null), `IrrfBracket` (faixa_min, faixa_max|null, aliquota, parcela_deduzir, formula_desconto|null).
  - `export function computeInss(valorBruto, modalidade: '11pct'|'progressivo', brackets): { valor, aliquota }` per RESEARCH lines 760-775: 11pct → base=min(valorBruto, teto=last bracket faixa_max ?? 8475.55) × 0.11, integer-cent round; progressivo → find bracket, valor=max(0, round(valorBruto*aliquota − parcela_deduzir)).
  - `export function computeIrrf(baseCalculo, brackets): { valor, aliquota }` per RESEARCH lines 793-806: ≤5000 → 0; 5000.01–7350 → round(base*0.275 − (978.62 − 0.133145*base)), max 0; >7350 → round(base*0.275 − 908.73). Use brackets/formula_desconto when present, else the literal formula as fallback.
  - `export function computeIss(valorServicos, aliquota): number` — reuse/re-export from src/lib/fiscal/iss.ts (integer-cent).
  - `export function computeRpaWithholdings(valorBruto, modalidade, inssBrackets, irrfBrackets, issAliquota): { inss, irrf, iss, liquido }` — inss=computeInss(...).valor; irrfBase = valorBruto − inss (Pitfall 4); irrf=computeIrrf(irrfBase, irrfBrackets).valor; iss=computeIss(valorBruto, issAliquota); liquido = round(valorBruto − inss − irrf − iss). All integer-cent.
  - `export function selectBracketsByVigencia<T extends {vigencia_inicio:string; vigencia_fim:string|null}>(rows: T[], date: string): T[]` — filter rows where vigencia_inicio ≤ date && (vigencia_fim==null || vigencia_fim ≥ date).

**src/lib/financeiro/payout-math.ts** (pure):
  - Export `CommissionRule` { service_id: string; percentual: number; deducoes: string[] }, `PayoutDeductions` (Record<string,number>), `PayoutItemInput` { service_id, valor_recebido, descricao?, ... }.
  - `export function applyDeductions(valorRecebido, deducoes: PayoutDeductions, ruleDeducoes: string[]): number` — subtract only the named keys present in ruleDeducoes; integer-cent; empty ruleDeducoes ⇒ returns valorRecebido (D-13).
  - `export function computePayout(items, rules, deducoes): PayoutItemResult[]` per RESEARCH lines 900-919: for each item find rule by exact service_id, else service_id==='*', else null → { ...item, percentual:0, valor_repasse:0, alerta:'sem_regra' }; otherwise valor_base=applyDeductions(...), valor_repasse=round(valor_base*rule.percentual).
  - `export function aggregatePayout(itemResults): { valor_bruto, valor_base, valor_repasse }` — integer-cent sums for the professional_payouts header row.
  </action>
  <verify>
    <automated>npx vitest run src/lib/financeiro/__tests__/tax-tables.test.ts src/lib/financeiro/__tests__/payout-math.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - tax-tables.test.ts and payout-math.test.ts fully GREEN
    - tax-tables.ts exports computeInss, computeIrrf, computeIss, computeRpaWithholdings, selectBracketsByVigencia
    - computeRpaWithholdings computes irrf on (bruto − inss), not bruto (grep shows `valorBruto - inss` or equivalent)
    - payout-math.ts exports computePayout, applyDeductions, aggregatePayout; sem_regra path returns alerta:'sem_regra'
    - Neither file contains `'use server'` (pure libs)
    - tsc clean for both
  </acceptance_criteria>
  <done>Tax + payout math GREEN; INSS teto, INSS-before-IRRF, precedence all proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconciliation + ofx-parser pure libs</name>
  <files>src/lib/financeiro/reconciliation.ts, src/lib/financeiro/ofx-parser.ts</files>
  <behavior>
    - matchExact hits within 3 days + 0.01; null otherwise
    - matchFuzzy returns scored matches ordered desc; >=0.85 high, <0.5 excluded
    - matchNToOne({amount:1000},[600,395],5) → fee 5; null when no combo within tolerance; caps N
    - parseOfxBuffer(fixture) → 3 lines with FITID, Date, signed amount, memo
  </behavior>
  <read_first>
    - src/lib/financeiro/__tests__/reconciliation.test.ts + ofx-parser.test.ts (RED specs)
    - src/__tests__/financeiro16/fixtures/sample.ofx (the fixture parseOfxBuffer must read)
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md §"Algoritmo de Conciliação" + §"Code Examples" (matchExact impl) + §"OFX Parsing" (parseOfxBuffer)
  </read_first>
  <action>
**src/lib/financeiro/reconciliation.ts** (pure):
  - Export `StatementLineInput` { id, amount, transaction_date, memo, bank_account_id }, `TransactionRow` { id, amount, transaction_date, description, bank_account_id }, `ExactMatch`, `ScoredMatch` { transaction_id, score, confidence }, `NToOneMatch` { transaction_ids: string[], fee: number }.
  - `export function matchExact(line, candidates, dateTolerance=3, amountTolerance=0.01): ExactMatch|null` — RESEARCH lines 1093-1107 verbatim (days diff via /86400000, amount diff).
  - `export function matchFuzzy(line, candidates): ScoredMatch[]` — score = (1 − |amountDiff|/lineAmount)*0.6 + (1 − min(dateDiffDays,30)/30)*0.3 + memoSimilarity*0.1; memoSimilarity = simple normalized Levenshtein or token overlap in app layer (RESEARCH lines 344-350). Return only score≥0.5, sorted desc. Mark confidence 'high' when score≥0.85 else 'low'.
  - `export function matchNToOne(depositLine, candidates, tolerance=5.00): NToOneMatch|null` — RESEARCH lines 356-368: sort candidates by date proximity; try combinations N=2..min(candidates.length, MAX_N=20) until |sum − deposit| ≤ tolerance; return { transaction_ids, fee: round(deposit − sum) }; fee positive = bank/card fee (despesa, Pitfall 5). Null if no combo. Integer-cent.

**src/lib/financeiro/ofx-parser.ts**:
  - `export interface StatementLine { fitid: string; date: Date; amount: number; memo: string; check_number?: string }`
  - `export async function parseOfxBuffer(buffer: Buffer): Promise<{ account_id?: string; lines: StatementLine[]; warnings: string[] }>` per RESEARCH lines 299-316: `const ofx = await Ofx.fromBuffer(buffer)` (lenient mode if available); map `.getTransactionsSummary()` to lines (fitid=t.FITID, date=new Date(parse DTPOSTED YYYYMMDD), amount=Number(t.TRNAMT), memo=t.MEMO ?? t.NAME ?? '', check_number=t.CHECKNUM); warnings = ofx.getWarnings?.() ?? []. Handle the OFX DTPOSTED format (YYYYMMDD or YYYYMMDDHHMMSS) — parse first 8 chars to a Date.
  </action>
  <verify>
    <automated>npx vitest run src/lib/financeiro/__tests__/reconciliation.test.ts src/lib/financeiro/__tests__/ofx-parser.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - reconciliation.test.ts and ofx-parser.test.ts fully GREEN
    - reconciliation.ts exports matchExact, matchFuzzy, matchNToOne; matchNToOne caps N at ≤20 and returns fee as deposit−sum
    - ofx-parser.ts exports parseOfxBuffer using `Ofx.fromBuffer`
    - Neither file contains `'use server'`
    - tsc clean
  </acceptance_criteria>
  <done>3-stage matching + OFX parse GREEN against the real fixture.</done>
</task>

<task type="auto">
  <name>Task 3: ReinfProvider (types/stub/factory) + ConnectorType + Zod validators</name>
  <files>src/lib/reinf/types.ts, src/lib/reinf/stub.ts, src/lib/reinf/index.ts, src/lib/integrations/types.ts, src/lib/validators/payable.ts, src/lib/validators/rpa.ts</files>
  <read_first>
    - src/lib/reinf/__tests__/reinf.test.ts (RED specs)
    - src/lib/fiscal/types.ts + src/lib/fiscal/stub.ts + src/lib/fiscal/index.ts (EXACT pattern to mirror: interface → stub class → credential-gated factory)
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md §"Padrão 2 ReinfProvider" (types/stub/factory verbatim) + §"Code Examples" (getReinfProvider lines 1118-1134)
    - src/lib/validators/charge.ts (Zod v3 + isMoney2dp; no .default() D-133)
    - src/lib/validators/service-order.ts (validator structure to mirror)
  </read_first>
  <action>
**src/lib/reinf/types.ts** — copy RESEARCH lines 225-252 verbatim: `ReinfEventInput` (tipo 'R2010'|'R4020', competencia, clinic_id, prestador_cnpj?, prestador_nome?, valor_bruto?, valor_retencao_inss?, beneficiario_cnpj?, valor_retencao_irrf?, idempotency_key), `ReinfEventResult` (provider_ref, status 'pendente'|'transmitido'|'erro', protocolo?, error_message?), `ReinfProvider` interface (transmitir, consultar, retificar).

**src/lib/reinf/stub.ts** — copy RESEARCH lines 256-270 verbatim: `StubReinfProvider implements ReinfProvider` returning status 'transmitido' + protocolo `STUB-${Date.now()}` from transmitir; consultar/retificar mirror.

**src/lib/reinf/index.ts** — `import 'server-only'`; `export type { ... } from './types'`; `export async function getReinfProvider(clinicId: string): Promise<ReinfProvider>` mirroring getFiscalProvider (RESEARCH lines 1118-1134): query integration_connectors via createAdminClient WHERE type='reinf' AND clinic_id=clinicId AND status='enabled' is null deleted_at maybeSingle; no credential_enc → new StubReinfProvider(); with credential_enc → (gated comment) still StubReinfProvider() until real Tecnospeed adapter.

**src/lib/integrations/types.ts** — extend ConnectorType union (line 7) to add `'reinf' | 'open_finance'`. Do not change other exports.

**src/lib/validators/payable.ts** — Zod v3 (no .default()): `payableSchema` (supplierId uuid, descricao min 1, accountId uuid, costCenterId uuid, unitId uuid optional/nullable, valorTotal isMoney2dp positive, dueDate ISO date, parcelas int ≥1, origem enum default-less, notes optional, documentId optional), `baixaSchema` (installmentId uuid, bankAccountId uuid, valorPago isMoney2dp positive, dataPagamento ISO date, comprovanteDocumentId optional). Export inferred types.

**src/lib/validators/rpa.ts** — `rpaSchema` (supplierId uuid filtered autonomo at action layer, competencia /^\d{4}-\d{2}$/, dataPagamento ISO date, valorBruto isMoney2dp positive, modalidadeInss enum ('11pct','progressivo'), issOverride number optional, unitId uuid). Export inferred type.
  </action>
  <verify>
    <automated>npx vitest run src/lib/reinf/__tests__/reinf.test.ts 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | grep -E "reinf|integrations/types|validators/(payable|rpa)" | head</automated>
  </verify>
  <acceptance_criteria>
    - reinf.test.ts fully GREEN (StubReinfProvider.transmitir → 'transmitido'+protocolo; getReinfProvider → Stub without credential)
    - src/lib/reinf/index.ts contains `import 'server-only'` and `StubReinfProvider`
    - ConnectorType in src/lib/integrations/types.ts includes 'reinf' and 'open_finance'
    - payable.ts exports payableSchema + baixaSchema; rpa.ts exports rpaSchema; neither uses `.default(`
    - regression-guard-phase16 still GREEN (ConnectorType still has asaas/nfse/tiss/banco)
    - tsc clean for all 6 files
  </acceptance_criteria>
  <done>ReinfProvider stub+factory + ConnectorType extension + payable/rpa Zod validators; reinf.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pure libs → callers | Tax/matching functions consumed by Server Actions; correctness is the contract |
| getReinfProvider → integration_connectors | Credential-gated factory reads encrypted connector config (service role) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Tampering | float drift in tax/repasse math | mitigate | integer-cent rounding in every compute fn; unit tests assert exact numbers |
| T-16-11 | Information Disclosure | Reinf credential leak | mitigate | getReinfProvider uses createAdminClient server-only; credential_enc never returned, only gates stub-vs-real |
| T-16-12 | Spoofing | malformed OFX crashes parser | mitigate | ofx-data-extractor lenient mode + getWarnings; parseOfxBuffer returns warnings without throwing on minor issues |
</threat_model>

<verification>
- `npx vitest run src/lib/financeiro/__tests__/ src/lib/reinf/__tests__/` all GREEN.
- `npx tsc --noEmit` clean for the 10 touched files.
</verification>

<success_criteria>
All pure tax/payout/reconciliation/OFX functions GREEN; ReinfProvider stub+factory mirror FiscalProvider; ConnectorType extended; payable/rpa validators ready for Wave 3 actions.
</success_criteria>

<output>
After completion, create `.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-04-SUMMARY.md`
</output>
