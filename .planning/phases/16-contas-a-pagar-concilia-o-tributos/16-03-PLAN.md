---
phase: 16
plan: 03
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - supabase/migrations/20260621000300_payout_rpa_tables.sql
  - supabase/migrations/20260621000500_phase16_alters.sql
  - supabase/migrations/20260621000600_phase16_rls.sql
autonomous: true
requirements: [FOP-03, TRIB-01, TRIB-02, TRIB-03]
user_setup: []

must_haves:
  truths:
    - "professional_payouts/payout_items/rpa_records/reinf_events/unit_rpa_counters/competencia_fechamentos exist with clinic_id index + competência uniqueness (D-26)"
    - "next_rpa_number(p_unit_id) SECURITY DEFINER atomically increments per-unit (no MAX+1 race — Pitfall 2)"
    - "ALTERs add financial_transactions.reconciliation_status (D-08), bank_accounts.saldo_atual/data_abertura (D-12), professionals.supplier_id (D-01), integration_connectors 'reinf'/'open_finance' types (D-22)"
    - "Every phase-16 table has RLS USING get_my_tenant_id() + WITH CHECK with admin/superadmin write (D-23); tax tables read-only to tenants"
  artifacts:
    - path: "supabase/migrations/20260621000300_payout_rpa_tables.sql"
      provides: "payout/rpa/reinf/competência tables"
      contains: "CREATE TABLE public.rpa_records"
    - path: "supabase/migrations/20260621000500_phase16_alters.sql"
      provides: "ALTERs + next_rpa_number + forward-ref FKs"
      contains: "next_rpa_number"
    - path: "supabase/migrations/20260621000600_phase16_rls.sql"
      provides: "RLS write-by-role for all phase-16 tables"
      contains: "ENABLE ROW LEVEL SECURITY"
  key_links:
    - from: "next_rpa_number()"
      to: "unit_rpa_counters"
      via: "INSERT ON CONFLICT DO NOTHING + atomic UPDATE RETURNING"
      pattern: "last_rpa_number = last_rpa_number \\+ 1"
    - from: "rpa_records.pdf_storage_path"
      to: "never exposed to client"
      via: "RLS + action never selects it"
      pattern: "pdf_storage_path"
---

<objective>
Wave 1 — escrever as migrations de repasse/RPA/Reinf/competência, os ALTERs nas tabelas existentes, a função `next_rpa_number()` SECURITY DEFINER e a RLS write-by-role de TODAS as tabelas da Fase 16 em um único arquivo. NÃO faz db push (Plan 05 [BLOCKING]). Roda em paralelo com 16-02 (sem overlap de arquivos) e 16-04. DDL verbatim de 16-RESEARCH.md (linhas 555-650 + 685-707).

Purpose: FOP-03 (reconciliation_status no fluxo de caixa), TRIB-01 (payouts), TRIB-02 (rpa_records), TRIB-03 (reinf_events + connector types).
Output: 1 migration de tabelas + 1 de ALTERs/função + 1 de RLS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-CONTEXT.md

<interfaces>
Existing function to mirror EXACTLY (from 20260620000200_faturamento_os_tables.sql lines 206-222):
```sql
CREATE OR REPLACE FUNCTION public.next_os_number(p_unit_id UUID)
RETURNS TEXT LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
DECLARE next_num INT;
BEGIN
  INSERT INTO public.unit_os_counters (unit_id, clinic_id, last_os_number)
  SELECT u.id, u.clinic_id, 0 FROM public.units u WHERE u.id = p_unit_id
  ON CONFLICT (unit_id) DO NOTHING;
  UPDATE public.unit_os_counters SET last_os_number = last_os_number + 1
    WHERE unit_id = p_unit_id RETURNING last_os_number INTO next_num;
  RETURN 'OS-' || LPAD(next_num::TEXT, 6, '0');
END; $$;
```
RLS helpers (SECURITY DEFINER, Phase 7): get_my_tenant_id() → clinic_id; get_my_role() → text role.
Existing RLS write-by-role pattern (20260620000400_faturamento_rls.sql): SELECT USING (clinic_id = get_my_tenant_id()); write policies WITH CHECK (clinic_id = get_my_tenant_id() AND get_my_role() IN ('admin','superadmin',...)).
ConnectorType current values (src/lib/integrations/types.ts): 'asaas','whatsapp','email','nfse','banco','tiss'. integration_connectors.type CHECK must gain 'reinf','open_finance'.
Forward-ref FKs to resolve here (deferred from Plan 02): payables.payout_id → professional_payouts(id); rpa_records.reinf_event_id → reinf_events(id) (both tables created in THIS plan's Task 1, so the FK ALTERs go in Task 2 alters file after both exist).
Write roles per D-23 + RESEARCH Open Question 3: 'admin','superadmin' (no distinct 'financeiro' role in RBAC matrix — proxy.ts confirms). auditor/dpo/socio get SELECT-only (no write policy). receptionist/dentist excluded by module gate (proxy.ts), not table RLS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: payout/rpa/reinf/competência migration</name>
  <files>supabase/migrations/20260621000300_payout_rpa_tables.sql</files>
  <read_first>
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md §"Schema Design" lines ~555-650 (professional_payouts, payout_items, rpa_records, reinf_events) + §"Padrão 6" lines 392-408 (unit_rpa_counters, competencia_fechamentos)
    - supabase/migrations/20260620000200_faturamento_os_tables.sql (unit_*_counters table shape)
    - src/__tests__/financeiro16/migrations-phase16.test.ts (RED assertions)
  </read_first>
  <action>
Create `supabase/migrations/20260621000300_payout_rpa_tables.sql` with 6 tables verbatim from RESEARCH:

`public.professional_payouts` (TRIB-01, lines 556-574): clinic_id, unit_id, professional_id REFERENCES professionals ON DELETE RESTRICT, competencia TEXT NOT NULL, valor_bruto NUMERIC(12,2) NOT NULL, deducoes JSONB NOT NULL DEFAULT '{}', valor_base NUMERIC(12,2) NOT NULL, percentual NUMERIC(5,4) NOT NULL, valor_repasse NUMERIC(12,2) NOT NULL, status TEXT NOT NULL DEFAULT 'rascunho' CHECK (status IN ('rascunho','aprovado','pago')), payable_id UUID REFERENCES payables(id) ON DELETE SET NULL, created_by UUID REFERENCES users(id), created_at/updated_at, UNIQUE(clinic_id, professional_id, competencia). Indexes idx_payouts_clinic, idx_payouts_professional.

`public.payout_items` (lines 578-592): clinic_id, payout_id REFERENCES professional_payouts ON DELETE CASCADE, service_order_id UUID REFERENCES service_orders(id) ON DELETE SET NULL, statement_line_id UUID REFERENCES statement_lines(id) ON DELETE SET NULL, descricao TEXT NOT NULL, valor_recebido NUMERIC(12,2) NOT NULL, valor_base_item NUMERIC(12,2) NOT NULL, percentual_item NUMERIC(5,4) NOT NULL, valor_repasse_item NUMERIC(12,2) NOT NULL, created_at. Indexes idx_payout_items_payout, idx_payout_items_clinic.

`public.rpa_records` (TRIB-02, lines 595-627): clinic_id, unit_id, supplier_id REFERENCES suppliers ON DELETE RESTRICT, professional_id REFERENCES professionals ON DELETE SET NULL, payout_id REFERENCES professional_payouts ON DELETE SET NULL, numero TEXT NOT NULL, competencia TEXT NOT NULL, data_pagamento DATE NOT NULL, valor_bruto NUMERIC(12,2) NOT NULL, valor_inss/valor_irrf/valor_iss NUMERIC(12,2) NOT NULL DEFAULT 0, valor_liquido NUMERIC(12,2) NOT NULL, aliquota_inss/aliquota_irrf/aliquota_iss NUMERIC(5,4), municipio_ibge TEXT, regime_tributario TEXT, pdf_storage_path TEXT (comment: NUNCA retornar ao cliente; signed URL TTL=60s — Pitfall 7), status TEXT NOT NULL DEFAULT 'rascunho' CHECK (status IN ('rascunho','emitido','cancelado')), payable_id UUID REFERENCES payables(id) ON DELETE SET NULL, reinf_event_id UUID (forward-ref — FK added in Task 2 alters; comment it), modalidade_inss TEXT, created_by UUID REFERENCES users(id), created_at/updated_at. Indexes idx_rpa_clinic, idx_rpa_supplier, idx_rpa_competencia; `CREATE UNIQUE INDEX idx_rpa_numero ON public.rpa_records(clinic_id, numero);`.

`public.reinf_events` (TRIB-03, lines 630-647): clinic_id, unit_id, tipo TEXT NOT NULL CHECK (tipo IN ('R2010','R4020')), competencia TEXT NOT NULL, provider_ref TEXT, status TEXT NOT NULL DEFAULT 'pendente' CHECK (status IN ('pendente','transmitido','erro','retificado')), protocolo TEXT, payload JSONB NOT NULL, error_message TEXT, idempotency_key TEXT NOT NULL, created_by UUID REFERENCES users(id), created_at/updated_at, UNIQUE(clinic_id, idempotency_key). Indexes idx_reinf_clinic, idx_reinf_competencia.

`public.unit_rpa_counters` (D-26, lines 393-397): unit_id UUID PRIMARY KEY REFERENCES units(id) ON DELETE CASCADE, clinic_id UUID NOT NULL REFERENCES clinics(id) ON DELETE CASCADE, last_rpa_number INT NOT NULL DEFAULT 0.

`public.competencia_fechamentos` (D-26, lines 399-407): id, clinic_id, unit_id REFERENCES units ON DELETE CASCADE, competencia TEXT NOT NULL, fechado_at TIMESTAMPTZ NOT NULL DEFAULT now(), fechado_por UUID REFERENCES users(id), UNIQUE(clinic_id, unit_id, competencia). Index idx_competencia_clinic.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro16/migrations-phase16.test.ts -t "payout|rpa|reinf|competencia|counter" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File contains all 6 CREATE TABLE statements (professional_payouts, payout_items, rpa_records, reinf_events, unit_rpa_counters, competencia_fechamentos)
    - professional_payouts has UNIQUE(clinic_id, professional_id, competencia); competencia_fechamentos has UNIQUE(clinic_id, unit_id, competencia)
    - rpa_records has UNIQUE INDEX on (clinic_id, numero) and a pdf_storage_path comment about never exposing it
    - reinf_events tipo CHECK is `('R2010', 'R4020')`; UNIQUE(clinic_id, idempotency_key)
    - clinic_id index on payouts/payout_items/rpa/reinf/competencia
    - migrations-phase16 payout/rpa/reinf rows GREEN
  </acceptance_criteria>
  <done>6 payout/rpa/reinf/competência tables written verbatim; source-inspection GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: ALTERs + next_rpa_number + forward-ref FKs</name>
  <files>supabase/migrations/20260621000500_phase16_alters.sql</files>
  <read_first>
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md §"ALTER TABLE existentes" lines 685-707 + §"Pitfall 2" (next_rpa_number)
    - supabase/migrations/20260620000200_faturamento_os_tables.sql lines 206-222 (next_os_number to mirror)
    - supabase/migrations/20260619001100_financial_cadastros_tables.sql (financial_transactions ALTER pattern; bank_accounts columns)
    - migration that defines integration_connectors.type CHECK (Phase 9: grep `integration_connectors` in supabase/migrations)
  </read_first>
  <action>
Create `supabase/migrations/20260621000500_phase16_alters.sql`:

1. financial_transactions (D-08) — copy RESEARCH lines 689-694:
   `ALTER TABLE public.financial_transactions ADD COLUMN IF NOT EXISTS reconciliation_status TEXT NOT NULL DEFAULT 'pendente' CHECK (reconciliation_status IN ('pendente','baixado','conciliado')), ADD COLUMN IF NOT EXISTS statement_line_id UUID REFERENCES public.statement_lines(id) ON DELETE SET NULL;`
   `CREATE INDEX IF NOT EXISTS idx_ft_reconciliation_status ON public.financial_transactions(tenant_id, reconciliation_status) WHERE reconciliation_status != 'conciliado';`

2. bank_accounts (D-12) — copy RESEARCH lines 697-699:
   `ALTER TABLE public.bank_accounts ADD COLUMN IF NOT EXISTS data_abertura DATE, ADD COLUMN IF NOT EXISTS saldo_atual NUMERIC(12,2) NOT NULL DEFAULT 0;`

3. professionals (D-01) — RESEARCH lines 704-706:
   `ALTER TABLE public.professionals ADD COLUMN IF NOT EXISTS supplier_id UUID REFERENCES public.suppliers(id) ON DELETE SET NULL;`

4. integration_connectors type CHECK (D-22): drop and re-add the CHECK to include 'reinf' and 'open_finance'. Read the existing constraint name from the Phase 9 migration; emit:
   `ALTER TABLE public.integration_connectors DROP CONSTRAINT IF EXISTS <existing_type_check>;`
   `ALTER TABLE public.integration_connectors ADD CONSTRAINT integration_connectors_type_check CHECK (type IN ('asaas','whatsapp','email','nfse','banco','tiss','reinf','open_finance'));`
   (If the original used an inline unnamed CHECK, add the new named constraint and document that the old inline one is superseded; verify the exact constraint name during execution.)

5. Forward-ref FKs (deferred from Plan 02 / Task 1 — both target tables now exist):
   `ALTER TABLE public.payables ADD CONSTRAINT fk_payables_payout FOREIGN KEY (payout_id) REFERENCES public.professional_payouts(id) ON DELETE SET NULL;`
   `ALTER TABLE public.rpa_records ADD CONSTRAINT fk_rpa_reinf_event FOREIGN KEY (reinf_event_id) REFERENCES public.reinf_events(id) ON DELETE SET NULL;`

6. next_rpa_number (D-26, Pitfall 2) — mirror next_os_number EXACTLY but for unit_rpa_counters:
```sql
CREATE OR REPLACE FUNCTION public.next_rpa_number(p_unit_id UUID)
RETURNS TEXT LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
DECLARE next_num INT;
BEGIN
  INSERT INTO public.unit_rpa_counters (unit_id, clinic_id, last_rpa_number)
  SELECT u.id, u.clinic_id, 0 FROM public.units u WHERE u.id = p_unit_id
  ON CONFLICT (unit_id) DO NOTHING;
  UPDATE public.unit_rpa_counters SET last_rpa_number = last_rpa_number + 1
    WHERE unit_id = p_unit_id RETURNING last_rpa_number INTO next_num;
  RETURN 'RPA-' || LPAD(next_num::TEXT, 6, '0');
END; $$;
```
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro16/migrations-phase16.test.ts -t "ALTER|reconciliation_status|saldo_atual|supplier_id|reinf|open_finance|next_rpa_number" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File ALTERs financial_transactions (reconciliation_status + statement_line_id), bank_accounts (saldo_atual + data_abertura), professionals (supplier_id)
    - integration_connectors CHECK includes 'reinf' and 'open_finance'
    - Two forward-ref FK ALTERs (payables.payout_id, rpa_records.reinf_event_id)
    - `CREATE OR REPLACE FUNCTION public.next_rpa_number` present with `SECURITY DEFINER` and `last_rpa_number = last_rpa_number + 1`
    - migrations-phase16 ALTER + next_rpa_number rows GREEN
  </acceptance_criteria>
  <done>ALTERs + connector types + forward-ref FKs + atomic next_rpa_number written; source-inspection GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: RLS write-by-role for all phase-16 tables</name>
  <files>supabase/migrations/20260621000600_phase16_rls.sql</files>
  <read_first>
    - supabase/migrations/20260620000400_faturamento_rls.sql (write-by-role SELECT/WITH CHECK pattern to replicate)
    - supabase/migrations/20260606000200_financial_rls.sql (financial RLS baseline)
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-CONTEXT.md D-23 (admin/financeiro write; auditor/dpo/socio read-only; recepção/dentista sem acesso) + 16-RESEARCH Open Question 3 (use admin/superadmin as writers)
    - src/proxy.ts (READ_ONLY_ROLES, MODULE_PERMISSIONS — financeiro module gating)
  </read_first>
  <action>
Create `supabase/migrations/20260621000600_phase16_rls.sql` enabling RLS on every phase-16 table.

For each tenant-scoped table (suppliers, payables, payable_installments, recorrente_templates, bank_statements, statement_lines, professional_payouts, payout_items, rpa_records, reinf_events, unit_rpa_counters, competencia_fechamentos):
  - `ALTER TABLE public.<t> ENABLE ROW LEVEL SECURITY;`
  - SELECT policy: `USING (clinic_id = get_my_tenant_id())` (all allowed roles read; module gate in proxy excludes recepção/dentista at route level — D-23).
  - INSERT policy: `WITH CHECK (clinic_id = get_my_tenant_id() AND get_my_role() IN ('admin','superadmin'))`.
  - UPDATE policy: `USING (clinic_id = get_my_tenant_id() AND get_my_role() IN ('admin','superadmin')) WITH CHECK (clinic_id = get_my_tenant_id() AND get_my_role() IN ('admin','superadmin'))`.
  - DELETE policy: same role gate as UPDATE (soft-delete preferred in actions, but DELETE gated to admin/superadmin).
  (auditor/dpo/socio get SELECT only — no write policy applies to them → write denied. This is the write-by-role evolution per D-23. A SQL comment must state: "financeiro write = admin/superadmin; auditor/dpo/socio read-only via SELECT-only; recepção/dentista blocked at proxy module gate.")

For the global tax tables (inss_tax_tables, irrf_tax_tables, iss_tax_tables) — shared reference data, no clinic_id:
  - `ALTER TABLE ... ENABLE ROW LEVEL SECURITY;`
  - SELECT policy `USING (true)` (any authenticated user reads).
  - NO INSERT/UPDATE/DELETE policy for authenticated → tenant writes denied; only service role (migrations/seed) mutates. Comment: "tax tables are system reference; mutated only by migration/seed (service role bypasses RLS)."

End-of-file: a comment block listing the 15 tables and confirming each has RLS enabled.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/financeiro16/migrations-phase16.test.ts -t "RLS|WITH CHECK|get_my_role|ROW LEVEL" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Every tenant-scoped phase-16 table has `ENABLE ROW LEVEL SECURITY`
    - Write policies use `get_my_role() IN ('admin','superadmin')` with both USING and WITH CHECK on UPDATE
    - SELECT policies use `clinic_id = get_my_tenant_id()`
    - tax tables have SELECT USING(true) and NO authenticated write policy
    - `grep -c "WITH CHECK" supabase/migrations/20260621000600_phase16_rls.sql` ≥ 12
    - migrations-phase16 RLS write-by-role assertion GREEN
  </acceptance_criteria>
  <done>RLS for all 15 phase-16 tables: tenant-isolated, admin/superadmin write, auditor/dpo/socio read-only, tax tables read-only reference; source-inspection GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| authenticated user → financial/PII tables | Payouts, RPA (CPF/CNPJ, retenções), reinf payloads are sensitive — RLS is the isolation backstop |
| concurrent RPA emission → numbering | Two simultaneous emits must not collide on RPA numero |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Information Disclosure | cross-tenant read of payouts/RPA | mitigate | RLS USING (clinic_id = get_my_tenant_id()) on all 12 tenant tables + index on clinic_id |
| T-16-07 | Elevation of Privilege | read-only role writing CP/RPA | mitigate | INSERT/UPDATE WITH CHECK get_my_role() IN ('admin','superadmin') — auditor/dpo/socio denied (D-23) |
| T-16-08 | Tampering | RPA number race (MAX+1 dup) | mitigate | next_rpa_number SECURITY DEFINER atomic INSERT-ON-CONFLICT + UPDATE RETURNING (Pitfall 2) |
| T-16-09 | Tampering | tenant mutating shared tax tables | mitigate | tax tables RLS: SELECT-only for authenticated; mutation only via service-role migration/seed |
</threat_model>

<verification>
- `npx vitest run src/__tests__/financeiro16/migrations-phase16.test.ts` payout/rpa/reinf/ALTER/RLS/next_rpa_number rows GREEN.
- No db push here (Plan 05 [BLOCKING]).
</verification>

<success_criteria>
6 payout/rpa/reinf/competência tables + ALTERs + atomic next_rpa_number + full write-by-role RLS written verbatim from RESEARCH; source-inspection GREEN; no overlap with Plan 02 files.
</success_criteria>

<output>
After completion, create `.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-03-SUMMARY.md`
</output>
