---
phase: 16
plan: 09
type: execute
wave: 4
depends_on: [16-06, 16-07]
files_modified:
  - src/app/(dashboard)/clinica/financeiro/contas-a-pagar/page.tsx
  - src/components/financeiro/PayablesTable.tsx
  - src/components/financeiro/PayableFormDialog.tsx
  - src/components/financeiro/BaixaDialog.tsx
  - src/app/(dashboard)/clinica/financeiro/conciliacao/page.tsx
  - src/components/financeiro/ReconciliationUpload.tsx
  - src/components/financeiro/StatementLinesTable.tsx
  - src/components/financeiro/NToOneBuilder.tsx
  - src/components/financeiro/PrevistoxRealizadoChart.tsx
autonomous: true
requirements: [FOP-01, FOP-02, FOP-03]
user_setup: []

must_haves:
  truths:
    - "Contas a Pagar screen at /clinica/financeiro/contas-a-pagar lists payables with 3 KPI cards (A Vencer / Vencido / Pago no Mês) + nuqs-filtered TanStack table (status, fornecedor, vencimento range, unidade/centro de custo) (FOP-01)"
    - "PayableFormDialog creates/edits a CP (fornecedor + descrição + conta contábil + centro de custo + unidade + valor BRL + vencimento + parcelas + anexo) calling createPayable; BaixaDialog escolhe bank_account + valor (total/parcial) + comprovante calling baixarPayable (D-02a/D-03/D-04)"
    - "auditor/dpo/sócio see the payables table read-only — Ações write items hidden, only Ver Detalhes (D-23)"
    - "Conciliação screen at /clinica/financeiro/conciliacao imports OFX (ReconciliationUpload→importOFX) and renders stage-coded StatementLinesTable: green=exact auto-conciliado read-only, amber=fuzzy w/ Confirmar/Recusar inline, unmatched=DropdownMenu Criar Lançamento 1-click + Lançar como N:1 (D-06/D-07)"
    - "NToOneBuilder Sheet (side=right) sums selected lançamentos, shows Diferença as bank-fee despesa note, confirm enabled only within R$5,00 tolerance calling matchNToOne (D-09)"
    - "Previsto × Realizado tab shows cashFlowPrevistoVsRealizado split (pendente/baixado vs conciliado) with variance per conta contábil (D-08/FOP-03)"
  artifacts:
    - path: "src/app/(dashboard)/clinica/financeiro/contas-a-pagar/page.tsx"
      provides: "Contas a Pagar list RSC (Screen 1)"
      min_lines: 40
    - path: "src/components/financeiro/PayablesTable.tsx"
      provides: "TanStack v8 payables table + Ações + read-only gate"
      min_lines: 40
    - path: "src/components/financeiro/BaixaDialog.tsx"
      provides: "Baixa flow dialog (bank_account, total/parcial, comprovante)"
      min_lines: 30
    - path: "src/app/(dashboard)/clinica/financeiro/conciliacao/page.tsx"
      provides: "Conciliação RSC (Screen 2) with Tabs Extrato/Previsto×Realizado"
      min_lines: 40
    - path: "src/components/financeiro/StatementLinesTable.tsx"
      provides: "Stage-coded statement-lines table (green/amber/muted highlight)"
      min_lines: 50
    - path: "src/components/financeiro/NToOneBuilder.tsx"
      provides: "N:1 match Sheet with running sum + tolerance gate"
      min_lines: 40
  key_links:
    - from: "PayableFormDialog submit"
      to: "createPayable action"
      via: "server action call"
      pattern: "createPayable"
    - from: "BaixaDialog submit"
      to: "baixarPayable action"
      via: "server action call"
      pattern: "baixarPayable"
    - from: "ReconciliationUpload submit"
      to: "POST /api/financeiro/ofx (route, 16-07)"
      via: "fetch + FormData multipart (file + bankAccountId)"
      pattern: "fetch\('/api/financeiro/ofx'|FormData"
    - from: "NToOneBuilder confirm"
      to: "matchNToOne action"
      via: "server action call"
      pattern: "matchNToOne"
    - from: "StatementLinesTable Confirmar"
      to: "confirmMatch action"
      via: "server action call"
      pattern: "confirmMatch"
---

<objective>
Wave 4 — construir as duas telas mais centrais do "lado pagar": **Contas a Pagar** (Screen 1) e **Conciliação Bancária** (Screen 2), sob `/clinica/financeiro`, ligadas às Server Actions das ondas 3 (16-06 payables) e 16-07 (bank-statements/reconciliation). Toda a aparência segue `16-UI-SPEC.md` à risca (KPIs em 3 cards estilo CashFlowTotals, stage-coded reconciliation table, N:1 Sheet, statusBadgeClass, cópia pt-BR verbatim, formatBRL com U+2212, máscara BRL via padrão BankAccountFormDialog). Espelha o formato de 15-08-PLAN.md.

Purpose: FOP-01 (operar CP e baixa), FOP-02 (conciliar extrato OFX), FOP-03 (fluxo previsto × realizado).
Output: 2 páginas RSC + 7 componentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-UI-SPEC.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-CONTEXT.md

<interfaces>
Actions (Plan 06 — src/actions/payables.ts): listPayables(filters?:{status?,supplierId?,from?,to?,unitId?})→{success,payables}, getPayable(id), createPayable(rawInput), baixarPayable(rawInput:{installmentId,bankAccountId,valorPago,dataPagamento,comprovanteDocumentId?}), cancelarPayable(payableId,motivo), attachPayableDocument(payableId,documentId).
Actions (Plan 06 — src/actions/suppliers.ts): listSuppliers(filters?)→{success,suppliers}.
Actions (Plan 07 — src/actions/bank-statements.ts): importOFX({bankAccountId,filename,buffer:Buffer})→{success,statementId,imported,skipped,warnings}. IMPORTANTE: importOFX recebe um Node Buffer e e server-only — um Client Component NAO pode passar Buffer para uma Server Action. O caminho CANONICO do cliente e a ROTA POST /api/financeiro/ofx (16-07, runtime nodejs) via fetch + FormData multipart (campos: file=<File>, bankAccountId=<uuid>). A rota faz auth/role/ownership/size-limit e delega a importOFX. NUNCA chamar importOFX direto do componente.
Actions (Plan 07 — src/actions/reconciliation.ts): runAutoReconciliation(bankAccountId), suggestMatches(statementLineId)→ranked, confirmMatch({statementLineId,financialTransactionId}), matchNToOne({statementLineId,financialTransactionIds,feeAccountId?})→{success}, createReconciledTransaction({statementLineId,accountId,costCenterId,descricao,tipo})→{success}, cashFlowPrevistoVsRealizado(filters)→{previsto:{entradas,saidas,saldo},realizado:{...},porConta:[{conta,previsto,realizado,variance}]}.
Existing reuse: PageHeader (src/components/shell/PageHeader.tsx), EmptyState (src/components/shell/EmptyState.tsx), Button (@base-ui/react via src/components/ui/button.tsx — render-prop, NO asChild), formatBRL/formatBRLSigned (src/lib/format/money.ts), statusBadgeClass (src/components/financeiro/ReceivablesTable.tsx — copy/import the mapping), assertNotReadOnly (src/lib/auth/guards.ts — used in actions, not UI), BankAccountFormDialog BRL-mask handleAmountBlur pattern, TransactionModal date Popover+Calendar pattern, fluxo-de-caixa nuqs + NuqsAdapter (src/components/.../fluxo-de-caixa).
Read-only gate in UI (D-23): page reads role via `headers().get('x-user-role')` and read-only via `headers().get('x-read-only') === 'true'` (set by proxy.ts). Pass `readOnly` + `canWrite` props down to tables/dialogs; auditor/dpo/socio → readOnly=true → hide write Ações (show only "Ver Detalhes"). Writers = admin/superadmin (NAO existe papel 'financeiro' no enum — D-23 'admin/financeiro' satisfeito por admin/superadmin, igual a RLS de 16-03; auditor/dpo/socio readOnly). Server actions ALSO enforce via assertNotReadOnly — UI is a convenience guard only.
Container: `PageHeader` + `main className="p-6 max-w-5xl mx-auto w-full space-y-6"`.
nuqs: all filter Selects + date range use useQueryState; wrap list pages with NuqsAdapter from nuqs/adapters/next/app (mirror fluxo-de-caixa). Keys: ?status= ?supplier= ?from= ?to= ?unit= (CP); ?conta= ?tab= (conciliação).
Destructive (Cancelar CP): shadcn AlertDialog, copy verbatim from UI-SPEC §Copywriting "Destructive: Cancelar CP" / "Desfazer Conciliação". NEVER window.confirm.
Currency: formatBRL; negative = U+2212 (not hyphen) text-red-600; positive credit text-green-700; zero "R$ 0,00".
Dual-library rule: prefer `shadcn add <component>`; Button uses @base-ui render-prop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contas a Pagar screen — page + PayablesTable + PayableFormDialog + BaixaDialog</name>
  <files>src/app/(dashboard)/clinica/financeiro/contas-a-pagar/page.tsx, src/components/financeiro/PayablesTable.tsx, src/components/financeiro/PayableFormDialog.tsx, src/components/financeiro/BaixaDialog.tsx</files>
  <read_first>
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-UI-SPEC.md §"Screen 1: Contas a Pagar" (Layout, PayableFormDialog fields, BaixaDialog fields), §"Copywriting Contract" (CTA "Nova Conta a Pagar", empty state, "Baixa parcial…", "Destructive: Cancelar CP"), §"Interaction Contracts" (Read-Only Gate D-23, Currency Input BRL mask, Date Picker), §"Component Inventory"
    - src/actions/payables.ts (listPayables/createPayable/baixarPayable/cancelarPayable/attachPayableDocument exact return shapes — confirm field names like valor_total, valor_pago, due_date, status, supplier name join)
    - src/actions/suppliers.ts (listSuppliers — for the Fornecedor Select)
    - src/components/financeiro/ReceivablesTable.tsx (statusBadgeClass mapping to reuse; TanStack v8 column + DropdownMenu Ações pattern)
    - src/components/financeiro/BankAccountFormDialog.tsx (BRL handleAmountBlur + Dialog+RHF+zodResolver structure to mirror)
    - src/components/financeiro/TransactionModal.tsx (date Popover+Calendar field; accountId/costCenter Select pattern)
    - src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx (nuqs + NuqsAdapter + month/range nav; main layout)
    - src/lib/format/money.ts (formatBRL; U+2212 rule)
    - src/lib/auth/guards.ts (read-only header semantics; UI reads x-read-only via headers())
  </read_first>
  <action>
**contas-a-pagar/page.tsx** (RSC): `import { headers } from 'next/headers'`. PageHeader title "Contas a Pagar", breadcrumbs [{label:'Clínica',href:'/clinica'},{label:'Financeiro',href:'/clinica/financeiro'},{label:'Contas a Pagar'}]. Read `role = headers().get('x-user-role')`, `readOnly = headers().get('x-read-only') === 'true'`, `canWrite = !readOnly`. Header action: Button "Nova Conta a Pagar" (render-prop opens PayableFormDialog trigger) — render only when canWrite. Read filters from searchParams (status/supplier/from/to/unit). Call listPayables(filters) + listSuppliers() (for filter + form). Wrap body in NuqsAdapter. `main className="p-6 max-w-5xl mx-auto w-full space-y-6"`.
  - KPI row (3 cards, mirror CashFlowTotals layout, text-2xl font-semibold tabular-nums min-h-[72px]): "A Vencer" (sum pendente/parcial futuro → text-foreground), "Vencido" (sum vencido → text-red-600), "Pago no Mês" (sum pago no mês → text-green-700). Derive 'vencido' at read-time (due_date < today AND status != 'pago'/'cancelado') mirroring receivables D-04.
  - Filter bar (nuqs): Select Status (Todos/Pendente/Parcial/Pago/Vencido/Cancelado), Select Fornecedor (from listSuppliers), date range (Vencimento from/to via Popover+Calendar), Select Unidade/Centro de Custo. Each uses useQueryState in a small 'use client' FilterBar OR inline client wrapper.
  - `<PayablesTable rows={payables} suppliers={suppliers} canWrite={canWrite} />`.
  - EmptyState (no results): icon CreditCard, title "Nenhuma conta a pagar", body "Cadastre a primeira conta para controlar os pagamentos.", cta Button "Nova Conta a Pagar" (only if canWrite).

**PayablesTable.tsx** ('use client'): @tanstack/react-table v8. Columns: Vencimento (`format(due_date,'dd/MM/yyyy',{locale:ptBR})`), Fornecedor (supplier name), Descrição, Valor (`formatBRL(valor_total)` tabular-nums right), Pago (`formatBRL(valor_pago ?? 0)` tabular-nums), Status (Badge via statusBadgeClass: pendente/parcial=amber, pago=green, vencido=red, cancelado=muted), Ações (DropdownMenu, MoreHorizontal trigger size-8). Ações items (ONLY when canWrite): "Baixar" (opens BaixaDialog for the próxima parcela pendente), "Editar" (opens PayableFormDialog edit), "Anexar Documento" (attachPayableDocument), "Cancelar" (AlertDialog — copy verbatim "Cancelar Conta a Pagar" / body / actions "Manter"+"Cancelar CP" destructive → cancelarPayable). When `!canWrite`: render Ações column with single "Ver Detalhes" item only. After actions call router.refresh().

**PayableFormDialog.tsx** ('use client'): Dialog + RHF + zodResolver, mirror BankAccountFormDialog/TransactionModal. Fields per UI-SPEC: Fornecedor Select (required), Descrição Input (required), Conta Contábil Select (required, leaf accounts), Centro de Custo Select (required), Unidade Select (optional), Valor Total Input BRL-masked `valorStr` (handleAmountBlur), Data de Vencimento Popover+Calendar (required, dd/MM/yyyy ptBR), Parcelas Input number (default 1; if >1 note "Gera N parcelas com vencimentos mensais"), Notas Textarea (optional), Anexo file trigger (optional). Footer: Button variant="outline" "Cancelar" + Button "Salvar" (spinner on submit). Submit → createPayable; on success router.refresh() + close.

**BaixaDialog.tsx** ('use client'): Dialog + RHF, mirror BankAccountFormDialog. Triggered from Ações > Baixar. Fields: Conta Corrente Select bank_account (required, ativo=true), Data do Pagamento Popover+Calendar (required, default today), Valor Pago Input BRL-masked (required, pre-fill saldo_pendente; allow < for partial), Comprovante file upload (optional). If valorPago < saldo pendente → inline `Alert` "Baixa parcial — o saldo restante permanece em aberto." Submit label "Confirmar Baixa" → baixarPayable({installmentId,bankAccountId,valorPago,dataPagamento,comprovanteDocumentId?}); on success router.refresh().
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "contas-a-pagar|Payable|Baixa" | head && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/clinica/financeiro/contas-a-pagar/page.tsx` exists; `grep "listPayables" src/app/(dashboard)/clinica/financeiro/contas-a-pagar/page.tsx` present
    - page reads read-only: `grep "x-read-only" src/app/(dashboard)/clinica/financeiro/contas-a-pagar/page.tsx` present
    - `grep "function PayablesTable" src/components/financeiro/PayablesTable.tsx` present; table uses @tanstack/react-table (grep "useReactTable") and statusBadgeClass
    - PayablesTable hides write Ações when `!canWrite` (grep "canWrite")
    - `grep "createPayable" src/components/financeiro/PayableFormDialog.tsx` present
    - `grep "baixarPayable" src/components/financeiro/BaixaDialog.tsx` present; partial Alert "Baixa parcial" present
    - Cancelar uses AlertDialog (grep "AlertDialog") and never window.confirm (grep returns 0)
    - 3 KPI cards present (A Vencer / Vencido / Pago no Mês) — grep "A Vencer" and "Vencido"
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Contas a Pagar list + KPIs + nuqs filters + create/baixa/cancel wired to payables actions; read-only gate honored; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Conciliação screen — page + ReconciliationUpload + StatementLinesTable + NToOneBuilder + PrevistoxRealizadoChart</name>
  <files>src/app/(dashboard)/clinica/financeiro/conciliacao/page.tsx, src/components/financeiro/ReconciliationUpload.tsx, src/components/financeiro/StatementLinesTable.tsx, src/components/financeiro/NToOneBuilder.tsx, src/components/financeiro/PrevistoxRealizadoChart.tsx</files>
  <read_first>
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-UI-SPEC.md §"Screen 2: Conciliação" (Layout, row background colors, Status column, Ações per row, Criar Lançamento inline, NToOneBuilder, Previsto×Realizado), §"Copywriting Contract" (OFX success/fail, "Conciliar N:1", "Criar Lançamento", "Tudo conciliado", "Desfazer Conciliação"), §"Interaction Contracts" (Conciliation Stage Colors, N:1 tolerance R$5)
    - src/actions/bank-statements.ts (importOFX signature + return {imported,ignored,autoConciliated})
    - src/actions/reconciliation.ts (runAutoReconciliation/suggestMatches/confirmMatch/matchNToOne/createReconciledTransaction/cashFlowPrevistoVsRealizado return shapes; statement line fields: status, match_score, suggested_transaction, valor, data, historico/memo)
    - src/components/financeiro/ReceivablesTable.tsx (statusBadgeClass; TanStack v8 column patterns)
    - src/components/financeiro/CashFlowTotals.tsx OR src/components/prototipos/charts.tsx (3-card entradas/saídas/saldo layout for Previsto×Realizado)
    - src/components/financeiro/NfseEmitForm.tsx (Dialog + file/upload zone pattern for ReconciliationUpload)
    - src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx (nuqs + NuqsAdapter; main layout)
    - src/lib/format/money.ts (formatBRL; U+2212)
  </read_first>
  <action>
**conciliacao/page.tsx** (RSC): PageHeader title "Conciliação Bancária", breadcrumbs [Clínica → Financeiro → Conciliação]. Read role/read-only from headers (x-user-role/x-read-only) → canWrite. Header actions: Select Conta Corrente (required to load — nuqs ?conta=) + Button "Importar OFX" (only if canWrite, opens ReconciliationUpload). Read searchParams (conta/tab). Wrap in NuqsAdapter. `main className="p-6 max-w-5xl mx-auto w-full space-y-6"`.
  - KPI row (3 cards): "Total do Extrato" (sum abs amounts → text-foreground), "Conciliados" (sum conciliado → text-green-700), "Pendentes" (count pendente → text-amber-700).
  - shadcn Tabs (nuqs ?tab=, default "extrato"): Tab "Extrato & Matching" → `<StatementLinesTable conta={conta} lines={...} canWrite={canWrite} />`; Tab "Previsto × Realizado" → `<PrevistoxRealizadoChart data={await cashFlowPrevistoVsRealizado(filters)} />`.
  - When no conta selected → prompt to pick conta corrente. EmptyStates (copy verbatim): sem extrato "Nenhum extrato importado" / "Importe um arquivo OFX para iniciar a conciliação bancária."; todas conciliadas "Tudo conciliado" / "Nenhuma linha pendente para este período."

**ReconciliationUpload.tsx** ('use client'): Dialog. Drop zone `border-2 border-dashed border-border rounded-lg p-8 text-center`, label "Arraste o arquivo OFX ou clique para selecionar", sub-label text-muted-foreground text-xs "Formatos: OFX (SGML ou XML) — máx. 5 MB". `<input type="file" accept=".ofx">`. Conta Corrente Select (required, pre-filled from page filter). Ao submeter, montar `const fd = new FormData(); fd.append('file', file); fd.append('bankAccountId', bankAccountId);` e `await fetch('/api/financeiro/ofx', { method: 'POST', body: fd })` (NUNCA chamar a Server Action importOFX direto — Buffer nao trafega do cliente; a rota nodejs de 16-07 faz auth/ownership/size-limit e delega a importOFX). Ler o JSON da resposta { success, imported, skipped, warnings }. On success Alert "[imported] transações importadas, [skipped] já existiam (ignoradas)." (warnings, se houver, em texto secundario). On error (res nao-ok ou success=false) Alert variant="destructive" "Não foi possível importar o arquivo. Verifique se é um OFX válido (máx. 5 MB) e tente novamente." router.refresh() on success.

**StatementLinesTable.tsx** ('use client'): @tanstack/react-table v8. Columns: Data (dd/MM/yyyy ptBR), Histórico (memo), Valor (formatBRL tabular-nums; negative U+2212 red, positive green), Status (Badge: Conciliado=green bg-green-100, Sugestão=amber bg-amber-100, Pendente=muted), Correspondência (matched transaction descrição or "—"), Ações.
  Row background by status: conciliado (exact auto) → `bg-green-50 dark:bg-green-950/20`; fuzzy score>=0.85 → `bg-amber-50 dark:bg-amber-950/20`; fuzzy 0.5–0.84 → `bg-muted`; pendente/sem sugestão → no highlight.
  Ações per row:
    - Sugestão rows: inline Button "Confirmar" (sm outline → confirmMatch({statementLineId,financialTransactionId})) + Button "Recusar" (sm ghost → dismiss suggestion). After confirm row turns green. (canWrite only.)
    - Pendente rows: DropdownMenu (canWrite only): "Buscar Lançamento" (opens match selector — may reuse suggestMatches list), "Lançar como N:1" (opens NToOneBuilder for this line), "Criar Lançamento" (opens inline create form: Tipo auto from amount sign, Conta Contábil Select required, Centro de Custo Select, Descrição pre-filled from memo → createReconciledTransaction), "Ignorar".
    - Conciliado rows: Button "Desfazer" (sm ghost; AlertDialog copy verbatim "Desfazer Conciliação?" / body / "Cancelar"+"Desfazer"; disabled/hidden when !canWrite).
  Hold selected N:1 line in component state and render `<NToOneBuilder line={...} open=.../>`.

**NToOneBuilder.tsx** ('use client'): shadcn Sheet side="right". Left: selected OFX line details (valor, data, memo). Right: searchable list of pending financial_transactions (checkboxes; show amount/date/descrição). Running total "Selecionado: R$ X.XXX,XX" updates on selection. Difference indicator "Diferença: R$ X,XX"; if diff > 0 show "Taxa bancária de R$ X,XX será lançada como despesa". Confirm Button "Conciliar N:1" disabled until `Math.abs(lineAmount - selectedSum) <= 5.00` (R$5 tolerance). On confirm → matchNToOne({statementLineId,financialTransactionIds,feeAccountId?}); router.refresh() + close.

**PrevistoxRealizadoChart.tsx** ('use client' or server): two-column comparison (Previsto = pendente/baixado; Realizado = conciliado), each showing Entradas/Saídas/Saldo (3-card pattern like CashFlowTotals, tabular-nums). Below: grouped list por conta contábil with Variância column (positive text-green-700, negative text-red-600 U+2212). Consumes cashFlowPrevistoVsRealizado data passed as prop.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "conciliacao|Statement|NToOne|Reconciliation|Previsto" | head && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/clinica/financeiro/conciliacao/page.tsx` exists; renders Tabs Extrato & Matching + Previsto × Realizado; reads x-read-only
    - `grep "function StatementLinesTable" src/components/financeiro/StatementLinesTable.tsx` present; row highlight mapping present (grep "bg-green-50" and "bg-amber-50")
    - StatementLinesTable Sugestão rows call confirmMatch (grep "confirmMatch"); Pendente rows offer "Criar Lançamento" (grep "createReconciledTransaction") + "Lançar como N:1"
    - `grep "function NToOneBuilder" src/components/financeiro/NToOneBuilder.tsx` present; calls matchNToOne; tolerance gate present (grep "5" near disabled / "Conciliar N:1")
    - ReconciliationUpload POSTa para a rota (NAO chama importOFX direto): `grep -E "fetch\('/api/financeiro/ofx'|FormData" src/components/financeiro/ReconciliationUpload.tsx` present E `grep "importOFX" src/components/financeiro/ReconciliationUpload.tsx` retorna 0; OFX success Alert copy present
    - PrevistoxRealizadoChart consumes cashFlowPrevistoVsRealizado (grep "cashFlowPrevistoVsRealizado" in page) with variance column
    - Desfazer uses AlertDialog (grep "AlertDialog"); window.confirm grep returns 0 across the 5 files
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Conciliação screen: OFX import + stage-coded matching table + 1-click create + N:1 Sheet with tolerance + Previsto×Realizado; read-only gate honored; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client UI → createPayable/baixarPayable/matchNToOne | User-triggered money/CP side-effects |
| read-only role → write Ações | auditor/dpo/socio must not reach write controls |
| OFX file upload → importOFX | Untrusted file content crosses into reconciliation |
| supplier bank data (CNPJ/CPF/PIX) render | Sensitive cadastral data shown only to authorized roles |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-40 | Elevation of Privilege | read-only role hits write Ações (Baixar/Cancelar/Confirmar/N:1) | mitigate | UI hides write items when canWrite=false (x-read-only header); server actions enforce assertNotReadOnly — UI guard is convenience only, cannot bypass server gate (D-23) |
| T-16-41 | Tampering | client-side baixa/N:1 bypass of CAS/tolerance | accept | Button enable/tolerance is client guard; baixarPayable CAS + matchNToOne tolerance enforced server-side (16-06/16-07). UI cannot bypass |
| T-16-42 | Repudiation | cancel CP / desfazer conciliação without motivo trail | mitigate | Cancelar CP + Desfazer Conciliação use AlertDialog (copy verbatim); cancelarPayable on paid routes through alçada server-side (D-24) |
| T-16-43 | Information Disclosure | supplier CNPJ/CPF/bank rendered to unauthorized role | mitigate | sensitive supplier fields rendered only within canWrite/detail context; tables show fornecedor name only; read-only roles see no bank/PIX columns |
| T-16-44 | Tampering | malformed/oversized OFX upload | accept | client size hint (5 MB) + importOFX validates/idempotent (FITID) server-side (D-11) |
</threat_model>

<verification>
- `npm run build` green (Vitest/tsc do not catch 'use server' errors — build does, per project memory).
- Manual (Vercel): CP list → Nova Conta a Pagar (2 parcelas) → Baixar parcial → status 'parcial', KPI atualiza. Conciliação → Importar OFX → linhas exatas verdes read-only, fuzzy âmbar com Confirmar; pendente → Lançar como N:1 com soma dentro de R$5 → Conciliar N:1; Previsto × Realizado mostra variância.
</verification>

<success_criteria>
FOP-01/FOP-02/FOP-03 usáveis: Contas a Pagar (KPIs + filtros nuqs + create/baixa/cancel) e Conciliação (OFX + matching stage-coded + N:1 + previsto×realizado), com gate read-only (D-23) e cópia pt-BR do UI-SPEC; build verde.
</success_criteria>

<output>
After completion, create `.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-09-SUMMARY.md`
</output>
