---
phase: 16
plan: 10
type: execute
wave: 4
depends_on: [16-08]
files_modified:
  - src/app/(dashboard)/clinica/financeiro/repasse/page.tsx
  - src/components/financeiro/PayoutTable.tsx
  - src/components/financeiro/PayoutDemonstrativoSheet.tsx
  - src/components/financeiro/CompetenciaSelector.tsx
  - src/app/(dashboard)/clinica/financeiro/rpa/page.tsx
  - src/components/financeiro/RpaTable.tsx
  - src/components/financeiro/RpaFormDialog.tsx
  - src/components/financeiro/ReinfStatusBadge.tsx
  - src/app/(dashboard)/clinica/financeiro/page.tsx
autonomous: true
requirements: [TRIB-01, TRIB-02, TRIB-03]
user_setup: []

must_haves:
  truths:
    - "Repasse screen at /clinica/financeiro/repasse shows CompetenciaSelector (prev/next + 'Jun/2026' label, nuqs), 3 KPI cards (Total Bruto / Total a Repassar text-primary / Profissionais), PayoutTable grouped by professional, and Fechar Competência destructive AlertDialog calling fecharCompetencia (D-26, TRIB-01)"
    - "PayoutDemonstrativoSheet (side=right) shows per-professional line items (deduções accordion → base → % → Total a Repassar text-primary font-semibold) from demonstrativo + PDF export via signed URL (D-13/D-15)"
    - "PayoutTable Ações Gerar CP calls a payable-from-repasse path and Gerar RPA navigates to /rpa pre-filled (D-02d/D-16)"
    - "RPA screen at /clinica/financeiro/rpa shows CompetenciaSelector + 3 KPIs (RPAs Emitidos / INSS Total / IRRF Total) + RpaTable + Tributos a Recolher section + Emitir RPA button (TRIB-02)"
    - "RpaFormDialog computes INSS/IRRF/ISS Estimativa preview on BRL blur (labeled 'Estimativa — valores definitivos calculados no servidor ao emitir'), calls gerarRpa on submit; PDF opened via signed URL only, never pdf_storage_path (D-17/D-20)"
    - "ReinfStatusBadge renders EFD-Reinf stub status (pendente=amber/transmitido=green/erro=red + STUB tooltip) (D-18/D-22, TRIB-03); financeiro hub gains 4 cards (D-25)"
  artifacts:
    - path: "src/app/(dashboard)/clinica/financeiro/repasse/page.tsx"
      provides: "Repasse list RSC (Screen 3)"
      min_lines: 40
    - path: "src/components/financeiro/CompetenciaSelector.tsx"
      provides: "Reusable competência prev/next nuqs selector (Repasse + RPA)"
      min_lines: 25
    - path: "src/components/financeiro/PayoutDemonstrativoSheet.tsx"
      provides: "Per-professional demonstrativo Sheet + PDF export"
      min_lines: 40
    - path: "src/app/(dashboard)/clinica/financeiro/rpa/page.tsx"
      provides: "RPA & Tributos RSC (Screen 4)"
      min_lines: 40
    - path: "src/components/financeiro/RpaFormDialog.tsx"
      provides: "RPA emit dialog + Estimativa withholding preview"
      min_lines: 40
    - path: "src/components/financeiro/ReinfStatusBadge.tsx"
      provides: "EFD-Reinf stub status badge + tooltip"
      min_lines: 15
  key_links:
    - from: "PayoutTable Fechar Competência"
      to: "fecharCompetencia action"
      via: "server action call"
      pattern: "fecharCompetencia"
    - from: "PayoutDemonstrativoSheet"
      to: "getDemonstrativo action"
      via: "server action call"
      pattern: "getDemonstrativo"
    - from: "RpaFormDialog submit"
      to: "gerarRpa action"
      via: "server action call"
      pattern: "gerarRpa"
    - from: "financeiro/page.tsx nav grid"
      to: "/clinica/financeiro/{contas-a-pagar,conciliacao,repasse,rpa}"
      via: "nav cards"
      pattern: "contas-a-pagar"
---

<objective>
Wave 4 (final) — construir **Repasse do Profissional** (Screen 3) e **RPA & Tributos** (Screen 4) sob `/clinica/financeiro`, adicionar os **4 nav cards** do financeiro hub (D-25) e confirmar que os 4 subroutes já caem no módulo `financeiro` do proxy. Liga às Server Actions de 16-08 (professional-payouts/rpa/reinf) e reaproveita a origem 'repasse' de CP (16-06). Toda a aparência segue `16-UI-SPEC.md` à risca: CompetenciaSelector reusável, demonstrativo com "Total a Repassar" como text-primary, RPA com preview "Estimativa" recalculada no servidor, PDF via signed URL, ReinfStatusBadge stub. Espelha o formato de 15-08-PLAN.md (telas + nav cards + registro de módulo).

Purpose: TRIB-01 (repasse + demonstrativo + CP ao profissional), TRIB-02 (RPA com INSS/IRRF/ISS + PDF), TRIB-03 (EFD-Reinf stub-gated).
Output: 2 páginas RSC + 6 componentes + atualização do hub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-UI-SPEC.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-CONTEXT.md

<interfaces>
Actions (Plan 08 — src/actions/professional-payouts.ts): computePayouts(competencia,unitId?)→{success,payouts}, getDemonstrativo(payoutId)→{success,payout,items} (nome EXATO exportado por 16-08; payout = linha professional_payouts {valor_bruto,deducoes,valor_base,percentual,valor_repasse,status}, items = payout_items[] {service_order_id,descricao,valor_recebido,valor_base_item,percentual_item,valor_repasse_item}; confirmar shape no 16-08-SUMMARY), fecharCompetencia(competencia,unitId)→{success}. Repasse "Gerar CP" → createPayableFromRepasse (src/actions/payables.ts, origem='repasse', D-02d) then set professional_payouts.payable_id — confirm whether 16-08 exposes a wrapper (e.g. gerarCpRepasse) or the UI calls createPayableFromRepasse directly; prefer the action the SUMMARY of 16-08 documents.
Actions (Plan 08 — src/actions/rpa.ts): listRpas(competencia,unitId?)→{success,rpas}, computeRpaPreview(input)→{inss,irrf,iss,liquido} (client-callable estimate; if not exported, compute client-side from tax bracket props passed by the page — UI-SPEC §RPA Calculated Preview), gerarRpa(input)→{success,rpaId,numero}, getRpaPdfUrl(rpaId)→{success,url} (signed, TTL=60s), cancelarRpa(rpaId,motivo) (routes through alçada D-24).
Actions (Plan 08 — src/actions/reinf.ts): reinf status read for a competência/rpa (stub) → returns status ('pendente'|'transmitido'|'erro') + isStub flag.
Tributos a Recolher: read from payables origem='tributo' (createTributoPayable in payables.ts) — list via listPayables({status,...}) filtered to origem tributo, or a dedicated getter if 16-08 SUMMARY documents one. Baixar links to Contas a Pagar baixa flow (route to /contas-a-pagar or open BaixaDialog).
Suppliers for autônomo: listSuppliers({tipo:'autonomo'}) (src/actions/suppliers.ts).
Existing reuse: PageHeader, EmptyState, Button (@base-ui render-prop), formatBRL/formatBRLSigned (src/lib/format/money.ts), statusBadgeClass (ReceivablesTable.tsx), Badge/Tooltip/Sheet/Accordion/Dialog/AlertDialog/Select/Popover/Calendar/Switch (shadcn), BankAccountFormDialog BRL handleAmountBlur, NfseEmitForm Dialog+RHF+calculated-panel pattern, OsSheet Sheet+Accordion pattern, fluxo-de-caixa nuqs month nav + NuqsAdapter.
Read-only gate (D-23): page reads `headers().get('x-user-role')` + `headers().get('x-read-only')==='true'` → canWrite; auditor/dpo/socio readOnly → hide Aprovar/Gerar CP/Gerar RPA/Fechar Competência/Emitir RPA/Cancelar. Server actions enforce assertNotReadOnly (src/lib/auth/guards.ts).
Proxy module (D-25): src/proxy.ts already maps prefix '/clinica/financeiro' → module 'financeiro' in ROUTE_MODULE_MAP; the 4 subroutes (/contas-a-pagar,/conciliacao,/repasse,/rpa) inherit it automatically. NO new ModuleKey needed — verify the prefix mapping exists; do NOT add per-subroute entries.
Container: `PageHeader` + `main className="p-6 max-w-5xl mx-auto w-full space-y-6"`.
CompetenciaSelector: 'use client', useQueryState('competencia') nuqs, label `text-sm font-semibold capitalize` "Jun/2026" + prev/next chevron buttons (mirror fluxo-de-caixa month nav). Reused by Repasse and RPA pages.
PDF: getRpaPdfUrl / demonstrativo PDF → signed URL only; `window.open(url,'_blank')`; button label "Visualizar PDF" / "Exportar PDF"; NEVER render pdf_storage_path in client.
Dual-library rule: prefer shadcn; Button uses @base-ui render-prop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repasse screen — page + CompetenciaSelector + PayoutTable + PayoutDemonstrativoSheet</name>
  <files>src/app/(dashboard)/clinica/financeiro/repasse/page.tsx, src/components/financeiro/CompetenciaSelector.tsx, src/components/financeiro/PayoutTable.tsx, src/components/financeiro/PayoutDemonstrativoSheet.tsx</files>
  <read_first>
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-UI-SPEC.md §"Screen 3: Repasse do Profissional" (Layout, PayoutDemonstrativoSheet content), §"Copywriting Contract" (CTA "Fechar Competência", empty state, "Repasse sem regra alert", "Destructive: Fechar Competência"), §"Interaction Contracts" (Competência Selector, PDF Download, Read-Only Gate)
    - src/actions/professional-payouts.ts (computePayouts/getDemonstrativo/fecharCompetencia exact shapes)
    - src/actions/payables.ts (createPayableFromRepasse — Gerar CP path) AND 16-08-SUMMARY.md (confirm whether a gerarCpRepasse wrapper exists)
    - src/components/financeiro/OsSheet.tsx (Sheet side=right + Accordion + tabs pattern for PayoutDemonstrativoSheet)
    - src/components/financeiro/ReceivablesTable.tsx (statusBadgeClass; TanStack v8 + DropdownMenu Ações)
    - src/app/(dashboard)/clinica/financeiro/fluxo-de-caixa/page.tsx (nuqs month nav + NuqsAdapter; main layout — model for CompetenciaSelector)
    - src/lib/format/money.ts (formatBRL)
  </read_first>
  <action>
**CompetenciaSelector.tsx** ('use client', BUILD FIRST — reused by RPA too): useQueryState('competencia') (default current YYYY-MM). Renders prev chevron Button (ghost size-8) + label `<span className="text-sm font-semibold capitalize">` formatted "Jun/2026" (date-fns ptBR `MMM/yyyy`, capitalize) + next chevron Button. Prev/next shift competência by ±1 month, preserving other nuqs filters (mirror monthHref pattern). Export default.

**repasse/page.tsx** (RSC): PageHeader title "Repasse de Profissionais", breadcrumbs [Clínica → Financeiro → Repasse]. Read role/x-read-only → canWrite. Header actions: `<CompetenciaSelector />` + Select Unidade (nuqs ?unit=) + Button "Fechar Competência" (canWrite only; AlertDialog). Read searchParams (competencia/unit). Call computePayouts(competencia,unit). Wrap NuqsAdapter. main p-6 max-w-5xl space-y-6.
  - KPI row (3 cards): "Total Bruto Recebido" (text-foreground), "Total a Repassar" (text-primary — THE key metric, text-2xl font-semibold tabular-nums), "Profissionais" (count text-foreground).
  - Alert (competência fechada): variant default, LockIcon, "Competência [MÊS] fechada. Novos recebimentos irão para [MÊS+1]." (copy from UI-SPEC).
  - Alert (itens sem_regra > 0): variant destructive "X procedimentos sem regra de comissão configurada. Repasse zerado nesses itens."
  - `<PayoutTable rows={payouts} canWrite={canWrite} competencia={competencia} />`.
  - EmptyState: icon Users, title "Nenhum repasse calculado", body "Concilie os recebimentos do período para calcular os repasses automaticamente."
  - Fechar Competência AlertDialog: copy verbatim title "Fechar Competência [MÊS]?" / body "Após fechar, novos recebimentos irão para a próxima competência. Esta ação não pode ser desfeita." / actions "Cancelar" (outline) + "Fechar Competência" (destructive) → fecharCompetencia(competencia,unit). router.refresh().

**PayoutTable.tsx** ('use client'): @tanstack/react-table v8. Columns: Profissional, Competência, Bruto (formatBRL), Deduções (formatBRL), Base (formatBRL), % , Repasse (formatBRL tabular-nums font-semibold), Status (Badge: rascunho=outline, aprovado=default cyan, pago=green), Ações (DropdownMenu, canWrite only): "Ver Demonstrativo" (opens PayoutDemonstrativoSheet), "Aprovar" (rascunho→aprovado), "Gerar CP" (only when aprovado → createPayableFromRepasse/gerarCpRepasse), "Gerar RPA" (router.push to `/clinica/financeiro/rpa?competencia=...&supplier=...` pre-filled). When !canWrite: only "Ver Demonstrativo". router.refresh() after writes.

**PayoutDemonstrativoSheet.tsx** ('use client'): Sheet side="right" (~640px), mirror OsSheet. Header "Demonstrativo — Dr. Nome — Jun/2026" + Status badge. Calls getDemonstrativo(payoutId) on open. Summary box (Card bg-muted): Valor Bruto Recebido / (−) Deduções / = Base de Cálculo / × Percentual / = Valor do Repasse (`font-semibold text-primary`). Deduções shadcn Accordion (Lab / Materiais / Taxa de Cartão / Impostos Retidos, each formatBRL). Itens do Repasse table: Data OS | Procedimento | Valor Recebido | Base Item | % | Repasse Item (all tabular-nums). Footer: Button outline "Fechar" + Button "Exportar PDF" (canWrite only — opens signed URL via window.open; NEVER pdf_storage_path).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "repasse|Payout|Competencia" | head && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/clinica/financeiro/repasse/page.tsx` exists; `grep "computePayouts"` present; reads x-read-only
    - `grep "function CompetenciaSelector" src/components/financeiro/CompetenciaSelector.tsx` present; uses useQueryState('competencia')
    - PayoutTable Ações include "Ver Demonstrativo", "Gerar CP", "Gerar RPA"; "Total a Repassar" KPI uses text-primary (grep "text-primary")
    - `grep "fecharCompetencia" src/app/(dashboard)/clinica/financeiro/repasse/page.tsx` (or PayoutTable) present; Fechar Competência uses AlertDialog; window.confirm grep returns 0
    - `grep "getDemonstrativo" src/components/financeiro/PayoutDemonstrativoSheet.tsx` present; "Valor do Repasse" rendered with text-primary font-semibold
    - PDF export uses signed URL (window.open) and no pdf_storage_path in component (grep pdf_storage_path returns 0)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Repasse screen: competência selector + KPIs (Total a Repassar text-primary) + payout table + demonstrativo Sheet + Fechar Competência + Gerar CP/RPA; read-only gate; build green.</done>
</task>

<task type="auto">
  <name>Task 2: RPA screen + RpaFormDialog + ReinfStatusBadge + financeiro hub nav cards (D-25)</name>
  <files>src/app/(dashboard)/clinica/financeiro/rpa/page.tsx, src/components/financeiro/RpaTable.tsx, src/components/financeiro/RpaFormDialog.tsx, src/components/financeiro/ReinfStatusBadge.tsx, src/app/(dashboard)/clinica/financeiro/page.tsx</files>
  <read_first>
    - .planning/phases/16-contas-a-pagar-concilia-o-tributos/16-UI-SPEC.md §"Screen 4: RPA" (Layout, RpaFormDialog fields, Calculated preview panel, Tributos a Recolher section), §"Hub Page Update" (4 card objects + icons), §"Copywriting Contract" (CTA "Emitir RPA", empty state, "RPA sem regra INSS", "Destructive: Cancelar RPA", "Reinf status: stub indicator", "RPA emitido"), §"Interaction Contracts" (RPA Calculated Preview, PDF Download)
    - src/actions/rpa.ts (listRpas/computeRpaPreview/gerarRpa/getRpaPdfUrl/cancelarRpa) AND src/actions/reinf.ts (stub status) — exact shapes; 16-08-SUMMARY.md for tax bracket prop source
    - src/actions/suppliers.ts (listSuppliers tipo='autonomo')
    - src/actions/payables.ts + listPayables (origem='tributo' for Tributos a Recolher section)
    - src/components/financeiro/NfseEmitForm.tsx (Dialog + RHF + calculated panel mirror for RpaFormDialog Estimativa)
    - src/components/financeiro/NfseTable.tsx (TanStack v8 + status badge contract mirror for RpaTable + ReinfStatusBadge)
    - src/app/(dashboard)/clinica/financeiro/page.tsx (existing nav-card grid — add 4 cards; it ALREADY has OS/NFS-e/Convênios; mirror that exact card object shape + filter(show))
    - src/proxy.ts (CONFIRM ROUTE_MODULE_MAP has '/clinica/financeiro' → 'financeiro'; do NOT add subroutes — D-25 already satisfied by prefix)
    - src/components/financeiro/CompetenciaSelector.tsx (reuse from Task 1)
  </read_first>
  <action>
**rpa/page.tsx** (RSC): PageHeader title "RPA & Tributos", breadcrumbs [Clínica → Financeiro → RPA & Tributos]. Read role/x-read-only → canWrite. Header actions: `<CompetenciaSelector />` (reused) + Select Unidade + Button "Emitir RPA" (canWrite only, opens RpaFormDialog). Read searchParams (competencia/unit/supplier). Call listRpas(competencia,unit) + listSuppliers({tipo:'autonomo'}) + load tax bracket seed (pass to RpaFormDialog as props for client-side Estimativa). Wrap NuqsAdapter. main p-6 max-w-5xl space-y-6.
  - KPI row (3 cards): "RPAs Emitidos" (count text-foreground), "INSS Total" (formatBRL tabular-nums), "IRRF Total" (formatBRL tabular-nums).
  - `<RpaTable rows={rpas} canWrite={canWrite} />`.
  - Tributos a Recolher section (below table): heading text-sm font-semibold "Obrigações a Recolher — [MÊS]". Table from listPayables origem='tributo' for competência: columns Tipo (DARF-INSS/DARF-IRRF/ISS) | Vencimento | Valor (formatBRL) | Status (Badge a recolher=amber / recolhido=green) | Ações ("Baixar" → links to /clinica/financeiro/contas-a-pagar baixa flow).
  - EmptyState: icon FileText, title "Nenhum RPA emitido", body "Emita o primeiro RPA para um autônomo nesta competência.", cta Button "Emitir RPA" (canWrite only).
  - If supplier query present → auto-open RpaFormDialog pre-filled (from Repasse "Gerar RPA").

**RpaTable.tsx** ('use client'): @tanstack/react-table v8. Columns: Número (font-medium tabular-nums; exibe o `numero` VERBATIM retornado por next_rpa_number / gravado em rpa_records — formato padronizado "RPA-000001", contador por unidade D-26; NAO inserir token de unidade na string), Autônomo, Competência, Bruto, INSS, IRRF, ISS, Líquido (all formatBRL tabular-nums), EFD-Reinf (`<ReinfStatusBadge status={...} isStub />`), Ações (DropdownMenu, canWrite only): "Visualizar PDF" (getRpaPdfUrl → window.open signed URL new tab; NEVER pdf_storage_path), "Cancelar" (AlertDialog copy verbatim "Cancelar RPA" / body "O cancelamento do RPA requer aprovação. Informe o motivo abaixo." / Textarea motivo / actions "Voltar"+"Solicitar Cancelamento" destructive → cancelarRpa → alçada D-24).

**RpaFormDialog.tsx** ('use client'): Dialog + RHF, mirror NfseEmitForm. Fields: Autônomo Select (filtered tipo='autonomo', required), Competência Input type="month" or Select YYYY-MM (required), Data de Pagamento Popover+Calendar (required), Valor Bruto Input BRL-masked `valorBrutoStr` (handleAmountBlur), Modalidade INSS Select ("11% Flat (Prestador a Empresa)"/"Progressiva", default "11pct"), ISS Override Switch + percentage Input (only if supplier iss_retido_fonte). Calculated preview Card bg-muted (updates on BRL blur, client-side via computeRpaPreview or tax-bracket props): Valor Bruto / (−) INSS (11%) / (−) IRRF (or "Isento" if base ≤ R$ 5.000,00) / (−) ISS (X%) (or "N/A") / = Valor Líquido (font-semibold). Label the panel "Estimativa — valores definitivos calculados no servidor ao emitir." Inline note "Após emissão, o RPA não pode ser editado. As retenções serão lançadas como Contas a Pagar para recolhimento." Footer: Button outline "Cancelar" + Button "Emitir RPA" (spinner) → gerarRpa(input). On success Alert/toast "[numero] emitido. PDF disponível em Documentos." router.refresh().

**ReinfStatusBadge.tsx** ('use client'): shadcn Badge + Tooltip. status pendente→amber (bg-amber-100 text-amber-800), transmitido→green (bg-green-100 text-green-800), erro→red (bg-red-100 text-red-800). When isStub: append outline "STUB" with Tooltip "EFD-Reinf em modo simulação. Conecte o provedor real no Hub de Integrações."

**financeiro/page.tsx** (UPDATE): add the 4 new nav cards from UI-SPEC §"Hub Page Update" VERBATIM, mirroring the existing OS/NFS-e/Convênios card object shape (href/title/description/icon/show), inside the same navItems array + filter(show):
  - href `/clinica/financeiro/contas-a-pagar`, title "Contas a Pagar", desc "Fornecedores, vencimentos e baixa de despesas.", icon CreditCard, show:true
  - href `/clinica/financeiro/conciliacao`, title "Conciliação Bancária", desc "Importe extratos OFX e bata com os lançamentos automaticamente.", icon ArrowLeftRight, show:true
  - href `/clinica/financeiro/repasse`, title "Repasse de Profissionais", desc "Calcule comissões e gere demonstrativos por competência.", icon Users, show:true
  - href `/clinica/financeiro/rpa`, title "RPA & Tributos", desc "Emita RPA de autônomos com INSS/IRRF/ISS calculados.", icon FileText, show:true
  Import the new icons (CreditCard, ArrowLeftRight, Users) from lucide-react (FileText already imported). Do NOT touch proxy.ts — '/clinica/financeiro' prefix already gates these subroutes (D-25 satisfied).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "rpa|Rpa|Reinf|financeiro/page" | head && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/clinica/financeiro/rpa/page.tsx` exists; `grep "listRpas"` present; reads x-read-only; Tributos a Recolher section present (grep "Recolher")
    - `grep "gerarRpa" src/components/financeiro/RpaFormDialog.tsx` present; Estimativa panel labeled (grep "Estimativa") and recalculates on blur
    - RpaTable "Visualizar PDF" uses getRpaPdfUrl + window.open; no pdf_storage_path in components (grep returns 0); Cancelar uses AlertDialog → cancelarRpa (alçada)
    - `grep "function ReinfStatusBadge" src/components/financeiro/ReinfStatusBadge.tsx` present; STUB tooltip copy present
    - financeiro/page.tsx contains the 4 new hrefs `/clinica/financeiro/contas-a-pagar`, `/conciliacao`, `/repasse`, `/rpa` with icons CreditCard/ArrowLeftRight/Users/FileText
    - proxy.ts unchanged for subroutes; `grep "/clinica/financeiro" src/proxy.ts` confirms prefix → financeiro mapping exists
    - window.confirm grep returns 0 across the 4 component/page files
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>RPA screen: competência + KPIs + RpaTable + Estimativa preview dialog + Tributos a Recolher + Reinf stub badge; 4 financeiro hub cards added; proxy mapping confirmed; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client UI → gerarRpa/fecharCompetencia/Gerar CP | User-triggered fiscal/CP side-effects |
| read-only role → write Ações | auditor/dpo/socio must not reach write controls |
| RPA/demonstrativo PDF link → bucket | Signed URL only, never raw pdf_storage_path |
| client Estimativa preview → server emit | Preview is non-authoritative; server recalculates |
| autônomo supplier CPF/bank render | Sensitive cadastral data shown only to authorized roles |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-50 | Information Disclosure | RPA/demonstrativo PDF path exposed in client | mitigate | "Visualizar PDF"/"Exportar PDF" use getRpaPdfUrl/demonstrativo signed URL (TTL=60s) + window.open; components never reference pdf_storage_path |
| T-16-51 | Elevation of Privilege | read-only role hits Emitir/Gerar CP/Gerar RPA/Fechar Competência | mitigate | UI hides write items when canWrite=false (x-read-only header); server actions enforce assertNotReadOnly — UI guard cannot bypass (D-23) |
| T-16-52 | Tampering | client Estimativa values submitted as authoritative | mitigate | preview labeled "Estimativa — valores definitivos calculados no servidor ao emitir"; gerarRpa recalculates INSS/IRRF/ISS server-side from versioned brackets (D-17) — client values are display-only |
| T-16-53 | Repudiation | cancel RPA emitido / fechar competência without trail | mitigate | Cancelar RPA AlertDialog requires Textarea motivo → cancelarRpa routes through approval_requests alçada + audit (D-24); Fechar Competência destructive AlertDialog (D-26) |
| T-16-54 | Information Disclosure | autônomo CPF/bank rendered to unauthorized role | mitigate | sensitive supplier fields shown only in canWrite/detail context; RpaTable shows autônomo name only; read-only roles see no bank/CPF columns |
| T-16-55 | Spoofing | EFD-Reinf stub presented as real transmission | mitigate | ReinfStatusBadge shows "STUB" indicator + tooltip "EFD-Reinf em modo simulação" (D-18/D-22) |
</threat_model>

<verification>
- `npm run build` green (Vitest/tsc do not catch 'use server' errors — build does, per project memory).
- Manual (Vercel): Repasse → competência Jun/2026 → Ver Demonstrativo (deduções accordion, Total a Repassar em destaque) → Gerar CP → Gerar RPA pré-preenchido. RPA → Emitir RPA: valor bruto blur atualiza Estimativa INSS/IRRF/ISS → emitir → número sequencial + Visualizar PDF (signed URL) + tributo a recolher aparece. ReinfStatusBadge mostra STUB. Hub financeiro mostra 4 cards novos.
</verification>

<success_criteria>
TRIB-01/TRIB-02/TRIB-03 usáveis: Repasse (competência + demonstrativo + Total a Repassar + Gerar CP/RPA + Fechar Competência) e RPA & Tributos (Estimativa preview + emitir + PDF signed URL + tributos a recolher + Reinf stub badge), 4 nav cards no hub (D-25), gate read-only (D-23) e cópia pt-BR do UI-SPEC; build verde.
</success_criteria>

<output>
After completion, create `.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-10-SUMMARY.md`
</output>
