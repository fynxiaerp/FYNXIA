---
phase: 16
plan: 08
type: execute
wave: 3
depends_on: [16-04, 16-05, 16-06]
files_modified:
  - src/actions/professional-payouts.ts
  - src/actions/rpa.ts
  - src/actions/reinf.ts
autonomous: true
requirements: [TRIB-01, TRIB-02, TRIB-03]
user_setup: []

must_haves:
  truths:
    - "computePayouts roda sobre recebimentos CONCILIADOS (regime caixa, D-14), aplica regra service>wildcard, sem_regra→0%+alerta, e gera demonstrativo por profissional/competência (TRIB-01)"
    - "createPayableFromRepasse gera o CP origem='repasse' ao profissional e fecharCompetencia bloqueia a competência (recebimento posterior → próxima, D-15/D-26)"
    - "gerarRpa para autônomo (vínculo D-16) computa INSS/IRRF/ISS por vigência (D-17), numera via next_rpa_number() por unidade, gera PDF arquivado e lança retenções como CP origem='tributo' (TRIB-02, D-20)"
    - "PDF de RPA: storage_path NUNCA retornado; somente getRpaDocumentUrl devolve signed URL TTL=60s (Pitfall 7)"
    - "gerarReinfEvent R-2010/R-4020 via getReinfProvider STUB com idempotency_key, cobrindo RPA autônomo + serviços tomados PJ/lab; estorno de baixa/CP-pago/RPA via approval_requests por alçada + audit (TRIB-03, D-18/D-21/D-22/D-24)"
  artifacts:
    - path: "src/actions/professional-payouts.ts"
      provides: "computePayouts, getDemonstrativo, fecharCompetencia, listPayouts"
      exports: ["computePayouts", "fecharCompetencia"]
    - path: "src/actions/rpa.ts"
      provides: "gerarRpa, getRpaDocumentUrl, listRpas, estornarRpa"
      exports: ["gerarRpa", "getRpaDocumentUrl"]
    - path: "src/actions/reinf.ts"
      provides: "gerarReinfEvent, listReinfEvents, estornarBaixaConciliada"
      exports: ["gerarReinfEvent", "estornarBaixaConciliada"]
  key_links:
    - from: "computePayouts"
      to: "createPayableFromRepasse (src/actions/payables 16-06)"
      via: "CP origem='repasse', payout.payable_id set"
      pattern: "createPayableFromRepasse|origem.*repasse"
    - from: "gerarRpa"
      to: "computeRpaWithholdings (src/lib/financeiro/tax-tables) + createTributoPayable"
      via: "retenções → CP origem='tributo'"
      pattern: "computeRpaWithholdings|createTributoPayable"
    - from: "gerarReinfEvent"
      to: "getReinfProvider (src/lib/reinf)"
      via: "STUB provider.transmitir + idempotency_key"
      pattern: "getReinfProvider"
---

<objective>
Wave 3 — fechar o ciclo fiscal/repasse: cálculo de repasse profissional sobre recebimentos conciliados (regime caixa, D-14), emissão de RPA com retenções INSS/IRRF/ISS por vigência e PDF arquivado (D-20), e os eventos EFD-Reinf R-2010/R-4020 via provider STUB (D-18/D-22), incluindo o estorno por alçada (D-24). Toda a matemática (computePayout, computeInss/Irrf/Iss, computeRpaWithholdings) já está pronta e testada em `src/lib/financeiro/` (16-04); o provider STUB (`getReinfProvider`) já existe (16-04); o CP `createPayableFromRepasse`/`createTributoPayable` e o alçada já estão em 16-06. Aqui orquestramos com I/O + numeração sequencial + PDF + arquivamento. Espelha `src/actions/service-orders.ts` (next_os_number via RPC, PDF gated com signed URL TTL=60s, cancelamento via alçada da Fase 10) e `src/actions/receivables.ts` (getActor).

Purpose: TRIB-01 (repasse), TRIB-02 (RPA + retenções), TRIB-03 (EFD-Reinf stub + estorno por alçada).
Output: 3 arquivos de Server Action.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-CONTEXT.md

<interfaces>
Mirror EXACTLY: src/actions/receivables.ts (getActor verbatim); src/actions/service-orders.ts (next_os_number via supabase.rpc; PDF render → upload no bucket → NUNCA retornar storage_path; getXDocumentUrl signed URL TTL=60s via createAdminClient; cancelarOs faturada → alçada Fase 10; logBusinessEvent; DI opcional para testes).

Libs PURAS prontas e testadas (Plan 04 — APENAS importar, NÃO reimplementar):
  src/lib/financeiro/tax-tables.ts:
    - computeInss(valorBruto, modalidade:'11pct'|'progressivo', brackets): { valor, aliquota }  // teto, integer-cent
    - computeIrrf(baseCalculo, brackets): { valor, aliquota }  // base = bruto − INSS (Pitfall 4)
    - computeIss(valorServicos, aliquota): number
    - computeRpaWithholdings(valorBruto, modalidade, inssBrackets, irrfBrackets, issAliquota): { inss, irrf, iss, liquido }
    - selectBracketsByVigencia(rows, date): rows vigentes na data
  src/lib/financeiro/payout-math.ts:
    - computePayout(items, rules, deducoes): PayoutItemResult[]  // service>wildcard, sem_regra→{percentual:0, valor_repasse:0, alerta:'sem_regra'}
    - applyDeductions(valorRecebido, deducoes, ruleDeducoes): number
    - aggregatePayout(itemResults): { valor_bruto, valor_base, valor_repasse }
    CommissionRule { service_id, percentual, deducoes: string[] }
  src/lib/reinf/index.ts:
    - getReinfProvider(clinicId): Promise<ReinfProvider>  // STUB-gated; transmitir(ReinfEventInput)→{ provider_ref, status, protocolo }
    Tipos em src/lib/reinf/types.ts: ReinfEventInput { tipo:'R2010'|'R4020', competencia, clinic_id, prestador_cnpj?, prestador_nome?, valor_bruto?, valor_retencao_inss?, beneficiario_cnpj?, valor_retencao_irrf?, idempotency_key }

CP/alçada já em 16-06 (importar de '@/actions/payables'):
  - createPayableFromRepasse(payoutId, supplierId, valorRepasse, competencia, dueDate) → payableId (idempotente por payout_id)
  - createTributoPayable({ supplierId|null, descricao, valor, dueDate, competencia, accountId, costCenterId, documentId? }) → payableId
  - cancelarPayable / alçada: createApprovalRequest from src/actions/approval-actions.ts (Fase 10)

Validators (Plan 04, vivos): src/lib/validators/rpa.ts → rpaSchema (supplierId uuid, competencia /^\d{4}-\d{2}$/, dataPagamento ISO, valorBruto isMoney2dp positive, modalidadeInss enum '11pct'|'progressivo', issOverride number optional, unitId uuid).

Schema vivo após 16-05 (16-RESEARCH §"Schema Design", linhas 555-665):
  - professional_payouts (id, clinic_id, unit_id, professional_id, competencia, valor_bruto, deducoes JSONB DEFAULT '{}', valor_base, percentual NUMERIC(5,4), valor_repasse, status CHECK('rascunho','aprovado','pago') DEFAULT 'rascunho', payable_id) UNIQUE(clinic_id, professional_id, competencia)
  - payout_items (id, clinic_id, payout_id, service_order_id, statement_line_id, descricao, valor_recebido, valor_base_item, percentual_item, valor_repasse_item)
  - rpa_records (id, clinic_id, unit_id, supplier_id, professional_id, payout_id, numero TEXT, competencia, data_pagamento, valor_bruto, valor_inss, valor_irrf, valor_iss, valor_liquido, aliquota_inss/irrf/iss, municipio_ibge, regime_tributario, pdf_storage_path [NUNCA retornar], status CHECK('rascunho','emitido','cancelado'), payable_id, reinf_event_id) UNIQUE(clinic_id, numero)
  - reinf_events (id, clinic_id, unit_id, tipo CHECK('R2010','R4020'), competencia, provider_ref, status CHECK('pendente','transmitido','erro','retificado') DEFAULT 'pendente', protocolo, payload JSONB, error_message, idempotency_key) UNIQUE(clinic_id, idempotency_key)
  - inss_tax_tables / irrf_tax_tables / iss_tax_tables (seed por vigência — carregar brackets via SELECT + selectBracketsByVigencia)
  - competencia_fechamentos (clinic_id, unit_id, competencia, fechado_at, fechado_por) UNIQUE(clinic_id, unit_id, competencia)
  - unit_rpa_counters (unit_id PK, clinic_id, last_rpa_number) — numeração via next_rpa_number(p_unit_id) SECURITY DEFINER (mesmo padrão de next_os_number, Pitfall 2). CONFIRMAR nome exato da função RPC lendo a migration 16-05.
  - clinics.regime_tributario (Fase 15) dirige a apuração (D-19): simples_nacional pula ISS a pagar (Pitfall 8).
  - professionals.commission_rules JSONB (Fase 11) + professionals.vinculo / supplier.vinculo ('clt'|'pj'|'autonomo', D-16).

PDF: usar @react-pdf/renderer (já instalado) — criar componente src/components/pdf/RpaPDF.tsx espelhando src/components/pdf/ReceiboPDF.tsx (Flexbox, sem CSS Grid). render → Buffer → upload no bucket de documentos (Fase 8) → gravar pdf_storage_path. NUNCA retornar o path.
Roles (D-23): writers = ['admin','superadmin']; auditor/socio readOnly. Estorno (D-24) sempre via approval_requests + audit. CANONICAL ROLE SET: o sistema NAO possui papel 'financeiro' — o enum (users_role_check, 20260614000400_role_expansion.sql) e ['admin','dentist','receptionist','patient','superadmin','dpo','auditor','socio','ti','implantacao','aluno']. A intencao 'escrita admin/financeiro' do D-23 e satisfeita por admin/superadmin (escritores financeiros canonicos), igual a RLS da Fase 15 e de 16-03 (get_my_role() IN ('admin','superadmin')). Gates de action usam o MESMO conjunto da RLS de 16-03 para DB e app concordarem.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: professional-payouts.ts — repasse sobre conciliados (regime caixa) + CP repasse + fechamento de competência</name>
  <files>src/actions/professional-payouts.ts</files>
  <read_first>
    - src/lib/financeiro/payout-math.ts (computePayout, applyDeductions, aggregatePayout — APENAS chamar)
    - src/actions/receivables.ts (getActor)
    - src/actions/payables.ts (createPayableFromRepasse — assinatura acima; importar e chamar)
    - src/actions/service-orders.ts (padrão de leitura de commission_rules / professional_id por OS — base de repasse D-29 Fase 15)
    - src/lib/audit (logBusinessEvent)
    - 16-RESEARCH.md §"Repasse Profissional — Fluxo Completo" (linhas 882-929) + §"Padrão 6 Competência" (linhas 388-410) + CONTEXT D-13/D-14/D-15/D-16/D-26
  </read_first>
  <action>
`'use server'` + `import 'server-only'`. getActor. Writer gate ['admin','superadmin'].

  - `export async function computePayouts(input: { professionalId: string; competencia: string; unitId: string }): Promise<{ success: boolean; payoutId?: string; alertas?: string[]; error?: string }>` (TRIB-01, regime caixa D-14):
    1. getActor → writer gate.
    2. Guard de competência fechada (D-26): SELECT competencia_fechamentos WHERE clinic_id, unit_id, competencia; se existe → { success:false, error:'Competência {competencia} fechada' }.
    3. Verificar vínculo (D-16): só processa quando o profissional é autônomo/PJ; CLT fora de escopo. (ler vinculo do supplier vinculado / professionals).
    4. Coletar os recebimentos CONCILIADOS do profissional na competência (regime caixa). ATENCAO: financial_transactions NAO possui professional_id — o vinculo do profissional ao dinheiro recebido vem pela CADEIA DE JOIN abaixo (confirmar nomes exatos lendo as migrations 20260620000200_faturamento_os_tables.sql e 20260620000100_*; charges.service_order_id e service_order_items.professional_id sao as colunas-chave, D-29 Fase 15):
       CADEIA DE JOIN (recebimento conciliado → profissional):
         financial_transactions (reconciliation_status='conciliado', type='receita', transaction_date dentro de competencia)
           → statement_lines (via financial_transactions.statement_line_id) — confirma que o recebimento foi conciliado contra o extrato
           → charge/receivable (o recebivel que originou o FT; via charge_id/receivable_id no FT OU statement_lines.matched_transaction_ids → financial_transactions → charges) que carrega charges.service_order_id
           → service_orders (id = charges.service_order_id) e suas linhas service_order_items (service_order_id) que carregam service_order_items.professional_id (= input.professionalId)
       Filtrar service_order_items WHERE professional_id = input.professionalId. Cada linha elegivel vira um PayoutItemInput { service_id (= service_order_items.service_id), valor_recebido (rateio do valor recebido conciliado por item da OS), descricao, service_order_id, statement_line_id }.
       ALTERNATIVA / agregacao por lote conciliado: quando os recebimentos ja foram materializados como payout_items numa passada anterior, usar payout_items.statement_line_id e payout_items.service_order_id como ponte (payout_items carrega ambos os FKs, ver 16-03 Task 1). NUNCA inferir professional_id do financial_transaction diretamente — ele nao existe la.
    5. Carregar commission_rules JSONB do profissional → CommissionRule[].
    6. `const results = computePayout(items, rules, deducoes)`; coletar alertas = results.filter(r => r.alerta==='sem_regra'). `const header = aggregatePayout(results)`.
    7. Upsert professional_payouts (UNIQUE clinic_id,professional_id,competencia) { clinic_id=actor.tenant_id, unit_id, professional_id, competencia, valor_bruto:header.valor_bruto, deducoes, valor_base:header.valor_base, percentual (média ponderada ou da regra header), valor_repasse:header.valor_repasse, status:'rascunho', created_by=actor.id } → payoutId. Inserir payout_items (um por result).
    8. logBusinessEvent (compute_payout, payoutId, alertas). revalidatePath('/clinica/financeiro/repasses'). Return { success:true, payoutId, alertas }.
    NUNCA recalcular imposto/percentual inline — sempre via payout-math.

  - `export async function getDemonstrativo(payoutId: string): Promise<{ success: boolean; payout?: unknown; items?: unknown[]; error?: string }>` (D-15): getActor; SELECT professional_payouts + payout_items + professional name (RLS escopa tenant). É o demonstrativo por profissional/competência.

  - `export async function aprovarEgerarCP(payoutId: string, dueDate: string, supplierId: string): Promise<{ success: boolean; payableId?: string; error?: string }>` (D-15/D-02d):
    1. writer gate. SELECT o payout (valor_repasse, competencia). CAS UPDATE status 'rascunho'→'aprovado' (`.eq('status','rascunho')`); se 0 rows → { success:false, error:'Repasse já aprovado/pago' }.
    2. `const r = await createPayableFromRepasse(payoutId, supplierId, valor_repasse, competencia, dueDate)` (idempotente por payout_id). UPDATE professional_payouts SET payable_id=r.payableId.
    3. logBusinessEvent. Return { success:true, payableId: r.payableId }.

  - `export async function fecharCompetencia(input: { unitId: string; competencia: string }): Promise<{ success: boolean; error?: string }>` (D-26):
    1. writer gate. INSERT competencia_fechamentos { clinic_id=actor.tenant_id, unit_id, competencia, fechado_por=actor.id } — ON CONFLICT (clinic_id,unit_id,competencia) DO NOTHING (idempotente). Recebimentos conciliados APÓS o fechamento entram na próxima competência (computePayouts já recusa a fechada; a UI usa next_competencia).
    2. logBusinessEvent (fechar_competencia). Return { success:true }.

  - `export async function listPayouts(filters: { competencia?: string; professionalId?: string; status?: string; unitId?: string }): Promise<{ success: boolean; payouts?: unknown[]; error?: string }>`: getActor; SELECT professional_payouts (RLS) com filtros; order by competencia desc.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "professional-payouts" | head && grep -E "computePayout|aggregatePayout" src/actions/professional-payouts.ts && grep -E "createPayableFromRepasse|competencia_fechamentos" src/actions/professional-payouts.ts</automated>
  </verify>
  <acceptance_criteria>
    - professional-payouts.ts exporta computePayouts, getDemonstrativo, aprovarEgerarCP, fecharCompetencia, listPayouts
    - computePayouts importa computePayout/aggregatePayout (NÃO recalcula inline) e coleta alertas 'sem_regra'
    - computePayouts checa competencia_fechamentos antes de processar (grep)
    - computePayouts liga recebimento→profissional pela cadeia de join (service_order_items.professional_id via charges.service_order_id / payout_items), NAO por financial_transactions.professional_id (que nao existe): grep mostra 'service_order' e/ou 'payout_items' no path de coleta e NENHUM acesso a um campo professional_id em financial_transactions
    - aprovarEgerarCP chama createPayableFromRepasse e seta payable_id com CAS (.eq('status','rascunho'))
    - fecharCompetencia faz INSERT idempotente em competencia_fechamentos (ON CONFLICT DO NOTHING)
    - `grep "actor.tenant_id" src/actions/professional-payouts.ts` presente
    - `grep -c "import 'server-only'" src/actions/professional-payouts.ts` retorna 1
    - tsc limpo
  </acceptance_criteria>
  <done>Repasse regime caixa sobre conciliados + demonstrativo + CP origem='repasse' + fechamento de competência idempotente; tudo tenant-scoped e writer-gated.</done>
</task>

<task type="auto">
  <name>Task 2: rpa.ts + RpaPDF.tsx — RPA autônomo, retenções por vigência, PDF gated, CP tributo</name>
  <files>src/actions/rpa.ts, src/components/pdf/RpaPDF.tsx</files>
  <read_first>
    - src/lib/financeiro/tax-tables.ts (computeRpaWithholdings, computeInss/Irrf/Iss, selectBracketsByVigencia — APENAS chamar)
    - src/actions/service-orders.ts (next_os_number via supabase.rpc → ESPELHAR next_rpa_number; PDF render→upload→pdf NUNCA retorna path; getOsDocumentUrl signed URL TTL=60s via createAdminClient)
    - src/components/pdf/ReceiboPDF.tsx (componente @react-pdf/renderer base — Flexbox, fontes; espelhar para RpaPDF)
    - src/app/api/financeiro/charges/[id]/recibo.pdf/route.ts (render PDF→Buffer no servidor; reutilizar idioma de renderToBuffer/renderToStream)
    - src/actions/payables.ts (createTributoPayable — assinatura acima)
    - src/lib/validators/rpa.ts (rpaSchema)
    - supabase/migrations (16-05) para o nome EXATO da função next_rpa_number e do bucket de documentos
    - 16-RESEARCH.md §"Brazilian Tax Withholding" (linhas 743-829) + §"Common Pitfalls" 2/3/4/7/8 + CONTEXT D-16/D-17/D-19/D-20
  </read_first>
  <action>
**src/components/pdf/RpaPDF.tsx**: componente React-PDF (espelhar ReceiboPDF.tsx — `Document`/`Page`/`View`/`Text`, StyleSheet Flexbox, sem CSS Grid). Props: { numero, competencia, prestadorNome, prestadorDoc, valorBruto, valorInss, valorIrrf, valorIss, valorLiquido, dataPagamento, clinicaNome }. Layout de recibo de pagamento a autônomo com a discriminação das retenções.

**src/actions/rpa.ts** (`'use server'` + `import 'server-only'`, getActor, writer gate ['admin','superadmin']):
  - import { computeRpaWithholdings, selectBracketsByVigencia } from '@/lib/financeiro/tax-tables'; import { createTributoPayable } from '@/actions/payables'; import { rpaSchema } from '@/lib/validators/rpa'; createAdminClient para signed URL/upload; @react-pdf/renderer renderToBuffer; logBusinessEvent.

  - `export async function gerarRpa(rawInput: unknown): Promise<{ success: boolean; rpaId?: string; numero?: string; payableIds?: string[]; error?: string }>` (TRIB-02, D-16/D-17/D-19/D-20):
    1. getActor → writer gate. Parse rpaSchema (friendly first error).
    2. Carregar o supplier (deve ser vinculo='autonomo', D-16; senão → { success:false, error:'RPA só para autônomo' }). Carregar clinics.regime_tributario (D-19) e municipio_ibge do unit_fiscal_config (para ISS).
    3. Guard de competência fechada (competencia_fechamentos) — recusar se fechada.
    4. Carregar brackets por vigência da data de pagamento: SELECT inss_tax_tables + irrf_tax_tables (+ iss_tax_tables por municipio) → `selectBracketsByVigencia(rows, dataPagamento)`.
    5. Determinar issAliquota: simples_nacional → 0 (pula ISS a pagar, Pitfall 8); senão alíquota da tabela ISS do município OU issOverride do input.
    6. `const w = computeRpaWithholdings(valorBruto, modalidadeInss, inssBrackets, irrfBrackets, issAliquota)` → { inss, irrf, iss, liquido }. (a lib já deduz INSS antes do IRRF — Pitfall 4 — e respeita o teto — Pitfall 3.)
    7. Numeração: `const { data: numero } = await supabase.rpc('next_rpa_number', { p_unit_id: unitId })` (SECURITY DEFINER, atômico, Pitfall 2). FORMATO PADRONIZADO: a funcao ja retorna a string completa `'RPA-' || LPAD(seq,6,'0')` → ex. 'RPA-000001' (contador POR UNIDADE em unit_rpa_counters, D-26 — o escopo por unidade vem do contador, NAO de um token na string; identico ao next_os_number 'OS-000001'). USAR `numero` VERBATIM no INSERT; NAO reformatar para 'RPA-{unit}-{seq}' nem concatenar unit_id.
    8. INSERT rpa_records { clinic_id=actor.tenant_id, unit_id, supplier_id, professional_id?, numero, competencia, data_pagamento, valor_bruto, valor_inss:w.inss, valor_irrf:w.irrf, valor_iss:w.iss, valor_liquido:w.liquido, aliquota_*, municipio_ibge, regime_tributario (snapshot), status:'emitido', created_by=actor.id } → rpaId.
    9. PDF: `const buffer = await renderToBuffer(<RpaPDF .../>)`; upload no bucket de documentos via createAdminClient (path tipo `rpa/{clinic_id}/{rpaId}.pdf`); UPDATE rpa_records SET pdf_storage_path=path. NUNCA retornar o path.
    10. Lançar retenções como CP origem='tributo' (D-20): para cada retenção > 0 (INSS→GPS, IRRF→DARF, ISS→guia municipal) chamar `createTributoPayable({ supplierId:null, descricao:'INSS/IRRF/ISS RPA {numero}', valor, dueDate:<vencimento do tributo>, competencia, accountId:<conta de tributo>, costCenterId })` → coletar payableIds.
    11. logBusinessEvent (gerar_rpa, rpaId, numero). revalidatePath('/clinica/financeiro/rpa'). Return { success:true, rpaId, numero, payableIds }.

  - `export async function getRpaDocumentUrl(rpaId: string): Promise<{ success: boolean; url?: string; error?: string }>` (Pitfall 7): getActor; SELECT pdf_storage_path do rpa (RLS); `createSignedUrl(path, 60)` via createAdminClient; retornar APENAS a url. (pdf_storage_path nunca sai em listRpas/getRpa).

  - `export async function listRpas(filters: { competencia?: string; supplierId?: string; status?: string }): Promise<{ success: boolean; rpas?: unknown[]; error?: string }>`: getActor; SELECT rpa_records SEM pdf_storage_path no select (Pitfall 7); filtros; order desc.

  - `export async function estornarRpa(rpaId: string, motivo: string): Promise<{ success: boolean; error?: string }>` (D-24): writer gate; RPA emitido → SEMPRE via createApprovalRequest (alçada Fase 10) + logBusinessEvent; NÃO cancelar diretamente.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "actions/rpa|RpaPDF" | head && grep -E "computeRpaWithholdings" src/actions/rpa.ts && grep -E "next_rpa_number" src/actions/rpa.ts && grep -E "createSignedUrl" src/actions/rpa.ts</automated>
  </verify>
  <acceptance_criteria>
    - rpa.ts exporta gerarRpa, getRpaDocumentUrl, listRpas, estornarRpa
    - gerarRpa importa computeRpaWithholdings + selectBracketsByVigencia (NÃO calcula imposto inline)
    - gerarRpa usa supabase.rpc('next_rpa_number', ...) para numeração (grep)
    - gerarRpa só processa supplier vinculo='autonomo' (grep 'autonomo')
    - gerarRpa chama createTributoPayable para as retenções (grep)
    - listRpas NÃO inclui pdf_storage_path no select; getRpaDocumentUrl usa createSignedUrl(..., 60) (TTL=60s)
    - estornarRpa roteia por createApprovalRequest (alçada), não cancela direto (grep)
    - RpaPDF.tsx usa @react-pdf/renderer (Flexbox; sem display:'grid')
    - `grep -c "import 'server-only'" src/actions/rpa.ts` retorna 1
    - tsc limpo
  </acceptance_criteria>
  <done>RPA autônomo com retenções por vigência (teto INSS + INSS-antes-IRRF + ISS por regime), numeração atômica por unidade, PDF arquivado gated (signed URL 60s), CP origem='tributo', estorno por alçada.</done>
</task>

<task type="auto">
  <name>Task 3: reinf.ts — eventos EFD-Reinf R-2010/R-4020 via STUB + estorno por alçada</name>
  <files>src/actions/reinf.ts</files>
  <read_first>
    - src/lib/reinf/index.ts (getReinfProvider — APENAS chamar; ReinfProvider.transmitir) + src/lib/reinf/types.ts (ReinfEventInput/ReinfEventResult)
    - src/lib/fiscal/index.ts + um caller de getFiscalProvider em src/actions/service-orders.ts (padrão de uso do provider gated + idempotency)
    - src/actions/approval-actions.ts (createApprovalRequest — alçada Fase 10, D-24)
    - src/actions/receivables.ts (getActor); src/lib/audit (logBusinessEvent)
    - 16-RESEARCH.md §"EFD-Reinf — Guia para o Stub" (linhas 832-878) + CONTEXT D-18/D-21/D-22/D-24
  </read_first>
  <action>
`'use server'` + `import 'server-only'`. getActor. Writer gate ['admin','superadmin'].
  - import { getReinfProvider } from '@/lib/reinf'; tipos de '@/lib/reinf/types'; createApprovalRequest from '@/actions/approval-actions'; logBusinessEvent.

  - `export async function gerarReinfEvent(input: { tipo: 'R2010' | 'R4020'; competencia: string; unitId?: string; sourceRpaId?: string; sourcePayableId?: string; prestadorCnpj?: string; prestadorNome?: string; valorBruto?: number; valorRetencaoInss?: number; beneficiarioCnpj?: string; valorRetencaoIrrf?: number }): Promise<{ success: boolean; eventId?: string; protocolo?: string; error?: string }>` (TRIB-03, D-18/D-21/D-22):
    1. getActor → writer gate.
    2. Montar idempotency_key determinístico: `${input.tipo}|${input.competencia}|${input.sourceRpaId ?? input.sourcePayableId ?? input.prestadorCnpj}` (cobre RPA de autônomo E serviços tomados PJ/lab — D-21).
    3. Idempotência: SELECT reinf_events WHERE clinic_id, idempotency_key (UNIQUE). Se já existe → retornar o evento existente { success:true, eventId, protocolo } (não retransmitir).
    4. `const provider = await getReinfProvider(actor.tenant_id)` (STUB-gated, D-22).
    5. Montar ReinfEventInput { tipo, competencia, clinic_id:actor.tenant_id, prestador_cnpj, prestador_nome, valor_bruto, valor_retencao_inss, beneficiario_cnpj, valor_retencao_irrf, idempotency_key } e `const r = await provider.transmitir(input)` → { provider_ref, status, protocolo }.
    6. INSERT reinf_events { clinic_id=actor.tenant_id, unit_id, tipo, competencia, provider_ref:r.provider_ref, status:r.status, protocolo:r.protocolo, payload:<ReinfEventInput>, idempotency_key, created_by=actor.id } → eventId. Se input.sourceRpaId → UPDATE rpa_records SET reinf_event_id=eventId.
    7. logBusinessEvent (gerar_reinf, eventId, tipo). revalidatePath('/clinica/financeiro/reinf'). Return { success:true, eventId, protocolo:r.protocolo }.

  - `export async function listReinfEvents(filters: { competencia?: string; tipo?: string; status?: string }): Promise<{ success: boolean; events?: unknown[]; error?: string }>`: getActor; SELECT reinf_events (RLS) com filtros; order by competencia desc.

  - `export async function estornarBaixaConciliada(input: { financialTransactionId: string; motivo: string }): Promise<{ success: boolean; error?: string }>` (D-24 — estorno de baixa conciliada):
    1. writer gate. Re-fetch o financial_transaction (deve estar 'conciliado'/'baixado'). O estorno de baixa conciliada / CP pago / RPA emitido SEMPRE passa por `createApprovalRequest` (alçada Fase 10) com motivo + logBusinessEvent na trilha. NÃO reverter o saldo/lançamento diretamente — a aprovação por alçada é quem dispara a reversão.
    2. Return { success:true } (pedido de aprovação criado). (O ponto de mutação reversora vive no fluxo de aprovação da Fase 10 — esta action apenas enfileira o pedido + audita, mesmo padrão do estorno fiscal da Fase 15.)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "actions/reinf" | head && grep -E "getReinfProvider" src/actions/reinf.ts && grep -E "idempotency_key" src/actions/reinf.ts && grep -E "createApprovalRequest" src/actions/reinf.ts</automated>
  </verify>
  <acceptance_criteria>
    - reinf.ts exporta gerarReinfEvent, listReinfEvents, estornarBaixaConciliada
    - gerarReinfEvent chama getReinfProvider e provider.transmitir (STUB)
    - gerarReinfEvent monta idempotency_key determinístico e checa reinf_events existente antes de transmitir (grep idempotency_key + select-before)
    - gerarReinfEvent cobre R2010 e R4020 (tipo no input) — RPA autônomo + serviços tomados PJ/lab (D-21)
    - estornarBaixaConciliada roteia por createApprovalRequest (alçada D-24), não reverte direto (grep)
    - `grep "actor.tenant_id" src/actions/reinf.ts` presente
    - `grep -c "import 'server-only'" src/actions/reinf.ts` retorna 1
    - tsc limpo
  </acceptance_criteria>
  <done>Eventos EFD-Reinf R-2010/R-4020 via STUB com idempotency_key cobrindo RPA + serviços tomados; estorno de baixa/CP-pago/RPA por alçada + audit (D-24).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → gerarRpa/computePayouts | Input não confiável cruza para cálculo fiscal + mutação financeira |
| gerarReinfEvent → getReinfProvider | Credencial Reinf (AES-256) gated server-only; só decide stub-vs-real |
| RPA PDF → bucket de documentos | Documento fiscal sensível; path nunca exposto |
| estorno → approval_requests | Reversão de dinheiro só por alçada + audit |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-40 | Tampering | imposto recalculado/forjado no cliente | mitigate | tudo via libs puras testadas (computeRpaWithholdings); valorBruto validado por rpaSchema; nada de cálculo inline |
| T-16-41 | Information Disclosure | pdf_storage_path do RPA exposto | mitigate | path nunca no select de listRpas/getRpa; só getRpaDocumentUrl → createSignedUrl TTL=60s (Pitfall 7) |
| T-16-42 | Information Disclosure | credencial Reinf vazada | mitigate | getReinfProvider via createAdminClient server-only; credential_enc nunca retornado, só gateia stub-vs-real |
| T-16-43 | Tampering | numeração RPA duplicada sob concorrência | mitigate | next_rpa_number() SECURITY DEFINER atômico (Pitfall 2); UNIQUE(clinic_id,numero) backstop |
| T-16-44 | Repudiation | estorno de baixa/CP-pago/RPA sem trilha | mitigate | estornarRpa/estornarBaixaConciliada → createApprovalRequest (alçada) + logBusinessEvent (D-24) |
| T-16-45 | Tampering | retransmissão duplicada de evento Reinf | mitigate | idempotency_key determinístico + UNIQUE(clinic_id,idempotency_key); select-before-transmit retorna o existente |
| T-16-46 | Elevation of Privilege | auditor/socio gera RPA/repasse/reinf | mitigate | writer gate ['admin','superadmin'] em toda action |
| T-16-47 | Tampering | repasse/RPA em competência fechada | mitigate | guard competencia_fechamentos em computePayouts e gerarRpa (D-26) |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo para professional-payouts, rpa, reinf, RpaPDF.
- Manual: computePayouts(profissional, '2026-06') → demonstrativo com itens + alertas sem_regra; aprovarEgerarCP → CP origem='repasse'. gerarRpa(autônomo, R$6000, '11pct') → INSS 660 + IRRF sobre (6000−660) + ISS por regime; numero sequencial; getRpaDocumentUrl → signed URL 60s. gerarReinfEvent(R2010) duas vezes → mesmo eventId.
</verification>

<success_criteria>
TRIB-01 entregue: repasse regime caixa sobre conciliados + demonstrativo + CP origem='repasse' + fechamento de competência. TRIB-02 entregue: RPA autônomo com retenções por vigência + numeração atômica + PDF arquivado gated + CP origem='tributo'. TRIB-03 entregue: EFD-Reinf R-2010/R-4020 via STUB idempotente cobrindo RPA + serviços tomados, com estorno por alçada (D-24).
</success_criteria>

<output>
After completion, create `.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-08-SUMMARY.md`
</output>
