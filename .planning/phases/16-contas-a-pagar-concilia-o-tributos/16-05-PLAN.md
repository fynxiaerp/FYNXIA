---
phase: 16
plan: 05
type: execute
wave: 2
depends_on: [16-02, 16-03]
files_modified:
  - src/lib/database.types.ts
autonomous: false
requirements: [FOP-01, FOP-02, FOP-03, TRIB-01, TRIB-02, TRIB-03]
user_setup:
  - service: supabase
    why: "Push 7 phase-16 migrations to project jqjwyqlbbuqnrffdnlpp; CLI may be logged into wrong account (nexus-*) per project memory"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (for non-TTY push)"
    dashboard_config:
      - task: "Verify CLI is logged into the correct account before push (re-login if nexus-*)"
        location: "supabase login / supabase projects list"

must_haves:
  truths:
    - "All 7 phase-16 migrations are applied to the live Supabase project jqjwyqlbbuqnrffdnlpp"
    - "Regenerated database.types.ts includes payables, statement_lines, professional_payouts, rpa_records, reinf_events, inss_tax_tables, suppliers"
    - "financial_transactions row type now includes reconciliation_status; bank_accounts includes saldo_atual"
  artifacts:
    - path: "src/lib/database.types.ts"
      provides: "Regenerated Supabase types including all 15 new tables + ALTERs"
      contains: "payables"
  key_links:
    - from: "supabase db push"
      to: "live DB jqjwyqlbbuqnrffdnlpp"
      via: "migration apply in timestamp order 000100→000700"
      pattern: "rpa_records"
---

<objective>
Wave 2 — [BLOCKING] aplicar as 7 migrations da Fase 16 ao banco Supabase ao vivo e regenerar os tipos TypeScript. Sem isto, build/tsc passaria em tipos obsoletos e a verificação seria um falso-positivo (per schema_push_requirement). Todas as Server Actions/UI das Waves 3/4 dependem das tabelas ao vivo + tipos regenerados. Espelha 14-03 e 15-04.

Purpose: tornar o schema real e tipado antes de qualquer action/UI.
Output: migrations aplicadas + src/lib/database.types.ts regenerado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Verify Supabase login is the correct account</name>
  <files>(none — login verification, no file changes)</files>
  <read_first>
    - Project memory: project_fynxia_supabase_account.md (project jqjwyqlbbuqnrffdnlpp, org kczvihafddupruvsrrsc, sa-east-1; gotcha: CLI often logged into nexus-* account → re-login before db push)
  </read_first>
  <what-built>Seven migrations staged: 20260621000100..000700 (payables, reconciliation, payout/rpa, tax, alters, rls, seed). Before push, CLI login must point at the org owning jqjwyqlbbuqnrffdnlpp (NOT a nexus-* account).</what-built>
  <action>
HUMAN CHECKPOINT — do not push until confirmed:
  1. Run `supabase projects list` and confirm `jqjwyqlbbuqnrffdnlpp` appears.
  2. If it does NOT appear (CLI logged into nexus-*), run `supabase login` and authenticate with the FYNXIA account, OR export `SUPABASE_ACCESS_TOKEN` for non-TTY.
  3. Confirm linked: `supabase link --project-ref jqjwyqlbbuqnrffdnlpp` if not already.
This guards against pushing schema to the wrong project (project memory gotcha).
  </action>
  <how-to-verify>
    1. `supabase projects list` shows jqjwyqlbbuqnrffdnlpp.
    2. If absent, `supabase login` / export SUPABASE_ACCESS_TOKEN.
    3. `supabase link --project-ref jqjwyqlbbuqnrffdnlpp` if needed.
  </how-to-verify>
  <resume-signal>Type "logged in" once `supabase projects list` shows jqjwyqlbbuqnrffdnlpp.</resume-signal>
  <verify>Human confirms `supabase projects list` output includes jqjwyqlbbuqnrffdnlpp.</verify>
  <acceptance_criteria>
    - `supabase projects list` output includes jqjwyqlbbuqnrffdnlpp
  </acceptance_criteria>
  <done>CLI authenticated to the FYNXIA account; jqjwyqlbbuqnrffdnlpp linked and visible.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push migrations + regenerate types</name>
  <files>src/lib/database.types.ts</files>
  <read_first>
    - supabase/migrations/20260621000100_payables_tables.sql
    - supabase/migrations/20260621000200_reconciliation_tables.sql
    - supabase/migrations/20260621000300_payout_rpa_tables.sql
    - supabase/migrations/20260621000400_tax_tables.sql
    - supabase/migrations/20260621000500_phase16_alters.sql
    - supabase/migrations/20260621000600_phase16_rls.sql
    - supabase/migrations/20260621000700_phase16_seed.sql
  </read_first>
  <action>
Run, from project root:
```
supabase db push
```
This applies the 7 staged migrations in timestamp order (000100 payables → 000200 reconciliation → 000300 payout/rpa → 000400 tax → 000500 alters → 000600 rls → 000700 seed). If push reports migrations partially applied, do NOT re-edit migration files — resolve with `supabase migration repair` only if the CLI explicitly instructs, then re-run push.

After a successful push, regenerate types:
```
supabase gen types typescript --project-id jqjwyqlbbuqnrffdnlpp --schema public > src/lib/database.types.ts
```
Truncation guard (established phase-9/10/11/14/15 pattern): if the generated file is smaller than the prior version or does NOT contain `payables` and `rpa_records`, the gen output was truncated — re-run the gen command. Confirm the file ends with a complete closing brace.

If `supabase` CLI is unavailable, fall back to Supabase MCP `apply_migration` per file in order, then MCP type generation; PREFER the CLI.
  </action>
  <verify>
    <automated>grep -c "payables\|statement_lines\|professional_payouts\|rpa_records\|reinf_events\|inss_tax_tables\|suppliers" src/lib/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "payables" src/lib/database.types.ts` ≥ 1
    - `grep -c "rpa_records" src/lib/database.types.ts` ≥ 1
    - `grep -c "statement_lines" src/lib/database.types.ts` ≥ 1
    - `grep "reconciliation_status" src/lib/database.types.ts` shows financial_transactions gained the column
    - `grep "saldo_atual" src/lib/database.types.ts` shows bank_accounts gained the column
    - database.types.ts is NOT truncated (ends with a closing `}`, ≥ prior file size)
    - `npx vitest run src/__tests__/financeiro16/migrations-phase16.test.ts` still GREEN (sanity)
  </acceptance_criteria>
  <done>7 migrations applied to live DB; database.types.ts regenerated with all 15 new tables + ALTERs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local CLI → production DB | Schema migration crosses into live tenant data store |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Tampering | wrong-account push | mitigate | Human checkpoint verifies `supabase projects list` shows jqjwyqlbbuqnrffdnlpp before push (project memory gotcha) |
| T-16-14 | Denial of Service | truncated gen types breaks build | mitigate | Truncation guard re-runs gen; verify file completeness + payables/rpa_records presence |
</threat_model>

<verification>
- database.types.ts contains all 15 new tables + reconciliation_status + saldo_atual.
- migrations-phase16 source-inspection still GREEN.
</verification>

<success_criteria>
Live schema matches migrations; types regenerated and complete; Wave 3 unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-05-SUMMARY.md`
</output>
