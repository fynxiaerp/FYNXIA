---
phase: 16
plan: 07
type: execute
wave: 3
depends_on: [16-04, 16-05]
files_modified:
  - src/app/api/financeiro/ofx/route.ts
  - src/actions/bank-statements.ts
  - src/actions/reconciliation.ts
autonomous: true
requirements: [FOP-02, FOP-03]
user_setup: []

must_haves:
  truths:
    - "POST /api/financeiro/ofx (runtime nodejs) recebe multipart, valida tamanho/ownership da bank_account, parseia via parseOfxBuffer e importa as linhas (FOP-02)"
    - "importOFX insere bank_statements + statement_lines com idempotência por (bank_account_id, fitid) ON CONFLICT DO NOTHING; sem FITID usa hash fallback (D-05/D-11)"
    - "runAutoReconciliation (Stage 1 exato ±0.01/±3 dias) seta reconciliation_status='conciliado' sem confirmação humana (D-06)"
    - "suggestMatches (Stage 2 fuzzy ranqueado) e confirmMatch + createReconciledTransaction (1 clique para linha sem par, D-07) cobrem o trabalho manual"
    - "matchNToOne (Stage 3 lote Asaas, taxa→despesa, tolerância R$5, D-09) + reconcileLoteConvenio com glosa parcial (D-10) + cashFlowPrevistoVsRealizado (D-08/FOP-03)"
  artifacts:
    - path: "src/app/api/financeiro/ofx/route.ts"
      provides: "POST handler nodejs runtime: multipart→Buffer→parseOfxBuffer→importOFX"
      exports: ["POST", "runtime"]
    - path: "src/actions/bank-statements.ts"
      provides: "importOFX, listStatementLines"
      exports: ["importOFX", "listStatementLines"]
    - path: "src/actions/reconciliation.ts"
      provides: "runAutoReconciliation, suggestMatches, confirmMatch, matchNToOne, createReconciledTransaction, reconcileLoteConvenio, cashFlowPrevistoVsRealizado"
      exports: ["runAutoReconciliation", "suggestMatches", "confirmMatch", "createReconciledTransaction", "cashFlowPrevistoVsRealizado"]
  key_links:
    - from: "importOFX"
      to: "statement_lines upsert idempotente"
      via: "insert ON CONFLICT (bank_account_id, fitid) DO NOTHING"
      pattern: "onConflict|fitid"
    - from: "runAutoReconciliation"
      to: "matchExact (src/lib/financeiro/reconciliation)"
      via: "import + reconciliation_status='conciliado'"
      pattern: "matchExact|'conciliado'"
    - from: "matchNToOne action"
      to: "financial_transactions taxa type='despesa'"
      via: "fee→lançamento despesa (Pitfall 5)"
      pattern: "type: 'despesa'"
---

<objective>
Wave 3 — construir a importação de extrato (OFX upload real) e o motor de conciliação em 3 estágios. A rota nodejs recebe o arquivo OFX, valida ownership da conta e delega o parse à lib pura `parseOfxBuffer` (16-04); `bank-statements.ts` persiste as linhas de forma idempotente por FITID (D-11); `reconciliation.ts` orquestra Stage 1 exato (auto), Stage 2 fuzzy (usuário confirma), Stage 3 N:1 (lote Asaas com taxa→despesa) + baixa/glosa de lote convênio (D-10) + a consulta previsto-vs-realizado (FOP-03). Toda a matemática de matching já está pronta e testada em `src/lib/financeiro/reconciliation.ts` — aqui só orquestramos com I/O ao Supabase. Espelha exatamente os padrões de `src/actions/receivables.ts` (getActor) e `src/actions/service-orders.ts` (RLS via createClient, zod parse, audit, gates).

Purpose: FOP-02 (importar extrato + conciliar) e FOP-03 (fluxo de caixa previsto vs realizado).
Output: 1 route handler nodejs + 2 arquivos de Server Action.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-RESEARCH.md
@.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-CONTEXT.md

<interfaces>
Mirror EXACTLY: src/actions/receivables.ts (getActor helper, copiar verbatim no topo); src/actions/service-orders.ts (zod safeParse → friendly first error → role gate → mutation → logBusinessEvent → revalidatePath; padrão de DI opcional para testes).

Libs PURAS prontas e testadas (Plan 04 — APENAS importar e orquestrar, NÃO reimplementar):
  src/lib/financeiro/reconciliation.ts:
    - matchExact(line, candidates, dateTolerance=3, amountTolerance=0.01): ExactMatch|null
    - matchFuzzy(line, candidates): ScoredMatch[]  // já ordenado desc, score≥0.5, confidence 'high'≥0.85
    - matchNToOne(depositLine, candidates, tolerance=5.00): NToOneMatch|null  // { transaction_ids, fee }
    Tipos: StatementLineInput { id, amount, transaction_date, memo, bank_account_id }, TransactionRow { id, amount, transaction_date, description, bank_account_id }, ScoredMatch { transaction_id, score, confidence }, NToOneMatch { transaction_ids: string[], fee: number }
  src/lib/financeiro/ofx-parser.ts:
    - parseOfxBuffer(buffer: Buffer): Promise<{ account_id?: string; lines: StatementLine[]; warnings: string[] }>
    - StatementLine { fitid: string; date: Date; amount: number; memo: string; check_number?: string }

Schema vivo após 16-05 (16-RESEARCH §"Schema Design"):
  - public.bank_statements (id, clinic_id, bank_account_id, fonte CHECK('ofx','open_finance') DEFAULT 'ofx', periodo_inicio DATE, periodo_fim DATE, filename, imported_by)
  - public.statement_lines (id, clinic_id, bank_account_id, bank_statement_id, fitid TEXT NULL, fitid_fallback TEXT NULL, transaction_date DATE, amount NUMERIC(12,2) [+=crédito, −=débito], memo, check_number, reconciliation_status CHECK('pendente','conciliado','ignorado') DEFAULT 'pendente', matched_transaction_ids UUID[], fee_transaction_id) — UNIQUE(bank_account_id, fitid) + UNIQUE(bank_account_id, fitid_fallback)
  - public.financial_transactions (Fase 3/14 + colunas novas reconciliation_status DEFAULT 'pendente', statement_line_id). Inserir lançamento com: tenant_id, type 'receita'|'despesa', account_id, cost_center_id, bank_account_id, amount, transaction_date, description, reconciliation_status. CONFIRMAR nome exato das colunas lendo src/actions/transactions.ts (Fase 3 usa tenant_id).
  - public.bank_accounts (existing + saldo_atual NUMERIC(12,2) DEFAULT 0)
  - lote/recebível de convênio (Fase 15: insurer_receivables / tiss lote) — alvo de reconcileLoteConvenio (D-10); confirmar nome da tabela lendo a migration faturamento_tiss_tables.

Hash fallback FITID (D-11): quando parseOfxBuffer retorna fitid vazio/ausente, computar SHA-256 de `${bank_account_id}|${YYYY-MM-DD}|${amount}|${memo}` (use node:crypto createHash('sha256')) e gravar em fitid_fallback (fitid fica NULL). ON CONFLICT no índice correspondente DO NOTHING.

Cron-auth / upload: rota usa runtime nodejs (OFX pode exceder limite Edge 1MB, RESEARCH Pitfall OFX). Limite de tamanho: rejeitar > 5MB.
Roles (D-23): writers = ['admin','superadmin']; auditor/socio readOnly. Gate inline em cada write/import; rejeitar não-writer com erro citando 'permissão'. CANONICAL ROLE SET: o sistema NAO possui papel 'financeiro' — o enum (users_role_check, 20260614000400_role_expansion.sql) e ['admin','dentist','receptionist','patient','superadmin','dpo','auditor','socio','ti','implantacao','aluno']. A intencao 'escrita admin/financeiro' do D-23 e satisfeita por admin/superadmin (escritores financeiros canonicos), igual a RLS da Fase 15 (20260620000400_faturamento_rls.sql usa get_my_role() IN ('admin','superadmin')). Os gates de action usam o MESMO conjunto da RLS de 16-03 (['admin','superadmin']) para DB e app concordarem.
Audit: logBusinessEvent from '@/lib/audit' nas mutações de conciliação/estorno (mesmo uso de service-orders.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OFX route (nodejs) + bank-statements.ts — upload, parse, importação idempotente</name>
  <files>src/app/api/financeiro/ofx/route.ts, src/actions/bank-statements.ts</files>
  <read_first>
    - src/app/api/cron/reminder-dispatch/route.ts (idioma `export const runtime = 'nodejs'` no topo + auth gate; espelhar o cabeçalho de runtime)
    - src/app/api/ocr/route.ts (handler de upload multipart real: request.formData() → File → arrayBuffer → Buffer; auth via createClient; padrão de resposta NextResponse.json)
    - src/lib/financeiro/ofx-parser.ts (parseOfxBuffer — APENAS chamar; assinatura acima)
    - src/actions/receivables.ts (getActor verbatim)
    - src/actions/transactions.ts (confirmar nome EXATO das colunas tenant_id/type/account_id/cost_center_id/bank_account_id/amount/transaction_date para o insert futuro de lançamentos)
    - 16-RESEARCH.md §"Padrão 3 OFX Import com Idempotência" (linhas 285-327)
    - CLAUDE.md (runtime nodejs em rotas que tocam DB; RLS via @supabase/ssr; service role server-only)
  </read_first>
  <action>
**src/app/api/financeiro/ofx/route.ts**:
  - `export const runtime = 'nodejs'` e `export const maxDuration = 60` no topo (copiar idioma de reminder-dispatch).
  - `export async function POST(request: Request)`:
    1. Auth: `const supabase = await createClient()`; `auth.getUser()`; se !user → `NextResponse.json({ error: 'Não autenticado' }, { status: 401 })`.
    2. Role gate: buscar role do usuário em `users`; se não estiver em ['admin','superadmin'] → 403 `{ error: 'Sem permissão para importar extrato' }`.
    3. `const form = await request.formData()`; `const file = form.get('file') as File | null`; `const bankAccountId = String(form.get('bankAccountId') ?? '')`.
    4. Validar: file presente, bankAccountId UUID; `file.size <= 5 * 1024 * 1024` (5MB) senão 413 `{ error: 'Arquivo OFX excede 5MB' }`.
    5. Ownership: SELECT id FROM bank_accounts WHERE id=bankAccountId (RLS escopa o tenant); se não achar → 404 `{ error: 'Conta bancária não encontrada' }`.
    6. `const buffer = Buffer.from(await file.arrayBuffer())`.
    7. Chamar a action `importOFX({ bankAccountId, filename: file.name, buffer })` (importar de '@/actions/bank-statements'); retornar `NextResponse.json(result, { status: result.success ? 200 : 400 })`.
  - NUNCA parsear OFX com regex aqui; toda a extração é da lib.

**src/actions/bank-statements.ts** (`'use server'` + `import 'server-only'`, getActor, writer gate ['admin','superadmin']):
  - import { parseOfxBuffer } from '@/lib/financeiro/ofx-parser'; import { createHash } from 'node:crypto'; logBusinessEvent.
  - `export async function importOFX({ bankAccountId, filename, buffer }: { bankAccountId: string; filename: string; buffer: Buffer }): Promise<{ success: boolean; statementId?: string; imported?: number; skipped?: number; warnings?: string[]; error?: string }>`:
    1. getActor → writer gate. supabase = await createClient().
    2. Re-validar ownership de bankAccountId (RLS).
    3. `const { lines, warnings } = await parseOfxBuffer(buffer)`. Se lines vazio → { success:false, error:'Nenhuma transação no OFX' }.
    4. Calcular periodo_inicio/periodo_fim = min/max das line.date (ISO YYYY-MM-DD).
    5. Insert bank_statements (clinic_id=actor.tenant_id, bank_account_id, fonte:'ofx', periodo_inicio, periodo_fim, filename, imported_by=actor.id) → capturar statementId.
    6. Para cada linha montar a row: clinic_id=actor.tenant_id, bank_account_id, bank_statement_id=statementId, transaction_date=line.date.toISOString().slice(0,10), amount=line.amount, memo=line.memo, check_number=line.check_number ?? null, reconciliation_status:'pendente'. Se line.fitid truthy → fitid=line.fitid, fitid_fallback=null; senão → fitid=null, fitid_fallback = createHash('sha256').update(`${bankAccountId}|${date}|${line.amount}|${line.memo}`).digest('hex').
    7. Inserir as linhas com idempotência: para linhas com fitid → `.upsert(rowsComFitid, { onConflict: 'bank_account_id,fitid', ignoreDuplicates: true })`; para linhas com fallback → `.upsert(rowsComFallback, { onConflict: 'bank_account_id,fitid_fallback', ignoreDuplicates: true })`. imported = nº de rows retornadas; skipped = total − imported.
    8. logBusinessEvent (import_ofx, statementId, imported/skipped). revalidatePath('/clinica/financeiro/conciliacao'). Return { success:true, statementId, imported, skipped, warnings }.
  - `export async function listStatementLines(filters: { bankAccountId?: string; status?: string; from?: string; to?: string }): Promise<{ success: boolean; lines?: unknown[]; error?: string }>`: getActor; select id, bank_account_id, transaction_date, amount, memo, check_number, reconciliation_status, matched_transaction_ids from statement_lines (RLS escopa tenant); aplicar filtros opcionais; order by transaction_date desc.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "ofx/route|bank-statements" | head && grep -E "runtime = 'nodejs'" src/app/api/financeiro/ofx/route.ts && grep -E "onConflict.*fitid" src/actions/bank-statements.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const runtime = 'nodejs'" src/app/api/financeiro/ofx/route.ts` presente
    - route valida file.size limit (grep para `5 * 1024 * 1024` ou `5242880`) e ownership da bank_account
    - route faz role gate (grep -E "admin|permissão|403")
    - bank-statements.ts exporta importOFX e listStatementLines
    - importOFX chama parseOfxBuffer e faz upsert com onConflict em 'bank_account_id,fitid' E fallback 'bank_account_id,fitid_fallback' (grep ambos)
    - `grep "createHash" src/actions/bank-statements.ts` presente (hash fallback FITID)
    - `grep "actor.tenant_id" src/actions/bank-statements.ts` presente; clinic_id nunca do input
    - `grep -c "import 'server-only'" src/actions/bank-statements.ts` retorna 1
    - tsc limpo para ambos os arquivos
  </acceptance_criteria>
  <done>Upload OFX nodejs com size-limit + ownership + role gate; importação idempotente por FITID com hash fallback; listStatementLines tenant-scoped.</done>
</task>

<task type="auto">
  <name>Task 2: reconciliation.ts — Stage 1 exato, Stage 2 fuzzy, confirmMatch, 1-clique D-07, cashFlow previsto/realizado</name>
  <files>src/actions/reconciliation.ts</files>
  <read_first>
    - src/lib/financeiro/reconciliation.ts (matchExact, matchFuzzy — APENAS chamar; tipos StatementLineInput/TransactionRow/ScoredMatch acima)
    - src/actions/receivables.ts (getActor)
    - src/actions/transactions.ts (insert shape de financial_transactions — colunas exatas; reusar para createReconciledTransaction)
    - src/lib/audit (logBusinessEvent)
    - 16-RESEARCH.md §"Padrão 4 Algoritmo de Conciliação em 3 Estágios" (linhas 329-369) + §"Padrão 5 Status financial_transactions" (linhas 371-386)
    - CLAUDE.md (RLS; writer gate)
  </read_first>
  <action>
`'use server'` + `import 'server-only'`. getActor. Writer gate ['admin','superadmin'] em todas as mutações.

  - `export async function runAutoReconciliation(bankAccountId: string): Promise<{ success: boolean; conciliated?: number; error?: string }>` (Stage 1 — D-06):
    1. getActor → writer gate.
    2. SELECT statement_lines pendentes (reconciliation_status='pendente') do bankAccountId → mapear para StatementLineInput.
    3. SELECT financial_transactions do mesmo bank_account_id com reconciliation_status='pendente' → mapear para TransactionRow (id, amount, transaction_date, description, bank_account_id).
    4. Para cada linha: `const m = matchExact(line, candidates)` (default ±3 dias / ±0.01); se m não-null E o transaction ainda não foi casado nesta passada:
       - UPDATE statement_lines SET reconciliation_status='conciliado', matched_transaction_ids=[m.transaction_id] WHERE id=line.id AND reconciliation_status='pendente' (CAS).
       - UPDATE financial_transactions SET reconciliation_status='conciliado', statement_line_id=line.id WHERE id=m.transaction_id AND reconciliation_status='pendente' (CAS — evita reuso do mesmo lançamento).
       - Remover o candidato casado da lista para a próxima linha. Incrementar conciliated.
    5. logBusinessEvent. revalidatePath. Return { success:true, conciliated }.

  - `export async function suggestMatches(statementLineId: string): Promise<{ success: boolean; suggestions?: ScoredMatch[]; error?: string }>` (Stage 2): getActor; carregar a linha + candidatos pendentes do mesmo bank_account_id; `return { success:true, suggestions: matchFuzzy(line, candidates) }` (lib já ordena desc / filtra ≥0.5 / marca confidence). NÃO concilia — só sugere.

  - `export async function confirmMatch(statementLineId: string, transactionId: string): Promise<{ success: boolean; error?: string }>` (usuário confirma sugestão): writer gate; CAS UPDATE da linha (status 'pendente'→'conciliado', matched_transaction_ids=[transactionId]) + CAS UPDATE do financial_transaction (status→'conciliado', statement_line_id). Se 0 rows → { success:false, error:'Conciliação concorrente detectada' }. logBusinessEvent. revalidatePath.

  - `export async function createReconciledTransaction(input: { statementLineId: string; accountId: string; costCenterId: string; description?: string }): Promise<{ success: boolean; transactionId?: string; error?: string }>` (D-07 — 1 clique para linha sem par): writer gate; re-fetch a linha (amount, transaction_date, bank_account_id, memo). type derivado do sinal: amount >= 0 → 'receita', amount < 0 → 'despesa' (Pitfall 5); amount no lançamento = Math.abs(amount) ou o valor assinado conforme convenção de transactions.ts (CONFIRMAR no insert existente). Insert financial_transactions { tenant_id:actor.tenant_id, type, account_id:accountId, cost_center_id:costCenterId, bank_account_id:line.bank_account_id, amount, transaction_date:line.transaction_date, description: input.description ?? line.memo, reconciliation_status:'conciliado', statement_line_id:line.id }. UPDATE statement_lines SET reconciliation_status='conciliado', matched_transaction_ids=[novoId]. logBusinessEvent. Return { success:true, transactionId }.

  - `export async function cashFlowPrevistoVsRealizado(filters: { from?: string; to?: string; unitId?: string }): Promise<{ success: boolean; previsto?: { entradas: number; saidas: number }; realizado?: { entradas: number; saidas: number }; baixadoNaoConciliado?: { entradas: number; saidas: number }; error?: string }>` (FOP-03 / D-08 — TRES status): getActor; uma query em financial_transactions (RLS escopa tenant) trazendo reconciliation_status + type + amount, com os filtros de data/unit, agregando por status integer-cent ('receita'→entradas, 'despesa'→saidas):
    - `pendente` → bucket **previsto** (CP/CR em aberto; ainda nao impactou o saldo bancario).
    - `baixado` → CP pago / saldo bancario JA debitado (baixarPayable cria o FT com reconciliation_status='baixado', 16-06) mas a linha do extrato ainda NAO foi casada: conta como **realizado-by-cash** (impacta o saldo) E TAMBEM no bucket `baixadoNaoConciliado` (flag de pendencia de conciliacao). Ou seja: realizado.entradas/saidas SOMA pendente=NAO + (baixado + conciliado); baixadoNaoConciliado isola apenas os 'baixado'.
    - `conciliado` → **realizado-confirmado** (casado com o extrato).
    Definicao final dos buckets retornados: previsto = soma dos 'pendente'; realizado = soma de ('baixado' + 'conciliado') — o dinheiro que ja saiu/entrou no caixa; baixadoNaoConciliado = soma apenas dos 'baixado' (subconjunto do realizado, sinalizado na UI como "realizado mas nao conciliado"). NUNCA omitir 'baixado': se for ignorado, o realizado fica subestimado e o saldo nao bate. Aplicar filtros de data/unit. Return os tres agregados.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "actions/reconciliation" | head && grep -E "matchExact|matchFuzzy" src/actions/reconciliation.ts && grep -E "reconciliation_status.*'conciliado'|'conciliado'" src/actions/reconciliation.ts</automated>
  </verify>
  <acceptance_criteria>
    - reconciliation.ts exporta runAutoReconciliation, suggestMatches, confirmMatch, createReconciledTransaction, cashFlowPrevistoVsRealizado
    - runAutoReconciliation importa matchExact e seta reconciliation_status='conciliado' com CAS (.eq('reconciliation_status','pendente'))
    - suggestMatches importa matchFuzzy e NÃO escreve no DB (somente SELECT)
    - createReconciledTransaction deriva type do sinal do amount (grep -E "type.*despesa|amount < 0|amount >= 0")
    - cashFlowPrevistoVsRealizado trata os TRES status (D-08): previsto='pendente', realizado=('baixado'+'conciliado'), e expoe baixadoNaoConciliado (apenas 'baixado') (grep 'baixado' E 'baixadoNaoConciliado' E 'conciliado' em src/actions/reconciliation.ts)
    - `grep "actor.tenant_id" src/actions/reconciliation.ts` presente
    - `grep -c "import 'server-only'" src/actions/reconciliation.ts` retorna 1
    - tsc limpo
  </acceptance_criteria>
  <done>Pipeline exato→fuzzy→confirm + 1-clique D-07 + previsto/realizado FOP-03; tudo tenant-scoped, writer-gated, com CAS contra dupla conciliação.</done>
</task>

<task type="auto">
  <name>Task 3: reconciliation.ts (cont.) — Stage 3 N:1 lote Asaas (taxa→despesa) + baixa/glosa lote convênio</name>
  <files>src/actions/reconciliation.ts</files>
  <read_first>
    - src/lib/financeiro/reconciliation.ts (matchNToOne — APENAS chamar; retorna { transaction_ids, fee })
    - src/actions/transactions.ts (insert da taxa como financial_transaction type='despesa')
    - supabase/migrations/20260620000300_faturamento_tiss_tables.sql (nome real da tabela de recebível/lote de convênio para reconcileLoteConvenio — D-10)
    - src/actions/service-orders.ts (padrão de baixa idempotente/CAS + alçada de Fase 15 para reuso da glosa por item)
    - 16-RESEARCH.md §"Padrão 4 Stage 3" (linhas 354-369) + §"Common Pitfalls" Pitfall 5 (taxa = despesa) + CONTEXT D-09/D-10
  </read_first>
  <action>
ORDEM INTRA-PLAN: a Task 2 cria src/actions/reconciliation.ts; esta Task 3 APPENDA (matchNToOne/reconcileLoteConvenio) ao MESMO arquivo — portanto a Task 2 roda PRIMEIRO. Adicionar ao mesmo arquivo src/actions/reconciliation.ts (mesma estrutura: getActor + writer gate ['admin','superadmin'] + audit):

  - `export async function matchNToOne(input: { statementLineId: string; feeAccountId?: string; feeCostCenterId?: string; tolerance?: number }): Promise<{ success: boolean; matched?: string[]; fee?: number; feeTransactionId?: string; error?: string }>` (D-09 — lote Asaas):
    1. writer gate. Re-fetch a depositLine (amount positivo = crédito do lote). Mapear para StatementLineInput.
    2. SELECT candidatos: financial_transactions type='receita', reconciliation_status='pendente', mesmo bank_account_id → TransactionRow[].
    3. `const result = matchNToOne(depositLine, candidates, input.tolerance ?? 5.00)`; se null → { success:false, error:'Nenhuma combinação dentro da tolerância' }.
    4. CAS UPDATE de cada financial_transaction em result.transaction_ids → reconciliation_status='conciliado', statement_line_id (apenas as que ainda estão 'pendente'). UPDATE statement_lines SET reconciliation_status='conciliado', matched_transaction_ids=result.transaction_ids.
    5. Se result.fee > 0.005 (taxa de cartão/banco — Pitfall 5): insert UM financial_transactions { tenant_id, type:'despesa', account_id: input.feeAccountId, cost_center_id: input.feeCostCenterId, bank_account_id, amount: result.fee, transaction_date: depositLine.transaction_date, description:'Taxa de lote Asaas', reconciliation_status:'conciliado' } → feeTransactionId; UPDATE statement_lines SET fee_transaction_id=feeTransactionId. (a taxa NUNCA é receita).
    6. logBusinessEvent (n_to_one, statementLineId, matched count, fee). revalidatePath. Return { success:true, matched: result.transaction_ids, fee: result.fee, feeTransactionId }.

  - `export async function reconcileLoteConvenio(input: { statementLineId: string; loteId: string; itens: Array<{ receivableId: string; valorAutorizado: number; glosado?: boolean }> }): Promise<{ success: boolean; baixados?: number; glosados?: number; error?: string }>` (D-10 — baixa de lote convênio com glosa parcial item a item):
    1. writer gate. Re-fetch a statement_line (crédito da operadora) + o lote (tabela confirmada na migration faturamento_tiss).
    2. Para cada item: se glosado → NÃO baixa (mantém o recebível em aberto/recurso, incrementa glosados); senão → baixa o recebível pelo valorAutorizado (UPDATE status do recebível para 'pago'/'baixado' com CAS contra dupla baixa), incrementa baixados. (item a item, coerente com a glosa da Fase 15 D-28).
    3. UPDATE statement_lines SET reconciliation_status='conciliado', matched_transaction_ids = (ids dos lançamentos gerados/casados). logBusinessEvent. revalidatePath. Return { success:true, baixados, glosados }.
    Se a tabela exata de recebível de convênio não existir com o nome esperado, ler a migration faturamento_tiss e usar o nome real; manter a lógica item-a-item.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "actions/reconciliation" | head && grep -E "matchNToOne" src/actions/reconciliation.ts && grep -E "type: 'despesa'" src/actions/reconciliation.ts</automated>
  </verify>
  <acceptance_criteria>
    - reconciliation.ts exporta matchNToOne e reconcileLoteConvenio (além dos da Task 2)
    - matchNToOne importa a lib matchNToOne e insere a taxa como type='despesa' (grep `type: 'despesa'`), nunca receita
    - matchNToOne usa tolerância default 5.00 e CAS nos lançamentos casados
    - reconcileLoteConvenio trata glosa parcial item a item (item glosado NÃO é baixado — grep `glosado`)
    - `grep -c "import 'server-only'" src/actions/reconciliation.ts` retorna 1
    - tsc limpo
  </acceptance_criteria>
  <done>Stage 3 N:1 do lote Asaas com taxa→despesa + baixa/glosa parcial de lote convênio; idempotente via CAS; tudo auditado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| upload OFX → route handler | Arquivo não confiável (tamanho, conteúdo) cruza para o servidor |
| route → bank_account ownership | A conta-alvo deve pertencer ao tenant do usuário |
| reimportação OFX → statement_lines | Reupload não pode duplicar linhas |
| confirmMatch/matchNToOne → financial side-effect | Conciliação concorrente não pode casar o mesmo lançamento duas vezes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-30 | Denial of Service | OFX gigante satura a função | mitigate | route rejeita file.size > 5MB (413) antes de bufferizar; runtime nodejs com maxDuration 60 |
| T-16-31 | Spoofing | importar extrato para bank_account de outro tenant | mitigate | ownership check da bank_account via RLS + clinic_id=actor.tenant_id; nunca do input |
| T-16-32 | Tampering | reimportação duplica linhas do extrato | mitigate | upsert ON CONFLICT (bank_account_id,fitid)/(bank_account_id,fitid_fallback) DO NOTHING (D-11) |
| T-16-33 | Elevation of Privilege | auditor/socio importa ou concilia | mitigate | writer gate ['admin','superadmin'] em route + toda action |
| T-16-34 | Tampering | dupla conciliação casa o mesmo lançamento 2× | mitigate | CAS `.eq('reconciliation_status','pendente')` no statement_line E no financial_transaction antes de marcar 'conciliado' |
| T-16-35 | Tampering | taxa de lote lançada como receita (sinal invertido) | mitigate | fee sempre type='despesa' (Pitfall 5); sinal do amount define receita/despesa em createReconciledTransaction |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo para ofx/route, bank-statements, reconciliation.
- Manual: POST /api/financeiro/ofx com OFX de 3 linhas → 3 statement_lines; reupload → 0 novas (skipped=3). runAutoReconciliation casa as exatas. cashFlowPrevistoVsRealizado retorna previsto≠realizado.
</verification>

<success_criteria>
FOP-02 entregue: upload OFX real + importação idempotente por FITID + conciliação 3 estágios (exato auto / fuzzy confirma / N:1 lote com taxa→despesa) + baixa/glosa de lote convênio. FOP-03 entregue: fluxo de caixa previsto vs realizado por status de conciliação.
</success_criteria>

<output>
After completion, create `.planning/phases/16-contas-a-pagar-concilia-o-tributos/16-07-SUMMARY.md`
</output>
